# NoraMedi (DisKlinikCRM) — KVKK Compliance Audit & Remediation Tracker

**Report date:** 2026-07-15
**Last updated:** 2026-07-21 (**KVKK-HIGH-008-F1-PBV-S1 — Safe Behavioral Production Verification Feasibility Review, documentation-only, read-only repository analysis.** The KVKK-HIGH-008 row below still reads "no `COMMUNICATION_CONSENT_*` gate exists and none was added" — that is now **superseded**: [PR #186](https://github.com/MustafaBasol/DisKlinikCRM/pull/186) (`MERGED`, merge commit `1a82302861139441f07ff5c0209728d3e711728f`) added a dedicated `PlatformSetting`-backed runtime kill switch (`privacy.legacyConsentCorrection.runtimeEnabled`, default-disabled), and per operator-supplied, read-only production evidence (see `docs/program/KVKK_HIGH008_FREEZE_BOUNDARY.md` §8) that gate is now deployed to production with the setting row **absent** (effective fail-closed `false`). This task itself performed a read-only source-code confirmation that the gate check on the mutation route executes after patient lookup (so it cannot be exercised with a nonexistent patient ID) and that no unset/delete API exists for the underlying `PlatformSetting` (so an explicit-false PATCH that would close the remaining audit-verification gaps is technically feasible but **not authorized**, being an irreversible state change). `docs/program/RISK_REGISTER.md` R-061 remains `OPEN`. No production access, no code change, no test run by this task. Full evidence: `docs/program/evidence/KVKK-HIGH-008-F1-PBV-S1_SAFE_BEHAVIORAL_PRODUCTION_VERIFICATION_FEASIBILITY.md`. Separately, **KVKK-HIGH-006-S2** (occurrence classification, KVKK-HIGH-006 row below) is confirmed `MERGED` via [PR #191](https://github.com/MustafaBasol/DisKlinikCRM/pull/191) — the row's own "not merged" wording predates this merge; KVKK-HIGH-006 itself remains `STILL_OPEN`, zero of the 47 occurrences fixed.)
**Prior update:** 2026-07-20 (**production deployment record correction:** KVKK-HIGH-007 follow-up (PR #175) and KVKK-HIGH-008 (PR #180) are both confirmed `MERGED` into `main`, superseding "not yet merged"/"open, not merged" language below. **New, read-only production evidence (2026-07-20):** KVKK-HIGH-008's migration (`20260719155318_kvkk_high008_legacy_consent_correction`) is confirmed **applied to production** — 64/64 migrations present, `prisma migrate status` reports the schema up to date, which also confirms KVKK-HIGH-007's own migration (`20260719120821_kvkk_high007_consent_reconciliation`) is applied; the `PatientLegacyConsentCorrection` table and `PatientLegacyConsentField` enum exist in production with **0** correction rows; all `COMMUNICATION_CONSENT_*` flags remain confirmed disabled and no reconciliation backfill has been run. This is a **technical deployment verification only** — it is not a legal KVKK compliance determination, no correction record has been created via this workflow, and independent rollback/tenant-impact verification has not been performed for either migration. See the 2026-07-20 row in Section 8, updated Sections 1/2/5/6 below, and `docs/program/KVKK_HIGH008_FREEZE_BOUNDARY.md` §6.)
**Current Git commit / branch:** production `HEAD` `87b7dcd9bfe5d27956e32e8b61590667e36bac86` (F0-011/PR #182), confirmed via 2026-07-20 production evidence pass; PR #175 (KVKK-HIGH-007 follow-up, commit `1da9586`) and PR #180 (KVKK-HIGH-008, commit `e972bfe`) are both ancestors and both `MERGED` — recorded 2026-07-20, update this line whenever the document changes
**Audit scope:** NoraMedi/DisKlinikCRM platform's compliance with Law No. 6698 (KVKK). Based on source-code review plus verification against official Turkish sources (kvkk.gov.tr, Official Gazette references). No access to Google/Meta contracts, VPS/backup infrastructure, or clinic-level legal documents — those items are explicitly marked "not verified" below and require separate production/legal verification.
**Legal disclaimer:** This document is **not** a legal compliance certificate. No item in this document may be read as a declaration that the system is "fully KVKK compliant." Final legal determinations belong to Turkish legal counsel. Status labels here describe engineering/documentation state only, except where explicitly marked as legally approved with a recorded approval reference.

**Full narrative report (all matrices, correction logs, lawful-basis analysis, decision trees):**
`docs/compliance/archive/NoraMedi_KVKK_Denetim_Raporu_2026-07-15_v3_REVIZE_full.md` (this is the authoritative detailed narrative; this tracker is the authoritative status/progress view — the two are companions, not duplicates).
Original (superseded) first-pass report: `docs/compliance/archive/NoraMedi_KVKK_Denetim_Raporu_2026-07-15_v1_original.md`.

---

## 1. Current controlled-pilot status

**KOŞULLU KONTROLLÜ PİLOT (Conditional Controlled Pilot) — not yet cleared.** Two of the ten Phase 0 items (KVKK-CRIT-001a, KVKK-ATTACH-IMAGING-001) are now `Completed and verified` on their technical/engineering sub-item — merged into `main`, deployed to NoraMedi production, and manually verified in production (see Sections 6.1/6.2 and the evidence below). The remaining eight items are unchanged: three purely technical items are `Not started`, four are `Waiting for legal review`, and one (`HIGH-001`) is `Waiting for operational/infrastructure evidence`. Unlimited production use with real patient data is **still not recommended** until all Phase 0 items are either `Completed and verified`, `N/A` with documented justification, or their `[L]`/`[O]` dependencies are resolved by legal counsel / production/infrastructure verification. A limited single-clinic pilot with marketing features disabled is technically reasonable once Phase 0 closes; reaching two `Completed and verified` items does not by itself clear that bar.

**Important limitation on the two `Completed and verified` items (do not over-read these):**
- KVKK-CRIT-001a's `Completed and verified` status covers the *delivery/evidence* mechanism only — it proves the server issued and linked a notice-version record to the booking, not that any visitor read, understood, or consented to the notice, and it does **not** approve the legal wording of any clinic's own privacy notice (that remains a separate, still-outstanding legal dependency — see Section 3).
- KVKK-ATTACH-IMAGING-001's `Completed and verified` status covers the *technical lifecycle control* only — legal-hold, export package, anonymization redaction, and deletion-review dry-run are implemented, deployed, and verified in production. It does **not** mean medical-record retention periods are legally approved, that hard deletion of medical records is approved, that backup deletion/restore is resolved, or that infrastructure/application-level file encryption is settled — all of those remain explicit outstanding dependencies (Section 6.2).

This tracker is **not** a KVKK compliance certificate. Reaching `Completed and verified` on these two items does not automatically clear the system for unlimited production use, and outstanding Phase 0 technical, legal, and infrastructure controls remain. Legal dependencies cannot be completed based on code, and infrastructure controls cannot be completed based on repository inspection alone.

## 2. Current remediation progress (summary)

| Status | Count (Phase 0 items) |
|---|---|
| Completed and verified | 2 (KVKK-CRIT-001a, KVKK-ATTACH-IMAGING-001) |
| Merged — awaiting deployment and production verification | 2 (KVKK-HIGH-004, KVKK-CRIT-003) |
| In progress | 1 (KVKK-HIGH-007) |
| Not started | 0 |
| Blocked | 0 |
| Waiting for legal review | 4 (KVKK-CRIT-002, KVKK-CRIT-005, Platform↔clinic processor agreement, VERBİS decision tree) |
| Waiting for operational/infrastructure evidence | 1 (HIGH-001) |
| N/A | 0 |
| **Total Phase 0 items** | **10** |

Each Phase 0 item carries exactly one primary status above. Legal and operational dependencies that remain attached to an otherwise-technically-complete item (e.g. the legal-wording/retention/backup/encryption caveats on the two `Completed and verified` items above) are documented as notes on that item, not double-counted as separate Phase 0 rows.

**2026-07-20 note (counts not yet rebalanced — flagged, not silently fixed):** This 10-item count table predates KVKK-HIGH-008 and was never updated to include it as an 11th row (a documentation-completeness gap already identified by `docs/program/evidence/F0-011-P1_KVKK_HIGH008_ACTIVE_WORK_BASELINE.md` §2). KVKK-HIGH-008 is now merged and **deployed to production, technically verified** (migration applied, table/enum present with 0 rows, no feature flag — see Section 6 row below and `docs/program/KVKK_HIGH008_FREEZE_BOUNDARY.md` §6); it is not counted above. Separately, KVKK-HIGH-007 (counted above as "In progress") is now merged (PR #175) and its migration is confirmed production-applied — its row and status label in Section 6 below are updated accordingly, but this summary table's "In progress: 1" figure has not been mechanically recomputed in this pass to avoid introducing an unreviewed second inconsistency; treat Section 6 (the detailed table) as authoritative over this summary count until a dedicated pass rebalances it.

## 3. Outstanding legal dependencies

- International transfer mechanism selection for Google (Gemini) and Meta (WhatsApp/Instagram Cloud API) under Art. 9 (post-2024 regime) — contracts/DPAs not yet reviewed by counsel.
- Platform↔clinic data-processor agreement and sub-processor list — not yet drafted/approved.
- Lawful-basis matrix (Section 4 of the full report) — not yet approved by counsel.
- Per-clinic VERBİS decision tree outcome — not yet finalized per clinic, and depends on each clinic's own bookkeeping method (balance-sheet vs. non-balance-sheet, per Board Decision 2025/2393).
- Written breach-response plan content — not yet legally approved.

## 4. Outstanding production-verification controls

- VPS disk/volume encryption status.
- PostgreSQL storage-level encryption.
- Backup script (`noramedi-db-backup.sh`) encryption step.
- VPS provider (Hostinger) snapshot encryption settings.
- TLS/HSTS reverse-proxy configuration.
- `server/.env` file permissions and production `ENCRYPTION_KEY` fail-closed behavior.
- Key rotation policy.
- Primary legal texts of Board Decisions 2025/2393 (25.12.2025) and 2026/1026 (13.05.2026) — this tracker's dates were cross-verified via search summaries and one official kvkk.gov.tr page (Icerik/8577); the primary decision texts themselves were not directly viewed and should be pulled from kvkk.gov.tr before relying on them in a legal filing.

---

## 5. Living remediation checklist — Phase 0 (before first real patient data)

Status legend: `[ ]` Not started · `[-]` In progress · `[x]` Completed and verified · `[!]` Blocked · `[L]` Waiting for legal review · `[O]` Waiting for operational/infrastructure evidence · `[N/A]` Not applicable

- `[x]` KVKK-CRIT-001a — Public booking privacy-notice display and automatic notice-version evidence record (no consent/acknowledgment checkbox) — **Merged (PRs #156–#159), deployed to NoraMedi production, and production-verified** (technical/evidence sub-item only — see Section 6.1 for the still-outstanding legal-wording caveat)
- `[x]` KVKK-ATTACH-IMAGING-001 — Attachment/imaging physical-file lifecycle: legal hold, downloadable export package (streamed, bounded), anonymization metadata redaction, deletion-review dry-run only (no live-delete endpoint exists) — **Merged (PRs #160–#163), deployed to NoraMedi production, and production-verified** (see Section 6.2 / `docs/compliance/53-kvkk-attachment-imaging-lifecycle.md` for still-outstanding legal/infrastructure caveats)
- `[~]` KVKK-HIGH-007 — Normalized channel+purpose `PatientCommunicationPreference`/`PatientCommunicationConsentEvent` model + central `evaluateCommunicationPermission`/`assertCommunicationPermission` decision service, plus the legacy/central reconciliation follow-up — **Merged and deployed to production, technically verified (2026-07-20)** (see `docs/compliance/56-kvkk-communication-preference-and-consent-management.md`); base merged via PR #169, follow-up (reconciliation, audit-mode readiness, UX hardening) merged via PR #175; both migrations confirmed applied in production (`prisma migrate status` clean, 64/64 migrations); enforcement/reconciliation/audit-logging flags all confirmed still disabled by default in production, no backfill executed — technical control only, not legal approval, and not yet independently rollback/tenant-impact-verified
- `[L]` KVKK-CRIT-002 — International transfer inventory and mechanism decision (Google/Meta)
- `[~]` KVKK-CRIT-003 — Security incident response foundation (technical) + baseline security alerting — **Merged (PR #167) — awaiting deployment and production verification** (VPS currently inaccessible; see `docs/compliance/55-kvkk-security-incident-response-foundation.md`); breach-classification/notification legal plan remains a separate, still-outstanding legal dependency
- `[L]` KVKK-CRIT-005 — Lawful-basis matrix documentation + notice-evidence at patient intake
- `[O]` HIGH-001 (conditional) — Backup/disk/file-permission/download-authorization infrastructure verification (determines whether attachment encryption stays Phase 1)
- `[L]` Platform↔clinic data-processor agreement + sub-processor list
- `[~]` KVKK-HIGH-004 — Secure clinic bulk/structured-data export: legacy endpoint disabled (410), server-enforced feature flag (default false), OWNER/ORG_ADMIN-only + multi-branch scope, step-up auth, Postgres-authoritative rate limiting/concurrency, async worker generation, one-time hashed download token, audit/alerting — **Merged — awaiting deployment and production verification** (PR #165 merged to `main`; see `docs/compliance/54-kvkk-secure-clinic-bulk-export.md`)
- `[L]` Per-clinic VERBİS decision-tree outcome (bookkeeping-method-aware, per 2025/2393)
- `[~]` KVKK-HIGH-008 — Audited, management-only correction workflow for a stale legacy `Patient.smsOptOut=true` (new immutable `PatientLegacyConsentCorrection` model, never grants consent, never touches the central preference system) + consent-action-modal validation UX fix + `PatientDetail` responsive/accessible tab-overflow fix — **Merged and deployed to production, technically verified (2026-07-20)**, on branch `feature/kvkk-high008-legacy-consent-correction` (PR #180, merged). Migration `20260719155318_kvkk_high008_legacy_consent_correction` confirmed applied in production; `PatientLegacyConsentCorrection` table and `PatientLegacyConsentField` enum confirmed present with **0** correction rows — no correction has yet been created via this workflow in production. This workflow has no `COMMUNICATION_CONSENT_*` gate and is immediately active for OWNER/ORG_ADMIN/CLINIC_MANAGER now that it is deployed; it is NOT inert, and no human accept/reject decision on this no-flag design has been recorded (`docs/program/RISK_REGISTER.md` R-061, still open). All `COMMUNICATION_CONSENT_*` flags (the separate KVKK-HIGH-007 reconciliation/enforcement rollout) remain confirmed disabled by default in production; no production backfill executed. Deployment verification is technical only — it is not a legal KVKK compliance determination, and independent rollback/tenant-impact verification has not been performed (`docs/program/KVKK_ARCHITECTURE_FREEZE_BOUNDARY.md` §5 condition 4, unsatisfied). See `docs/compliance/56-kvkk-communication-preference-and-consent-management.md` §21.12.14 and `docs/program/KVKK_HIGH008_FREEZE_BOUNDARY.md` §6

The detailed remediation table (Section 6) is the source of truth for status — the checkbox markers above are a quick-glance view only, per the custom-marker limitation of standard Markdown.

---

## 6. Detailed remediation table

| ID | Remediation | Status | Evidence | Completed in | Verified by | Notes |
|---|---|---|---|---|---|---|
| KVKK-CRIT-001a | Public booking privacy notice display + automatic notice-version evidence record | Completed and verified (technical/evidence sub-item; legal-wording approval remains separate, see Section 3) | See Section 6.1 (implementation evidence) | PRs #156, #157, #158, #159, merged into `main` | Automated tests + real-browser acceptance (this session) + production deployment, production migration, and manual production browser verification with real clinic-specific published legal profiles (confirms correct clinic-specific controller/notice display, blocks booking when no published profile exists, creates/links notice evidence on successful booking, no consent/acknowledgment checkbox, no cross-clinic demo-data leak) | No consent/acknowledgment checkbox of any kind (see full report Section 10, item 1). New model `PublicBookingNoticeEvidence` reuses the existing `ClinicLegalProfile` publish/versioning architecture; does not touch `ChannelConsentLog`. Notice evidence proves server-side delivery and linkage only — not that the patient read, understood, or consented to the notice. Separate public-booking slot-consistency/recovery defects surfaced during production testing were fixed via PRs #157–#159 (non-KVKK; see Section 6.1) |
| KVKK-HIGH-007 | Normalized `PatientCommunicationPreference`/`PatientCommunicationConsentEvent` model (channel+purpose) + central `evaluateCommunicationPermission`/`assertCommunicationPermission` decision service + `smsOptOut` backfilled into `channel=sms` scope only | **Merged and deployed to production, technically verified (2026-07-20)** — see the 2026-07-20 change-history row for full production evidence (migration `20260719120821_kvkk_high007_consent_reconciliation` confirmed applied, flags confirmed disabled). Row text below is preserved as the original pre-merge record | `docs/compliance/56-kvkk-communication-preference-and-consent-management.md` | branch `feature/kvkk-high-007-communication-consent-management` (PR #169, merged) + follow-up (PR #175, merged) | Automated tests (author-reported, not independently re-executed) — production deployment/migration confirmed 2026-07-20; rollback/tenant-impact not independently verified; no legal verification | Technical control only, not legal approval of marketing-consent basis/wording/İYS. `smsOptOut` is NOT interpreted as blocking WhatsApp — the legacy backfill only ever writes `channel=sms` rows from that field, exactly per this row's original note. Enforcement (`COMMUNICATION_CONSENT_ENFORCEMENT_ENABLED`) ships `false`; no production behavior changes from this work alone. Remediated: scope now resolves before policy-exception purposes; WhatsApp consent context is mandatory (TS boundary) with mode-specific missing-context handling; preference history is now advisory-lock-serialized with a monotonic `revision` column (not `createdAt`) as the authoritative chain order; timestamp policy uses explicit `null` (not `undefined`) to clear stale fields; a notice-version/evidence matrix is now enforced for grants |
| KVKK-CRIT-002 | International transfer contracts, DPA review, Art. 9 mechanism selection (Google/Meta) | Waiting for legal review | Google/Meta contracts required | — | Legal counsel | Physical processing country not asserted; see full report Section 7 matrix |
| KVKK-CRIT-003 | Security incident response TECHNICAL foundation (SecurityIncident/SecuritySignalEvent models, ≥3 detection rules, Platform Admin lifecycle UI, dedup/aggregation) + baseline in-app alerting | Merged — awaiting deployment and production verification | `docs/compliance/55-kvkk-security-incident-response-foundation.md` | PR #167, merged to `main` (`368bcc8d0a9f4c0ea185ca33d4dd1193d8def9ef`) | — | Technical foundation only — does NOT constitute legal approval of breach classification, notification obligations/deadlines/recipients, legal wording, or regulator/data-subject communication; those remain a separate, still-outstanding legal dependency (written breach-response *plan* content, Section 3). Merged but NOT deployed to production and NOT production-verified — the VPS is currently inaccessible |
| KVKK-CRIT-005 | Lawful-basis matrix (per processing activity) approved + intake-time notice-evidence recording | Waiting for legal review | Matrix drafted in full report Section 4 | — | Legal counsel | Default basis is NOT explicit consent; Art. 6/3 (unlettered) is the health-data candidate condition |
| KVKK-HIGH-001 | Application-level attachment encryption (`fileStorage.ts`) | Waiting for operational evidence | — | — | — | Conditional: stays Phase 1 only if infra encryption checklist (Section 4 above) passes; otherwise becomes a Phase 0 blocker for real patient files |
| Processor agreement | Platform↔clinic data-processing agreement + sub-processor list | Waiting for legal review | — | — | Legal counsel | Agreement documents roles/instructions only; does not independently legalize processing |
| KVKK-HIGH-004 | Secure clinic bulk/structured-data export (legacy disable, feature flag, step-up, rate-limit, async worker, one-time token, audit/alert) | Merged — awaiting deployment and production verification | `docs/compliance/54-kvkk-secure-clinic-bulk-export.md` | PR #165, merged to `main` | — | Feature flag ships `false`; legacy `gdprExport.ts` GET now always returns 410 regardless of parameters; not yet enabled/exercised in production |
| VERBİS decision tree | Per-clinic VERBİS registration assessment, aware of each clinic's bookkeeping method | Waiting for legal review | Decision tree in full report Section 6 | — | Legal counsel / clinic accountant | Uses official 04.09.2025 (2025/1572) and 25.12.2025 (2025/2393) criteria |
| KVKK-HIGH-006 | KVKK-HIGH-006 tracks inconsistent direct use of the authenticated request's resolved clinic context, `req.user.clinicId`, in runtime authorization/data-scope paths instead of the centralized multi-clinic scope contracts (`validateAndGetClinicIdScope`/`validateAndGetScope`, `server/src/utils/clinicScope.ts:87,210`). | **`STILL_OPEN` / `READY_FOR_DETAILED_SCOPING`** — corrected 2026-07-20 evidence-tightening pass (see prior row text, preserved in change history below) and reconciled 2026-07-21 against a separate, narrower `defaultClinicId` verification that was mistakenly proposed as a closure candidate for this same task ID; see **KVKK-HIGH-006-S1** below. This row's own required conclusion wording is sharpened here to avoid overstating severity beyond what current evidence supports (see Notes column). | A reproducible search for `req\.user(!\|\?)?\.clinicId` across `server/src` (excluding `node_modules`/`dist`/`coverage`/generated Prisma client) found **93 occurrences across 23 files** as of 2026-07-20. Of these, the 2026-07-20 pass classified **47 as live runtime authorization/data-scope usages across 10 files** — `services.ts` (8), `postTreatment.ts` (7), `messages.ts` (7), `insuranceProvisions.ts` (6), `inventory.ts` (5), `paymentPlans.ts` (4), `dentalChart.ts` (3), `appointmentRequests.ts` (3), `reports.ts` (1, at `reports.ts:405`), `middleware/planLimits.ts` (2) — each using `req.user!.clinicId` directly instead of the centralized helper, which 6 of these 10 files already import and use correctly on *other* routes in the same file (an inconsistently-applied fix, not an absent one); **10 further occurrences** as safe UI-default/non-authorization usage (activity-log attribution in `organizationWhatsApp.ts`/`organizationBranches.ts`, display-locale formatting in `dashboard.ts`/`reports.ts`); **3** as dead/unreachable code (`planLimits.ts`); **1** requiring manual review (`reports.ts:73`); and the remaining **32** as comment/documentation/test-only. **Count-discrepancy note (2026-07-21 reconciliation):** an independent literal re-search of `req\.user(!\|\?)?\.clinicId` (excluding test files only, not the finer test/comment/dead-code sub-buckets above) found **64 non-test matches**, consistent with — not contradicting — the 93-occurrence/47-live-runtime classification above once the same file-by-file buckets are applied; the files this reconciliation initially flagged as "not named in the tracker" (`dashboard.ts`, `organizationBranches.ts`, `organizationWhatsApp.ts`) are in fact already accounted for in the "10 safe non-authorization usages" bucket. This reconciliation did **not** independently re-derive the full 93-item bucket-by-bucket classification line-by-line; that remains a **KVKK-HIGH-006-S2** task, not a completed re-verification. Do not treat "64" as a corrected count and do not treat "93/47" as newly independently re-verified without qualification — both figures stand, pending S2's occurrence-by-occurrence pass. **`reports.ts:405` (`/reports/no-show-analysis`):** the route uses the authenticated server-resolved `req.user.clinicId` directly and does not expose an explicit validated multi-clinic selector, unlike its four sibling routes in the same file. Current evidence indicates OWNER/ORG_ADMIN branch-selection/access-completeness narrowing rather than attacker-controlled cross-tenant access — `req.user.clinicId` is never client-supplied per request (see KVKK-HIGH-006-S1 §4 for the resolution chain). **`appointmentRequests.ts:192`:** the route uses server-resolved `req.user.clinicId` directly for lookup and PHI-bearing record creation (new `Patient`/`Appointment` rows). Current evidence does not show client control of the clinic identifier or cross-organization write capability (the created `Patient`'s `organizationId` is independently re-derived from the resolved clinic record, never trusted from request input); the confirmed issue is inconsistent centralized scope resolution and inability for authorized multi-branch actors to target another accessible clinic through this endpoint. **Both `reports.ts:405` and `appointmentRequests.ts:192` remain high-priority remediation candidates** — they perform reporting/PHI operations and bypass the accepted centralized scope contract, even though no attacker-controlled cross-tenant path was demonstrated. This item's original acceptance criteria ("fix only confirmed-incorrect ones") remain **unmet — none of the 47 live usages have been fixed**; it is not a documentation-closure task, and no code has changed. **Distinct from `defaultClinicId`:** the nullable `User.defaultClinicId` UI/navigation-preference field was separately reviewed in **KVKK-HIGH-006-S1** ([evidence/KVKK-HIGH-006-S1_DEFAULT_CLINIC_ID_AUTHORIZATION_PATH_VERIFICATION.md](evidence/KVKK-HIGH-006-S1_DEFAULT_CLINIC_ID_AUTHORIZATION_PATH_VERIFICATION.md)) — no direct authorization violation was found for that narrower question. KVKK-HIGH-006-S1 is a narrow side-verification; it does not replace, satisfy, supersede, close, mitigate, or reduce the required scope of this KVKK-HIGH-006 row. `req.user.clinicId` itself is server-resolved and safe against direct client injection, but is not sufficient for endpoints that must support explicit clinic selection or organization-wide access — it is still used directly (bypassing the centralized helper) in the routes named above, so this parent remediation remains open. | — | — | `STILL_OPEN` / `READY_FOR_DETAILED_SCOPING`. Next task: **KVKK-HIGH-006-S2 — Raw `req.user.clinicId` Occurrence Classification and Remediation Plan** — reproduce/reconcile the full occurrence set (including `dashboard.ts`, `organizationBranches.ts`, `organizationWhatsApp.ts`), inspect every live runtime occurrence at enclosing-function/final-Prisma-operation level, classify each (safe resolved context / audit attribution only / functional multi-clinic narrowing / authorization consistency defect / genuine tenant-isolation risk / false positive), define the smallest coherent implementation batches prioritizing `reports.ts`, `appointmentRequests.ts`, PHI, financial, and clinical mutation paths, and define targeted tests per endpoint — no code change during S2 unless separately authorized. **[Updated 2026-07-21, self-reference-lag correction]:** KVKK-HIGH-006-S2 is now confirmed `MERGED` via [PR #191](https://github.com/MustafaBasol/DisKlinikCRM/pull/191) (merge commit `548bcb7c81065fc71474fdff3632f4759528ffd3`, current tip of `origin/main`) — the "not merged" wording in this row's own text predates that merge. Runtime remediation has not started; this parent KVKK-HIGH-006 row remains `STILL_OPEN`/`READY_FOR_DETAILED_SCOPING`; zero of the 47 occurrences are fixed. Next task: KVKK-HIGH-006-S3 — Batch 1 implementation. |
| KVKK-HIGH-005 | İYS integration (only if marketing/campaign features are ever built) | N/A (feature not active) | No active marketing send path found in code | — | — | Re-evaluate if/when a marketing feature is scoped |
| KVKK-HIGH-003 | Medical-record retention period + periodic anonymization policy | Waiting for legal review | `dataRetentionPolicy.ts` explicitly excludes medical/financial records from cleanup | — | Legal counsel | Hard-delete of medical records is explicitly NOT recommended |
| KVKK-ATTACH-IMAGING-001 | Attachment/imaging physical-file lifecycle (legal hold, streamed/bounded export package, anonymization redaction, deletion-review dry-run only, orphan-check) | Completed and verified (technical lifecycle control; legal/infrastructure dependencies below remain separate) | See Section 6.2 below and `docs/compliance/53-kvkk-attachment-imaging-lifecycle.md` | PRs #160, #161 (CI-flake tracking issue, not a code PR), #162, #163, merged into `main` | Automated tests + production deployment, production migration, production schema/index verification, production backup created and checksum/content verified, patient ZIP export production-verified (contents, archive-row lifecycle, no leftover temp/`.partial-*` artifacts), `PATIENT_PRIVACY_EXPORT_CLEANUP_ENABLED=true` and its cron job confirmed running in production logs, cross-platform storage-key validation fix (PR #162) production-tested, attachment legal-hold enforcement/UI (PR #163) manually accepted in production | No PatientAttachment live-delete path exists at all (dry-run inventory only — the original narrow execute endpoint was removed per PR #160 review, not hardened; see Section 6.2). Imaging/clinical data likewise has no live-delete path, pending legal retention-period decisions. Still-outstanding: KVKK-HIGH-001 (attachment encryption, separately tracked below), KVKK-HIGH-003 (retention period, waiting for legal review), medical/DICOM retention approval, hard-deletion-of-medical-records policy, backup deletion/restore expectations, infrastructure encryption verification |
| KVKK-HIGH-008 | Audited, management-only (OWNER/ORG_ADMIN/CLINIC_MANAGER) correction workflow for a stale legacy `Patient.smsOptOut=true` — new immutable, create-only `PatientLegacyConsentCorrection` model (never grants consent, never creates/alters a central `PatientCommunicationPreference`/`PatientCommunicationConsentEvent` row); consent-action-modal Notes/NoticeVersion validation UX fix (dynamic required labels, no silent no-op on invalid submit, focus management, aria wiring); `PatientDetail` responsive/accessible tab-overflow fix (URL-backed active tab, scroll chevrons, keyboard nav) | **Merged and deployed to production, technically verified (2026-07-20)** — migration `20260719155318_kvkk_high008_legacy_consent_correction` confirmed applied (64/64 migrations, `prisma migrate status` clean); table/enum confirmed present, **0** correction rows. **[Superseded 2026-07-20/2026-07-21 by PR #186/KVKK-HIGH-008-F1, merged, deployed]:** the "no `COMMUNICATION_CONSENT_*` gate exists and none was added" claim below no longer reflects current `main` — a dedicated `PlatformSetting`-backed runtime kill switch (`privacy.legacyConsentCorrection.runtimeEnabled`, default-disabled) now gates the mutation route only (read/history routes remain unaffected); production evidence confirms the setting row is **absent**, effective fail-closed **`false`** — the workflow is deployed but not reachable for OWNER/ORG_ADMIN/CLINIC_MANAGER mutations while the gate stays disabled. No human accept/reject decision on controlled activation has been recorded (R-061, `OPEN`); a 2026-07-21 read-only feasibility review (KVKK-HIGH-008-F1-PBV-S1) confirmed the gate check is unreachable without a real patient and that no unset/delete API exists for the setting, so an explicit-false PATCH that would close remaining audit-verification gaps is technically feasible but **not authorized** (not a reversible test) — see `docs/program/evidence/KVKK-HIGH-008-F1-PBV-S1_SAFE_BEHAVIORAL_PRODUCTION_VERIFICATION_FEASIBILITY.md`. | `docs/compliance/56-kvkk-communication-preference-and-consent-management.md` §21.12; `docs/program/KVKK_HIGH008_FREEZE_BOUNDARY.md` §6-§9 | branch `feature/kvkk-high008-legacy-consent-correction` (PR #180, merged); runtime toggle branch `fix/kvkk-high008-runtime-toggle` (PR #186, merged) | Automated tests (author-reported, not independently re-executed, incl. claimed real-Postgres concurrency) — production deployment/migration confirmed 2026-07-20; runtime gate deployed and effective-false-verified 2026-07-20; rollback/tenant-impact not independently verified; behavioral (live disabled-response, audit-creation) verification not independently verified — feasibility of a production-safe path analyzed 2026-07-21 (see Notes); no legal verification | Technical control only, not legal approval of any consent basis/wording. **[Historical, capture-time text, preserved below; superseded by the correction above]:** Activation readiness (§21.12.14): code merge readiness is pending review; deployment readiness is separate from consent-enforcement activation — this workflow carries no `COMMUNICATION_CONSENT_*` gate and becomes immediately active for OWNER/ORG_ADMIN/CLINIC_MANAGER once deployed; it must not be described as inert. The `COMMUNICATION_CONSENT_*` flags that DO remain disabled belong to the separate KVKK-HIGH-007 reconciliation/audit-logging/enforcement rollout, unaffected by this workflow's deployment; no production backfill executed; does not alter runtime consent precedence beyond recognizing a corrected legacy value; the three deferred inbound-reply paths (§21.7/§21.9) remain untouched. Retention of the new correction record is pending legal review, following the existing `AuditLog`/`ActivityLog` by-omission exclusion from `dataRetentionCleanupJob.ts` |

---

## 6.1 KVKK-CRIT-001a — Implementation evidence

**Status:** Completed and verified (technical/evidence sub-item — merged into `main` via PRs #156–#159, deployed to NoraMedi production, migration applied successfully, and manually browser-verified in production; see Section 6.1.2 below). Legal-text approval for each clinic's own privacy notice remains a separate, still-outstanding dependency (see Section 3) — publishing/approving the notice *content* is not part of this remediation item, only the delivery/evidence mechanism. Notice evidence proves server-side delivery and linkage of the notice version only — it is not proof that any patient read, understood, or consented to the notice, and must never be described as such.

**Root cause / previous behavior:** The public booking widget (`src/pages/BookingWidget.tsx`, `server/src/routes/publicBooking.ts`) collected and stored patient personal data (name, phone, email, notes) with no Art. 10 privacy-notice display and no record of which notice version, if any, was shown at the time of collection. `ClinicLegalProfile` and its publish/versioning flow already existed for the clinic KVKK page (`publicClinicKvkk.ts`) but was never consulted by the booking flow.

**Chosen architecture:** Public booking now resolves and reuses the clinic's currently published `ClinicLegalProfile` (no second legal-content system). Because `ClinicLegalProfile` is mutable in place (POST `/publish` can re-save an already-published profile), a new table `PublicBookingNoticeEvidence` snapshots the notice text, controller identity, version, and effective date at issuance time, so historical attribution survives later edits. Evidence is issued automatically (no acknowledgment action), server-validated, and atomically linked to the created `AppointmentRequest`. Full design rationale is in the module docstring of `server/src/services/publicBookingNoticeEvidence.ts`.

**Database model / migration:**
- Model: `PublicBookingNoticeEvidence` (`server/prisma/schema.prisma`) — organization/clinic-scoped, snapshots `noticeTextSnapshot`, `controllerNameSnapshot`, `privacyContactSnapshot`, `noticeVersion`, `noticeEffectiveDate`, `noticeHash`; `channel` fixed to `web_booking`; unique `appointmentRequestId` (one evidence row per booking request); `legalProfileId`/`appointmentRequestId` FKs use `onDelete: SetNull` so historical evidence is never destroyed by later profile edits or (hypothetical) request deletion.
- **Field naming correction (this review pass, 2026-07-15):** the timestamp field was renamed `displayedAt` → `deliveredAt` and the token field `token` → `tokenHash`. Rationale below (Timing semantics / Token security).
- Migration: `server/prisma/migrations/20260715090000_add_public_booking_notice_evidence/migration.sql` — hand-authored to contain **only** this table's DDL (an initial `prisma migrate dev` run picked up unrelated pre-existing drift between migration history and `schema.prisma` that predates this change; that drift was discarded from this migration and is not part of this remediation item).
- No changes to `ChannelConsentLog` or its columns.

**Backend changes:**
- `server/src/services/publicBookingNoticeEvidence.ts` (new) — `resolvePublishedLegalProfile`, `issueOrReuseNoticeEvidence` (idempotent per clinic+session+version), `validateNoticeEvidenceToken`, `linkNoticeEvidenceToRequest` (race-safe via guarded `updateMany`).
- `server/src/routes/publicBooking.ts` — `GET /api/public/booking/:clinicId` now also returns `legalNotice` (no internal IDs exposed); new `POST /api/public/booking/:clinicId/notice-evidence` issues/reuses evidence (rate-limited, no side effects on GET); `POST /api/public/booking/:clinicId` now requires and server-validates `noticeEvidenceToken` before creating any `AppointmentRequest`, and links evidence atomically inside the same transaction as request creation (both the full-slot-info and partial-request code paths).

**Frontend changes:**
- `src/pages/BookingWidget.tsx` — fetches `legalNotice` from the bootstrap response; blocks the entire form (no data collection) with a neutral message when no published profile exists; silently issues notice evidence via a per-tab-session id (`sessionStorage`, not a patient identifier); displays clinic controller name + an expandable full-notice text section before submission; disables submit until evidence is ready; no acknowledgment/consent checkbox anywhere.
- `src/services/api.ts` — added `publicBookingService.getNoticeEvidence`; `submit` now carries `noticeEvidenceToken`.
- Translations added to `src/locales/{tr,en,fr,de}/booking.json` under `notice.*` (all 4 shipped app locales).

**Tenant isolation / security:**
- Evidence lookups always re-derive the clinic from the URL param and check `evidence.clinicId === clinicId` server-side — a client cannot use one clinic's token for another (verified in the smoke test below).
- Client-supplied clinic ID, legal-profile ID, notice text, and notice version are never trusted; only the opaque evidence token is accepted, and it is resolved against the database.
- Booking submission is rejected (400, generic message, no info leak) when the clinic has no published profile, when the token is missing/unknown/expired/wrong-clinic/wrong-channel/already-linked.
- Rate limiting on both the booking-submit endpoint (existing) and the new notice-evidence endpoint.

**WhatsApp/Instagram non-regression:** `channelConsentGate.test.ts` (28/28) and `channelConsentFlowResume.test.ts` (17/17) pass unchanged; `publicClinicKvkk.ts` and `clinicLegalProfile.ts` were not modified; `clinicLegalProfile.test.ts` (29/29) passes unchanged.

---

### 6.1.1 Final pre-merge review (2026-07-15) — timing semantics, token security, orphan lifecycle, browser acceptance

This sub-section records a second review pass performed before merging PR #156, focused specifically on evidentiary accuracy and token-handling security. At the time this review pass was written, PR #156 had not yet been merged. **Update (2026-07-16): PR #156, along with the follow-up PRs #157–#159, has since been merged into `main`, deployed to NoraMedi production, and production-verified — see Section 6.1.2 below.**

**1. Evidence timing semantics — code change required.**
The field was originally named `displayedAt`, which implied the notice had actually been rendered/read by the visitor. In reality the timestamp is written when the server *issues* the evidence token in response to the frontend's `GET .../notice-evidence` bootstrap call — this proves server-side delivery of the notice payload to the client, not that the visitor scrolled to, read, or acknowledged it. **Fixed:** renamed to `deliveredAt` throughout (`schema.prisma`, migration SQL, `publicBookingNoticeEvidence.ts`, the `POST /notice-evidence` route response). The Prisma model doc comment now states explicitly: "proves server-side delivery only, NOT that the visitor actually read or scrolled to the notice — never describe it as proof of reading or review anywhere in code, UI copy, or documentation." No compliance-tracker or UI copy anywhere claims proof of reading, review, or consent; this was verified by the existing static no-consent-wording tests (test 13/14 in `publicBookingNoticeEvidence.test.ts`), which also scan this exact wording.

**2. Token security — code change required.**
The original implementation stored the raw opaque token in the `token` column. **Fixed:** the column is now `tokenHash` — a SHA-256 hash (`hashNoticeToken()`, `crypto.createHash('sha256')`) of a cryptographically random token (`crypto.randomUUID()`-based generation, unchanged from the original design). The raw token is returned to the client exactly once (in the bootstrap response body) and is never persisted anywhere; only its hash is stored and looked up. Other properties, confirmed unchanged/still correct: token is bound to `clinicId` and `channel` (`web_booking`) at validation time; tokens expire (`expiresAt`, checked server-side); linking a token to an `AppointmentRequest` is one-time (`already_linked` failure mode) and race-safe (guarded `updateMany`, verified in unit test 12: concurrent second link loses the race); the raw token is never logged (grepped `publicBookingNoticeEvidence.ts` and `publicBooking.ts` for `console.log`/`logger` calls touching `token` — none found); the full notice text is never logged either.
Because only the hash can be persisted, idempotent reuse (same clinic+session+channel+version, unexpired) now **rotates** the token on every reissue rather than returning the original raw value — safe because the frontend never persists the token across mounts, only the session id (`sessionStorage`), and always re-fetches a fresh token immediately before submission.

**3. Orphan-evidence lifecycle — code change required.**
Evidence rows created via the bootstrap call but never linked to a booking request (visitor loads the page, notice is issued, visitor leaves without submitting) previously had no expiry/cleanup path. **Fixed:** added `cleanupExpiredNoticeEvidence()` (`publicBookingNoticeEvidence.ts`) — deletes only rows where `appointmentRequestId IS NULL AND expiresAt < now`; rows already linked to an `AppointmentRequest` are never touched by this or any other cleanup path, regardless of age. Wired into a new dedicated hourly cron job, `server/src/jobs/publicBookingNoticeEvidenceCleanupJob.ts` (`node-cron`, `withJobLock`-guarded, mirrors the existing `metaTemplateSyncJob.ts` pattern), started from `startBackgroundJobs.ts`. A dedicated job (rather than folding into the larger `dataRetentionCleanupJob.ts`) was chosen to keep this additive and avoid touching that file's ~20 existing DB-less unit tests, which always construct a full dependency object. Idempotent-reuse (item 2 above) already prevents uncontrolled duplicate-row growth on simple page refresh/retry within a token's TTL, independent of this cleanup job.

**Real-DB smoke verification of items 2 and 3 (this review pass, disposable Postgres, throwaway script, deleted after use):** token stored as a hash, not plaintext; idempotent reuse rotates the token without creating a duplicate row; the pre-rotation (old) token is rejected on submit; the new (rotated) token succeeds and links exactly one evidence row to one `AppointmentRequest`; the orphan-cleanup query deletes only expired+unlinked rows and never deletes a linked row even when force-expired; a clinic with no published legal profile is still blocked with `blocked_missing_legal_profile`. All 6 scenarios passed.

**4. Browser acceptance — performed this review pass (2026-07-15), real Chromium via Playwright.**
Stack: disposable Postgres (Docker, ephemeral, destroyed after the run) → backend (`tsx src/index.ts`, port 5000) → frontend (`vite`, port 5173), seeded with two clinics: Clinic A ("Deniz Diş Kliniği", legal name "Deniz Diş Sağlığı Hizmetleri A.Ş.") with a published KVKK profile, and Clinic B — deliberately named literally **"Mustafa Basol"** (the operator's own name, used as an adversarial demo-data leak probe) — with **no** published legal profile. All 11 checks passed:
- Clinic A booking page loads; no `<input type="checkbox">` exists anywhere on any step; no consent/agreement wording (Turkish or English) present on the service-selection step or the contact/notice step.
- Controller identity ("Deniz Diş Sağlığı Hizmetleri A.Ş.") and the full KVKK notice text (containing "6698 sayılı KVKK") are correctly displayed on the contact step, both collapsed and after expanding via the "Aydınlatma metnini görüntüle" toggle.
- A full booking submission (name + phone filled, notice evidence token attached) succeeds and reaches the "Talebiniz Alındı!" success screen.
- Mobile viewport (390×844) renders the page correctly, not blank.
- Clinic B never displays Clinic A's controller name (no cross-clinic leak) and — because it has no published profile — presents **no** name/phone input fields at all (the widget shows the neutral "notice unavailable" blocking screen instead of any data-collection form), so "Mustafa Basol" demo data is never surfaced as notice/controller content for any clinic.
- WhatsApp/Instagram flows were not touched by this review pass (see automated non-regression suites above) and were not separately re-exercised in the browser.

**Post-submission database verification (same disposable DB, direct Prisma queries, throwaway script, deleted after use):** the successful submission created exactly one `AppointmentRequest` with exactly one linked `PublicBookingNoticeEvidence` row (1:1, confirmed by id). Unlinked evidence rows from page loads that were not submitted remained in the table as expected (orphans, subject to the new cleanup job). Immediately after, the clinic's `ClinicLegalProfile` was republished with different text, a different version string (`v2-2026-07-16`), and a different controller title — the already-linked (historical) evidence row's `noticeVersion` remained `v1-2026-07-15` and its snapshot fields were unchanged, confirming a later publication does not alter a historical evidence snapshot.

**Cleanup after this review pass:** disposable Postgres container, backend/frontend dev processes, and all throwaway scripts (`__seed_browser_review.ts`, `__verify_browser_review.ts`, `__browser_review.mjs`) were stopped/deleted; none were committed. `server/.env` was temporarily swapped to point at the disposable database for the duration of the backend process only and was restored to its original contents immediately afterward (verified via `git status`/`git diff` showing no changes to `server/.env`).

**Files changed (original implementation):**
- `server/prisma/schema.prisma`, `server/prisma/migrations/20260715090000_add_public_booking_notice_evidence/migration.sql`
- `server/src/services/publicBookingNoticeEvidence.ts` (new)
- `server/src/routes/publicBooking.ts`
- `server/src/tests/publicBookingNoticeEvidence.test.ts` (new), `server/package.json` (test script wiring)
- `src/pages/BookingWidget.tsx`, `src/services/api.ts`
- `src/locales/tr/booking.json`, `src/locales/en/booking.json`, `src/locales/fr/booking.json`, `src/locales/de/booking.json`
- `docs/compliance/KVKK_COMPLIANCE_AUDIT_AND_REMEDIATION.md` (this file)

**Files changed (this review pass, 2026-07-15 — see 6.1.1):**
- `server/prisma/schema.prisma` — `token`→`tokenHash`, `displayedAt`→`deliveredAt`, index rename
- `server/prisma/migrations/20260715090000_add_public_booking_notice_evidence/migration.sql` — same rename (migration was still undeployed anywhere, so it was safely edited in place rather than adding a second migration)
- `server/src/services/publicBookingNoticeEvidence.ts` — added `hashNoticeToken`, `cleanupExpiredNoticeEvidence`; rewrote issuance to hash+rotate tokens and use `deliveredAt`
- `server/src/routes/publicBooking.ts` — response field `displayedAt`→`deliveredAt`
- `server/src/jobs/publicBookingNoticeEvidenceCleanupJob.ts` (new) — hourly orphan-evidence cleanup cron job
- `server/src/jobs/startBackgroundJobs.ts` — registers the new cleanup job
- `server/src/tests/publicBookingNoticeEvidence.test.ts` — mock updated for hashed lookup; 2 new tests for `hashNoticeToken` (18 total)
- `docs/compliance/KVKK_COMPLIANCE_AUDIT_AND_REMEDIATION.md` (this file)

**Tests added:** `server/src/tests/publicBookingNoticeEvidence.test.ts` — **18 unit tests** (16 original + 2 added this pass): language normalization, evidence-token validation (missing/not-found/wrong-clinic/wrong-channel/expired/already-linked/valid), atomic-link race behavior, static no-consent-wording checks on the Prisma model and service exports, channel/language constants, `hashNoticeToken` determinism and non-collision.

**Exact validation commands and results (this session, 2026-07-15):**
- `npx prisma validate` → `The schema at prisma\schema.prisma is valid`
- Disposable Postgres 16 (Docker, ephemeral, destroyed after validation) seeded with the pre-existing 57 committed migrations via `prisma migrate deploy`, then the new isolated migration applied the same way → `npx prisma migrate status` → `Database schema is up to date!`
- `npx prisma generate` → succeeded
- `npm run typecheck` (server; `prisma generate && tsc --noEmit`) → 0 errors
- `npx tsx src/tests/publicBookingNoticeEvidence.test.ts` → **18 passed, 0 failed** (this review pass; 16 originally, +2 for `hashNoticeToken`)
- `npx tsx src/tests/clinicLegalProfile.test.ts` → **29 passed, 0 failed** (unchanged)
- `npx tsx src/tests/channelConsentGate.test.ts` → **28 passed, 0 failed** (unchanged)
- `npx tsx src/tests/channelConsentFlowResume.test.ts` → **17 passed, 0 failed** (unchanged)
- `npx tsx src/tests/publicBookingAvailability.test.ts` → **19/19 passed** (unchanged)
- `npm test` (server, full suite, 48 test scripts including the new one) → **all passed, 0 failed**
- `npx tsc -b` (frontend project references) → 0 errors
- `npm run build` (frontend, `tsc -b && vite build`) → succeeded (pre-existing chunk-size warnings only, unrelated to this change)
- Ad hoc Node-level HTTP smoke test (not committed — run against the same disposable Postgres, exercised the real Express router with real Prisma queries, then deleted): bootstrap with/without published profile, submission blocked with no evidence, idempotent re-issuance (1 row for repeated bootstrap calls), cross-clinic token rejection, successful submission + atomic evidence linking, rejected reuse of an already-linked token, historical snapshot unchanged after publishing a new notice version, forged/unknown token rejection — **all 10 scenarios passed**.

**Browser acceptance (dev-environment):** Performed 2026-07-15 — see 6.1.1 item 4 above for the full checklist and results (11/11 automated Playwright checks passed against a real Chromium browser, real frontend/backend dev servers, and a disposable Postgres database). This satisfied the *local/dev-environment* browser acceptance checklist but did not by itself constitute production verification.

### 6.1.2 Production deployment and verification (2026-07-16)

**PRs #156, #157, #158, #159 have been merged into `main`, deployed to NoraMedi production, and their migration applied successfully.** Manual production verification (real Chromium browser, against production, real clinic-specific published legal profiles) confirmed all of the following:

- Public booking correctly blocks data collection when the clinic has no published `ClinicLegalProfile`.
- Public booking shows the correct clinic-specific controller identity and notice text for clinics that do have a published profile.
- A successful booking creates and links a `PublicBookingNoticeEvidence` row to the created `AppointmentRequest`.
- No mandatory consent/acknowledgment checkbox exists anywhere in the flow.
- No cross-clinic data leak — the operator's own demo/legal-profile data ("Mustafa Basol") does not surface as notice/controller content for any other clinic.
- Production verification surfaced a separate, non-KVKK public-booking defect (stale time-slot display vs. submit-time rejection) — fixed separately via PRs #157, #158, #159 (see the cross-reference paragraph below).

**Scope of "Completed and verified":** this covers only the technical delivery/evidence control — that the server correctly resolves the clinic's published legal profile, blocks booking when none exists, displays the correct clinic-specific notice/controller, and creates+links a notice-version evidence record on successful booking. It does **not** approve the legal wording of any clinic's own privacy notice, and notice evidence must never be described as proof that a patient read, understood, or consented to the notice — it proves server-side delivery and linkage of the notice version only. Per-clinic notice wording and lawful-basis approval remain separate, outstanding legal dependencies (Section 3).

**Remaining dependency (not blocking this technical sub-item's status):**
1. Legal-text approval of each clinic's own privacy-notice content remains with legal counsel / clinic operators — this remediation item only fixes *delivery and evidence*, not notice wording.

Status is now **Completed and verified** for the technical/evidence sub-item, per the Section 7 rules (implementation present, migrations applied and validated, type checks and focused/regression tests pass, tenant-scope tests pass, and production verification has now been performed).

**Cross-reference (not a KVKK finding):** Production verification of this item surfaced a separate, non-KVKK public-booking defect — the widget could display a time slot that submit-time validation then rejected with `409 SLOT_UNAVAILABLE` (stale-slot / recovery-UX bug, unrelated to notice delivery or evidence). This does not change the legal interpretation, scope, or status of KVKK-CRIT-001a above. See `docs/50-public-booking-slot-consistency-hotfix.md` for the full technical defect record; fixed and merged via PRs #157, #158, #159.

---

## 6.2 KVKK-ATTACH-IMAGING-001 — Implementation evidence

**Status:** Completed and verified (technical lifecycle control — merged into `main` via PRs #160, #162, #163, deployed to NoraMedi production, migration applied successfully, and manually production-verified; see Section 6.2.1 below). Legal/infrastructure dependencies listed at the end of this section remain outstanding and are not blocking this technical sub-item's status.

**Root cause / previous behavior:** every other KVKK remediation item covered structured database records and messaging metadata, but `PatientAttachment` and `ImagingStudy`/`ImagingImage` physical files were untouched by any privacy workflow — no legal-hold concept, no downloadable export including actual file bytes, no anonymization of attachment/imaging metadata, no deletion-review coverage, and no orphan-file (DB row present, physical file missing) visibility.

**Chosen architecture:** extended the single existing storage abstraction (`server/src/services/fileStorage.ts`) rather than introducing a second one; added `legalHold`/`legalHoldReason` to `PatientAttachment` and `ImagingStudy` (images inherit their study's hold — no separate field); added a new `PatientPrivacyExportArchive` model following the token-hash + expiry pattern already established by `PublicBookingNoticeEvidence`, extended with a `status` lifecycle column for per-clinic concurrency control; extended `anonymizePatientData()` with a per-object, try/catch-isolated redaction pass over attachments and imaging images; added a dry-run-only deletion-review service (no live-delete endpoint) and a bounded orphan-check service. Full design rationale, lifecycle policy matrix, and remaining legal decisions are in `docs/compliance/53-kvkk-attachment-imaging-lifecycle.md`.

**PR #160 human review pass (same day) found and this session fixed:** (1) migration drift from an unrelated stale dev-database capture — cleaned to contain only this feature's DDL; (2) the deletion-review "administrative" classification was an unsafe blanket label with a live-delete endpoint not bound to any privacy-request workflow — **the execute endpoint was removed entirely**, not hardened, and the field renamed `deletableAdministrative` → `unclassifiedRetained`; (3) export-package generation buffered the entire ZIP and every attachment file fully in memory with no size/count bounds, and leaked raw exception text into the manifest/API response — rewritten to stream to a temp file with enforced bounds (500 files / 10 MB per file, reusing the existing attachment upload cap / 2 GB total) and stable reason codes; (4) the download token was carried in a URL query parameter, marked "downloaded" before the stream completed, and had no true one-time atomicity — moved to a request header, made atomically one-time-consumable, and split into three distinct started/completed/failed audit events; (5) the attachments legal-hold route used `req.user.clinicId` directly instead of the org/branch-scoped helper, had no audit log, and only required a reason to place (not release) a hold — all four fixed; (6) the new export-cleanup cron job had no env kill-switch — added `PATIENT_PRIVACY_EXPORT_CLEANUP_ENABLED`, deliberately separate from `DATA_RETENTION_CLEANUP_ENABLED`; (7) `PatientPrivacyPanel.tsx`'s new strings were hardcoded Turkish with no i18n — moved into a new `patientPrivacy` tr/en/fr/de locale namespace. See `docs/compliance/53-kvkk-attachment-imaging-lifecycle.md` (updated same day) for full detail.

**Follow-up review pass (same day) found and this session fixed:** the remediation above initially shipped item (3)'s new `status` column as a *second*, separate migration on top of the already-cleaned first one, leaving the first migration's `CREATE TABLE` for `PatientPrivacyExportArchive` stale relative to `schema.prisma` (no `status` column; `storageKey`/`manifestJson`/`tokenHash`/`expiresAt` still `NOT NULL`) — a real P0, since a fresh `prisma migrate deploy` of just that migration would have produced a table the export-reservation code (which inserts a `status="generating"` row with those four columns still empty) could not actually write to. Fixed by merging both migrations into the single original migration file and proving it matches `schema.prisma` exactly on a completely empty disposable database — see the updated validation commands below and Sections 12–13 of `docs/compliance/53-kvkk-attachment-imaging-lifecycle.md`.

**Second follow-up review pass (same day) found and this session fixed two further P0s and one P1:** (1) `fileStorage.ts`'s `saveFileFromPath()` uploaded the export ZIP to S3 but never removed the local temp file — the caller assumed the helper had consumed it, so a sensitive patient ZIP could be left indefinitely under the OS temp directory, invisible to both the TTL cleanup job (which only tracks the storage key, not this local scratch path) and disk-usage monitoring, and repeated exports could exhaust disk. Fixed: `saveFileFromPath()` now always removes the temp file itself in every mode (local rename, local copy-fallback, S3 success, S3 failure) via `try`/`finally`, and the S3 multipart upload explicitly sets `leavePartsOnError: false` so a failed upload aborts and cleans up already-uploaded parts. (2) `reserveGenerationSlot()`'s `findFirst`-then-`create` had no lock, so two concurrent export requests for the same clinic could both read "no active generation" and both create a `generating` row under READ COMMITTED — the one-in-flight-per-clinic guarantee was not actually enforced. Fixed: the reservation now runs inside a `prisma.$transaction` that first acquires a PostgreSQL advisory transaction lock keyed off `clinicId` (same pattern as `acquireAppointmentSlotLock` in `appointmentRequestSafety.ts`), then sweeps stale rows, checks for an active row, and creates the new row — all inside the lock, released automatically at transaction end. Proven with genuinely concurrent calls to the actual exported `reserveGenerationSlot()` (not a reimplementation) via `Promise.allSettled` against a real disposable Postgres database. (3, P1) Export read/upload/download error paths logged raw exception objects/`.message`, which can embed local absolute paths (Node `ENOENT` messages) or S3 endpoints — replaced with a new `safeErrorFields()` helper (`server/src/utils/safeError.ts`) logging only a stable `errorName`/`errorCode` plus already-safe identifiers (`exportId`/`attachmentId`). Also corrected this document's and `docs/compliance/53-...md`'s rollback guidance, which had incorrectly implied `prisma migrate resolve --rolled-back` performs a schema rollback (it only edits Prisma's own bookkeeping table) — see the corrected Section 14 of `docs/compliance/53-kvkk-attachment-imaging-lifecycle.md`.

**Database model / migration:**
- `server/prisma/migrations/20260715145843_add_kvkk_attachment_imaging_lifecycle/migration.sql` — a single, additive migration: `legalHold`/`legalHoldReason`/`storageVerifiedMissingAt` on `PatientAttachment`, `legalHold`/`legalHoldReason` on `ImagingStudy`, `storageVerifiedMissingAt` on `ImagingImage`, and a new `PatientPrivacyExportArchive` table including its `status TEXT NOT NULL DEFAULT 'ready'` column, nullable `storageKey`/`manifestJson`/`tokenHash`/`expiresAt`, and the `(clinicId, status)` index for the per-clinic export-generation concurrency guard. (An interim second migration, `20260715160000_add_export_archive_status`, existed briefly during this same review round and has since been merged back into this one — see the follow-up review pass above.)

**Backend changes:** `server/src/services/fileStorage.ts` (`isSafeStorageKey`/`fileExists`/`statFile`/`buildExportStorageKey`, plus new `saveFileFromPath` for streamed writes), `server/src/services/privacy/patientPrivacyExportPackage.ts` (rewritten: streaming, bounds, status/concurrency, atomic token consumption, stable reason codes), `server/src/services/privacy/deletionReviewInventory.ts` (field renamed, execute-endpoint reference removed), `server/src/services/privacy/orphanFileInspection.ts`, `server/src/services/privacy/patientAnonymization.ts`, `server/src/routes/patientPrivacy.ts` (execute route deleted; download route rewritten for header-token + atomic claim + 3-event audit; export-package route maps 409), `server/src/routes/attachments.ts` (legal-hold route: scope helper, reason-both-ways, audit log; exports `ATTACHMENT_MAX_FILE_SIZE_BYTES`), `server/src/routes/imaging.ts` (legal-hold route: reason-both-ways, extra audit metadata), `server/src/jobs/patientPrivacyExportCleanupJob.ts` (env kill-switch), `server/src/jobs/startBackgroundJobs.ts` (unchanged registration), `server/package.json` (new dependency `@aws-sdk/lib-storage`).

**Frontend changes:** `src/components/PatientPrivacyPanel.tsx` (execute/live-delete UI never existed in the UI to begin with — only the dry-run summary display remains; field rename; new strings moved to i18n), `src/services/api.ts` (`executeDeletionReview` removed; `downloadExportPackage` now sends the token via header), `src/locales/{tr,en,fr,de}/patientPrivacy.json` (new), `src/i18n/config.ts` + `src/i18n/namespaces.ts` (new namespace wired in).

**Tenant isolation / security:** export-download token now header-based, cross-checked against `clinicId`/`organizationId`/`patientId`/`exportId`/`status`/`downloadedAt` (forged `exportId` → `not_found`; wrong clinic/org/patient → `wrong_scope`; already-consumed → `already_downloaded`, atomically enforced; all covered by automated tests); no route in this PR ever serializes `filePath`/`storageKey`, and manifest/error text is always a stable reason code, never a raw exception message; legal-hold PATCH endpoints restricted to `OWNER`/`ORG_ADMIN`, both routes now scope via the shared clinic-scope helper and write an audit log for both hold directions.

**Tests added:** `server/src/tests/kvkkAttachmentImagingLifecycle.test.ts` — 61 unit tests (storage-key safety, real local-disk `fileExists`/`statFile`, export-token validation incl. tenant isolation, export-cleanup job dependency-injected behavior, anonymization redaction semantics incl. legal-hold skip and partial-failure counting, deletion-review dry-run inventory, orphan classification, migration-cleanliness + migration-shape regression checks, live-delete-removal/header-token source scans, atomic one-time claim, export bounds/reason-code checks, cleanup kill-switch, legal-hold reason/audit checks, i18n key parity, **and now: local-mode rename/copy-fallback temp-file cleanup, mocked-S3-client success/failure temp-file cleanup, export-lock-key determinism**). Registered as `npm run test:kvkk-lifecycle`, wired into the `test` chain. Also extended `server/scripts/verify-export-archive-lifecycle.ts` — a manual (not wired into `npm test`) disposable-DB script exercising the real `PatientPrivacyExportArchive` reserve/complete/claim/fail lifecycle **and now genuinely concurrent calls to the real, exported `reserveGenerationSlot()`** against a real Postgres database with real fixtures.

**Exact validation commands and results (this session, 2026-07-15, S3-leak/reservation-race follow-up pass):**
- `npx prisma validate` → `The schema at prisma\schema.prisma is valid`
- Disposable Postgres 16 (Docker, ephemeral, completely empty — never a shared database) → `npx prisma migrate deploy` → all migrations, including the single KVKK migration, applied cleanly
- `information_schema.columns` inspected directly against the disposable database → confirmed `status`/nullability/index shape matches `schema.prisma` exactly (unchanged from the prior pass)
- `npx prisma migrate diff --from-config-datasource prisma/schema.prisma --to-schema prisma/schema.prisma --script` against the disposable database → zero remaining diff for `PatientPrivacyExportArchive`/`PatientAttachment`/`ImagingStudy`/`ImagingImage`; the only remaining diff line is a pre-existing, unrelated `ImagingStudy_createdById_fkey` rebuild on `main`, confirmed untouched by this branch's schema diff
- `DATABASE_URL=<disposable> npx tsx scripts/verify-export-archive-lifecycle.ts` → **9 passed, 0 failed** (real reserve/complete/claim/fail lifecycle, plus real concurrent `reserveGenerationSlot()` calls: exactly one of two simultaneous same-clinic reservations succeeds and the other is rejected with `ExportGenerationInProgressError`, a different clinic reserves concurrently without contention, a manually-staled row is swept to `failed` and no longer blocks)
- `npm run typecheck` (server) → 0 errors
- `npx tsx src/tests/kvkkAttachmentImagingLifecycle.test.ts` → **61 passed, 0 failed** (up from 55 — added the temp-file-cleanup and lock-key tests)
- `npm test` (server, full suite incl. the updated test) → **all suites passed, 0 failed** (no regressions in any pre-existing suite)
- `npx tsc -b` (frontend) → 0 errors
- `npm run build` (frontend, `tsc -b && vite build`) → succeeded (pre-existing chunk-size warnings only, unrelated to this PR; no frontend files changed this round)

### 6.2.1 Production deployment and verification (2026-07-16)

**PRs #160, #162, #163 have been merged into `main`, deployed to NoraMedi production, and their migration applied successfully.** (PR #161 is a CI-flake tracking issue, not a code change — see Section 12 of `docs/compliance/53-kvkk-attachment-imaging-lifecycle.md`.) Production schema and indexes were verified directly, backend and frontend focused tests passed, GitHub Actions checks passed, and a production backup was created with its checksum and content verified.

Production verification, following the 8-step checklist in `docs/compliance/53-kvkk-attachment-imaging-lifecycle.md` Section 15, confirmed:

1. A patient ZIP export was successfully generated and downloaded.
2. The ZIP contained `data.json`, `manifest.json`, and physical attachment files where present.
3. The corresponding `PatientPrivacyExportArchive` row had `status=ready`, `storageKey` present, `tokenHash` present, and `downloadedAt` present.
4. No `queued`/`generating` export rows remained after completion.
5. No `kvkk-export` temp files or `.partial-*` artifacts remained on disk/storage.
6. `PATIENT_PRIVACY_EXPORT_CLEANUP_ENABLED=true` was verified in the production environment.
7. The worker log confirmed the cleanup job is scheduled: `[privacy-export-cleanup] Scheduled cleanup job cron="*/15 * * * *".`
8. The cross-platform unsafe storage-key validation fix (PR #162) passed its production focused tests.

**PR #163 — attachment legal-hold enforcement and UI — production-verified:**
- `OWNER`/`ORG_ADMIN`-only hold mutation, with a reason required both when placing and releasing a hold.
- A held attachment cannot be deleted; the enforcement is atomic at the DB level.
- `legalHoldReason` is hidden from unauthorized roles.
- Audit logs exclude filename and free-text reason.
- Attachment upload/list/download/preview support authorized non-default clinics (multi-branch scope).
- Imaging-study legal-hold UI is available.
- No imaging deletion endpoint exists (matches the "diagnostic data is never hard-deleted" design — Section 4.5/8 of the lifecycle document).

Manual production acceptance for legal hold, localization, the attachment export package, and the deletion-review dry-run was completed.

**Scope of "Completed and verified":** this covers the technical lifecycle control only. It does **not** mean:
- medical/DICOM retention periods have legal approval (still awaiting legal counsel);
- hard deletion of medical records is approved (it is not);
- backup deletion/restore expectations are resolved (still unresolved);
- infrastructure encryption is verified (still a separate control, see Section 4 above);
- application-level file encryption is settled (may still become mandatory depending on infrastructure evidence);
- normal attachment physical-storage deletion failure/orphan handling is fully closed (remains a separate technical follow-up).

This tracker does not claim complete KVKK compliance from this item's completion.

Status is now **Completed and verified** for the technical lifecycle control, per the Section 7 rules (implementation present, migrations applied and validated, type checks and focused/regression tests pass, tenant-scope tests pass, and production verification has now been performed). The legal decisions listed in `docs/compliance/53-kvkk-attachment-imaging-lifecycle.md` Section 16 (clinical/DICOM retention periods, hard-deletion-of-medical-records policy, backup deletion expectations, legal-hold trigger authority) remain outstanding and are **not** blocking this technical sub-item's status, consistent with how KVKK-CRIT-001a separates its own technical-vs-legal dependencies.

---

## 7. Rules for marking work completed

An item may only be set to **Completed and verified** when all applicable conditions hold:
1. Implementation present. 2. Required migrations present and validated. 3. Backend/frontend type checks pass. 4. Focused regression tests pass. 5. Authorization/tenant-scope tests pass where relevant. 6. Production verification done where required. 7. Legal approval obtained for legal-dependent controls. 8. Evidence recorded in this table.

Never mark a legal dependency complete based only on code. Never mark an infrastructure control complete based only on repository inspection. Never mark a control complete if required tests are missing or failing. Never downgrade/remove a finding without a correction-log entry (see full report Section 1/1.1). Never silently delete checklist items — if a finding becomes not applicable, mark `N/A` and record why in Section 8 below.

---

## 8. Remediation Change History

| Date | Finding ID | Previous status | New status | Commit/PR | Summary | Verified by |
|---|---|---|---|---|---|---|
| 2026-07-15 | (all Phase 0 items) | (n/a — first tracked entry) | Not started / Waiting for legal review / Waiting for operational evidence (see Section 6) | — | Initial repository-tracked baseline created from third-pass revised audit report | — |
| 2026-07-15 | KVKK-CRIT-001a | Not started | Implemented — awaiting deployment/operational verification | branch `feature/kvkk-public-booking-notice-evidence` | Added `PublicBookingNoticeEvidence` model/migration, public-booking notice bootstrap + evidence issuance + server-validated linking, BookingWidget notice display (no checkbox), 4-language translations, 16 focused tests + full regression suite pass; browser/production verification and legal-text approval still outstanding (see Section 6.1) | Automated tests only (this session) |
| 2026-07-15 | KVKK-CRIT-001a | Implemented — awaiting deployment/operational verification | Implemented — awaiting deployment/operational verification (status unchanged) | PR #156 review pass, branch `feature/kvkk-public-booking-notice-evidence` | Final pre-merge review: renamed `displayedAt`→`deliveredAt` (delivery, not proof of reading), renamed `token`→`tokenHash` (SHA-256, raw token never persisted, rotates on reuse), added hourly orphan-evidence cleanup job (`publicBookingNoticeEvidenceCleanupJob.ts`), and performed real-browser (Playwright/Chromium) acceptance testing (11/11 checks: no checkbox/consent wording, correct controller+notice display, successful booking links exactly 1 evidence row to 1 request, later republish does not alter historical snapshot, clinic without published profile blocked, no cross-clinic data leak, mobile layout usable). Tests: 18/18 unit (was 16), full server regression suite, WhatsApp/Instagram non-regression suites unchanged. Status deliberately kept unchanged pending real deployment + production verification (see Section 6.1.1) | Automated tests + real-DB smoke test + real-browser acceptance test, all this review session; no production verification yet |

| 2026-07-15 | (n/a — not a KVKK finding, cross-referenced only) | — | — | branch `hotfix/public-booking-slot-consistency-and-recovery` | Production verification of KVKK-CRIT-001a surfaced a separate public-booking stale-slot defect (widget showed a slot that submit-time validation rejected with `409 SLOT_UNAVAILABLE`). Fixed: unified slot-listing conflict rules with submit-time `assertSlotAvailable`, added a real `GET /booking/:clinicId/slots` endpoint, added graceful frontend 409 recovery. No change to KVKK-CRIT-001a's status, scope, or legal interpretation. See `docs/50-public-booking-slot-consistency-hotfix.md` | Automated tests only (this session); no production/browser verification yet |
| 2026-07-15 | KVKK-ATTACH-IMAGING-001 (new) | Not started | Implemented — awaiting deployment/operational verification | branch `feature/kvkk-attachment-and-imaging-lifecycle` | Added attachment/imaging physical-file lifecycle governance: `legalHold`/`legalHoldReason` on `PatientAttachment`/`ImagingStudy`, `PatientPrivacyExportArchive` model + downloadable export-package (ZIP w/ manifest+sha256, token-hash-gated download, dedicated cleanup job), `anonymizePatientData()` extended to redact attachment/imaging metadata with legal-hold-skip and partial-failure reporting, dry-run deletion-review inventory + narrow idempotent live-delete of non-legal-hold attachments only, bounded patient-scoped orphan-check. 39 new unit tests, full existing server suite unchanged/passing, frontend typecheck clean. See Section 6.2 and `docs/compliance/53-kvkk-attachment-imaging-lifecycle.md` | Automated tests only (this session); no production/browser verification yet |
| 2026-07-15 | KVKK-ATTACH-IMAGING-001 | Implemented — awaiting deployment/operational verification | Implemented — awaiting deployment/operational verification (status unchanged; PR #160 review remediation pass, same PR) | PR #160 review pass, branch `feature/kvkk-attachment-and-imaging-lifecycle` | Human review of PR #160 found 7 merge-blocking issues, all fixed same day: (1) migration.sql drift cleaned (confirmed pre-existing/unrelated via before/after diff against `main`'s own migration history — byte-identical remaining drift on both); (2) removed the deletion-review live-delete endpoint entirely (unsafe blanket classification, no privacy-request-workflow binding) rather than hardening it, renamed `deletableAdministrative`→`unclassifiedRetained`; (3) rewrote export-package generation to stream to a temp file (no whole-archive in-memory buffer) with enforced file-count/file-size/total-size bounds and stable reason codes (no raw exception text in manifest/responses); added a `status` column + migration for per-clinic generation concurrency (409 if already in flight); (4) moved the one-time download token from a query parameter to a request header, made consumption atomic (`updateMany` guarded by `downloadedAt: null`), split into 3 distinct started/completed/failed audit events; (5) fixed `attachments.ts`'s legal-hold route to use `validateAndGetClinicIdScope` instead of `req.user.clinicId`, required a reason for release (not just placing) a hold, added a missing audit log call; (6) added `PATIENT_PRIVACY_EXPORT_CLEANUP_ENABLED` kill-switch, deliberately separate from `DATA_RETENTION_CLEANUP_ENABLED`; (7) added a new `patientPrivacy` i18n namespace (tr/en/fr/de) for every string this PR introduces. Tests: 54/54 focused suite (was 39), full server suite unchanged/passing, frontend `tsc -b` + `vite build` clean | Automated tests + disposable-Postgres migration-diff verification only (this session); no production/browser verification |
| 2026-07-15 | KVKK-ATTACH-IMAGING-001 | Implemented — awaiting deployment/operational verification | Implemented — awaiting deployment/operational verification (status unchanged; second follow-up review pass, same PR) | Independent review pass, branch `feature/kvkk-attachment-and-imaging-lifecycle`, commit `ca5e936` → this pass | Independent review of the migration-drift-fix pass found two further P0s and a P1, all fixed same day: (1) `saveFileFromPath()` uploaded the export ZIP to S3 but never removed the local temp file — fixed with `try`/`finally` cleanup in every mode (local rename, local copy-fallback, S3 success, S3 failure) plus explicit `leavePartsOnError: false` on the multipart upload; (2) `reserveGenerationSlot()`'s findFirst-then-create had no lock, allowing two concurrent requests for the same clinic to both create a `generating` row — fixed with a `prisma.$transaction` + PostgreSQL advisory transaction lock keyed off `clinicId` (mirrors `acquireAppointmentSlotLock`), proven with genuinely concurrent calls to the real exported function via `Promise.allSettled` against a real disposable Postgres database (exactly one winner, one `ExportGenerationInProgressError`, exactly one active row; different clinics don't contend; stale rows are swept to `failed`); (3, P1) replaced raw-exception-object logging on export read/upload/download paths with a new `safeErrorFields()` helper (stable error name/code only, never a local path/S3 endpoint/raw exception). Also corrected this document's and `docs/compliance/53-...md`'s rollback guidance (`prisma migrate resolve --rolled-back` is bookkeeping-only, not a schema rollback). Tests: 61/61 focused suite (was 55, +6 temp-file-cleanup/lock-key tests), disposable-DB script now 9/9 (was 6/6, +3 real-concurrency tests), full server suite unchanged/passing, frontend `tsc -b` + `vite build` clean | Automated tests + disposable-Postgres migration-diff + real concurrent-reservation verification, all this session; no production/browser verification |

| 2026-07-16 | KVKK-CRIT-001a | Implemented — awaiting deployment/operational verification | Completed and verified (technical/evidence sub-item) | PRs #156, #157, #158, #159, merged into `main` | Completed and verified after production deployment and browser verification. PRs #156–#159 merged into `main`, deployed to NoraMedi production, migration applied successfully, and manually browser-verified in production with real clinic-specific published legal profiles: blocks public booking data collection when the clinic has no published legal profile; shows the correct clinic-specific controller and notice; creates and links notice evidence during successful booking; no mandatory consent/acknowledgment checkbox; no cross-clinic demo-data leak. Per-clinic notice wording and lawful-basis approval remain separate, outstanding legal dependencies (Section 3) — this status does not approve notice content, only the delivery/evidence mechanism. See Section 6.1.2 | Manual production browser verification (operator) |
| 2026-07-16 | KVKK-ATTACH-IMAGING-001 | Implemented — awaiting deployment/operational verification | Completed and verified (technical lifecycle control) | PRs #160, #161 (CI-flake tracking issue, no code change), #162, #163, merged into `main` | Completed and verified after PRs #160–#163, production migration, and backup/schema/worker/export/legal-hold/manual acceptance verification. Production schema/indexes verified; production backup created and checksum/content verified; patient ZIP export production-verified end-to-end (contents, archive-row lifecycle status/storageKey/tokenHash/downloadedAt, no leftover queued/generating rows, no temp/`.partial-*` artifacts); `PATIENT_PRIVACY_EXPORT_CLEANUP_ENABLED=true` and its 15-minute cron confirmed in production logs; cross-platform storage-key validation (PR #162) production-tested; attachment legal-hold enforcement and UI (PR #163) manually accepted in production, including multi-branch clinic scope for attachment upload/list/download/preview. Medical/DICOM retention periods, hard-deletion-of-medical-records policy, backup deletion/restore expectations, and infrastructure/application-level encryption remain outstanding, non-blocking dependencies. See Section 6.2.1 | Manual production verification + production backup checksum/content verification (operator) |

| 2026-07-16 | KVKK-HIGH-004 | Not started | Implemented — awaiting deployment/operational verification | branch `feature/kvkk-high-004-secure-clinic-bulk-export`, based on `main` after PR #164 | Legacy `GET /api/clinic/export-data` permanently disabled (always `410 CLINIC_BULK_EXPORT_LEGACY_DISABLED`, no data queried, cannot be reactivated by any parameter). New secure flow added: `CLINIC_BULK_EXPORT_ENABLED` fail-closed feature flag (default off), OWNER/ORG_ADMIN-only + `clinicScope.ts` multi-branch validation (never `req.user.clinicId`), step-up re-authentication via a shared `verifyCurrentPassword()` helper with a PostgreSQL-authoritative, `pg_advisory_xact_lock`-serialized brute-force lockout (`ClinicBulkExportPasswordAttempt`, IP stored only as an HMAC-SHA256 hash keyed by a dedicated required `CLINIC_BULK_EXPORT_IP_HASH_SECRET`), Postgres-authoritative creation cooldown/daily-cap and single-active-job-per-clinic invariant enforced twice (advisory-lock reservation transaction + a hand-written partial unique index as the final DB-level backstop), a new `ClinicBulkExportArchive` model with an explicit stored `expired` terminal status (never derived ad hoc — `expireArchiveIfPastTtl()` is the one function every route and the cleanup cron call), a two-step non-atomic artifact-deletion lifecycle (`artifactDeletedAt`/`cleanupFailureCode` for retry), an async worker (`clinicBulkExportWorker.ts`, process-local non-overlap guard + bounded concurrency, NOT a cross-replica `withJobLock`) that streams a ZIP (manifest + clinic.json + 10 cursor-paginated `*.ndjson` entity files, explicit per-entity field-allowlist selects excluding every secret field) to a temp file — never buffering a whole table or the whole archive in memory — a size/record-ceiling fail-fast path (`SIZE_LIMIT_EXCEEDED`, no partial ZIP ever persisted), an atomically-issued one-time hashed download token (guarded `downloadTokenHash IS NULL` update, stream opened before an atomic claim, claim+audit in one transaction with fail-closed rollback on audit failure), a dedicated cleanup cron (`CLINIC_BULK_EXPORT_CLEANUP_ENABLED`, independent flag, default on), full audit-trail coverage via a new transaction-scoped `writeAuditLogInTx()` for the three critical events (creation, token issuance, download claim), and an atomically-deduplicated `OperationalEvent` alert. Frontend: OWNER/ORG_ADMIN-gated Settings tab, dedicated (non-imaging) bounded-backoff status-polling hook, password/purpose/confirmation-gated create flow, re-auth-gated one-time blob download, tr/en/fr/de translations. Backend/frontend type checks and builds pass; migration validated against a fresh disposable Postgres database (`prisma migrate deploy` + `prisma migrate status`, no drift, partial unique index confirmed to reject a concurrent second active row via a direct raw-SQL test). Focused/regression test suite, disposable-Postgres concurrency verification script, and production deployment/verification are still outstanding — see `docs/compliance/54-kvkk-secure-clinic-bulk-export.md` | Automated typecheck/build + disposable-Postgres migration and partial-unique-index verification only (this session); no production/browser verification yet |

| 2026-07-17 | KVKK-HIGH-004 | Implemented — awaiting deployment/operational verification | Merged — awaiting deployment and production verification | PR #165, merged into `main` | KVKK-HIGH-004 (secure clinic bulk/structured-data export) merged into `main` after the review remediation rounds recorded in `docs/compliance/54-kvkk-secure-clinic-bulk-export.md`. Feature flag (`CLINIC_BULK_EXPORT_ENABLED`) still ships `false` — no production behavior change until explicitly enabled per that document's enablement checklist. Status renamed from "Implemented" to "Merged" to reflect that the code is now in `main`, not merely on a feature branch | Automated tests + disposable-Postgres verification (prior sessions); no production/browser verification yet |
| 2026-07-17 | KVKK-CRIT-003 | Not started | In progress | branch `feature/kvkk-crit-003-security-incident-foundation`, based on `main` after PR #165 | Added the KVKK-CRIT-003 TECHNICAL foundation only (no legal breach-notification plan — that remains a separate outstanding item). New `SecuritySignalEvent` (append-only raw evidence), `SecurityIncident` (deduplicated mutable aggregate, DB-unique `incidentKey`, atomic upsert + race-safe conditional severity escalation via raw SQL), and `SecurityIncidentActivity` (immutable lifecycle history) models. `securitySignalService.ts`: HMAC-SHA256 IP/account-identifier hashing keyed by a dedicated required `SECURITY_SIGNAL_IP_HASH_SECRET` (fail-closed in production, never reused from JWT_SECRET/ENCRYPTION_KEY/webhook secrets/CLINIC_BULK_EXPORT_IP_HASH_SECRET), bounded/hashed user-agent fingerprint, allowlist/sanitizer metadata scrubber (drops password/token/secret/cookie/body/rawPayload/messageText/accessToken/storageKey/filePath/exportPath keys outright, redacts email/phone-shaped substrings, bounds size/nesting). Three mandatory detection rules wired into real integration points: (1) auth brute-force — clinic `routes/auth.ts` login + Platform Admin `routes/platformAdmin.ts` login, env-configurable threshold/window, never leaks account existence; (2) cross-tenant access — the shared `validateAndGetScope`/`validateAndGetClinicIdScope` (`utils/clinicScope.ts`) and `clinicAccess.ts` middleware denial paths only (not individual routes), single accidental denial stays low-level evidence, multi-resource probing escalates to high; (3) clinic export anomaly — step-up lockout, download-token replay/expired/invalid, `TEMP_STORAGE_UNSAFE`/ZIP-integrity generation failures, persistent artifact-cleanup failure, and abnormal export-request bursts, all wired into the existing KVKK-HIGH-004 `clinicBulkExport.ts`/`clinicBulkExportPackage.ts`. Platform-Admin-only lifecycle API (`platformSecurityIncidents.ts`) with Zod-validated status transitions (open→acknowledged/investigating/false_positive, etc.; closed/false_positive reachable only via an explicit reopen action that spawns a new dated incident rather than reopening the old one forever), transactional incident+activity writes, rate-limited mutations, dashboard summary counts. Platform Admin UI at `/platform/security-incidents` (list/filter/paginate, detail drawer with activity timeline and lifecycle actions, always-visible "not a legal determination" disclaimer), tr/en/fr/de translations. `SecurityIncidentActivity` — not the existing org/user-centric `AuditLog` model, which has no `actorPlatformAdminId` column — is the audit trail for these lifecycle mutations (documented design decision, not an oversight). Migration generated against a fresh disposable PostgreSQL database (not the pre-existing drifted local dev DB) to avoid capturing unrelated pre-existing schema drift; confirmed zero additional drift beyond that pre-existing, unrelated baseline drift. Runbook: `docs/compliance/55-kvkk-security-incident-response-foundation.md`. Backend/frontend type checks pass. Test suite, full validation run, and production deployment/verification are still outstanding | Automated typecheck + disposable-Postgres migration/index verification only (this session); no production/browser verification yet |
| 2026-07-18 | KVKK-CRIT-003 | In progress | In progress (status unchanged — validation completed on a fresh development machine; PR #167 not yet merged/deployed) | PR #167, branch `feature/kvkk-crit-003-security-incident-foundation` (open, not yet merged) | Full validation completed on a fresh development machine (previous machine had failed mid-session; no local uncommitted work existed, recovered from the open PR). `npx prisma validate` clean; `npx prisma generate` clean; a disposable `postgres:16-alpine` Docker container had all 61 migrations applied via `prisma migrate deploy` with zero errors (including this branch's `20260717180000_add_security_incident_foundation`); backend `npm run typecheck` clean; frontend `npx tsc -b` clean; `npm run test:security-incidents` 31/31 passed (matches PR-claimed count); named regression suites also run individually: `test:auth` 39/39, `test:roles` 142/142, `test:ops` 65/65, `test:kvkk-lifecycle` 110/110, `test:msg-safety` 36/36, all passing; `test:clinic-bulk-export` 116/117 (the 1 failure was reproduced identically against a temporary, disposable `origin/main` worktree of the same source file, confirming it is the already-known, pre-existing Windows `core.autocrlf=true` CRLF/raw-`\n`-indexOf issue documented in this PR's own test plan, not a regression introduced by this branch); full `npm test` ran every suite up to and including `test:clinic-bulk-export` with the same single pre-existing failure and stopped there (the script is `&&`-chained), matching the PR's own reported "Full `npm test` — same single pre-existing failure, no new regressions." No implementation code was changed during this validation. Production deployment/verification remains explicitly outstanding and was not attempted | Automated typecheck + disposable-Postgres migration/full test-suite verification (this session, fresh machine); no production/browser verification yet |
| 2026-07-18 | KVKK-CRIT-003 | In progress | In progress (**clarification, no code executed** — the row above's clean validation did not cover four unresolved merge-blocking issues found by a separate code review of the same PR) | PR #167, branch `feature/kvkk-crit-003-security-incident-foundation` (open, not yet merged) | **Clarification appended to the row immediately above, same day.** The 2026-07-18 validation row confirmed the *original* implementation's tests/migration/typecheck all passed, but that pass did not exercise, and therefore could not have caught, four unresolved merge-blocking issues a subsequent code review found in the same PR: (1) `applyLifecycleTransition()` read the incident's status and later updated it by id only — under PostgreSQL READ COMMITTED, two concurrent lifecycle requests (e.g. `investigating`→`contained` and `investigating`→`resolved`) could both read the same starting status and both succeed, producing a contradictory activity history; (2) `upsertIncidentFromSignal()` inferred "created vs. updated" from a pre-read (`preUpsertExisting === null`) rather than the atomic upsert operation itself — the DB-unique `incidentKey` prevented duplicate `SecurityIncident` rows, but concurrent callers could each observe `null` and each insert a duplicate `created` `SecurityIncidentActivity` row; (3) Platform-Admin-entered incident text (containment/resolution/false-positive/reopen summaries, notes) was only trimmed/length-truncated, with no dedicated check against accidentally-pasted patient PII, passwords, tokens, or other confidential data; (4) `sanitizeSecurityMetadata()`'s docstring called itself an "allowlist," but the implementation only rejected keys matching a denylist pattern and otherwise accepted any key — the opposite of the explicit per-signal-family field allowlist the design intended. None of these four had a regression test before this point, so the clean 31/31 `test:security-incidents` result in the row above is accurate but does not represent merge-readiness. As of this row, all four remained **open**, PR #167's status remained **In progress**, and no production deployment had been performed | This row is a documentation correction only; no code was executed for it — see the remediation row immediately below for the fix and its verification |
| 2026-07-18 | KVKK-CRIT-003 | In progress | In progress (status unchanged — all four review blockers remediated same day; PR #167 still open, not yet merged/deployed) | PR #167, branch `feature/kvkk-crit-003-security-incident-foundation` (open, not yet merged) | Remediated all four blockers from the two rows above, same day. **(1) Concurrency-safe lifecycle:** `applyLifecycleTransition()` (`securityIncidentService.ts`) now performs a compare-and-set `updateMany` conditioned on the exact status value just read (`WHERE id = ... AND status = existing.status`) and requires `count === 1`; PostgreSQL blocks a concurrent second UPDATE on the same row until the first commits, then re-evaluates the second UPDATE's WHERE clause against the now-committed (changed) status, so the race loser's UPDATE matches zero rows. A new `LifecycleFailure` value, `concurrent_transition` (mapped to HTTP 409), is returned for the loser, distinct from `invalid_transition`; the loser never gets a second attempt and never has an activity row inserted for it. **(2) Atomic created-vs-update:** `upsertIncidentFromSignal()` now acquires a `pg_advisory_xact_lock` keyed on the target `incidentKey` (same SHA-256 → `(int4,int4)` derivation and locking shape as the existing `appointmentRequestSafety.ts` pattern) as the first operation inside its transaction, before its pre-read — serializing every concurrent call for the same incident so the "am I the creator" read is no longer racing anything, while the DB-unique `incidentKey` remains the uniqueness backstop. **(3) Operator-text sanitization:** new `sanitizeSecurityOperatorText()` (`securitySignalService.ts`) redacts email/phone-shaped substrings, strips control characters, outright rejects (whole field, not partial) bearer tokens, passwords, API keys, cookies/session values, authorization headers, and storage/filesystem paths, bounds length, and rejects text that becomes empty or only redaction markers — never logging or returning the raw rejected input. Wired into every Platform-Admin-entered incident text field (acknowledge/investigate/contain/resolve/close/false-positive/reopen/note). A visible warning was added above the incident action textarea in tr/en/fr/de. **(4) Explicit per-rule metadata allowlists:** `sanitizeSecurityMetadata()`'s docstring now accurately describes itself as a second-layer denylist; four new closed Zod schemas (`AUTH_SIGNAL_METADATA_SCHEMA`, `CROSS_TENANT_SIGNAL_METADATA_SCHEMA`, `EXPORT_SIGNAL_METADATA_SCHEMA`, `INCIDENT_ACTIVITY_METADATA_SCHEMA`) now gate every rule's/activity's metadata *before* that generic pass, applied at each metadata-construction call site in `securityDetectionRules.ts`/`securityIncidentService.ts`; unknown keys strip silently, a wrong-typed or oversized field drops the whole object (fail-closed, never throws). Added 21 new tests to `securityIncident.test.ts` (sections H–K, tests 34–54, real disposable-Postgres-backed): 2-way and 3-way concurrent-lifecycle races (exactly one winner, no contradictory/duplicate activity, loser always `concurrent_transition`); two independent 20-concurrent-call stress runs (exactly 1 row / 1 `created` activity / `occurrenceCount` exactly 20 / correct highest-severity and timestamp invariants each); a 20-concurrent-call post-terminal-incident recurrence stress test (exactly 1 new dated incident, 1 `created` activity, `occurrenceCount` 20, original terminal row untouched); operator-text sanitization (raw email/phone/bearer-token/password rejection, control-character stripping, oversized-input bounding, end-to-end DB-row verification that raw values never persist); metadata-allowlist tests (unknown-key and nested-object rejection, per-rule field retention, denylist-as-defense-in-depth, bounding). Re-ran the full validation from a fresh disposable `postgres:16-alpine` container: `prisma validate`/`generate` clean, all 61 migrations applied via `prisma migrate deploy`, `prisma migrate status` clean; backend `typecheck` clean; `test:security-incidents` 52/52, run twice with identical results; `test:auth` 39/39; `test:roles` 142/142; `test:ops` 65/65; `test:kvkk-lifecycle` 110/110; `test:msg-safety` 36/36; `test:clinic-bulk-export` 116/117 (the same pre-existing Windows `core.autocrlf=true` CRLF issue, re-confirmed reproducing identically against a fresh, unmodified `origin/main` worktree of the same file); full `npm test` ran every suite up to that same single pre-existing failure with no other failures (the script is `&&`-chained and this file was not touched by this remediation); frontend `npx tsc -b` and `npm run build` both clean; all test-created rows confirmed removed from the disposable database afterward via direct row-count query. PR #167 remains **open** and **In progress** — this remediation is not itself a merge or deployment event, and no production deployment was performed | Automated typecheck/build + disposable-Postgres migration + full/focused/concurrency test-suite verification (this session; concurrency-sensitive suite run twice); no production/browser verification |
| 2026-07-18 | KVKK-CRIT-003 | In progress (all four review blockers remediated, PR open) | Merged — deployment and production verification pending | PR #167, merged into `main` at commit `368bcc8d0a9f4c0ea185ca33d4dd1193d8def9ef` | PR #167 (KVKK-CRIT-003 security incident response foundation and baseline alerting) merged into `main`. This is a merge event only: the VPS is currently inaccessible, so no deployment was performed and no production/browser verification has taken place. All prior validation (disposable-Postgres migration, full/focused/concurrency test suites, backend/frontend type checks) remains as recorded in the two rows above and was performed on a development machine, not in production. The Platform Admin security-incident dashboard, detection rules, and lifecycle API are present in `main`'s source but have not been confirmed running against the production database or production traffic. Status renamed from "In progress" to "Merged — deployment and production verification pending" to reflect that the code is now in `main` while explicitly preserving that deployment and production verification remain outstanding; this status must not be read as, and does not constitute, production deployment or completion | Code merge confirmed via repository inspection of `main` at the stated commit; no production/browser verification performed |
| 2026-07-18 | KVKK-HIGH-007 (new) | Not started | In progress — technical implementation complete, pending review, merge, deployment, production verification, and legal-policy validation | branch `feature/kvkk-high-007-communication-consent-management`, based on `main` at commit `368bcc8d0a9f4c0ea185ca33d4dd1193d8def9ef` (post PR #167 merge) | Added the KVKK-HIGH-007 TECHNICAL foundation for centralized, auditable, channel-specific communication preference and consent management. This is a technical compliance control only — it does not constitute legal approval or complete KVKK/İYS compliance; legal basis, mandatory notice wording, retention periods, marketing-consent requirements, İYS obligations, and controller/processor responsibilities remain separate, outstanding legal dependencies. New `PatientCommunicationPreference` (current-state, one row per patient+clinic+channel+purpose via a `@@unique` compound constraint, updated only through atomic `upsert`) and `PatientCommunicationConsentEvent` (append-only immutable history: channel, purpose, status [granted/denied/withdrawn/unknown/not_required], effectiveAt/grantedAt/withdrawnAt/expiresAt, source, evidenceType, notice/policy version, actorUserId/actorPlatformAdminId, HMAC-SHA256-hashed IP/user-agent, externalProviderRef, sanitized/bounded notes) models, migration hand-verified to introduce zero drift beyond `main`'s existing pre-existing drift. Central decision service `evaluateCommunicationPermission()`/`assertCommunicationPermission()` (`communicationConsentPolicy.ts`) returns a structured `{allowed, reasonCode, channel, purpose, effectiveStatus, preferenceId?, consentEventId?, evaluatedAt}` from a closed reason-code list, fails closed on unknown/marketing status, logs no PII. Wired additively (existing legacy gates such as SMS's `evaluateSmsConsent()` and the narrower Meta-scoped `ChannelConsentLog`/`channelConsentGate.ts` left unchanged) into SMS send (`smsService.ts`), and into all four WhatsApp dispatchers (`whatsappOutboundMessaging.ts`: proactive, no-show recovery, appointment confirmation, post-treatment) via `checkWhatsAppConsent()`, with call sites updated in `postTreatmentMessaging.ts`, `noShows.ts`, and `jobs/reminders.ts` (appointment reminders and payment reminders); `appointmentRequestNotification.ts` received the plumbing (optional `organizationId`/`patientId` params) but its own callers are not yet updated — a documented, known partial-integration gap, not a silent bypass. Re-evaluation happens at send time, not enqueue time; denied sends use a distinct `blocked_by_consent` status, never a provider-failure status. Enforcement is gated by `COMMUNICATION_CONSENT_ENFORCEMENT_ENABLED`/`COMMUNICATION_CONSENT_ENFORCEMENT_MODE` (disabled/audit/enforce), defaulting to disabled and NOT enabled in production by this change. A dedicated `COMMUNICATION_CONSENT_EVIDENCE_HASH_SECRET` (never reusing `SECURITY_SIGNAL_IP_HASH_SECRET`) gates evidence hashing, fail-closed in production. Non-destructive legacy backfill script (`backfillCommunicationPreferences.ts`) is dry-run by default, `--execute`-gated, idempotent, and only ever writes `withdrawn` rows for patients with `smsOptOut=true` — it never infers or auto-grants consent from silence; not executed against production data. Clinic-scoped, role-gated (excluding BILLING) REST API (`communicationPreferences.ts`: get/update/bulk/withdraw/record/export/aggregate) with stable error codes. Frontend "Communication Preferences / İletişim Tercihleri" Patient Detail tab (`CommunicationPreferencesPanel.tsx`) with the required TR/EN (plus FR/DE) warning banner stating this is a technical communication-preference record and not a legal determination, confirmation-required actions, no grant-all shortcut, no optimistic updates. New `communicationConsent` i18n namespace (tr/en/fr/de). Tests: 31 backend consent-policy/enforcement tests + 7 backend backfill-script tests (both against real disposable PostgreSQL) + 13 frontend matrix-helper tests, all 51 new tests passing; full backend suite 116/117 (the 1 failure is the pre-existing, unrelated Windows `core.autocrlf=true` CRLF issue in `test:clinic-bulk-export`, confirmed via `git diff --name-only main...HEAD` to be untouched by this branch); backend/frontend type checks and frontend `vite build` clean; migration validated against a fresh disposable Postgres database with no new drift. See `docs/compliance/56-kvkk-communication-preference-and-consent-management.md`. Not merged, not deployed, enforcement not enabled anywhere; PR not yet opened as of this row | Automated typecheck/build + disposable-Postgres migration + full/focused test-suite verification only (this session); no production/browser verification |

| 2026-07-18 | KVKK-HIGH-007 | In progress — technical implementation complete, pending review | In progress — code review blockers remediated same day, pending further review (PR #169 still open, not merged/deployed) | PR #169, branch `feature/kvkk-high-007-communication-consent-management` (open, not yet merged) | Code review of PR #169 found 5 blockers plus 4 additional-review items before merge could be considered; all remediated same day. **(1) Scope-before-exception ordering:** `evaluateCommunicationPermission()` previously checked the policy-exception branch (`transactional`/`legal_notice`/`security_notice`) before resolving the patient/organization/clinic scope, so a missing patient or cross-org/cross-clinic identifier could receive an `allowed` decision for those purposes. Scope validation now runs first; the exception branch is reachable only once scope passes. **(2) Mandatory WhatsApp consent context:** `checkWhatsAppConsent()` previously returned "proceed" whenever `organizationId`/`patientId`/`consentPurpose` was omitted, regardless of rollout mode, and all 4 public WhatsApp dispatchers declared these fields optional — a caller could bypass audit/enforcement by omitting one field, including once a future switch to `enforce` mode. These 3 fields are now mandatory at the TypeScript boundary (mirroring `smsService.ts`'s `SendClinicSmsArgs`, which has always required them); a defensive runtime check adds mode-specific behavior (disabled: unchanged; audit: logs an explicit safe observation, never blocks; enforce: blocks before any connection/provider call with a new `COMMUNICATION_CONSENT_CONTEXT_REQUIRED` code, never classified as a provider failure). The one real caller gap (`appointmentRequestNotification.ts` → `routes/appointmentRequests.ts`'s approval flow) is now fully wired — no patient-facing caller of the four dispatchers remains unplumbed. **(3) Deterministic concurrent history:** `setCommunicationPreference()` read the current row, then upserted, with no lock — Postgres serialized the upsert but not the read, so recorded `previousStatus` values were not authoritative under concurrency. Fixed with a `pg_advisory_xact_lock` (SHA-256-derived, domain-separated key) as the first statement in the transaction, plus a new monotonic `revision` integer column on both `PatientCommunicationPreference` and `PatientCommunicationConsentEvent` (added to the still-unmerged, not-yet-deployed migration in place, since these tables exist only in this unreleased PR) as the authoritative chain order — `createdAt` alone was insufficient (millisecond precision, can tie under fast concurrency). The transition timestamp is now computed after the lock is acquired, not before. Verified with 20 concurrent mixed grant/deny/withdraw calls against both an existing key and a brand-new key, each run twice: exactly one current row, contiguous gap-free revisions, `event[n].previousStatus === event[n-1].newStatus`, current-row revision equals the final event's revision, no unhandled rejection. **(4) Timestamp policy:** replaced `undefined` (a Prisma no-op leaving stale DB values) with explicit `null` for fields that must clear on a transition (`denied`/`unknown` now null both `grantedAt`/`withdrawnAt`; `granted` nulls `withdrawnAt`; `withdrawn` sets `withdrawnAt` and preserves the existing `grantedAt` as evidence of the original grant time, or nulls it on a first-ever withdraw with no prior grant). **(5) Notice-version/evidence matrix:** `notice_version_required` was declared as an error code but never enforced. Now enforced for `grant` only: the 5 patient-facing digital sources (`patient_portal`/`public_booking`/`whatsapp`/`sms_keyword`/`email_unsubscribe`) require `noticeVersion`; a `staff`-sourced grant requires a bounded source description in `notes`; `deny`/`withdraw`/`reset` never require either, so an opt-out is never blocked on paperwork; only the version/reference string is ever stored, never raw notice text. **Additional review items:** removed the never-populated, misleading `consentEventId` field from the decision contract; added `evaluatedAllowed` to `assertCommunicationPermission()`'s result so audit mode's real decision is never simply discarded; corrected `jobs/reminders.ts`/`routes/noShows.ts`/`services/postTreatmentMessaging.ts` to record a distinct `blocked_by_consent` status (not `'failed'`) for consent-blocked WhatsApp sends, so policy blocks no longer pollute failure-count dashboards; re-confirmed no message body/recipient/template-variable/evidence-note/raw-IP/raw-UA/provider-token is ever logged, including in the new log lines. Backend test count grew from 31 to 89 in `communicationConsent.test.ts` (all passing, concurrency sections run twice); `communicationPreferenceBackfill.test.ts` (7 tests) required a fix for the new required `revision` column on direct `prisma.patientCommunicationConsentEvent.create()` calls in `backfillCommunicationPreferences.ts` (no default on that column; would otherwise have thrown at runtime — caught before any production impact since the script was never executed against production data). Full validation against a fresh disposable `postgres:16-alpine` container: `prisma validate`/`generate` clean, all 62 migrations applied via `prisma migrate deploy` with zero errors, `prisma migrate status` clean; backend `typecheck` clean; every required focused suite passed (`test:communication-consent` 89/89 run twice, `test:communication-consent-backfill` 7/7, `test:auth` 39/39, `test:roles` 142/142, `test:msg-safety` 36/36, `test:whatsapp`, `test:sms` 77/77, `test:post-treatment` 16/16, `test:no-shows` 110/110, `test:purpose` 17/17, `test:contact-requests` 33/33, `test:kvkk-lifecycle` 110/110, `test:security-incidents` 52/52); full `npm test` ran every suite up to and including `test:clinic-bulk-export`, which halted the `&&`-chain on the same single pre-existing, unrelated failure documented in the KVKK-CRIT-003 rows above (`status DTO never serializes sensitive fields` — a brittle exact-source-text-match test; confirmed byte-identical to `origin/main` at `131c7cc` via `git diff origin/main`, i.e. not a regression); frontend `test:communication-consent-matrix` 13/13, `npx tsc -b` clean, `npm run build` clean. Current `main` has advanced to `131c7cc` (PR #168, docs-only) since this branch's base — not merged into this branch, since there is no file overlap and no conflict. Three unrelated items noted during review (CSP blocking `fonts.googleapis.com`/an inline script; Platform Admin's unnecessary `/api/auth/me` 401) were explicitly left out of this PR as separate follow-up items, per the review's own instruction not to broaden this PR's scope. PR #169 remains **open**; nothing merged or deployed | Automated typecheck/build + disposable-Postgres migration + full/focused test-suite verification (this session; concurrency-sensitive sections run twice); no production/browser/legal verification |

| 2026-07-19 | KVKK-HIGH-007 (follow-up) | Merged into `main` (PR #169, commit `7fcf2f8`) — enforcement/reconciliation still disabled, migration applied, backfill not executed | Follow-up implemented on branch `feature/kvkk-high007-consent-reconciliation-ux`, based on `main` at `d9fc408` (post PR #171) — technical implementation complete, not merged | branch `feature/kvkk-high007-consent-reconciliation-ux`, PR not yet opened as of this row | **Record correction:** PR #169 (the row above, and the `In progress`/`not yet merged` language in the two rows above it) is now stale — `feat(kvkk): add centralized communication preference and consent management (#169)` is confirmed merged into `main` at commit `7fcf2f8`; production has the migration applied, `COMMUNICATION_CONSENT_ENFORCEMENT_ENABLED=false`, `_MODE=disabled`, evidence-hash secret configured, and production backfill NOT executed. This row is not a rewrite of the rows above (per this table's append-only rule) — it is the next chronological entry, correcting the record going forward. **This follow-up's scope:** production surfaced a real UX contradiction (the legacy "Onaylar ve Kaynak" sidebar card showing "Hayır" while the new matrix showed "Bilinmiyor" for the same patient) that traced back to a genuine, deeper defect: `smsService.ts` and `recallCandidateService.ts` each run their own legacy consent gate (`Patient.communicationConsent`/`marketingConsent`/`smsOptOut`) completely independently of the central decision service, with the legacy gate running first, unconditionally, and returning immediately on failure — so an explicit central `granted` row had **zero effect** on either sender, not merely a cosmetic UI mismatch. Fixed with a single new orchestration function, `resolveCommunicationConsent()` (`legacyReconciliationResolver.ts`), that both senders now call instead of chaining two independent checks; gated by a new, independent, default-`false` flag `COMMUNICATION_CONSENT_LEGACY_RECONCILIATION_ENABLED` (separate from the existing enforcement flag) so today's production behavior is byte-for-byte unchanged until an operator deliberately turns it on. Full reconciliation mode matrix (reconciliation × enforcement, all 6 combinations) documented and tested. A stale legacy hard-veto (`smsOptOut=true`, itself a dormant field with no write path anywhere in the app) disagreeing with an explicit central grant is classified as a new `legacy_central_conflict` state — fails closed, never auto-resolved in either direction, recorded via an atomic Postgres upsert into a new, patient-identifier-free aggregate table (`CommunicationConsentConflictBucket`, concurrency-tested: 20 parallel identical detections produce exactly one row with `occurrenceCount===20`) rather than a process-local mechanism (rejected because NoraMedi runs multiple API/worker instances). Also fixed: `POST /api/messages/:id/send`'s WhatsApp branch previously called `sendWhatsAppMessage()` with zero consent context of any generation (the one gap among the app's WhatsApp senders) — now gated via `assertCommunicationPermission` with a new template-purpose mapping; the matrix endpoint's N+1 (5×12 cells, ~120 redundant per-cell DB queries) fixed by extracting a pure, I/O-free decision function and evaluating in memory from the already-loaded preference rows; the history endpoint gained `status`/`source` filters and bounded `limit`/`cursor` pagination. Bounded, deterministic, PII-free audit-mode observability added: per-evaluation (not per-clinic/channel/purpose/hour) deterministic sampling (FNV-1a hash, dedicated `COMMUNICATION_CONSENT_AUDIT_SAMPLE_SALT`, samplingVersion-tagged so historical rows stay interpretable across algorithm changes), a bounded audit-summary report (mandatory ≤90-day range, Postgres-side aggregation only, `OWNER`/`ORG_ADMIN`-only route), and the backfill script gained an additive, always-read-only `--report` mode classifying legacy/central conflicts into named categories (`legacy_opt_out_vs_central_granted`, `legacy_false_or_default_vs_central_unknown` — deliberately never described as an explicit denial or agreement, `legacy_yes_without_evidence`, `already_reconciled`) plus a real `ChannelConsentLog` aggregate summary (not joined against `Patient` — that model has no `patientId` FK, only a raw `contactIdentifier`). Frontend `CommunicationPreferencesPanel.tsx` redesigned: one authoritative summary bar, channel-tabs + purpose-group-accordion default view (no default horizontal scroll) with an explicit Matrix-view toggle for power users, a single per-cell "Yönet" action menu (Grant/Deny/Withdraw/Reset) replacing three permanently-visible inline links, a distinct non-actionable "Gerekli Değil" treatment and a distinct "Manuel İnceleme Gerekli" (conflict) treatment, and — closing the production UX defect directly — the legacy sidebar card is **deleted** (desktop and mobile), replaced by a collapsed, `canManage`-only "Eski Kayıt Sinyalleri" disclosure inside the Communication tab that explicitly states these fields do not reflect current consent status. Two small additive migrations (the new conflict-bucket table + an `OperationalEvent` index) — hand-verified against a fresh disposable Postgres to contain **only** these additive changes after `prisma migrate dev` initially produced unrelated pre-existing drift (FK/index/default changes from a Prisma-version mismatch between the checked-in migration history and the installed CLI) that was deliberately discarded rather than shipped. Tests: 4 new backend suites (`legacyReconciliationResolver.test.ts` 30, `communicationConsentAuditReport.test.ts` 7, `communicationPreferencesRoute.test.ts` 6, `messagesConsentGate.test.ts` 4, `recallConsentGate.test.ts` 4, `communicationPreferenceReconciliationReport.test.ts` 9 — this repo has no HTTP-test-server library anywhere, so route-level tests extract the Express handler directly from the router's stack, consistent with the project's established no-supertest convention) plus 5 new frontend pure-logic tests, all passing; all pre-existing communication-consent/backfill/SMS/no-show/post-treatment/outbound-WhatsApp/data-retention suites re-run and still passing unmodified. This repo has no React render-testing infrastructure (no RTL/jsdom/vitest); rather than introduce one solely for this component, its conditional rendering logic was extracted into pure, directly-tested functions, and live rendering was verified in an actual browser (a real register→login→create-patient→open-Communication-tab flow via a locally-driven Edge browser against the dev server) at 320/375/768/1024/1440px — no required horizontal scroll at any width, legacy card confirmed absent, summary/accordion/matrix-toggle/action-modal/legacy-disclosure all functioning; the only console error observed was the pre-existing, already-documented Platform-Admin `/api/auth/me` 401 (unrelated, not introduced by this work). This remains a technical control only — does not claim KVKK/İYS/marketing-consent legal compliance; enforcement, the new reconciliation flag, and audit logging are not enabled anywhere; production backfill (dry-run or execute) was not run; all of §15's legal-review items in `docs/compliance/56-kvkk-communication-preference-and-consent-management.md` remain open | Automated typecheck/build + disposable-Postgres migration + full/focused test-suite verification + live browser verification (this session); no production deployment or legal verification |

| 2026-07-20 | KVKK-HIGH-008 | Merged, not deployed | **Merged and deployed to production, technically verified** | PR #180, merged into `main` at `e972bfef918471074137bb0f11705d43a1ca2ce5` (ancestor of production `HEAD` `87b7dcd9bfe5d27956e32e8b61590667e36bac86`) | User-supplied, read-only production evidence, gathered after a pre-deployment backup (PostgreSQL custom-format dump + `server/.env` backup, SHA256-verified, `pg_restore --list`-validated, `root:root 700`/`600` ownership/modes): production `HEAD` confirmed `87b7dcd9bfe5d27956e32e8b61590667e36bac86`, worktree clean; 64 migrations found, `20260719155318_kvkk_high008_legacy_consent_correction` applied successfully, `prisma migrate status` reports the schema up to date (this also confirms KVKK-HIGH-007's own migration, `20260719120821_kvkk_high007_consent_reconciliation`, is applied); `public."PatientLegacyConsentCorrection"` table and `PatientLegacyConsentField`/`SMS_OPT_OUT` enum confirmed present, correction-row count **0**; all four `COMMUNICATION_CONSENT_*` flags (`ENFORCEMENT_ENABLED=false`, `_MODE=disabled`, `LEGACY_RECONCILIATION_ENABLED=false`, `AUDIT_LOGGING_ENABLED=false`) confirmed still fail-safe disabled, no production reconciliation backfill run; backend validation (`npm ci`, 0 vulnerabilities, Prisma Client v7.8.0, schema validation, typecheck) and frontend validation (production build, Vitest 2 files/26/26 passed, incl. `communicationConsentMatrixHelpers` 29/29 and `patientDetailTabsHelpers` 12/12; React `act(...)` warnings emitted for `LegacyConsentCorrectionModal`/`CommunicationPreferencesPanel` but did not fail tests) both passed; `noramedi-api`/`noramedi-worker` PM2 processes online, five consecutive `/api/health` 200s, restart count stable at 16 (0 unstable), worker rescheduled all jobs, `noramedi.com` 200, `app.noramedi.com` 302→`/login`. One transient ~5s HTTP 502 observed on the first health check after PM2 reload (API does not begin listening until `npx prisma generate` completes — a deployment/readiness reliability finding, not a failed deployment; tracked separately as `docs/program/RISK_REGISTER.md` R-063, not a KVKK-HIGH-008 defect). **This is a technical deployment verification only** — it is not a legal KVKK compliance determination; no correction record was created during verification; no central `PatientCommunicationPreference`/`PatientCommunicationConsentEvent` row was mutated; no reconciliation/backfill/enforcement flag was activated; and no rollback or tenant-impact verification was independently performed (`docs/program/KVKK_ARCHITECTURE_FREEZE_BOUNDARY.md` §5 condition 4, still unsatisfied) or external "KVKK baseline stable" declaration made (condition 5, still unsatisfied). Six further, separately-tracked, non-blocking technical/operational findings from the same production-log review (frontend dependency vulnerabilities, `pg` client deprecation warning, historical `node-cron` missed-execution warnings, missing approved Meta WhatsApp reminder templates — WhatsApp AI JSON-parsing robustness was found via code review, not logs) are recorded as `docs/program/RISK_REGISTER.md` R-064…R-069, each with its own isolated remediation-PR boundary; a normal HTTP 401 after an expired browser session during polling is explicitly **not** classified as a service failure. Separately, a same-day, narrower-pattern repository grep initially found KVKK-HIGH-006 (`req.user.clinicId` reclassification, row above) apparently resolved — **that finding was corrected within this same pass** (before commit) once a broader, equivalent-pattern search (`req.user!.clinicId`, the codebase's dominant idiom) found 47 remaining live runtime authorization/data-scope usages across 10 files; KVKK-HIGH-006 is `STILL_OPEN`, not a documentation-closure item — see the corrected row above. See `docs/program/NORAMEDI_MASTER_TRACKER.md`, `docs/program/KVKK_ARCHITECTURE_FREEZE_BOUNDARY.md`, and `docs/program/KVKK_HIGH008_FREEZE_BOUNDARY.md` §6 for the full program-level reconciliation | User-supplied, read-only production command output (operator) — this document's own review did not independently re-run production commands, only recorded the supplied evidence |
| 2026-07-21 | KVKK-HIGH-006 | `STILL_OPEN`/`READY_FOR_SCOPING` | `STILL_OPEN`/`READY_FOR_DETAILED_SCOPING` — wording sharpened, not resolved | Docs-only reconciliation, branch `fix/kvkk-high006-default-clinic-scope`, base `origin/main` @ `7174f3bb3518bca6b2640694ee2f9264c293318f` (no drift) | A separate, narrower `defaultClinicId` (nullable UI/navigation-preference field) authorization-path review, conducted earlier under this same task ID by mistake, is reconciled here as its own subordinate task, **KVKK-HIGH-006-S1** ([evidence/KVKK-HIGH-006-S1_DEFAULT_CLINIC_ID_AUTHORIZATION_PATH_VERIFICATION.md](evidence/KVKK-HIGH-006-S1_DEFAULT_CLINIC_ID_AUTHORIZATION_PATH_VERIFICATION.md), status `VERIFIED_NO_VIOLATION_FOUND`) — it does not close, satisfy, or narrow this KVKK-HIGH-006 row. The row above's severity wording is sharpened (see corrected row, top of Section 8): `reports.ts:405` and `appointmentRequests.ts:192` are restated as authorization-consistency/branch-selection-narrowing findings rather than "zero scope validation"/"unvalidated PHI write" implying attacker-controlled cross-tenant access — no such access was demonstrated for either route, `req.user.clinicId` being server-resolved and never client-supplied per request. Both remain high-priority remediation candidates because they bypass the accepted centralized scope contract on PHI-bearing routes. A fresh, independent re-search of `req\.user(!\|\?)?\.clinicId` (64 non-test matches) is reconciled against the existing 93-occurrence/47-live-runtime classification — found consistent once the same file buckets are applied, not contradictory; the count is **not** changed to 64, and 93/47 is **not** claimed as freshly independently re-verified line-by-line. Next task defined as **KVKK-HIGH-006-S2** (see corrected row above). No runtime code, test, schema, or migration file was changed by this reconciliation | Read-only repository review only (this session); no production access, no code change |

| 2026-07-21 | KVKK-HIGH-006-S2 | (new task, no prior status) | `AGENT_COMPLETED` — occurrence classification and remediation plan complete; documentation-only, not committed, no PR | Isolated worktree/branch `docs/kvkk-high006-s2-occurrence-classification`, base `origin/main` @ `447ef6e72b2884ca932f813f85469c1e7133538d` (no drift) | Independently reproduced and classified every one of the 93 raw / 67 non-test `req.user(!\|?)?.clinicId` occurrences across 17 non-test files (4 more files discovered than the 13 previously named — `attachments.ts`, `gdprExport.ts`, `labOrders.ts`, `clinicBulkExport.ts`, all 4 resolving to comment-only/false-positive, zero remediation needed). Reconciled the remediation-required count to exactly **47**, resolving the prior document's own internal arithmetic ambiguity (the per-file list in the row above sums to 46 against a stated 47 headline) by confirming `reports.ts` genuinely has **2** occurrences requiring remediation — `:73` (a raw-SQL byPeriod-breakdown scope-translation gap that silently narrows an org-wide `'all'`-scoped revenue report to one clinic, previously flagged only as "1 requiring manual review") and `:405` (the no-show-analysis route, which has no clinic selector or `'all'` support at all, unlike its 4 sibling routes in the same file) — not 1. All other 9 files' prior per-file counts are independently reproduced exactly. The "10 safe non-authorization usages" bucket is independently re-verified occurrence-by-occurrence (not merely accepted): confirmed correct — none of the 10 (`dashboard.ts:249`, `organizationBranches.ts:668`, `organizationWhatsApp.ts` ×7) gate a Prisma `where`/`data` clause. **Zero occurrences in the entire 93-match inventory rise to a genuine tenant-isolation or cross-organization risk** — `req.user.clinicId` is server-resolved once at authentication time and never re-read from client input per request in any of the 17 files, so no client-controlled value reaches an unvalidated scope decision anywhere; every confirmed defect is access-narrowing (a legitimately multi-clinic/org-wide-authorized user silently restricted to one clinic) or a consistency gap (`reports.ts:73`'s internally-inconsistent org-wide response; `middleware/planLimits.ts:64,86`'s no-plan-organization quota-scope ambiguity, which requires a product decision before any fix and is explicitly left unresolved by this document). Defined a 4-batch remediation plan (Batch 1 — `reports.ts`/`appointmentRequests.ts`/`dentalChart.ts`, the highest-priority PHI/clinical/reporting paths this document has named since 2026-07-20; Batch 2 — `paymentPlans.ts`/`inventory.ts`/`insuranceProvisions.ts`; Batch 3 — `messages.ts`/`postTreatment.ts`/`services.ts`; Batch 4 — `middleware/planLimits.ts`, gated on a prerequisite product decision, plus the already-confirmed-safe dashboard/branches/WhatsApp occurrences), each with a 12-case test plan (single-clinic user, multi-clinic explicit-allowed, multi-clinic disallowed, OWNER/ORG_ADMIN all-clinic, cross-org denial, missing/invalid selector, inactive clinic, record-owned-clinic mismatch, PHI/financial/clinical mutation scope, backward compatibility, 403-vs-404). No schema/migration required for any of the 47. **KVKK-HIGH-006 remains `STILL_OPEN`/`READY_FOR_DETAILED_SCOPING` — this is a classification/planning deliverable only; zero of the 47 occurrences have been fixed, no code changed, no test run, not committed, no PR opened.** Full detail: [evidence/KVKK-HIGH-006-S2_REQ_USER_CLINIC_ID_OCCURRENCE_CLASSIFICATION.md](evidence/KVKK-HIGH-006-S2_REQ_USER_CLINIC_ID_OCCURRENCE_CLASSIFICATION.md). Next task: **KVKK-HIGH-006-S3** — implement Batch 1 as an isolated, reviewable PR with independent (not author-only) test verification | Read-only repository review only (this session); no production access, no code change |

| 2026-07-21 | KVKK-HIGH-006-S2 | `AGENT_COMPLETED` — occurrence classification and remediation plan complete; documentation-only, not committed, no PR | `AGENT_COMPLETED`; documentation committed and PR'd — commit `d9176e1f2acb5625ce8d94017ab96bbe283be518`, PR **#191** `OPEN`/`MERGEABLE`/`CLEAN`, not merged | branch `docs/kvkk-high006-s2-occurrence-classification`, PR [#191](https://github.com/MustafaBasol/DisKlinikCRM/pull/191), base `main` | Post-PR status reconciliation: the S2 occurrence-classification-and-remediation-plan documentation recorded in the row above is now committed at `d9176e1f2acb5625ce8d94017ab96bbe283be518` and pushed to branch `docs/kvkk-high006-s2-occurrence-classification`; **PR #191** is open (`state: OPEN`, `mergeable: MERGEABLE`, `mergeStateStatus: CLEAN`), based on `main`, not yet merged. This is a documentation-only change — no runtime code, test, schema, or migration file was modified, and none was run. **KVKK-HIGH-006 remains `STILL_OPEN`/`READY_FOR_DETAILED_SCOPING`; R-071 remains `OPEN`.** No remediation has been implemented and no risk reduction is claimed by this row. NEXT_AFTER_MERGE: **KVKK-HIGH-006-S3** — implement Batch 1, once PR #191 merges | PR #191: [https://github.com/MustafaBasol/DisKlinikCRM/pull/191](https://github.com/MustafaBasol/DisKlinikCRM/pull/191) (state `OPEN`, mergeable `MERGEABLE`, mergeStateStatus `CLEAN`); commit `d9176e1f2acb5625ce8d94017ab96bbe283be518` |
| 2026-07-21 | KVKK-HIGH-006-S2 | `AGENT_COMPLETED`; PR **#191** `OPEN`/`MERGEABLE`/`CLEAN`, not merged | **`MERGED`** — self-reference-lag correction | `git log --oneline --all` confirms PR #191's merge commit `548bcb7c81065fc71474fdff3632f4759528ffd3` is the current tip of `origin/main` | **Self-reference-lag correction (same pattern this document and program have corrected repeatedly):** the row above's own committed snapshot predates PR #191's own merge and still reads "not merged." PR #191 is confirmed `MERGED`. Runtime remediation has still not started; **KVKK-HIGH-006 remains `STILL_OPEN`/`READY_FOR_DETAILED_SCOPING`; R-071 remains `OPEN`**; zero of the 47 occurrences are fixed. NEXT_AFTER_MERGE condition is now satisfied — KVKK-HIGH-006-S3 (Batch 1 implementation) is ready to start | `git rev-parse origin/main` = `548bcb7c81065fc71474fdff3632f4759528ffd3`; found by task KVKK-HIGH-008-F1-PBV-S1 |
| 2026-07-21 | KVKK-HIGH-008-F1-PBV-S1 (new task, no prior status) | (new task, no prior status) | `AGENT_COMPLETED` — read-only feasibility analysis complete; documentation-only, not committed, no PR | Isolated worktree/branch `docs/kvkk-high008-pbv-feasibility-evidence`, base `origin/main` @ `548bcb7c81065fc71474fdff3632f4759528ffd3` (PR #191's own merge commit, no drift) | **Safe Behavioral Production Verification Feasibility Review** for KVKK-HIGH-008-F1's residual R-061 gaps. Confirmed by direct source read (`server/src/routes/communicationPreferences.ts:119-134,560-624`): the disabled mutation route's `loadScopedPatient()` (line 567) executes and can already 404 a nonexistent/out-of-organization patient before the runtime gate check (`isLegacyConsentCorrectionRuntimeEnabled()`, line 574) is ever reached; the two read/history routes (lines 626-684) never check the gate at all. **Gaps 1-3** (disabled-route fail-closed behavior, exact response contract, read/history behavior while disabled — all as live-observed facts) therefore cannot be exercised with a synthetic/nonexistent patient identifier and require a real, in-scope production patient — **not authorized** by this task. Confirmed by direct source read (`server/src/services/platformSettings.ts`, `server/src/routes/platformAdmin.ts:1091-1134`): **gaps 4-8** (successful `PlatformSetting` write, `PlatformAdminAuditEvent` creation, actor attribution, previous/new values, zero-`SecuritySignalEvent`, audit-content safety) are technically exercisable only via `PATCH /api/platform/privacy/legacy-consent-correction/settings` with `{ runtimeEnabled: false }` — but no `deletePlatformSetting`/unset function exists anywhere in the repository (`setPlatformSetting()` always `upsert()`s), so this PATCH would permanently move production from "row absent, effective fail-closed false" (the current safe state) to "row present with value `false`," with no supported audited path back — classified technically feasible but **NOT AUTHORIZED**, because it is not a reversible test. **Gap 9** (controlled activation, `runtimeEnabled: true`) remains **NOT AUTHORIZED**, unchanged from the existing R-061/`KVKK_HIGH008_FREEZE_BOUNDARY.md` §8 decision. `docs/program/RISK_REGISTER.md` R-061 **remains `OPEN`** — not `MITIGATED`/`CLOSED`/`RESOLVED`/`PRODUCTION_VERIFIED`. No production connection, no production endpoint call, no production PATCH, no feature activation, no production verification, and no code/test/schema/migration change occurred. Full evidence: `docs/program/evidence/KVKK-HIGH-008-F1-PBV-S1_SAFE_BEHAVIORAL_PRODUCTION_VERIFICATION_FEASIBILITY.md`. Documentation prepared but **not committed, not pushed, no PR opened** — stops here for external review. Exact next task: **KVKK-HIGH-006-S3** — Batch 1 implementation, now that PR #191 has merged (see row above); the KVKK-HIGH-008 production PATCH analyzed here remains a separately deferred human decision, not made automatic by this row | Read-only repository source review only (this session); no production access, no code change, no test run |

Every future status change adds a new row here. Do not rewrite or delete existing rows.

---

## 9. Next recommended remediation item

**KVKK-CRIT-001a and KVKK-ATTACH-IMAGING-001 are now Completed and verified** (see Sections 6.1/6.1.2 and 6.2/6.2.1) — production-deployed and production-verified on their technical/evidence sub-items; only their documented legal/infrastructure dependencies remain, not further technical implementation.

**KVKK-HIGH-004 is now Implemented — awaiting deployment/operational verification** (branch `feature/kvkk-high-004-secure-clinic-bulk-export`, see `docs/compliance/54-kvkk-secure-clinic-bulk-export.md`). The legacy `GET /api/clinic/export-data` endpoint now always returns `410 CLINIC_BULK_EXPORT_LEGACY_DISABLED` and touches no data; the new secure flow is feature-flagged off by default (`CLINIC_BULK_EXPORT_ENABLED` must be explicitly `true`), OWNER/ORG_ADMIN-only, multi-branch-scope-validated, step-up authenticated with a Postgres-authoritative brute-force lockout, rate-limited/concurrency-guarded via both an advisory-lock reservation transaction and a database partial unique index, generated asynchronously by a background worker with bounded concurrency, and downloaded via a one-time hashed token. This status will only move to **Completed and verified** after the flag has actually been enabled and exercised in production per Section 7's rules — do not enable `CLINIC_BULK_EXPORT_ENABLED=true` in production without following the enablement checklist in `docs/compliance/54-kvkk-secure-clinic-bulk-export.md`.

~~**Next engineering control (highest priority): KVKK-HIGH-007 — Normalized `CommunicationPreference` model (channel+purpose) + migration of `smsOptOut` to `channel=sms` scope.**~~ **Stale as of 2026-07-20 — superseded below.** KVKK-HIGH-007 (base + follow-up) and its direct continuation KVKK-HIGH-008 are both merged and now confirmed deployed to production (see the 2026-07-20 change-history row above). Rationale for the original recommendation (KVKK-HIGH-004 prioritized first for its standing bulk-export exposure risk; `smsOptOut` requiring a channel-scoped model, not a blanket reinterpretation) is preserved below as historical context, not current guidance.

**2026-07-20 update — no new, unblocked Phase-0 remediation *code* item remains.** A fresh review of this document plus current repository code (not just prior documentation claims) against the four previously-open technical candidates found:

- **KVKK-HIGH-001** (attachment encryption) — still `Waiting for operational/infrastructure evidence` (Section 4); genuinely blocked, not re-evaluated by this pass.
- **KVKK-HIGH-003** (medical-record retention) — still `Waiting for legal review`; genuinely blocked, not re-evaluated by this pass.
- **KVKK-HIGH-005** (İYS integration) — re-confirmed `N/A`: a fresh grep of `server/src` for marketing/campaign/broadcast/bulk-send code paths found none; still no active marketing-send path exists.
- **KVKK-HIGH-006** (reclassify `req.user.clinicId` usages) — **`STILL_OPEN`, corrected within this same pass**: an initial grep for only the literal string `req.user.clinicId` found 29 matches/11 files, all comments/tests, and was read as "already resolved." A broader, equivalent-pattern search (`req.user!.clinicId`, the codebase's actual dominant idiom) found **47 live runtime authorization/data-scope usages across 10 files**, none of which have the codebase's own validated-scope helper (`validateAndGetClinicIdScope`/`validateAndGetScope`) applied. This is an open remediation item, not a documentation-closure task — see the corrected row above.

With HIGH-001/003 externally blocked and HIGH-005 confirmed still not applicable, **KVKK-HIGH-006 is a ready, unblocked, new Phase-0 remediation *code* item as of 2026-07-20** (corrected from the prior, same-day "no item remains" finding, which rested on HIGH-006's now-superseded "already resolved" claim). The concrete next KVKK-relevant work is: (0) a bounded remediation PR migrating the 10 files identified above to `validateAndGetClinicIdScope`/`validateAndGetScope`, prioritizing `reports.ts:405` (no scope validation at all) and `appointmentRequests.ts:192` (unvalidated PHI-record creation); alongside the pre-existing verification tail for the merged/deployed KVKK-HIGH-007/HIGH-008 items: (1) independently execute the KVKK-HIGH-007/HIGH-008 test suites (currently author-reported only, not CI-covered per `docs/program/RISK_REGISTER.md` R-049) against a disposable database; (2) perform and evidence a rollback/tenant-impact verification pass for both production-applied migrations (`docs/program/KVKK_ARCHITECTURE_FREEZE_BOUNDARY.md` §5 condition 4); (3) obtain an explicit human accept/reject decision on KVKK-HIGH-008's no-feature-flag design (`docs/program/RISK_REGISTER.md` R-061). Items (1)-(3) do not require new remediation code and are prerequisites this document and `KVKK_ARCHITECTURE_FREEZE_BOUNDARY.md` have named since F0-007/F0-011-P1 without yet being executed; item (0) is new, scoped remediation code, separate from and not blocking (1)-(3). KVKK-HIGH-004's flag-enablement checklist (`docs/compliance/54-kvkk-secure-clinic-bulk-export.md`) remains a separate, already-implemented, operator-decision item, not a new code task.

**[Wording correction, 2026-07-21 reconciliation, appended not rewritten]:** the phrases "no scope validation at all" (`reports.ts:405`) and "unvalidated PHI-record creation" (`appointmentRequests.ts:192`) in the paragraph above are preserved as historical text but should be read together with the sharpened wording in the corrected KVKK-HIGH-006 row (Section 8): both routes use the authenticated, server-resolved `req.user.clinicId` directly instead of the centralized scope helper, and neither has been shown to allow attacker-controlled or cross-organization access — `req.user.clinicId` is never client-supplied per request. The confirmed defect is authorization-consistency/branch-selection narrowing on PHI-bearing routes, not a demonstrated cross-tenant leak. Item (0)'s priority ranking is unchanged by this wording correction. See **KVKK-HIGH-006-S1** ([evidence/KVKK-HIGH-006-S1_DEFAULT_CLINIC_ID_AUTHORIZATION_PATH_VERIFICATION.md](evidence/KVKK-HIGH-006-S1_DEFAULT_CLINIC_ID_AUTHORIZATION_PATH_VERIFICATION.md)) for the separately-reconciled, non-closing `defaultClinicId` side-verification, and **KVKK-HIGH-006-S2** (corrected row, Section 8) for the next occurrence-classification/remediation-planning task.

**[KVKK-HIGH-006-S2 complete, 2026-07-21, appended not rewritten]:** item (0) above is now fully scoped. S2 independently reproduced and classified all 93 raw / 67 non-test occurrences (17 files, 4 more than previously named), confirmed the remediation-required count is exactly **47** (`reports.ts` corrected from an ambiguous "1 + manual review" to a confirmed **2** occurrences), independently re-verified the 10-item safe bucket, and confirmed **zero** occurrences anywhere in the inventory rise to a genuine tenant-isolation or cross-organization risk. Item (0) is replaced by a 4-batch plan (Batch 1: `reports.ts`/`appointmentRequests.ts`/`dentalChart.ts`; Batch 2: `paymentPlans.ts`/`inventory.ts`/`insuranceProvisions.ts`; Batch 3: `messages.ts`/`postTreatment.ts`/`services.ts`; Batch 4: `middleware/planLimits.ts`, gated on a prerequisite product decision, plus the already-safe dashboard/branches/WhatsApp occurrences). No code changed by S2. Full detail: [evidence/KVKK-HIGH-006-S2_REQ_USER_CLINIC_ID_OCCURRENCE_CLASSIFICATION.md](evidence/KVKK-HIGH-006-S2_REQ_USER_CLINIC_ID_OCCURRENCE_CLASSIFICATION.md). Next task: **KVKK-HIGH-006-S3** — implement Batch 1.

**[Post-PR reconciliation, 2026-07-21, appended not rewritten]:** S2's documentation (rows above) is now committed at `d9176e1f2acb5625ce8d94017ab96bbe283be518` and pushed; **PR #191** ([https://github.com/MustafaBasol/DisKlinikCRM/pull/191](https://github.com/MustafaBasol/DisKlinikCRM/pull/191)) is open (`OPEN`, `mergeable: MERGEABLE`, `mergeStateStatus: CLEAN`), based on `main`, not yet merged — see the corresponding change-history row in Section 8. **NEXT_AFTER_MERGE: KVKK-HIGH-006-S3** — implement Batch 1, once PR #191 merges; S3 is not `READY_TO_IMPLEMENT` as a merged-baseline task before then. No runtime code, test, schema, or migration file was changed by this reconciliation; KVKK-HIGH-006 remains `STILL_OPEN`/`READY_FOR_DETAILED_SCOPING`; R-071 remains `OPEN`.

---

**Prior recommendation (2026-07-17, preserved for historical context — superseded above):**

Rationale: both are `Not started` Phase 0 items that are purely technical (no legal-counsel dependency, unlike KVKK-CRIT-002/KVKK-CRIT-005/VERBİS/processor-agreement which are `Waiting for legal review`, and unlike HIGH-001 which is `Waiting for operational evidence`). KVKK-HIGH-004 is prioritized first because an unrestricted bulk-export path is a standing exposure risk independent of pilot scale. Per the existing correction-log context (Section 0.1 of the full narrative report), `smsOptOut` must **not** be mechanically interpreted as blocking WhatsApp — the KVKK-HIGH-007 fix requires an explicit channel-scoped preference model, not a blanket reinterpretation. Neither item was started in this task.
