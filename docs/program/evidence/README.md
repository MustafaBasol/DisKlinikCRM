# Evidence Directory — F0 Baseline and Validation

This directory holds task-scoped, reproducible evidence reports produced during Phase F0. Evidence files are **not** the authoritative live-status source — that remains [`NORAMEDI_MASTER_TRACKER.md`](../NORAMEDI_MASTER_TRACKER.md) per its source hierarchy (§2.1). Evidence files back the claims made in the tracker with reproducible locators (repository path, function/constant/script name, line range where practical) and an explicit evidence classification.

## Evidence classifications

| Classification | Meaning |
|---|---|
| `VERIFIED_GIT` | Confirmed directly from local Git metadata (SHA, branch, ancestry, worktree state) |
| `VERIFIED_GITHUB` | Confirmed via authenticated GitHub CLI/API against github.com (PR state, merge commit, branch protection) |
| `VERIFIED_REPOSITORY` | Confirmed by direct inspection of tracked repository files (source, config, scripts, manifests) |
| `OBSERVED_LOCAL_ONLY` | Observed in the local working environment but not confirmed against any external system of record |
| `UNVERIFIED_PRODUCTION` | A production-environment fact that has not been checked against the live VPS; repository evidence may exist but does not by itself prove production reality |
| `VERIFIED_PRODUCTION_OBSERVED` | Confirmed by read-only commands run directly on the production VPS by the user (F0-002 Stage B or later). An observed operational fact, point-in-time — not a `PRODUCTION_VERIFIED` task-status/release-gate claim (see [`NORAMEDI_MASTER_TRACKER.md` §2.2](../NORAMEDI_MASTER_TRACKER.md)) |
| `CONFLICTING_EVIDENCE` | Two or more repository/documentation sources disagree; both are cited, neither is assumed correct |
| `NOT_APPLICABLE` | The row/field does not apply to the current repository state (e.g. a capability that doesn't exist) |

These are evidence-quality labels, not task status values. Task status values (`TODO`, `READY`, `IN_PROGRESS`, `AGENT_COMPLETED`, `REVIEW_REQUIRED`, `MERGED`, etc.) are defined in [`NORAMEDI_MASTER_TRACKER.md` §2.2](../NORAMEDI_MASTER_TRACKER.md) and are never conflated with evidence classifications in these files.

## Files in this directory

| File | Task | Purpose |
|---|---|---|
| [F0-002_REPOSITORY_BASELINE.md](F0-002_REPOSITORY_BASELINE.md) | F0-002 Stage A | Factual, reproducible repository/deployment-capability inventory as of the F0-002 worktree HEAD. §6.9 has been reconciled with Stage B; a few rows remain `UNVERIFIED_PRODUCTION`/`UNVERIFIED` where no evidence was supplied. |
| [F0-002_PRODUCTION_EVIDENCE_REQUEST.md](F0-002_PRODUCTION_EVIDENCE_REQUEST.md) | F0-002 Stage B (input) | Copy-pasteable, read-only command set for the user to run on the production VPS. Reusable runbook — kept for future re-verification (e.g. F0-006). |
| [F0-002_PRODUCTION_BASELINE_EVIDENCE.md](F0-002_PRODUCTION_BASELINE_EVIDENCE.md) | F0-002 Stage B (output) | Sanitized summary of the production/VPS evidence supplied 2026-07-19T13:43:12+03:00 in response to the evidence request above: host, runtime, PM2 topology, health, TLS, database/migrations, configuration presence, storage, backup/PITR/restore-test status, accepted findings, and risks. |
| [F0-006_PRODUCTION_TOPOLOGY_EVIDENCE.md](F0-006_PRODUCTION_TOPOLOGY_EVIDENCE.md) | F0-006 | Source-level process/deployment/config/storage/backup/security tracing (entrypoints, job scheduling, graceful shutdown, storage/backup client code), reconciliation of a second task-supplied production evidence snapshot against F0-002 Stage B, and the required drift/contradiction table. Does not duplicate F0-002's evidence — cites it by section. |
| [F0-006_configuration_inventory.json](F0-006_configuration_inventory.json) | F0-006 | Structured machine-readable inventory: topology, configuration sources/variables, deployment steps, drift findings, risks, unverified fields. |
| [F0-007_KVKK_BASELINE_EVIDENCE.md](F0-007_KVKK_BASELINE_EVIDENCE.md) | F0-007 | Worktree/primary-tree safety record, full GitHub PR verification for all known KVKK PRs (repository-wide sweep found 0 open PRs), three-way migration baseline (repository/production/active-uncommitted), dependency/conflict analysis built from F0-003/F0-004 evidence, and test/acceptance baseline per KVKK task. |
| [F0-007_kvkk_work_inventory.json](F0-007_kvkk_work_inventory.json) | F0-007 | Structured machine-readable inventory: known KVKK tasks/PRs, stable baseline, active-work scope metadata, freeze boundary, parallel-safe/blocked work, conflict risks, unverified items, exit criteria. |
| [F0-014_G0_APPROVAL_F0_CLOSURE_F1_TRANSITION.md](F0-014_G0_APPROVAL_F0_CLOSURE_F1_TRANSITION.md) | F0-014 | Records the externally-granted G0 decision (`APPROVED_WITH_CONDITIONS`, 2026-07-25), F0 phase closure, F1 phase transition, and the F1-001 initial task assignment. **Note:** this index is otherwise stale beyond the rows above (F0-013 itself flagged this in its own §9 item 5) — a full evidence-index refresh remains a separate, non-blocking documentation task, out of F0-014's own narrow scope. |
| [F1-001_IMPACT_TEST_SELECTION_DESIGN_EVIDENCE.md](F1-001_IMPACT_TEST_SELECTION_DESIGN_EVIDENCE.md) | F1-001 (phase F1) | Process/validation record for the impact-based test-selection design: baseline reconciliation, worktree/primary-tree safety, required-document reading, CodeGraph-unavailable fallback and targeted repository inspection, files changed, JSON/diff validation commands, parallel-KVKK-work observation. The design document itself lives at [../architecture/F1-001_IMPACT_BASED_TEST_SELECTION_ARCHITECTURE.md](../architecture/F1-001_IMPACT_BASED_TEST_SELECTION_ARCHITECTURE.md), with machine-readable companions [../architecture/evidence/f1-001-test-scope-classification.json](../architecture/evidence/f1-001-test-scope-classification.json) and [../architecture/evidence/f1-001-impact-selection-rules.json](../architecture/evidence/f1-001-impact-selection-rules.json) — kept in a phase-scoped `architecture/` directory rather than this F0-labeled evidence directory, since this is the first F1-phase design deliverable. |
| [F1-002_TEST_INVENTORY_REFRESH_AND_OWNERSHIP_RECONCILIATION.md](F1-002_TEST_INVENTORY_REFRESH_AND_OWNERSHIP_RECONCILIATION.md) | F1-002 (phase F1) | Process/validation record for the test-inventory refresh that remediates R-072 (F1-001's own discovered evidence-staleness risk): baseline reconciliation (execution baseline `08f2eaf82a205cf3f997c57e6a295fedd66b142d`), worktree/primary-tree safety, CodeGraph-unavailable fallback, exact enumeration commands, ownership/script reconciliation methodology, R-072 closure assessment. Updated 2026-07-28 by F1-002-R1 (§20) to integrate F1-002-P1/P2 below and correct one numeric inconsistency (§14). Machine-readable companions: [F1-002_test_inventory.json](F1-002_test_inventory.json) (137 test/verification targets — **current authoritative inventory**, supersedes [F0-005_test_inventory.json](F0-005_test_inventory.json) as of 2026-07-28 while preserving it for historical lineage), [F1-002_test_script_reconciliation.json](F1-002_test_script_reconciliation.json) (every server/root/bridge-agent test-related package script matched to file(s), 0 stale references found), [F1-002_test_ownership_gaps.json](F1-002_test_ownership_gaps.json) (0 UNKNOWN ownership, 8 never-scripted backend files, R-072 condition-by-condition closure table). |
| [F1-002-P1_TEST_SCRIPT_REACHABILITY_RECONCILIATION.md](F1-002-P1_TEST_SCRIPT_REACHABILITY_RECONCILIATION.md) | F1-002-P1 (phase F1, parallel evidence subtask, `MERGED` PR #255, merge commit `954168ed4399f572e776fbfd30fa391aad574610`) | Programmatic reconciliation of every test-related package script (103: 8 root + 95 server) against the current filesystem: script-to-file matching, full-suite (`server:test`) chain resolution (77 references → 83 distinct files), 17 scripts excluded from that chain, 8 UNSCRIPTED backend files, 0 stale references, CI-workflow cross-reference (only 7 of 116 JS/TS test-related files reached by any workflow). Machine-readable companions: [F1-002-P1_test_script_reconciliation.json](F1-002-P1_test_script_reconciliation.json) (one record per script), [F1-002-P1_test_file_reachability.json](F1-002-P1_test_file_reachability.json) (one record per test file, 116 records). Integrated into `TEST_OWNERSHIP.md` §0.1 and this task's own evidence doc §20 by F1-002-R1. |
| [F1-002-P2_TEST_RUNTIME_DEPENDENCY_BASELINE.md](F1-002-P2_TEST_RUNTIME_DEPENDENCY_BASELINE.md) | F1-002-P2 (phase F1, parallel evidence subtask, `MERGED` PR #254, merge commit `035d4db59cf429777a4bb71a5e72203f6c99b571`) | Runtime-class and disposable-environment baseline across all 137 F1-002 targets (17 test groups): 22 files requiring disposable Postgres, 1 additionally requiring a MinIO/S3-compatible emulator, 0 Redis dependency, 0 live external-provider/network calls, 1 explicit UNKNOWN (`messagesConsentGate.test.ts`), and a 10-question disposable-Postgres-pattern assessment (pattern demonstrated ad hoc 3×, never committed as reusable provisioning; 5 concrete gaps before CI automation; migration-rollback tooling gap cross-referenced to `RISK_REGISTER.md` R-070). Machine-readable companions: [F1-002-P2_test_runtime_requirements.json](F1-002-P2_test_runtime_requirements.json), [F1-002-P2_disposable_environment_capabilities.json](F1-002-P2_disposable_environment_capabilities.json). Integrated into `TEST_OWNERSHIP.md` §0.1 and this task's own evidence doc §20 by F1-002-R1. |
| [F1-003-P1_TEST_SCRIPT_CLOSURE_AND_EXECUTION_CONTRACT.md](F1-003-P1_TEST_SCRIPT_CLOSURE_AND_EXECUTION_CONTRACT.md) | F1-003-P1 (phase F1, first ordered subtask of F1-003, `MERGED` PR #257) | Closes the 8 unscripted backend test files (now 0), reclassifies the 17 aggregate-chain-excluded scripts with the required vocabulary (`accidental_gap`/`intentional_runtime_separated`/`duplicate_alias`/`unsafe_until_f1-003-p2`), resolves `messagesConsentGate.test.ts`'s runtime `UNKNOWN`, adds 3 new additive `server/package.json` aggregates (`server:test:non-disposable` originally executed exit 1, sole known `test:overdue-installments` failure — **historical P1 result, superseded 2026-07-29 by F1-003-B1 (PR #258, `MERGED`): now exit 0, 9/9**; `server:test:disposable-db`/`server:test:storage-integration` still structurally validated only — no disposable Postgres/MinIO in this environment, F1-003-P2 not started), documents legacy `server:test`'s own 23 silently-DB-required members as an unresolved finding for F1-003-P2 (corrected 2026-07-28, F1-003-R1A: the originally-claimed 1 additional MinIO-required member, `test:kvkk-lifecycle`, was an objective factual error — its S3 provider is fully mocked; legacy `server:test` is PostgreSQL-required, MinIO-free; `test:file-backup-db-integration` is the sole genuine MinIO-dependent target — see the evidence doc's §18), and 2 corrections to prior F1-002/F1-002-P1 evidence (`test:platform-admin-password-recovery` bucket, `platformBackup.test.ts` DB-required classification). Defines a forward-looking (contract-only, not implemented) PR/main/nightly/release CI command matrix and exact F1-003-P2 handoff inputs/acceptance criteria. Machine-readable companion: [F1-003-P1_test_execution_contract.json](F1-003-P1_test_execution_contract.json) (114 canonical-script records, supersedes/extends F1-002-P1's 103). |
| [F1-003-B1_OVERDUE_INSTALLMENTS_BASELINE_DRIFT_RESOLUTION.md](F1-003-B1_OVERDUE_INSTALLMENTS_BASELINE_DRIFT_RESOLUTION.md) | F1-003-B1 (phase F1, ran parallel to F1-003-R1, `MERGED` PR #258, merge commit `bb4186793323485c71b91ad6939a0d8469f886cd`) | Resolves the sole known cause of `server:test:non-disposable` exit 1: the 2/9-assertion drift in `overdueInstallments.test.ts` first documented by F1-003-P1. Root-cause classification Case A (stale test) — git history, schema comment, live route consumers, frontend, and the sibling `overdueReceivables.test.ts` all corroborate that commit `81323a88` intentionally extended the shared definition to treat the legacy/persisted literal status `overdue` as overdue, but never updated this test file. Test-only fix (2 assertions + docstring corrected, none weakened); `server:test:non-disposable` now exits `0` (68/68 members, was exit 1). |
| [F1-003-P2A_DISPOSABLE_RUNTIME_PROVISIONING_DESIGN.md](F1-003-P2A_DISPOSABLE_RUNTIME_PROVISIONING_DESIGN.md) | F1-003-P2A/R1/R1A/R1B (phase F1, PR #256, confirmed `MERGED` 2026-07-28T22:50:43Z via `gh pr view 256` — corrects this file's own prior "still open" notes, not rewritten in place, see [F1-003-P2V_POSTGRESQL_MAJOR_VERSION_EVIDENCE.md](F1-003-P2V_POSTGRESQL_MAJOR_VERSION_EVIDENCE.md) §2) | Disposable PostgreSQL/MinIO provisioning design, reconciled (F1-003-R1) against F1-003-P1's merged execution contract and independently re-confirmed the F1-003-R1A MinIO finding for `test:kvkk-lifecycle` (fully mocked S3 provider, no live MinIO required); provides the final F1-003-P2 implementation contract (§K). **§13/§G's "no authoritative production PostgreSQL version evidence" finding is corrected as an incomplete search by [F1-003-P2V_POSTGRESQL_MAJOR_VERSION_EVIDENCE.md](F1-003-P2V_POSTGRESQL_MAJOR_VERSION_EVIDENCE.md)** — not edited in place here, see that document. |
| [F1-003-P2_DISPOSABLE_RUNTIME_IMPLEMENTATION_AND_VERIFICATION.md](F1-003-P2_DISPOSABLE_RUNTIME_IMPLEMENTATION_AND_VERIFICATION.md) | F1-003-P2 (phase F1, `AGENT_COMPLETED`, PR #260 opened, not merged; reconciled with main by **F1-003-P2-R1**, parallel-generation race fixed by **F1-003-P2-R2**, parallel signal-cleanup race fixed by **F1-003-P2-R3**) | Implements the F1-003-P2A/R1 contract (§K/§L) in full: shared TypeScript orchestrator + PowerShell/Bash entry points + stale-resource sweeper under `scripts/test-runtime/`, one-container-per-run PostgreSQL/MinIO, dynamic host ports, collision-resistant naming, a fail-closed production-endpoint guard, and deterministic cleanup — all four required profiles (`postgres`/`storage`/`verify-parallel`/`cleanup-stale`) live-verified against a real Docker Desktop engine, including all 6 mandatory failure-injection scenarios and a dry-run/`--live` stale-sweeper test against artificial fixtures. Three real bugs found and fixed by this task's own verification process are reported transparently. **PostgreSQL major-version parity with production (`16.14`) is now CONFIRMED** (see [F1-003-P2V_POSTGRESQL_MAJOR_VERSION_EVIDENCE.md](F1-003-P2V_POSTGRESQL_MAJOR_VERSION_EVIDENCE.md), merged PR #259) — exact image/build/package parity remains NOT claimed. **F1-003-P2-R1 additionally switched the PostgreSQL runtime reference from the floating tag `postgres:16-alpine` to the digest-pinned reference `postgres@sha256:57c72fd2a128e416c7fcc499958864df5301e940bca0a56f58fddf30ffc07777`** and reran all real profiles against it; the pre-existing MinIO reference was already digest-pinned (`minio/minio@sha256:8834ae47a2de3509b83e0e70da9369c24bbbc22de42f2a2eddc530eee88acd1b`) and required no change. **F1-003-P2-R1's own `verify-parallel` reruns found a real Windows `EBUSY` in concurrent `prisma generate` (2 of 3 reruns clean); F1-003-P2-R2 found the root cause (all invocations racing on the shared `server/node_modules/.prisma/client` output) and fixed it via a generate-once-before-fan-out coordination mechanism (`lib/prismaGeneration.ts`), re-verified with 5 consecutive clean `verify-parallel` runs (8 total attempts across both tasks, none hidden) and 9 new Docker-free unit tests (59/59 total).** **F1-003-P2-R3 found and fixed a separate defect: a single global `activeCleanupTargets` slot could represent only one invocation's SIGINT/SIGTERM cleanup state at a time, even though `verify-parallel` normally runs two invocations concurrently. Replaced with a concurrency-safe `Map<runId, TeardownTargets>` registry (`lib/cleanupRegistry.ts`) whose signal handler snapshots and attempts teardown for every registered invocation via `Promise.allSettled`, with a cached in-flight promise preventing overlapping passes from a duplicate signal — verified by 9 new Docker-free unit tests (68/68 total). Automated live-interruption verification was attempted (Node `child.kill('SIGINT')`, then the stronger Win32 `GenerateConsoleCtrlEvent` approach) but not completed on this Windows environment in this session; documented honestly as attempted-but-inconclusive, with the unit-level multi-target signal-handler tests serving as the fallback verification the task itself permits. Also pinned the cleanup-failure-injection sidecar from `alpine:latest` to a locally-cached digest.** Machine-readable companion: [F1-003-P2_disposable_runtime_verification.json](F1-003-P2_disposable_runtime_verification.json). |
| [F1-003-P2V_POSTGRESQL_MAJOR_VERSION_EVIDENCE.md](F1-003-P2V_POSTGRESQL_MAJOR_VERSION_EVIDENCE.md) | F1-003-P2V/P2V-R1 (phase F1, `MERGED` PR #259, merge commit `8592a570708c6308d7a19aff703db6e0a699ece7`, mergedAt `2026-07-29T07:57:11Z`) | Establishes production PostgreSQL's major version from repository-committed, already-accepted evidence only (no production access): Case A — `16.14`, `VERIFIED_PRODUCTION_OBSERVED` per `F0-002_PRODUCTION_BASELINE_EVIDENCE.md` §B.3/§B.7, independently corroborated by `F0-006_PRODUCTION_TOPOLOGY_EVIDENCE.md`, `PRODUCTION_TOPOLOGY.md`, `KVKK-HIGH-008-PMVR_POST_MERGE_READINESS_EVIDENCE.md`, and `R046_PRODUCTION_VERIFICATION.md`. F1-003-P2V-R1 (§15 of this document, same PR) additionally corrected this lineage's own active-work-status wording for F1-003-P2 based on a worktree-listing-level observation only (no P2 file inspected by that task). **F1-003-P2's disposable-runtime image now matches production at the major-version level** (major-only parity — F1-003-P2's exact image/build/package is not claimed to match production). Machine-readable companion: [F1-003-P2V_postgresql_version_evidence.json](F1-003-P2V_postgresql_version_evidence.json). |
| [F1-003-P3_LAYERED_CI_WORKFLOWS_AND_FULL_SUITE_FAIL_SAFE.md](F1-003-P3_LAYERED_CI_WORKFLOWS_AND_FULL_SUITE_FAIL_SAFE.md) | F1-003-P3 / F1-003-P3-R1 / F1-003-P3-R2 (phase F1, `AGENT_COMPLETED` / `TESTS_PASSED` / `REMOTE_CI_VERIFIED`, PR #268 open, not merged) | **P3-R2 (§18 of the same document):** reconciled PR #268 with current `origin/main` after PR #280 (F1-003-B2/B2-R1 retention-lock closure) merged — 5 conflicts, all resolved manually (no `--ours`/`--theirs`), merge commit `6da0bf43234e2dbb0b7ef1a4db80090e12a047b6`; preserved PR #280's fix, PR #282's dependency remediation, and F2-PREP-001..004 unchanged; retention concurrency directly re-verified 5/5 runs, 29/29 assertions each, against a manually-provisioned disposable PostgreSQL (the local `server:test:legacy-db-required` aggregate itself was blocked from reaching the retention test by the same pre-existing Windows-only CRLF artifact documented in P3-R1); **real GitHub Actions run `30747523882` on the pushed head is fully green, 9/9 jobs `success`, including the previously-failing Layer 5 backend job (5m54s)** — `REMOTE_CI_VERIFIED` genuinely established. **P3:** **P3:** Implements F1-003-P1's own forward-looking `futureCiCommandMatrix` as a real, additive GitHub Actions layer: one reusable workflow (`ci-layers.yml`) with 8 jobs mapping to the 5 required layers. **P3-R1 (§17 of the same document):** reconciled with `origin/main` (merge commit `7ab630c9a5a54acc7bb757b06c908b772c5f138d`, 0 conflicts); root-caused and fixed a real defect the original P3 run exposed on actual GitHub Actions — `non-disposable-backend-tests` never generated the Prisma Client on a from-scratch checkout (`SyntaxError: ... does not provide an export named 'PrismaClient'`), fixed with an explicit `npx prisma generate` step; closed the backend full-suite compatibility gap the original P3 evidence had mis-estimated as "roughly 14" scripts — the exact, machine-verified figure is **23** named PostgreSQL-required/MinIO-free scripts silently chained by legacy `server:test` — via a new additive `server:test:legacy-db-required` aggregate and a new additive `postgres-compat` orchestrator profile (100% reuses F1-003-P2's provisioning/guard/migration/cleanup/pinning code path, no second PostgreSQL implementation, legacy `server:test` itself untouched); fixed the artifact-generation defect (combined stdout mislabeled as JSON) with a genuine `--summary-file` mechanism plus a secret-pattern scan before upload, and found+fixed a second real defect in that same fix (a write failure could have masked the real test exit code) before it reached CI. Existing `windows-bridge-pr.yml`/`windows-bridge-release.yml` remain untouched. All GitHub Actions pinned by commit SHA; actionlint v1.7.12 pinned by checksum. Local validation (P3-R1): `test:runtime:unit` 74/74, `server:test:non-disposable` 68/68, `postgres`/`storage` profiles unaffected (regression-clean), `postgres-compat` 116/117 (the 1 failure is a proven Windows-local-only CRLF artifact, not reproducible on Linux CI), `verify-parallel` regression collision-free, `test:vitest` 35/35 (3 files post-merge), 7 frontend leaf scripts all passing, `npm run lint` still confirmed non-functional. Machine-readable companion: [F1-003-P3_layered_ci_verification.json](F1-003-P3_layered_ci_verification.json) (`p3r1BackendCompatibilityClosure`, `p3r1RootCausedCiDefect`, `p3r1ArtifactGenerationFix`, `p3r1LocalValidation`, `githubActionsEvidence.postR1Run`). |
| [F1-003-B2_RETENTION_RUN_MUTUAL_EXCLUSION_RACE.md](F1-003-B2_RETENTION_RUN_MUTUAL_EXCLUSION_RACE.md) | F1-003-B2 (phase F1, blocking production-behavior defect discovered by F1-003-P3's Layer 5 compatibility CI on PR #268 — PR #268 left open, unmodified, blocked; `AGENT_COMPLETED`, PR opened, not merged; §9's residual-limitation bullet superseded by F1-003-B2-R1 below) | Root-causes and fixes the `POST /privacy/data-retention/run` concurrent-manual-runs race (PR #268 run `30542271302`, `actual=[200,200]` vs `expected=[200,409]`). Proves `server/src/utils/jobLock.ts`'s `acquireJobLock`/`releaseJobLock` primitive is atomic and race-free (130 direct concurrent-acquisition trials against real PostgreSQL, 0 double-grants) before identifying the real defect: `platformAdmin.ts`'s manual route acquired the shared lock only after two other variable-latency DB round trips both concurrent racers perform independently, letting a fast racer's whole cycle finish before a simultaneous second racer even attempted the lock. Fix: `acquireJobLock`/`releaseJobLock` exported and called first in the route, before either audit write; both winner and loser still write their own `started` row; a new lock-acquired-then-started-write-fails edge case now releases the lock before its `500`. 5 new + 1 strengthened test (20→25 assertions), 8 consecutive post-fix full-suite runs (80 race-gate rounds, 0 double-successes). No schema/migration change. Machine-readable companion: [F1-003-B2_retention_run_mutual_exclusion_race.json](F1-003-B2_retention_run_mutual_exclusion_race.json). |
| [F1-003-B2-R1_ADMISSION_LOCK_CLOSURE.md](F1-003-B2-R1_ADMISSION_LOCK_CLOSURE.md) | F1-003-B2-R1 (phase F1, continued on the existing open branch `fix/f1-003-b2-retention-run-lock-race` / PR #280, no new branch/PR; `AGENT_COMPLETED`, not merged) | Closes the residual admission-timing gap F1-003-B2 itself had flagged: `getPlatformSetting` previously ran before `acquireJobLock` for live attempts, leaving one variable-latency DB round trip capable of staggering two genuinely-simultaneous requests. Moves lock acquisition ahead of the policy read entirely (via a new exported `createDataRetentionRunHandler` factory), so admission is the very first DB operation a live attempt performs; a new deterministic (barrier-based, non-timing-luck) test proves admission is fully resolved before either racer's policy lookup can complete. Also reconciles with `origin/main` (PR #276/#277, zero conflicts), adds 4 new failure-branch tests (25→29 assertions), reports remote CI status precisely (`gh pr checks 280`: no checks reported — diagnosed as a pre-existing CI-topology gap, not claimed as `REMOTE_CI_VERIFIED`), and sanitizes one author-machine absolute-path occurrence from task-owned evidence. No schema/migration change. Machine-readable companion: [F1-003-B2-R1_admission_lock_closure.json](F1-003-B2-R1_admission_lock_closure.json). |
| [F2-PREP-001_DOMAIN_OWNERSHIP_AND_BOUNDARY_INVENTORY.md](F2-PREP-001_DOMAIN_OWNERSHIP_AND_BOUNDARY_INVENTORY.md) | F2-PREP-001 (phase F2 PREPARATION, read-only architecture discovery, `MERGED` PR #276, merge commit `0de9a04a8c7b4fefe5e7f525f9cab031b55fcb83`) | Domain ownership and boundary inventory: 38 domains (37 inherited from F0-003/F0-004 + External Calendar Integration, newly identified), 99 Prisma models mapped to ownership. Read-only evidence only — no code/schema/tracker change. Machine-readable companion: [F2-PREP-001_domain_ownership_inventory.json](F2-PREP-001_domain_ownership_inventory.json). Reconciled by [F2-PREP-005](../architecture/F2-PREP-005_CONSOLIDATED_MODULARIZATION_CHARTER.md). |
| [F2-PREP-002_CROSS_DOMAIN_DEPENDENCY_AND_DIRECT_ACCESS_MAP.md](F2-PREP-002_CROSS_DOMAIN_DEPENDENCY_AND_DIRECT_ACCESS_MAP.md) | F2-PREP-002 (phase F2 PREPARATION, read-only dependency discovery, `MERGED` PR #279, merge commit `97bdde6bc5e96a047f54ee2ec2647df5a9152bcc`) | Route-to-Prisma-model dependency/direct-access map for the highest-risk domain clusters: 124 classified edges, 25 direct-Prisma-access sites, 16 ownership-unknown catalog entries, 20 contract candidates, ranked top-20 remediation list. Read-only evidence only. Machine-readable companion: [F2-PREP-002_cross_domain_dependency_map.json](F2-PREP-002_cross_domain_dependency_map.json). Reconciled by [F2-PREP-005](../architecture/F2-PREP-005_CONSOLIDATED_MODULARIZATION_CHARTER.md). |
| [F2-PREP-003_FEATURE_INTAKE_AND_CLICKUP_DOMAIN_MAPPING.md](F2-PREP-003_FEATURE_INTAKE_AND_CLICKUP_DOMAIN_MAPPING.md) | F2-PREP-003 (phase F2 PREPARATION, backlog classification, `MERGED` PR #277, merge commit `e8e05f669f049c4895e0c9a7f95db6f3fbd463fe`) | Feature-intake/ClickUp-to-domain mapping framework: 17 repository-derived backlog items classified (0 ClickUp-derived — ClickUp access unavailable in this environment, import remains `PENDING_EXTERNAL_IMPORT`). Read-only evidence only. Machine-readable companion: [F2-PREP-003_feature_intake_domain_mapping.json](F2-PREP-003_feature_intake_domain_mapping.json). Reconciled by [F2-PREP-005](../architecture/F2-PREP-005_CONSOLIDATED_MODULARIZATION_CHARTER.md). |
| [F2-PREP-004_MODULARIZATION_SEQUENCE_AND_PILOT_SELECTION.md](F2-PREP-004_MODULARIZATION_SEQUENCE_AND_PILOT_SELECTION.md) | F2-PREP-004 (phase F2 PREPARATION, sequencing design, `MERGED` PR #278, merge commit `83701882bd0f48a34ca8f8b02beb8a779003e638`) | Modularization sequencing and pilot-candidate scoring across 10 domains: Imaging independently recommended primary pilot candidate, Billing independently recommended fallback — candidates only, no selection made or authorized by this task. External Calendar Integration and CC-04 explicitly deferred to F2-PREP-005 for comparison. Machine-readable companion: [F2-PREP-004_modularization_sequence.json](F2-PREP-004_modularization_sequence.json). Reconciled (and Billing's favorability materially revised) by [F2-PREP-005](../architecture/F2-PREP-005_CONSOLIDATED_MODULARIZATION_CHARTER.md). |
| [F2-PREP-005_CONSOLIDATION_EVIDENCE.md](F2-PREP-005_CONSOLIDATION_EVIDENCE.md) | F2-PREP-005 (phase F2 PREPARATION, consolidated modularization charter, `AGENT_COMPLETED` / `PR_OPENED_AWAITING_REVIEW`, not merged) | Reconciles F2-PREP-001..004 into one authoritative charter: canonical 38-domain classification table, a 3-way scored pilot candidate matrix (Imaging/Billing/External Calendar Integration), a recommended-not-yet-approved pilot decision (Imaging), a boundary-tooling POC decision (dependency-cruiser, report-only, pilot-scoped), and 8 explicitly resolved cross-task contradictions. Implementation-authorization decisions remain `NOT_APPROVED` — F2 remains preparation only. Full charter: [../architecture/F2-PREP-005_CONSOLIDATED_MODULARIZATION_CHARTER.md](../architecture/F2-PREP-005_CONSOLIDATED_MODULARIZATION_CHARTER.md). Machine-readable companion: [../architecture/evidence/F2-PREP-005_consolidated_modularization_charter.json](../architecture/evidence/F2-PREP-005_consolidated_modularization_charter.json). |
| [F2-PREP-006-A_IMG_BRG_OWNERSHIP_AND_IMPLEMENTATION_INVENTORY.md](F2-PREP-006-A_IMG_BRG_OWNERSHIP_AND_IMPLEMENTATION_INVENTORY.md) | F2-PREP-006-A (phase F2 PREPARATION, one of four independent parallel discovery siblings F2-PREP-006-A..D under frozen baseline `4cb334d213b4dbbac4193f1a8c1878deddb55714`, `MERGED` PR #288, merge commit `46ba7b219002c8267bf127b39718efea091657b4`) | Repository-grounded IMG (`imaging-server-viewer`)/BRG (`imaging-device-bridge`) ownership and implementation inventory: 32 routes independently counted (27 in `imaging.ts` + 5 in `imagingBridgePublic.ts`, 8 of `imaging.ts`'s own routes found to be BRG-owned), 7 services, 8 Prisma models, and 7 total relevant test files (5 dedicated Imaging/Bridge test files + 2 incidental cross-domain test files). New finding: duplicated study-ingest transaction logic between the manual and bridge upload paths. Independently reconfirms F2-PREP-002's cross-domain direct-Prisma-access findings and F2-PREP-004's tenant-scoping gap and CI-isolation claims; additionally finds a CI-coverage gap (`windows-bridge-pr.yml` skips `test:kvkk-lifecycle`). Classifies BRG's role as an unresolved combination split by edge and records a proposed, not-approved IMG/BRG responsibility split. Read-only discovery/evidence only — no code/schema/test/workflow/package change. Machine-readable companion: [F2-PREP-006-A_img_brg_inventory.json](F2-PREP-006-A_img_brg_inventory.json). Reconciled by [F2-PREP-006-E](../architecture/F2-PREP-006-E_IMAGING_BOUNDARY_CONTRACT.md). |
| [F2-PREP-006-B_IMAGING_DATA_STORAGE_TENANT_KVKK_ASSESSMENT.md](F2-PREP-006-B_IMAGING_DATA_STORAGE_TENANT_KVKK_ASSESSMENT.md) | F2-PREP-006-B (phase F2 PREPARATION, one of four independent parallel discovery siblings F2-PREP-006-A..D under frozen baseline `4cb334d213b4dbbac4193f1a8c1878deddb55714`, `MERGED` PR #289, merge commit `38249af95f3975af1088e03c856c1443a034f517`) | Imaging data, storage, tenant, and KVKK boundary assessment: 12-model ownership classification, tenant-scope enforcement breakdown (enforced/inherited/unverified), authorization/audit findings, storage/signed-URL/retention/export/anonymization/deletion/orphan-cleanup findings, backup findings, `PZ-IMG-03` refined from "location unverified" to "location resolved, authentication confirmed, RBAC granularity unproven" (remains OPEN), 9 risks (`R1`-`R9`), 8 contract requirements for a future boundary contract. Read-only assessment only. Machine-readable companion: [F2-PREP-006-B_imaging_data_storage_kvkk.json](F2-PREP-006-B_imaging_data_storage_kvkk.json) (note: the real filename omits "tenant", unlike this row's own document title — see F2-PREP-006-E's contradiction `CTR-02`). Reconciled by [F2-PREP-006-E](../architecture/F2-PREP-006-E_IMAGING_BOUNDARY_CONTRACT.md). |
| [F2-PREP-006-C_IMAGING_CALLER_DIRECT_ACCESS_AND_TRANSACTION_MAP.md](F2-PREP-006-C_IMAGING_CALLER_DIRECT_ACCESS_AND_TRANSACTION_MAP.md) | F2-PREP-006-C (phase F2 PREPARATION, one of four independent parallel discovery siblings F2-PREP-006-A..D under frozen baseline `4cb334d213b4dbbac4193f1a8c1878deddb55714`, `MERGED` PR #290, merge commit `5a90eb3edd3842d107825169423bb10db53ee403`) | Imaging caller, direct-access, and transaction flow map: 20 inbound callers, 8 outbound dependencies, 11 direct-access findings across 4 required categories (direct Prisma, storage bypass, signed-URL bypass, status-mutation bypass), 10 transaction flows, 6 atomic/8 eventual operations, 9 partial-failure points, 6 compensation requirements, 5 idempotency requirements, a 6-step proposed (not implemented) caller migration order, 2 named blockers (`BLK-01` storage/DB compensation gap, `BLK-02` `ImagingRequest` PATCH concurrency). Headline positive finding: zero live cross-domain transaction coupling (`DAV-06`). Read-only evidence and migration-planning only. Machine-readable companion: [F2-PREP-006-C_imaging_callers_transactions.json](F2-PREP-006-C_imaging_callers_transactions.json). Reconciled by [F2-PREP-006-E](../architecture/F2-PREP-006-E_IMAGING_BOUNDARY_CONTRACT.md). |
| [F2-PREP-006-D_IMAGING_PUBLIC_CONTRACT_AND_TEST_DESIGN.md](F2-PREP-006-D_IMAGING_PUBLIC_CONTRACT_AND_TEST_DESIGN.md) | F2-PREP-006-D (phase F2 PREPARATION, one of four independent parallel discovery siblings F2-PREP-006-A..D under frozen baseline `4cb334d213b4dbbac4193f1a8c1878deddb55714`, `MERGED` PR #291, merge commit `8e77f2fe4d82b9b6e5e1364e18c8a16bfba1711b`) | Imaging public contract and characterization-test design: 20 candidate commands, 12 candidate queries, 1 candidate event (`ImagingStudyReceived`, mechanism not decided), a consolidated error contract, 4 idempotency rules, 31 candidate characterization tests across 13 required areas (20 blocking/11 non-blocking), 8 backward-compatibility requirements, a report-only `dependency-cruiser` POC specification (not installed/configured). Every item explicitly marked `candidate`, never `accepted` — nothing in this document is authoritative pending F2-PREP-006-E. Machine-readable companion: [F2-PREP-006-D_imaging_contract_test_design.json](F2-PREP-006-D_imaging_contract_test_design.json). Reconciled by [F2-PREP-006-E](../architecture/F2-PREP-006-E_IMAGING_BOUNDARY_CONTRACT.md). |
| [F2-PREP-006-E_CONSOLIDATION_EVIDENCE.md](F2-PREP-006-E_CONSOLIDATION_EVIDENCE.md) | F2-PREP-006-E (phase F2 PREPARATION, consolidation of F2-PREP-006-A..D, `AGENT_COMPLETED` / `VALIDATION_PASSED` / `PR_OPENED_AWAITING_REVIEW`, not merged) | Methodology/validation record for the authoritative Imaging/Bridge boundary contract: pre-flight baseline verification, reading order, CodeGraph discipline (none invoked, no scope expansion required), and full validation commands/results (JSON parse, deterministic counts, `git diff --check`, scope-restriction checks). Full contract: [../architecture/F2-PREP-006-E_IMAGING_BOUNDARY_CONTRACT.md](../architecture/F2-PREP-006-E_IMAGING_BOUNDARY_CONTRACT.md). Machine-readable companion: [../architecture/evidence/F2-PREP-006-E_imaging_boundary_contract.json](../architecture/evidence/F2-PREP-006-E_imaging_boundary_contract.json). |
| [F2-PREP-007-A_IMAGING_CHARACTERIZATION_AUTH_SHAPE_EVIDENCE.md](F2-PREP-007-A_IMAGING_CHARACTERIZATION_AUTH_SHAPE_EVIDENCE.md) | F2-PREP-007-A (phase F2 PREPARATION, Stage 0 characterization, `MERGED` PR #293, merge commit `43b92dde43e4c0606a6333997a81de7ea3ed247c`) | Implements 5 of the 18 Stage-1-gating characterization tests from the F2-PREP-006-E contract: `CT-06` (bridge Bearer auth rejects clinic-session JWT), `CT-16` (`legalHoldReason` redaction uniformity), `CT-19` (no `DELETE` route for `ImagingStudy`/`ImagingImage`), `CT-21` (legal-hold audit metadata PII-free shape), `CT-28` (response-shape snapshots). 36/36 assertions passed; no `VERIFIED_DEFECT` found. Test-only; no application/schema/workflow/package-script change. Machine-readable test file: `server/src/tests/imagingCharacterizationAuthShape.test.ts`. Reconciled by [F2-PREP-007-E](F2-PREP-007-E_IMAGING_CHARACTERIZATION_WAVE_CONSOLIDATION_EVIDENCE.md). |
| [F2-PREP-007-B_IMAGING_CHARACTERIZATION_TENANT_LIFECYCLE_EVIDENCE.md](F2-PREP-007-B_IMAGING_CHARACTERIZATION_TENANT_LIFECYCLE_EVIDENCE.md) | F2-PREP-007-B (phase F2 PREPARATION, Stage 0 characterization, `MERGED` PR #294, merge commit `bfc1b7317e95881fa68306a0f190f3d0570596a3`) | Implements 7 characterization tests: `CT-02`/`CT-03`/`CT-17`/`CT-26` (mandatory-before-refactor, all pass), `CT-05`/`CT-30` (mandatory-before-caller-migration, both pass), and `CT-23` (mandatory-before-caller-migration) — found and reported as **`VERIFIED_DEFECT`**: `LinkImagingStudy` can change `ImagingStudy.patientId` while retaining an `imagingRequestId` still belonging to the original patient; reproduced 2/2, not fixed (out of test-only scope). 29/29 assertions passed. Test-only; no application/schema/workflow/package-script change. Machine-readable test file: `server/src/tests/imagingCharacterizationTenantLifecycle.test.ts`. Reconciled by [F2-PREP-007-E](F2-PREP-007-E_IMAGING_CHARACTERIZATION_WAVE_CONSOLIDATION_EVIDENCE.md), which also corrects a 1-off internal arithmetic inconsistency in this document's own per-CT summary table (see that document's §6). |
| [F2-PREP-007-C_IMAGING_CHARACTERIZATION_INGEST_STORAGE_EVIDENCE.md](F2-PREP-007-C_IMAGING_CHARACTERIZATION_INGEST_STORAGE_EVIDENCE.md) | F2-PREP-007-C (phase F2 PREPARATION, Stage 0 characterization, `MERGED` PR #295, merge commit `0d48d9fdea5d33f0a1acf88da9060702526c8bd1` — also `origin/main`'s current tip) | Implements 8 characterization tests covering manual/bridge upload, idempotency, and compensation: `CT-07`, `CT-08`, `CT-10`, `CT-11`, `CT-12` (duplicate manual uploads are non-idempotent — documented intentional current behavior, not a defect), `CT-13`, `CT-14`, `CT-27`. 13/13 assertions passed per run (validated 3× by the originating task); no `VERIFIED_DEFECT` found. Explicitly characterizes, without converging, the manual/bridge ingest duplication (`OVL-01`) and the reachable storage-compensation paths. Test-only; no application/schema/workflow/package-script change. Machine-readable test file: `server/src/tests/imagingCharacterizationIngestStorage.test.ts`. Reconciled by [F2-PREP-007-E](F2-PREP-007-E_IMAGING_CHARACTERIZATION_WAVE_CONSOLIDATION_EVIDENCE.md). |
| [F2-PREP-007-D_IMAGING_REQUEST_CONCURRENCY_EVIDENCE.md](F2-PREP-007-D_IMAGING_REQUEST_CONCURRENCY_EVIDENCE.md) | F2-PREP-007-D (phase F2 PREPARATION, Stage 0 characterization, `MERGED` PR #296, merge commit `801ddbe4714b9aa92bafa4689be9c474eb2270db`) | Implements `CT-32` — `ImagingRequest` PATCH/cancel concurrency characterization, making the pre-catalogued `CR-03`/`BLK-02`/`FP-06` silent-clobber gap test-tracked rather than only narratively documented. 153/153 assertions passed (30/30 rounds `BOTH_SUCCESS_SILENT_CLOBBER`, deterministic, not a flake); classified pre-contract-exposure blocker, not a Stage 0 blocker, target resolution no later than Stage 2. Test-only; not wired into any aggregate by this task (deferred to consolidation), no application/schema/workflow/package-script change. Machine-readable test file: `server/src/tests/imagingRequestConcurrencyCharacterization.test.ts`. Reconciled by [F2-PREP-007-E](F2-PREP-007-E_IMAGING_CHARACTERIZATION_WAVE_CONSOLIDATION_EVIDENCE.md). |
| [F2-PREP-007-E_IMAGING_CHARACTERIZATION_WAVE_CONSOLIDATION_EVIDENCE.md](F2-PREP-007-E_IMAGING_CHARACTERIZATION_WAVE_CONSOLIDATION_EVIDENCE.md) | F2-PREP-007-E (phase F2 PREPARATION, consolidation of F2-PREP-007-A..D, `MERGED` PR [#298](https://github.com/MustafaBasol/DisKlinikCRM/pull/298), `mergedAt` 2026-08-03T08:20:40Z per `gh pr view`, independently re-confirmed by F2-PREP-008 below) | Consolidates all four Stage 0 characterization sub-tasks into one Stage 0 coverage matrix (32 tests: 21 blocking implemented and passing, 11 non-blocking not yet implemented), preserves `CT-23` (`VERIFIED_DEFECT`) and `CT-32`/`CR-03` (verified concurrency gap) as explicit unresolved blockers, establishes permanent test ownership on the PR branch (`server/package.json`'s new `test:imaging-characterization` script, wired into the existing CI-owned `server:test:disposable-db` aggregate — zero new CI workflow files, increases Layer 3 execution time only), and demonstrates all 10 Stage 0 closure acceptance conditions. This document's own text still reads `Stage 0: CLOSURE_PROPOSED_AWAITING_MERGE_AND_MAIN_CI` (accurate as authored, pre-merge) — not edited in place; see [F2-PREP-008](F2-PREP-008_STAGE1_IMAGING_FACADE_PREP_AND_AUTHORIZATION.md) for the post-merge closure confirmation. No application/schema/migration file touched. Machine-readable test-ownership target: `server/package.json` (`test:imaging-characterization`, `server:test:disposable-db`). |
| [F2-PREP-008_STAGE1_IMAGING_FACADE_PREP_AND_AUTHORIZATION.md](F2-PREP-008_STAGE1_IMAGING_FACADE_PREP_AND_AUTHORIZATION.md) | F2-PREP-008 (phase F2, Stage 1 preparation/authorization, documentation/evidence and program-control only, `AGENT_COMPLETED`, not merged) | Independently re-verifies (via `gh pr view`/`gh run view`/`git merge-base --is-ancestor`) that PR #298 is `MERGED` and its post-merge main CI (run `30797032835`) passed 9/9 `ci-layers` jobs on the exact baseline SHA, establishing Stage 0's own authoritative-closure criterion is satisfied (F2-PREP-007-E's own entry left unedited). Targeted repository-evidence analysis with exact file/line citations for `CT-23` (`imaging.ts:807-854`), `CT-32`/`CR-03`/`BLK-02`/`FP-06` (`imaging.ts:487-524`/`530-552`), `OVL-01` (`imaging.ts:614-656` vs `imagingBridgePublic.ts:301-341`), `BLK-01` (`imaging.ts:679`). Proposes a paper-only Stage 1 internal facade design (`server/src/services/imaging/public.ts`, the already-accepted `F2-CC-14`/`ImagingLifecyclePort` 4-method slice), explicitly excluding the `CT-23`/`CT-32` defect surfaces from the operation inventory. Authorization decision: `AUTHORIZED_FOR_STAGE_1_IMPLEMENTATION`, scoped strictly to that 4-method slice; names `F2-IMPL-001-A` (confirmed unused before assignment) as the next implementation task. No application/schema/migration/workflow/package-script file touched; no facade code exists after this task. Machine-readable companion: [F2-PREP-008_stage1_imaging_facade_prep_and_authorization.json](F2-PREP-008_stage1_imaging_facade_prep_and_authorization.json). |
| [F2-IMPL-001-A_ADDITIVE_UNUSED_INTERNAL_IMAGING_FACADE_SKELETON.md](F2-IMPL-001-A_ADDITIVE_UNUSED_INTERNAL_IMAGING_FACADE_SKELETON.md) | F2-IMPL-001-A (phase F2, Stage 1 implementation, original implementation, superseded by F2-IMPL-001-A-R2 below) | Implements the F2-PREP-008-authorized, additive, zero-caller `ImagingLifecyclePort` facade (`server/src/services/imaging/public.ts`, exactly the accepted 4-method slice — `markStorageMissing`/`redactForAnonymization`/`getImagesForLifecycleReview`/`checkImageStorageExists` — with a purpose-built DTO/typed-error contract; `legalHoldReason` deliberately excluded, role-gated and unavailable to a request-context-free facade). Tenant enforcement for the three `imageId`-only methods: ownership re-derivation via `ImagingImage -> ImagingStudy -> clinicId` before every read/mutation, fail-closed indistinguishable not-found on any mismatch — the accepted signature carries no ambient-tenant parameter for these methods (no such mechanism exists anywhere in this codebase), so safety is enforced by provenance (`imageId` only obtainable via the clinicId-scoped query) plus this re-derivation check, verified strictly stronger than the two existing direct-Prisma callers it may later replace. `LinkImagingStudy`/`UpdateImagingRequest`/`CancelImagingRequest` (`CT-23`/`CT-32` surfaces) excluded, unresolved, unchanged. 25 new focused tests (disposable PostgreSQL, wired into the existing `server:test:disposable-db` aggregate as a new narrowly-named script, not folded into `test:imaging-characterization`) — all pass; existing `test:imaging` (103/103) and `test:patient-privacy` (38/38) regression-clean; zero production import (independently proven twice). No schema/migration/route/workflow file touched. **Later found by F2-IMPL-001-A-R1 (below) to have an unenforced tenant-authorization gap on the three `imageId`-only methods; superseded by F2-IMPL-001-A-R2's re-implementation against the accepted F2-PREP-009 contract.** Machine-readable companion: [F2-IMPL-001-A_additive_unused_internal_imaging_facade_skeleton.json](F2-IMPL-001-A_additive_unused_internal_imaging_facade_skeleton.json). |
| [F2-IMPL-001-A_R1_TENANT_CONTEXT_REVIEW_CORRECTION.md](F2-IMPL-001-A_R1_TENANT_CONTEXT_REVIEW_CORRECTION.md) | F2-IMPL-001-A-R1 (phase F2, review/correction of F2-IMPL-001-A / PR #304, documentation-only) | Reviews PR #304 (original review head `f8a37b72c4cc1800126b67e451e35238080cfe17`, `OPEN`, not merged) against its own evidence and confirms two defects without editing the original evidence files: (1) `checkImageStorageExists`'s accepted 1-argument signature was altered by an optional test-injection parameter; (2) the three `imageId`-only methods' ownership re-derivation (`ImagingImage.clinicId` vs `ImagingStudy.clinicId`) is a data-integrity consistency check, not caller tenant authorization — no caller-supplied `clinicId`/principal/context value exists anywhere in the accepted signatures to check against. Status: `BLOCKED_TENANT_CONTEXT_CONTRACT_INSUFFICIENT`. PR #304 left open, unmodified by this document, marked blocked. Triggers [F2-PREP-009](../architecture/F2-PREP-009_IMAGING_LIFECYCLE_PORT_TENANT_CONTEXT_CONTRACT_AMENDMENT.md). This document's own §§1-8 still read against the original review head (accurate as authored) — not edited in place; a new §9, added by the F2-PREP-009 correction task (2026-08-03), reconciles against PR #304's current head `abac5e361abd0913dadbce1e124c2ca113600fb7`: Finding 1 (tenant-authorization gap) `STILL_OPEN_ON_CURRENT_PR304_HEAD`; Finding 2 (`fileExistsForTest` drift) `CORRECTED_ON_CURRENT_PR304_HEAD`. Overall disposition (`BLOCKED_TENANT_CONTEXT_CONTRACT_INSUFFICIENT`) unchanged. Machine-readable companion: [F2-IMPL-001-A_R1_tenant_context_review_correction.json](F2-IMPL-001-A_R1_tenant_context_review_correction.json) (now includes a `headReconciliation` object). |
| F2-PREP-009 correction (2026-08-03, same PR #307, not a new evidence file) | F2-PREP-009 correction (phase F2, tenant-context wording hardening + PR #304 head reconciliation, documentation-only) | Corrects [F2-PREP-009_IMAGING_LIFECYCLE_PORT_TENANT_CONTEXT_CONTRACT_AMENDMENT.md](../architecture/F2-PREP-009_IMAGING_LIFECYCLE_PORT_TENANT_CONTEXT_CONTRACT_AMENDMENT.md)'s §3/§4 tenant-context-source wording (previously readable as permitting a raw session/JWT `clinicId` as sufficient authorization; now requires `resolveEffectiveClinicId`/`validateAndGetClinicIdScope`/`getAccessibleClinicIds` or an equivalent access-scoped record lookup, per new §8a/§13) and reconciles PR #304's head against the row above. Independently verifies (direct source read of `patientPrivacy.ts`, not assumed) that all three currently-intended Privacy callers (`patientAnonymization.ts`, `orphanFileInspection.ts`, `deletionReviewInventory.ts`) already receive an authorization-validated `clinicId` via the shared `resolvePatient()` route helper, recorded as a Stage 3 precondition rather than a standing guarantee. Option A contract retained unchanged. F2-PREP-009 remained `PROPOSED` as of this entry — since accepted and merged via PR #307 (see below); F2-IMPL-001-A remained `BLOCKED_TENANT_CONTEXT_CONTRACT_INSUFFICIENT` as of this entry. |
| F2-PREP-009 — PR #307 main reconciliation (2026-08-03, same PR #307, not a new evidence file) | F2-PREP-009 — unrelated-CI-blocker closeout + main reconciliation (phase F2, documentation/architecture-only) | PR #307's pre-reconciliation head (`eb0865c4e174d0cf21a16b9c110888025ec8f2e1`) had failed CI twice on run [30813103465](https://github.com/MustafaBasol/DisKlinikCRM/actions/runs/30813103465), `ci-layers / Layer 3` (`patientEmergencyContactsPrimaryConcurrency.test.ts`) — a pre-existing race unrelated to F2-PREP-009's own scope, independently fixed via `MERGED` PR #310 (merge commit `a59c4b7c8b9d09f2dc353fdf38a63705e0881c56`, current `origin/main` tip); duplicate fix PR #311 closed unmerged. Merged `origin/main` into the PR #307 branch via a single plain `git merge --no-ff` (no rebase, no force-push, no `-ours`/`-theirs`) — automatic, zero conflicts. New head `56329369656b1338a358a243935823bc8b067b0a`, pushed. Re-verified the contract rows above (F2-PREP-009 / F2-IMPL-001-A_R1) are unchanged in substance on the reconciled head: explicit `clinicId` on all four methods, fail-closed predicates, no bypass, correctly-split cross-tenant test cases, Finding 1 open/Finding 2 historical against PR #304's unchanged head. Corrected PR #307's own body (removed stale "CI passed in full" claim; recorded twice-failed-run/PR #310/PR #311 history; requires CI evaluated only on the final reconciled head; no deployment/production-verification claim). **Final CI on reconciled head, run [30819294580](https://github.com/MustafaBasol/DisKlinikCRM/actions/runs/30819294580): 9/9 required `ci-layers` jobs `SUCCESS`**, including the previously-failing Layer 3 job. No application/schema/migration/package-script/workflow file authored by this task's own commits. **F2-PREP-009 was subsequently accepted and merged via PR #307 (merge commit `967ed95c114f07580bfb69fe0386becc45b4313d`); F2-IMPL-001-A-R2 (below) re-implements the facade against that accepted contract.** |
| [F2-IMPL-001-A_ADDITIVE_UNUSED_INTERNAL_IMAGING_FACADE_SKELETON.md §27](F2-IMPL-001-A_ADDITIVE_UNUSED_INTERNAL_IMAGING_FACADE_SKELETON.md) | F2-IMPL-001-A-R2 (phase F2, re-implementation against the accepted F2-PREP-009 explicit-tenant contract, continued on the existing open PR #304 branch, `AGENT_COMPLETED` / `TESTS_PASSED`, not merged) | Reconciled PR #304 (`abac5e361abd0913dadbce1e124c2ca113600fb7`) with `origin/main` (merge commit `d2eaeaa9240e576c201eb5e84bb3defc9d7d53de`, 4 conflicts, all resolved manually) and re-implemented `server/src/services/imaging/public.ts` against the F2-PREP-009 Option A contract: `markStorageMissing(clinicId, imageId)`, `redactForAnonymization(clinicId, imageId, reason)`, `checkImageStorageExists(clinicId, imageId)`, `getImagesForLifecycleReview(clinicId, patientId)` (unchanged) — runtime arity verified `2`/`3`/`2`/`2`. Read/write predicate `{ id: imageId, clinicId, study: { clinicId } }` on every method, including mutations. Missing, same-org-sibling cross-tenant, cross-org cross-tenant, and denormalization-mismatch cases all produce an identical, message-equal `ImagingNotFoundError` — no existence side-channel. Storage-existence test seam retained (zero production importer, cannot alter authorization). `imagingLifecycleFacade.test.ts` rewritten with cross-tenant/indistinguishability/rejected-write-untouched tests. **30/30 facade tests passing** (was 25); `CT-32` independently re-confirmed 153/153; full `server:test:disposable-db` aggregate exit `0`, cleanup succeeded; existing `test:imaging` 103/103 and `test:patient-privacy` 38/38 regression-clean; zero production callers re-confirmed. No schema/migration/route/workflow file touched by this task. Machine-readable companion: [F2-IMPL-001-A_additive_unused_internal_imaging_facade_skeleton.json](F2-IMPL-001-A_additive_unused_internal_imaging_facade_skeleton.json) `r2Reimplementation`. |
| F2-IMPL-001-A-R2 (2026-08-03, same PR #304, §27 of the row above, not a new evidence file) | F2-IMPL-001-A-R2 (phase F2, Stage 1 re-implementation against the accepted F2-PREP-009 contract, `OPEN`, not merged) | Reconciled PR #304's branch with `origin/main` (`git merge --no-ff`, merge commit `d2eaeaa9240e576c201eb5e84bb3defc9d7d53de`, 4 conflicted files resolved manually: `CURRENT_PHASE.md`, `NORAMEDI_MASTER_TRACKER.md`, `evidence/README.md`, `server/package.json`). Re-implemented `server/src/services/imaging/public.ts`'s `findOwnedImage` and all three `imageId`-only methods to require an explicit, caller-supplied, already-authorization-validated `clinicId` (`markStorageMissing(clinicId, imageId)`, `redactForAnonymization(clinicId, imageId, reason)`, `checkImageStorageExists(clinicId, imageId)`), applied as `{ id: imageId, clinicId, study: { clinicId } }` on every read AND write predicate — closing Finding 1. `getImagesForLifecycleReview(clinicId, patientId)` unchanged. No compatibility overload, no ambient tenant context, no system bypass. Test-only `__setImagingStorageExistenceCheckerForTest` seam retained (module-private state, no parameter on `checkImageStorageExists`, no production importer). `server/src/tests/imagingLifecycleFacade.test.ts` rewritten: **30/30 passing** (cross-tenant denial for all three `imageId` methods against both a same-org sibling clinic and a cross-org clinic, missing-vs-cross-tenant indistinguishability, denormalization-mismatch-still-fails-closed in both directions, Clinic-A-clinicId+Clinic-B-patientId lifecycle-review empty-result, rejected-write-leaves-target-row-untouched). `test:imaging` **103/103**, `test:patient-privacy` **38/38** regression-clean; full `server:test:disposable-db` aggregate (incl. unmodified `test:imaging-characterization`, `CT-32` sub-suite 153/153) exit `0`, disposable-PostgreSQL cleanup succeeded. `npx prisma generate && tsc --noEmit`: exit `0`. `git diff --check`: clean. Facade remains additive/unused — zero production callers (routes/middleware/jobs/services scan), independently re-confirmed. No schema/migration file touched by this task. Full detail: [F2-IMPL-001-A_ADDITIVE_UNUSED_INTERNAL_IMAGING_FACADE_SKELETON.md §27](F2-IMPL-001-A_ADDITIVE_UNUSED_INTERNAL_IMAGING_FACADE_SKELETON.md). Machine-readable companion: [F2-IMPL-001-A_additive_unused_internal_imaging_facade_skeleton.json](F2-IMPL-001-A_additive_unused_internal_imaging_facade_skeleton.json) (`r2Reimplementation` object). Task status: `AGENT_COMPLETED` / `TESTS_PASSED` / `PR_CI_PASSED` / `PR_OPENED_AWAITING_PROGRAM_REVIEW`. Merge safe: no. Deployment safe: no. |
| [F2-IMPL-001-A_ADDITIVE_UNUSED_INTERNAL_IMAGING_FACADE_SKELETON.md §28](F2-IMPL-001-A_ADDITIVE_UNUSED_INTERNAL_IMAGING_FACADE_SKELETON.md) | F2-IMPL-001-A-R3 (phase F2, atomic legal-hold enforcement correction, continued on the existing open PR #304 branch, not merged) | Closed a TOCTOU race in `redactForAnonymization`: legal hold was checked in memory but never re-applied inside the `updateMany` write predicate, so a hold acquired between the read and the write had no effect. Fixed by adding `study.legalHold: false` to the mutation's own `where` clause, with zero-row results classified using only a second tenant-scoped `findOwnedImage` follow-up (never an unscoped-by-id lookup) — not-found → `ImagingNotFoundError`, already-redacted → benign no-op, otherwise → `ImagingLegalHoldViolationError`. A cross-tenant image is rejected before this logic is ever reached, so it can never surface `ImagingLegalHoldViolationError`, verified by a direct test. Added a deterministic, promise-gated (non-sleep) test-only synchronization seam (`__setRedactionPreMutationBarrierForTest`) and a real disposable-Postgres regression test proving the race is closed, plus 3 more direct tests (mutation-predicate capture, cross-tenant-held, denormalized-mismatch-under-hold). Test-seam review: both this seam and the pre-existing `__setImagingStorageExistenceCheckerForTest` retain Option B with full precisely-scoped evidence (sequential execution, process-local, `finally`-restored, zero production importers, cannot alter tenant predicates). **34/34 facade tests passing** (was 30, +4 net); `CT-32` re-confirmed 153/153; full `server:test:disposable-db` aggregate exit `0`; `test:imaging` 103/103, `test:patient-privacy` 38/38 regression-clean; zero production callers re-confirmed. Windows Bridge `.NET` job (`BridgeOrchestratorTests.Heartbeat_AfterPairing_TransitionsToOnlineAndOmitsNullCapabilities`) failed on this task's fresh push then passed on rerun — classified `FLAKY_UNVERIFIED` (zero file overlap with this PR; no `main`-branch control run obtainable since the job is PR-only-triggered). No schema/migration/route/workflow file touched. Machine-readable companion: [F2-IMPL-001-A_additive_unused_internal_imaging_facade_skeleton.json](F2-IMPL-001-A_additive_unused_internal_imaging_facade_skeleton.json) (`r3AtomicLegalHold` object). Final CI status recorded in this task's delivery report. Merge safe: no. Deployment safe: no. |

| F2-IMPL-001-A merge reconciliation (2026-08-03, not a new evidence file) | F2-IMPL-001-A-R3 → `MERGED` (phase F2, reconciliation only) | `gh pr view 304` confirms PR #304 **`MERGED`** — head `56be3e324f27f9f9ee125f6fbda1b2d386a2b554`, merge commit `c731e7af9a552dde2da45fee52fa318192da28f8`, `mergedAt: 2026-08-03T17:47:49Z`; post-merge main CI run `30838364039` (`ci-main-and-nightly`, `push`) `conclusion: success` — both independently confirmed by F2-GUARDRAIL-PREP-010-D (below). Corrects the R3 row above, whose own text still reads "Merged: no" (not edited in place, per this program's convention). |
| [F2-GUARDRAIL-PREP-010-A_CROSS_DOMAIN_ACCESS_INVENTORY.md](F2-GUARDRAIL-PREP-010-A_CROSS_DOMAIN_ACCESS_INVENTORY.md) | F2-GUARDRAIL-PREP-010-A-R1 (phase F2, guardrail-baseline discovery, PR [#313](https://github.com/MustafaBasol/DisKlinikCRM/pull/313), head `2f38424a26c5f0bee7ca731352cfcbe5ef74c4a7`, `NOT_MERGED`) | **`VERIFIED_BRANCH_EVIDENCE` / `PR_CI_PASSED` / `AWAITING_CONSOLIDATION` — this file does not yet exist on `main`; not authoritative repository state until merged.** Freezes 71 exact cross-domain edges (identity tuple `(callerPath, callerSymbol, ownerDomain, targetModelOrSymbol, accessKind)`; classified `ACCEPTED_PUBLIC_CONTRACT` 12 / `ACCEPTED_RESTRICTED_CONTRACT` 7 / `UNRESOLVED_OWNERSHIP` 5 / `VERIFIED_SECURITY_DEFECT_REQUIRES_SEPARATE_FIX` 1 / `PROHIBITED_NEW_PATTERN` 10 / `LEGACY_ALLOWLISTED_DIRECT_ACCESS` 36); the legacy-allowlisted bucket is explicitly descriptive-only, never a target architecture or new-edge authorization. Independently re-verified by F2-GUARDRAIL-PREP-010-D (below): counts/tuple fields confirmed by direct read; `gh pr checks 313` 9/9 `pass` (run `30834243419`); `gh api graphql reviewThreads` 4/4 `isResolved:true`; touches no shared program-control document. Machine-readable companion: [F2-GUARDRAIL-PREP-010-A_cross_domain_access_inventory.json](F2-GUARDRAIL-PREP-010-A_cross_domain_access_inventory.json). Reconciled by [F2-GUARDRAIL-PREP-010-D](F2-GUARDRAIL-PREP-010-D_PARALLEL_WAVE_CONSOLIDATION.md). |
| [F2-GUARDRAIL-PREP-010-B_TENANT_SCOPE_USAGE_AND_BYPASS_INVENTORY.md](F2-GUARDRAIL-PREP-010-B_TENANT_SCOPE_USAGE_AND_BYPASS_INVENTORY.md) | F2-GUARDRAIL-PREP-010-B-R1 (phase F2, guardrail-baseline discovery, PR [#315](https://github.com/MustafaBasol/DisKlinikCRM/pull/315), head `d14a75fc9c491b310f7b946af4b309a78f3bd8b9`, `NOT_MERGED`) | **`VERIFIED_BRANCH_EVIDENCE` / `PR_CI_PASSED` / `AWAITING_CONSOLIDATION` — this file does not yet exist on `main`; not authoritative repository state until merged.** Inventories 127 tenant-scope records (109 non-imaging route + 2 imaging + 16 job/worker); discovers **F2-SEC-001** (Instagram inbox status-mutation route, no clinic-membership check) and **F2-SEC-002** (legacy WhatsApp public API, global default-clinic resolution). **Both independently re-confirmed present on current `origin/main` by F2-GUARDRAIL-PREP-010-D (below) via direct source read of `server/src/routes/instagramInbox.ts` and `server/src/routes/whatsapp.ts` — these are real, live, unpatched tenant-isolation defects, not merely cited from this branch.** `gh pr checks 315` 9/9 `pass` (run `30835217833`); `gh api graphql reviewThreads` 2/2 `isResolved:true`; touches no shared program-control document. Machine-readable companion: [F2-GUARDRAIL-PREP-010-B_tenant_scope_usage_and_bypass_inventory.json](F2-GUARDRAIL-PREP-010-B_tenant_scope_usage_and_bypass_inventory.json). Reconciled by [F2-GUARDRAIL-PREP-010-D](F2-GUARDRAIL-PREP-010-D_PARALLEL_WAVE_CONSOLIDATION.md). |
| [F2-GUARDRAIL-PREP-010-C_REFERENCE_PUBLIC_CONTRACT_PATTERN_INVENTORY.md](F2-GUARDRAIL-PREP-010-C_REFERENCE_PUBLIC_CONTRACT_PATTERN_INVENTORY.md) | F2-GUARDRAIL-PREP-010-C-R1 (phase F2, guardrail-baseline discovery, PR [#314](https://github.com/MustafaBasol/DisKlinikCRM/pull/314), head `f1408546040eaa5fa7ddd5833a28b097ea15e3a5`, `NOT_MERGED`) | **`VERIFIED_BRANCH_EVIDENCE` / `PR_CI_PASSED` / `AWAITING_CONSOLIDATION` — this file does not yet exist on `main`; not authoritative repository state until merged.** Inspects 7 reference tenant-scoped-mutation-pattern candidates; selects 3 (`CAND-01` Billing financial-select restrictor, `CAND-02` `findTreatmentCaseInClinic` scoped-ownership guard, `CAND-03` `resolvePatient` parent-resolution) as `SELECTED_REFERENCE_BASE`, each additionally `HARDENING_REQUIRED_BEFORE_CANONICAL_ADOPTION`/`NOT_APPROVED_AS_CANONICAL_CONTRACT`; **no canonical tenant-scoped mutation contract exists.** `CAND-07` = the `ImagingLifecyclePort` facade (F2-IMPL-001-A) — classified `REJECTED_AS_REFERENCE` on this branch, pinned to a stale pre-R2 head; this document's own §11 already independently discovered (mid-review) that PR #304 had since closed the tenant-authorization gap, but declined to reclassify. **F2-GUARDRAIL-PREP-010-D (below) performs that reclassification: `STRENGTHENED_REFERENCE_BASE`, still not promoted to canonical.** `gh pr checks 314` 9/9 `pass` (run `30834413566`); 0 review threads; touches no shared program-control document. Machine-readable companion: [F2-GUARDRAIL-PREP-010-C_reference_public_contract_pattern_inventory.json](F2-GUARDRAIL-PREP-010-C_reference_public_contract_pattern_inventory.json). Reconciled by [F2-GUARDRAIL-PREP-010-D](F2-GUARDRAIL-PREP-010-D_PARALLEL_WAVE_CONSOLIDATION.md). |
| [F2-GUARDRAIL-PREP-010-D_PARALLEL_WAVE_CONSOLIDATION.md](F2-GUARDRAIL-PREP-010-D_PARALLEL_WAVE_CONSOLIDATION.md) | F2-GUARDRAIL-PREP-010-D (phase F2, parallel-wave consolidation and guardrail-authorization gate, documentation/evidence-reconciliation/program-control only, `AGENT_COMPLETED`, not merged) | Reconciles PR #304 (F2-IMPL-001-A, confirmed `MERGED`/`MAIN_CI_PASSED`) against the three PRs above (#313/#315/#314), none of which is merged by this task. Independently re-verifies every reported count/claim against branch content and live GitHub state rather than accepting the assigning prompt's figures at face value. Normalizes F2-SEC-001 (Instagram)/F2-SEC-002 (WhatsApp) — no conflicting reference found anywhere. Adopts PR #313's frozen exact-edge tuple identity unchanged; records the legacy-allowlist as descriptive-only; records the canonical-mutation-contract gap as still open; re-evaluates CAND-07 as `STRENGTHENED_REFERENCE_BASE` (not canonical). **Decision: CI guardrail implementation is NOT authorized while F2-SEC-001/F2-SEC-002 remain open** — no narrower safe subset is authorized either, pending a future task cross-referencing PR #313's own single security-defect edge against F2-SEC-001/F2-SEC-002. Recommends merge order: this consolidation, then #315, #313, #314. Stage 2 Imaging remains blocked, not started. No application/schema/migration/test/workflow/package/ESLint/dependency-cruiser file touched; no other branch's files written. Full details in `CURRENT_PHASE.md`'s F2-GUARDRAIL-PREP-010-D entry and `NORAMEDI_MASTER_TRACKER.md` §4/§7/§12/§13. Machine-readable companion: [F2-GUARDRAIL-PREP-010-D_parallel_wave_consolidation.json](F2-GUARDRAIL-PREP-010-D_parallel_wave_consolidation.json). |
| F2-GUARDRAIL-PREP-010-A/B/C merge reconciliation (2026-08-04, F2-DOC-001, not new evidence files) | F2-DOC-001 (phase F2, guardrail evidence status reconciliation, documentation-only) | Corrects the three rows above (F2-GUARDRAIL-PREP-010-A/B/C), each of which still reads `NOT_MERGED` / "this file does not yet exist on `main`; not authoritative repository state until merged" — accurate as authored (2026-08-03, pre-merge), now stale, not edited in place, per this file's own established convention (see the F2-IMPL-001-A merge-reconciliation row above). **All three evidence files are now present on `main`:** **PR #315** (F2-GUARDRAIL-PREP-010-B) merge commit `0881d389ade37606a561659409cc27e38857c318`, `mergedAt 2026-08-03T21:26:22Z`, PR CI `success`, post-merge main CI run `30854633672` `success`; **PR #313** (F2-GUARDRAIL-PREP-010-A) merge commit `9e4bd3cf12100f06c6068d29cb35d9d37f0031fa`, `mergedAt 2026-08-03T21:36:04Z`, PR CI `success`, post-merge main CI run `30855318200` `success`; **PR #314** (F2-GUARDRAIL-PREP-010-C) merge commit `e9c1765fa191223cb036ebfdb9c72b898e2fc52e`, `mergedAt 2026-08-03T21:45:29Z`, PR CI `success`, post-merge main CI run `30855966630` `success` — merge order #315, then #313, then #314 (the F2-GUARDRAIL-PREP-010-D consolidation, PR #317, had already merged first). Status for all three: `AGENT_COMPLETED` / `EVIDENCE_VALIDATED` / `PR_CI_PASSED` / `REVIEW_THREADS_RESOLVED` / `MERGED` / `MAIN_CI_PASSED` / `NOT_DEPLOYED` / `NOT_PRODUCTION_VERIFIED`. **These files remain evidence, not the authoritative live-status source** (§1 above, this program's source hierarchy) — the F2-GUARDRAIL-PREP-010-D consolidation's own decisions (CAND-07 `STRENGTHENED_REFERENCE_BASE`, not canonical; guardrail CI not authorized; the frozen 71-edge tuple identity; the legacy-allowlist descriptive-only status) remain authoritative over any raw evidence file's own text, unchanged by this reconciliation — notably PR #314's own raw file still reads CAND-07 as `REJECTED_AS_REFERENCE` at its own original observation head, a historical candidate classification only, not edited by this task. **Security status, current (not touched by this task; verified live via `gh pr view` immediately before this task started):** F2-SEC-001's fix is tracked separately by **PR #318** — `OPEN`, head `0bb832baf78be430cfdfd428382084b14b042fae`, `NOT_MERGED`, `NOT_DEPLOYED`, `NOT_PRODUCTION_VERIFIED` (CI still partially in-progress at last check, re-verify at merge time); F2-SEC-002's fix is tracked separately by **PR #319** — `OPEN`, head `e53116c393d02be900ce48e187e1d90c59a6d015`, CI all `SUCCESS`, `NOT_MERGED`, `NOT_DEPLOYED`, `NOT_PRODUCTION_VERIFIED`. **Guardrail CI implementation remains NOT AUTHORIZED and Stage 2 Imaging remains BLOCKED until both PR #318 and PR #319 close (merge + post-merge main CI).** No evidence file conclusion, count, or classification altered by this task — only this row added. |
| Late-breaking security-status correction (2026-08-04, same task F2-DOC-001, discovered before commit) | F2-DOC-001 | The row immediately above records F2-SEC-001/PR #318 as `OPEN`/`NOT_MERGED` — accurate moments earlier in this same task, now stale. Re-checked via `gh pr view 318`: **`MERGED`**, merge commit `2452b8c4a4fcd963878bbc0fbc37045d9cef170c`, `mergedAt 2026-08-04T08:18:03Z`; post-merge main CI confirmed via `gh run list`/check-runs on that commit: run `30891381612` `success`, all 9 `ci-layers` checks `success`. **F2-SEC-001: `MERGED` / `MAIN_CI_PASSED` / `NOT_DEPLOYED` / `NOT_PRODUCTION_VERIFIED`.** F2-SEC-002/PR #319 remains `OPEN`/`NOT_MERGED`, unchanged. **Guardrail CI implementation remains NOT AUTHORIZED and Stage 2 Imaging remains BLOCKED** — closing one of two blockers does not authorize either. No evidence file conclusion, count, or classification altered; the F2-SEC-001/F2-SEC-002 branches themselves are not touched by this task. |
| F2-SEC-002 evidence refresh (2026-08-04, F2-DOC-001-R1, PR #320 review follow-up, not a new evidence file) | F2-DOC-001-R1 | The two rows above cite F2-SEC-002/PR #319 at head `e53116c393d02be900ce48e187e1d90c59a6d015` — accurate as recorded, now stale; not rewritten in place. **Re-verified via `gh pr view 319`/`gh pr checks 319`/`gh run view 30893070050`: PR #319 current head `fb4e23766478db413f9e34f009302d4f4f0948f7`, PR CI run `30893070050` (`ci-layers`, all jobs) `success`, still `OPEN`/`NOT_MERGED`/`NOT_DEPLOYED`/`NOT_PRODUCTION_VERIFIED`.** F2-SEC-001/PR #318's `MERGED`/`MAIN_CI_PASSED` status (row above) is unchanged and re-confirmed. **Current status, restated: F2-SEC-001 `MERGED`/`MAIN_CI_PASSED`; F2-SEC-002 `OPEN`/`NOT_MERGED`; guardrail CI implementation `NOT_AUTHORIZED`; Stage 2 Imaging `BLOCKED`.** Triggered by a review finding that `phases/F2_MODULAR_BOUNDARIES.md`'s own "Faz durumu" current-status line still read both PR #318 and PR #319 as open, contradicting its own late-breaking-correction paragraph — that line is corrected directly in that file. F2-SEC-002 is not closed by this task; guardrail CI is not authorized by this task; Stage 2 Imaging is not unblocked by this task. No evidence file conclusion, count, or classification altered. Files also corrected by this same task: `CURRENT_PHASE.md`, `NORAMEDI_MASTER_TRACKER.md`, `phases/F2_MODULAR_BOUNDARIES.md`. |
| F2-SEC-002 merge reconciliation (2026-08-04, F2-DOC-001-R2, PR #320 review follow-up, not a new evidence file) | F2-DOC-001-R2 | The row above cites F2-SEC-002/PR #319 as `OPEN`/`NOT_MERGED` at head `fb4e23766478db413f9e34f009302d4f4f0948f7` — accurate as recorded, now stale; not rewritten in place. **PR #319 has since merged:** `gh pr view 319` confirms state `MERGED`, merge commit `09ee20b7f1f655a4025a32927a8e81e596e1bb97`, independently confirmed an ancestor of `origin/main` (`git merge-base --is-ancestor`, exit `0`; also now `origin/main`'s exact tip). It adds two new standalone evidence files, [F2-SEC-001_INSTAGRAM_INBOX_CLINIC_MEMBERSHIP.md](F2-SEC-001_INSTAGRAM_INBOX_CLINIC_MEMBERSHIP.md) and [F2-SEC-002_WHATSAPP_EXPLICIT_CLINIC_RESOLUTION.md](F2-SEC-002_WHATSAPP_EXPLICIT_CLINIC_RESOLUTION.md), pulled into this branch via `git merge --no-ff origin/main` (zero conflicts — neither PR #318 nor PR #319 touched this file or the other three shared program-control documents). **Post-merge main CI status at the time of this row:** run `30949232722` — `status: in_progress`, now stale, see row below. **F2-SEC-002 status at the time of this row: `MERGED` / `MAIN_CI_PENDING`.** This documentation task does not itself authorize guardrail CI or unblock Stage 2 Imaging. No evidence file conclusion, count, or classification altered by this task (the two new evidence files' own conclusions are theirs, not authored here). Files also corrected by this same task: `CURRENT_PHASE.md`, `NORAMEDI_MASTER_TRACKER.md`, `phases/F2_MODULAR_BOUNDARIES.md`. |
| F2-SEC-002 post-merge main CI confirmation (2026-08-04, F2-DOC-001-R3, PR #320, not a new evidence file) | F2-DOC-001-R3 | The row above recorded post-merge main CI run `30949232722` as `in_progress` — that run has since completed. Re-verified via `gh run view 30949232722`: `headSha` `09ee20b7f1f655a4025a32927a8e81e596e1bb97` (exact match), `status: completed`, `conclusion: success`, `event: push`, `workflowName: ci-main-and-nightly`. **F2-SEC-002 final status: `MERGED` / `MAIN_CI_PASSED` / `NOT_DEPLOYED` / `NOT_PRODUCTION_VERIFIED`.** **Both concrete security blockers are now repository-level closed: F2-SEC-001 `MERGED`/`MAIN_CI_PASSED`; F2-SEC-002 `MERGED`/`MAIN_CI_PASSED`.** This does **not** authorize guardrail CI implementation, does **not** unblock Stage 2 Imaging, and does **not** accept any canonical tenant-scoped mutation contract. **A further F2-SEC-003 reconciliation remains required** (deployment/production-verification for both PRs is unaddressed by merge/CI alone). Guardrail CI: `NOT_AUTHORIZED`. Stage 2 Imaging: `BLOCKED`. No evidence file conclusion, count, or classification altered. Files also corrected by this same task: `CURRENT_PHASE.md`, `NORAMEDI_MASTER_TRACKER.md`, `phases/F2_MODULAR_BOUNDARIES.md`. |
| [F2-SEC-003_SECURITY_GATE_RECONCILIATION.md](F2-SEC-003_SECURITY_GATE_RECONCILIATION.md) | F2-SEC-003 (phase F2, security blocker closure, deployment-readiness analysis, and guardrail authorization reconciliation, documentation/repository-verification only, `AGENT_COMPLETED`, not merged) | Independently re-verifies PR #318/#319/#320's exact head/merge/CI facts and their merge commits' ancestry of `origin/main` (`be72eb9b44829c4e6ad9d88e6bd80bcfe88c8fdd`). Independently re-confirms, by direct source-code and test-file read plus verification of already-passing CI-owned disposable-PostgreSQL test results (no local/task-specific test re-execution by this task), that F2-SEC-001 (`instagramInbox.ts` `PATCH .../status`, atomic clinic-membership-scoped `updateManyAndReturn`, no pre-write target-row read) and F2-SEC-002 (six legacy `/api/public/whatsapp/*` routes, shared connection-bound resolution middleware, fail-closed on zero/multiple secret matches and non-unique/org-inconsistent clinic links) are both `CONFIRMED CLOSED` at the repository level on current `main`. Classifies deployment readiness: F2-SEC-001 `CODE_DEPLOYABLE`; F2-SEC-002 `CONFIGURATION_VERIFICATION_REQUIRED` (production `WhatsAppConnection`/`ClinicWhatsAppConnection` topology not inspected — no production access). Defines (does not execute) pre-deploy read-only SQL queries, smoke tests, rollback plans, and a 15-scenario production-verification checklist. Authorizes repository-only, non-blocking/report-only guardrail implementation only; explicitly withholds CI-blocking/production enforcement authorization pending baseline/false-positive validation plus F2-SEC-001/F2-SEC-002 deployment + production verification. Names, does not implement, `F2-GUARDRAIL-IMPL-001`. Stage 2 Imaging remains `BLOCKED`. No runtime/schema/migration/test/workflow/package file touched. Machine-readable companion: none (documentation-only). |
| [F2-GUARDRAIL-IMPL-001_EXACT_EDGE_GUARDRAIL_IMPLEMENTATION.md](F2-GUARDRAIL-IMPL-001_EXACT_EDGE_GUARDRAIL_IMPLEMENTATION.md) | F2-GUARDRAIL-IMPL-001 (phase F2, implementation, repository-only/report-only/non-blocking, `AGENT_COMPLETED` / `TESTS_PASSED` / `PR_OPENED` once opened — `NOT_MERGED` / `NOT_DEPLOYED` / `NOT_PRODUCTION_VERIFIED`) | Implements the guardrail F2-SEC-003 §6 named but did not build: a small TypeScript-compiler-API CLI (`scripts/architecture-guardrail/`, no new dependency) that scans exactly the five authorized roots (`server/src/{routes,services,jobs,middleware,utils}`), extracts cross-domain relative-import edges into the frozen `(callerPath, callerSymbol, ownerDomain, targetModelOrSymbol, accessKind)` tuple, and compares them (read-only) against the existing `F2-GUARDRAIL-PREP-010-A` 71-edge baseline — no new baseline artifact is committed. Deterministic (byte-identical across two runs, verified), zero absolute paths, zero secret patterns, 56/56 focused unit tests passing, exit 0 regardless of finding count (non-zero only for a genuine config/tool failure, tested both as a pure function and a real spawned subprocess). Real-scope run on `origin/main` @ `da23f6f`: 1,031 findings (1,018 `NEW` / 13 `EXISTING`), 0 errors, 0 warnings — the high `NEW` count is expected/documented (exhaustive detection vs. a curated manual baseline), explicitly **not** claimed as 1,018 real violations. Adds one new standalone, non-blocking Layer 1 CI job (`architecture-guardrail-report-only` in `ci-layers.yml`, SHA-pinned actions, not in any `needs:` list, nothing needs it). Does **not** authorize CI-blocking enforcement or claim any tenant-isolation proof — every report carries an explicit advisory-only tenant-scope disclaimer. Machine-readable companion: [F2-GUARDRAIL-IMPL-001_exact_edge_guardrail_implementation.json](F2-GUARDRAIL-IMPL-001_exact_edge_guardrail_implementation.json). |
| [F2-GUARDRAIL-VAL-001_SIGNAL_QUALITY_VALIDATION.md](F2-GUARDRAIL-VAL-001_SIGNAL_QUALITY_VALIDATION.md) | F2-GUARDRAIL-VAL-001 (phase F2, read-only validation and evidence, no scanner/domain-map/application code change, `AGENT_COMPLETED` / `TESTS_PASSED` / `PR_OPENED` once opened — `NOT_MERGED` / `NOT_DEPLOYED` / `NOT_PRODUCTION_VERIFIED`) | Performs the false-positive-validation pass F2-SEC-003/F2-GUARDRAIL-IMPL-001 both named as the prerequisite before any CI-blocking-enforcement proposal. Re-runs the guardrail unchanged, reproducing identical counts (247 files, 1,031 findings, 1,018 `NEW`/13 `EXISTING`, deterministic). Draws a deterministic, checked-in, reproducible 169-finding stratified sample (exceeds the required 120 minimum) covering every required stratum, and classifies all 169 via direct source reads against the 9-category taxonomy: 87 `EXPECTED_PLATFORM_SHARED_EDGE`, 50 `EXPECTED_PUBLIC_CONTRACT_EDGE`, 15 `DOMAIN_CLASSIFICATION_FALSE_POSITIVE`, 9 `LEGACY_TECHNICAL_DEBT` (0 new — all trace to prior evidence), 7 `DATA_OWNERSHIP_REVIEW_REQUIRED`, 1 `REAL_BOUNDARY_VIOLATION` (genuinely new), 0 `PARSER_FALSE_POSITIVE`/`GENERATED_OR_TOOLING_NOISE`. Independently reconciles all 4 F0-003-declared ownership collisions against `F0-003_MODULE_OWNERSHIP_EVIDENCE.md` (3 recommended for map correction — `utils/encryption.ts`/`utils/secrets.ts` are F0-003's own "true platform primitives"; `services/treatmentStockDeduction.ts`'s location is already settled — 1, `routes/organizationDashboard.ts`, requires a program-owner ADR). Separately discovers and characterizes a much larger domain-map coverage gap (23 files/~250 findings `UNRESOLVED` purely from missing map entries) and a directly-measured `NEW`/`EXISTING` baseline-match undercount (12/12 sampled already-accepted edges misreported `NEW`). **Enforcement readiness assessed against all 8 required criteria: `NOT_READY`.** No scanner, domain-map, or application code change made — every finding is a data-completeness gap, not a scanner logic defect. Does not authorize CI-blocking enforcement. Machine-readable companions: [tooling/F2-GUARDRAIL-VAL-001_raw_scan_report.json](tooling/F2-GUARDRAIL-VAL-001_raw_scan_report.json), [tooling/F2-GUARDRAIL-VAL-001_sample_manifest.json](tooling/F2-GUARDRAIL-VAL-001_sample_manifest.json), [tooling/F2-GUARDRAIL-VAL-001_classified_sample.json](tooling/F2-GUARDRAIL-VAL-001_classified_sample.json); reproducible sampling script `scripts/architecture-guardrail-validation/buildSample.js`. |
| [F2-GUARDRAIL-VAL-003_BASELINE_MATCH_KEY_RECONCILIATION.md](F2-GUARDRAIL-VAL-003_BASELINE_MATCH_KEY_RECONCILIATION.md) | F2-GUARDRAIL-VAL-003 (phase F2, baseline-data authoring only, no scanner/matcher code change, `AGENT_COMPLETED` / `TESTS_PASSED` / `PR_OPENED` once opened — `NOT_MERGED` / `NOT_DEPLOYED` / `NOT_PRODUCTION_VERIFIED`) | **Task-ID correction:** the assigning brief's `F2-GUARDRAIL-PREP-010-B` was already taken (PR #315, tenant-scope inventory) — adopted as `F2-GUARDRAIL-VAL-003`, continuing the VAL sequence, verified unclaimed before adoption. Performs the baseline-entry authoring `F2-GUARDRAIL-VAL-002` §9/§15 named as its own next, separately-scoped follow-up for the ~88 `F2-GUARDRAIL-VAL-001`-classified-`B`/`C` findings still reporting `NEW`. Independently re-derived the exact 88-candidate set (cross-referencing VAL-001's 137 `B`/`C` sample against a fresh before-scan by finding ID) and **disproved** VAL-002's own causal claim that a `callerSymbol` match-key defect was responsible: `lib/baseline.ts`'s `callerSymbol` exclusion is intentional, already correct, and already covered by a passing regression test (`__tests__/baseline.test.ts:58-68`); direct inspection of all 88 candidates against the 71-edge baseline found 87/88 have no baseline entry at all for their exact edge and 1/88 has entries for the same caller file covering a different edge — a pure data-completeness gap, not a matcher defect. Selected Option D (author exact baseline entries, no matcher change) after explicitly rejecting Options A/B/C on this evidence. All 88 candidates individually re-verified against current source (import re-confirmed present in each caller file; each of the 48 distinct target files' own header read directly) before being added as `CDA-073..CDA-160` in `F2-GUARDRAIL-PREP-010-A_cross_domain_access_inventory.json` (append-only — all 71 pre-existing edges byte-verified unchanged). Before/after scan (each run twice/thrice, byte-identical): 1,039 total findings unchanged; `NEW` 1,024 → 846 (**−178**); `EXISTING` 15 → 193 (**+178**) — the −178 (not −88) fully traced: 88 explicitly-verified candidates plus 90 "swept-in" same-edge sibling imports (additional named symbols from the same already-vetted target in the same already-verified caller file, matched because `callerSymbol` is correctly excluded from the key), 100% attributable by exact key, 0 unattributed, 0 involving `organizationDashboard.ts`. 47/88 new entries touch a high-risk domain — every one individually reviewed: all are routes/services/jobs importing centralized security/tenant/audit/privacy utilities, the correct pattern, none hiding a violation. No scanner/matcher/application/schema/migration/tenant/auth code touched. 74/74 guardrail unit tests, 74/74 runtime-orchestrator unit tests, `typecheck:guardrail` clean, `git diff --check` clean. Machine-readable companions: [tooling/F2-GUARDRAIL-VAL-003_candidate_analysis.json](tooling/F2-GUARDRAIL-VAL-003_candidate_analysis.json), [tooling/F2-GUARDRAIL-VAL-003_accepted_baseline_entries.json](tooling/F2-GUARDRAIL-VAL-003_accepted_baseline_entries.json), [tooling/F2-GUARDRAIL-VAL-003_candidate_classification.json](tooling/F2-GUARDRAIL-VAL-003_candidate_classification.json), [tooling/F2-GUARDRAIL-VAL-003_swept_in_siblings.json](tooling/F2-GUARDRAIL-VAL-003_swept_in_siblings.json), [tooling/F2-GUARDRAIL-VAL-003_before_scan_run1.json](tooling/F2-GUARDRAIL-VAL-003_before_scan_run1.json) / [tooling/F2-GUARDRAIL-VAL-003_after_scan_run1.json](tooling/F2-GUARDRAIL-VAL-003_after_scan_run1.json). **§15 addendum (F2-GUARDRAIL-VAL-003-R1, same file, same PR #330, not merged):** fixed 4 automated-review findings in the reproducible scripts (loose basename import matching → full normalized-path matching; unstripped `/**` docblock opener leaking into evidence text; unused `explicit88Ids` → now an independent cross-check, hard-asserted; a repeated baseline filter → precomputed once) and re-ran the entire derivation pipeline from a restored pre-VAL-003 baseline. **All 88 entries independently re-verified as still valid — 0 removed, 0 reclassified**; the only change is cosmetic (`acceptedEvidence` text cleaned of leaked `/**` tokens) on 24 of the 88 entries (`CDA-074, CDA-080, CDA-081, CDA-083, CDA-084, CDA-090, CDA-093, CDA-106, CDA-109, CDA-114, CDA-118, CDA-121, CDA-124, CDA-125, CDA-126, CDA-141, CDA-146, CDA-148, CDA-149, CDA-154, CDA-155, CDA-156, CDA-157, CDA-160`). The 88/90/178/0/0 explicit/swept-in/total-flip/unattributed/organizationDashboard proof is unchanged and now independently cross-checked from two separately-sourced evidence artifacts. 30/30 focused tests (22 pre-existing + 8 new regression tests for the docblock fix in `__tests__/generateVal003Header.test.js` + 12 new self-check tests for the matcher fix in `__tests__/verifyVal003Matcher.test.js`), `typecheck:guardrail`/`guardrail:test` (74/74)/`test:runtime:unit` (74/74)/`guardrail:scan`/`git diff --check` all clean. **Rebase addendum (F2-GUARDRAIL-VAL-003-R2, same PR #330, not merged):** PR #330 was rebased onto post-F2-ADR-ORG-DASH-002 `origin/main` (`9196ceb9a8beff561af55ff2d15b13c0a7874bc2`, new head `52e99d1c1fe8b25a453ce64125ce65624898791f`); the entire pipeline was independently re-derived from scratch against the rebased head — 88/88/90/178/0/0 counts, and every tooling JSON companion, unchanged from R1. `organizationDashboard.ts` isolation re-verified (still `core-org-clinic-membership` per PR #331, 0 involvement). See `CURRENT_PHASE.md`/`NORAMEDI_MASTER_TRACKER.md`/`phases/F2_MODULAR_BOUNDARIES.md` for full detail. |
| [F2-GUARDRAIL-VAL-002_DOMAIN_MAP_AND_BASELINE_RECONCILIATION.md](F2-GUARDRAIL-VAL-002_DOMAIN_MAP_AND_BASELINE_RECONCILIATION.md) | F2-GUARDRAIL-VAL-002 (phase F2, domain-map completeness correction, config-only, `AGENT_COMPLETED` / `TESTS_PASSED` / `PR_OPENED` once opened — `NOT_MERGED` / `NOT_DEPLOYED` / `NOT_PRODUCTION_VERIFIED`) | Performs the domain-map completeness correction F2-GUARDRAIL-VAL-001 handed off as its exact next task. Exhaustive (not sampled) audit found 70 of 247 scan-root files (plus 2 further valid import-target files outside the scan roots) completely absent from `domain-map.json`, cross-referenced against the more-current, already-accepted `F2-PREP-001` 38-domain inventory (33 of 70 resolved by verbatim citation) and own reverse-dependency-tool evidence (`scripts/architecture-guardrail-validation/buildReverseDeps.ts`, checked in, reuses the guardrail's own import-resolution logic) for the remaining 37. Applied, each with per-file caller/import/tenant-impact evidence: 72 new entries (introducing 2 new domains — `core-shared-platform-infrastructure`, `messaging-shared-channel-infrastructure` — plus the already-accepted `external-calendar-integration` F2-PREP-001 domain), 5 path-heuristic reclassifications, 3 of the 4 F0-003-declared ownership collisions resolved (`utils/encryption.ts`/`utils/secrets.ts` -> new `core-platform-crypto` domain; `services/treatmentStockDeduction.ts` -> its settled `clinical-dental-chart-procedures` location), and 2 non-matching dead-key removals. `routes/organizationDashboard.ts`'s collision and its real `getDateRange` boundary violation are deliberately left untouched/actionable, per the assigning brief, pending a required program-owner ADR. Domain-map completeness independently re-verified: 0 of 247 scan-root files unmapped (was 70). Before/after scan, each run twice and byte-compared for determinism: 1,031 -> 1,038 findings (net +7, individually traced to 61 truly-added previously-hidden edges minus 54 truly-removed false-cross-domain edges, not a raw reduction); `UNRESOLVED`-owner findings 308 -> 1; `UNRESOLVED`-caller findings 75 -> 4; one baseline edge (`CDA-031`) newly machine-matches `EXISTING` purely as a side effect, with zero baseline-file edit. All 54 removed findings individually risk-checked; exactly one touched a high-risk domain and was independently confirmed a same-domain false positive, not a suppressed violation. **Enforcement readiness re-assessed against all 8 VAL-001 criteria: `NOT_READY`** (unchanged classification — meaningfully stronger evidence, but the `callerSymbol`-exclusion baseline-match limitation, the `organizationDashboard.ts` ADR, program-owner sign-off, and a fresh quantified false-positive re-sample all remain open). Explicitly declines to bulk-author new `F2-GUARDRAIL-PREP-010-A` baseline entries for the ~88 already-VAL-001-classified-but-still-`NEW` findings, handing that off as a separately-scoped follow-up requiring the same per-edge rigor as the original baseline task. 74/74 guardrail unit tests and 74/74 runtime-orchestrator unit tests pass both before and after. No scanner (`lib/`, `cli.ts`), scan-roots, baseline-file, or application-source change. Machine-readable companions: [tooling/F2-GUARDRAIL-VAL-002_before_scan_run1.json](tooling/F2-GUARDRAIL-VAL-002_before_scan_run1.json) / [tooling/F2-GUARDRAIL-VAL-002_after_scan_run1.json](tooling/F2-GUARDRAIL-VAL-002_after_scan_run1.json), [tooling/F2-GUARDRAIL-VAL-002_additions_evidence.json](tooling/F2-GUARDRAIL-VAL-002_additions_evidence.json), [tooling/F2-GUARDRAIL-VAL-002_reclassifications_and_collisions_evidence.json](tooling/F2-GUARDRAIL-VAL-002_reclassifications_and_collisions_evidence.json), [tooling/F2-GUARDRAIL-VAL-002_edges_newly_surfaced_correct_classification.json](tooling/F2-GUARDRAIL-VAL-002_edges_newly_surfaced_correct_classification.json), [tooling/F2-GUARDRAIL-VAL-002_edges_removed_same_domain_corrected.json](tooling/F2-GUARDRAIL-VAL-002_edges_removed_same_domain_corrected.json). |

| [F1-004-P1-R2_EMERGENCY_CONTACT_UPDATE_RACE_ROOT_CAUSE_AND_FIX.md](F1-004-P1-R2_EMERGENCY_CONTACT_UPDATE_RACE_ROOT_CAUSE_AND_FIX.md) | F1-004-P1-R2 (phase F1 — Engineering Safety Net and Concurrency Verification, `AGENT_COMPLETED` / `TESTS_PASSED` / `PR_OPENED`, PR #325, not merged) | Main CI run [31002888303](https://github.com/MustafaBasol/DisKlinikCRM/actions/runs/31002888303) failed `patientEmergencyContactsPrimaryConcurrency.test.ts`'s concurrent-UPDATE scenario (`A=200,B=200`) on a commit that already included PR #310's advisory-lock fix; a rerun passed. Root-caused (deterministically reproduced via a forced-interleaving harness, not left as a correlation) to PR #310's "prior primary" snapshot being read via a separate, unlocked query independently racing its own transaction's connection checkout against the shared 10-connection pg pool — the database's single-primary invariant was never violated, only the "loser must get 409" HTTP contract was. A SERIALIZABLE-isolation alternative was implemented, verified to correctly catch genuine overlaps via Postgres's own predicate-lock detection, and then rejected after it caused false-positive 409s between unrelated patients on this sparse table (sequential-scan-driven table/page-granularity predicate locks) — an unacceptable cross-tenant regression its own tenant-isolation test scenario caught. Fix: both reads now run on the same transaction/connection as each other, closing the proven mechanism without SERIALIZABLE. Stress coverage raised to 100 (CREATE race) and 150 (UPDATE race) rounds, verified 0 failures across 3 independent disposable-Postgres process executions. No migration. Machine-readable companion: [F1-004-P1-R2_emergency_contact_update_race_root_cause_and_fix.json](F1-004-P1-R2_emergency_contact_update_race_root_cause_and_fix.json). **§16–25 (F1-004-P1-R2-R3, same file, same PR, `AGENT_COMPLETED` / `TESTS_PASSED`, not merged):** deterministically reproduced (via real, production-embedded, no-op-unless-installed test synchronization hooks) the CREATE-race residual limitation R2 §15 observed in real CI (`A=201 B=201`) but left open — R2's design cannot detect a race where the loser's own first read is itself delayed past the winner's commit. Proved by elimination that no further server-only design (read/lock reordering, row-lock sentinel, SERIALIZABLE, wall-clock heuristic) can close this airtight; escalated per this task's own stop condition, program owner selected an additive, optional, backward-compatible `expectedCurrentPrimaryContactId` precondition on `POST`/`PUT`, enforced under the existing advisory lock as true optimistic-concurrency control. Verified airtight under the exact worst-case forced schedule (20/20 deterministic rounds) plus 500/500 token-protected CREATE and 500/500 token-protected UPDATE natural-race stress rounds; legacy (no-token) behavior unchanged and its known limitation permanently characterization-tested. Frontend updated to always send the precondition. No migration. |
| [F2-ADR-ORG-DASH-001_DATE_RANGE_BOUNDARY_REMEDIATION.md](F2-ADR-ORG-DASH-001_DATE_RANGE_BOUNDARY_REMEDIATION.md) | F2-ADR-ORG-DASH-001 (phase F2 — Modular Monolith Boundary Remediation, narrow additive/backward-compatible fix, isolated worktree/branch `refactor/f2-org-dashboard-date-range-boundary`, baseline `origin/main` @ `61ca9d82507b3f24f3c943104cb6aa20ad13faa0`, `AGENT_COMPLETED` / `TESTS_PASSED` / `GUARDRAIL_DELTA_VERIFIED` / `PR_OPENED` once opened, not merged) | Closes the sole `REAL_BOUNDARY_VIOLATION` F2-GUARDRAIL-VAL-001 found (§8 row above): `financeDashboard.ts` imported the generic, DB-free, timezone-naive `getDateRange()` date-math helper directly from `routes/organizationDashboard.ts` (a route module, not a public contract). Relocated the helper verbatim (Option 2 — existing platform-shared `utils/helpers.ts`, which already houses the same category of generic date helper and already carries 72 pre-existing cross-domain `UNRESOLVED`-target edges per F2-GUARDRAIL-VAL-001 §9's own domain-map-coverage-gap finding); rejected moving it into the reporting domain (would presuppose `organizationDashboard.ts`'s own deferred org/reporting ownership ADR, F2-GUARDRAIL-VAL-001 §9 collision #4, out of scope) and rejected a reporting facade (helper is not reporting-owned, second consumer is not a reporting subdomain). Both route files now import from `utils/helpers.ts`; no route imports another route for this symbol. Guardrail exact-edge diff (stash-based before/after on the identical commit): 1,031→1,031 total findings, 0/0 errors, 1,018/1,018 `NEW` baseline count, 499/499 high-risk-domain-touching findings — byte-identical except the exact violating edge (`financeDashboard.ts→routes/organizationDashboard.ts`) removed and one same-shape edge (`financeDashboard.ts→utils/helpers.ts`) added, joining the already-existing 72-member family targeting that file — not a new violation category, not achieved by baseline/allowlist edit (`config/domain-map.json` untouched). `server` typecheck clean; `test:finance` 25/25, `test:orgdash` 35/35 (both suites' assertions byte-unchanged, import path only updated); `guardrail:test` 74/74 (scanner itself untouched); `git diff --check` clean. No API/auth/tenant-scope/DB-query/migration change. Does not resolve `organizationDashboard.ts`'s own deferred ownership ambiguity — explicitly out of scope, unchanged. Machine-readable companion: none (documentation-only; the guardrail's own before/after JSON scans are process artifacts, not checked in, per this task's narrow scope). **§13 addendum (F2-ADR-ORG-DASH-001-R2, same file, same PR #328, `AGENT_COMPLETED` / `TESTS_PASSED` / `GUARDRAIL_DELTA_VERIFIED` / `REBASED_ONTO_POST_VAL_002_MAIN` / `PR_OPENED`, not merged):** rebased onto `origin/main` after F2-GUARDRAIL-VAL-002 (PR #329, merge commit `99436e5`, post-merge `main` CI `31093678375` `success`) — all three preconditions independently re-verified before rebasing. Conflicts in `CURRENT_PHASE.md`/`NORAMEDI_MASTER_TRACKER.md` resolved by keeping both tasks' entries and correcting this task's own stale references to `utils/helpers.ts`'s domain-map status; `domain-map.json` and `evidence/README.md` merged with no real conflict. The stale pre-VAL-002 "R1" no-op commit was dropped (`git rebase --skip`), replaced by this addendum. **Final mapped domain of `server/src/utils/helpers.ts`: `core-shared-platform-infrastructure`** (resolved by VAL-002, no longer `UNRESOLVED`); `organizationDashboard.ts` itself remains `UNRESOLVED`, deliberately, pending its own still-open ownership ADR. Re-run against the rebased head: `typecheck` clean, `test:finance` 25/25, `test:orgdash` 35/35, `guardrail:test` 74/74, `test:runtime:unit` 74/74, `git diff --check` clean. Two independent deterministic full scans byte-identical: 247/247 files, 0 skipped, 0 errors, 0 warnings, 1,039 findings (1,024 `NEW`/15 `EXISTING`). Before(`99436e5`)/after(rebased head) exact-edge diff: exactly one edge removed (`financeDashboard.ts→routes/organizationDashboard.ts`, the violation), exactly two added (`financeDashboard.ts→utils/helpers.ts` and `organizationDashboard.ts→utils/helpers.ts`, both `ownerDomain: core-shared-platform-infrastructure` — net +1 is expected, since `organizationDashboard.ts` now also imports rather than defines the helper). Findings targeting `organizationDashboard.ts`: 1→0. High-risk-domain-touching findings: 526→526, byte-identical. No `REAL_BOUNDARY_VIOLATION` introduced (both new edges join the existing 79→81-member platform-shared-utility family targeting `utils/helpers.ts`); 0/0 errors and 0 skipped files in both scans; `resolvedBaselineEdgeIds` unchanged. PR review state: 1 automated Copilot review (`COMMENTED`, no line comments), 0 inline review threads. New head pushed, **not merged**. |
| [F2-ADR-ORG-DASH-002_ORGANIZATION_DASHBOARD_OWNERSHIP.md](F2-ADR-ORG-DASH-002_ORGANIZATION_DASHBOARD_OWNERSHIP.md) | F2-ADR-ORG-DASH-002 (phase F2 — Modular Monolith Boundary Remediation / Guardrail Signal Quality; ADR + one-value static-analysis config change; isolated worktree/branch `docs/f2-adr-org-dashboard-002-ownership`, baseline `origin/main` @ `46acae8415020cb0bd340fbc854c4187c43e3662`; `AGENT_COMPLETED` / `TESTS_PASSED` / `GUARDRAIL_DELTA_VERIFIED` / `PR_OPENED`, PR #331, not merged) | Resolves the dual-domain ownership ambiguity of `server/src/routes/organizationDashboard.ts` — the **last** `UNRESOLVED` entry in `scripts/architecture-guardrail/config/domain-map.json` and the **fourth and final** F0-003-declared ownership collision, deferred in turn by F2-GUARDRAIL-VAL-001 §9 (collision #4), F2-GUARDRAIL-VAL-002 §5.5, and F2-ADR-ORG-DASH-001. Baseline independently verified: PRs #329 (`99436e5…`) and #328 (`46acae8…`, the current tip) both `MERGED` and ancestors of `origin/main`; post-merge `main` CI run `31098023681` `success`. **Baseline correction recorded:** the pre-created agent worktree was one commit behind at `99436e5`, so the branch was created explicitly from `origin/main`. **CodeGraph limitation recorded:** the `.codegraph/` index resolved to the primary worktree (HEAD `255392c`) and was demonstrably stale (still showed `getDateRange` inside `organizationDashboard.ts`); used only to corroborate caller-graph shape, with every ownership-relevant fact taken from direct source reads at the baseline SHA. **Decision (ADR [`docs/architecture/f2/ADR-F2-ORG-DASH-OWNERSHIP.md`](../../architecture/f2/ADR-F2-ORG-DASH-OWNERSHIP.md), Option A, HIGH confidence): owned by `core-org-clinic-membership`; no code split is a prerequisite.** Adjudicated on authorization semantics, tenant-scope derivation, owned-model access, response identity, and accepted consumer ownership — never on filename, never to reduce finding counts. Decisive: the route's gate (`authorize(['OWNER','ORG_ADMIN'])` + `canAccessOrganizationDashboard()` = `isOrganizationAdmin()`) is byte-identical to `organizationBranches.ts`'s organization-administration gate and strictly narrower than the gate all five `reports.ts` endpoints use (`OWNER|ORG_ADMIN|CLINIC_MANAGER|BILLING` = exactly `canAccessReports()`); the handler derives its own security boundary from `core-org-clinic-membership`-owned state (`Clinic.organizationId`/`Clinic.status`/`UserClinic`/`canAccessAllClinics`/`allowedClinicIds`); its sole consumer `src/pages/OrganizationDashboard.tsx` is already accepted-listed under `core-org-clinic-membership` in both F0-003 and F2-PREP-001; and the map already assigns dashboard routes by subject domain (`financeDashboard.ts` → `finance-advanced-compensation`). Eight-block decomposition: 6 blocks unambiguously ORG, 1 already relocated to `core-shared-platform-infrastructure` by merged PR #328, 1 mixed — **no block resolved to `reporting-analytics`**. Option B (reporting-owned) rejected; Option C (physical split) rejected for now — one handler, one composed response, no clean seam, would need ≥4 new contracts and risk response-shape drift, which the task's own authorization boundary requires be stopped and reported; **Option D (composition shell over accepted public read contracts) accepted as target end-state, not achievable now** since none of those contracts exists. **Implementation:** exactly one `domain-map.json` value (`UNRESOLVED` → `core-org-clinic-membership`) plus an additive `provenance.subsequentCorrections[]` note that supersedes VAL-002's entry **without rewriting it**; `fileCount` (249) and the key set unchanged; `provenance` is never read by the scanner. Zero application source, Prisma schema, migration, route-path, response-shape, or authorization change; guardrail `lib/`, `cli.ts`, `scan-roots.json`, the `F2-GUARDRAIL-PREP-010-A` baseline, and `ARCHITECTURE_DECISIONS.md` untouched. **Guardrail before/after (4 `--deterministic` runs, byte-compared pairwise; 415,403 B ×2 before, 415,483 B ×2 after — the 80-byte delta is exactly 5 × the 16-char label growth):** 1,039 → 1,039 total findings; `NEW`/`EXISTING` 1,024/15 unchanged; errors/warnings 0/0 and 247/247/0 files in all four runs; **0 tuples added, 0 removed, 0 finding IDs changed** (`computeFindingId` excludes `callerDomain`); `callerDomain: UNRESOLVED` **5 → 0**; findings targeting `organizationDashboard.ts` **0 → 0** (durably re-confirms PR #328's fix); high-risk-domain-touching **526 → 526, identical set by ID** (3 of the 5 relabelled findings target high-risk domains, all 3 survive); `resolvedBaselineEdgeIds` `["CDA-072"]` unchanged. **Exactly five finding labels changed and nothing else — no violation suppressed, no wildcard (the map has no glob support), no baseline edit.** Domain map now: 0 `UNRESOLVED`, 247/247 mapped, 0 open F0-003 collisions, 37 domains. **Explicitly NOT resolved:** the handler's direct cross-domain Prisma reads of `Appointment`/`Patient`/`TreatmentCase`/`Payment` remain open debt — this decision *increases* their visibility (under `reporting-analytics` they would have been domain-normal) and they are invisible to the guardrail either way (`edgeExtraction.ts` is deliberately import-only, not Prisma-aware). Pre-existing route-level test gaps recorded, not closed. **Tests:** `typecheck`, `typecheck:guardrail`, `git diff --check` clean; `test:orgdash` 35/35; `guardrail:test` 74/74; `test:runtime:unit` 74/74; `test:finance` 25/25; `test:dashboard` 38/38; `test:roles` 142/142. `server:test:non-disposable` deliberately not run — its "changes route or service code" trigger is not met. **Deployment classification: docs/config-only, no application deployment required.** Blocking enforcement remains unauthorized and was not enabled. **No ADR registry number claimed** — `ARCHITECTURE_DECISIONS.md` is intentionally unmodified; there is no `docs/program/adr/` directory, so the ADR follows the repository's real convention under `docs/architecture/f2/`. Machine-readable companions: four checked-in scan reports, [`tooling/F2-ADR-ORG-DASH-002_before_scan_run1.json`](tooling/F2-ADR-ORG-DASH-002_before_scan_run1.json) / [`run2`](tooling/F2-ADR-ORG-DASH-002_before_scan_run2.json) / [`after_scan_run1`](tooling/F2-ADR-ORG-DASH-002_after_scan_run1.json) / [`run2`](tooling/F2-ADR-ORG-DASH-002_after_scan_run2.json). Exact next task: `F2-ORG-DASH-METRICS-CONTRACT` — execute Option D. |
| [F2-GUARDRAIL-VAL-004_FRESH_STRATIFIED_SIGNAL_QUALITY.md](F2-GUARDRAIL-VAL-004_FRESH_STRATIFIED_SIGNAL_QUALITY.md) | F2-GUARDRAIL-VAL-004 (phase F2 — Modular Monolith Boundary Remediation / Guardrail Signal Quality; read-only validation/evidence and reproducible sampling tooling only, no scanner/domain-map/baseline/application code change; isolated worktree/branch `docs/f2-guardrail-val-004-fresh-signal-quality`, baseline `origin/main` @ `81eab4cfb45e115f61d3a151c987d1de97a10cdc`; `AGENT_COMPLETED`, not merged) | **Merge-status correction (supersedes the stale labels above without rewriting them):** the two rows immediately above (`F2-GUARDRAIL-VAL-003`, `F2-ADR-ORG-DASH-002`) and the `F2-GUARDRAIL-VAL-002` row above that still read "not merged" in their own status-tag text, written at the time each was authored. Independently re-verified by this task via `gh pr view 330`/`gh pr view 331` (not assumed): PR #330 is `MERGED` (merge commit `81eab4cfb45e115f61d3a151c987d1de97a10cdc`, `mergedAt` 2026-08-07T09:01:11Z) and PR #331 is `MERGED` (merge commit `9196ceb9a8beff561af55ff2d15b13c0a7874bc2`); both merge commits confirmed ancestors of current `origin/main` via `git merge-base --is-ancestor`; post-merge closure independently re-verified via `gh run view 31165704904` (`workflow_dispatch`, head exactly `81eab4cfb45e115f61d3a151c987d1de97a10cdc`, `conclusion: success`, 10/10 jobs). Performs the fresh stratified false-positive re-sample that `F2-GUARDRAIL-VAL-002` §15/`F2-GUARDRAIL-VAL-003` both named as the still-open next task, against the fully-reconciled post-VAL-003/post-ADR-DASH-002 baseline (fresh deterministic scan, 3× byte-identical: `1,039` total / `846` `NEW` / `193` `EXISTING`, matching VAL-003's own final counts exactly — no drift). Sampling/classification unit is the distinct baseline-matcher edge `(callerPath, ownerDomain, targetModelOrSymbol, accessKind)`, not the raw scanner finding (per the `F2-GUARDRAIL-VAL-004-REVIEW-A` methodology addendum) — `846` `NEW` findings collapse to `552` distinct edges; target sample `n = max(150, ceil(0.20×552)) = 150`; `192` edges actually reviewed after mandatory census (organizationDashboard.ts callers, route→route, cluster≥5, small high-risk/small-cell domains) and high-risk oversampling floors. `organizationDashboard.ts` re-confirmed `core-org-clinic-membership`; `4` distinct `NEW` edges as caller, `0` `EXISTING`, all `4` in-sample by census; source unchanged since F2-ADR-ORG-DASH-002 — ADR not reopened. Does not author baseline entries, does not change `domain-map.json` or scanner matching semantics, does not decide or authorize CI-blocking enforcement — `NOT_AUTHORIZED`, unchanged. **PR #332 opened for this row's task; superseded in place by the R1 row directly below (§0.1 of the same evidence file), not by a rewrite.** |
| [F2-ORG-DASH-METRICS-CONTRACT_PUBLIC_READ_CONTRACTS.md](F2-ORG-DASH-METRICS-CONTRACT_PUBLIC_READ_CONTRACTS.md) | F2-ORG-DASH-METRICS-CONTRACT (phase F2 — Modular Boundaries and Public Contracts; new domain-owned service modules + route composition change, no microservice extraction/schema change/API redesign; isolated worktree/branch `refactor/f2-org-dashboard-metrics-public-contracts`, baseline `origin/main` @ `06f832c229b9f5398c7f52def16e0d37b18c6417`; `AGENT_COMPLETED` / `TESTS_PASSED`, `PR_OPENED` once opened, not merged) | Executes `ADR-F2-ORG-DASH-OWNERSHIP` §9 follow-up 1 (Option D): introduces four purpose-built public read contracts, one per owning domain, replacing `organizationDashboard.ts`'s 11 direct foreign-domain Prisma operations (Appointment ×5, Patient ×2, TreatmentCase ×2, Payment ×2 — exact count from a full base-SHA re-read, not assumed) with composed DTO calls — `getOrganizationAppointmentMetrics`/`getOrganizationPatientMetrics`/`getOrganizationTreatmentCaseMetrics`/`getOrganizationPaymentMetrics`, owned respectively by `clinical-appointments-availability`/`clinical-patients`/`clinical-treatment-cases`/`clinical-basic-payments`. Every query predicate and formula is copied unchanged from the pre-refactor handler; each contract takes an already-authorized `clinicId` and performs no authorization of its own. `organizationDashboard.ts`'s own ownership (`core-org-clinic-membership`), authorization gate, and tenant-scope derivation are unchanged; zero response field renamed/removed/added. Guardrail before/after: `totalFindings` 1,039 → 1,047 (+8, all explainable — 4 route→contract + 4 contract→`db.ts` edges), 0 removed, no baseline/allowlist edit. Source-level proof (scanner cannot see this): `organizationDashboard.ts` contains zero `prisma.appointment./patient./treatmentCase./payment.` occurrences post-refactor, asserted as an executable test. Tests: `typecheck` clean; `test:orgdash` 35/35; `test:finance` 25/25; `test:dashboard` 38/38; `test:roles` 142/142; `guardrail:test` 74/74; `git diff --check` clean; new `organizationDashboardMetricsContracts.test.ts` run against real disposable PostgreSQL (72/72 migrations applied), 13/13 passed, covering DTO shape, tenant scoping, date-range filtering, OWNER/ORG_ADMIN/multi-clinic scopes, cross-org non-leakage, a zero-data organization, and aggregation equivalence. No schema/migration change; no new patient-level PII exposed. Exact next task: none named by this task. |
| [F2-GUARDRAIL-VAL-004_FRESH_STRATIFIED_SIGNAL_QUALITY.md §0.1](F2-GUARDRAIL-VAL-004_FRESH_STRATIFIED_SIGNAL_QUALITY.md) | F2-GUARDRAIL-VAL-004-R1 (phase F2 — Modular Monolith Boundary Remediation / Guardrail Signal Quality; docs/evidence/tooling-scripts-only correction round on the same PR; existing worktree/branch `docs/f2-guardrail-val-004-fresh-signal-quality`, PR [#332](https://github.com/MustafaBasol/DisKlinikCRM/pull/332), reviewed/starting head `e8e57bd8bb6ce7e3dd950a808cf4f1d31c1a8628`; `AGENT_COMPLETED`, not merged) | **Same evidence document as the row above, updated in place (§0.1) — not a new file, per this task's own note that its evidence is live/unmerged, unlike the chronological tracker logs.** Resolves 2 blocking findings from an external architecture review of PR #332: (1) the high-risk census/oversample partition in `buildVal004Sample.mjs` was keyed only by `ownerDomain` despite `isHighRisk(e)` correctly considering either endpoint — fixed with a new deterministic `highRiskDomainKey(e)`, plus a fix-adjacent double-counting defect the rework surfaced, both closed in the same pass, corrected partition verified to sum to exactly N=552 with 0 unassigned/duplicated edges; (2) the 21.10% figure was mislabeled a formal `conservative violation-rate ceiling` with no simultaneous confidence-coverage guarantee — relabeled (computation unchanged) `zeroEventSensitivityAnalysis()` in `buildVal004Metrics.mjs`, with explicit `hasConfidenceCoverage: false`. Reviewed sample grew `192 → 266` edges (159 reused unchanged, 33 dropped — 2 of the prior 4 true positives among them, still valid against the population, simply outside this round's sample — 107 newly classified against current source, 0 new true positives/ambiguous/scanner-defects); weighted accepted-rate `99.27% → 99.64%`; sensitivity value `21.10% → 28.02%` (broader zero-event coverage, not more uncertainty). Regenerated deterministically (2×+ reruns, byte-identical); 6 new regression tests (`val004Determinism.test.js`, now 50/50). `BLOCKING_ENFORCEMENT = NOT_AUTHORIZED`, unchanged. **Not merged — external re-review required before merge.** |
| — (no new evidence file; see `CURRENT_PHASE.md`'s top `F2-DOC-002` entry) | F2-DOC-002 (phase F2 — Modular Monolith Boundary Remediation / Program-Control Reconciliation; documentation/program-control only, no runtime/schema/workflow/package file touched; fresh isolated worktree/branch `docs/f2-doc-002-post-merge-stage2-gate-reconciliation` off freshly-fetched `origin/main` @ `06f832c229b9f5398c7f52def16e0d37b18c6417`; `AGENT_COMPLETED`, not merged) | **Merge-status correction (supersedes the "not merged" status tags on the two rows immediately above without rewriting them):** PR #332 (`F2-GUARDRAIL-VAL-004`/`VAL-004-R1`) independently re-verified `MERGED` via `gh pr view 332` (merge commit `4680e1d1ae50a1bce5f1a9a99425f0f2de42de23`, `mergedAt` 2026-08-07T17:48:27Z, confirmed ancestor of `origin/main`); PR #333 (`F2-CT-23`, no dedicated evidence file of its own — code fix + regression tests only, see `server/src/routes/imaging.ts` and `server/src/tests/dbVerification/{imagingStudyRequestPatientConsistency,detectImagingStudyRequestPatientMismatch}.test.ts`) independently re-verified `MERGED` via `gh pr view 333` (merge commit `06f832c229b9f5398c7f52def16e0d37b18c6417`, also `origin/main`'s exact current tip, `mergedAt` 2026-08-07T17:49:04Z, confirmed ancestor of `origin/main`). Post-merge main CI independently re-verified via `gh run view 31204059995`: `status: completed`, `conclusion: success`, `headSha` exact match, 10/10 `ci-layers` jobs `success`. VAL-004 metrics unchanged from the R1 row above (266-edge sample, 99.64% weighted accepted-rate, 28.02% sensitivity, 56/56 determinism tests) — not restated as a new evidence conclusion. `CT-23` (`VERIFIED_DEFECT`, F2-PREP-007-B) remediated: guarded atomic `updateMany` patient/clinic-consistency invariant; bounded cursor+`take` historical-mismatch detector, default batch 500; 12/12 + 7/7 regression; no schema/migration; production detector not run — `CT-23`/PR #333 changed runtime backend code, so it is `NOT_DEPLOYED`/`NOT_PRODUCTION_VERIFIED`, not `N/A` (corrected by F2-DOC-002-R1, same PR, before merge). **Stage 2 Imaging entry gate (corrected by F2-DOC-002-R1): `STAGE_2_ENTRY_GATE = SATISFIED`/`AUTHORIZED_TO_BEGIN_STAGE_2_IMPLEMENTATION = TRUE`**, scoped strictly to `OVL-01` ingest convergence + `ImagingRequest` concurrency hardening (`CR-03`/`BLK-02`/`FP-06`/`CT-32`, per `F2-PREP-006-E`'s §15 Stage-2 row — that concurrency hardening is Stage 2's own exit condition, not yet implemented on `main`, worked separately by `F2-CT-32-R1`, not an entry precondition) — Stage 3 and CI-blocking enforcement (`NOT_AUTHORIZED`, unchanged) remain not authorized; **Stage 2 itself remains `IN_PROGRESS`/`NOT_COMPLETE`/`EXIT_GATE_NOT_SATISFIED`.** **Not merged — this reconciliation PR itself, per instruction not to merge.** |
| [F2-OVL-01_IMAGING_INGEST_CONVERGENCE_EVIDENCE.md](F2-OVL-01_IMAGING_INGEST_CONVERGENCE_EVIDENCE.md) | F2-OVL-01 (phase F2 — Modular Boundaries and Public Contracts, Imaging Stage 2; branch `fix/f2-ovl-01-imaging-ingest-convergence`, baseline `origin/main` @ `5a6944ae60b182f1c79c96c9febead7b25c59d9b`; `AGENT_COMPLETED`, not merged — **superseded 2026-08-08 by F2-DOC-004: PR #338 is now `MERGED`, merge commit `babedf82913740c9b73f3ec85a6253200661c52f`, exact post-merge main CI run `31270570690` `success`, see the F2-DOC-004 row below**) | Converges manual (`POST /api/imaging/studies`) and bridge (`POST /api/public/imaging/bridge/studies`) `ImagingStudy` ingest behind one new internal function, `ingestImagingStudyCore()` (`server/src/services/imaging/imagingIngestCore.ts`), per `F2-PREP-006-E` §8/§15. Narrow authorized core boundary (file-signature validation, storage write, the `ImagingStudy`+`ImagingImage` transaction with the pre-existing tenant-scoped `ImagingRequest` CAS `updateMany`, best-effort storage compensation) with a mandatory explicit `clinicId`; auth/clinic-resolution/`validateClinicalLinks`/request-lookup/bridge-dedupe/rate-limiting/audit/ActivityLog all explicitly kept out and unchanged in each route (audit-outside-the-core is a recorded architecture decision, not an oversight; two known audit/ActivityLog asymmetries remain open, tracked as `F2-IMG-AUDIT-001`/`F2-IMG-AUDIT-002`). No schema/migration change. Full test/validation evidence and delivery report in the linked file. |
| [F2-DOC-004_STAGE_2_EXIT_GATE_RECONCILIATION.md](F2-DOC-004_STAGE_2_EXIT_GATE_RECONCILIATION.md) | F2-DOC-004 (phase F2 — Modular Boundaries and Public Contracts / Program-Control Reconciliation; documentation-only, no runtime/test/schema/workflow/package file touched; `AGENT_COMPLETED`, not merged) | Independently re-verifies PR #338 (`F2-OVL-01`) `MERGED` with exact post-merge main CI `success` (run `31270570690`, 10/10 jobs), PR #335 (`F2-CT-32-R1`) likewise `MERGED`/`MAIN_CI_PASSED`, and PR #337 (`F2-DOC-003`, an earlier reconciliation attempt) `CLOSED`/`NOT_MERGED` — superseded by PR #338's own merge. Both named halves of Stage 2's accepted scope (`F2-PREP-006-E` §15) are complete: `STAGE_2_EXIT_GATE = SATISFIED`. `F2-IMG-AUDIT-001`/`F2-IMG-AUDIT-002` reconciled as accepted deferred debt, not exit blockers. Does **not** authorize Stage 3 (`AUTHORIZED_TO_BEGIN_STAGE_3 = FALSE`) or CI-blocking guardrail enforcement (unchanged `NOT_AUTHORIZED`). Full PR-by-PR verification, evidence matrix, and validation commands in the linked file. |

| [F2-STAGE3-AUTH-001_IMAGING_PRIVACY_CALLER_MIGRATION_AUTHORIZATION.md](F2-STAGE3-AUTH-001_IMAGING_PRIVACY_CALLER_MIGRATION_AUTHORIZATION.md) | F2-STAGE3-AUTH-001 (phase F2 — Modular Boundaries and Public Contracts, Imaging Stage 3; read-only architecture/program-control authorization review — Stage 3 not implemented by this task; baseline `origin/main` @ `5eedf8ff5e8285a4c5b623119ab173ffa8d8ed23`, unchanged from F2-DOC-004) | Independently evaluates whether Stage 3 (Privacy/KVKK caller migration onto `ImagingLifecyclePort`) may begin. Port contract re-verified drift-free on all 10 tenant-predicate sub-checks, zero production callers re-confirmed. All three privacy caller files' live `clinicId` provenance traced to `patientPrivacy.ts`'s `resolvePatient()` — VALIDATED. `CT-05`/`CT-23`/`CT-30` independently re-run against a disposable PostgreSQL instance (5/5, 2/2+12/12+7/7, 7/7), all CI-wired, zero weakened assertions. Two small scope gaps found and explicitly deferred (not blockers): `orphanFileInspection.ts`'s `markConfirmedMissing` (no `clinicId` param, zero production callers) and `deletionReviewInventory.ts`'s imaging read (needs a port DTO extension, not authorized here). `STAGE_3_ENTRY_GATE = SATISFIED` / `AUTHORIZED_TO_BEGIN_STAGE_3_IMPLEMENTATION = TRUE`. Exact next task: `F2-STAGE3-IMPL-001`, scoped to the two production-reachable, cleanly-mapping call sites only. No `server/src`, schema/migration, workflow, or guardrail scanner/config/baseline file touched; `AGENT_COMPLETED`, `PR_OPENED` once opened, `NOT_MERGED`/`NOT_DEPLOYED`/`NOT_PRODUCTION_VERIFIED`. |
| [F2-STAGE3-IMPL-001_PRIVACY_IMAGING_LIFECYCLE_CALLER_MIGRATION.md](F2-STAGE3-IMPL-001_PRIVACY_IMAGING_LIFECYCLE_CALLER_MIGRATION.md) | F2-STAGE3-IMPL-001 (phase F2 — Modular Boundaries and Public Contracts, Imaging Stage 3 implementation, initial slice; ClickUp `869efu8br`; branch `feature/f2-stage3-impl-001-privacy-imaging-lifecycle-migration`, baseline `origin/main` @ `27c54f3aa81af4eecc33e79d77e366a0fe4915bc`) | Migrates `patientAnonymization.ts`'s imaging redaction and `orphanFileInspection.ts`'s `inspectOrphans` lifecycle-review/existence-check path onto `ImagingLifecyclePort` (`getImagesForLifecycleReview`/`redactForAnonymization`/`checkImageStorageExists`), replacing direct `prisma.imagingImage`/`prisma.imagingStudy` access. Caller-side legal-hold adapter preserves pre-migration skip/count semantics and additionally closes a TOCTOU race the old code never guarded; caller-side `BATCH_SIZE` truncation reproduces the old cap the port itself doesn't enforce. `markConfirmedMissing`/`deletionReviewInventory.ts` remain explicitly deferred, doc-commented, and regression-tested as untouched. A counter-semantics divergence on idempotent re-runs (port DTO has no `originalName`), initially regression-tested here, was **fixed** (not accepted or deferred) same-day by [F2-STAGE3-IMPL-001-R1](F2-STAGE3-IMPL-001-R1_IMAGING_LIFECYCLE_REDACTION_MUTATION_OUTCOME_AMENDMENT.md) (`redactForAnonymization` now returns `Promise<{ changed: boolean }>`, additive/DTO-unchanged, still exactly six fields). `test:privacy-imaging-lifecycle-migration` **18/18** (17/17 + 1 R1 regression); disposable-PostgreSQL `CT-05`/`CT-30` **29/29**, `CT-23` **12/12+7/7**, `test:imaging-lifecycle-facade` **36/36** (34/34 + 2 R1 regressions), `guardrail:test` **74/74**, `guardrail:scan` exit 0, `typecheck`/`git diff --check` clean; full `test:runtime:postgres` orchestrator run (PostgreSQL 16, 72/72 migrations existing, no new migration) exit 0. No schema/migration/route/response-shape change. Stage 3 as a whole not claimed complete — initial slice only. Current head `39364f117874668c2bde34fe1fbca03de7430245`. `AGENT_COMPLETED`/`TESTS_PASSED`/`PR_OPENED` ([PR #344](https://github.com/MustafaBasol/DisKlinikCRM/pull/344))/`PR_CI_PASSED` (exact-head CI: `ci` run `31310907422` `SUCCESS`, `windows-bridge-pr` run `31310907297` `SUCCESS`)/`NOT_MERGED`/`NOT_DEPLOYED`/`NOT_PRODUCTION_VERIFIED`. |
| [F2-STAGE3-IMPL-001-R1_IMAGING_LIFECYCLE_REDACTION_MUTATION_OUTCOME_AMENDMENT.md](F2-STAGE3-IMPL-001-R1_IMAGING_LIFECYCLE_REDACTION_MUTATION_OUTCOME_AMENDMENT.md) | F2-STAGE3-IMPL-001-R1 (phase F2 — Modular Boundaries and Public Contracts, Imaging Stage 3; correction pass on F2-STAGE3-IMPL-001, continues PR #344, not a new PR) | Fixes the counter-semantics divergence F2-STAGE3-IMPL-001 accepted: `ImagingLifecyclePort.redactForAnonymization` now returns `Promise<{ changed: boolean }>` instead of `Promise<void>` (Option B — mutation-outcome result; `ImagingLifecycleImageDto` deliberately NOT extended with `originalName`/`isRedacted`, no schema/migration). `changed: true` only when THIS call's own atomic `originalName` compare-and-set actually flipped the row; `changed: false` for an already-redacted row, whether via the top-of-function idempotent short-circuit or because a concurrent writer's own call won the race and flipped it first (that winning call, not this one, reports `changed: true`). `patientAnonymization.ts`'s `redactPatientImagingImages` now increments `redacted` only when `outcome.changed === true`, restoring exact pre-migration `RedactionCounters` semantics on a re-run (new image added after a prior anonymization run is correctly counted; an already-redacted image is not re-counted). Legal-hold/tenant/TOCTOU semantics and every DB predicate unchanged from F2-IMPL-001-A-R3. New mutation-outcome tests in `imagingLifecycleFacade.test.ts` §8c; `privacyImagingLifecyclePortMigration.test.ts`'s idempotent-rerun assertion corrected to `redacted === 0`, plus a new "image added after anonymization" regression test. `AGENT_COMPLETED`/`NOT_MERGED`/`NOT_DEPLOYED`/`NOT_PRODUCTION_VERIFIED`. |
| [F2-STAGE3-IMPL-001-POSTMERGE-CI-001_SECURITY_INCIDENT_MAIN_CI_REMEDIATION.md](F2-STAGE3-IMPL-001-POSTMERGE-CI-001_SECURITY_INCIDENT_MAIN_CI_REMEDIATION.md) | F2-STAGE3-IMPL-001-POSTMERGE-CI-001 (phase F2 — Modular Boundaries and Public Contracts; post-merge main CI failure investigation/remediation, unrelated in domain to F2-STAGE3-IMPL-001's own imaging scope; dedicated worktree `fix/f2-stage3-postmerge-securityincident-ci`, baseline `origin/main` @ `6a248ea1c359b2e86f39f9fff0b1d8577357fc10`, the PR #344 merge commit) | Investigates and fixes a real post-merge `main` CI failure (`ci-main-and-nightly` run `31313169502`, Layer 5 backend, `securityIncident.test.ts` test 22, `assert.ok(singleResourceIncident)` on `null`). Causality check proves zero dependency-graph overlap with PR #344 — not a regression it caused. Root cause: Category C, a pre-existing (PR #167/#178) test-timing race — 8 of 9 fixed `sleep(150\|200)` calls in the test file raced a guessed delay against genuinely async, intentionally non-blocking (`void`-returning) detection-rule DB work, no durable-completion barrier; reproduced 0/7 naturally, reproduced the exact CI assertion 4/4 times under a test-only adversarial-delay hook. **Fix:** `securityDetectionRules.ts`'s shared `safely()` wrapper now tracks its own in-flight promise; new export `flushPendingSecurityDetectionWork()` replaces the 8 racy `sleep()` calls with a real completion barrier — no production caller signature/timing/behavior changed (non-blocking contract regression-proven unaffected), no assertion weakened, no schema/migration. `test:security-incidents` **55/55**, repeated 6× post-fix (3× normal, 3× under the same adversarial delay that broke the pre-fix code 4/4 times) — 0 failures. An unrelated, pre-existing Windows-`core.autocrlf`-CRLF-checkout artifact in `test:clinic-bulk-export` was observed blocking the full `server:test:legacy-db-required` chain locally — out of scope, not caused by this task, not expected on Linux CI runners. `AGENT_COMPLETED`/`TESTS_PASSED`/`PR_OPENED`/`NOT_MERGED`/`NOT_DEPLOYED`/`NOT_PRODUCTION_VERIFIED`. |

| [F2-CT-32-R2_IMAGING_REQUEST_RESIDUAL_RACE_REMEDIATION.md](F2-CT-32-R2_IMAGING_REQUEST_RESIDUAL_RACE_REMEDIATION.md) | F2-CT-32-R2 (phase F2 — Modular Boundaries and Public Contracts, Imaging Stage 2 follow-up; BLOCKER/URGENT, opened by a real post-merge main CI failure; branch `fix/f2-ct-32-r2-imaging-request-residual-race`, baseline `origin/main` @ `51e102d19a5ddda87a49ad0367172b3eaae05d09`; `AGENT_COMPLETED`, not merged) | Closes a residual gap in the merged F2-CT-32-R1 CAS guard, surfaced by `ci-main-and-nightly` run `31276844302` (Layer 3, CT-32 148/6 failed, round-1 `PATCH=200,cancel=200,final=cancelled`). Deterministically reproduced (not accepted as flake) via a new test-only forced-interleaving hook; root cause is a *chainable*-pair ambiguity (`scheduled` is a valid predecessor of `cancelled`) already flagged by `imagingRequestConcurrencyGuard.test.ts`'s own scenario-A header comment, and matches this codebase's own accepted precedent for the identical problem shape (`patientEmergencyContactsConcurrency.ts`, F1-004-P1-R2-R3): once a competing transaction has already committed before this transaction's own first read executes, no signal inside the second transaction alone can detect it. **Fix:** additive, optional `expectedStatus` precondition field on both PATCH and cancel — mirrors that file's own `expectedCurrentPrimaryContactId` token-protected mode. No schema/migration. CT-32 revised in place (504/504 at 100 rounds); new `imagingRequestConcurrencyForcedInterleaving.test.ts` (55/55) proves the fix closes the exact forced schedule that broke legacy mode while characterizing the residual gap for opt-out callers; `imagingRequestConcurrencyGuard.test.ts` re-verified 73/73, no regression. Full disposable-Postgres orchestrator run (`test:runtime:postgres`, matching CI Layer 3 exactly) green. `F2-STAGE3-AUTH-001`'s authorization is **not** revoked; `F2-STAGE3-IMPL-001` remains explicitly **BLOCKED** until this PR merges and post-merge main CI is green. |

| [F2-IMG-AUDIT-002_MACHINE_ACTOR_ACTIVITYLOG_DECISION.md](F2-IMG-AUDIT-002_MACHINE_ACTOR_ACTIVITYLOG_DECISION.md) | F2-IMG-AUDIT-002-DECIDE / R1 (phase F2 — Modular Boundaries and Public Contracts; ClickUp `869efu8ce`; read-only architecture decision, PR #343; R1 (2026-08-09) reconciles the PR against `origin/main` @ `6a248ea1c359b2e86f39f9fff0b1d8577357fc10` — the exact PR #344 merge commit — via a normal `git merge`, no rebase, zero conflicts, zero source files touched, and corrects wording overreach found on review; `AGENT_COMPLETED`, not merged) | Resolves the bridge machine-actor `ActivityLog` question F2-OVL-01 deferred: **Accepted Option A — bridge ingest activity stays in `AuditLog` only, no `ActivityLog` write.** `ActivityLog.userId` is a mandatory, non-nullable `User` FK (human-staff activity-feed semantics, independently re-verified against source); the bridge is a machine actor with no session to attribute a write to. The existing WhatsApp/Instagram "borrowed real staff `User`" pattern is evaluated and explicitly **not** adopted as precedent — flagged as existing technical debt, not a template. `AuditLog` remains the bridge's established audit sink (R1 narrows the prior wording: authoritative *for the bridge flow and the one other codebase flow that already treats it as such*, not a codebase-wide invariant). **KVKK per-patient export completeness for bridge-linked imaging is explicitly identified as a separate, still-open gap** (`activityHistory` in `patientPrivacy.ts`'s export reads only `ActivityLog`, never `AuditLog`) — `NO_ACTION_REQUIRED` here does not mean KVKK completeness is resolved; tracked as proposed follow-up `F2-IMG-AUDIT-003`, not implemented. No schema/migration/runtime change in either the original pass or the R1 correction. `F2-IMG-AUDIT-002-DECIDE = NO_ACTION_REQUIRED`, `NOT_MERGED`/`NOT_DEPLOYED`/`NOT_PRODUCTION_VERIFIED`. |

| [F2-STAGE3-DEFERRED-GAPA-001_MARK_CONFIRMED_MISSING_TENANT_SCOPE_MIGRATION.md](F2-STAGE3-DEFERRED-GAPA-001_MARK_CONFIRMED_MISSING_TENANT_SCOPE_MIGRATION.md) | F2-STAGE3-DEFERRED-GAPA-001 (phase F2, Imaging Stage 3 gap-closure) | Closes the `markConfirmedMissing` tenant-scope gap `F2-STAGE3-IMPL-001` explicitly deferred (no `clinicId` parameter). Migrated onto the `ImagingLifecyclePort`. `MERGED` via [PR #347](https://github.com/MustafaBasol/DisKlinikCRM/pull/347) (merge commit `a10ac98cf3b209b5e14cddf9ab1c997dbc44cf15`) — independently re-confirmed by [F2-DOC-005](F2-DOC-005_STAGE3_AND_F2_EXIT_GATE_RECONCILIATION.md); this document's own body text still reads `NOT_MERGED` (capture-time, not rewritten in place — see F2-DOC-005 §3.1). |
| [F2-STAGE3-DEFERRED-GAPB-001_IMAGING_FILESIZE_DTO_DELETION_REVIEW_MIGRATION.md](F2-STAGE3-DEFERRED-GAPB-001_IMAGING_FILESIZE_DTO_DELETION_REVIEW_MIGRATION.md) | F2-STAGE3-DEFERRED-GAPB-001 (phase F2, Imaging Stage 3 gap-closure) | Closes the `deletionReviewInventory.ts` imaging `fileSize` direct-Prisma-read gap `F2-STAGE3-IMPL-001` explicitly deferred, via a port DTO extension. `MERGED` via [PR #348](https://github.com/MustafaBasol/DisKlinikCRM/pull/348) (merge commit `d452cd2ec4fa16f53de1783fc882a57385a9ffa6`) — independently re-confirmed by [F2-DOC-005](F2-DOC-005_STAGE3_AND_F2_EXIT_GATE_RECONCILIATION.md); this document's own body text still reads `NOT_MERGED` (capture-time, not rewritten in place). |
| [F2-IMG-AUDIT-003_KVKK_IMAGING_BRIDGE_EXPORT_COMPLETENESS.md](F2-IMG-AUDIT-003_KVKK_IMAGING_BRIDGE_EXPORT_COMPLETENESS.md) | F2-IMG-AUDIT-003 (+R1) (phase F2, KVKK per-patient export completeness for bridge-linked imaging — the follow-up `F2-IMG-AUDIT-002-DECIDE-R1` proposed, not implemented, at the time) | Closes the bridge-linked-imaging KVKK export-completeness gap `F2-IMG-AUDIT-002-DECIDE-R1` identified as separate and still-open. `MERGED` via [PR #349](https://github.com/MustafaBasol/DisKlinikCRM/pull/349) (commits `e42f543`/`4d5a81a`) — independently confirmed by [F2-DOC-005](F2-DOC-005_STAGE3_AND_F2_EXIT_GATE_RECONCILIATION.md) §5 item 20; not independently re-verified in depth by that task, since it falls outside Stage-3 Imaging's own zero-direct-access scope. |
| [F2-DOC-005_STAGE3_AND_F2_EXIT_GATE_RECONCILIATION.md](F2-DOC-005_STAGE3_AND_F2_EXIT_GATE_RECONCILIATION.md) | F2-DOC-005 (phase F2 — Modular Boundaries and Public Contracts / Program-Control Reconciliation; documentation-only, dedicated worktree, baseline `origin/main` @ `1fffaddc0054c1b3eb5d9a4455a52d3893e17d86`, no drift) | Formal Stage-3 exit reconciliation plus whole-F2-phase exit review. Independently re-verifies GAPA/GAPB/GAPC/GAPD ([PR #347](https://github.com/MustafaBasol/DisKlinikCRM/pull/347)/[#348](https://github.com/MustafaBasol/DisKlinikCRM/pull/348)/[#352](https://github.com/MustafaBasol/DisKlinikCRM/pull/352)/[#353](https://github.com/MustafaBasol/DisKlinikCRM/pull/353)) and F2-CT-32-R3 ([PR #351](https://github.com/MustafaBasol/DisKlinikCRM/pull/351) — a `patients`-domain fix, **not** Imaging Stage 3, despite the reused `CT-32` numbering) all `MERGED`, and finds `F2-STAGE3-IMPL-001`/`-R1`/`-POSTMERGE-CI-001`'s own evidence docs still stale at `NOT_MERGED` despite their PRs (#344/#345) being merged. Finds that `F2-STAGE3-EXIT-SWEEP-001`/`-EXIT-PRE-001`/`-EXIT-DECIDE-001`, all claimed complete by the assigning prompt, have **zero repository evidence trace** as formally executed tasks — and independently performs its own read-only zero-direct-Imaging-access sweep in their place: **zero forbidden cross-domain Imaging Prisma access found** on current `main`. **Stage 3: complete.** Whole-F2 20-item requirement matrix: no blocking gap remains (entitlement/release-flag/queue/worker/job items confirmed out of F2's actually-executed scope, per the standing `F2-DOC-004` precedent, not reopened). **`F2_IMPLEMENTATION_EXIT_GATE = SATISFIED`; `DEPLOYMENT = NOT_DONE`; `PRODUCTION_VERIFICATION = NOT_DONE`.** Does not authorize deployment or F3. `AGENT_COMPLETED`/`DOC_VALIDATION_PASSED` — `PR_OPENED` once opened — `NOT_MERGED`/`NOT_DEPLOYED`/`NOT_PRODUCTION_VERIFIED`. |
| [F3-IMPL-001_FIRST_CUSTOMER_PRODUCTION_HARDENING.md](F3-IMPL-001_FIRST_CUSTOMER_PRODUCTION_HARDENING.md) | F3-IMPL-001 (phase F3 — Production Hardening; first F3 implementation task; branch `feature/f3-impl-001-first-customer-production-hardening`, dedicated worktree, baseline `origin/main` @ `9cb256856b628a5ed5cc6ff85f84ec01a4a12cf0`, the F2-DOC-005/PR #354 merge commit) | Bounded pre-work inventory (items A–O: request/correlation id, structured logging, error tracking, secret fail-hard, API/worker separation, `RUN_BACKGROUND_JOBS`, worker lifecycle, PII/PHI log redaction, platform-admin audit, security headers, rate limiting, startup validation, health endpoints, Node/runtime contract, smoke support) with file:line evidence, then 5 coupled P0 gaps closed: `DATABASE_URL` production-only fail-hard (`utils/databaseUrl.ts` — an unconditional version was tried first and confirmed, by actually running `server:test:non-disposable`, to crash ~68 unrelated tests that import `db.ts` only transitively; scoped to production, mirroring the existing `ENCRYPTION_KEY` check); explicit, unit-tested API/worker background-jobs-ownership decision (`utils/backgroundJobsOwnership.ts`) plus a startup diagnostic log in both processes — **the default is deliberately unchanged**, since `scripts/noramedi-deploy.sh` manages only a `noramedi-api` PM2 process (no dedicated worker process referenced anywhere in deploy tooling), so flipping it would silently stop production background jobs; `X-Request-Id` response header (`middleware/requestId.ts`) echoing pino-http's per-request id back to the client for log correlation; admin email removed from 7 `console.log` sites in `routes/platformAdmin.ts` (R-018); durable `PlatformAdminAuditEvent` audit trail added to `POST /backups/run`/`POST /backups/restore-test`, previously fully unaudited (R-019), via a new standalone `writePlatformAdminAuditEvent` helper. New tests: `databaseUrlValidation.test.ts` (11/11), `backgroundJobsOwnership.test.ts` (7/7), `requestIdCorrelation.test.ts` (2/2) — all wired into `server:test:non-disposable`; `platformBackupAudit.test.ts` (3/3, real disposable Postgres, proves admin attribution + zero PII in the audit row) — wired into `server:test:disposable-db`. Full `server:test:non-disposable` (68 members) and `test:runtime:postgres` orchestrator (`server:test:disposable-db`, 22 members) both re-run in full after the fix, zero regressions. No schema/migration change. R-018/R-019 **reduced, not closed** — most platform-admin mutation endpoints remain unaudited, app-level `console.log` PII discipline is not repo-wide; both explicitly out of this task's bounded scope. `AGENT_COMPLETED`/`TESTS_PASSED`/`PR_OPENED` ([PR #355](https://github.com/MustafaBasol/DisKlinikCRM/pull/355)) — `NOT_MERGED`/`NOT_DEPLOYED`/`NOT_PRODUCTION_VERIFIED`. **R1 update (§25 of this evidence file):** PR #355 exact-head CI (run `31394080617`) found `platformAdmin.test.ts` still asserting the pre-R0 email-in-log contract R0 itself had just replaced — a test-only fix (admin-id + negative email assertion), zero production-code change, R0's security decision unchanged. `platformAdmin.test.ts` now `60/60`; full `postgres-compat` orchestrator exit `0`. `TESTS_PASSED`/`PR_UPDATED`/`PR_CI_PENDING`/`NOT_MERGED`/`NOT_DEPLOYED`/`NOT_PRODUCTION_VERIFIED`. |
| [F3-IMPL-002_PRODUCTION_WORKER_PROCESS_CONTRACT.md](F3-IMPL-002_PRODUCTION_WORKER_PROCESS_CONTRACT.md) | F3-IMPL-002 (phase F3 — Production Hardening; second F3 implementation task; branch `feature/f3-impl-002-production-worker-contract`, dedicated worktree, baseline `origin/main` @ `1909b186a01611c8be90313b7166085a887d05f4`, the PR #355/F3-IMPL-001 merge commit) | Closes the worker/job-ownership gap F3-IMPL-001 deliberately left open. Pre-work inventory (12-question matrix, classified `PARTIAL`) found `server/src/worker.ts` already existed and worked correctly (`JobLock`-protected, always owns jobs) — the actual gap was entirely deployment/process-management: no `ecosystem.config.*` in-repo (R-040), `scripts/noramedi-deploy.sh` only ever reloaded `noramedi-api` (R-033), and the API's job-ownership default could not safely be flipped without a repository-defined worker lifecycle (R-034). **Three coupled changes in one PR, as required:** `ecosystem.config.cjs` (new — repository-defined PM2 process contract for both `noramedi-api`/`noramedi-worker`, the api app explicitly setting `RUN_BACKGROUND_JOBS=false` now that the worker's own lifecycle is repository-managed); `scripts/noramedi-deploy.sh` (reloads/starts both processes via `pm2 startOrReload ecosystem.config.cjs --only <name> --update-env`, then verifies the worker reaches PM2 `online` status via a Node-parsed `pm2 jlist` check — no HTTP endpoint added to the worker — aborting the deploy with a non-zero exit if it does not); `server/src/utils/processRole.ts` (new — optional, validated `NORAMEDI_PROCESS_ROLE` env var asserted at startup by both `index.ts`/`worker.ts`, failing closed on an unrecognized or mismatched declared role, fully backward compatible when unset). `resolveWorkerBackgroundJobsOwnership()` added to `backgroundJobsOwnership.ts` alongside the pre-existing, unmodified API-side function so the intended production ownership matrix (worker owns, API does not, no duplicate registration, no zero-owner state) is proven by a test rather than read by eye. New tests: `processRole.test.ts` (8/8), `backgroundJobsOwnership.test.ts` extended (10/10, was 7/7). `typecheck` exit `0`; full `server:test:non-disposable` (69 members) exit `0`; `bash -n scripts/noramedi-deploy.sh` syntax-clean; `test:runtime:postgres` orchestrator result recorded in the evidence file's §9. No schema/migration change; no HTTP surface added to the worker. `RISK_REGISTER.md` R-033/R-040 rows updated to record this repository-level mitigation, R-034 exposure reduced — **none closed**, all remain `OPEN` pending production deployment/verification. `AGENT_COMPLETED`/`TESTS_PASSED`/`PR_OPENED` — `NOT_MERGED`/`NOT_DEPLOYED`/`NOT_PRODUCTION_VERIFIED`. |
| [F3-IMPL-003_PLATFORM_ADMIN_AUDIT_COVERAGE.md](F3-IMPL-003_PLATFORM_ADMIN_AUDIT_COVERAGE.md) | F3-IMPL-003 (phase F3 — Production Hardening; third F3 implementation task, parallel with F3-IMPL-002 on disjoint files; branch `feature/f3-impl-003-platform-admin-audit`, dedicated worktree, baseline `origin/main` @ `1909b186a01611c8be90313b7166085a887d05f4`) | Direct follow-up to F3-IMPL-001's own named next R-019 slice. Full inventory of all 70 authenticated platform-admin routes (`platformAdmin.ts` 46, `platformSecurityIncidents.ts` 13, `platformExternalCalendar.ts` 11): 28 `READ_ONLY`, 5 `OUT_OF_SCOPE`, 19 already `AUDITED_DURABLY` (5 via `PlatformAdminAuditEvent`, 9 security-incident routes via a separate `SecurityIncidentActivity` trail, 5 external-calendar routes via `writeAuditLog()` — both left untouched, already covered by a shared service), 18 `UNAUDITED` at task start (closely matching F3-IMPL-001's own independent "~18 remaining" estimate). Closed the 6 highest-risk (priority classes 1–3 of 5): `POST /auth/mfa/{setup,verify,disable}` (security/auth config — `disable` is the highest-risk, silently weakening the acting admin's own account security), `PUT`/`DELETE /sms-providers` (global provider/credential config), `PATCH /privacy/data-retention/settings` (the most glaring gap — its sibling `legacy-consent-correction` toggle already had this exact `PlatformAdminAuditEvent` pattern). All six write transactionally via `writePlatformAdminAuditEventInTx`, id-attributed, zero PII/secrets/credentials in the row — proven via real-Postgres forced-FK-violation atomicity tests (`sms-providers`/`data-retention`) and a real actor-must-exist-before-transaction-opens 404 proof (the three MFA routes). 12 endpoints (priority classes 4–5: org/clinic/plan/user lifecycle [10], file-backup triggers [2]) intentionally deferred — full list in §4. No schema/migration change. New tests: 22, added to the existing `platformAdmin.test.ts` (now 82/82, was 60/60). Full `test:runtime:postgres-compat` (`server:test:legacy-db-required`, 25 suites) and `test:runtime:postgres` (`server:test:disposable-db`, 25 suites) both exit `0`, zero failures. One environment-only fixture gap found/fixed (fresh worktree had no local `.env`, so `ENCRYPTION_KEY` was unset for the new encryption-touching test paths — fixed with a freshly generated disposable test-only key, not a copy of any real secret). R-019 **reduced, not closed** — 25 of 37 platform-admin mutation endpoints now `AUDITED_DURABLY` (19 pre-existing + 6 here), 12 remain `UNAUDITED`. `AGENT_COMPLETED`/`TESTS_PASSED`/`PR_OPENED` ([PR #358](https://github.com/MustafaBasol/DisKlinikCRM/pull/358)) — `NOT_MERGED`/`NOT_DEPLOYED`/`NOT_PRODUCTION_VERIFIED`. |
| [F3-IMPL-003-R1_POSTMERGE_RECONCILIATION.md](F3-IMPL-003-R1_POSTMERGE_RECONCILIATION.md) | F3-IMPL-003-R1 (phase F3 — Production Hardening; reconciliation-only, same branch/worktree/PR #358 as F3-IMPL-003) | Reconciles PR #358 against `origin/main` now that F3-IMPL-002 has merged (previous head `756ed26`, left the PR non-mergeable). Normal `git merge` (no rebase, no force-push) against `origin/main` @ `d1765a5787b9118584a778a08cb52b276fb438a3`, independently confirmed. 4 conflicts, all in the predicted docs files, all resolved additively — both F3-IMPL-002's and F3-IMPL-003's own content preserved, neither side dropped; zero remaining conflict markers. No runtime file conflicted; `git diff origin/main --stat` post-merge shows only F3-IMPL-003's own original scope. `typecheck` exit `0`; focused `platformAdmin.test.ts` 82/82; `test:runtime:postgres-compat`/`test:runtime:postgres` orchestrator runs both `outcome.exitCode: 0`, zero failures, Docker cleanup confirmed. No F3-IMPL-002 documentation lost; no PII/secret/credential in any audit row; no authorization/tenant-scoping change. `AGENT_COMPLETED`/`TESTS_PASSED`/`PR_UPDATED` (pushed to existing PR #358, no merge) — `NOT_MERGED`/`NOT_DEPLOYED`/`NOT_PRODUCTION_VERIFIED`. |
| [F3-IMPL-004_PII_PHI_LOG_HYGIENE_WAVE1.md](F3-IMPL-004_PII_PHI_LOG_HYGIENE_WAVE1.md) | F3-IMPL-004 (phase F3 — Production Hardening; ran in parallel with F3-IMPL-002/F3-IMPL-003 under a separate task ID; dedicated worktree, branch `feature/f3-impl-004-pii-log-hygiene-wave1`, baseline `origin/main` @ `1909b186a01611c8be90313b7166085a887d05f4`, no drift) | Bounded first-wave PII/PHI runtime log hygiene, following on from F3-IMPL-001's R-018 work. Full `console.*`/`logger.*` inventory across production runtime source (~541 call sites, excluding tests and this task's parallel-safety exclusions): ~378 `SAFE_METADATA`, 2 `DEBUG_ONLY`, ~116 `POTENTIAL_PII`, 12 `CONFIRMED_PII`, 24 `PHI_MEDICAL`, 7 `SECRET_TOKEN`, 2 `MESSAGE_CONTENT`. **Fixed every `SECRET_TOKEN`/`CONFIRMED_PII`/`PHI_MEDICAL` finding — 43 call sites, 18 files:** an Instagram/Facebook access-token prefix logged on every outbound Meta API call (`InstagramMessagingProvider.ts`); two Google AI Studio API-key-in-URL leaks via generic catch blocks (`whatsappStepAwareNlu.ts`, `whatsappConversationAgent.ts`); a `dropdb`-failure message leaking `PGHOST`/`PGPORT`/`PGUSER` (`backupService.ts`); 9 admin/user emails removed or masked across 2 CLI scripts (`repair-owner-admin.ts`, `platform-admin-recover-password.ts`, `migrate-to-multibranch.ts`) and 2 routes (`users.ts`, `usersImport.ts`); 12 structured WhatsApp/Instagram assistant logs that already redacted phone but still logged the co-located treatment/practitioner name in the clear (`whatsappBookingFlow.ts`, `instagramAiConversationProcessor.ts`, `routes/whatsapp.ts`); 5 clinical-write error catches (`patientMedicalHistory.ts`, `dentalChart.ts`, `imaging.ts`, `imagingBridgePublic.ts`, `treatmentCases.ts`) routed through the existing `safeErrorFields()` helper instead of raw `err.message`; 1 unmasked inbound rate-limiter sender identifier (`inboundRateLimiter.ts`). **Deliberately deferred as wave 2, not touched:** ~116 `POTENTIAL_PII` + 2 `MESSAGE_CONTENT` sites (same fix pattern, ~50+ additional files, out of this task's explicitly bounded scope); ~378 `SAFE_METADATA`/2 `DEBUG_ONLY` left alone per instruction. 6 new test files + 5 extended (negative test per changed seam, mix of runtime console-spy and static source-scan), all passing; `typecheck` exit `0`; 13 regression suites re-run, 0 failures; `git status` confirms only the 18 target files + `package.json` + 11 test files touched, none of the parallel-safety-excluded files. No schema/migration/tenant/authorization change; `test:runtime:postgres` judged not required (no data-access path changed) and not run, decision recorded. `AGENT_COMPLETED`/`TESTS_PASSED` — `PR_OPENED` ([PR #356](https://github.com/MustafaBasol/DisKlinikCRM/pull/356)) — `MERGED` (merge commit `13caabb2644d586097d133d72c258ceed33e1f35`), post-main CI (`ci-main-and-nightly` run `31495634507`) `SUCCESS` — `NOT_DEPLOYED`/`NOT_PRODUCTION_VERIFIED`. Reconciled post-merge against `origin/main` by **F3-IMPL-004-R1** (2026-08-11, same worktree/PR, no new branch) — see `F3-IMPL-004-R1_CURRENT_MAIN_RECONCILIATION.md`. |
| [F3-DIGIDENTIS-MAP-001_NORAMEDI_PRACTITIONER_DROPDOWN_ROOT_CAUSE.md](F3-DIGIDENTIS-MAP-001_NORAMEDI_PRACTITIONER_DROPDOWN_ROOT_CAUSE.md) | F3-DIGIDENTIS-MAP-001 / -R1 (phase F3 — Production Hardening, DigiDentiS integration pilot stabilization; ClickUp `869egukwb`; branch `fix/f3-digidentis-map-001-noramedi-practitioner-dropdown`, baseline `origin/main` @ `13caabb2644d586097d133d72c258ceed33e1f35`) | Bug fix: NoraMedi-side practitioner dropdown on the Platform Admin DigiDentiS mapping screen showed only "—" for clinics with active dentists (DigiDentiS-side dropdown/connection test unaffected). Root cause: `GET /clinics/:clinicId/external-calendar/local-options` filtered strictly on canonical-cased `role: 'DENTIST'` against `User.role`, missing legacy-cased (`doctor`/`dentist`, per `utils/roles.ts`'s own documented normalization table) and branch-scoped (`UserClinic.role`) dentists — the identical condition `routes/whatsapp.ts`'s pre-existing `getClinicPractitioners()` already handles via `role: { in: ['DENTIST','dentist','doctor'] }` across both `UserClinic` and `User.clinicId`. Fix mirrors that existing, already-shipped pattern — no second practitioner source of truth, no schema/migration change, no clinic/org scoping change, DigiDentiS-side (`remote-options`) route untouched. New `platformExternalCalendarLocalOptions.test.ts`: real-HTTP E2E against the actual unmodified router, 14/14 (auth, multi-dentist, legacy-role, branch-role, non-dentist/inactive/other-clinic/other-org exclusion, single-dentist, zero-dentist-vs-404, DTO shape). Pre-existing `externalCalendarMapping.test.ts` (10/10) and `externalCalendarConnectionService.test.ts` (22/22) re-run unchanged, zero regression; `typecheck` exit `0`. **R1 (2026-08-11, same PR): architecture review found the eligibility rule above was enforced only on this read path, not on the mapping write path** (`externalCalendarMappingService.ts`'s `verifyLocalEntityBelongsToClinic()` accepted any active clinic user regardless of role) — extracted one shared contract (`listClinicPractitioners()`/`findClinicPractitioner()` in `utils/relationGuards.ts`) and made both the dropdown route and the write-path validator delegate to it. New `externalCalendarMappingPractitionerEligibility.test.ts`: 18/18, directly proving the write path now rejects non-practitioners/other-clinic/other-org/inactive users and accepts branch-scoped/legacy eligible dentists, consistent with the dropdown listing. `AGENT_COMPLETED`/`TESTS_PASSED`/`PR_OPENED` ([PR #360](https://github.com/MustafaBasol/DisKlinikCRM/pull/360)) — `MERGED` (merge commit `ab5f39a08a95fc5fdd2f7e7df03cd00c8522377a`, `mergedAt` 2026-08-11T14:39:04Z), post-main CI (`ci-main-and-nightly` run `31502703267`) `SUCCESS` (exact-head verified by F3-WA-META-COEX-002-R4) — `NOT_DEPLOYED`/`NOT_PRODUCTION_VERIFIED`. |
| [F3-IMPL-005_PLATFORM_ADMIN_FINAL_AUDIT_COVERAGE.md](F3-IMPL-005_PLATFORM_ADMIN_FINAL_AUDIT_COVERAGE.md) | F3-IMPL-005 (phase F3 — Production Hardening; direct follow-up to F3-IMPL-003; branch `feature/f3-impl-005-platform-admin-audit-final`, dedicated worktree, baseline `origin/main` @ `92fc0c0c5eee34ae71bd2508bbfcc2f0309e3055`, no drift) | Closes the exact 12-endpoint residual R-019 gap F3-IMPL-003 deliberately deferred (its §4/§12). Fresh current-main inventory of all 70 authenticated platform-admin routes reconfirms zero drift from F3-IMPL-003's classification (same 70 routes, same 37 mutation-only subset, same 12-item residual list). Added durable `PlatformAdminAuditEvent` coverage via the existing `writePlatformAdminAuditEventInTx`/`writePlatformAdminAuditEvent` pattern to all 12: the 10 organization/clinic/plan/user lifecycle mutations transactionally (mutation + audit atomic, proven via real-Postgres forced-FK-violation rollback tests including a multi-table proof for `POST /clinics`'s nested org+clinic creation), and the 2 file-backup operational triggers via the same best-effort pattern already used by `POST /backups/run`/`restore-test`. All 37 platform-admin mutation-only endpoints are now `AUDITED_DURABLY` — **0 remain `UNAUDITED_PERSISTED_MUTATION`**. No schema/migration change. 37 new tests: 32 added to `platformAdmin.test.ts` (now 118/118, was 82/82) plus a new `platformFileBackupAudit.test.ts` (5/5, mirrors `platformBackupAudit.test.ts`'s deterministic-failure/completion technique). `cd server && npm run typecheck` exit `0`; `test:runtime:postgres-compat` exit `0`; `test:runtime:postgres` exit `0` (new `test:platform-file-backup-audit` script added to its aggregate chain). Two unrelated `node_modules` install-corruption issues (`@prisma/client`'s own `.d.ts` stubs; `@smithy/core`'s `dist-cjs/submodules/`) found and fixed via `npm ci` before relying on any result — not code defects, not reproduced in a known-working sibling worktree with an identical lockfile — see evidence §4.2. **R-019's audit-coverage gap is closed at the repository level; `RISK_REGISTER.md` row updated to `CLOSURE_PROPOSED_AWAITING_EXTERNAL_CONFIRMATION`, not unilaterally `CLOSED`**, per this program's own established rule (R-071/R-072 precedent) that a task's own self-verification is not independent risk acceptance — R-019's break-glass-procedure and scope-boundary mitigations remain separately unaddressed. `AGENT_COMPLETED`/`TESTS_PASSED`/`PR_OPENED` ([PR #365](https://github.com/MustafaBasol/DisKlinikCRM/pull/365)) — `NOT_MERGED`/`NOT_DEPLOYED`/`NOT_PRODUCTION_VERIFIED`. |
| [F3-IR-001_INCIDENT_RESPONSE_TABLETOP_DRILL.md](F3-IR-001_INCIDENT_RESPONSE_TABLETOP_DRILL.md) | F3-IR-001 (phase F3 — Production Hardening; documentation/runbook/evidence-only, branch `docs/f3-ir-001-incident-response-drill`, baseline `origin/main` @ `92fc0c0c5eee34ae71bd2508bbfcc2f0309e3055`) | A `SIMULATED`/`NOT_PRODUCTION_VERIFIED` repository-based tabletop drill of two scenarios (`noramedi-worker` stops while the API stays healthy; suspected cross-tenant patient-data exposure) exercised against [../runbooks/F3_FIRST_CUSTOMER_INCIDENT_RESPONSE.md](../runbooks/F3_FIRST_CUSTOMER_INCIDENT_RESPONSE.md) (the companion runbook — SEV-1/2/3 classification, 10 required runbooks, KVKK breach-path with legal timing/process marked `LEGAL_VERIFICATION_REQUIRED`). No production command executed; no `RISK_REGISTER.md` row changed; one gap (no per-patient-record freeze action in the platform-admin API) surfaced for a future task. |
| [F3-SEC-002_PLATFORM_ADMIN_SESSION_REVOCATION.md](F3-SEC-002_PLATFORM_ADMIN_SESSION_REVOCATION.md) | F3-SEC-002 (phase F3 — Production Hardening; CRITICAL/first-customer security blocker, new risk R-073; branch `feature/f3-sec-002-platform-admin-session-revocation`, dedicated worktree, baseline `origin/main` @ `2d87d7dd3f9dcc3818703bf32814e70b091d2c3c`) | Platform Admin JWT session revocation and credential-change kill switch. Root cause: `authenticatePlatformAdmin` had no persistent DB-backed admin lookup at all — a Platform Admin JWT stayed usable for its full 8h life after a password reset/deactivation, exactly the gap the recovery CLI's own prior output had already named and proposed fixing. Additive `PlatformAdmin.passwordChangedAt DateTime?` (migration `20260811120000_add_platform_admin_password_changed_at`, mirrors `User.passwordChangedAt`); middleware now performs an uncached, fail-closed, per-request DB lookup (exists/active/`iat` vs `passwordChangedAt`/non-numeric-`iat`, uniform generic 401, identity read from the DB row not the token); the emergency recovery CLI (the only credential-reset path in this codebase) now sets `passwordChangedAt` transactionally with the password-hash update. MFA disable deliberately does not invalidate sessions (recorded threat-model decision — already requires password+TOTP re-proof). Fabricated-JWT tests refactored to seed real disposable `PlatformAdmin` fixtures wherever DB-backed identity is now required; new dedicated `platformAdminSessionRevocation.test.ts` (12/12) covers valid/nonexistent/inactive admin, before/after-`passwordChangedAt` (exact-second boundary proof), null-checkpoint-never-rejects, missing/non-numeric `iat`, cross-admin isolation, no-sensitive-logging. `typecheck` exit `0`; 8 focused suites all green (`test:auth` 82/82, `test:platform-admin-password-recovery` 22/22, `test:platform-admin-session-revocation` 12/12, `test:security-incidents` 57/57, `test:retention-manual-run-audit` 29/29, `test:platform-backup` 25/25, `test:platform-backup-audit` 3/3, `test:sms` 77/77); full `test:runtime:postgres`/`test:runtime:postgres-compat` orchestrator runs both `outcome.exitCode: 0` (73 migrations applied cleanly); `server:test:non-disposable` (68 members) exit `0`. No tenant/clinic-facing change. Additive-only rollback (application-code revert, column retained). R-073 `CLOSURE_PROPOSED_AWAITING_EXTERNAL_CONFIRMATION` — not self-marked `CLOSED`. `AGENT_COMPLETED`/`TESTS_PASSED`/`PR_OPENED` — `NOT_MERGED`/`NOT_DEPLOYED`/`NOT_PRODUCTION_VERIFIED`. |
| [F3-WA-META-COEX-002_META_APP_PRODUCTION_READINESS.md](F3-WA-META-COEX-002_META_APP_PRODUCTION_READINESS.md) | F3-WA-META-COEX-002 (phase F3 — Production Hardening; documentation/evidence-only, dedicated branch `docs/f3-wa-meta-coex-002-meta-app-readiness`, baseline `origin/main` @ `1e7c3bbd1012f0c83ec1747b9f12f5cde9c12661`) | NoraMedi Meta App production readiness & official WhatsApp Business App Coexistence enrollment verification for many-clinic onboarding. **Predecessor F3-WA-META-COEX-001, which the task brief asserted was `ACCEPTED by program owner`, has zero corroborating trace in this repository or on GitHub** — recorded as an open program-history discrepancy (§0), not resolved, no fabricated tracker entry written. Direct code inspection (CodeGraph unavailable, documented `Grep`/`Read` fallback bounded to 5 named files + `MetaCallbackPage.tsx`) confirmed: Embedded Signup is launched via a raw `facebook.com/dialog/oauth` popup, not the Meta JS SDK; OAuth `state` is round-tripped but never generated with a stored nonce nor validated anywhere in the callback path (`CONFIRMED` CSRF gap); no `subscribed_apps`/phone-registration-PIN/token-refresh Graph API calls exist; the callback route persists client-submitted WABA/phone/business IDs with no server-side ownership cross-check against the exchanged token. **All Meta-provider-contract verdicts (account model, permissions/Advanced Access, App Review, Embedded Signup JS SDK contract, Coexistence QR behavior, provisioning requirements) are `UNVERIFIED`** — `WebFetch` to `developers.facebook.com`/`www.facebook.com` returned `EGRESS_BLOCKED` on every attempt, confirmed via the environment's own proxy status as a network-policy-level 403 (not transient), independently reproducing COEX-001's own reported blocker; `WebSearch` snippets were found but explicitly excluded as final evidence per this task's Gate 0. Ends **`STATE C — BLOCKED/UNVERIFIED`**, with 3 Meta-Dashboard-dependent `USER ACTION REQUIRED` items (§13) plus the COEX-001 discrepancy. No Meta configuration made or recommended; no runtime file touched (`organizationWhatsApp.ts`/`metaWhatsAppWebhook.ts`/`services/whatsapp/**`/`WhatsAppConnections.tsx`/`schema.prisma`/migrations all untouched). `AGENT_COMPLETED`/`TESTS_PASSED: NOT_APPLICABLE`/`PR_OPENED` (pending) — `NOT_MERGED`/`NOT_DEPLOYED`/`NOT_PRODUCTION_VERIFIED`/`EXTERNAL_META_CONFIG_VERIFIED: NO`. **Corrected/reconciled same-day by F3-WA-META-COEX-002-R1** (2026-08-11, same evidence file, no separate evidence file): the COEX-001 program-history discrepancy is resolved (program-owner-accepted read-only audit, now recorded in `NORAMEDI_MASTER_TRACKER.md` §7), `NORAMEDI_MASTER_TRACKER.md`/`CURRENT_PHASE.md` are now actually updated (§14's original claim that `CURRENT_PHASE.md` was already modified was incorrect and is corrected in place), and primary-source access to `developers.facebook.com`'s official documentation (reachable this session, unlike the original) upgraded 5 Embedded-Signup/Coexistence facts from `UNVERIFIED` to `PRIMARY_META_VERIFIED` — see `phases/F3_PRODUCTION_HARDENING.md`'s F3-WA-META-COEX-002-R1 entry and this file's own §0/§4–9/§14. **F3-WA-META-COEX-002-R2** (2026-08-11, same PR #359) narrowed `CURRENT_PHASE.md`'s R1 "independent tracks" wording (COEX prep may run parallel to F3 hardening; COEX production pilot/deployment/verification are not independent of F3's hardening gates; PR #356/F3-IMPL-004 flagged as an active WhatsApp-runtime overlap surface) and corrected a stale evidence §13 cross-reference. **F3-WA-META-COEX-002-R3** (2026-08-11, same PR #359) reconciles the branch against `origin/main` now that PR #356/F3-IMPL-004 has merged (merge commit `13caabb2644d586097d133d72c258ceed33e1f35`, post-main CI `ci-main-and-nightly` run `31495634507` `SUCCESS`) — the PR #356 overlap blocker recorded by R2 is resolved; Meta Dashboard evidence items #1–#3 remain outstanding. **F3-WA-META-COEX-002-R4** (2026-08-11, same PR #359) reconciles the branch against `origin/main` now that PR #360/F3-DIGIDENTIS-MAP-001(+R1) has also merged (merge commit `ab5f39a08a95fc5fdd2f7e7df03cd00c8522377a`, post-main CI `ci-main-and-nightly` run `31502703267` `SUCCESS`, exact-head verified) — docs-only reconciliation, no Meta-provider verdict changed, both confirmed security findings (OAuth `state` CSRF gap; server-side WABA/phone ownership-verification gap) remain open. Task remains `STATE C — BLOCKED/USER_ACTION_REQUIRED`; F3-WA-META-COEX-003 remains NOT authorized. |
| [F3-IMPL-006_PII_MESSAGE_CONTENT_LOG_HYGIENE_WAVE2.md](F3-IMPL-006_PII_MESSAGE_CONTENT_LOG_HYGIENE_WAVE2.md) | F3-IMPL-006 (phase F3 — Production Hardening; Wave 2 of the PII/PHI log-hygiene program F3-IMPL-004 began; dedicated worktree, branch `feature/f3-impl-006-runtime-log-hygiene-wave2`, baseline `origin/main` @ `92fc0c0` [PR #359/F3-WA-META-COEX-002-R4 merge], no drift) | Full re-inventory (not trusting Wave 1's own ~116/2 estimate): 423 current-main call sites across `routes/`/`services/`/`jobs/`/`middleware/`/`utils/` — 214 `SAFE_METADATA`, 4 `DEBUG_ONLY`, 92 `POTENTIAL_PII_SAFE_AFTER_REVIEW`, 107 `RAW_ERROR_REQUIRES_SAFE_ERROR_FIELDS`, 6 `MESSAGE_CONTENT_REQUIRES_REMOVAL`, 0 `PII_REQUIRES_REDACTION`/`SECRET_REQUIRES_REMOVAL`/`UNKNOWN_REQUIRES_REVIEW`. **Fixed the full residual gap Wave 1 named — all 113 actionable sites across 49 files** — routing every one through the existing `safeErrorFields()` helper (or the pre-existing `err.name`-only sibling convention for 6 SMTP-bounce mail-send catches), including the 2 `MESSAGE_CONTENT` sites Wave 1 explicitly deferred (`instagramAiConversationProcessor.ts`'s inbound/outbound message-save catches) plus 4 newly-discovered ones of the same class (`metaWhatsAppAiProcessor.ts`'s WhatsApp counterpart, `instagramAiConversationProcessor.ts`'s appointment-create-error sites carrying `rawMessage`). 14 new test files + 7 extended (static-source-scan and runtime-spy, mirroring Wave 1's own pattern), all registered in `server/package.json`. Three parallel fix-agent groups (routes/services/jobs+utils) independently attempted `npm install` in the same shared worktree — 7 concurrent processes found racing and corrupting `node_modules`; killed and replaced with one centralized `npm ci`. Two test-authoring defects found and fixed centrally (a literal `*/` inside two JSDoc comments causing a `tsc` syntax error; two static-scan tests anchored on an ambiguous, non-unique log-label substring that matched an earlier unrelated call first) — both were test-file-only defects, not production-code defects. `npx tsc --noEmit` exit `0`. 65 named test scripts run: 57 passed outright; 8 initial failures — 7 share one root cause (`SASL: SCRAM-SERVER-FIRST-MESSAGE`, this sandbox has no reachable Postgres at all, independently confirmed via `Test-NetConnection`/no `postgres` process — a pre-existing environment limitation, not a regression, since none of the 113 fixes touch a Prisma query/schema/data-access path) and 1 was the test-anchor defect above, fixed and re-run 65/65. Wave 1's own 6 dedicated log-privacy suites re-confirmed not regressed. No schema/migration/tenant/authorization/business-logic/provider-payload/AI-prompt change. **R-018 reduced, not closed** — 92 `POTENTIAL_PII_SAFE_AFTER_REVIEW` sites remain judged-safe-by-review rather than by an enforced invariant, and no lint rule exists yet to prevent regression; `RISK_REGISTER.md`'s R-018 row updated additively to record both waves' progress without claiming closure. `AGENT_COMPLETED`/`TESTS_PASSED` — `PR_OPENED` ([PR #364](https://github.com/MustafaBasol/DisKlinikCRM/pull/364)) — `NOT_MERGED`/`NOT_DEPLOYED`/`NOT_PRODUCTION_VERIFIED`. |
| [F3-SEC-EXIT-001_FIRST_CUSTOMER_SECURITY_HARDENING_GATE.md](F3-SEC-EXIT-001_FIRST_CUSTOMER_SECURITY_HARDENING_GATE.md) | F3-SEC-EXIT-001 (phase F3 — Production Hardening, CRITICAL/F3-EXIT-GATE; dedicated worktree `E:\Ek Gelir\Siteler\DisKlinikCRM-worktrees\f3-sec-exit-001`, branch `feature/f3-sec-exit-001-first-customer-security-gate`, baseline `origin/main` @ `92fc0c0c5eee34ae71bd2508bbfcc2f0309e3055`, the PR #359/F3-WA-META-COEX-002-R4 merge commit — confirmed via `git fetch`/`git rev-parse`, no drift) | First-customer production security-hardening sign-off: independently verified 24 named checklist items (A–X) against current `origin/main` with `file:line` evidence, via CodeGraph (targeted to the exact auth/csrf/rate/secrets/encryption surfaces named in the task brief) plus 4 parallel direct-source research passes. **Most severe finding: platform-admin sessions cannot be revoked** — `authenticatePlatformAdmin` (`middleware/platformAuth.ts`) does pure JWT verification with no DB lookup at all, unlike the clinic-user path (`middleware/auth.ts`), which has both a live `isActive` check and `passwordChangedAt`-based revocation. **Deliberately not fixed in this task**: closing it requires a schema migration plus a DB lookup that would break the established test convention in `platformAdmin.test.ts` (and 4 other test files) of constructing fabricated, non-DB-backed admin JWTs to test pure-JWT-layer behavior — crossing from "small, bounded" into "redesigns an established test architecture," which this task's own brief reserves against. Reported as the top `OPEN_BLOCKER` instead, with the exact recommended fix specified. **`.github/dependabot.yml` added** (item W — dependency update automation; the repository had no mechanism of this kind before: no `npm audit` script, no CodeQL, no Snyk, no Dependabot) — purely additive CI-adjacent config, cannot fail any existing workflow, zero test/runtime impact. **R1 correction (2026-08-11):** this was originally described as closing the broader "dependency/security-scan availability" gap — corrected: `.github/dependabot.yml` is version-update automation only and does not by itself prove Dependabot vulnerability alerts, Dependabot security updates, code scanning, or `npm audit` in CI are enabled; those remain `PASS_WITH_EXTERNAL_VERIFICATION`/`OPEN_EXTERNAL_CONFIGURATION` pending independent repository-settings verification (see the evidence file's §11 R1 section). Two items explicitly deferred to their owning parallel tasks per this task's instructions, not duplicated: item I (platform-admin audit coverage, ~10/25 mutation endpoints audited) → F3-IMPL-005 (open, unmerged); item P (PII log hygiene wave 2, ~116 `POTENTIAL_PII` sites remain) → F3-IMPL-006 (open, unmerged). Every production-dependent claim (Redis/replica topology, webhook-secret-per-connection completeness, TLS cert/protocol, MFA-enrollment coverage, Dependabot-alerts-enabled repository setting) is marked `PASS_WITH_EXTERNAL_VERIFICATION` with an exact verification command in the evidence doc's §5 — none asserted as verified from repository evidence alone, per this task's own "no security theater" instruction. No schema/migration/route/response-shape change. **This task's own explicit verdict: the F3 exit gate is NOT satisfied** — item F (platform-admin session revocation) is an open blocker, F3-IMPL-005/F3-IMPL-006/F3-OBS-001 remain unmerged, and no incident-response drill (F3-IR-001) has occurred. `cd server && npm run typecheck` run as the baseline sanity check (no runtime file was changed, so no broader suite was in scope per the task brief's own "run tests only for runtime files actually changed" instruction). `AGENT_COMPLETED`/`TESTS_PASSED` (baseline only) — `PR_OPENED` ([PR #362](https://github.com/MustafaBasol/DisKlinikCRM/pull/362)) — `NOT_MERGED`/`NOT_DEPLOYED`/`NOT_PRODUCTION_VERIFIED`. |
| [F3-OBS-001_PRODUCTION_OBSERVABILITY_MINIMUM.md](F3-OBS-001_PRODUCTION_OBSERVABILITY_MINIMUM.md) | F3-OBS-001 (phase F3 — Production Hardening; CRITICAL/F3 exit-gate task; branch `feature/f3-obs-001-production-observability-minimum`, dedicated worktree, baseline `origin/main` @ `92fc0c0c5eee34ae71bd2508bbfcc2f0309e3055`, the PR #359/F3-WA-META-COEX-002-R4 merge commit — includes F3-IMPL-001…004(+R1) and F3-DIGIDENTIS-MAP-001(+R1)) | First-customer production observability minimum. 16-item pre-work inventory (§1) classifies every item `PRODUCTION_READY`/`PARTIAL`/`MISSING`/`EXTERNAL_CONFIG_REQUIRED`. Adds `GET /api/livez`/`GET /api/readyz` (`routes/health.ts` + `utils/readiness.ts` — DB mandatory/3s-bounded like the pre-existing, byte-for-byte-unchanged `/api/health`; Redis optional/non-blocking, matching `utils/redis.ts`'s own pre-existing fail-open design); process-level `uncaughtException`/`unhandledRejection` handling in both entrypoints (`utils/fatalErrorHandlers.ts`, reusing `utils/logger.ts`'s existing redaction policy via one export-visibility change, `safeErrorLog`); a low-lock-in, Sentry-protocol-compatible error-tracking boundary (`utils/errorTracking.ts`) that is a pure no-op unless `SENTRY_DSN` is set and stays a safe no-op even then unless `@sentry/node` is separately installed (not added as a dependency here). **Evaluated reusing `OperationalEvent` for process-level fatal errors and found it structurally unsuited** — `organizationId` is required/non-nullable, so a tenant-less process crash cannot be represented without a schema change; documented, not implemented. Full OTel tracing/metrics explicitly deferred as a P1/F7 follow-up. No worker HTTP server added — PM2 process status remains the accepted worker-liveness signal (F3-IMPL-002 precedent, unchanged). No schema/migration; `GET /api/health`/`scripts/noramedi-healthcheck.sh`/`scripts/noramedi-deploy.sh`/`ecosystem.config.cjs`/PM2 process names all unchanged. New tests: `readiness.test.ts` (8/8), `healthRoutes.test.ts` (7/7), `fatalErrorHandlers.test.ts` (5/5), `errorTracking.test.ts` (4/4) — all pure/in-process, no disposable Postgres/Redis required. `typecheck` exit `0`; regression re-run of `backgroundJobsOwnership.test.ts`/`processRole.test.ts`/`requestIdCorrelation.test.ts`/`httpRequestLogPrivacy.test.ts`, 64/64, 0 failures. §6 defines a repository-visible alerting contract (8 conditions: API unreachable, readiness failing, worker offline, DB unavailable, repeated job failure, backup stale, disk critical, elevated 5xx) against existing or newly-added signals; §7 lists exactly what external/production-side action would make any of them actually alert a human. `RISK_REGISTER.md` R-074 added (the "no live observability" gap itself). `AGENT_COMPLETED`/`TESTS_PASSED`/`PR_OPENED` — `NOT_MERGED`/`NOT_DEPLOYED`/`NOT_PRODUCTION_VERIFIED`/`LIVE_ALERT_VERIFIED: NO`. **Does not satisfy the F3 exit gate** — no live dashboard/alarm evidence exists or is claimed. |

## Stage model for F0-002

- **Stage A**: repository-only evidence collection (2026-07-18). No production access. Production-dependent rows were recorded as `UNVERIFIED_PRODUCTION`, never guessed or inferred from repository capability.
- **Stage B**: the user ran `F0-002_PRODUCTION_EVIDENCE_REQUEST.md` against the production VPS (read-only, 2026-07-19) and supplied the output; it is documented in `F0-002_PRODUCTION_BASELINE_EVIDENCE.md` and reconciled against `F0-002_REPOSITORY_BASELINE.md` §6.9. **Both stages are now complete.** F0-002's overall task status is `AGENT_COMPLETED` pending a pull request (see the tracker) — this is an inventory/documentation outcome, not a `MERGED`/`DEPLOYED`/`PRODUCTION_VERIFIED` claim, and several risks (storage locality, PITR, restore-test verification, offsite backup) remain open and unremediated by design.

Repository capability (e.g. "the code supports S3-compatible storage") is never treated as proof of production configuration (e.g. "production uses S3"). The two are kept in separate columns everywhere in these evidence files.
