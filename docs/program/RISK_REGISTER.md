# RISK_REGISTER — Program Risk Kaydı

Son güncelleme: 2026-08-16 (**F4-2 — İlk müşteri saha dışı kurtarma: program sahibi risk ayrımı + depo tarafı hazırlığı (üretimde hiçbir değişiklik yok; hiçbir bayt host dışına çıkmadı; tatbikat çalıştırılmadı; marker yazılmadı; migration yok).** Baseline `origin/main` @ `041283b963c3e1486b81903afd11968f86d7576d`, temiz çalışma ağacı. **İki karar kaydedilmiştir.** (1) **Program sahibi, runbook §16.1'in `R-030-DB` / `R-030-FILES` ayrımını ONAYLAMIŞTIR.** `R-030` satırı **kapatılmamış, indirilmemiş, yeniden numaralandırılmamış veya silinmemiştir** — şemsiye satır olarak `OPEN` kalır ve yalnızca her iki yarım kapandığında kapanabilir; böylece mevcut tüm `R-030` referansları geçerli kalır. `FIRST_CUSTOMER_RECOVERY_GATE` bundan böyle yalnızca **`R-030-DB`**'ye bağlıdır: PostgreSQL birincili `disklinik-prod-01`'de olduğundan ikincil bir Türkiye VPS'i o veri sınıfı için gerçekten bağımsız bir arıza alanıdır, oysa görüntüleme birincilinin yerleşimi henüz kararlaştırılmamıştır ve ilk kliniğin veritabanı kurtarımını bloke etmemelidir. Ayrım **yeni risk yaratmaz**, mevcut riski dürüstçe iki izlenebilir yarıma böler — runbook §16.1'in uyardığı tek bir küresel "off-host ✓" iddiasının **yanlış beyan** olmasını önlemek için. (2) **Depo tarafındaki sürdürülebilir-işletim boşlukları kapatılmıştır** (yalnızca ekleyici; `--repo 1` yolu birebir korunur): yedek sarmalayıcısı artık `--repo N` ile hedeflenebilir ve `expire` doğru repoya uygulanır; uzak bir repo için yerel disk/stanza ön koşulları atlanır (yerel-yol repo2 için abort korunur); preflight repo2 şifrelemesini ve repo1'den farklı parolayı **baytlar host dışına çıkmadan önce** doğrular; durum belgesi yedek tazeliğini **repo başına** yayımlar; opscheck, `offHost="yes"` olsa bile **aç kalan veya boş** bir repo2'yi yakalar. Bu son madde gerçek bir yanlış-yeşili kapatır: kanıt marker'ı 30 gün geçerlidir ve repo1 taze kaldığı için, hiçbir yedek almayan bir repo2 bir ay boyunca sağlıklı görünebilirdi. **Hiçbir risk satırı bu görevle kapatılmamıştır:** `R-030`, `R-030-DB` ve `R-030-FILES` `OPEN`; `R-031`/`R-032` 2026-08-15'teki hâlleriyle `CLOSED` ve bu görevce **değerlendirilmemiştir**. **`FIRST_CUSTOMER_RECOVERY_GATE = NOT_SATISFIED`** — kalan blokaj tedarik ve hukuktur, kod değildir: ikincil Türkiye VPS'i **TEDARİK EDİLMEMİŞ** ve runbook §16.2'nin yedi ön koşulunun **tamamı karşılanmamıştır**. Testler: `npm run test:shell` **397 passed / 0 failed**, `test:pitr-status-contract` **16 passed / 0 failed**, `test:platform-recovery-safety` **60 passed / 0 failed**, `tsc --noEmit` temiz.)

Önceki güncelleme: 2026-08-15 (**F4-FCR-002A-CLOSE — Controlled Production PITR Drill Closure Evidence (documentation/evidence-only; this task performed no production mutation, ran no drill, wrote no marker, took no backup and changed no runtime file).** Branch `docs/f4-fcr-002a-close-production-pitr-evidence`, baseline `origin/main` @ `309351885c1389c53d40e4b15e630264dc54954f` — which is also PR #427's merge commit **and** the production release/tooling commit the drill ran against, an exact match, clean working tree, no drift. **Two risks closed on their own written criteria: `R-031` → `CLOSED` and `R-032` → `CLOSED`.** The fifth controlled production restore/PITR drill (`run_id = 20260815-213109-709154`, backup `20260815-224355F`, marker `runId = F4-FCR-002A-20260815-03`, target `2026-08-15 21:26:35.026000+00`, marker WAL `00000001000000000000008C`) completed end to end: **PITR stop point VERIFIED** — marker A = 1, marker B = 0, recovery point `2026-08-15T21:26:00Z`; migrations **74/74** applied/expected with `missing=0`/`ahead=0`/unfinished `0`/rolled back `0`; **106** public tables; **application smoke `passed`** — the stage that proves the restored database is usable by *the code production actually runs*, and the exact stage that failed the previous drill; **tenant-isolation smoke `passed`** (cross-clinic appointments `0`, orphan clinic references `0`, orphaned appointments `0`, RLS policies `0` of `0` expected); **effective RPO 5 min ≤ 60 min target**; **measured RTO 7 s ≤ 14400 s target**; **cleanup verified** (disposable cluster shut down, `/dev/shm/noramedi-pitr-drill-20260815-213109-709154` removed, removal checked rather than assumed). `RESULT = PASS`, `R032_eligible = true`. **Neither closure is self-closure:** the remediating tasks were F4-FCR-002 (repository foundation) and F4-FCR-002A-R4 (the Prisma 7 application-smoke fix, merged as PR #427), the executor was the operator, and this task only records what they produced — the `R-019`/`R-071`/`R-072`/`R-073`/`R-075`/`R-033`/`R-040` no-self-closure precedent is honoured. **`R-030` remains `OPEN`, unweakened, and this drill is explicitly NOT evidence for it:** the restore came from **`repo1`, which is LOCAL** — the same host, disk, hypervisor, provider account and facility as the PostgreSQL primary it protects, i.e. **one failure domain**. The drill enforces this itself and **refused** to write an off-host proof marker from `repo < 2`; none was written. AES-256-CBC on `repo1` protects the backup's **confidentiality** and does nothing for **durability against host loss**; encrypted local pgBackRest does not solve off-host durability and must not be cited as if it did. The secondary Türkiye VPS is still **NOT PROCURED**, all seven §16.2 prerequisites (health-data DPA, E1–E5 residency evidence including E5, subprocessor register §1/§6, contractual support-access answers, the three encryption-at-rest rows, Türkiye-scoped replication, the four-role distinction) are **unmet**, and the `R-030-DB`/`R-030-FILES` split remains an undecided program-owner question — deliberately not performed here either. **`FIRST_CUSTOMER_RECOVERY_GATE = NOT_SATISFIED`**, blocked by `R-030`: the runbook's "closed only by an executed, passing, cleaned-up drill" states a **necessary** condition, not a sufficient one, and the same paragraph holds that `R-030` stays `OPEN` regardless of the drill's outcome. **The four earlier failed drills are preserved as history and none is rewritten as a success** — (1) stale inputs (73/74 migrations, RPO 385 min), (2) application smoke only (Prisma 7 constructor contract), (3) RPO, because the re-used marker target had aged to **96 minutes**, (4) a **sentinel `organizationId` mismatch** between the operator marker utility and the drill verifier. That fourth failure's lesson is recorded, **not normalized**: the drill's default sentinel is `__noramedi_pitr_drill__` and the successful run had to pass `NORAMEDI_PITR_MARKER_ORG=noramedi-f4-pitr-sentinel`; unifying them is an open follow-up and **no runtime script was changed by this task**. **No other row is closed, downgraded, renumbered or re-opened** — in particular `R-003`, `R-004`, `R-005`, `R-006`, `R-007` and `R-029` were **not** assessed by this task and are untouched. Full evidence: [phases/F4_STORAGE_AND_BACKUP.md](phases/F4_STORAGE_AND_BACKUP.md) `F4-FCR-002A-CLOSE` section and [runbooks/F4_RECOVERY_OPERATIONS.md](runbooks/F4_RECOVERY_OPERATIONS.md) §21.7.)

Önceki güncelleme: 2026-08-15 (**F4-FCR-002 — pgBackRest / PITR / Off-Host Recovery Foundation (repository-only; no pgBackRest installed, `archive_mode` unchanged at `off`, no repository created, no credentials, no byte moved off-host, no schema change, no migration).** Baseline `origin/main` @ `6a096d7b2efdeeaf401d506b328783f29c440f1a`. **No risk row below is closed, downgraded, or renumbered by this task.** `R-030`, `R-031` and `R-032` all remain `OPEN` — repository work cannot close any of them, because each requires production evidence that does not exist: R-031 needs `archive_mode=on` observed in production, R-032 needs an executed restore, and R-030 needs an independent failure domain that is not procured. `pgbackrest verify` is repository-integrity checking and is explicitly **not** a restore test. **One structural observation is recorded for the program owner, without creating or renumbering a row:** R-030 as currently worded is a single claim covering data whose primaries live in different places, and it cannot be answered with one boolean. Independence is decided by where the PRIMARY lives — the PostgreSQL primary is on `disklinik-prod-01`, so a repository on the secondary Türkiye VPS **is** an independent failure domain for it (`R-030-DB`, genuinely closable, and pre-authorized as a claimable gain by `F3-C2-ERR-002` §11.5); whereas imaging whose primary would live on that same VPS gains nothing from a backup there (`R-030-FILES` — same disk, hypervisor, provider account and facility), which **relocates** R-030 rather than closing it and implies a third copy elsewhere. Splitting the row is a program-owner decision and is deliberately NOT performed here; the reasoning is recorded in [runbooks/F4_RECOVERY_OPERATIONS.md](runbooks/F4_RECOVERY_OPERATIONS.md) §16.1 so that the eventual closure evidence cannot be over-claimed. Two defects unrelated to the risk rows were fixed: `redactBackupLogLine()` did not redact the pgBackRest repository passphrase in any spelling (served verbatim by `GET /api/platform/backups/logs`), and shell scripts under `scripts/` had no automated guard of any kind (now `npm run test:shell`, wired into CI Layer 1). The scoped freeze exception is `DRAFT_PENDING_PROGRAM_OWNER` — **NOT GRANTED**; `FIRST_CUSTOMER_RECOVERY_GATE = NOT_SATISFIED`.)

Önceki güncelleme: 2026-08-14 (**F3-C2-ERR-001 — External Error Tracking Minimum Safe Activation (repository enablement only; no provider adopted, no DSN configured, no synthetic event sent, nothing deployed).** Branch `feature/f3-c2-err-001-external-error-tracking-activation`, baseline `origin/main` @ `49e7da9e8da07376a312f4013068496308027216`, clean, no rebase. **One new row: `R-078`** — external error-tracking egress boundary and the supply-chain expansion that activating it required. **No existing row closed, none re-opened, none renumbered; R-018, R-019, R-063, R-073, R-074, R-076, R-077 untouched.** `R-074` stays `CLOSED` and this task does **not** disturb it: that row's retained caveat explicitly excluded Sentry/error-tracking adoption from its closure, and adoption still has not happened. **`@sentry/node` is now a pinned dependency (`10.70.0`) but is never imported unless `SENTRY_DSN` is set**, which it is not in any environment — so the runtime state is unchanged and the boundary remains a pure no-op in production. The activation runbook, the adversarial SDK review (17 default integrations that would each have breached the privacy contract, plus two client-level leaks — `server_name`=`os.hostname()` and `contexts.runtime` — that `defaultIntegrations: false` does **not** cover), the verified-on-the-wire payload, and the provider/KVKK classification are all in [evidence/F3-OBS-001_PRODUCTION_OBSERVABILITY_MINIMUM.md](evidence/F3-OBS-001_PRODUCTION_OBSERVABILITY_MINIMUM.md) §15. **No KVKK/legal approval is asserted** — `docs/compliance/62-kvkk-subprocessor-register.md` §7 stays `NO SUBPROCESSOR IDENTIFIED` for error tracking, corrected only on the now-untrue claim that the package is not installed. **`F3-SEC-EXIT-001` §5 item 10 remains `NOT_SATISFIED`; `F3_EXIT_CRITERION_2 = NOT_SATISFIED`; `F3_EXIT_GATE = NOT SATISFIED`; `F3_COMPLETE = NO`; `F4_TRANSITION_AUTHORIZED = NO`.**)

Önceki güncelleme: 2026-08-13 (**F3-OBS-002-CLOSE — Live Observability Closure Evidence and Program-State Update (documentation/evidence-only; this task performed no production mutation — the production facts are operator-executed and were accepted by architecture review in two prior reports).** Branch `docs/f3-obs-002-close-evidence-r074-closure`, baseline `origin/main` @ `0478c86bf97b74b2aa9f465130d2a4daaa3579ec` (PR #405's merge commit, which is also the deployed production SHA — exact match), clean, no drift. **One risk closed: `R-074` → `CLOSED`** on its own stated criteria — live external-prober and dead-man alert chains both production-verified, real human DOWN **and** recovery email delivery proven by two controlled drills that induced no application failure, and the one monitoring credential exposed during evidence capture rotated and permanently retired before closure. Full argument in the R-074 row below. **F3 exit-gate criterion 1 → `SATISFIED`.** **The overall F3 exit gate remains `NOT SATISFIED`** — criteria 2 (security-hardening checklist) and 3 (incident-response drill sufficiency) were out of scope and are **unassessed** by this task, not merely unchanged-by-inference. **`F3_COMPLETE = NO`. `F4_TRANSITION_AUTHORIZED = NO`.** **R-018, R-019, R-063, R-073, R-076, R-077 untouched.** Five documentation defects discovered by live validation are corrected in the same PR (missing `install -d /etc/noramedi` step; an incorrect service/timer-restart instruction in the Drill B procedure — `EnvironmentFile` is re-read on every oneshot `ExecStart`; Period 5 m + Grace 15 m restated as **~20 minutes** to dead-man DOWN rather than a 15-minute SLA; a provider-UI `+02:00` timezone reading hazard; and an obsolete host artifact marked for post-merge operator cleanup). Two monitoring subprocessors (UptimeRobot, Healthchecks.io) added to `docs/compliance/62-kvkk-subprocessor-register.md` §7 with **all legal/DPA/international-transfer fields `TO BE VERIFIED / COUNSEL REVIEW PENDING`** — this risk closure asserts **no** legal or KVKK approval. No runtime/schema/migration/dependency/lockfile file changed; `git diff --check` clean. Full evidence: [evidence/F3-OBS-002_LIVE_OBSERVABILITY_WIRING_ALERT_VERIFICATION.md](evidence/F3-OBS-002_LIVE_OBSERVABILITY_WIRING_ALERT_VERIFICATION.md) §17–§20.)

Önceki güncelleme: 2026-08-12 (**F3-PROD-002 — Deploy-Script Execution Verification and API Ecosystem-Contract Confirmation (documentation/program-control only; this task performed no production access — the production facts are operator-executed and user-supplied).** Branch `docs/f3-prod-002-deploy-script-execution-verification`, baseline `origin/main` @ `0d1b4ae9597b07dc0509f992a2a05663c015388f` (PR #402's merge commit), clean. Baseline ≠ the production deployed SHA `aa18b064267ff5846ae60f73889c3322030bd4a8`, and that is harmless: `git diff --name-only aa18b06… 0d1b4ae… | grep -v '^docs/'` returns **0 files**, and all 8 contract files (`ecosystem.config.cjs`, both `scripts/noramedi-*.sh`, `server/package.json`, `index.ts`, `worker.ts`, `processRole.ts`, `backgroundJobsOwnership.ts`) are **byte-identical blob-for-blob** between the two SHAs — the repository contract compared against production *is* the bytes production runs. **The gap this task existed to close:** F3-IMPL-002-PROD-RECON synchronized the 44-day-stale installed deploy script but explicitly **did not execute it**; three open risks were waiting on that single unexercised run. **It has now run.** One operator invocation of `/usr/local/sbin/noramedi-deploy.sh --skip-pull --skip-build --skip-migrate --skip-generate` — the minimal form exercising exactly steps 5–8 — produced `deploy_exit=0` with production HEAD unchanged before and after. **Accepted production evidence:** `noramedi-api` reloaded from `ecosystem.config.cjs` (PID `607545`, `restart_time` `11`→`12`, `online`, `0` unstable, `cwd` `/var/www/noramedi/server`, `npm run start`, startup log `role=api (declared=true) ownsJobs=false (RUN_BACKGROUND_JOBS=false — jobs delegated to a dedicated worker process)`); `noramedi-worker` reloaded (PID `607578`, `11`→`12` — **continuous with the `11` F3-IMPL-002-PROD-RECON recorded, so the same PM2 app record was acted on, not a duplicate** —, `online`, `0` unstable, `cwd` `/var/www/noramedi/server`, `npm run start:worker`, `role=worker (declared=true) ownsJobs=true`, `All background jobs scheduled.`); step 8 emitted `OK — PM2 process 'noramedi-worker' is online after 1 attempt(s).`, the exact format literal at `scripts/noramedi-deploy.sh:119`; post-deploy external `GET /api/health` `{"status":"ok"}` and installed healthcheck `OK (200) … after 1 attempt(s)`. **A 14-point reconciliation of the observed lifecycle against the repository contract found zero mismatches.** **Two risks closed, each strictly on its own row's stated criteria and neither self-closed** (the remediating task was F3-IMPL-002, the installer was F3-IMPL-002-PROD-RECON, the executor was the operator): **R-033 → `CLOSED`** — its row named *"steps 5-8'in tek bir başarılı koşuda yakalanması … script `exit 0`"* and all five elements are in hand from one run; **R-040 → `CLOSED`** — its row named *"ecosystem-kaynaklı API `startOrReload` + `role=api (declared=true)` + … `cwd` = `/var/www/noramedi/server` teyidi"* and all three are in hand, with `script`/`args` matching additionally. `declared=true` is the load-bearing token: `processRole.ts:75` is its sole path and requires `NORAMEDI_PROCESS_ROLE` present, recognized **and** matching, so it is direct runtime proof the ecosystem file's `env` block reached the process — an unset var would still print `role=api` with `declared=false`. Likewise `ownsJobs=false` arises only on exact `RUN_BACKGROUND_JOBS === 'false'` (`backgroundJobsOwnership.ts:38`). **R-034 is NOT re-closed** — it was already `CLOSED` by the R2 reconciliation; this run only corroborates it with stronger provenance (the same `ownsJobs=false`, now on an API process demonstrably started *from* the repository config). **R-077 remains `OPEN`, one half resolved:** the `noramedi-healthcheck.sh` sync state — `UNVERIFIED` since F0-002, never once measured — is now verified, installed and repository copies both `14a148982d74eda76d7adf757e09a32c0329b65195413961ca32cd4b8c6cef7e` (deploy script re-confirmed at `794375b4…`; both hashes independently reproduced from `git cat-file` here); but **none of the row's three drift-prevention options was adopted**, so recurrence is unaddressed. Beyond the hashes, this run *behaviourally* proves the installed script is the post-F3-IMPL-002 version — it executed steps 5–8 and emitted `verify_pm2_online`'s literal, none of which exists in the 2026-06-29 revision. **R-063 evidence strengthened, not closed:** the first **raw committed measurement** of the reload unavailability window (local healthcheck `000`/`000`/`000` then `200` at attempt 4 of 12, API healthy ≈19 s after deploy start — bounded startup recovery the script explicitly permits, **not** a failure and **not** zero-downtime), plus a sharper root-cause statement — `--skip-generate` **cannot** suppress `prisma generate`, because it lives in `server/package.json:10-11`'s `start`/`start:worker` scripts, not only in deploy step 4; Prisma Client generation therefore occurred at both process startups. That is precisely R-063's own named remediation ("pre-generate at build/deploy time, not at process start") and is recorded as an operational-contract nuance warranting a separate task, **not implemented here**. **Historical `pm2 logs` findings are mapped to existing rows, not duplicated and not attributed to this deploy** (no temporal evidence was supplied): WhatsApp NLU parse → **R-066**; `pg` deprecation → **R-067**; `node-cron` missed executions → **R-068**; missing Meta templates → **R-069**; the revenue-report `GROUP BY` error was fixed by PR #252 / `08f2eaf82a205cf3f997c57e6a295fedd66b142d`, **independently verified here to be an ancestor of the deployed SHA**; and the Redis `ECONNREFUSED` lines are **explicitly not a current fault** — the current API startup reports `[redis] Shared store enabled via REDIS_URL.` (`server/src/utils/redis.ts:41`) and `/api/readyz` reported `redis=ok`. **R-037** gains an observation only (API `11`→`12` is *lower* than F0-002's `14` of 2026-07-19 — the same counter-reset phenomenon the row already carries for the worker); **R-036, R-018, R-019, R-073, R-074, R-075, R-076 untouched.** **[Rollback-anchor correction, additive — the R2 entry below is narrowed, not rewritten.]** `b21cae911a0aa3444ebcd6e714a92c4f0802608a` **does exist on the production host** (type `commit`, `git show` succeeds, subject `fix(external-calendar): enforce practitioner eligibility on the mapping write path (F3-DIGIDENTIS-MAP-001-R1)`), so **it is no longer accurate to call it globally nonexistent**; everything R2 says about this repository and about `origin` remains true and was re-verified. The decisive new fact is different: `git merge-base --is-ancestor b21cae91… aa18b06…` exits **`1`** on production — **it is not on current production's lineage**. **Corrected classification: locally resolvable on production, but invalid as the documented rollback-lineage anchor for current production.** The valid anchor is **`aa18b064267ff5846ae60f73889c3322030bd4a8`** (resolvable here, ancestor of `origin/main`, present on `origin`, and the current deployed SHA); the repository has **zero tags**. *(Repository-side: `origin/main` carries `152ebaf5e9ad9b2da822d286c9354a9755d63440` with the byte-identical subject and it **is** an ancestor of `aa18b06…` — consistent with `b21cae91…` being a pre-squash branch commit; recorded as inference, not proof.)* **F3-IMPL-002 is now `PRODUCTION_VERIFIED`** — both halves, the role/ownership contract and the deploy lifecycle (previously `INSTALLED_NOT_YET_EXERCISED`). Not claimed: job **execution** (registration only — no completed tick, no `JobLock` lease), PM2 **reboot persistence** (`pm2 save` not evidenced), drift prevention, or zero-downtime. **F3 exit gate: `NOT SATISFIED` — unchanged.** R-033 and R-040 are F0-006/F3 deployment/configuration-topology rows named by none of the gate's three criteria; the same non-advancing precedent that applied to R-034's and R-075's closures applies here. Criterion 1 gains only a *negative* signal — a 19-second outage passed with no automated detection, which is exactly **R-074**'s cost. **F3 COMPLETE: `NO`. F4 transition authorized: `NO`.** No schema/runtime/CI/deployment file changed; diff limited to `docs/program/**`; `git diff --check` clean. Full evidence: [evidence/F3-PROD-002_DEPLOY_SCRIPT_EXECUTION_VERIFICATION.md](evidence/F3-PROD-002_DEPLOY_SCRIPT_EXECUTION_VERIFICATION.md).)

Prior update: 2026-08-12 (**F3-IMPL-002-PROD-RECON — Production Worker Contract Verification and Deploy-Script Drift Reconciliation (documentation/program-control only; this task performed no production access — the production facts are user/operator-supplied).** Baseline `origin/main` @ `aa18b064267ff5846ae60f73889c3322030bd4a8`, which is also the supplied production deployed SHA — **exact match**, no drift. Reconciles a second set of operator-supplied production evidence against F3-IMPL-002's worker contract. **Accepted:** `noramedi-api` online with `health`/`livez`/`readyz` `200` (`database=ok`, `redis=ok`); a **stale** `noramedi-worker` instance (PID `603932`, `restart_count` `10`) discovered after deployment and manually reconciled via `pm2 startOrReload /var/www/noramedi/ecosystem.config.cjs --only noramedi-worker --update-env` to PID `605889` / `restart_count` `11` (clean `SIGINT`/clean exit, `Prisma Client v7.9.1`, `role=worker (declared=true)`, `ownsJobs=true`, `All background jobs scheduled.`); and the synchronization of the drifted deploy script (rollback backup `/usr/local/sbin/noramedi-deploy.sh.bak.20260812-174738`, `bash -n` clean on both copies, installed `root:root` `0755`, both hashes now `794375b4249b063e167a8d6f885df1a590bd9de9df367e91a0d3eaaf635f5012`, `--help` smoke listing all four capabilities, **no deploy executed during synchronization**). **Repository-side corroboration this task did run (no production access):** the supplied post-sync hash is byte-identical to `git show HEAD:scripts/noramedi-deploy.sh`, and the stale hash `8dffcd13…` is byte-identical to the blob at commit `96313f39` (2026-06-29) — establishing that the installed script was **44 days stale and predated F3-IMPL-002 entirely**. **This falsifies a claim F3-PROD-001 recorded:** both 2026-08-12 production deploys ran the old six-step script (`pm2 reload noramedi-api` only, no `ecosystem.config.cjs`, no worker reload, no worker verification), so F3-PROD-001 §3's worker row described the **stale** instance and its API row's `RUN_BACKGROUND_JOBS=false` comes from PM2's cached environment rather than the repository-defined config source (additive correction banner added to that evidence file; its body preserved unedited). **R-033/R-034/R-040 rows below updated with this evidence and with sharper status wording — **[R2] of these, only R-034 is closed; R-033 and R-040 are not, and neither is moved to `MITIGATED`:** R-033 is now installed at the production invocation path and hash-verified but **has never executed**; **[R2] R-034 → `CLOSED`** — its own named missing control was *verification of the real production `RUN_BACKGROUND_JOBS` value*, and the production API's observed `ownsJobs=false` verifies exactly that (per `backgroundJobsOwnership.ts`, only literal `'false'` yields it), with the worker owning jobs — **exactly one job owner, verified**; the R1 wording that kept it `OPEN` on config-*provenance* grounds is withdrawn as over-strict, provenance being **R-040**'s criterion, not this row's; registration-level only, no job execution claimed, prior token preserved as history; R-040 is **partially verified** — the worker app's first production application of `ecosystem.config.cjs` is confirmed, the API app's `cwd`/`env` are not. **New row R-077 added** (installed-vs-repository operational script drift; `noramedi-healthcheck.sh` sync state `UNVERIFIED`). R-036/R-037 touched only as observations, both rows left unedited — note under R-037 that the worker's `restart_count` of `10`→`11` is **lower** than the `13` F0-002 recorded on 2026-07-19, implying PM2's counter for that app was reset between the two observations, consistent with R-033's out-of-band-lifecycle thesis. R-018/R-019/R-073/R-074/R-076 untouched. **[R1, 2026-08-12 — reviewer correction] `R-075` → `CLOSED`.** A further set of operator-supplied production evidence establishes both gaps that row itself named: post-deploy `npm audit --omit=dev` **was** run on production at the deployed SHA and reports **`found 0 vulnerabilities`**, and engine fetch is verified exactly — the production Prisma engine hash `e922089b7d7502aff4249d5da3420f6fa55fc6ad` is **byte-identical** to the `@prisma/engines-version` pin in `server/package-lock.json` at that SHA. The row's only remaining condition was **external confirmation**, now explicitly provided by the PR #402 reviewer; the `R-019/R-071/R-072/R-073` no-self-closure precedent is honoured because the confirming task is neither the remediating task (F3-SEC-003) nor the confirming party. The prior `CLOSURE_PROPOSED_AWAITING_EXTERNAL_CONFIRMATION` token is **preserved in the row as history, not deleted**. The closure is **narrow**: continuous dependency monitoring, the accepted frontend `react-router` advisories, and **R-076** are explicitly *not* resolved by it, and **R-033 and R-040 are not affected — both remain `OPEN`** (**[R2]** R-034 is separately `CLOSED`, on its own criteria, not via R-075).

**[R2] Second finding recorded without a new risk ID — `F3-PROD-001`'s rollback anchor.** The pre-deploy production SHA / rollback anchor `b21cae911a0aa3444ebcd6e714a92c4f0802608a` recorded by F3-PROD-001 **is not resolvable in this repository or on `origin`** — re-verified six ways, including `git fetch origin <sha>` rejected server-side with `upload-pack: not our ref`. The documented anchor is therefore **not inspectable or diffable**; this is **not** a claim that rollback was operationally impossible, and **no replacement SHA is invented**. Remediation: record a resolvable commit/tag/release reference before relying on that rollback procedure. **Deliberately not given a risk ID** — it is a record defect in an evidence document, not a new control gap, and the underlying deployment-hygiene exposure is already carried by **R-077**; recorded as an evidence correction (additive banner on F3-PROD-001) plus an open operational follow-up. *(This R1 also withdraws a statement in the first draft of this entry's evidence document asserting that no post-deploy production audit had been supplied; that statement was incorrect and has been removed everywhere it appeared.)* **F3 exit gate unchanged: `NOT SATISFIED` — R-075 is explicitly non-blocking and named by none of the gate's three criteria, so its closure does not advance the gate.** No schema/runtime/CI/deployment file changed; diff limited to `docs/program/**`. Full evidence: [evidence/F3-IMPL-002-PROD-RECON_PRODUCTION_WORKER_CONTRACT_VERIFICATION.md](evidence/F3-IMPL-002-PROD-RECON_PRODUCTION_WORKER_CONTRACT_VERIFICATION.md).)

Prior update: 2026-08-12 (**F3-SEC-003 — Production Dependency Vulnerability Remediation and Exploitability Classification.** All 8 advisories behind **R-075** were reproduced, itemized, dependency-path-traced and exploitability-classified; **none was found exploitable from any NoraMedi request or job path**, and all 8 were remediated anyway at the repository level — `server/` `npm audit` is now **0 vulnerabilities** in both `--omit=dev` and full scopes (was 1 low / 5 moderate / 2 high). Minimum safe fix: Prisma family `7.8.0`→`7.9.1` (**minor**, not major — `@prisma/dev@0.24.17` is patched and drops `hono`/`@hono/node-server` entirely), plus a **non-forced** `npm audit fix` for `brace-expansion` (both copies), `fast-uri` and `body-parser`; the orphaned `overrides["@hono/node-server"] = "1.19.13"` was removed because it force-pinned a version *below* that advisory's own fix. **R-075 → `CLOSURE_PROPOSED_AWAITING_EXTERNAL_CONFIRMATION`** (not self-closed — post-deploy production `npm audit` has not been run; this task performed no production access). **New R-076 added** — an incidental, non-dependency stored-XSS finding (`ClinicLegalProfile.website` accepts any scheme and reaches a live `href` on an unauthenticated public page), deliberately **not** fixed by F3-SEC-003 because its PR scope is dependency remediation only. Frontend `react-router` remains **accepted, not fixed**: the 6.x line ends at 6.30.4 and the fix lands only at 7.18.0, a v6→v7 major; both advisories are structurally inapplicable to this client-only SPA. No schema/migration/data change. Does **not** close F3 or satisfy its exit gate. Full detail: [evidence/F3-SEC-003_PRODUCTION_DEPENDENCY_VULNERABILITY_REMEDIATION.md](evidence/F3-SEC-003_PRODUCTION_DEPENDENCY_VULNERABILITY_REMEDIATION.md).)

Prior update: 2026-08-12 (**F3-PROD-001 — F3 Exit-Gate Closure Reconciliation of User-Supplied Production Deployment Evidence (documentation-only; this task performed no production access itself).** The merged F3 train (previously deployment-blocked per F3-PROGRAM-RECON-001) is now confirmed deployed to production: prior SHA `b21cae911a0aa3444ebcd6e714a92c4f0802608a` → deployed SHA `f0ff4c70abe08b192233d17cdcd1b7a77dbe4b08`; migration `20260811120000_add_platform_admin_password_changed_at` confirmed applied (73 migrations, schema up to date); PM2 `noramedi-api`/`noramedi-worker` both `ONLINE` with the repository-defined role/job-ownership contract confirmed matching; `/api/health`, `/api/livez`, `/api/readyz` all confirmed `200` with real dependency checks passing; normal Platform Admin login confirmed working. **R-073 and R-074 rows updated with this new evidence, status tokens unchanged** (`CLOSURE_PROPOSED_AWAITING_EXTERNAL_CONFIRMATION` and `OPEN` respectively — deployment evidence narrows but does not itself satisfy either row's remaining closure requirement, per this program's own R-071/R-072/R-073 precedent that self/operator-supplied evidence is not a substitute for the specific external confirmation or live-monitoring evidence each row still names). **R-019 row reviewed, found already correct, not edited** — still `CLOSURE_PROPOSED_AWAITING_EXTERNAL_CONFIRMATION`. **R-018's status token unchanged (`OPEN`) but its evidence text is corrected**: this session's own working branch was found stale by 10 `origin/main` commits during this task; F3-IMPL-007 (PR #399, the log-privacy regression guard) and F3-CI-OPT-001 (PR #398, its CI wiring) are both confirmed `MERGED` and ancestors of the deployed SHA — the guard is live in production's own deployed code, correcting the prior "not implemented" reading; this does not close R-018, whose own evidence names 103 grandfathered + 92 review-only sites as still unremediated. **New risk R-075 added** (production `npm audit`: 0 critical/2 high/5 moderate/1 low), assigned to a separate, not-yet-started follow-up task `F3-SEC-003`, no dependency changed by this task. **F3 exit gate re-assessed: still `NOT SATISFIED`** — deployment resolves the "nothing in the merge train protects a real customer until deployed" blocker but none of the three named exit-gate criteria (live observability wiring, full external security-checklist verification, incident-response drill sufficiency decision) are satisfied by deployment alone. Full detail: [evidence/F3-PROD-001_PRODUCTION_DEPLOYMENT_AND_VERIFICATION.md](evidence/F3-PROD-001_PRODUCTION_DEPLOYMENT_AND_VERIFICATION.md).)

Prior update: 2026-08-12 (**F3-PROGRAM-RECON-001, corrected same-day by F3-PROGRAM-RECON-001-R1 — Post-Merge Program-State Reconciliation + F3-IMPL-007 Program-Control Correction (documentation-only, no status token changed in this file).** **R1 addendum:** the program controller confirms F3-IMPL-007 ("Sensitive Runtime Logging Regression Guard") as an external program-backlog item, now `UNBLOCKED` (its F3-IMPL-006 dependency is merged) — it is the planned regression-guard implementation for the gap R-018 already names below (missing lint rule/automated check preventing sensitive-log regression). This is program-management input, not repository evidence; F3-IMPL-007 has no repository trace, is not implemented, and **does not close R-018** — R-018's status below is unchanged. Independently re-confirmed via `git merge-base --is-ancestor`+`gh api .../actions/runs` that F3-IMPL-005/PR #365, F3-IMPL-006/PR #364, F3-OBS-001/PR #363, F3-SEC-002/PR #396, F3-SEC-EXIT-001/PR #362, and F3-IR-001/PR #361 are now all merged into `origin/main` with green post-merge main CI — see [F3-PROGRAM-RECON-001_POST_MERGE_RECONCILIATION.md](F3-PROGRAM-RECON-001_POST_MERGE_RECONCILIATION.md) §2/§3 for exact SHAs/CI run IDs. **R-018/R-019/R-073/R-074 rows below were reviewed against this program's own closure rules and found already correct — none edited**: R-018 correctly `OPEN` (both log-hygiene waves merged, but 92 `POTENTIAL_PII_SAFE_AFTER_REVIEW` sites remain review-only with no lint-rule regression guard); R-019 correctly `CLOSURE_PROPOSED_AWAITING_EXTERNAL_CONFIRMATION`, not self-closed; R-073 correctly `CLOSURE_PROPOSED_AWAITING_EXTERNAL_CONFIRMATION` (implementation merged, no production deployment/migration-application evidence); R-074 correctly `OPEN` (repository-side observability foundation merged, zero live dashboard/alert/uptime-probe evidence). **F3 exit gate independently re-assessed: NOT satisfied** — see the evidence doc §5 for the full three-criterion matrix. No schema/migration/application file touched by this task.)

Prior update: 2026-08-11 (**F3-OBS-001-R1 — External Error-Telemetry Privacy Hardening, Risk-ID Reconciliation, and Main Reconciliation (same PR #363 as F3-OBS-001 below; no production deployment).** Fixes two architecture-review blockers on F3-OBS-001: (1) `server/src/utils/errorTracking.ts`'s `captureFatalError` no longer forwards the raw caught error (or anything derived from it — message/stack/nested `cause`) to the external provider; it now sends only a fixed generic message plus `err.name`/requestId/role/safe-route-template/environment/release via `Sentry.captureMessage()`, proven by new sentinel-secret tests in `errorTracking.test.ts` (patient name/email/phone/token, including via a nested `Error.cause`, never appear in the captured payload); (2) **R-073 (added by F3-OBS-001 below) renumbered to R-074** — PR #362 (open, F3-SEC-EXIT-001) independently claims R-073 for the platform-admin session-revocation gap, and this row's own original R-073 numbering would have collided with it once both merge; every F3-OBS-001 R-073 reference in this file and elsewhere updated to R-074, R-073 itself untouched. Also reconciled against `origin/main` (PR #361/F3-IR-001, 2 commits) via a normal, non-force `git merge`; the resulting conflicts in this file and the other shared program-control docs were resolved additively, dropping neither side's content. No schema/migration change. `cd server && npm run typecheck` exit `0`. `AGENT_COMPLETED`/`TESTS_PASSED`/`PR_UPDATED` (pushed to existing PR #363) — `NOT_MERGED`/`NOT_DEPLOYED`/`NOT_PRODUCTION_VERIFIED`/`LIVE_ALERT_VERIFIED: NO`. Still does not satisfy F3's exit gate.)

Prior update: 2026-08-11 (**F3-OBS-001 — First-Customer Production Observability Minimum and Alerting Foundation (repository-level foundation only; no production deployment, no live dashboard/alert wiring, no F3 exit-gate claim by this task).** Adds **R-074** below (no live observability/alerting evidence existed anywhere in this repository prior to this task — the exact gap `phases/F3_PRODUCTION_HARDENING.md`'s own exit gate names). Pre-work inventory across 16 items found: `GET /api/health` already production-ready and left byte-for-byte unchanged; no readiness/liveness split existed (added `GET /api/readyz`/`GET /api/livez`, `server/src/routes/health.ts` + `server/src/utils/readiness.ts` — DB mandatory/3s-bounded, Redis optional/non-blocking per `utils/redis.ts`'s own pre-existing fail-open design); no `uncaughtException`/`unhandledRejection` handling existed in either entrypoint (added `server/src/utils/fatalErrorHandlers.ts`, installed in both `index.ts` and `worker.ts`, reusing `utils/logger.ts`'s existing `safeErrorLog` redaction policy — that function's only change is private → exported); no error-tracking provider is configured anywhere in the repository (added a low-lock-in, Sentry-protocol-compatible boundary, `server/src/utils/errorTracking.ts`, a pure no-op unless `SENTRY_DSN` is set and stays a safe no-op even then unless `@sentry/node` is separately installed — not added as a new dependency by this task). **Evaluated reusing the existing `OperationalEvent` model for process-level fatal errors and found it structurally unsuited** — `organizationId` is a required, non-nullable field (`schema.prisma:1801`), so a process-wide crash with no tenant context cannot be represented without a schema change, which this task's own brief instructs against absent a compelling need; not reused, documented instead. Full OTel tracing/metrics explicitly deferred (documented as a P1/F7 follow-up, not built — the brief's own instruction when full OTel is not small/low-risk). Worker gets **no** new HTTP server — PM2 process status remains the accepted worker-liveness signal (F3-IMPL-002 precedent, unchanged, not re-litigated). No schema/migration change; `GET /api/health`, `scripts/noramedi-healthcheck.sh`, `scripts/noramedi-deploy.sh`, `ecosystem.config.cjs`, PM2 process names all unchanged. `cd server && npm run typecheck` exit `0`; 4 new test files (24/24 assertions: `test:readiness`, `test:health-routes`, `test:fatal-error-handlers`, `test:error-tracking`, all pure/in-process, no disposable Postgres/Redis required since `/readyz`'s real DB check is the same one-line query `/api/health` already used in production, now unit-tested via dependency injection); regression re-run of `test:background-jobs-ownership`/`test:process-role`/`test:request-id-correlation`/`test:http-log-privacy` (the logger-adjacent suite), 64/64, 0 failures. `AGENT_COMPLETED`/`TESTS_PASSED`/`PR_OPENED` — `NOT_MERGED`/`NOT_DEPLOYED`/`NOT_PRODUCTION_VERIFIED`/`LIVE_ALERT_VERIFIED: NO`. **This does not satisfy F3's exit gate** — no live observability dashboard/alarm evidence exists; the evidence doc's own §7 enumerates exactly what external/production-side action (uptime-prober wiring, log-aggregation/alert-channel setup, optional Sentry DSN provisioning, PM2/backup-staleness alert wiring, disk monitoring) would produce it. Full evidence: [evidence/F3-OBS-001_PRODUCTION_OBSERVABILITY_MINIMUM.md](evidence/F3-OBS-001_PRODUCTION_OBSERVABILITY_MINIMUM.md).)

Prior update: 2026-08-11 (**F3-IMPL-005 — Platform Admin Remaining Privileged Mutation Audit Coverage (repository-level; `AGENT_COMPLETED`/`TESTS_PASSED`, `NOT_MERGED`/`NOT_DEPLOYED`/`NOT_PRODUCTION_VERIFIED`).** Closes the exact 12-endpoint residual R-019 gap F3-IMPL-003 deliberately left open (its evidence file §4/§12): a fresh current-main inventory of all 70 authenticated platform-admin routes reconfirms zero drift from F3-IMPL-003's own classification, then adds durable `PlatformAdminAuditEvent` coverage (via `writePlatformAdminAuditEventInTx`/`writePlatformAdminAuditEvent`, the same pattern F3-IMPL-001/F3-IMPL-003 established) to the 10 remaining organization/clinic/plan/user lifecycle mutations and the 2 remaining file-backup operational triggers — all 37 mutation-only platform-admin endpoints are now `AUDITED_DURABLY`, 0 remain `UNAUDITED_PERSISTED_MUTATION`. **R-019 row below updated to `CLOSURE_PROPOSED_AWAITING_EXTERNAL_CONFIRMATION`, not unilaterally `CLOSED`** — per this program's own established rule (R-071/R-072 precedent), a task's own self-verification is not independent risk acceptance. No schema/migration change. Full evidence: [evidence/F3-IMPL-005_PLATFORM_ADMIN_FINAL_AUDIT_COVERAGE.md](evidence/F3-IMPL-005_PLATFORM_ADMIN_FINAL_AUDIT_COVERAGE.md).)

Prior update: 2026-08-11 (**F3-SEC-EXIT-001-R1 — Security Gate Evidence Correction and Main Reconciliation.** Architecture review accepted R-073 (below) and the overall assessment, but flagged that F3-SEC-EXIT-001's original text overstated `.github/dependabot.yml` as "closes one dependency-scanning gap." **Corrected:** `.github/dependabot.yml` is dependency **update automation** only (scheduled version-update PRs, added by F3-SEC-EXIT-001) — it does not by itself prove Dependabot vulnerability alerts, Dependabot security updates, code scanning, or `npm audit` in CI are enabled; the broader dependency/security-scanning gap remains `PASS_WITH_EXTERNAL_VERIFICATION`/`OPEN_EXTERNAL_CONFIGURATION` pending independent verification of the repository's Code security and analysis settings — not claimed closed. **R-073 itself is unchanged and not renumbered** (F3-OBS-001 will use R-074). No risk row status changed by this correction beyond this note. Full evidence: [evidence/F3-SEC-EXIT-001_FIRST_CUSTOMER_SECURITY_HARDENING_GATE.md](evidence/F3-SEC-EXIT-001_FIRST_CUSTOMER_SECURITY_HARDENING_GATE.md) §11 (R1).)

Prior update: 2026-08-11 (**F3-SEC-EXIT-001 — First-Customer Production Security Hardening Exit Checklist and Gap Closure.** Independently verified 24 security checklist items (A–X) against current `origin/main`. **Adds R-073** (new row below): platform-admin JWT sessions have no revocation mechanism whatsoever — `middleware/platformAuth.ts`'s `authenticatePlatformAdmin` does pure `jwt.verify` with zero DB lookup, so there is no `isActive` re-check and no `passwordChangedAt`-based kill-switch (the `PlatformAdmin` model doesn't even have that column), unlike the equivalent, already-correct clinic-user path (`middleware/auth.ts:138-150`). Deliberately **not fixed** by this task — mirroring the clinic-user pattern requires a schema migration and would break the established test convention (5 test files, including `platformAdmin.test.ts`, construct fabricated non-DB-backed admin JWTs specifically to test pure-JWT-layer behavior) — reported as this task's own top `OPEN_BLOCKER` instead of silently worked around. **`.github/dependabot.yml` added** — dependency update automation; see the F3-SEC-EXIT-001-R1 note above for the corrected description (originally overstated as "closes one dependency-scanning gap"). R-018/R-019 rows below **not** touched by this task (owned by F3-IMPL-006/F3-IMPL-005 respectively, both open/unmerged — this task explicitly deferred to them, not duplicated). Full evidence: [evidence/F3-SEC-EXIT-001_FIRST_CUSTOMER_SECURITY_HARDENING_GATE.md](evidence/F3-SEC-EXIT-001_FIRST_CUSTOMER_SECURITY_HARDENING_GATE.md).)

Prior update: 2026-08-11 (**F3-IR-001 — First-Customer Incident Response Runbook and Tabletop Drill Evidence (documentation/runbook/evidence-only task; no application/runtime/schema/migration file changed; changes no risk row's status below).** Baseline `origin/main` @ `92fc0c0c5eee34ae71bd2508bbfcc2f0309e3055`, clean, no drift. Produced [docs/program/runbooks/F3_FIRST_CUSTOMER_INCIDENT_RESPONSE.md](runbooks/F3_FIRST_CUSTOMER_INCIDENT_RESPONSE.md) and [evidence/F3-IR-001_INCIDENT_RESPONSE_TABLETOP_DRILL.md](evidence/F3-IR-001_INCIDENT_RESPONSE_TABLETOP_DRILL.md) — an incident-response runbook and a `SIMULATED`/`NOT_PRODUCTION_VERIFIED` repository-based tabletop drill directly informed by, and citing, this file's own R-018 (log leakage), R-019 (platform-admin authority overreach), R-029…R-032 (local-only storage, no offsite backup, no PITR, unverified restore), R-033/R-034/R-040 (worker deploy automation, duplicate job registration, config-source ambiguity — all `OPEN — repository-level mitigation implemented, production not yet verified`, per F3-IMPL-002 below), R-035 (Node version drift), R-036 (PM2-as-root), R-037 (unexplained restart counts), R-046 (migration rollback/tenant-impact evidence), R-054/R-055 (raw-SQL tenant-scoping), and R-061/R-062 (KVKK kill-switch/migration-ordering, both since closed/mitigated) rows. **This task does not close, mitigate, reopen, or otherwise change the status of any row below** — it is a downstream consumer of this file's existing risk evidence, not a new risk-assessment task. One operational gap was newly surfaced by the tabletop drill's Scenario B (suspected cross-tenant exposure) and is recorded here for future risk-registration consideration, not added as a new numbered row by this documentation-only task: **no per-patient-record freeze/suspension action exists in the platform-admin API as inspected in this task's scope — only organization-level (`PATCH /api/platform/organizations/:id/status`) and clinic-level (`PATCH /api/platform/clinics/:id/status`) suspension exist**, which is coarser than a real single-patient-record exposure may warrant. A future task may choose to formalize this as a new risk-register row; this task deliberately does not do so itself, consistent with its documentation/runbook-only scope.)

Prior update: 2026-08-10 (**F3-IMPL-002 — Production Worker Process Contract, Deploy Lifecycle & Job Ownership Hardening (repository-level mitigation, no production deployment/verification performed by this task).** Closes R-033's and R-040's repository-level gaps and reduces R-034's duplicate-registration exposure: adds `ecosystem.config.cjs` (repository-defined PM2 process contract for both `noramedi-api` and `noramedi-worker`, closing R-040's "config source ambiguity"), extends `scripts/noramedi-deploy.sh` to reload/verify **both** processes via `pm2 startOrReload` and a new PM2-status-based worker liveness check that aborts the deploy (non-zero exit) if the worker does not reach `online` (closing R-033's "no deploy/reload automation for `noramedi-worker`"), and standardizes `RUN_BACKGROUND_JOBS=false` for the `noramedi-api` app inside that same ecosystem file — now safe because the worker's deploy lifecycle is defined in the same change, per this task's own instruction not to silently alter production job ownership otherwise. Adds a new, optional, validated `NORAMEDI_PROCESS_ROLE` env var (`server/src/utils/processRole.ts`) asserted at startup by both entrypoints, failing closed (throws) on an unrecognized or mismatched declared role while remaining fully backward compatible when unset (existing dev/test/pre-existing-deployment behavior unchanged). **R-033/R-034/R-040 rows below updated with this mitigation's evidence pointer; none moved to `CLOSED` or `MITIGATED`** — this task performed no production deployment and no production verification (`AGENT_COMPLETED`/`TESTS_PASSED`/`PR_OPENED`, `NOT_MERGED`/`NOT_DEPLOYED`/`NOT_PRODUCTION_VERIFIED`), consistent with this program's standing rule that repository-level fixes do not by themselves close a production-topology risk. No schema/migration change. Full evidence: [evidence/F3-IMPL-002_PRODUCTION_WORKER_PROCESS_CONTRACT.md](evidence/F3-IMPL-002_PRODUCTION_WORKER_PROCESS_CONTRACT.md).)

Prior update: 2026-07-28 (**F1-002-R2 — External R-072 Closure Recording (documentation-only, no new test/package-script/CI-workflow/application/schema/migration/deployment file changed).** Existing branch `docs/f1-002-test-inventory-refresh` (PR #253), no new PR opened, not merged. `origin/main` re-fetched at task start; the branch's own tip already contains `origin/main` as an ancestor — **no drift, no reconciliation merge required.** **External program decision:** the external architecture review (ChatGPT architecture review) accepted the full F1-002 / F1-002-P1 / F1-002-P2 evidence chain and, jointly with Mustafa Basol's merge decision, granted closure confirmation for R-072 — the one element the immediately-preceding F1-002-R1 entry below found still missing. **R-072 status updated below: `CLOSED`, closure date 2026-07-28, closure basis: F1-002 current test inventory and ownership reconciliation + F1-002-P1 script reachability evidence + F1-002-P2 runtime dependency evidence + 0 `UNKNOWN` canonical ownership + complete package-script reachability classification + current-`main` reconciliation completed, external confirmation authority: ChatGPT architecture review and Mustafa Basol (merge decision).** This closure remains narrow and explicitly does **not** resolve: the 8 unscripted backend test files; the 17 aggregate-chain-excluded test scripts; only 7 of 116 CI-reached JS/TS test-related files; disposable-Postgres provisioning; MinIO provisioning; port/database collision avoidance; the 1 runtime-dependency `UNKNOWN` (`messagesConsentGate.test.ts`); the unimplemented CI test-selection engine; or R-070 — all remain separate, open, unchanged risk/readiness items. **Exact next task assigned: F1-003 — Baseline CI Test Execution and Disposable Runtime Readiness, status `READY, not started` — not executed by this task.** This pass also corrects stale PR #253 metadata (the "16 scripted-but-not-in-full-suite-chain files" figure superseded by P1's reconciled 17; confirms P1/P2 are merged and integrated; states R-072 `CLOSED` by external confirmation; does not claim F1 completion or CI/runtime readiness). No test/package-script/CI-workflow/application/schema/migration/deployment file changed; F1-002 remains `AGENT_COMPLETED / PR_OPENED_AWAITING_REVIEW`, not merged; F1 remains `IN_PROGRESS` with its exit gate not satisfied; G1/G2 unchanged; R-046/R-071/KVKK-baseline-stable status unaffected.)

Prior update: 2026-07-28 (**F1-002-R1 — Parallel Evidence Integration and Main Reconciliation: R-072 assessment reaffirmed under `CLOSURE_PROPOSED_AWAITING_EXTERNAL_CONFIRMATION`, not `CLOSED` (documentation-only).** Two accepted parallel evidence subtasks, both externally reviewed and `MERGED` before this update — **F1-002-P1** (Test Script Reachability and Runner Reconciliation, PR #255, merge commit `954168ed4399f572e776fbfd30fa391aad574610`) and **F1-002-P2** (Test Runtime Dependency and Disposable Environment Baseline, PR #254, merge commit `035d4db59cf429777a4bb71a5e72203f6c99b571`) — were integrated into the F1-002 evidence set (`TEST_OWNERSHIP.md` §0.1, [F1-002_TEST_INVENTORY_REFRESH_AND_OWNERSHIP_RECONCILIATION.md](evidence/F1-002_TEST_INVENTORY_REFRESH_AND_OWNERSHIP_RECONCILIATION.md) §20) after the F1-002 branch (PR #253) was reconciled with `origin/main` via a conflict-free `--no-ff` merge. R-072 is, strictly, a test-**inventory evidence staleness** risk; assessed against that narrow scope, all 10 exit conditions from F1-002's own task specification remain met (now additionally corroborated by P1/P2's independently-derived script-reachability and runtime-dependency evidence), so the row below **remains** `CLOSURE_PROPOSED_AWAITING_EXTERNAL_CONFIRMATION` — not moved to `CLOSED` by this or any agent task. Two numeric inconsistencies discovered during integration were corrected in the underlying evidence documents (not in this row's own claims, which were already accurate): 17, not 16, `server:test`-chain-excluded scripts; 6/9, not 7/9, frontend files uncovered by the one existing CI workflow. **This update explicitly separates R-072's narrow closure from still-open implementation/readiness gaps that do not disappear with it**: 8 unscripted backend test files and 17 chain-excluded scripts; only 7 of 116 JS/TS test-related files reached by any CI workflow; no committed disposable-Postgres provisioning; no MinIO/S3-emulator provisioning for the one test that needs it; no port/database collision-avoidance mechanism for parallel test runs; 1 runtime-dependency `UNKNOWN` (`messagesConsentGate.test.ts`); the impact-based CI test-selection engine itself (F1-001's design) remains unimplemented. None of these is a new risk-register row as of this update — they are recorded as evidence-backed findings for a future task/risk-registration decision, not invented here. Full evidence: [F1-002-P1_TEST_SCRIPT_REACHABILITY_RECONCILIATION.md](evidence/F1-002-P1_TEST_SCRIPT_REACHABILITY_RECONCILIATION.md), [F1-002-P2_TEST_RUNTIME_DEPENDENCY_BASELINE.md](evidence/F1-002-P2_TEST_RUNTIME_DEPENDENCY_BASELINE.md). This update changed no test/package-script/CI-workflow/application/schema/migration/deployment file, touched no KVKK implementation branch or worktree, and did not close R-046, close R-071, alter G0/G1/G2 status, or declare the KVKK baseline stable.)

Prior update: 2026-07-28 (**F1-002 — Test Inventory Refresh and Ownership Reconciliation: R-072 remediated, closure proposed (documentation-only).** Isolated worktree `E:\Ek Gelir\Siteler\DisKlinikCRM-worktrees\f1-002-test-inventory-refresh`, branch `docs/f1-002-test-inventory-refresh`, execution baseline `origin/main` @ `08f2eaf82a205cf3f997c57e6a295fedd66b142d` (F1-001/PR #241's own merge commit `94cc4ac58f0487dd186886878c5628627f0b1ce3` confirmed an ancestor; 27 intervening commits, PR #242–#252, inspected and found to be ordinary forward-progression KVKK backup/retention/AI-privacy/schema-integrity work plus one unrelated revenue-report fix, none of it CI-workflow-affecting). Directly addresses R-072's own recorded remediation ("A refreshed, F0-005-equivalent test-inventory pass against the actual implementation task's own execution baseline"): every current backend (105) and frontend (9) test file was enumerated via exact `find` commands, plus bridge-agent (9, unchanged), windows-bridge (8 project/script targets, unchanged), and manual disposable-DB verify scripts (4, +1 new) — **137 test/verification targets total**, up from F0-005's 100 and F1-001's own intermediate 96+9=105 snapshot. **0 `UNKNOWN` canonical-ownership entries** across all 137 targets (34 new backend + 3 new frontend + 1 new manual script classified fresh via file-header/import inspection since CodeGraph remained unavailable in this task's environment too — same finding as F1-001; 100 carried forward from F0-005 with re-verified path existence, one file, `aiPrivacyBoundary.test.ts`, carried forward with confidence explicitly downgraded to `MEDIUM` since it was materially rewritten in the intervening PRs). Package scripts reconciled programmatically (not manually read): **0 stale/dead script references found**, 8 backend files identified with zero script of any kind (5 pre-existing since F0-005, 3 newly added without a script — recorded, not fixed, since adding a script is out of this task's prohibited scope), 16 backend files scripted but correctly outside the full-suite aggregate chain (8 by design, DB/external-service-dependent; 6 pre-existing orphans unchanged; 2 flagged as worth a future look). Full evidence: [F1-002_TEST_INVENTORY_REFRESH_AND_OWNERSHIP_RECONCILIATION.md](evidence/F1-002_TEST_INVENTORY_REFRESH_AND_OWNERSHIP_RECONCILIATION.md), [F1-002_test_inventory.json](evidence/F1-002_test_inventory.json), [F1-002_test_script_reconciliation.json](evidence/F1-002_test_script_reconciliation.json), [F1-002_test_ownership_gaps.json](evidence/F1-002_test_ownership_gaps.json). **R-072 status updated below to `CLOSURE_PROPOSED_AWAITING_EXTERNAL_CONFIRMATION`** — every exit condition in this task's own specification is met by the evidence above, but per this program's own established rule (see the R-071 precedent in this same file), an agent's own self-verification does not itself constitute risk acceptance; external review is required before `CLOSED`. This task changed no test/package-script/CI-workflow/application/schema/migration/deployment file, touched no KVKK implementation branch or worktree, and did not close R-046, close R-071, alter G0/G1/G2 status, or declare the KVKK baseline stable.)

Prior update: 2026-07-28 (**F1-001 — Impact-Based Test-Selection Design: new risk R-072 added (documentation-only; discovered, not created to fill activity).** During F1-001's evidence-gathering (targeted inspection of `server/src/tests/` and the frontend test directories, since CodeGraph was unavailable in this task's execution environment), the F0-005 test-inventory evidence backing `TEST_OWNERSHIP.md` was found stale relative to current `origin/main` (`d03116368e6c55cfa87ff1e35b95c485f7ff240d`) — 96 backend test files today versus 72 at the evidence's own 2026-07-19 baseline, and 9 frontend test files versus 6, including 2 new `vitest`-based component tests outside the evidence's `testType` vocabulary. **R-072 added below**, next available ID, not reusing or renumbering any existing row. This does not affect R-001…R-071's own status, does not close or reopen any existing risk, and does not touch any application/runtime/test/schema/migration/CI file — see [F1-001_IMPACT_TEST_SELECTION_DESIGN_EVIDENCE.md](evidence/F1-001_IMPACT_TEST_SELECTION_DESIGN_EVIDENCE.md) for the full process record.)

Prior update: 2026-07-25 (**F0-013-R1 — Risk-Register Status-Cell Consistency Correction (documentation-only correction pass, no technical evidence changed).** Continued on existing branch `docs/f0-013-consolidated-architecture-validation` (PR #228). **Correction 1:** the R-061 row's own status-column cell (below) began with an obsolete `OPEN` token even though that same cell's own final entry, and this file's 2026-07-24 top entry (immediately below), already recorded `R-061 status: CLOSED` — corrected the cell's leading token to `CLOSED`; the full historical narrative, including the prior `OPEN` wording, is preserved unchanged, not deleted. **Correction 2:** the R-071 row's status-column cell asserted `R-071 status is now CLOSED...` although that closure was self-proposed by the same task that performed its own verification, with no independent external (risk-owner/ChatGPT/user) confirmation recorded anywhere in this repository — corrected to `CLOSURE_PROPOSED_AWAITING_EXTERNAL_CONFIRMATION`, per this program's own rule that unilateral agent judgment is not risk acceptance. Neither correction changes underlying technical evidence, reopens R-061, closes R-071, touches R-046, declares the KVKK baseline stable, or approves any gate. Only this file, `NORAMEDI_MASTER_TRACKER.md`, `CURRENT_PHASE.md`, `phases/F0_BASELINE_AND_VALIDATION.md`, and `F0-013_CONSOLIDATED_ARCHITECTURE_VALIDATION_REPORT.md` changed by this pass. See the consolidated report's §7/§8/§9/§10 for the updated F0 exit-criteria verdict on this evidence item.)

Prior update: 2026-07-24 (**R-061 — Authenticated Production Safe-Reset Verification complete; R-061 `CLOSED`.** Isolated worktree `D:\Mustafa\Siteler\DisKlinikCRM-worktrees\r061-production-closure`, branch `docs/r061-production-closure-20260724`, base `origin/main` @ `db53f374f49b9381ab55347871c50d0479ba8b69` (includes PR #221, the audited PlatformAdmin password-recovery CLI) — no drift at task start. The authenticated-login blocker recorded by every prior R-061 pass (most recently `HTTP 401` in the 2026-07-23 residual-safe-reset entry below) is now resolved: an authorized operator recovered access to the existing production account `admin@noramedi.com` via the audited operator CLI added by PR #221 (`action: platform_admin.password_recovered`, `outcome: success`, `method: operator_cli`, `actorPlatformAdminId: null` — a system-operator action, since the recovered account cannot itself supply an authenticated-session actor —, `mfaPreserved: true`, `sessionsInvalidated: 0`, a documented limitation of `PlatformAdmin`'s stateless-JWT sessions, not a new gap). No password, token, cookie, or credential value is recorded anywhere. **Diagnostic correction (not a defect):** an initial preflight attempt against `GET /api/platform/privacy/legacy-consent-correction/settings` returned misleading `401 Unauthorized: Missing token` responses because no `GET` route exists on `/settings` — the correct read endpoint is `GET .../policy`. Cookie parsing, JWT validity/claims, and cookie-only (bearer-rejecting) session enforcement were all independently confirmed correct during this diagnosis. With a valid authenticated cookie session and CSRF protection, the full previously-blocked chain was then executed against the public production API: `GET policy` → `HTTP 200 {runtimeEnabled:false}`; `PATCH {runtimeEnabled:false}` → `HTTP 200 {runtimeEnabled:false}`; first `DELETE` → `HTTP 200 {runtimeEnabled:false, settingPresent:false, removed:true}`; second idempotent `DELETE` → `HTTP 200 {runtimeEnabled:false, settingPresent:false, removed:false}`; final `GET policy` → `HTTP 200 {runtimeEnabled:false}`. `runtimeEnabled=true` was never submitted at any point. Final `PlatformSetting` row count for this key: `0`. Two `PlatformAdminAuditEvent` rows were written, both attributed to the authenticated platform administrator (`platform_setting.updated`, previous/new both `false`; `platform_setting.reset`, previous `false`/new `null`, `safeMetadata.restoredDefaultState=true`); the idempotent second `DELETE` correctly wrote no additional audit row. All temporary `r061_*` scripts/session artifacts were removed from production (`session_directory_removed=true`, `remaining_r061_artifacts=false`, working tree clean). Final health: API `200`; `noramedi-api` `online`, unstable restarts `0`, restart count `20`. **This satisfies every remaining closure requirement named by the 2026-07-23 residual-safe-reset entry below** (default-deny confirmed, explicit-false persistence confirmed, safe reset confirmed, reset idempotency confirmed, audit attribution/atomicity confirmed, final state setting-row-absent/`runtimeEnabled=false` confirmed, `runtimeEnabled=true` never used). **R-061's explicit human decision is recorded as: reject the original no-kill-switch design; require and implement a runtime default-deny kill switch (already done, PR #186); verify it safely in production without enabling the workflow (now done, this entry). R-061 status: `CLOSED`.** This closure does **not** extend to: legal KVKK compliance approval, an overall "KVKK baseline stable" declaration, freeze-boundary condition 4 (independent rollback/tenant-impact verification — separate F0-011-P2 evidence chain, unaffected), R-049, R-062, or any other risk row; gaps 1–3 (real-patient behavioral verification) remain a separate, unauthorized, unchanged item; no production rollback rehearsal, no real legacy consent correction, and no patient/clinic data mutation occurred. This documentation task itself performed no production access and changed no application code, test, schema, migration, deployment script, package file, runtime setting, or configuration. Full evidence: [evidence/R061_AUTHENTICATED_PRODUCTION_SAFE_RESET_VERIFICATION.md](evidence/R061_AUTHENTICATED_PRODUCTION_SAFE_RESET_VERIFICATION.md).)

Prior update: 2026-07-23 (**R-061 Residual-Safe-Reset — Production Deployment and Verification: `PASS WITH BLOCKED AUTHENTICATED RESIDUAL`; R-061 remains `OPEN` (documentation-only reconciliation of operator-executed production evidence).** Isolated worktree `D:\wt\r061-prod-verify`, branch `audit/r061-residual-reset-production-verification`, base `origin/main` @ `8906e66af5169220a4aed48fe4cfea8524976fb8` — no drift at task start; this SHA is also the deployed production SHA recorded below. Implementation commit `b86001779fbbc2cfdcf76b84568d3d960850a761` (branch `feature/r061-residual-safe-closure`, merged before this task) adds `unsetPlatformSetting()` and `DELETE /api/platform/privacy/legacy-consent-correction/settings`, resolving the previously-documented absence of an unset/delete path for `privacy.legacyConsentCorrection.runtimeEnabled` (see the R-061 row's supersession note below — the historical "no unset/delete API exists" text is preserved, not deleted). An authorized operator deployed this SHA to production (`noramedi-api` restarted, PM2 `online`, `npm run typecheck` clean, repository clean) and independently verified: unauthenticated `GET` policy (local and public) and unauthenticated `DELETE` settings both return HTTP `401` `{"error":"Unauthorized: Missing token"}`; the setting row remains absent/`null` with zero audit rows both before and after a single authenticated login attempt. That single normal platform-admin login attempt returned HTTP `401` — per the existing stop condition, no second credential was attempted, no authenticated `GET`/`PATCH`/`DELETE` request was sent, and no setting/audit row was created. An initial external `502` occurred only during the restart/startup window (consistent with the pre-existing R-063 readiness gap) and is not classified as a deployment failure. **Verdict: `PASS WITH BLOCKED AUTHENTICATED RESIDUAL`** — deployment, API health, unauthenticated route protection, and the default-false/absent database invariant are all `PASS`; successful authenticated `PATCH`/`DELETE`/idempotency and production audit-write verification remain `BLOCKED / NOT EXECUTED` because the login attempt returned `401`; controlled activation and real-patient tests remain `NOT AUTHORIZED / NOT EXECUTED`. **R-061 remains `OPEN`.** No authenticated setting request was sent, no real patient identifier was used, and the feature was not activated. Full evidence: [evidence/R061_RESIDUAL_SAFE_RESET_PRODUCTION_VERIFICATION.md](evidence/R061_RESIDUAL_SAFE_RESET_PRODUCTION_VERIFICATION.md). Documentation-only reconciliation; this task itself performed no production access and changed no application code, test, schema, migration, deployment script, package file, or configuration.)

Prior update: 2026-07-23 (**R-061 Package A — Authenticated Production Verification `PASS`; Test C1/Test C3 closed; R-061 remains `OPEN`.** An authorized operator executed the already-authorized, non-activating Package A against production application SHA `1aa741d1dc1e1888b1dfdb9b911d0123b4eea1ab`. Platform-admin login returned HTTP `200`; Test C1 returned HTTP `200` with `{"runtimeEnabled":false}`; Test C3 returned HTTP `400` with `{"error":"runtimeEnabled must be a boolean"}`. Final before/after invariants were identical (`0||0|0|0|0|18|0|0`); patient-state digest was identical (`c91b3a90502dad4b92ee465c477190b6`); health remained HTTP `200`; API/worker restart counts remained `18`/`16`; temporary session material was cleaned. Package B was not executed. No real patient identifier was used, no valid-boolean setting PATCH was sent, no setting/audit row was created, and the feature was not activated. **R-061 remains `OPEN`** because gaps 1–3, gaps 4–8, and controlled activation remain separately gated and unauthorized. Full evidence: [evidence/R061_PACKAGE_A_AUTHENTICATED_PRODUCTION_VERIFICATION.md](evidence/R061_PACKAGE_A_AUTHENTICATED_PRODUCTION_VERIFICATION.md). Documentation-only reconciliation; this task itself performed no production access and changed no application code, test, schema, migration, deployment script, package file, or configuration.)

Son güncelleme: 2026-07-22 (**KVKK-HIGH-006 — Production Deployment and Smoke Verification; R-071 proposed `CLOSED` (documentation-only reconciliation of user-executed production evidence).** Isolated worktree `D:\Mustafa\Siteler\DisKlinikCRM-worktrees\kvkk-high006-production-verification-close-r071`, branch `docs/kvkk-high006-production-verification-close-r071`, base freshly-fetched `origin/main` @ `1aa741d1dc1e1888b1dfdb9b911d0123b4eea1ab` — no drift at task start. Production deployment executed by an authorized operator per [evidence/KVKK-HIGH-006-PRODUCTION_DEPLOYMENT_GATE.md](evidence/KVKK-HIGH-006-PRODUCTION_DEPLOYMENT_GATE.md) §5: pre-deploy SHA `85e3ffbca7ee1b53789564e16c5e58c5ec498cf2` → deployed SHA `1aa741d1dc1e1888b1dfdb9b911d0123b4eea1ab`, clean fast-forward pull, working tree clean before/after, Prisma v7.8.0 generated, 65 migration directories, `prisma migrate status`/`migrate deploy` clean with no pending migrations, only `noramedi-api` reloaded (worker correctly not reloaded), PM2/API/worker healthy, all health checks `200`/`{"status":"ok"}` (`app.noramedi.com` `302` → `/login` as expected), no new high-severity log error. **Production-safe authenticated smoke verification against a dedicated synthetic fixture set — the minimum mandatory set S1/S2/S3/S6/S7 all PASSED** (authorized sibling-clinic access, unauthorized cross-organization denial, message-template record scope including the `metaTemplateConnectionId`/`metaWabaIdSnapshot` non-exposure property, insurance all-authorized-clinics behavior, and an aggregate zero-leakage check). **S4 and S5 were NOT EXECUTED** (both optional, outside the minimum closure set — no confirmed mock WhatsApp provider for S4; minimum set already satisfied without S5) and are **not** claimed to have passed. All temporary synthetic fixtures cleaned up with verified zero post-cleanup row counts. Full evidence: [evidence/KVKK-HIGH-006-PRODUCTION_DEPLOYMENT_AND_SMOKE_VERIFICATION.md](evidence/KVKK-HIGH-006-PRODUCTION_DEPLOYMENT_AND_SMOKE_VERIFICATION.md). **R-071 updated below and proposed `CLOSED`** — final composite status `IMPLEMENTATION_MERGED — SOURCE_DB_AND_PRODUCTION_VERIFIED — COMPLETE`. **R-061 explicitly remains `OPEN`, unaffected** — a separate KVKK-HIGH-008 risk with its own, separate Package A closure criteria not touched by this evidence (see R-061 row below). No application code, test, Prisma schema, migration, package file, deployment script, or configuration file changed by this documentation task; no production access performed by this task itself — it records operator-supplied evidence only. Not merged.)

Prior update: 2026-07-22 (**KVKK-HIGH-006 — PR #205 reconciliation with merged PR #204 (documentation-only, no new PR opened).** Existing worktree/branch `docs/kvkk-high006-production-deployment-gate` (PR [#205](https://github.com/MustafaBasol/DisKlinikCRM/pull/205)) reconciled with newly-fetched `origin/main` @ `e84d60b7dfbba8986c424accae9699552b194189` — [PR #204](https://github.com/MustafaBasol/DisKlinikCRM/pull/204) independently confirmed `MERGED` and an ancestor of this tip, its one-file test-only diff confirmed to have zero overlap with this branch, reconciled via a normal `git merge` commit (no rebase, no force-push). **PR #204 replaced the brittle WABA-binding literal-string assertion with a resilient security-property assertion** — `messageTemplateWabaBinding.test.ts` now passes **20/20**, resolving the sole failure in PR #202's "549/550" combined post-merge result; no production code changed. **Migration-count discrepancy (65 vs. 66) reassessed and explained:** `server/prisma/migrations/` holds 65 migration directories plus one non-directory file, `migration_lock.toml`; a naive `ls | wc -l` (66) counts that lock file, a directories-only count (65) does not — a counting-surface artifact, not schema drift; production `prisma migrate status`/`migrate deploy` against `_prisma_migrations` remain authoritative. **Authoritative baseline now `e84d60b7dfbba8986c424accae9699552b194189`**, superseding `6b51f8d4520acc2e2cf56001e8a85d1737f16e1f`; PR ancestry/accounting extended from #194–#203 to **#194–#204** where applicable (see R-071/R-061 rows below). **R-071 and R-061 both remain `OPEN`**, unaffected in substance. **The production deployment gate is not blocked by this test-only cleanup** and deployment remains not executed and not authorized by this reconciliation. Only the same five documentation files changed. PR #205 updated/pushed — not merged.)

Prior update: 2026-07-22 (**KVKK-HIGH-006 — Post-DB-Verification Program Reconciliation and Production Deployment Gate (documentation-only).** Isolated worktree `D:\Mustafa\Siteler\DisKlinikCRM-worktrees\kvkk-high006-deployment-gate`, branch `docs/kvkk-high006-production-deployment-gate`, base freshly-fetched `origin/main` @ `6b51f8d4520acc2e2cf56001e8a85d1737f16e1f` (PR #203's own merge commit) — no drift at task start. **PR [#202](https://github.com/MustafaBasol/DisKlinikCRM/pull/202)** (combined post-merge independent source verification: 15 core checks pass, 549/550 assertions across 21 suites, the sole failure being the pre-existing known-stale `messageTemplateWabaBinding.test.ts` assertion, not a functional/security defect) and **PR [#203](https://github.com/MustafaBasol/DisKlinikCRM/pull/203)** (disposable-PostgreSQL DB-backed verification: all existing migrations applied cleanly via `prisma migrate deploy`, **63/63 DB-backed assertions passed**, direct DB side-effect verification on every mutation) are both independently confirmed merged and ancestors of current `origin/main`, both self-classified `PASS WITH CONDITIONS`, **no genuine runtime/security defect found by either**. This task independently recounted `server/prisma/migrations/` at this commit and found **65** migration directories, not the "66" PR #203's own evidence text states — an unresolved ±1 documentation discrepancy in that prior file, not a functional defect (see [evidence/KVKK-HIGH-006-PRODUCTION_DEPLOYMENT_GATE.md](evidence/KVKK-HIGH-006-PRODUCTION_DEPLOYMENT_GATE.md) §4.3). **R-071 updated below** — status is now `IMPLEMENTATION_MERGED — SOURCE_AND_DB_VERIFIED — PRODUCTION_VERIFICATION_PENDING` (operationally: `TECHNICALLY VERIFIED — PRODUCTION VERIFICATION PENDING`), remains `OPEN`, not `MITIGATED`/`CLOSED`. **R-061 unaffected in substance** (a separate KVKK-HIGH-008 risk), remains `OPEN` — this reconciliation explicitly records that KVKK-HIGH-006's DB verification does not close R-061, and that Package A's production execution should be coordinated with, not folded into, the upcoming KVKK-HIGH-006 deployment/smoke window (see R-061 row below and [evidence/KVKK-HIGH-006-PRODUCTION_DEPLOYMENT_GATE.md](evidence/KVKK-HIGH-006-PRODUCTION_DEPLOYMENT_GATE.md) §7). A new document, [evidence/KVKK-HIGH-006-PRODUCTION_DEPLOYMENT_GATE.md](evidence/KVKK-HIGH-006-PRODUCTION_DEPLOYMENT_GATE.md), defines the exact next production deployment sequence, a production-safe smoke matrix, and closure criteria for both R-071 and R-061 — neither risk is closed by this task itself. No production access, no deployment, no runtime/test/schema/migration file changed.)

Prior update: 2026-07-22 (**KVKK-HIGH-006 — Final Program Reconciliation, all implementation batches merged into `main` (documentation-only).** Isolated worktree `D:\Mustafa\Siteler\DisKlinikCRM-worktrees\kvkk-high006-final-program-reconciliation`, branch `docs/kvkk-high006-final-program-reconciliation`, base freshly-fetched `origin/main` @ `6ec61ba036849153382c965fe333af9a176a35e5` — no drift at task start. All seven KVKK-HIGH-006 PRs independently confirmed merged and ancestors of current `origin/main` (`git merge-base --is-ancestor`, exit `0` each): **PR [#194](https://github.com/MustafaBasol/DisKlinikCRM/pull/194)** (`7ee68ef73c203a1d239086c9f2c2e5090011e08a`, Batch 1), **PR [#195](https://github.com/MustafaBasol/DisKlinikCRM/pull/195)** (`b372406a191e4c557c58a1823cd441bd67f7ae27`, Batch 2), **PR [#196](https://github.com/MustafaBasol/DisKlinikCRM/pull/196)** (`47dea4d534aa8e464e186e448e51a31a31e61cf3`, Batch 3), **PR [#197](https://github.com/MustafaBasol/DisKlinikCRM/pull/197)** (`965b0288248175174174cfa0da25730974d5c03b`, R-061 authenticated verification package — "Package A" prepared, not executed against production), **PR [#198](https://github.com/MustafaBasol/DisKlinikCRM/pull/198)** (`4712870fbf9b1cc1dee582fb8981648c482014ad`, message read/send record-scope), **PR [#199](https://github.com/MustafaBasol/DisKlinikCRM/pull/199)** (`0a5be5e4e77cea864ea785451acb1d05f184bc9a`, Batch 4 target-clinic quota-scope), **PR [#200](https://github.com/MustafaBasol/DisKlinikCRM/pull/200)** (`6ec61ba036849153382c965fe333af9a176a35e5`, Batch 4 characterization/product-decision — current `origin/main`'s own tip). **R-071 updated below** — all 47 previously-classified occurrences plus the messages.ts record-scope defect and the target-clinic quota gap are now migrated to the centralized scope helpers; every merged batch's own evidence document reports source-level review plus local/disposable in-process simulated regression tests only (no live Postgres was available in any batch's task environment) — **no disposable-PostgreSQL-DB-backed or production test evidence exists for any of the six merged implementation PRs**. **R-071 remains `OPEN`**, status corrected from "implementation not started" to `IMPLEMENTATION_MERGED — VERIFICATION_PENDING`; not `MITIGATED`/`CLOSED`. **R-061 is unaffected by the implementation batches** (it is a KVKK-HIGH-008 risk) but is updated to reference PR #197's authenticated verification package — remains `OPEN`, Package A not yet executed against production. **Exact next task: combined post-merge independent verification of KVKK-HIGH-006 on current `origin/main`, followed by disposable PostgreSQL DB-backed integration verification.** Production deployment not authorized by this reconciliation. No runtime/test/schema/migration file changed.)

Prior update: 2026-07-21 (**KVKK-HIGH-008-F1-PBV-S1 — Safe Behavioral Production Verification Feasibility Review (documentation-only, `AGENT_COMPLETED`, evidence-only, not committed).** Isolated worktree/branch `docs/kvkk-high008-pbv-feasibility-evidence`, base freshly-fetched `origin/main` @ `548bcb7c81065fc71474fdff3632f4759528ffd3` (PR #191's own merge commit) — no drift at task start. **Self-reference-lag correction:** the entry immediately below records PR #191 as `OPEN`/`MERGEABLE`/`CLEAN`, not merged — `git log`/`git rev-parse origin/main` confirm PR #191's merge commit (`548bcb7c81065fc71474fdff3632f4759528ffd3`) is now the tip of `origin/main`; **KVKK-HIGH-006-S2 → `MERGED`**. Runtime implementation has not started; parent KVKK-HIGH-006 remains `STILL_OPEN`; R-071 remains `OPEN` (see updated R-071 row below). **This task performed a read-only repository call-path analysis of R-061's residual gaps 1-9** (no production access, no code change, no test run — see [evidence/KVKK-HIGH-008-F1-PBV-S1_SAFE_BEHAVIORAL_PRODUCTION_VERIFICATION_FEASIBILITY.md](evidence/KVKK-HIGH-008-F1-PBV-S1_SAFE_BEHAVIORAL_PRODUCTION_VERIFICATION_FEASIBILITY.md)). **Confirmed by source (`server/src/routes/communicationPreferences.ts:567,574`):** `loadScopedPatient()` executes before the runtime-gate check on the disabled mutation route, and the read/history routes never check the gate at all — so gaps 1-3 (disabled-route fail-closed behavior, exact response contract, read/history behavior, all live-observed) cannot be exercised with a nonexistent/synthetic patient identifier and require a real, in-scope production patient; this task does not authorize that. **Confirmed by source (`server/src/services/platformSettings.ts:11-34`, `server/src/routes/platformAdmin.ts:1091-1134`):** gaps 4-8 (successful `PlatformSetting` write, `PlatformAdminAuditEvent` creation, actor attribution, previous/new values, zero-`SecuritySignalEvent`, audit-content safety) are technically exercisable only via `PATCH /api/platform/privacy/legacy-consent-correction/settings` with `{ runtimeEnabled: false }` — but no `deletePlatformSetting`/unset function exists anywhere in the repository, so this PATCH would move production from "row absent" (current safe state) to "row present with value `false`" **permanently, with no supported audited path back** — **this PATCH remains `NOT AUTHORIZED`**, not because it is technically infeasible, but because it is not a reversible test by this program's own standard. Gap 9 (controlled activation, `runtimeEnabled: true`) remains **`CONTROLLED_ACTIVATION_NOT_AUTHORIZED`**, unchanged from the existing decision below. **R-061 remains `OPEN`** — not `MITIGATED`/`CLOSED`/`RESOLVED`/`PRODUCTION_VERIFIED`; wording below is unchanged in substance, only cross-referenced to this new feasibility evidence. No production connection, no production endpoint call, no production PATCH, no feature activation, and no production verification occurred. Documentation prepared but **not committed, not pushed, no PR opened** — this pass stops for external review per its own task instructions. Exact next task: **KVKK-HIGH-006-S3 — Batch 1 implementation**, now that PR #191 has merged; the KVKK-HIGH-008 production PATCH remains a separately deferred human decision, not made automatic by this task.)

**Merge reconciliation note (2026-07-21, PR #193):** the entry immediately below (**KVKK-HIGH-008-F1-PROD-BEHAVIORAL-SAFE-VERIFY**) ran in parallel, on an isolated worktree/branch based on an earlier `origin/main` baseline (the PR #187 merge commit) — it predates, and was not aware of, KVKK-HIGH-008-F1-PBV-S1/KVKK-HIGH-006-S1/S2/F0-011-P3 above. Unlike PBV-S1's read-only, no-production-access feasibility review, that entry supplies **actual, user-executed production behavioral evidence** for two of R-061's residual items: the unauthenticated-rejection check (independently verified `401` in production, log-confirmed) and all 10 before/after database invariants (zero deltas). Its authenticated read-only policy check and authenticated invalid-payload check are `BLOCKED / NOT EXECUTED` (a platform-admin login attempt returned `401 Invalid credentials`) — consistent with, and not contradicting, PBV-S1's own classification of the remaining authenticated/explicit-false-PATCH gaps as requiring a separate, explicit authorization decision. **R-061 remains `OPEN` under both entries; neither closes it.** This reconciliation does not change KVKK-HIGH-006-S3 as the next technical task named by PBV-S1 above.

Prior update: 2026-07-21 (**KVKK-HIGH-008-F1-PROD-BEHAVIORAL-SAFE-VERIFY — Non-Activating Behavioral Production Verification, partial (documentation-only; this task never received SSH access, credentials, cookies, tokens, or MFA codes).** All production commands were prepared by this task and executed exclusively by the user, who reported back redacted, aggregate-only results. **Verified (`VERIFIED_USER_SUPPLIED_PRODUCTION_EVIDENCE`):** the `PlatformSetting` row for `privacy.legacyConsentCorrection.runtimeEnabled` remained **absent** (effective `false`) both before and after all tests, with all 10 measured before/after database values (setting row/value, `PlatformAdminAuditEvent` totals, `PatientLegacyConsentCorrection` total, `Patient.smsOptOut` aggregate counts/digest, `SecuritySignalEvent` count) identical — zero deltas; an unauthenticated `PATCH` to the settings endpoint independently returned `401` in production (log-confirmed, `responseTime 2ms`); health/logs showed no blocker, no `5xx`, no restart-count change. **Blocked / not executed (credential rejection, not a finding):** the authenticated read-only policy check and authenticated invalid-payload check — the platform-admin login attempt returned `401 Invalid credentials`, and the user correctly stopped rather than retry with a workaround, per this task's own stop condition. **Test 4** (disabled mutation route via a real patient) remains **not safely executable in production** and stays excluded from this verification method — a synthetic patient would 404 before reaching the gate check and prove nothing, and a real patient was correctly not used. **Overall result: PARTIALLY VERIFIED**, not complete. **R-061 updated** (still `OPEN` — see its row below); R-046/R-062/R-070 unaffected by this pass. Full evidence: [evidence/KVKK-HIGH-008-F1_PRODUCTION_SAFE_BEHAVIORAL_VERIFICATION.md](evidence/KVKK-HIGH-008-F1_PRODUCTION_SAFE_BEHAVIORAL_VERIFICATION.md). No `PlatformSetting` write, no feature activation, no patient/consent mutation, no commit/push performed by this pass.)

Prior update: 2026-07-21 (**KVKK-HIGH-006-S2 — Raw `req.user.clinicId` Occurrence Classification and Remediation Plan (documentation-only, `AGENT_COMPLETED`; committed `d9176e1f2acb5625ce8d94017ab96bbe283be518`, pushed, PR [#191](https://github.com/MustafaBasol/DisKlinikCRM/pull/191) `OPEN`/`MERGEABLE`/`CLEAN`, not merged).** Isolated worktree/branch `docs/kvkk-high006-s2-occurrence-classification`, base `origin/main` @ `447ef6e72b2884ca932f813f85469c1e7133538d` (PR #190's own merge commit) — re-confirmed via fresh `git fetch` at task start, no drift. Full occurrence-by-occurrence classification of all 93 raw / 67 non-test `req.user.clinicId` matches across 17 non-test files (4 more files than previously named, all 4 resolving to false-positive/comment-only). **R-071 updated below** with the full reconciliation; **R-071 remains `OPEN`** — this is a classification/planning deliverable, zero of the 47 remediation-required occurrences have been fixed, and no remediation has been implemented. No other risk row's status changes. No runtime/test/schema/migration file changed; documentation committed and pushed, PR #191 open (not merged). KVKK baseline not declared stable. Full evidence: [evidence/KVKK-HIGH-006-S2_REQ_USER_CLINIC_ID_OCCURRENCE_CLASSIFICATION.md](evidence/KVKK-HIGH-006-S2_REQ_USER_CLINIC_ID_OCCURRENCE_CLASSIFICATION.md).)

Prior update: 2026-07-21 (**Docs-only reconciliation — KVKK-HIGH-006 risk row added, task-identity correction.** Isolated worktree/branch `fix/kvkk-high006-default-clinic-scope`, base `origin/main` @ `7174f3bb3518bca6b2640694ee2f9264c293318f` — no drift. Checked the full risk register and confirmed KVKK-HIGH-006 (inconsistent direct `req.user.clinicId` use) had no prior dedicated risk row — R-046/R-061/R-062/R-070 all belong to KVKK-HIGH-007/HIGH-008 (legacy consent correction), not KVKK-HIGH-006. Added **R-071** below, next available ID, not reusing or renumbering any existing row. A separate, narrower `defaultClinicId` review recorded as subordinate task KVKK-HIGH-006-S1 does not affect any risk row. No existing risk row's status changed by this reconciliation.)

Son güncelleme: 2026-07-20 (**KVKK-HIGH-008-F1-PROD-DOCS — Reconcile PR #186 Production Deployment, Effective-False Runtime Control, and Partial Behavioral Verification (documentation-only).** Isolated worktree/branch from freshly fetched `origin/main` @ `85e3ffbca7ee1b53789564e16c5e58c5ec498cf2` (PR #187's own merge commit) — no drift at task start; PR #186 (`1a82302861139441f07ff5c0209728d3e711728f`) and PR #187 (`85e3ffbca7ee1b53789564e16c5e58c5ec498cf2`) both independently confirmed ancestors (exit `0` each). `gh pr view 187` confirms `MERGED`, `mergedAt: 2026-07-20T18:49:13Z` — the entry immediately below, which still described PR #187 as pending reconciliation, is superseded on that point. **New production evidence, user-supplied and read-only (`VERIFIED_USER_SUPPLIED_PRODUCTION_EVIDENCE` — this task did not execute any production command itself):** production HEAD `85e3ffbca7ee1b53789564e16c5e58c5ec498cf2` includes both PR #186 and PR #187 (ancestor checks exit `0`); all three named migrations, including PR #186's `20260720180000_add_platform_admin_audit_event`, are confirmed `applied: true`/`rolledBack: false` (65 migrations, schema up to date); the `PlatformSetting` row for `privacy.legacyConsentCorrection.runtimeEnabled` is confirmed **absent**, so the setting is effectively `false` through the repository's missing-row default (recorded precisely as *persisted row absent; effective default false*); `PlatformAdminAuditEvent`/`PatientLegacyConsentCorrection` counts are both `0`; `noramedi-api`/`noramedi-worker` are `online`, 5/5 local `/api/health` checks returned `200`. **R-046 remains `OPEN`** — production deployment success does not close it; full production cross-tenant/audit verification is still missing. **R-061 updated** to: `OPEN — mitigation merged and deployed; audit migration applied and effective-default-false verified in production. Authenticated disabled-route behavior, authorized read/history behavior, successful platform-admin audit creation/attribution, and the controlled-activation decision remain unverified or unresolved.` **R-062 remains `MITIGATED`** for its migration-ordering component only — the audit migration's production application is now additionally confirmed; raw SQL idempotency is still not claimed. **R-070 remains `OPEN`** — this deployment used standard `prisma migrate deploy`, no physical rollback and no `_prisma_migrations` mutation occurred, so no new ledger drift was introduced by it, but the underlying tooling gap is unchanged. Full evidence: [evidence/KVKK-HIGH-008-F1_PRODUCTION_DEPLOYMENT_VERIFICATION.md](evidence/KVKK-HIGH-008-F1_PRODUCTION_DEPLOYMENT_VERIFICATION.md). No production access; no flag enabled in any persisted configuration; no backfill executed; no `PlatformAdminAuditEvent` row created; KVKK baseline not stable. **[Reconciled 2026-07-20, PR #189 — F0-011-P3 merge]:** [PR #189](https://github.com/MustafaBasol/DisKlinikCRM/pull/189) (F0-011-P3, branch `docs/kvkk-high008-production-deployment-evidence`), a parallel documentation-only pass off the same PR #187 base, independently captured the same production facts recorded above — see [evidence/F0-011-P3_KVKK_HIGH008_F1_PRODUCTION_DEPLOYMENT_EVIDENCE.md](evidence/F0-011-P3_KVKK_HIGH008_F1_PRODUCTION_DEPLOYMENT_EVIDENCE.md), kept as a distinct file rather than restated here. No risk row's status changes as a result of this reconciliation; R-046/R-061/R-070 remain `OPEN`, R-062 remains `MITIGATED`. PR #189 is the current docs-only reconciliation PR completing this merge against current `main`.)

Prior update: 2026-07-20 (**F0-011-P2 — Independent Rollback, Tenant-Impact, and Test Re-Verification.** Isolated worktree/branch from `origin/main` @ `4c439716d9d56a989a1beb177e426eaae65255dd` (this file's own prior baseline, PR #185's merge commit — no drift at task start). Independently re-executed (not reused from any prior report) 9 targeted/adjacent HIGH-007/HIGH-008 test suites against a disposable Postgres 16 container (193/193 assertions passing), a migration-forward rehearsal from the pre-HIGH-007 schema state, a two-scenario rollback rehearsal (empty additive tables: clean, reversible at the schema level, pre-existing data unaffected; populated tables: destructively demonstrated — a synthetic conflict-bucket row and a synthetic correction row were irrecoverably lost when the tables were dropped), and a 14-assertion synthetic multi-clinic tenant-isolation matrix (0 leakage, 0 unscoped queries found). **New finding beyond PMVR Phase B1's own evidence:** after a physical DDL rollback, `_prisma_migrations` bookkeeping does not self-reconcile, `prisma migrate resolve --rolled-back` refuses to act on a cleanly-applied migration (`P3012`), and the only path found to make `prisma migrate deploy` treat the migration as pending again is an unsupported raw `DELETE` against Prisma's own bookkeeping table — reinforcing the retain-and-cutback recommendation over physical schema rollback. Also independently verified (not merely inspected) that a pre-HIGH-007 application commit's Prisma Client operates correctly against a database retaining the additive tables. **R-061 remains `OPEN`** — PR #186 has implemented and merged the default-disabled runtime control into `main`, but it has not yet been deployed or production-verified. The remaining gate is production deployment, confirmation that `privacy.legacyConsentCorrection.runtimeEnabled` is present and `false`, verification of the additive `PlatformAdminAuditEvent` migration, and a separate controlled-activation decision. Full evidence: [evidence/F0-011-P2_KVKK_HIGH007_HIGH008_ROLLBACK_TENANT_VERIFICATION.md](evidence/F0-011-P2_KVKK_HIGH007_HIGH008_ROLLBACK_TENANT_VERIFICATION.md). No production access; no flag enabled in any persisted configuration; no backfill executed beyond the pre-existing `--report`-mode test fixture; KVKK-HIGH-006 not implemented.)

Prior update: 2026-07-20 (**KVKK-HIGH-008-F1 — second external-review correction pass (same branch/PR #186, merged).** A second, independent external review found that the immediately-preceding correction pass's audit-trail fix (item 2 in the entry below) was itself architecturally wrong: reusing `SecuritySignalEvent` — security-detection telemetry aggregated by `securityDetectionRules.ts` — for a platform-admin configuration change mixed two distinct domains and risked future detection rules misinterpreting routine admin changes as security signals. **Corrected:** added a dedicated, additive `PlatformAdminAuditEvent` model (migration `20260720180000_add_platform_admin_audit_event`, FK to `PlatformAdmin`, indexed by actor/time and action/time, create-only) and a narrow `platformAdminAudit.ts` service (`writePlatformAdminAuditEventInTx`); the PATCH route now writes the `PlatformSetting` update and the audit row in one Prisma transaction — proven atomic by a real FK-violation test (non-existent admin id forces the audit insert to fail, and the setting change rolls back with it), not a mock — and the `SecuritySignalEvent` write is removed entirely. `platformAdmin.test.ts` re-verified green (49/49) with updated tests covering exactly-one-row-per-toggle, correct previous/new values and actor attribution, no patient data/secrets, no success row on a rejected payload or an unauthorized caller, the atomic rollback, and zero `SecuritySignalEvent` rows for this action. Also merged current `origin/main` (PR #185, merge commit `4c439716d9d56a989a1beb177e426eaae65255dd`, documentation-only) into this branch, resolving documentation-only conflicts in this file manually — both sides' entries preserved below, none deleted. R-061 remains `OPEN`; PR #186 was subsequently merged into `main` as commit `1a82302861139441f07ff5c0209728d3e711728f`, but it is not yet deployed, and the setting's production presence/value remains unverified.)

Prior update: 2026-07-20 (**KVKK-HIGH-008-F1 — external-review correction pass (same branch/PR #186, not merged).** Corrected two issues an external review found in the first implementation pass: (1) the rollback/cutback claim — the first pass said a pre-gate application rollback was "a harmless no-op"; this was wrong from a control perspective, since a pre-gate commit doesn't read the new `PlatformSetting` and would silently re-expose the mutation route. Corrected wording and a required cutback statement are now in [KVKK_HIGH008_FREEZE_BOUNDARY.md](KVKK_HIGH008_FREEZE_BOUNDARY.md) §7.1. (2) The admin-toggle audit trail — `console.log` alone was not durable/queryable evidence for a platform-wide KVKK consent-control change. Reused the existing `SecuritySignalEvent` model (nullable `organizationId`, existing `actorPlatformAdminId` field, existing `recordSecuritySignal()` service) instead of a new table/migration — see updated R-061 row below. Both real-Postgres suites re-verified green after these changes (`legacyConsentCorrection.test.ts` 36/36, `platformAdmin.test.ts` 46/46, up from 34/34 and 44/44 with the new tests added by this pass). This pass also merged current `origin/main` (F0-012's PR #183, merge commit `88646b0440f1f9f373d40eebecf0a8bdd5f554d3`) into this branch, resolving documentation-only conflicts in this file manually — both sides' entries preserved below, none deleted. R-061 remains `OPEN`; still not merged, not deployed, setting's production presence/value still unverified. **[superseded by the entry above: item (2)'s `SecuritySignalEvent` reuse was itself replaced by a dedicated `PlatformAdminAuditEvent` model, since reusing security-detection telemetry for a config-change audit mixed two distinct domains]**)

Prior update: 2026-07-20 (**KVKK-HIGH-008-F1 — Legacy Consent Correction Runtime Activation Control (implementation).** In a dedicated worktree/branch (`fix/kvkk-high008-runtime-toggle`), added a `PlatformSetting`-backed runtime kill switch (`privacy.legacyConsentCorrection.runtimeEnabled`, default `false`, same reused pattern as `privacy.dataRetention.runtimeEnabled`) that gates the KVKK-HIGH-008 mutation route — see updated R-061 row below and [KVKK_HIGH008_FREEZE_BOUNDARY.md](KVKK_HIGH008_FREEZE_BOUNDARY.md) §7. **This is implementation only**: no migration added (none needed — `PlatformSetting` already exists), not yet merged into `main`, not yet deployed, and the setting's presence/value in production is unverified. R-061 remains `OPEN` — the production-runtime fact (workflow live with no flag) is unchanged until this merges, deploys, and is confirmed. No other risk row, application behavior, or production state was touched by this task.)

Prior update: 2026-07-20 (**KVKK-HIGH-008-PMVR-R2 — F0-012 Integration.** `origin/main` (F0-012/[PR #183](https://github.com/MustafaBasol/DisKlinikCRM/pull/183), merge commit `88646b0440f1f9f373d40eebecf0a8bdd5f554d3`) is merged here via `git merge --no-ff origin/main`, on top of the KVKK-HIGH-008-PMVR-R1 reconciliation (preserved verbatim as the first "Prior update" below). F0-012's own correction pass (preserved verbatim as the second "Prior update" below) had already removed the "active immediately after deployment" wording from R-061 and added PMVR Phase B1's rehearsal evidence to R-062, at a point before this branch's PR #184 evidence reconciliation — its `R-062 remains OPEN` framing is **superseded here**: R-062's migration-ordering component is `MITIGATED` (production order accepted as `VERIFIED_USER_SUPPLIED_PRODUCTION_EVIDENCE`, plus disposable schema-order safety independently verified; raw SQL idempotency not claimed). R-046 and R-061 remain `OPEN`. No risk row is closed by this integration; the KVKK baseline is not stable.)

Prior update: 2026-07-20 (**KVKK-HIGH-008-PMVR-R1 — PR #184 Production Evidence Reconciliation.** This branch's Phase B1 disposable-DB rehearsal evidence (next "Prior update" below) is reconciled here, via `git merge --no-ff origin/main`, with PR #184's `VERIFIED_USER_SUPPLIED_PRODUCTION_EVIDENCE` (read-only production command results supplied by the user, merged into `main` as `cf12b05`, following "Prior update" below). **Evidence disposition:** PR #184's production observations (migration applied 64/64, PM2 health/restart-count figures, flag states) are specific, internally consistent, read-only production command results supplied by the user — accepted as `VERIFIED_USER_SUPPLIED_PRODUCTION_EVIDENCE`, not downgraded to unsupported speculation. **PMVR did not itself execute production commands**: raw production command output is not committed to this repository as a standalone evidence artifact, so PMVR did not independently reproduce the production observation — this reconciliation's acceptance rests on the user-supplied evidence, not on independent repository re-verification. R-046, R-061, and R-062 below are updated to combine both passes' evidence; **none is closed** — production migration-application and application deployment are accepted as occurred on user-supplied evidence, feature reachability is confirmed, no automatic execution path exists, and explicit activation remains undefined pending a runtime toggle/control decision; full production cross-tenant negative verification and full production audit verification were not performed.)

Prior update: 2026-07-20 (**F0-012 external-review correction pass** (PR #183 correction commit): corrected R-061's "active immediately after deployment" wording — removed, replaced with a precise reachable-vs-automatic-execution distinction per KVKK-HIGH-008-PMVR Phase B1 findings ([PR #185](https://github.com/MustafaBasol/DisKlinikCRM/pull/185), author-reported, not yet independently re-verified): no startup/cron/job/worker-driven invocation path found, manual API/UI-invoked administrative workflow only. Added PMVR's disposable-PostgreSQL-16.14 migration-rehearsal evidence to R-062 (64/64 clean-db, 483 assertions/19 commands, cross-org/cross-clinic negative scenarios reproduced) — this reduces schema-ordering uncertainty but **does not close R-062**, which remains `OPEN` pending production `_prisma_migrations` evidence **(superseded by the PMVR-R2 entry above, once PR #184's production evidence is reconciled)**. Added the PMVR rollback data-loss distinction (schema-drop rollback is safe only while new tables remain empty). Integrated `origin/main`'s post-deployment production-evidence reconciliation pass (below) via `git merge --no-ff`. Neither risk closed or downgraded; both remain `OPEN`.)

Prior update: 2026-07-20 (**KVKK-HIGH-008-PMVR — Post-Merge Deployment Readiness and Production Migration Verification, Phase B1:** independent read-only repository review of PR #175/#180 plus disposable-Postgres (16.14) migration rehearsal across three scenarios (clean; pre-PR-175 upgrade with seeded data; pre-PR-180 upgrade with edge-case data, including a real reproduced cross-tenant write attempt) confirms both migrations are additive-only, non-destructive, and order-independent of each other at the schema level. Re-running `prisma migrate deploy` after both were applied was a safe no-op because Prisma recorded them as already applied in `_prisma_migrations` — this validates Prisma migration-engine replay behavior, not raw-SQL statement-level idempotence of the underlying `CREATE TABLE`/`CREATE TYPE` statements, which was not separately tested. Physical schema rollback (dropping the new tables) remains data-loss-free only while those tables are empty — see [KVKK-HIGH-008-PMVR evidence §11](evidence/KVKK-HIGH-008-PMVR_POST_MERGE_READINESS_EVIDENCE.md). 483 independently-executed test assertions across 19 suites, 0 failures attributable to KVKK-HIGH-008/#175/#180 (one unrelated pre-existing failure in the KVKK-HIGH-004 `clinicBulkExport.test.ts` suite was observed and is out of scope). **R-046 and R-062: evidence strengthened, status remains `OPEN`** — production-application status of PR #175's and PR #180's migrations is still unverified; this task performed no production access (Phase B1 is repository/disposable-DB only). **R-061: evidence strengthened, status remains `OPEN`** — no feature flag/kill switch exists in `main` today; this task identified a ready-to-implement, no-migration-required mitigation (a `PlatformSetting`-backed runtime toggle mirroring the existing `privacy.dataRetention.runtimeEnabled` pattern at `server/src/routes/platformAdmin.ts:1050-1068`), recommended as an isolated follow-up PR, not implemented in this audit branch. Full detail: [evidence/KVKK-HIGH-008-PMVR_POST_MERGE_READINESS_EVIDENCE.md](evidence/KVKK-HIGH-008-PMVR_POST_MERGE_READINESS_EVIDENCE.md). No risk row is closed by this update — production migration-applied/deployed/feature-activated/production-verified status for KVKK-HIGH-007/-008 remain unconfirmed.)

Prior update: 2026-07-20 (**Post-deployment production-evidence reconciliation pass** (PR #184, merged into `main` as `cf12b05e9a65730e39b0aa667469e312fade5cc8`). KVKK-HIGH-008 is now confirmed deployed to production and technically verified (migration applied — 64/64, table/enum present with 0 rows, all `COMMUNICATION_CONSENT_*` flags still disabled, API/worker healthy) — see [KVKK_HIGH008_FREEZE_BOUNDARY.md](KVKK_HIGH008_FREEZE_BOUNDARY.md) §6. R-061 (no feature flag/kill switch) remains `OPEN` — the workflow is now live in production with no human accept/reject decision recorded. R-062 (stacked migration ordering) evidence updated — both KVKK-HIGH-007's and KVKK-HIGH-008's migrations are now confirmed applied with a clean `prisma migrate status`, resolving the ordering-uncertainty component, but rollback/tenant-impact verification still has not been performed; **not closed**. Seven new risks added (R-063…R-069) from production-log/dependency-review findings surfaced during this pass — a transient PM2-reload readiness gap, frontend dependency vulnerabilities, React `act()` warnings, WhatsApp AI JSON-parsing robustness, a `pg` client concurrency deprecation warning, historical `node-cron` missed-execution/observability gaps, and missing approved Meta WhatsApp reminder templates. Each is scoped to its own isolated remediation PR — none are combined. This pass does not declare the KVKK baseline stable and made no application/schema/migration/runtime/config change.)

Prior update: 2026-07-20 (**F0-012 — Controlled Pilot and General Launch Gate Definition:** R-046, R-061, R-062 gözden geçirildi ve [LAUNCH_GATES.md](LAUNCH_GATES.md)'in ilgili G1 bölümlerine çapraz referanslandı — üçü de G1 (Controlled Pilot Ready) için açık **blocker** olarak kaydedildi, hiçbiri kapatılmadı/`MITIGATED`'a taşınmadı. R-046'nın kendi metnindeki bayat "PR #175 açık" ifadesi düzeltildi (PR #175 artık `MERGED`, commit `1da9586995b625624b7385c14e70ba6a322def73` — risk kendisi değişmedi, yalnızca depo-durumu ifadesi). Hiçbir risk kapatılmadı; hiçbir yeni risk eklenmedi (mevcut R-024/R-029…R-032/R-046/R-061/R-062 zaten launch-gate kanıt gereksinimlerinin kapsamını karşılıyor, yinelenen kayıt oluşturulmadı).

Prior update: 2026-07-19 (**F0-011-P1 post-merge conflict resolution:** [PR #180](https://github.com/MustafaBasol/DisKlinikCRM/pull/180) (KVKK-HIGH-008, R-061/R-062) and [PR #182](https://github.com/MustafaBasol/DisKlinikCRM/pull/182) (F0-011, R-029/R-030/R-031/R-032 evidence pointers) have both since merged into `main`. R-061 ("no feature flag/kill switch") and R-062 ("stacked KVKK-consent-adjacent migration") wording updated below from merge-blocked framing to **post-merge residual risk**: the workflow is merged without production migration application or deployment/activation verification; the no-feature-flag exposure and migration-ordering-vs-PR #175 concern are now properties of code sitting in `main` awaiting deployment, not of an open PR. Neither risk is closed or downgraded by the merge alone — see rows below. No R-0xx row was deleted merely because its associated PR merged. Combines two independent same-day updates, both preserved below as "Prior update".)

Prior update: 2026-07-19 (F0-011-P1 — Active KVKK-HIGH-008 Work Baseline: R-061/R-062 eklendi (no feature flag/kill switch; stacked-migration risk relative to KVKK-HIGH-007's unconfirmed production-migration status) — bkz. [evidence/F0-011-P1_KVKK_HIGH008_ACTIVE_WORK_BASELINE.md](evidence/F0-011-P1_KVKK_HIGH008_ACTIVE_WORK_BASELINE.md). Önceki tur: F0-010 — Queue and Transactional Outbox PoC Design: R-056…R-060 eklendi (bkz. queue-outbox-poc-design.md, f0-010-async-flow-inventory.json); bu görev, kendi PR'ının base'i güncellenirken paralel F0-009-S1 görevini `origin/main`den normal, force-olmayan bir `git merge` ile aldı — bkz. bir alt satır. Önceki tur: F0-009-S1 — SecurityIncident raw-SQL tenant-ownership doğrulaması: R-054 `UNVERIFIED` → `MITIGATED` [secure-as-is, tam call-path kanıtı + 3 regresyon testi], bkz. [F0-009-S1 evidence](evidence/F0-009-S1_SECURITY_INCIDENT_TENANT_OWNERSHIP_EVIDENCE.md). Daha önceki tur: F0-009 — Tenant/RLS/PgBouncer PoC tasarımı: R-054/R-055 eklendi; R-001/R-008 "Mevcut kontrol" alanları depo kanıtıyla güncellendi (bkz. tenant-rls-pgbouncer-poc-design.md). Daha önceki tur: F0-007 düzeltme turu — R-053 eklendi; R-041/R-046 metni "commit edilmemiş migration" ifadesinin zaman-dilimli olarak düzeltilmesini yansıtacak şekilde güncellendi. Daha önceki tur: F0-007 — KVKK aktif çalışma taban çizgisi kanıtından R-041…R-052 eklendi)

Prior update: 2026-07-19 (F0-011 — Object Storage and Backup Migration Design: R-029/R-030/R-031/R-032'nin "Azaltım" ve "Kanıt" alanları, artık tamamlanmış F0-011 tasarım dokümanına/kanıt dosyalarına işaret edecek şekilde güncellendi; risklerin kendisi **çözülmedi**, durumları `OPEN` kalır — bkz. object-storage-backup-migration-design.md, f0-011-storage-flow-inventory.json, f0-011-backup-restore-gap-matrix.json. Önceki tur: F0-010 — Queue and Transactional Outbox PoC Design: R-056…R-060 eklendi (bkz. queue-outbox-poc-design.md, f0-010-async-flow-inventory.json); bu görev, kendi PR'ının base'i güncellenirken paralel F0-009-S1 görevini `origin/main`den normal, force-olmayan bir `git merge` ile aldı — bkz. bir alt satır. Önceki tur: F0-009-S1 — SecurityIncident raw-SQL tenant-ownership doğrulaması: R-054 `UNVERIFIED` → `MITIGATED` [secure-as-is, tam call-path kanıtı + 3 regresyon testi], bkz. [F0-009-S1 evidence](evidence/F0-009-S1_SECURITY_INCIDENT_TENANT_OWNERSHIP_EVIDENCE.md). Daha önceki tur: F0-009 — Tenant/RLS/PgBouncer PoC tasarımı: R-054/R-055 eklendi; R-001/R-008 "Mevcut kontrol" alanları depo kanıtıyla güncellendi (bkz. tenant-rls-pgbouncer-poc-design.md). Daha önceki tur: F0-007 düzeltme turu — R-053 eklendi; R-041/R-046 metni "commit edilmemiş migration" ifadesinin zaman-dilimli olarak düzeltilmesini yansıtacak şekilde güncellendi. Daha önceki tur: F0-007 — KVKK aktif çalışma taban çizgisi kanıtından R-041…R-052 eklendi)

> Olasılık/Etki ölçeği: Low / Medium / High. "Mevcut kontrol" alanı depo kanıtıyla doğrulanmadıkça `UNVERIFIED` yazılır — kontrolün var olduğu **iddia edilmez**. Durum: `OPEN` (aktif risk) / `MITIGATED` / `ACCEPTED` / `CLOSED`.

| ID | Risk | Olasılık | Etki | Mevcut kontrol | Eksik kontrol | Azaltım (mitigation) | Sahip/Domain | Faz | Durum | Kanıt |
|---|---|---|---|---|---|---|---|---|---|---|
| R-072 | **(Added 2026-07-28, F1-001 — discovered during impact-based test-selection design, next available ID, not reusing/renumbering any existing row. Remediated 2026-07-28, F1-002 — see below.)** `TEST_OWNERSHIP.md`/F0-005's test inventory evidence (`evidence/F0-005_test_inventory.json`, pinned to commit `7fcf2f850f151241266f07349c4bf4442c72bbca`, 2026-07-19) is stale relative to current repository `HEAD`: `server/src/tests/` now has 96 files versus the evidence's 72 (a 24-file/7-day drift), and the frontend now has 9 test files versus 6, including 2 new `*.vitest.test.tsx` component tests using a test runner/pattern (`vitest`) not represented in the evidence's `testType` vocabulary at all. If a future task implements the F1-001 impact-based test-selection design against this evidence without first refreshing it, the selector's test-to-domain mapping for any file added since 2026-07-19 would fall through to `UNKNOWN`-confidence handling (which the design's own fail-safe rules force to full-suite execution — see [F1-001_IMPACT_BASED_TEST_SELECTION_ARCHITECTURE.md §6](../architecture/F1-001_IMPACT_BASED_TEST_SELECTION_ARCHITECTURE.md)), so the immediate safety impact is bounded by design; the risk is coverage-planning inaccuracy and eventual selector performance/precision degradation, not a silent test-skip. **F1-002 remediation (2026-07-28):** a fresh, F0-005-equivalent test inventory was generated against execution baseline `08f2eaf82a205cf3f997c57e6a295fedd66b142d` — 137 test/verification targets (105 backend + 9 frontend + 22 other), 0 `UNKNOWN` ownership, full package-script reconciliation. See [F1-002_test_inventory.json](evidence/F1-002_test_inventory.json), [F1-002_TEST_INVENTORY_REFRESH_AND_OWNERSHIP_RECONCILIATION.md](evidence/F1-002_TEST_INVENTORY_REFRESH_AND_OWNERSHIP_RECONCILIATION.md). **F1-002-R1 integration (2026-07-28):** F1-002-P1 (script reachability, 116 JS/TS test-related files, 17 chain-excluded scripts, 8 UNSCRIPTED, only 7/116 CI-reached) and F1-002-P2 (runtime dependency baseline, 22 disposable-Postgres files + 1 MinIO-dependent, 1 UNKNOWN, 5-gap provisioning assessment) independently corroborate this inventory's completeness and add the runtime-requirements/script-reachability depth R-072's own exit conditions require; see [F1-002-P1_TEST_SCRIPT_REACHABILITY_RECONCILIATION.md](evidence/F1-002-P1_TEST_SCRIPT_REACHABILITY_RECONCILIATION.md), [F1-002-P2_TEST_RUNTIME_DEPENDENCY_BASELINE.md](evidence/F1-002-P2_TEST_RUNTIME_DEPENDENCY_BASELINE.md). **This closure is narrow**: it addresses inventory/ownership/script-reachability/runtime-classification staleness only — it does not resolve unscripted tests, chain-excluded scripts, lack of broad CI execution, disposable-Postgres/MinIO provisioning, collision avoidance, the 1 runtime UNKNOWN, or the unimplemented CI test-selection engine, all of which remain separate, open, evidence-recorded gaps. **[Updated 2026-07-28, F1-002-R2 — external R-072 closure recording]:** the external architecture review (ChatGPT architecture review) accepted the full F1-002 / F1-002-P1 / F1-002-P2 evidence chain — 0 `UNKNOWN` canonical ownership, complete package-script reachability classification, and current-`main` reconciliation completed — and granted closure confirmation for R-072, jointly with Mustafa Basol's merge decision. **R-072 status: `CLOSED`, closure date 2026-07-28, closure basis: F1-002 test inventory/ownership reconciliation + F1-002-P1 script reachability evidence + F1-002-P2 runtime dependency evidence, external confirmation authority: ChatGPT architecture review and Mustafa Basol (merge decision).** This closure remains narrow and does **not** resolve: the 8 unscripted backend test files; the 17 aggregate-chain-excluded test scripts; only 7 of 116 CI-reached JS/TS test-related files; disposable-Postgres provisioning; MinIO provisioning; port/database collision avoidance; the 1 runtime-dependency `UNKNOWN` (`messagesConsentGate.test.ts`); the unimplemented CI test-selection engine; or R-070 — all remain separate, open, unresolved items requiring their own future task/risk decisions. Exact next task assigned: **F1-003 — Baseline CI Test Execution and Disposable Runtime Readiness**, status `READY, not started`. | Low | Medium | `VERIFIED_REPOSITORY` (F1-002's own targeted filesystem enumeration + programmatic package-script parsing against execution baseline `08f2eaf82a205cf3f997c57e6a295fedd66b142d`, CodeGraph again unavailable — see [evidence/F1-002_TEST_INVENTORY_REFRESH_AND_OWNERSHIP_RECONCILIATION.md §5-6](F1-002_TEST_INVENTORY_REFRESH_AND_OWNERSHIP_RECONCILIATION.md); independently corroborated by F1-002-P1/P2, both `VERIFIED_REPOSITORY`, `MERGED`) | None remaining for the literal staleness gap this risk describes — the evidence is now current as of 08f2eaf/`origin/main`@`035d4db5`. A recurring residual: this evidence will itself go stale again as future PRs add test files; F1-001's own `confidenceModel.stalenessThreshold` (30 days / 50 merged PRs) is the design's answer to that recurrence, not a one-time fix. Separately (not part of this risk's own closure criteria): disposable-Postgres/MinIO provisioning, collision avoidance, and CI command coverage remain unimplemented — see F1-002-P2. | This F1-002 refresh IS the named remediation F1-001 itself specified, now corroborated by F1-002-P1/P2. A future CI-implementation task should still re-verify currency against its own execution baseline before going live, per F1-001 §7 item 1 and the staleness threshold above, and must separately address the provisioning/CI-coverage gaps named above (tracked as their own future task/risk decision, not by this row). | CI/Test Architecture | F1 | `CLOSED` — **(2026-07-28, F1-002-R2)** externally confirmed by the ChatGPT architecture review and Mustafa Basol's merge decision, on the accepted F1-002/F1-002-P1/F1-002-P2 evidence chain (0 `UNKNOWN` canonical ownership, complete package-script reachability classification, current-`main` reconciliation completed). Superseded: the prior `CLOSURE_PROPOSED_AWAITING_EXTERNAL_CONFIRMATION` token below is preserved as history, not deleted — external confirmation, previously the only missing element, is now recorded. This closure is narrow and does not resolve the 8 unscripted tests, 17 aggregate-chain-excluded scripts, the 7/116 CI-reached-test gap, disposable-Postgres/MinIO provisioning, collision avoidance, the 1 runtime `UNKNOWN`, the unimplemented CI selector, or R-070 — all remain open. Next task: **F1-003 — Baseline CI Test Execution and Disposable Runtime Readiness**, `READY, not started`. | [F1-002_TEST_INVENTORY_REFRESH_AND_OWNERSHIP_RECONCILIATION.md](evidence/F1-002_TEST_INVENTORY_REFRESH_AND_OWNERSHIP_RECONCILIATION.md), [F1-002_test_inventory.json](evidence/F1-002_test_inventory.json), [F1-002_test_ownership_gaps.json](evidence/F1-002_test_ownership_gaps.json) `r072ClosureAssessment`, [F1-002-P1_TEST_SCRIPT_REACHABILITY_RECONCILIATION.md](evidence/F1-002-P1_TEST_SCRIPT_REACHABILITY_RECONCILIATION.md), [F1-002-P2_TEST_RUNTIME_DEPENDENCY_BASELINE.md](evidence/F1-002-P2_TEST_RUNTIME_DEPENDENCY_BASELINE.md); historical: [F1-001_IMPACT_BASED_TEST_SELECTION_ARCHITECTURE.md §0, §7](../architecture/F1-001_IMPACT_BASED_TEST_SELECTION_ARCHITECTURE.md) |
| R-073 | **(Added 2026-08-11, F3-SEC-EXIT-001; remediated 2026-08-11, F3-SEC-002 — next available ID, not reusing/renumbering any existing row.)** `authenticatePlatformAdmin` (`server/src/middleware/platformAuth.ts`) verified only Platform Admin JWT signature/expiry/type/`jti` — unlike clinic-user auth (`middleware/auth.ts`), it performed no persistent DB-backed admin lookup and never checked `isActive` or any credential-change checkpoint. A previously issued, still-unexpired (up to 8h) Platform Admin JWT therefore remained fully usable after a password reset, an admin account being deactivated, or the admin row being deleted — the highest-privilege identity in the system had no server-side revocation mechanism at all. The emergency recovery CLI (`platform-admin-recover-password.ts`) already documented this exact gap in its own `printResult()` output ("this CLI cannot revoke tokens already issued to this account… proposed follow-up: passwordChangedAt-based invalidation, mirroring the clinic User model") — this risk and its remediation are that proposal, implemented. | Medium | Critical | `VERIFIED_REPOSITORY` (pre-change source read of `platformAuth.ts` confirmed no DB lookup existed; confirmed by CodeGraph-assisted targeted exploration of `platformAuth.ts`/`routes/platformAdmin.ts`/`platform-admin-recover-password.ts`/`schema.prisma`/existing platform-admin auth tests) | None remaining for the specific gap this risk describes, pending independent/external confirmation of the remediation below (not a code gap — a program-process gap: this risk cannot be self-closed by the same task that implemented its fix). | **F3-SEC-002 (2026-08-11):** additive, nullable `PlatformAdmin.passwordChangedAt DateTime?` column (migration `20260811120000_add_platform_admin_password_changed_at`, mirrors `User.passwordChangedAt` exactly); `authenticatePlatformAdmin` now performs a persistent, uncached, fail-closed DB lookup every request — rejects a missing/inactive admin, a missing/non-numeric `iat`, or an `iat` before `passwordChangedAt`, all via the same generic 401 (no state leaked); the emergency recovery CLI now sets `passwordChangedAt` in the same transaction as the password-hash update, so a reset genuinely revokes every previously issued token, not just future logins. MFA disable deliberately does **not** invalidate sessions (re-proof of password+TOTP already required; caller already holds a live session) — a recorded threat-model decision, not an oversight. No normal self-service Platform Admin password-change route exists in this codebase; nothing else to wire. Full detail: [evidence/F3-SEC-002_PLATFORM_ADMIN_SESSION_REVOCATION.md](evidence/F3-SEC-002_PLATFORM_ADMIN_SESSION_REVOCATION.md). | Security/Platform-Admin Auth | F3 | `CLOSURE_PROPOSED_AWAITING_EXTERNAL_CONFIRMATION` — implementation complete, DB-backed test evidence complete (12 new dedicated tests + updated existing suites, both `test:runtime:postgres`/`test:runtime:postgres-compat` orchestrator runs `outcome.exitCode: 0`), **not self-marked `CLOSED`** per this program's own established rule (see R-071/R-072's own closure history) that unilateral agent judgment on a risk it just remediated is not risk acceptance — external/independent confirmation and a merge decision remain outstanding. **[Updated 2026-08-12, F3-PROD-001, user-supplied production deployment evidence]:** the F3-SEC-002 merge commit (`6f7e580d1bab2a0f87baed1cfe5ec0a944a6b711`) is now deployed to production (previous SHA `b21cae911a0aa3444ebcd6e714a92c4f0802608a` → deployed SHA `f0ff4c70abe08b192233d17cdcd1b7a77dbe4b08`); migration `20260811120000_add_platform_admin_password_changed_at` confirmed applied (73 migrations, schema up to date); normal Platform Admin authentication independently confirmed still working post-deploy — this resolves the row's own previously-named "no production migration-application evidence" gap. **The destructive behavioral proof this risk's full closure would still require — an actual credential-change cycle demonstrating a previously issued token is rejected in production — was deliberately NOT performed** (a scoping choice, not a gap in this evidence pass). Status unchanged: still `CLOSURE_PROPOSED_AWAITING_EXTERNAL_CONFIRMATION`, not self-closed — see [evidence/F3-PROD-001_PRODUCTION_DEPLOYMENT_AND_VERIFICATION.md](evidence/F3-PROD-001_PRODUCTION_DEPLOYMENT_AND_VERIFICATION.md) §6/§9 for the two-path closure template prepared for the decision owner. | [evidence/F3-SEC-002_PLATFORM_ADMIN_SESSION_REVOCATION.md](evidence/F3-SEC-002_PLATFORM_ADMIN_SESSION_REVOCATION.md), [evidence/F3-PROD-001_PRODUCTION_DEPLOYMENT_AND_VERIFICATION.md](evidence/F3-PROD-001_PRODUCTION_DEPLOYMENT_AND_VERIFICATION.md) |
| R-001 | Cross-tenant veri sızıntısı | Medium | High | `PARTIALLY_VERIFIED` (F0-009: `clinicScope.ts`/`clinicAccess.ts` uygulama-katmanı scope kontrolleri ~40+ çağrı noktasında doğrulandı — manuel enjeksiyon, çerçeve-zorunlu değil; 13+ mevcut test dosyası cross-tenant/cross-clinic red senaryolarını kanıtlıyor, ancak 6 dosya hiçbir `package.json` script'ine bağlı değil — bkz. TEST_OWNERSHIP.md) | RLS, Prisma guard, otomatik izolasyon regresyonu (henüz uygulanmadı — yalnızca PoC tasarımı var) | Defense-in-depth: RLS + scope + izolasyon test matrisi (F5); PoC tasarımı F0-009 tarafından tamamlandı | Tenant Security | F5 | OPEN | [tenant-rls-pgbouncer-poc-design.md §2.3, §2.8](../architecture/tenant-rls-pgbouncer-poc-design.md) |
| R-002 | Mimari çalışma sırasında KVKK regresyonu | Medium | High | Mimari dondurma sınırı (tracker §8) | KVKK regresyon test paketi | KVKK baseline teyidine kadar fiziksel değişiklik yasağı; F0-007 sınır dokümanı | Compliance | F0 | OPEN | Tracker §8 |
| R-003 | Tek sunucu arızası | Medium | High | `UNVERIFIED` | Çok-instance, failover | F7 HA çalışması | Operations | F7 | OPEN | `UNVERIFIED` |
| R-004 | Tek veritabanı arızası | Medium | High | `UNVERIFIED` | Replica, failover, PITR | ADR-013; F4/F11 | Operations | F4/F11 | OPEN | `UNVERIFIED` |
| R-005 | Yerel disk tükenmesi | Medium | High | `UNVERIFIED` | Object storage, disk alarmları | F4 object storage; ADR-012 alarmlar | Storage | F4 | OPEN | `UNVERIFIED` |
| R-006 | Yedekleme hatası | Medium | High | `UNVERIFIED` | Yedek doğrulama, alarm | ADR-013; otomatik yedek doğrulama | Operations | F4 | OPEN | `UNVERIFIED` |
| R-007 | Restore hatası (yedek geri dönmüyor) | Medium | High | `UNVERIFIED` (restore testi kanıtı yok) | Düzenli restore provası | ADR-013; periyodik restore testi zorunluluğu | Operations | F4 | OPEN | `UNVERIFIED` |
| R-008 | DB bağlantı tükenmesi (connection exhaustion) | Medium | High | `VERIFIED_REPOSITORY` (F0-009: `DB_POOL_MAX` varsayılan 10, `server/src/db.ts:9-21`; 2 süreç × 10 = 20 potansiyel bağlantı + `utils/activity.ts`'nin kendi ayrı, `db.ts` singleton'ını kullanmayan canlı `Pool`'u; PgBouncer varlığı production'da `UNVERIFIED` — repository-çapında sıfır kod bulgusu) | PgBouncer, bağlantı bütçeleri | ADR-004; F0-009 PoC tasarımı tamamlandı (uygulama değil) | Database | F5 | OPEN | [tenant-rls-pgbouncer-poc-design.md §2.7, §8](../architecture/tenant-rls-pgbouncer-poc-design.md) |
| R-009 | Kuyruk birikmesi (queue backlog) | Medium | Medium | `UNVERIFIED` | Backlog metrikleri, ölçekleme | ADR-007; F6 gözlemlenebilirlik | Queue Infrastructure | F6 | OPEN | `UNVERIFIED` |
| R-010 | Kaybolan harici event'ler | Medium | High | `UNVERIFIED` | Transactional outbox | ADR-006; F6 | Domain Events | F6 | OPEN | `UNVERIFIED` |
| R-011 | Mükerrer webhook işleme | Medium | Medium | `UNVERIFIED` | Idempotency anahtarları | F6 idempotency standardı | Messaging | F6 | OPEN | `UNVERIFIED` |
| R-012 | Sağlayıcı kesintisi (WhatsApp/SMS/AI vb.) | High | Medium | `UNVERIFIED` (kısmi fallback mekanizmaları olabilir) | Devre kesici, fallback, kuyruklama | F6 retry/fallback; ADR-009 | Messaging / AI | F6/F8 | OPEN | `UNVERIFIED` |
| R-013 | AI veri yerleşimi (data residency) ihlali | Medium | High | `UNVERIFIED` | Sağlayıcı yerleşim envanteri, politika | ADR-009; F8 governance | AI Platform | F8 | OPEN | `UNVERIFIED` |
| R-014 | AI halüsinasyonu (yanlış içerik üretimi) | High | Medium | `UNVERIFIED` | Değerlendirme/regresyon, insan onayı | F8 evaluation + human review | AI Platform | F8 | OPEN | `UNVERIFIED` |
| R-015 | Hatalı klinik AI çıktısı (hasta güvenliği) | Medium | High | `UNVERIFIED` | Klinik doğrulama, yasal sınıflandırma | G4 kapısı; ayrı yasal/klinik validasyon | Medical Imaging AI | F10 | OPEN | `UNVERIFIED` |
| R-016 | DICOM verisinin ifşası | Medium | High | `UNVERIFIED` | Erişim kontrolü, ağ izolasyonu | ADR-011; F10 güvenlik tasarımı | DICOM/PACS | F10 | OPEN | `UNVERIFIED` |
| R-017 | Device bridge ele geçirilmesi | Medium | High | Bridge güvenlik sertleştirmesi yapıldı (PR #144/#150 çalışmaları) — kapsam ve güncel durumu `UNVERIFIED` | Sürekli güvenlik incelemesi, imzalı güncelleme zinciri kanıtı | F10 bridge güvenlik gözden geçirmesi | Device Bridge | F10 | OPEN | `UNVERIFIED` |
| R-018 | Hassas verinin loglara sızması | Medium | High | `UNVERIFIED` (Wave 1 — F3-IMPL-004: 43 site kapandı, PR #356 `MERGED`; Wave 2 — F3-IMPL-006: kalan 113 site kapandı [107 `RAW_ERROR_REQUIRES_SAFE_ERROR_FIELDS` + 6 `MESSAGE_CONTENT_REQUIRES_REMOVAL`, 49 dosya], `MERGED`; iki dalga toplam 156 site — reklasifikasyon/lint kuralı yok, 92 `POTENTIAL_PII_SAFE_AFTER_REVIEW` site sadece manuel incelemeyle güvenli kabul edildi, otomatik regresyon koruması yok — bu nedenle risk kapatılmadı) | PII/PHI log politikası, log denetimi, regresyonu önleyecek lint kuralı — **[Updated 2026-08-12, F3-PROD-001]: F3-IMPL-007 "Sensitive Runtime Logging Regression Guard" is now confirmed `MERGED` (PR #399, `380cf04`) and CI-wired into Layer 1 PR safety (F3-CI-OPT-001, PR #398), both independently confirmed ancestors of `origin/main` and of the F3-PROD-001-deployed SHA `f0ff4c70abe08b192233d17cdcd1b7a77dbe4b08` — i.e. the regression guard is now live in production's own deployed codebase, not merely repository-side.** Per its own evidence doc ([evidence/F3-IMPL-007_SENSITIVE_RUNTIME_LOGGING_REGRESSION_GUARD.md](evidence/F3-IMPL-007_SENSITIVE_RUNTIME_LOGGING_REGRESSION_GUARD.md) §14/§16), this closes the "no regression-guard mechanism exists" gap but explicitly does **not** remediate the 103 grandfathered/baseline-excepted sites nor the 92 `POTENTIAL_PII_SAFE_AFTER_REVIEW` sites, and its own text states `R-018: still OPEN` throughout, unchanged by this task. **This risk row's status remains `OPEN`, not closed by this update** — a going-forward regression guard existing is not the same as the pre-existing residual sites being remediated. | ADR-012 log standardı | Observability | F3 | OPEN | `UNVERIFIED` |
| R-019 | Platform-admin yetki aşımı | Medium | High | **[Updated 2026-08-11, F3-IMPL-005]** `CLOSURE_PROPOSED_AWAITING_EXTERNAL_CONFIRMATION` — a fresh current-main inventory of all 70 authenticated platform-admin routes (`platformAdmin.ts` 46, `platformSecurityIncidents.ts` 13, `platformExternalCalendar.ts` 11) reconfirms F3-IMPL-003's classification with zero drift (same 70 routes, same 37 mutation-only subset) and finds all 37 now `AUDITED_DURABLY` — 19 pre-existing + 6 added by F3-IMPL-001/F3-IMPL-003 + **12 added by this task** (`writePlatformAdminAuditEventInTx`/`writePlatformAdminAuditEvent`, same pattern, covering the remaining organization/clinic/plan/user lifecycle mutations and the two file-backup operational triggers) — **0 remaining `UNAUDITED_PERSISTED_MUTATION`**. Per this program's own established rule (see the R-071/R-072 precedent elsewhere in this file: a self-proposed closure by the same task that performed its own verification is not independent risk-owner acceptance), this row is proposed for `CLOSED` but not unilaterally marked so — external/program-owner confirmation required. **[F3-IMPL-005-R1, 2026-08-11]:** a merge-blocking sensitive-logging finding on this PR's 2 file-backup best-effort audit-write-failure catch paths (raw `auditErr` logged, 4 sites) was fixed — now routed through `safeErrorFields()` — with negative-test coverage; this does not change the audit-coverage inventory above (still 37/37, 0 unaudited), only the closure-blocking status of PR #365 itself. See [evidence/F3-IMPL-005_PLATFORM_ADMIN_FINAL_AUDIT_COVERAGE.md](evidence/F3-IMPL-005_PLATFORM_ADMIN_FINAL_AUDIT_COVERAGE.md) §11. | Denetim izi (audit genişletme **TAMAMLANDI** — F3-IMPL-001, F3-IMPL-003, F3-IMPL-005), break-glass prosedürü (ayrı, henüz ele alınmadı), kapsam sınırı | F3 sertleştirme; audit genişletme | Platform Administration | F3 | OPEN | `CLOSURE_PROPOSED_AWAITING_EXTERNAL_CONFIRMATION` |
| R-020 | Resmî API sözleşme değişikliği | High | Medium | Adapter yönü (PROGRAM DIRECTION) | Sözleşme sürüm izleme, contract testleri | ADR-010; F9 adapter testleri | Official Integrations | F9 | OPEN | `UNVERIFIED` |
| R-021 | Düzenleyici yanlış sınıflandırma (ör. tıbbi cihaz/AI mevzuatı) | Medium | High | `UNVERIFIED` | Yasal görüş, sınıflandırma kaydı | G4/G5 kapılarında yasal doğrulama | Compliance | F8/F10 | OPEN | `UNVERIFIED` |
| R-022 | Gürültücü komşu (noisy-neighbor) tenant | Medium | Medium | `UNVERIFIED` | Rate limit, kuyruk adaleti, kaynak kotaları | F6 fairness; F7 ölçekleme | Tenant Security | F6/F7 | OPEN | `UNVERIFIED` |
| R-023 | Büyük kurumsal tenant aşırı yükü | Medium | High | `UNVERIFIED` | Dedicated tenant yolu, kapasite planı | ADR-003; F11 | Enterprise | F11 | OPEN | `UNVERIFIED` |
| R-024 | Onboarding sonrası migration hatası | Medium | High | `UNVERIFIED` | Migration testleri, rollback provası | F1 migration test katmanı; release kapıları | Database | F1+ | OPEN | `UNVERIFIED` |
| R-025 | Entitlement uygulanmaması (frontend-only kontrol) | Medium | High | `UNVERIFIED` | Backend/service/job katmanı entitlement zorunluluğu | ADR-014; DEPENDENCY_MAP kural 7-8 | Entitlements | F2 | OPEN | `UNVERIFIED` |
| R-026 | Aşırı modülerleşme (over-modularization) | Medium | Medium | Program yönü: modüler monolit, erken microservice yok | Sınır sayısı/karmaşıklık ölçütleri | ADR-001/015'te sınır kriterleri | Architecture | F2 | OPEN | Tracker §9 |
| R-027 | Uzun ömürlü branch sapması (divergence) | High | Medium | `UNVERIFIED` | Küçük PR politikası, sık rebase | F1 CI hızlandırma; program görev boyutlandırması | Program | F0+ | OPEN | `UNVERIFIED` |
| R-028 | Ajanın işi yanlışlıkla "tamamlandı" ilan etmesi | High | Medium | Durum modeli: ajan en fazla `AGENT_COMPLETED`; kanıt hiyerarşisi | Otomatik kanıt doğrulama araçları | Tracker §2 kuralları; AGENT_DELIVERY_TEMPLATE zorunlu rapor | Program | F0 | MITIGATED (süreçle) | Tracker §2 |
| R-029 | Hasta/görüntüleme dosyaları veritabanıyla aynı tek VPS'te yerel diskte tutuluyor; S3-uyumlu object storage yapılandırılmamış | Medium | High | `UNVERIFIED` (S3 kodu mevcut ama kapalı) | Object storage migrasyonu, çoklu-replika dosya erişimi | F0-011 object storage **tasarımı tamamlandı** (sağlayıcı seçimi hâlâ dış karar) | Storage | F4/F0-011 | OPEN | F0-006 evidence §8, §11 satır 5; F0-002 §B.9/§B.11; [object-storage-backup-migration-design.md §6-§7](../architecture/object-storage-backup-migration-design.md), [f0-011-storage-flow-inventory.json](../architecture/evidence/f0-011-storage-flow-inventory.json) |
| R-030 | Veritabanı yedek dizini (`/root/noramedi-backups`) veritabanının bulunduğu aynı VPS'te; offsite kopya kanıtı yok | Medium | High | **[Updated 2026-08-15, F4-FCR-002A-CLOSE — durum `OPEN`, kanıt keskinleştirildi, iddia zayıflatılmadı]** `UNVERIFIED` (saha dışı için). Üretimde artık pgBackRest **`repo1`** vardır ve şifrelidir (AES-256-CBC) — fakat `repo1` **YEREL**'dir: PostgreSQL birincili ile aynı host, aynı disk, aynı hypervisor, aynı sağlayıcı hesabı ve aynı tesis, yani **tek arıza alanı**. Geçen PITR tatbikatı (`20260815-213109-709154`) tam olarak bu `repo1`'den geri yüklemiştir ve bu satır için **kanıt değildir**; tatbikat `repo < 2`'den saha dışı kanıt marker'ı yazmayı **reddetmiş** ve yazmamıştır. **Şifreleme dayanıklılık değildir:** AES-256-CBC yedeğin *gizliliğini* korur, host kaybına karşı *dayanıklılık* sağlamaz — şifreli yerel pgBackRest bu satırı çözmez | Offsite/object-storage yedek hedefi — **bağımsız arıza alanı**; `repo2` **oluşturulmamış**, ikincil Türkiye VPS'i **TEDARİK EDİLMEMİŞ**, hiçbir bayt host dışına çıkmamıştır | F0-011 backup pipeline genişletmesi **tasarımı tamamlandı** (uygulama hâlâ bloklu); F4-FCR-002 `repo2` şablonunu **yorum satırında ve açıkça yetkisiz** bırakır; runbook §16.2'nin yedi ön koşulu (KVKK Md. 6 sağlık verisi DPA, E1–E5 yerleşim kanıtı — **E5 dahil**, subprocessor register §1/§6, sözleşmesel destek-erişimi yanıtları, üç ayrı at-rest şifreleme satırı, Türkiye kapsamlı replikasyon, dört rolün ayrımı) **tamamı karşılanmamış**; §16.1'in `R-030-DB` / `R-030-FILES` ayrımı hâlâ program sahibinin kararı ve **yapılmadı** | Operations | F4/F0-011 | **OPEN** — **(2026-08-15'te F4-FCR-002A-CLOSE tarafından yeniden teyit edildi; geçen PITR tatbikatı bu satırı ilerletmez, çünkü tatbikat aynı arıza alanının içinden geri yüklemiştir.)** `FIRST_CUSTOMER_RECOVERY_GATE`'in kalan **tek** blokajıdır. **[2026-08-16, F4-2 — program sahibi kararı: bu satır artık iki yarım üzerinden izlenir.]** §16.1'in önerdiği ayrım **onaylanmıştır**: `R-030-DB` ve `R-030-FILES` aşağıya eklenmiştir. Bu satır **kapatılmamış, indirilmemiş ve yeniden numaralandırılmamıştır** — şemsiye satır olarak `OPEN` kalır ve yalnızca **her iki yarım da** kapandığında kapanabilir. Gelen tüm referanslar geçerliliğini korur. `FIRST_CUSTOMER_RECOVERY_GATE` bundan sonra **yalnızca `R-030-DB`'ye** bağlıdır; gerekçe runbook §16.1'de yazılıdır ve bu karar onu yeniden yazmaz, onaylar | F0-006 evidence §9, §11 satır 9; F0-002 §B.10/§B.11; [runbooks/F4_RECOVERY_OPERATIONS.md](runbooks/F4_RECOVERY_OPERATIONS.md) §16.1/§16.2 ve **§21.7 "What this run does NOT prove"**; [object-storage-backup-migration-design.md §9](../architecture/object-storage-backup-migration-design.md), [f0-011-backup-restore-gap-matrix.json](../architecture/evidence/f0-011-backup-restore-gap-matrix.json) GAP-A/GAP-C |
| R-030-DB | **[Yeni, 2026-08-16, F4-2 — program sahibi kararıyla `R-030`'un veritabanı yarısı; yeni bir risk değil, mevcut satırın izlenebilir hâle getirilmiş yarısı.]** PostgreSQL yedek deposu üretim birincili ile aynı arıza alanında: `repo1` **YEREL** — aynı host, disk, hypervisor, sağlayıcı hesabı ve tesis | Medium | High | `UNVERIFIED` (saha dışı için). `repo1` üretimde vardır ve şifrelidir (AES-256-CBC) ama **yereldir**; şifreleme gizliliktir, **dayanıklılık değildir**. **Depo tarafı artık hazırdır (F4-2, 2026-08-16, kod):** yedek sarmalayıcısında `--repo N` hedefi (varsayılan `1`, mevcut davranış birebir korunur), uzak repo için yerel disk/stanza ön koşullarının atlanması, preflight'ta repo2 şifreleme + repo1'den **farklı** parola doğrulaması, durum belgesinde **repo başına** yedek tazeliği (`repo2BackupCount` / `repo2LastBackupAgeMinutes`) ve opscheck'te aç kalan bir repo2'yi yakalayan kapı. **Hiçbir bayt host dışına çıkmamıştır** | İkincil Türkiye VPS'i **TEDARİK EDİLMEMİŞ**; `repo2` **oluşturulmamış**; runbook §16.2'nin yedi ön koşulu **tamamı karşılanmamış**; repo2'den kaynaklanan geçmiş bir restore tatbikatı yok | Depo uygulaması tamamlandı (F4-2); kalan iş **tedarik + hukuk + operatör aktivasyonu**dur, kod değildir. Aktivasyon sırası runbook §16.5'te | Operations | F4 | **OPEN** — `FIRST_CUSTOMER_RECOVERY_GATE`'in **tek** blokajı budur. Kapanışı runbook §16.1'in yazdığı gerekçeyle meşrudur: PostgreSQL birincili `disklinik-prod-01`'de olduğu için ikincil bir Türkiye VPS'i bu veri sınıfı için **gerçekten bağımsız** bir arıza alanıdır | [runbooks/F4_RECOVERY_OPERATIONS.md](runbooks/F4_RECOVERY_OPERATIONS.md) §16.1/§16.2/§16.5; `F3-C2-ERR-002` §11.5 |
| R-030-FILES | **[Yeni, 2026-08-16, F4-2 — program sahibi kararıyla `R-030`'un dosya/görüntüleme yarısı.]** Görüntüleme/dosya verisinin saha dışı yedeği; bağımsızlık **birincil deponun nereye konacağına** bağlıdır | Medium | High | `UNVERIFIED`. Dosya yedekleme kapsamı ayrıca `R-029` tarafından izlenir | Görüntüleme birincil depo yerleşimi kararı verilmemiştir. Birincil o VPS'te olursa **aynı VPS'teki yedek bağımsız değildir** (aynı disk, hypervisor, sağlayıcı hesabı, tesis) — bu, `R-030`'u **taşır**, kapatmaz ve **üçüncü** bir kopya gerektirir | F0-011 object storage tasarımı (uygulama bloklu); `R-029` ile bağlantılı | Storage | F4/F0-011 | **OPEN** — **ilk müşteri kritik yolunda DEĞİLDİR.** Ayrımın amacı tam olarak budur: ilk kliniğin PostgreSQL kurtarımı, henüz verilmemiş bir görüntüleme mimarisi kararını beklemek zorunda kalmasın | [runbooks/F4_RECOVERY_OPERATIONS.md](runbooks/F4_RECOVERY_OPERATIONS.md) §16.1; `R-029` |
| R-031 | PITR kullanılamıyor (`archive_mode=off`); kurtarma çözünürlüğü yalnızca periyodik yedek sıklığıyla sınırlı (gözlemde ~11 saat) | Medium | High | **[Updated 2026-08-15, F4-FCR-002A-CLOSE]** `VERIFIED_PRODUCTION` — `archive_mode=on`, sürekli WAL arşivleme çalışıyor ve **keyfî bir zamana kurtarma fiilen yapıldı**. Marker WAL `00000001000000000000008C` arşivlendi; kurtarma iki marker'ın **arasında** durdu (marker A = 1, marker B = 0), kurtarma noktası `2026-08-15T21:26:00Z`, hedef `2026-08-15 21:26:35.026000+00`. Efektif RPO **5 dk** — satırın tarif ettiği ~11 saatlik/24 saatlik çözünürlük artık geçerli değildir. *(Superseded, tarihsel kayıt olarak korunur: `NOT_CONFIGURED` (doğrulandı).)* | ~~WAL arşivleme / PITR yapılandırması~~ **karşılandı** — kurulu, etkin ve tek bir kontrollü tatbikatla uçtan uca doğrulandı | pgBackRest `repo1` + additive PostgreSQL drop-in (F4-FCR-002, depo); operatör aktivasyonu; F4-FCR-002A deterministik durma noktası doğrulaması; F4-FCR-002A-R4 uygulama smoke düzeltmesi (PR #427, merged, deployed); tatbikat `20260815-213109-709154` `PASS` | Operations | F4/F0-011 | `CLOSED` — **(2026-08-15, F4-FCR-002A-CLOSE, operatör tarafından çalıştırılan üretim kanıtı üzerinden)**. Bu satırın kriteri iki yerde yazılıdır ve ikisi de karşılanmıştır: bu dosyanın kendi F4-FCR-002 girdisi (*"R-031 needs `archive_mode=on` observed in production"*) ve runbook §21.5 (*"`R-031`/`R-032` … are closed only by an executed, passing, cleaned-up drill"*). **Kendi kendini kapatma değildir:** düzeltici görevler F4-FCR-002 ve F4-FCR-002A-R4, yürütücü operatördür; bu görev yalnızca kaydeder. **Kapsam dardır — kapatılmayanlar açıkça:** sürekli arşivleme sağlığının **izlenmesi** (opscheck `pitr` kontrolü / bit 128) bu görevce **doğrulanmamıştır** ve bu satırla kapanmaz (bkz. `R-006`, `R-074` — dokunulmadı); ölçümler **tek bir** tatbikata aittir, bir hizmet seviyesi taahhüdü değildir; **`R-030` bu satır üzerinden hiçbir biçimde ilerlemez** — kurtarma yerel `repo1`'den yapılmıştır | F0-006 evidence §6/§9, §11 satır 6; F0-002 §B.10; [object-storage-backup-migration-design.md §9.4](../architecture/object-storage-backup-migration-design.md), test matrix Experiment 28; **kapanış kanıtı: [runbooks/F4_RECOVERY_OPERATIONS.md](runbooks/F4_RECOVERY_OPERATIONS.md) §21.7 ve [phases/F4_STORAGE_AND_BACKUP.md](phases/F4_STORAGE_AND_BACKUP.md) `F4-FCR-002A-CLOSE`** |
| R-032 | Restore testinin gerçekten çalıştırıldığına dair kalıcı kanıt yok (`runRestoreTest()` kapasitesi var ama kanıt yok) | Medium | High | **[Updated 2026-08-15, F4-FCR-002A-CLOSE]** `VERIFIED_PRODUCTION` — kanıt üreten bir restore fiilen çalıştırıldı ve kalıcılaştırıldı. Tatbikat `run_id = 20260815-213109-709154`, yedek `20260815-224355F`, tek kullanımlık RAM destekli izole küme; `pitrVerification.verified = true` **ve** `smoke.application = passed`; migration 74/74 (`missing=0`, `ahead=0`, unfinished `0`, rolled back `0`); 106 public tablo; tenant izolasyon smoke `passed` (klinikler arası randevu 0, yetim klinik referansı 0, yetim randevu 0, RLS 0/0); RPO **5 dk** ≤ 60 dk; RTO **7 sn** ≤ 14400 sn; temizlik **doğrulandı**. `RESULT = PASS`, `R032_eligible = true`. *(Superseded, korunur: kapasite mevcut (`server/src/services/backupService.ts`), admin-tetiklemeli, kimlik doğrulamalı (`platformAdmin.ts`).)* | ~~Zamanlanmış, kanıt üreten restore-test koşusu~~ — **kanıt üreten koşu gerçekleşti**; koşunun **zamanlanmış/tekrarlayan** hâle getirilmesi bu satırla kapanmaz ve ayrı bir operasyonel görevdir | `scripts/noramedi-pgbackrest-restore-drill.sh` (F4-FCR-002/002A); deterministik PITR durma noktası doğrulaması (F4-FCR-002A); Prisma 7 uygulama smoke düzeltmesi (F4-FCR-002A-R4, PR #427, merged + deployed `3093518`); operatör tarafından çalıştırılan tatbikat `PASS` | Operations | F4 | `CLOSED` — **(2026-08-15, F4-FCR-002A-CLOSE, operatör tarafından çalıştırılan üretim kanıtı üzerinden)**. Bu satırın kapanış kriteri runbook §21.6'da **birebir** yazılıdır: *"`R-032` may be considered for closure only when that run exits `0` and `/var/lib/noramedi/pitr-drill-result.json` contains **both** `pitrVerification.verified = true` and `smoke.application = passed`"* — üçü de (`exit 0`, her iki alan) sağlanmıştır; ayrıca betiğin kendi `R032_ELIGIBLE` bileşimi (`result=passed` ∧ app smoke ∧ tenant smoke ∧ migration karşılaştırması ∧ RPO ∧ RTO ∧ PITR doğrulaması) `true` dönmüştür. **Kendi kendini kapatma değildir** (düzeltici görevler F4-FCR-002/F4-FCR-002A-R4; yürütücü operatör). **Kapatılmayanlar açıkça:** tekrarlayan/zamanlanmış restore tatbikatı ve bayatlık alarmı (`RecoveryDrillRun` defterine bu yoldan satır **yazılmamaktadır** — bilinçli boşluk, runbook §12); dosya (imaging) restore kapsamı; ve **`R-030`** — kurtarma **yerel `repo1`**'den yapılmıştır, tatbikat `repo < 2`'den saha dışı kanıt yazmayı reddetmiştir | F0-006 evidence §6/§9, §11 satır 7; F0-002 §B.10/§B.11; [f0-011-storage-backup-test-matrix.md](../architecture/f0-011-storage-backup-test-matrix.md) Experiments 25-27; **kapanış kanıtı: [runbooks/F4_RECOVERY_OPERATIONS.md](runbooks/F4_RECOVERY_OPERATIONS.md) §21.7 ve [phases/F4_STORAGE_AND_BACKUP.md](phases/F4_STORAGE_AND_BACKUP.md) `F4-FCR-002A-CLOSE`** |
| R-033 | `noramedi-worker` PM2 süreci için depoda tanımlı hiçbir deploy/reload otomasyonu yok — güncelleme/restart yaşam döngüsü belgesiz | Medium | High | `UNVERIFIED` — depoda `ecosystem.config.*` yok, `scripts/noramedi-deploy.sh` yalnızca `noramedi-api`'yi reload ediyor | ~~Worker'ı kapsayan deploy otomasyonu~~ **F3-IMPL-002: repository-level mitigation implemented** — `ecosystem.config.cjs` eklendi, `scripts/noramedi-deploy.sh` artık her iki süreci de `pm2 startOrReload` ile reload/start edip worker'ı PM2 status polling ile doğruluyor (başarısızsa deploy `exit 1` ile durur). **F3-IMPL-002-PROD-RECON (2026-08-12): bu otomasyon artık production'ın gerçekten çağırdığı yola (`/usr/local/sbin/noramedi-deploy.sh`) kurulu ve hash ile doğrulandı — fakat henüz bir kez bile çalıştırılmadı. Kalan tam kapanış kanıtı: steps 5-8'in tek bir başarılı koşuda yakalanması (API `startOrReload` + worker `startOrReload` + API healthcheck + `verify_pm2_online noramedi-worker` `online`, script `exit 0`).** **[F3-PROD-002 — 2026-08-12: KARŞILANDI, beş unsurun tamamı tek bir koşuda.]** Operatör `/usr/local/sbin/noramedi-deploy.sh --skip-pull --skip-build --skip-migrate --skip-generate` çalıştırdı → `deploy_exit=0`; API `startOrReload` (PID `607545`, `restart_time` `11`→`12`, `cwd` `/var/www/noramedi/server`, `role=api (declared=true) ownsJobs=false`); worker `startOrReload` (PID `607578`, `11`→`12`, `role=worker (declared=true) ownsJobs=true`, `All background jobs scheduled.`); API healthcheck `200` (4/12 denemede, `000` geri-denemeleri scriptin kendi bütçesi içinde); step 8 `OK — PM2 process 'noramedi-worker' is online after 1 attempt(s).` — `scripts/noramedi-deploy.sh:119`'daki biçim dizgisinin birebir kendisi | F0-006 bulgusu; uygulama F3-IMPL-002 (merged, PR #357; kod `aa18b064267ff5846ae60f73889c3322030bd4a8`'de mevcut). **Kurulum sapması F3-IMPL-002-PROD-RECON ile giderildi:** kurulu script 2026-06-29 revizyonunda (`8dffcd13…`, commit `96313f39`) donmuştu — F3-IMPL-002'den önceki, yalnızca `pm2 reload noramedi-api` yapan, worker'a hiç dokunmayan 6 adımlı sürüm; 2026-08-12'deki **her iki** production deploy'u da bu eski scriptle koştu ve worker bayat kaldı (bkz. R-077) | Deployment | F0-006/F3 | `CLOSED` — **(2026-08-12, F3-PROD-002, on operator-executed production evidence)**. This row's own named remaining closure evidence was *"steps 5-8 captured in one successful run — API `startOrReload` + worker `startOrReload` + API healthcheck + `verify_pm2_online noramedi-worker` `online`, script `exit 0`"*, and **all five elements are now in hand from a single invocation** of the installed `/usr/local/sbin/noramedi-deploy.sh` (see the mitigation cell for the values). A 14-point reconciliation of the observed lifecycle against `ecosystem.config.cjs` + `scripts/noramedi-deploy.sh` found **zero mismatches**. **Not self-closed:** the remediating task was F3-IMPL-002, the installer F3-IMPL-002-PROD-RECON, the executor the operator — the `R-019/R-071/R-072/R-073/R-075` no-self-closure precedent is honoured. Superseded and **preserved as history, not deleted:** the prior token `OPEN — repository-level fix implemented **and now installed at the production invocation path, hash-verified** (`794375b4249b063e167a8d6f885df1a590bd9de9df367e91a0d3eaaf635f5012`, `root:root` `0755`, `bash -n` clean, byte-identical to `scripts/noramedi-deploy.sh` at the deployed SHA); **end-to-end deploy execution still not observed** — **not closed**`, and before it `OPEN — repository-level fix implemented, production not yet verified`. **Caveats explicitly retained, not absorbed:** verification is **registration-level, not execution-level** (`All background jobs scheduled.` is registration — no completed job tick, no `JobLock` lease observed); the observation is **point-in-time**; **PM2 reboot persistence is unverified** (`pm2 save` not evidenced) and belongs to **R-077**; **drift prevention is unchanged** (**R-077**(b), still `OPEN`). **This closure does not close R-063, R-074, R-076 or R-077, does not close R-040 by implication** (R-040 is argued separately on its own criteria), **and does not satisfy the F3 exit gate**, which remains `NOT_SATISFIED` | F0-006 evidence §2/§3/§11, drift satır 3; F0-002 §6.6/§6.10 madde 3; [evidence/F3-IMPL-002_PRODUCTION_WORKER_PROCESS_CONTRACT.md](evidence/F3-IMPL-002_PRODUCTION_WORKER_PROCESS_CONTRACT.md); [evidence/F3-IMPL-002-PROD-RECON_PRODUCTION_WORKER_CONTRACT_VERIFICATION.md](evidence/F3-IMPL-002-PROD-RECON_PRODUCTION_WORKER_CONTRACT_VERIFICATION.md) §4/§6.1; **kapanış kanıtı: [evidence/F3-PROD-002_DEPLOY_SCRIPT_EXECUTION_VERIFICATION.md](evidence/F3-PROD-002_DEPLOY_SCRIPT_EXECUTION_VERIFICATION.md) §3/§4/§5/§9.1** |
| R-034 | API ve worker'ın ikisi de `RUN_BACKGROUND_JOBS` `'false'` değilse aynı 9 job'u bağımsız kaydedebilir — mükerrer job kaydı riski (mükerrer **koşu** DB-tabanlı `JobLock` ile önleniyor, ancak kayıt yedekliliği kontrolsüz) | Medium | Medium (JobLock ile azaltılmış) | `JobLock` (Postgres-tablosu tabanlı lease kilidi, `server/src/utils/jobLock.ts`) mükerrer koşuyu engelliyor | ~~`RUN_BACKGROUND_JOBS` gerçek değerinin production'da doğrulanması~~ ~~**F3-IMPL-002: API tarafı artık `ecosystem.config.cjs` içinde açıkça `RUN_BACKGROUND_JOBS=false` — production'da gerçek değerin doğrulanması hâlâ ayrı bir operasyonel adım**~~ **[Karşılandı — 2026-08-12, F3-IMPL-002-PROD-RECON(R2), operatör kaynaklı production kanıtı.]** Bu satırın adlandırdığı eksik kontrol *"`RUN_BACKGROUND_JOBS` gerçek değerinin production'da doğrulanması"* idi ve **tam olarak doğrulandı**: production API `ownsJobs=false` ile gözlendi; `server/src/utils/backgroundJobsOwnership.ts` uyarınca bu durum **yalnızca** `RUN_BACKGROUND_JOBS === 'false'` tam eşitliğinde oluşur, dolayısıyla gerçek production değerinin doğrudan doğrulamasıdır. Production worker ise `role=worker (declared=true)`, `ownsJobs=true`, `All background jobs scheduled` ile gözlendi. **Gerçek production sahiplik matrisi doğrulanmıştır: tam olarak bir job sahibi** — ne mükerrer kayıt ne de sıfır-sahip durumu mevcut. Konfigürasyon **kaynağı** (provenance) bu satırın değil **R-040**'ın konusudur; PM2 yeniden başlatma kalıcılığı ve script sapması ise **R-040/R-077**'ye aittir — hiçbiri bu satırın kapanışını geciktirmez | Değer doğrulaması veya API tarafında bayrağın açıkça `false` olarak standardize edilmesi — **standardizasyon tarafı F3-IMPL-002 ile depo düzeyinde tamamlandı**, yalnızca worker'ın deploy yaşam döngüsü de aynı PR'da tanımlandığı için güvenli (bkz. R-033). **F3-IMPL-002-PROD-RECON (2026-08-12): worker tarafı artık production'da doğrulandı** — `ecosystem.config.cjs` ile yeniden başlatılan worker `role=worker (declared=true)`, `ownsJobs=true`, `All background jobs scheduled.` logluyor; F3-PROD-001'in API tarafı gözlemiyle (`ownsJobs=false`) birlikte **gözlenen** sahiplik matrisi doğru (tek sahip, mükerrer kayıt yok, sahipsizlik yok). **Ancak API'nin `RUN_BACKGROUND_JOBS=false` değeri PM2'nin depo dışı saklanan environment'ından geliyor, `ecosystem.config.cjs`'ten değil** — API hiç ecosystem dosyasından başlatılmadı/reload edilmedi; `pm2 delete`/süreç tablosu kaybı/kutu yeniden kurulumu bu değeri düşürür. Kalan tam kapanış kanıtı: ecosystem-kaynaklı bir API `startOrReload` sonrası `role=api (declared=true)` + `ownsJobs=false` | Jobs/Scheduling | F0-006/F3 | `CLOSED` — **(2026-08-12, F3-IMPL-002-PROD-RECON(R2))** externally confirmed by this independent program-controller review on the accepted operator-supplied production evidence. This row's own named missing control was *verification of the real production `RUN_BACKGROUND_JOBS` value*, and that value is now directly verified: API observed `ownsJobs=false`, which per `server/src/utils/backgroundJobsOwnership.ts` arises **only** on exact `RUN_BACKGROUND_JOBS === 'false'`; worker observed `ownsJobs=true` with all 9 jobs scheduled. **Exactly one job owner in production — the duplicate-registration state this row describes is verified absent.** Superseded and preserved as history, not deleted: the prior `OPEN — … API side's flag observed but not sourced from the repository-defined config` wording, which is now withdrawn as **over-strict** — it imported R-040's config-*provenance* criterion into a row whose own criterion is the *value*. **Explicit caveats retained:** the verification is **registration/ownership-level, not execution-level** — no completed job tick and no `JobLock` lease acquisition is claimed or observed; the observation is point-in-time; PM2 reboot persistence of the reconciled worker definition is unknown (**R-040/R-077**, not this row); and `LAUNCH_GATES.md` §2.C's `JobLock` duplicate-run sub-criterion remains a separate G1 item. **This closure does not close R-033, R-040, R-074, R-076 or R-077, and does not satisfy the F3 exit gate**, which remains `NOT_SATISFIED`. | F0-006 evidence §2, §11 satır 1; `server/src/index.ts`, `server/src/worker.ts`, `server/src/utils/jobLock.ts`, `server/src/utils/backgroundJobsOwnership.ts`; [evidence/F3-IMPL-002_PRODUCTION_WORKER_PROCESS_CONTRACT.md](evidence/F3-IMPL-002_PRODUCTION_WORKER_PROCESS_CONTRACT.md); [evidence/F3-IMPL-002-PROD-RECON_PRODUCTION_WORKER_CONTRACT_VERIFICATION.md](evidence/F3-IMPL-002-PROD-RECON_PRODUCTION_WORKER_CONTRACT_VERIFICATION.md) §5/**§6.2 (R2 closure argument)**; `backgroundJobsOwnership.ts:38` — `env.RUN_BACKGROUND_JOBS === 'false'` is the sole path to `ownsJobs: false`, re-read at the deployed SHA by this task |
| R-035 | Node.js sürüm sapması: production Node `22.23.1` çalıştırıyor, tek CI kanıtı Node `20` sabitliyor, kökte/`server/`de `engines` alanı yok | Medium | Medium | `UNVERIFIED` (health check'ler geçiyor ama izlenmiyor) | `engines` pini, CI'da gerçek production sürümü testi | F1 CI kapsamı genişletmesi; F3 sertleştirme | CI/Runtime | F1/F3 | OPEN | F0-006 evidence drift satır 1; F0-002 §B.3/§6.10 madde 4 |
| R-036 | PM2 süreçleri (`noramedi-api`, `noramedi-worker`) `root` olarak çalışıyor; least-privilege ayrımı yok | Medium | High | `UNVERIFIED` (izolasyon kanıtı yok) | Dedike servis hesabı, container/namespace izolasyonu | F3 Production Hardening | Security | F3 | OPEN | F0-006 evidence §10, §11 satır 8; F0-002 §B.11 finding 10 |
| R-037 | PM2 restart sayıları (14 API / 13 worker) ~38-39 dk gözlemlenen uptime'da operasyonel inceleme gerektiriyor | Low | Medium | `UNVERIFIED` (neden — deploy reload/crash loop/manuel — belirlenmedi) | Restart nedeni log analizi | Operasyonel inceleme (F0-006 kapsamı dışı, izleme önerisi) | Operations | F3 | OPEN — **(gözlem eklendi 2026-08-12, F3-PROD-002; durum değişmedi.)** Deploy koşusunda her iki süreç de `11`→`12`'ye çıktı, `0` unstable restart. Worker tarafı **temiz ve sürekli**: F3-IMPL-002-PROD-RECON'un kaydettiği `11` ile birebir devam ediyor, yani deploy aynı PM2 app kaydı üzerinde çalıştı, yeni bir kayıt oluşturmadı. API tarafı ise bu satırın zaten taşıdığı sayaç-sıfırlanması olgusunu tekrarlıyor: `11` başlangıç değeri, F0-002'nin 2026-07-19'da gözlediği `14`'ten **düşük** — worker için kaydedilenle (`10` vs F0-002'nin `13`'ü) aynı desen. Neden hâlâ belirlenmedi | F0-002 §B.4/§B.11; [evidence/F3-PROD-002_DEPLOY_SCRIPT_EXECUTION_VERIFICATION.md](evidence/F3-PROD-002_DEPLOY_SCRIPT_EXECUTION_VERIFICATION.md) §3.2/§3.3/§9.6 |
| R-038 | Frontend build-artifact'in kaynak kod ile eşleştiği doğrulanmadı — hiçbir depo scripti frontend build'i deploy etmiyor | Low | Medium | `UNVERIFIED` | Build/deploy dry-run karşılaştırması | Gelecek görev: artifact-hash doğrulaması | Frontend/Deployment | F0-006 | OPEN | F0-006 evidence §3/§11 satır 4, drift satır 4; F0-002 §3 |
| R-039 | `docs/35-docker-deploy-runbook.md` bayat/aspirasyonel bir Docker Compose topolojisi tanımlıyor (eski ürün/DB adı, depoda karşılığı yok) — gelecekteki bir deploy girişimini yanıltabilir | Low | Medium | Belgelendi (bu görev) | Doküman kaldırma/deprecate işaretleme | Ayrı dokümantasyon-yalnız görev | Documentation | F0-006 | OPEN | F0-006 evidence §3/§11, drift satır 2; F0-002 §6.10 madde 1 |
| R-040 | Konfigürasyon kaynağı belirsizliği: `dotenv.config()` her giriş noktasında (`index.ts`, `worker.ts`, `db.ts`) bağımsız çağrılıyor, `process.cwd()`'ye dayanıyor; depoda bunu zorlayan bir `ecosystem.config.*` yok | Low | Medium | `UNVERIFIED` (gerçek PM2 `cwd` teyit edilmedi) | ~~Repository-tanımlı süreç/config sözleşmesi~~ **F3-IMPL-002: `ecosystem.config.cjs` eklendi** — her iki PM2 uygulaması için `cwd`/`script`/`args`/rol-özel `env` artık depoda tanımlı; gerçek production `cwd`'nin bu dosyayla eşleştiği ilk deploy'da doğrulanmalı (henüz production'a uygulanmadı) | `ecosystem.config.js` eklenmesi — **F3-IMPL-002 ile depo düzeyinde tamamlandı**, production'a ilk uygulanışı ayrı bir operasyonel adım — **F3-IMPL-002-PROD-RECON (2026-08-12): worker uygulaması için gerçekleşti.** `pm2 startOrReload /var/www/noramedi/ecosystem.config.cjs --only noramedi-worker --update-env` production'da başarıyla koştu; dosya production yolunda çözüldü, `env` bloğu sürece ulaştı (`declared=true` yalnızca `NORAMEDI_PROCESS_ROLE=worker` teslim edildiyse oluşur) ve çözülen `cwd` deploy edilen bağımlılık sürümlerini taşıyan ağacı gösterdi (`Prisma Client v7.9.1`). **`noramedi-api` uygulaması için hâlâ doğrulanmadı** — API hiç bu dosyadan başlatılmadı; production `cwd`/`script`/`args`/`env` değerleri hâlâ depo dışı ilk `pm2 start` kaydından geliyor. Kalan tam kapanış kanıtı: ecosystem-kaynaklı API `startOrReload` + `role=api (declared=true)` + `pm2 describe noramedi-api` ile `cwd` = `/var/www/noramedi/server` teyidi. **[F3-PROD-002 — 2026-08-12: KARŞILANDI, üç unsurun tamamı.]** Deploy scriptinin step 5'i `$APP_DIR/ecosystem.config.cjs`'i çözerek `noramedi-api`'yi `startOrReload` etti (PID `607545`, `restart_time` `11`→`12`, `online`, `0` unstable); süreç `role=api (declared=true) ownsJobs=false (RUN_BACKGROUND_JOBS=false — jobs delegated to a dedicated worker process)` logladı; PM2 süreç metadata'sı `cwd` = `/var/www/noramedi/server`, `script`/`args` = `npm run start` — dosyayla birebir. `declared=true` tek bir yoldan (`processRole.ts:75`) doğar ve `NORAMEDI_PROCESS_ROLE`'ün **teslim edilmiş, tanınmış ve eşleşmiş** olmasını gerektirir; değişken tanımsız olsaydı süreç yine `role=api` fakat `declared=false` yazardı — dolayısıyla bu, ecosystem dosyasının `env` bloğunun API sürecine ulaştığının doğrudan runtime kanıtıdır | Configuration | F0-006/F3 | `CLOSED` — **(2026-08-12, F3-PROD-002, on operator-executed production evidence)**. All three elements this row itself named are verified, and `script`/`args` match additionally; **both** PM2 apps now demonstrably derive `cwd`/`script`/`args`/role-specific `env` from the repository-defined `ecosystem.config.cjs`, which is exactly the config-source ambiguity this row describes. **Not self-closed** (remediation F3-IMPL-002; installation F3-IMPL-002-PROD-RECON; execution by the operator). Superseded and **preserved as history, not deleted:** `OPEN — partially verified: worker app's first production application of ecosystem.config.cjs confirmed; API app's cwd/env still unverified against the file — not closed`, and before it `OPEN — repository-level mitigation implemented, production not yet verified`. **Caveats retained:** `dotenv.config()`'s `process.cwd()` dependence is now *anchored* by a verified repository-defined `cwd` — this is not a claim that every configuration value is repository-defined, since secrets continue to come from the process environment by design (`ecosystem.config.cjs:35-38`), none of which was read or recorded; **PM2 reboot persistence is unverified** (**R-077**); point-in-time. **Does not close R-063, R-074, R-076 or R-077 and does not satisfy the F3 exit gate** | F0-006 evidence §5/§11, §2; [evidence/F3-IMPL-002_PRODUCTION_WORKER_PROCESS_CONTRACT.md](evidence/F3-IMPL-002_PRODUCTION_WORKER_PROCESS_CONTRACT.md); [evidence/F3-IMPL-002-PROD-RECON_PRODUCTION_WORKER_CONTRACT_VERIFICATION.md](evidence/F3-IMPL-002-PROD-RECON_PRODUCTION_WORKER_CONTRACT_VERIFICATION.md) §6.3; **kapanış kanıtı: [evidence/F3-PROD-002_DEPLOY_SCRIPT_EXECUTION_VERIFICATION.md](evidence/F3-PROD-002_DEPLOY_SCRIPT_EXECUTION_VERIFICATION.md) §3.2/§4/§5/§9.2** |

| R-041 | Aktif KVKK-HIGH-007 devam çalışmasının Prisma migration'ı (`20260719120821_kvkk_high007_consent_reconciliation`, görev başlangıcında commit edilmemiş olarak gözlemlendi, düzeltme turunda PR #175 içinde açık — bkz. F0-007 evidence §1.1/§5C), mimari refactor çalışmalarıyla (F0-009/F0-010 tasarımından implementasyona geçiş) çakışabilir | Medium | High | Program-çapında dondurma kuralı (tracker §8) + F0-007 KVKK_ARCHITECTURE_FREEZE_BOUNDARY.md | Migration içeriği incelenmedi (görev talimatı gereği, PR açık olsa da); rollback/tenant-etki kanıtı yok | KVKK baseline dış teyidine kadar şema-dokunan mimari işler bloklu kalır | Privacy/Prisma Schema | F0/F5 | OPEN | F0-007 evidence §5/§6 |
| R-042 | `docs/compliance/56-...md` ve `KVKK_COMPLIANCE_AUDIT_AND_REMEDIATION.md`, GitHub'ın `MERGED` gösterdiği PR #169'u hâlâ "not yet merged"/"in progress" olarak tanımlıyor (öz-referans gecikmesi, `docs/program/` dışında olduğu için bu görev tarafından düzeltilmedi) | Medium | Medium | Tracker kaynak hiyerarşisi §2.1 (git/PR kanıtı belge metninden üstün) | `docs/compliance/` güncellemesi bu görevin kapsamı dışında | Ayrı bir dokümantasyon-yalnız görevle `docs/compliance/` güncellemesi | Documentation/Compliance | F0-007 | OPEN | F0-007 evidence §3.3 |
| R-043 | WhatsApp/SMS/Recall kanallarında consent enforcement tutarsızlığı — aktif çalışma her kanala ayrı ayrı consent-gate ekliyor (`whatsappCommunicationPurposeMap.ts`, `messagesConsentGate.test.ts`, `recallConsentGate.test.ts`), merge edilene kadar kanallar arası tutarlılık doğrulanamaz | Medium | High | `UNVERIFIED` | Kanal-bağımsız tek bir consent-decision noktası kanıtı (`evaluateCommunicationPermission` PR #169 ile merge edildi ama devam çalışmasının onu her kanalda tutarlı çağırdığı kanıtlanmadı) | Devam çalışması merge edilince kanallar arası tutarlılık ayrıca doğrulanmalı | Messaging/Privacy | F0-007 | OPEN | F0-007 evidence §6; freeze boundary §2 satır 8-10 |
| R-044 | Backfill/reconciliation (`legacyReconciliationResolver.ts`, `communicationConsentConflictTracker.ts`) yetkili çakışma-çözme kuralları olmadan consent durumunu değiştirebilir | Medium | High | `UNVERIFIED` — içerik incelenmedi | Çakışma-çözme kurallarının kanıtı (ADR veya test) | Devam çalışması merge edilene ve backfill'in çalıştırıldığı doğrulanana kadar hiçbir consent state değişikliği production'a alınmamalı | Privacy/Consent | F0-007 | OPEN | F0-007 evidence §6; baseline doc §4 |
| R-045 | Retention cleanup job'ı (`dataRetentionCleanupJob.ts`) consent/audit şeması geçiş halindeyken çalışırsa, reconciliation'ın hâlâ ihtiyaç duyduğu veriyi silebilir/bozabilir | Medium | High | `UNVERIFIED` | Retention job'ın yeni şemayla davranışının kanıtı | Devam çalışması merge edilene ve retention job'ı ayrıca yeniden doğrulanana kadar retention schedule/scope değişikliği bağımsız deploy edilmemeli | Privacy/Retention | F0-007 | OPEN | F0-007 evidence §6 |
| R-046 | **[F0-012 correction, folded in: PR #175 is `MERGED` — commit `1da9586995b625624b7385c14e70ba6a322def73` — not "open"; risk unchanged, only the repository-fact status.]** KVKK-HIGH-007 devam migration'ı (artık `main`'de, PR #175, merge commit `1da9586`) production'a, rollback ve tenant-etki doğrulaması olmadan deploy edilirse geri alınamaz veri/şema hasarı riski | Medium | High | Program-çapında dondurma kuralı; deploy script `set -euo pipefail` fail-fast ama rollback-kapasiteli değil (F0-006 evidence). **[2026-07-20, KVKK-HIGH-008-PMVR Phase B1, corrected 2026-07-20 for rollback-conditionality]** Disposable Postgres 16.14 rehearsal (3 senaryo) migration'ın additive-only olduğunu ve mevcut veriyi değiştirmediğini/silmediğini bağımsız doğruladı; application cutback (uyumlu önceki commit'e redeploy) additive tablolar dururken uyumludur. **Physical schema rollback (yeni tabloların drop edilmesi) yalnızca o tablolar boşken veri kaybı olmadan mümkündür** — correction/conflict kayıtları oluştuktan sonra tabloları drop etmek "hiçbir şey"i değil, yeni oluşturulan operasyonel/KVKK kanıtını yok eder; table-drop bu nedenle workflow kullanımı başladıktan sonra varsayılan rollback yolu değildir. Varsayılan cutback: disable-feature/control (varsa) → uyumlu önceki application commit'e redeploy → additive tabloları koru → forward-fix — bkz. evidence §4/§6/§11. **[2026-07-20, PR #184, `VERIFIED_USER_SUPPLIED_PRODUCTION_EVIDENCE`]** the user supplied read-only production command results reporting the migration is applied (64/64 migrations, `prisma migrate status` clean) — this reconciliation accepts that as user-supplied production evidence that the migration is applied; **PMVR itself did not execute any production command, and no raw command output is committed to this repository as a standalone evidence artifact**, so this is not independently re-verified by an automated task — see the evidence-disposition note at the top of this file. **[2026-07-20, F0-011-P2, independent re-verification, fresh worktree/session, not reused from PMVR's own run]** Independently re-ran the disposable-Postgres rollback rehearsal with the specific populated-table scenario this row previously listed as still missing: 1 synthetic `CommunicationConsentConflictBucket` row and 1 synthetic `PatientLegacyConsentCorrection` row were inserted, then the same rollback DDL was re-executed — it dropped both tables **and irrecoverably destroyed both synthetic rows**, independently confirming (not merely asserting) that physical schema rollback is destructive once either table holds data. Also independently re-ran a 14-assertion synthetic multi-clinic tenant-isolation matrix (0 cross-tenant leakage, 0 unscoped queries) and independently tested (not just inspected) that a pre-HIGH-007 application commit's Prisma Client operates correctly against a database retaining the additive tables — supporting the disable-workflow → redeploy-compatible-commit → retain-tables → forward-fix cutback path already recommended here. New tooling finding: after a physical DDL rollback, `_prisma_migrations` bookkeeping does not self-reconcile and `prisma migrate resolve --rolled-back` refuses to act on a cleanly-applied migration (`P3012`) — see evidence §C. **Full production cross-tenant negative verification and full production audit verification were not performed by this task (no production access permitted in its scope) and remain outstanding — this risk is NOT moved to `MITIGATED` by this pass.** **[2026-07-20, KVKK-HIGH-008-F1-PROD-DOCS, user-supplied production evidence]** Production deployment of PR #186 and all three named migrations (including PR #186's own `PlatformAdminAuditEvent` migration) are now additionally confirmed applied/not-rolled-back on this same user-supplied, read-only evidence basis; the disposable-environment rollback and tenant-isolation rehearsal (F0-011-P2) already passed. **Full production cross-tenant negative verification and full production audit verification are still missing. Production deployment success does not close this risk — it remains `OPEN`, not `MITIGATED`.** | Migration rollback provası (disposable ortamda, hem boş hem de dolu tablo durumu için **2026-07-20 F0-011-P2 tarafından tamamlandı** — populated-table senaryosu artık disposable-ortam düzeyinde kanıtlanmıştır; production-düzeyinde populated-table provası hâlâ yok), tenant-izolasyon etki testi (Scenario C'de ve F0-011-P2'nin 14 iddialık matrisinde gerçek cross-tenant deneme ile doğrulandı), full production cross-tenant negative verification, full production audit verification, ve production'da workflow kullanımından sonraki cutback yolunun (disable → redeploy → retain → forward-fix, table-drop hariç) production ortamında fiilen provası (disposable ortamda F0-011-P2 tarafından ayrıca doğrulandı) | Migration deploy'u öncesi bağımsız rollback/tenant-etki kanıtı zorunlu kılınmalı — disposable-ortam kanıtı artık hem boş hem dolu tablo senaryoları için mevcut (F0-011-P2), ancak koşulludur (tablolar boşken veri-kaybı-yok; workflow kullanıldıktan sonra table-drop varsayılan cutback yolu değildir, ve bu artık disposable ortamda destructive olarak fiilen gösterildi). migration application in production and deployment are accepted as `VERIFIED_USER_SUPPLIED_PRODUCTION_EVIDENCE` (PR #184), but no independently-executed repository artifact exists; **full production cross-tenant negative verification and full production audit verification against the actual production database remain outstanding regardless of deployment status or disposable-rehearsal completeness.** **Every explicit acceptance criterion (independent rollback rehearsal reflecting the empty-vs-populated-table distinction, tenant-impact evidence, and full production cross-tenant/audit verification) must be met before this risk may move to `MITIGATED`** — the disposable-rehearsal criteria are now independently satisfied (F0-011-P2), but the full-production-verification criterion is not, so this risk **remains `OPEN`, not `MITIGATED`, do not close solely because disposable rehearsal passed.** **F0-012:** [LAUNCH_GATES.md](LAUNCH_GATES.md) §2 Section D bunu G1 (Controlled Pilot) için açık bir **blocker** olarak kaydeder — pilot klinikleri bu migration'ı taşıyan bir veritabanına sahip olmadan önce bu satırın rollback/tenant-etki kanıtı `MITIGATED`'a taşınmalıdır | Privacy/Database | F0-007 → KVKK-HIGH-008-PMVR Phase B1 → F0-011-P2 (independent re-verification, disposable-rehearsal criteria satisfied, full-production-verification criterion still outstanding) → G1 pre-condition (F0-012) | OPEN | F0-007 evidence §5/§7; PRODUCTION_TOPOLOGY.md §4; [KVKK-HIGH-008-PMVR evidence §4, §6, §11](evidence/KVKK-HIGH-008-PMVR_POST_MERGE_READINESS_EVIDENCE.md); [F0-011-P2 evidence §C, §D](evidence/F0-011-P2_KVKK_HIGH007_HIGH008_ROLLBACK_TENANT_VERIFICATION.md); [KVKK_HIGH008_FREEZE_BOUNDARY.md §6](../program/KVKK_HIGH008_FREEZE_BOUNDARY.md); [LAUNCH_GATES.md §2.D](LAUNCH_GATES.md#d-database-and-migration-readiness) |
| R-047 | Yeni consent-audit kavramı (`communicationConsentAuditLogging.ts`/`communicationConsentAuditReport.ts`) mevcut `AuditLog`/`ActivityLog` ile örtüşen veya yinelenen bir kavram olabilir; ADR-012 kararı henüz yok | Low | Medium | `UNVERIFIED` | ADR-012 kabulü, tek bir kanonik audit kavramı kararı | Devam çalışması merge edilince ADR-012 kapsamında gözden geçirilmeli | Audit/Privacy | F0-007 | OPEN | F0-007 freeze boundary §2 satır 5/13 |
| R-048 | Tracker/GitHub/çalışma-ağacı durum sapması: PR #167 tracker'da `OPEN` olarak kayıtlıydı, GitHub `MERGED` (2026-07-18T16:10:01Z) gösteriyor — aynı-gün kontrol zamanlaması nedeniyle | Low | Medium | F0-007 tarafından düzeltildi (bu görev) | Otomatik PR-durum senkronizasyon kontrolü yok | Gelecekteki her görev PR durumunu `gh pr view` ile yeniden doğrulamalı, tracker metnine güvenmemeli | Program | F0-007 | MITIGATED (bu turda düzeltildi) | F0-007 evidence §3.3 |
| R-049 | KVKK-HIGH-007 devam çalışmasının yeni test dosyaları (6 adet untracked test) merge edilse bile mevcut tek CI workflow'unun (`windows-bridge-pr.yml`) kapsamı dışında kalacak — PR #169'un 3 dosyasında zaten görülen aynı boşluk | Medium | Medium | `UNVERIFIED` (CI genişletmesi yok) | Backend/frontend JS/TS testlerini kapsayan bir CI workflow'u (F1'in işi) | F1 CI mimarisi bu boşluğu kapatana kadar yeni consent testleri de sessiz regresyon riski taşır | CI/Test Architecture | F0-007/F1 | OPEN | F0-007 evidence §7; TEST_OWNERSHIP.md §7 |
| R-050 | Bu F0-007 dokümantasyonu, devam çalışması commit/merge edildiğinde hızla bayatlayacak (path-metadata-yalnız gözlem, içerik doğrulanmadı) | Medium | Low | Freeze boundary §5'te açık exit-condition listesi | Otomatik bayatlama tespiti yok | Devam çalışması merge edildiğinde bu doküman seti yeniden doğrulanmalı (yeni bir F0-alt-görev veya PR-tetiklemeli inceleme) | Program/Documentation | F0-007 | OPEN | F0-007 baseline doc §1 |
| R-051 | Değişen dosyalar arasında (`recallCandidateService.ts`, `enforcementConfig.ts`, backfill script) yeni ortam değişkenleri/config bayrakları tanıtılmış olabilir; `.env.example` ve ENVIRONMENT_MATRIX.md ile senkron olduğu doğrulanmadı | Low | Medium | `UNVERIFIED` | Yeni bayrakların `.env.example`'a eklendiğinin kanıtı | Devam çalışması merge edilince ENVIRONMENT_MATRIX.md'ye eklenmeli (F0-006 deseniyle tutarlı) | Configuration | F0-007 | OPEN | F0-007 evidence §6; ENVIRONMENT_MATRIX.md §6 (mevcut benzer boşluk emsali) |
| R-052 | Aktif çalışma iki ayrı çakışma-çözme mekanizması gibi görünüyor: `legacyReconciliationResolver.ts` ve `communicationConsentConflictTracker.ts` — kavramsal örtüşme/yinelenme riski, içerik incelenmeden ayırt edilemez | Low | Medium | `UNVERIFIED` | Tek bir kanonik çakışma-çözme kavramının kanıtı | Devam çalışması merge edilince bu iki dosyanın sorumluluk ayrımı doğrulanmalı | Privacy/Consent | F0-007 | OPEN | F0-007 baseline doc §4 |
| R-053 | F0-007'nin orijinal "repository-çapında, 61/61 PR `MERGED`, sıfır açık" iddiası `gh pr list --state all --limit 60` komutundan üretildi — bu komut 60 satırdan fazla döndüremez ve tam PR geçmişini taramaz; düzeltilmiş bir tarama, orijinal dar sorgunun kaçırdığı, KVKK'ya ilgisiz, önceden var olan bir açık PR buldu (#48, 2026-06-16'dan beri açık) | Low | Medium | F0-007 düzeltme turu tarafından tespit edildi ve düzeltildi | Sorgu sonucunun satır sayısı sınırının, iddia edilen kapsamla (repository-çapında) karşılaştırılmaması | Gelecekteki her PR-tarama görevi, `--limit` olmadan veya toplam sayıyla karşılaştırarak tam kapsamı doğrulamalı | Program | F0-007 | MITIGATED (bu turda düzeltildi) | F0-007 evidence §3.1 (düzeltme turu) |
| R-054 | `services/security/securityIncidentService.ts:138`'deki `$executeRawUnsafe` çağrısı yalnızca `id` ile kapsamlanıyor (WHERE'de `clinicId`/`organizationId` yok); çağıranın `incidentId`'nin tenant sahipliğini önceden doğruladığı bu turda teyit edilmedi | Low | High | `VERIFIED_REPOSITORY` (F0-009-S1: `escalateSeverityAtomic()` dışa aktarılmıyor — modül-özel; repo genelinde tam olarak tek çağrı noktası var, aynı `prisma.$transaction` içinde, `upsertIncidentFromSignal`'da; `incidentId` her zaman o transaction'ın kendi `tx.securityIncident.upsert()` çağrısından dönen `upserted.id` — tenant-türetilmiş (`organizationId`+`clinicId`+`sourceRule`+kaynak) deterministik `incidentKey` ile bulunan/oluşturulan satır. Hiçbir route/job/başka çağıran bu fonksiyona harici veya rastgele bir `incidentId` sağlamıyor; tek HTTP yüzeyi olan `platformSecurityIncidents.ts` bu fonksiyona hiç dokunmuyor. Ayrıntı: [F0-009-S1 evidence](evidence/F0-009-S1_SECURITY_INCIDENT_TENANT_OWNERSHIP_EVIDENCE.md)) | Yok (bu spesifik raw-SQL vektörü için) — 5-modelin nullable `organizationId`/`clinicId` alanları için genel RLS tasarım kararı R-055'te ayrı takip ediliyor, bu bulguyla çözülmedi | F0-009-S1: 3 regresyon testi eklendi (`server/src/tests/securityIncident.test.ts` #55-57) — fonksiyonun dışa aktarılmadığını, tek çağrı noktasının `upserted.id` geçtiğini ve bir organizasyonun escalation'ının başka bir organizasyonun aynı-şekilli incident'ını asla etkilemediğini sabitliyor. Runtime kod değişmedi (secure-as-is sonucu) | Security/Incident Response | F0-009-S1 | MITIGATED (bu turda doğrulandı ve regresyon testiyle sabitlendi) | [F0-009-S1 evidence](evidence/F0-009-S1_SECURITY_INCIDENT_TENANT_OWNERSHIP_EVIDENCE.md); `server/src/tests/securityIncident.test.ts` §L (#55-57) |
| R-055 | 5 Prisma modeli (`SecuritySignalEvent`, `SecurityIncident`, `SecurityIncidentActivity` [inherited], `MessagingInboundEvent`, `CommunicationConsentConflictBucket`) `organizationId`/`clinicId` alanlarını kasıtlı olarak nullable taşıyor; naif bir RLS predicate'i bu satırları ya her bağlamdan gizler ya da beklenmedik şekilde eşleştirir — açık bir tasarım kararı gerektirir | Low | High | `DOCUMENTED_AMBIGUOUS` (F0-009 sınıflandırması: 4 model `ambiguous_nullable_tenant`, `SecurityIncidentActivity` kendi tenant kolonu olmadığı için `child_via_parent` olarak sınıflandırılmıştır ve aynı belirsizliği ebeveynden miras alır) | Policy-function tabanlı RLS yaklaşımı (nullable-tenant satırları yalnızca system/platform-admin bağlamında görünür kılan) | F0-009 PoC tasarımının §7.5'i bu vakayı özel olarak ele alıyor; gerçek karar gelecekteki RLS PoC'unda (F5) verilmeli | Security/Privacy/Messaging | F5 | OPEN | [tenant-rls-pgbouncer-poc-design.md §7.5](../architecture/tenant-rls-pgbouncer-poc-design.md#75-the-nullable-tenant-key-open-question); f0-009-tenant-model-inventory.json (4 `ambiguous_nullable_tenant` + 1 `child_via_parent` [inherited] girdisi) |

| R-056 | Evolution ve Instagram sağlayıcılı gelen webhook işleme (`whatsapp.ts`, `instagramWebhook.ts`), `inboundEventRetryJob.ts`'nin retry kapsamının dışında (yalnızca `channel='whatsapp', provider='meta_cloud_api'` destekleniyor); Instagram tarafında ayrıca HTTP 200 yanıtı işleme başlamadan önce gönderiliyor — geçici bir işleme hatası bu iki sağlayıcı için kalıcı ve sessiz veri kaybına yol açar | Medium | High | `UNVERIFIED` (kod kanıtıyla doğrulanan bir boşluk; telafi edici kontrol yok) | Sağlayıcı kapsamının `inboundEventRetryJob.ts`'ye genişletilmesi veya genel bir retry/dispatcher mekanizması | F0-010 PoC tasarımı bu boşluğu F010-001/F010-003 olarak sınıflandırdı ve deney 5/6/8'de doğrudan hedefliyor; dar kapsamlı düzeltme (mevcut retry job'un sağlayıcı listesinin genişletilmesi) herhangi bir kuyruk/outbox kararından bağımsız olarak mümkün | Messaging | F6 (dar kapsamlı düzeltme F1/F3 önerilir) | OPEN | [queue-outbox-poc-design.md §3](../architecture/queue-outbox-poc-design.md#3-current-repository-evidence--async-architecture-summary); f0-010-async-flow-inventory.json F010-001/F010-003 |
| R-057 | WhatsApp giden hatırlatma/ödeme mesajı gönderiminde (`jobs/reminders.ts`), sağlayıcı çağrısı başarılı olduktan sonra ve `SentMessage.status` güncellemesi öncesinde süreç çökerse satır kalıcı olarak `'prepared'` durumunda kalır; `MessagingInboundEvent`'in sahip olduğu türde bir "sıkışmış durum" kurtarma taraması bu tablo için yok, ayrıca hiçbir üretim kod yolu `SentMessage.status`'u `'delivered'`/`'read'` olarak ayarlamıyor | Medium | Medium | `UNVERIFIED` | Sıkışmış `'prepared'` satırları için mutabakat/kurtarma taraması; sağlayıcı-tarafı idempotency anahtarı kullanımı | F0-010 PoC tasarımı (deney 5/6/16) bu senaryoyu doğrudan hedefliyor | Messaging/Automations | F6 | OPEN | [queue-outbox-poc-design.md §7.11](../architecture/queue-outbox-poc-design.md#711-dispatcher-crash-after-side-effect-before-acknowledgment); f0-010-async-flow-inventory.json F010-004/F010-005 |
| R-058 | ADR-006/ADR-007'nin ihtiyaç duyduğu gerçek hacim/verim (throughput) projeksiyonları hiçbir üretim gözlemlenebilirliği olmadan üretilemez — ADR-012 hâlâ `DEFERRED`; bu, outbox/queue PoC'unun kendi ölçüm hedeflerinin (deney 20) ölçülmüş değil varsayılan bir taban çizgisiyle başlamak zorunda kalacağı anlamına gelir | Medium | Medium | `UNVERIFIED` | Log/metrik/trace/alarm standardı (ADR-012 kabulü ve uygulaması) | ADR-012 kanıt toplama görevi (henüz atanmadı) | Observability | F3 | OPEN | [queue-outbox-poc-design.md §11, §17](../architecture/queue-outbox-poc-design.md#11-kafka-trigger-objective-7) |
| R-059 | Uygulama-içi `Notification` satırları hiçbir retention/cleanup job'ının kapsamında değil (`dataRetentionCleanupJob.ts`'nin 7 kategorisinde yer almıyor) — sınırsız büyüme riski, ayrıca `Notification` oluşturma bir `GET` (okuma) endpoint'i içinde yan-etkili bir `upsert` olarak gerçekleşiyor (event-tabanlı değil, her poll'da yeniden hesaplanan bir materialized-view deseni) | Low | Medium | `UNVERIFIED` | `Notification` için retention kategorisi eklenmesi; event-tabanlı oluşturmaya geçiş değerlendirmesi | Gelecekteki bir retention-job genişletme görevi; F0-010 PoC tasarımı bu akışı outbox'ın en net "textbook" adayı olarak işaretledi (F010-018) ancak uygulamayı yetkilendirmiyor | Core Platform/Privacy | F6 (opsiyonel, Notification akışının outbox'a taşınmasıyla birlikte değerlendirilebilir) | OPEN | [queue-outbox-poc-design.md §5](../architecture/queue-outbox-poc-design.md#5-which-business-transitions-justify-outbox-semantics-answering-objective-12); f0-010-async-flow-inventory.json F010-018 |
| R-060 | İki mimari olarak farklı hasta/klinik veri export uygulaması bir arada var: senkron `patientPrivacy.ts` (F010-010) ve kuyruklu `clinicBulkExportWorker.ts` (F010-009, KVKK-HIGH-004) — kavramsal olarak benzer bir yetenek için tutarsız gecikme/güvenilirlik davranışı ve bakım yükü üretiyor | Low | Low | Belgelendi (bu görev, önceki bir görev tarafından tespit edilmemişti) | İkisinden birinin diğerinin deseniyle birleştirilmesi kararı | Gelecekteki, bu görevin kapsamı dışında bir tasarım kararı | Privacy/Export | F6 (opsiyonel) | OPEN | f0-010-async-flow-inventory.json F010-009 vs F010-010 (§6a/§6b) |
| R-061 | **[Reconciled 2026-07-21, PR #193 — two independent parallel efforts merged: KVKK-HIGH-008-F1-PBV-S1 (read-only feasibility analysis) and KVKK-HIGH-008-F1-PROD-BEHAVIORAL-SAFE-VERIFY (actual user-executed production evidence), both branched from the PR #188/#189 production-deployment-reconciliation baseline.]** KVKK-HIGH-008 legacy-consent-correction workflow was previously deployed without a dedicated runtime kill switch. PMVR and repository review confirmed that the workflow is manually invoked through authenticated API/UI actions by OWNER/ORG_ADMIN/CLINIC_MANAGER and that no startup, cron, scheduled-job, or worker-driven automatic execution path exists. Production deployment/schema availability is accepted as `VERIFIED_USER_SUPPLIED_PRODUCTION_EVIDENCE`. **PR #186 is now `MERGED` into `main` as commit `1a82302861139441f07ff5c0209728d3e711728f`.** It implements the platform-wide, default-disabled `PlatformSetting` gate `privacy.legacyConsentCorrection.runtimeEnabled`, keeps read/history routes available while blocking mutation, adds platform-admin GET/PATCH control, hides the frontend correction action while disabled, and records each successful setting change atomically in the dedicated `PlatformAdminAuditEvent` model. The previous-value read and setting update/audit insert occur in one transaction under a setting-scoped PostgreSQL advisory lock; the audit helper requires `Prisma.TransactionClient`; forced audit failure rolls the setting update back; no `SecuritySignalEvent` is written for this configuration change. **Important rollback constraint:** a blind application rollback to a pre-gate commit is unsafe because older code ignores the persisted setting and can re-expose the mutation route. Safe incident response is: set the toggle to `false` → keep the additive schema → redeploy only a gate-aware compatible commit or apply a temporary route/WAF block → forward-fix. | Low | Medium | `VERIFIED_REPOSITORY` for role gating, tenant scoping, transactional mutation logic, no automatic invocation path, default-false gate, atomic platform audit, concurrency serialization, and safe rollback semantics. `VERIFIED_USER_SUPPLIED_PRODUCTION_EVIDENCE` for the earlier deployment/schema state (PR #188/#189/F0-011-P3: `PlatformAdminAuditEvent` migration applied and schema-verified, table/PK/indexes/FK match design; `PlatformAdminAuditEvent`/`SecuritySignalEvent` rule `platform_admin.config_change.v1` both `0` rows; setting row absent, effective `false`; PM2/API/frontend/public-site health confirmed). **[2026-07-21, KVKK-HIGH-008-F1-PROD-BEHAVIORAL-SAFE-VERIFY, user-executed]:** this state was independently re-confirmed with a fresh before/after production verification pass — the `PlatformSetting` row remained **absent** both immediately before and immediately after all tests, with all 10 measured database values (setting row/value, `PlatformAdminAuditEvent` totals, `PatientLegacyConsentCorrection` total, `Patient.smsOptOut` aggregate counts/digest, `SecuritySignalEvent` count) identical — zero deltas; an unauthenticated `PATCH` to the settings endpoint independently returned `401` in production (log-confirmed, `responseTime 2ms`), matching the static-inspection proof already on file. | **[2026-07-21, KVKK-HIGH-008-F1-PBV-S1, read-only repository call-path analysis]:** confirmed `loadScopedPatient()` (`communicationPreferences.ts:567`) runs before the gate check (`:574`), so gaps 1-3 (disabled-route fail-closed behavior, exact response contract, read/history behavior while disabled, all live-observed) need a real production patient and remain **not authorized**; no `deletePlatformSetting`/unset function exists (`platformSettings.ts`), so the explicit-false `PlatformSetting` PATCH that would close gaps 4-8 (successful write, audit creation, actor attribution, previous/new values, zero-`SecuritySignalEvent`, audit-content safety) is technically feasible but would permanently move production from "row absent" to "row present with value `false`," with no supported audited path back — it is therefore classified **not a reversible test** and **remains `NOT AUTHORIZED`**; gap 9 (controlled activation, `runtimeEnabled: true`) remains **`CONTROLLED_ACTIVATION_NOT_AUTHORIZED`**, unchanged. **[2026-07-21, KVKK-HIGH-008-F1-PROD-BEHAVIORAL-SAFE-VERIFY]:** a real, non-activating production attempt at the authenticated read-only policy check (Test C1) and authenticated invalid-payload check (Test C3) was made — both are `BLOCKED / NOT EXECUTED`, not gaps left unattempted: the platform-admin login attempt returned `401 Invalid credentials`, and per this task's own stop condition the user correctly halted rather than retry with different credentials or any workaround. This is a credential-attempt rejection, not a finding about the policy/settings endpoints' own behavior — both remain open for a future retry with valid credentials, using the same non-activating command package. Test 4 (disabled mutation route via a real patient) remains **not safely executable** and permanently excluded from this verification method, consistent with PBV-S1's gaps 1-3 finding. No audit success row exists yet to inspect, so no claim is made about `actorPlatformAdminId`/previous-new-value attribution or zero-`SecuritySignalEvent`-on-toggle from a real action. | Perform a **non-activating** production verification of PR #186 that never changes `privacy.legacyConsentCorrection.runtimeEnabled`: read-only policy/settings `GET` verification; disabled-mutation-route fail-closed behavior; unauthorized-request rejection; invalid-payload rejection; confirmation that no `PlatformAdminAuditEvent` row is created by a rejected/disabled-route attempt; count/hash checks confirming no patient/consent data changed. **Done (2026-07-21):** the unauthenticated-rejection check and the before/after database-invariant checks are now independently production-verified. **Still needed:** retry the authenticated read-only policy check (Test C1) and authenticated invalid-payload check (Test C3) with valid platform-admin credentials — same non-activating command package, no new authorization required — since the prior attempt was blocked by a login credential rejection, not executed. **Separately, and explicitly not part of this verification:** (a) using a real, in-scope production patient to close gaps 1-3, and (b) the explicit-false `PlatformSetting` PATCH to close gaps 4-8, are each their own separate, explicit human authorization decisions (per PBV-S1's reversibility analysis above) — neither is authorized by this row. (c) A `PlatformSetting` PATCH that sets `runtimeEnabled` to `true`, even temporarily and even if immediately rolled back to `false`, is controlled activation, not verification — a `false → true → false` cycle is not authorized by this risk row and requires its own separate controlled-activation decision (named approver, maintenance window, rollback/cutback plan, monitoring plan, pre-agreed evidence-capture scope) before it may be proposed or scheduled. An explicit human accept/reject decision on this risk remains pending until authenticated coverage (and/or decisions a/b above) close. **[Updated 2026-07-22, PR [#197](https://github.com/MustafaBasol/DisKlinikCRM/pull/197), `965b0288248175174174cfa0da25730974d5c03b`, `MERGED` into `origin/main`]:** the retry of the authenticated read-only policy check (Test C1) and authenticated invalid-payload check (Test C3) named above is now packaged as "Package A" in [evidence/R061_REMAINING_AUTHENTICATED_VERIFICATION_PACKAGE.md](evidence/R061_REMAINING_AUTHENTICATED_VERIFICATION_PACKAGE.md) — a ready-to-run, non-activating command set for a human operator with valid platform-admin credentials. **This PR is documentation/command-preparation only; Package A has not been executed against production.** Test C1/C3 remain `BLOCKED / NOT EXECUTED`, unchanged. **[Updated 2026-07-22, KVKK-HIGH-006 post-DB-verification program reconciliation, unrelated-risk cross-reference only]:** KVKK-HIGH-006 (a distinct risk, R-071) has completed its own combined post-merge source verification and disposable-PostgreSQL DB-backed verification and is now `IMPLEMENTATION_MERGED — SOURCE_AND_DB_VERIFIED — PRODUCTION_VERIFICATION_PENDING`, proceeding toward its own controlled production deployment. **This does not close, mitigate, or otherwise change R-061** — KVKK-HIGH-006's DB verification provides no evidence toward R-061's own closure criteria (R-061 gates a separate KVKK-HIGH-008 feature). [evidence/KVKK-HIGH-006-PRODUCTION_DEPLOYMENT_GATE.md](evidence/KVKK-HIGH-006-PRODUCTION_DEPLOYMENT_GATE.md) §7 records that Package A's safe/read-only steps (baseline capture, log filtering, health/PM2 recheck, cleanup) may run in the same production window as that deployment for operator efficiency, but its authenticated steps (login, Test C1, Test C3) must remain a separately-delimited sequence with their own stop conditions, requiring a platform-admin session distinct from any role used by KVKK-HIGH-006's own smoke plan. **[Updated 2026-07-22, PR #205 reconciliation with merged PR #204, cross-reference only]:** PR #204 (a KVKK-HIGH-006 test-only fix, unrelated to this risk) merged and was reconciled into the KVKK-HIGH-006 deployment-gate branch; it does not touch R-061, Package A, or any KVKK-HIGH-008 file, and does not change this row's status. **R-061 remains `OPEN`** — unaffected. **[Updated 2026-07-22, KVKK-HIGH-006 production deployment and smoke verification, cross-reference only]:** KVKK-HIGH-006 (R-071) has now completed production deployment and its minimum mandatory production-safe smoke set (S1/S2/S3/S6/S7, all PASS) and is proposed `CLOSED` — see [evidence/KVKK-HIGH-006-PRODUCTION_DEPLOYMENT_AND_SMOKE_VERIFICATION.md](evidence/KVKK-HIGH-006-PRODUCTION_DEPLOYMENT_AND_SMOKE_VERIFICATION.md). **This does not close, mitigate, or otherwise change R-061** — that verification used clinic-level (OWNER/ORG_ADMIN-equivalent) synthetic fixtures, not the platform-admin session R-061's own Package A closure criteria require, and provides no evidence toward R-061's closure. **[Updated 2026-07-23, R-061 Package A production execution]:** Package A was executed by an authorized operator against production. Platform-admin login returned HTTP `200`; Test C1 returned HTTP `200` with `{"runtimeEnabled":false}`; Test C3 returned HTTP `400` with `{"error":"runtimeEnabled must be a boolean"}`; final before/after invariants and patient-state digest were identical; health and PM2 restart counts remained stable; temporary session material was cleaned. **Package A result: `PASS — AUTHENTICATED PRODUCTION VERIFIED`; Test C1/Test C3 are closed. R-061 remains `OPEN`** because gaps 1–3, gaps 4–8, and controlled activation remain separately gated and unauthorized. **[Updated 2026-07-23, R-061 residual-safe-reset production deployment and verification]:** implementation commit `b86001779fbbc2cfdcf76b84568d3d960850a761` (branch `feature/r061-residual-safe-closure`) added `unsetPlatformSetting()` and `DELETE /api/platform/privacy/legacy-consent-correction/settings`, and both were deployed to production at SHA `8906e66af5169220a4aed48fe4cfea8524976fb8`. **Supersession note: the historical "no unset/delete API exists" wording recorded elsewhere in this row (and in [KVKK-HIGH-008-F1-PBV-S1](evidence/KVKK-HIGH-008-F1-PBV-S1_SAFE_BEHAVIORAL_PRODUCTION_VERIFICATION_FEASIBILITY.md) and Package B item B.2 of [R061_REMAINING_AUTHENTICATED_VERIFICATION_PACKAGE.md](evidence/R061_REMAINING_AUTHENTICATED_VERIFICATION_PACKAGE.md)) was correct at its own capture time and is preserved, not deleted — it is now superseded**: a supported, audited unset/delete path exists in code and in production. An operator verified, post-deployment: unauthenticated `GET` policy (local + public) and unauthenticated `DELETE` settings both return HTTP `401`; the setting row remains absent/`null` with zero audit rows. A single normal platform-admin login attempt returned HTTP `401` — per the existing stop condition, no second credential was attempted and no authenticated `GET`/`PATCH`/`DELETE` request was sent. **Result: `PASS WITH BLOCKED AUTHENTICATED RESIDUAL`** — deployment/health/unauthenticated-protection/default-invariant all `PASS`; authenticated `PATCH`/`DELETE`/idempotency and production audit-write verification (gaps 4-8) remain `BLOCKED / NOT EXECUTED`, now because the authenticated chain has not been run, not because the reset path is absent. Gaps 1-3 and controlled activation remain `NOT AUTHORIZED / NOT EXECUTED`, unchanged. **R-061 remains `OPEN`.** Full evidence: [evidence/R061_RESIDUAL_SAFE_RESET_PRODUCTION_VERIFICATION.md](evidence/R061_RESIDUAL_SAFE_RESET_PRODUCTION_VERIFICATION.md). **[Updated 2026-07-24, R-061 authenticated production safe-reset verification, CLOSED]:** the authenticated-login blocker is resolved (production account `admin@noramedi.com` recovered via the PR #221 audited operator CLI). The full non-activating chain named above was then executed against production: `GET policy` (200, `false`) → `PATCH {runtimeEnabled:false}` (200, `false`) → `DELETE` (200, `removed:true`) → idempotent second `DELETE` (200, `removed:false`) → final `GET policy` (200, `false`). Final `PlatformSetting` row count `0`; two audit rows (`platform_setting.updated`, `platform_setting.reset`), both attributed to the authenticated admin; the idempotent `DELETE` wrote no extra audit row. `runtimeEnabled=true` was never submitted. This closes gaps 4-8 and the explicit human accept/reject decision this risk always called for. Gaps 1-3 (real-patient behavioral verification) remain separately unauthorized and unchanged — they are not required for this risk's own closure, which is scoped to the runtime kill switch's existence and authenticated verified behavior, not to real-patient exercise of the disabled mutation route. Full evidence: [evidence/R061_AUTHENTICATED_PRODUCTION_SAFE_RESET_VERIFICATION.md](evidence/R061_AUTHENTICATED_PRODUCTION_SAFE_RESET_VERIFICATION.md). | Privacy/Consent | F0-011-P1 → KVKK-HIGH-008-PMVR/PR #185 → KVKK-HIGH-008-F1/PR #186 `MERGED` → production deploy/disabled-state verification (PR #188 `MERGED`, PR #189 reconciling, done) → non-activating behavioral verification, partial (KVKK-HIGH-008-F1-PBV-S1 feasibility analysis done; KVKK-HIGH-008-F1-PROD-BEHAVIORAL-SAFE-VERIFY production evidence done, partial) → Package A authenticated policy-read/invalid-payload verification, `PASS` (2026-07-23) → residual-safe-reset mechanism deployed (2026-07-23) → authenticated policy and safe-reset verification completed, gaps 4-8 completed, R-061 `CLOSED` (2026-07-24) → gaps 1-3 (real-patient behavioral verification) remain a separate, unauthorized residual item, not an R-061 closure requirement → controlled activation remains separately unauthorized, not required for and not closed by R-061's closure | CLOSED — **(corrected 2026-07-25, F0-013-R1: this cell's leading status token previously read `OPEN`, contradicting this same cell's own later `R-061 status: `CLOSED`` entry dated 2026-07-24 and this program's `NORAMEDI_MASTER_TRACKER.md`/`CURRENT_PHASE.md`, which had already recorded R-061 `CLOSED`; corrected here to the current authoritative status. The historical `OPEN` narrative below is preserved unchanged as superseded history, not deleted or rewritten.)** production deployment, migration application, health, and effective-default-false behavior are verified, independently re-confirmed by a fresh production pass with zero database-invariant deltas; unauthenticated-rejection behavior is independently production-verified (`401`). Authenticated policy-read and invalid-payload behavior (Test C1/C3) is now `PASS — AUTHENTICATED PRODUCTION VERIFIED`: C1 returned HTTP `200` with `{"runtimeEnabled":false}` and C3 returned HTTP `400` with `{"error":"runtimeEnabled must be a boolean"}`. Patient-scoped behavioral checks (gaps 1-3) remain unverified and not authorized because the runtime gate executes after patient lookup and cannot be exercised with a nonexistent identifier. A platform-only explicit-false setting write followed by the supported audited `DELETE` reset is now reversible and already authorized as a non-activating verification chain; however, the chain remains `BLOCKED / NOT EXECUTED` in production because the single normal platform-admin login attempt returned HTTP `401`, so no authenticated setting request or production audit write was made. Controlled activation (gap 9) remains undecided/not authorized. **(2026-07-23) Package A has now been executed against production and passed; Test C1/Test C3 are closed. This narrows the residual evidence gap but does not change the `OPEN` status because the separately gated Package B items remain unexecuted and unauthorized.** **(2026-07-23) The residual-safe-reset mechanism (`unsetPlatformSetting`/`DELETE`) is now merged and deployed to production (SHA `8906e66af5169220a4aed48fe4cfea8524976fb8`), resolving the prior "no unset/delete API" limitation in code and in production. Unauthenticated protection and default database invariants are verified `PASS`; the authenticated `PATCH`/`DELETE`/idempotency/audit chain remains `BLOCKED / NOT EXECUTED` because a single platform-admin login attempt returned `401`. Verdict: `PASS WITH BLOCKED AUTHENTICATED RESIDUAL`. Status remains `OPEN`.** **(2026-07-24) The authenticated login blocker is resolved** (production account `admin@noramedi.com` recovered via the PR #221 audited operator CLI, `action: platform_admin.password_recovered`, `outcome: success`). The full authenticated, non-activating chain named as this risk's own remaining closure requirement has now been executed against production: `GET` policy (200, `runtimeEnabled:false`) → `PATCH {runtimeEnabled:false}` (200) → `DELETE` (200, `removed:true`) → idempotent second `DELETE` (200, `removed:false`) → final `GET` policy (200, `runtimeEnabled:false`). Final `PlatformSetting` row count `0`; both a `platform_setting.updated` and a `platform_setting.reset` `PlatformAdminAuditEvent` row were written and correctly attributed to the authenticated administrator; the idempotent second `DELETE` wrote no extra audit row. `runtimeEnabled=true` was never submitted at any point, and no patient/consent/tenant data was touched. **Verdict: `PASS — AUTHENTICATED PRODUCTION SAFE-RESET VERIFIED`. R-061 status: `CLOSED`.** This closes gaps 4-8 and satisfies the explicit human accept/reject decision this risk has called for since it was opened: reject the original no-kill-switch design; require and implement a runtime default-deny kill switch (done, PR #186); verify it safely in production without enabling the workflow (done, this update). This closure does not extend to legal KVKK compliance approval, an overall KVKK-baseline-stable declaration, freeze-boundary condition 4 (separate F0-011-P2 evidence, unaffected), or any other risk row; gaps 1-3 (real-patient behavioral verification) remain separately unauthorized and unchanged, and are not required for this risk's own closure. | [evidence/F0-011-P1_KVKK_HIGH008_ACTIVE_WORK_BASELINE.md §8](evidence/F0-011-P1_KVKK_HIGH008_ACTIVE_WORK_BASELINE.md), [evidence/KVKK-HIGH-008-PMVR_POST_MERGE_READINESS_EVIDENCE.md](evidence/KVKK-HIGH-008-PMVR_POST_MERGE_READINESS_EVIDENCE.md), [evidence/F0-011-P2_KVKK_HIGH007_HIGH008_ROLLBACK_TENANT_VERIFICATION.md](evidence/F0-011-P2_KVKK_HIGH007_HIGH008_ROLLBACK_TENANT_VERIFICATION.md), [evidence/F0-011-P3_KVKK_HIGH008_F1_PRODUCTION_DEPLOYMENT_EVIDENCE.md](evidence/F0-011-P3_KVKK_HIGH008_F1_PRODUCTION_DEPLOYMENT_EVIDENCE.md), [evidence/KVKK-HIGH-008-F1_PRODUCTION_DEPLOYMENT_VERIFICATION.md](evidence/KVKK-HIGH-008-F1_PRODUCTION_DEPLOYMENT_VERIFICATION.md), [evidence/KVKK-HIGH-008-F1-PBV-S1_SAFE_BEHAVIORAL_PRODUCTION_VERIFICATION_FEASIBILITY.md](evidence/KVKK-HIGH-008-F1-PBV-S1_SAFE_BEHAVIORAL_PRODUCTION_VERIFICATION_FEASIBILITY.md), [evidence/KVKK-HIGH-008-F1_PRODUCTION_SAFE_BEHAVIORAL_VERIFICATION.md](evidence/KVKK-HIGH-008-F1_PRODUCTION_SAFE_BEHAVIORAL_VERIFICATION.md), [KVKK_HIGH008_FREEZE_BOUNDARY.md §4, §6, §7, §7.1, §7.3, §8](KVKK_HIGH008_FREEZE_BOUNDARY.md), [LAUNCH_GATES.md §2.G](LAUNCH_GATES.md#g-feature-activation-readiness), [PR #185](https://github.com/MustafaBasol/DisKlinikCRM/pull/185), [PR #186](https://github.com/MustafaBasol/DisKlinikCRM/pull/186), [PR #187](https://github.com/MustafaBasol/DisKlinikCRM/pull/187), [PR #188](https://github.com/MustafaBasol/DisKlinikCRM/pull/188), [PR #189](https://github.com/MustafaBasol/DisKlinikCRM/pull/189), [evidence/R061_REMAINING_AUTHENTICATED_VERIFICATION_PACKAGE.md](evidence/R061_REMAINING_AUTHENTICATED_VERIFICATION_PACKAGE.md) (PR #197, Package A prepared and subsequently executed), [evidence/R061_PACKAGE_A_AUTHENTICATED_PRODUCTION_VERIFICATION.md](evidence/R061_PACKAGE_A_AUTHENTICATED_PRODUCTION_VERIFICATION.md) (2026-07-23, Package A production result: PASS), [evidence/R061_RESIDUAL_SAFE_RESET_PRODUCTION_VERIFICATION.md](evidence/R061_RESIDUAL_SAFE_RESET_PRODUCTION_VERIFICATION.md) (2026-07-23, residual-safe-reset deployment and verification: PASS WITH BLOCKED AUTHENTICATED RESIDUAL), [evidence/R061_PLATFORM_ADMIN_PASSWORD_RECOVERY_RUNBOOK.md](evidence/R061_PLATFORM_ADMIN_PASSWORD_RECOVERY_RUNBOOK.md) (PR #221, audited operator password-recovery CLI), [evidence/R061_AUTHENTICATED_PRODUCTION_SAFE_RESET_VERIFICATION.md](evidence/R061_AUTHENTICATED_PRODUCTION_SAFE_RESET_VERIFICATION.md) (2026-07-24, final authenticated production safe-reset verification: PASS — R-061 CLOSED) |
| R-062 | **[Updated 2026-07-20 after PR #185 PMVR reconciliation, PR #186 merge, F0-011-P2 independent verification, and PR #188/#189 production-deployment reconciliation.]** This risk originally tracked migration-order/dependency uncertainty between KVKK-HIGH-007 (`20260719120821_kvkk_high007_consent_reconciliation`) and KVKK-HIGH-008 (`20260719155318_kvkk_high008_legacy_consent_correction`). That narrow ordering component is now resolved: user-supplied production evidence reports both migrations applied in correct `_prisma_migrations` order with the schema up to date; PMVR independently rehearsed clean and upgrade paths on disposable PostgreSQL; F0-011-P2 independently repeated the pre-HIGH-007 → HIGH-007 → HIGH-008 apply sequence and confirmed correct ordering and clean Prisma migration-engine replay behavior. PR #186 adds the later, additive-only `20260720180000_add_platform_admin_audit_event` migration; it has no data/FK dependency on the HIGH-007/HIGH-008 migrations and was validated on disposable PostgreSQL before merge. **Raw SQL statement-level idempotency was not proven**; the verified re-run behavior is Prisma's `_prisma_migrations`-based no-op behavior. F0-011-P2 additionally verified the rollback-tier distinction: application cutback remains compatible while additive tables are retained; physical table drop is data-loss-free only while the new tables are empty; once correction/conflict/audit rows exist, dropping them destroys newly created operational/KVKK evidence. Manual DDL rollback can also leave actual schema state inconsistent with `_prisma_migrations`; that separate operational risk is tracked in R-070. Rollback and production tenant/audit verification are tracked in R-046 and must not be duplicated here. | Low | High | `VERIFIED_USER_SUPPLIED_PRODUCTION_EVIDENCE` for production application/order of HIGH-007/HIGH-008; `VERIFIED_DISPOSABLE_REHEARSAL` from PMVR and F0-011-P2 for migration ordering, additive-schema compatibility, Prisma replay behavior, and cross-org/cross-clinic isolation. PR #186's audit migration is additive and merged into `main`, and is now confirmed production-applied (see below). | Raw SQL idempotency remains unproven. **[Updated 2026-07-20, PR #188/#189]** PR #186's audit migration (`20260720180000_add_platform_admin_audit_event`) is now confirmed production-applied and schema-verified (table/PK/indexes/FK match design) — the "not yet verified as applied in production" gap this row previously listed is resolved. Physical rollback after data creation is destructive, and manual reverse-DDL can desynchronize schema state from `_prisma_migrations`; those residuals are owned by R-046 and R-070 and are unaffected by this update. | Treat the migration-ordering component as mitigated. Do not physically roll back additive migrations by default; retain schema during application cutback and forward-fix. ~~During PR #186 production deployment, verify the audit migration in `_prisma_migrations`, verify the `PlatformAdminAuditEvent` table/indexes/FK, and confirm schema status.~~ **Done (2026-07-20, PR #188, independently reconciled by PR #189)** — see evidence links below. Any exceptional physical rollback requires backup/export, approved reverse DDL, schema-and-ledger reconciliation, post-checks, and DBA/program approval. | Privacy/Consent | F0-011-P1 → KVKK-HIGH-008-PMVR/PR #185 → F0-011-P2 → PR #186 production migration verification (PR #188 `MERGED`, PR #189 reconciling, done) | MITIGATED — original migration-ordering component resolved; audit migration now confirmed production-applied and schema-verified; residual rollback/ledger risks tracked in R-046 and R-070 | [evidence/F0-011-P1_KVKK_HIGH008_ACTIVE_WORK_BASELINE.md §5, §9](evidence/F0-011-P1_KVKK_HIGH008_ACTIVE_WORK_BASELINE.md), [evidence/KVKK-HIGH-008-PMVR_POST_MERGE_READINESS_EVIDENCE.md](evidence/KVKK-HIGH-008-PMVR_POST_MERGE_READINESS_EVIDENCE.md), [evidence/F0-011-P2_KVKK_HIGH007_HIGH008_ROLLBACK_TENANT_VERIFICATION.md](evidence/F0-011-P2_KVKK_HIGH007_HIGH008_ROLLBACK_TENANT_VERIFICATION.md), [evidence/F0-011-P3_KVKK_HIGH008_F1_PRODUCTION_DEPLOYMENT_EVIDENCE.md](evidence/F0-011-P3_KVKK_HIGH008_F1_PRODUCTION_DEPLOYMENT_EVIDENCE.md), [evidence/KVKK-HIGH-008-F1_PRODUCTION_DEPLOYMENT_VERIFICATION.md](evidence/KVKK-HIGH-008-F1_PRODUCTION_DEPLOYMENT_VERIFICATION.md), [KVKK_HIGH008_FREEZE_BOUNDARY.md §6, §7.1, §7.3, §8](KVKK_HIGH008_FREEZE_BOUNDARY.md), [KVKK_ARCHITECTURE_FREEZE_BOUNDARY.md §5](KVKK_ARCHITECTURE_FREEZE_BOUNDARY.md), [PR #185](https://github.com/MustafaBasol/DisKlinikCRM/pull/185), [PR #186](https://github.com/MustafaBasol/DisKlinikCRM/pull/186), [PR #187](https://github.com/MustafaBasol/DisKlinikCRM/pull/187), [PR #188](https://github.com/MustafaBasol/DisKlinikCRM/pull/188), [PR #189](https://github.com/MustafaBasol/DisKlinikCRM/pull/189) |
| R-063 | Transient HTTP 502 (~5s) observed on the first health check after PM2 reload of `noramedi-api` during KVKK-HIGH-008 deployment (operator-reported, PR #184) — the API's startup command is `npm run start` → `npx prisma generate && tsx src/index.ts`; the process only begins listening (`Server is running on 127.0.0.1:5000`) after Prisma Client generation completes, so PM2/Nginx routes traffic to a not-yet-listening process for a short window on every reload. All subsequent health checks reportedly returned 200; API restart count reportedly stable (16, 0 unstable). | Medium | Low | `PARTIALLY_VERIFIED` (operator-reported deployment-log description, 2026-07-20: one 502 ~5s post-reload, then consistent 200s — no raw log excerpt committed to the repository) | No readiness gate between PM2 reload and Nginx/traffic routing; `prisma generate` runs on every start instead of at build time | Pre-generate the Prisma Client at build/deploy time (not at process start), and/or add a PM2 `wait_ready`/health-gated reload so traffic is not routed until the process signals ready | Deployment/Reliability | F3 (or an isolated pre-F3 hotfix) | Not a KVKK-HIGH-008 blocker | OPEN — **(kanıt güçlendirildi 2026-08-12, F3-PROD-002; durum değişmedi, remediation uygulanmadı.)** İki yeni unsur: **(1)** pencerenin **depoya işlenmiş ilk ham ölçümü** — bu satır daha önce açıkça "no raw log excerpt committed to the repository" diyordu. F3-PROD-002'nin deploy koşusunda step 7'nin local healthcheck'i `000`/`000`/`000` döndürdü ve **4.** denemede `200` aldı; API deploy başlangıcından ≈**19 sn** sonra sağlıklı oldu. Bu, scriptin kendi 12-deneme/5-sn bütçesi içinde **sınırlı başlangıç toparlanmasıdır** — bir başarısızlık **değildir**, ve `ecosystem.config.cjs:25-33`'ün ilk ecosystem-kaynaklı `startOrReload`'un tam restart'a düşeceği yönündeki kendi operasyonel notuyla birebir uyumludur; **zero-downtime olarak adlandırılmamalıdır**. **(2)** Kök nedenin daha keskin ifadesi: `prisma generate` yalnızca deploy step 4'te değil, `server/package.json:10-11`'deki `start`/`start:worker` scriptlerinin **içinde** yaşadığı için, deploy scriptinin **`--skip-generate` bayrağı onu bastıramaz** — F3-PROD-002 `--skip-generate` ile koşmasına rağmen her iki süreç başlangıcında da Prisma Client üretimi gerçekleşti. Bu, bu satırın **kendi adlandırdığı** remediation'ın ("pre-generate the Prisma Client at build/deploy time, not at process start") doğrudan gerekçesidir. Ayrı bir runtime/deployment görevi gerektirir; **F3-PROD-002 bunu uygulamadı ve satırı kapatmadı**. Ayrıca operasyonel olarak kayda değer: 19 saniyelik kesinti hiçbir otomatik tespit üretmedi — bu **R-074**'ün maliyetidir | Operator-reported production evidence, 2026-07-20 deployment pass; `server/package.json` `start` script; deploy log timestamps; [evidence/F3-PROD-002_DEPLOY_SCRIPT_EXECUTION_VERIFICATION.md](evidence/F3-PROD-002_DEPLOY_SCRIPT_EXECUTION_VERIFICATION.md) §6/§7/§9.5 |
| R-064 | Frontend `npm install` reports 3 moderate, 1 high, and 1 critical vulnerability in the dependency tree (operator-reported, PR #184). No automatic fix (`npm audit fix`/`--force`) has been run — the exact affected packages, whether they are reachable from production code paths, and whether a fix requires a breaking major-version bump are not yet determined by this pass. | Medium | High | `UNVERIFIED` (count reported, dependency paths/exploitability not yet reviewed) | A dedicated `npm audit`/dependency-path review has not been performed | Run `npm audit` (read-only) to enumerate the exact advisories and affected packages, trace each to its production reachability, and propose fixes individually (patch/minor bump vs. major bump vs. accepted-risk) — do not run `npm audit fix --force` blindly, since a forced major bump can break the build | Frontend/Dependencies | Isolated dependency-review PR (separate from any KVKK work) | Not a KVKK-HIGH-008 blocker | OPEN | Operator-reported production evidence, 2026-07-20 (`npm install` output); no fix applied |
| R-065 | React `act(...)` warnings emitted by Vitest for `LegacyConsentCorrectionModal` and `CommunicationPreferencesPanel` during the KVKK-HIGH-008 frontend test run (operator-reported, PR #184; independently reproduced by KVKK-HIGH-008-PMVR Phase B1's own test run — 12/12 passed with the same warnings observed). Did not fail any test, but `act()` warnings typically indicate a state update happening outside React's test-managed update batching, which can mask a real async-timing bug in CI-only or slower-CI conditions. | Low | Low | `VERIFIED_REPOSITORY` (warnings observed in test output by both PR #184 and this branch's own Phase B1 run, §7 of the PMVR evidence; tests still pass) | Root cause of the warning (which state update, which effect/async callback) not yet diagnosed | Diagnose and wrap the offending state updates in `act()`/`waitFor()` in the two named test files; re-run to confirm the warning is gone, not just silenced | Frontend/Testing | Isolated frontend-test-hygiene PR | Not a KVKK-HIGH-008 blocker | OPEN | Operator-reported production evidence, 2026-07-20 (Vitest run output); independently reproduced, [KVKK-HIGH-008-PMVR evidence §7](evidence/KVKK-HIGH-008-PMVR_POST_MERGE_READINESS_EVIDENCE.md); `src/components/CommunicationPreferencesPanel.vitest.test.tsx` and the `LegacyConsentCorrectionModal` test suite |
| R-066 | WhatsApp AI conversation/NLU services parse LLM JSON output with a hand-rolled `parseJsonObject()` (identical implementation in `server/src/services/whatsappConversationAgent.ts:44-54` and `server/src/services/whatsappStepAwareNlu.ts:317-327`): strips code fences, `JSON.parse`s, and on failure retries by slicing between the first `{` and last `}`, throwing if no braces are found. Callers (`whatsappConversationAgent.ts:288`, `whatsappStepAwareNlu.ts:432`) are wrapped in outer `try/catch` (`resolveWhatsAppConversationAgentDecision` at `whatsappConversationAgent.ts:301-317`; `resolveStepAwareWhatsAppIntent` at `whatsappStepAwareNlu.ts:446-452`) that fall back to a rule-based decision on any parse error — so a malformed-JSON failure does not crash the request, but is only logged via `console.error`, with no structured/metric alerting and no schema (zod) validation of the parsed shape before it reaches `normalizeWhatsAppAgentDecision()` (`server/src/services/whatsappAgentSchema.ts:135`, a manual allowlist normalizer, not a schema validator). | Medium | Medium | `PARTIALLY_VERIFIED` (fallback-on-failure exists and was confirmed by direct code read; no schema validation, no metrics, no regression test specifically targeting malformed-JSON responses was found) | No zod/schema validation of the parsed AI JSON shape before normalization; no metric/counter for parse-failure rate; no regression test simulating a malformed-JSON provider response | Add zod schema validation immediately after `parseJsonObject()` in both files, emit a counted/labeled metric or `OperationalEvent` on parse failure (not just `console.error`), and add regression tests that feed each function deliberately malformed JSON | Messaging/AI | Isolated PR, one per file or combined if genuinely coupled (do not bundle with unrelated messaging work) | Not a KVKK-HIGH-008 blocker | OPEN | Repository code-review finding (PR #184), 2026-07-20; `server/src/services/whatsappConversationAgent.ts:44-54,288,301-317`; `server/src/services/whatsappStepAwareNlu.ts:317-327,432,446-452`; `server/src/services/whatsappAgentSchema.ts:135` |
| R-067 | Production logs reportedly show a `pg` deprecation warning: "client.query() called while the client is already executing a query." (operator-reported, PR #184). A repository-wide check found no `new Client(` usage in `server/src` (only `new Pool(` — `server/src/db.ts:14` via the `PrismaPg` adapter, and a separate second pool in `server/src/utils/activity.ts:6-11`) — the reported failure mode was not reproduced from current source, meaning either it originates in a dependency's own internal client usage (e.g. inside a driver/adapter), is intermittent/environment-specific, or needs a wider trace than this pass performed. Recorded as a forward-looking `pg@9` compatibility/concurrency risk regardless of root cause. | Low | Medium | `UNVERIFIED` (warning reported in production logs; root cause not isolated to application code in this pass) | No instrumentation traces which caller/connection triggers the warning; two independent `Pool` instances (`db.ts`, `activity.ts`) is itself an unreconciled architecture question (also see R-008) | Capture a full stack trace the next time the warning fires (enable `NODE_OPTIONS=--stack-trace-limit` or wrap the relevant pg internals temporarily), reconcile the two independent `Pool` instances, and re-test against a `pg@9` prerelease if available | Database/Reliability | Isolated investigation PR (no fix without root-cause trace) | Not a KVKK-HIGH-008 blocker | OPEN | Operator-reported production evidence, 2026-07-20 (production log); `server/src/db.ts:14`; `server/src/utils/activity.ts:6-11` |
| R-068 | Historical `node-cron` missed-execution warnings reportedly observed in production logs (operator-reported, PR #184). All 9 job files in `server/src/jobs/` use `cron.schedule` + `withJobLock` (a DB-backed lease, e.g. `server/src/jobs/dataRetentionCleanupJob.ts:419`, `reminders.ts:636`, `metaTemplateSyncJob.ts:217`) which prevents *overlapping* runs, but no job file or `server/src/utils/jobLock.ts` has any event-loop-delay, run-duration, or missed-execution-count instrumentation — so a missed tick (e.g. from event-loop blocking) is not currently observable except via ad hoc log grep. | Medium | Medium | `VERIFIED_REPOSITORY` (overlap-prevention confirmed present via `withJobLock`; no duration/delay/miss metrics found anywhere in `server/src/jobs/` or `jobLock.ts`) | No event-loop-delay, job-duration, or missed-execution-count metrics/alerting | Add event-loop-delay monitoring (e.g. `perf_hooks.monitorEventLoopDelay`), per-job duration metrics, and a missed-tick counter/alert, wired into the same `OperationalEvent`/alerting path used elsewhere | Jobs/Observability | Isolated observability PR (do not bundle with R-063 or any KVKK work) | Not a KVKK-HIGH-008 blocker | OPEN | Operator-reported production evidence, 2026-07-20 (production log); `server/src/jobs/*.ts` (9 files); `server/src/utils/jobLock.ts` |
| R-069 | Appointment reminder sends reportedly fail when no approved Meta WhatsApp template exists for the target clinic/use-case (operator-reported, PR #184) — an operational messaging-readiness/configuration gap, not a code defect (Meta requires pre-approved templates for business-initiated WhatsApp conversations). | Low | Medium | `UNVERIFIED` (failure mode reported in production logs; scope of affected clinics not enumerated by this pass) | No per-clinic template-approval-status tracking/alerting surfaced to operators before a reminder send is attempted | Add a pre-send template-approval check (or a clear operator-facing status/alert) so a missing-template failure is visible before patients are affected, and document the Meta template-approval operational runbook | Messaging/Operations | Isolated operational-configuration task (not a code-architecture change) | Not a KVKK-HIGH-008 blocker | OPEN | Operator-reported production evidence, 2026-07-20 (production log) |
| R-071 | **[New, 2026-07-21, KVKK-HIGH-006 reconciliation]** 10 files in `server/src` (`services.ts`, `postTreatment.ts`, `messages.ts`, `insuranceProvisions.ts`, `inventory.ts`, `paymentPlans.ts`, `dentalChart.ts`, `appointmentRequests.ts`, `reports.ts`, `middleware/planLimits.ts`) contain 47 live-runtime authorization/data-scope usages of `req.user!.clinicId` directly, instead of the codebase's own centralized `validateAndGetClinicIdScope`/`validateAndGetScope` helper (`server/src/utils/clinicScope.ts:87,210`), which 6 of these 10 files already import and use correctly on other routes in the same file. `req.user.clinicId` is server-resolved at authentication time and is never client-supplied per request, so no cross-organization data leak was found; the confirmed risk is narrower: OWNER/ORG_ADMIN multi-branch users are silently restricted to their resolved clinic on these specific routes (functional access-completeness narrowing, not a leak), and writes on two routes (`appointmentRequests.ts:192`, and by the same pattern the mutation routes in `dentalChart.ts`/`insuranceProvisions.ts`/`messages.ts`/`postTreatment.ts`) are stamped with that resolved `clinicId` with no re-validation against an intended/selected branch — a data-mis-attribution risk on PHI-linked records, not attacker-controlled cross-tenant access. `reports.ts:405` is the one route in that file with no explicit multi-clinic selector at all. | Medium | Medium | `VERIFIED_REPOSITORY` (full enclosing-function read of `reports.ts:405` and `appointmentRequests.ts:152/192/346`; pattern-consistent grep evidence for the remaining 8 files — sibling routes in the same files correctly call the centralized helper) | ~~Migration of the 10 files' flagged routes to `validateAndGetClinicIdScope`/`validateAndGetScope` — **occurrence-by-occurrence classification itself is now DONE (see 2026-07-21 KVKK-HIGH-006-S2 update below); the remaining missing control is implementation, not classification.**~~ **[Superseded 2026-07-22]:** implementation is also now DONE — all 10 files migrated to the centralized helpers across PR #194/#195/#196/#198/#199 (see below). The remaining missing control is **independent post-merge verification**: none of the six merged PRs carries disposable-PostgreSQL-DB-backed or production test evidence (each batch's own evidence document reports local/disposable in-process simulated regression tests only, no live Postgres available in-task). | **[Updated 2026-07-21, KVKK-HIGH-006-S2, `AGENT_COMPLETED`, documentation-only]** S2 reproduced and independently re-verified the full occurrence set (93 raw / 67 non-test / 17 files, 4 more files than previously named — `attachments.ts`/`gdprExport.ts`/`labOrders.ts`/`clinicBulkExport.ts`, all resolving to false-positive/comment-only, zero remediation needed), classified every occurrence, reconciled the remediation-required count to exactly **47** (resolving reports.ts from an ambiguous "1 + 1 manual-review" to a confirmed **2**: `:73` raw-SQL org-wide scope-translation gap, `:405` no-selector-at-all), independently re-verified (not merely accepted) the 10-item safe-non-authorization bucket, and confirmed **zero** occurrences rise to TENANT_ISOLATION_RISK/CROSS_ORGANIZATION_RISK anywhere in the 93-match inventory — every confirmed defect is access-narrowing or a consistency gap, not unauthorized cross-clinic/cross-org data exposure. Defined a 4-batch remediation plan (Batch 1: `reports.ts`/`appointmentRequests.ts`/`dentalChart.ts`, highest PHI/clinical/reporting priority; Batch 2: `paymentPlans.ts`/`inventory.ts`/`insuranceProvisions.ts`; Batch 3: `messages.ts`/`postTreatment.ts`/`services.ts`; Batch 4: `middleware/planLimits.ts` — gated on a prerequisite product decision on no-plan-organization quota semantics — plus the already-safe `dashboard.ts`/`organizationBranches.ts`/`organizationWhatsApp.ts`), each with a 12-case test plan. **No code changed, no test run.** Full detail: [evidence/KVKK-HIGH-006-S2_REQ_USER_CLINIC_ID_OCCURRENCE_CLASSIFICATION.md](evidence/KVKK-HIGH-006-S2_REQ_USER_CLINIC_ID_OCCURRENCE_CLASSIFICATION.md). **[Updated 2026-07-21, post-PR reconciliation]:** S2's documentation is now committed at `d9176e1f2acb5625ce8d94017ab96bbe283be518` and pushed to branch `docs/kvkk-high006-s2-occurrence-classification`; **PR #191** ([https://github.com/MustafaBasol/DisKlinikCRM/pull/191](https://github.com/MustafaBasol/DisKlinikCRM/pull/191)) is open (`OPEN`, `mergeable: MERGEABLE`, `mergeStateStatus: CLEAN`), based on `main`, not yet merged. No remediation has been implemented and no risk reduction is claimed by this update — R-071 remains `OPEN`. NEXT_AFTER_MERGE: KVKK-HIGH-006-S3, implement Batch 1, once PR #191 merges. **[Updated 2026-07-21, KVKK-HIGH-008-F1-PBV-S1, self-reference-lag correction]:** `git rev-parse origin/main` confirms PR #191's merge commit (`548bcb7c81065fc71474fdff3632f4759528ffd3`) is now the tip of `origin/main` — **PR #191 is `MERGED`**, correcting the "not yet merged" wording immediately above (the committed snapshot predates its own PR's merge, the same self-reference-lag pattern this program has corrected repeatedly). Runtime implementation has still not started; zero of the 47 occurrences are fixed; R-071 remains `OPEN`. NEXT_AFTER_MERGE condition is now satisfied — KVKK-HIGH-006-S3 (Batch 1 implementation) is ready to start. **[Updated 2026-07-22, KVKK-HIGH-006 final program reconciliation, `AGENT_COMPLETED`, documentation-only]:** all implementation batches have since merged into `origin/main`, independently confirmed via `git merge-base --is-ancestor` (exit `0` each) — **PR [#194](https://github.com/MustafaBasol/DisKlinikCRM/pull/194)** (`7ee68ef73c203a1d239086c9f2c2e5090011e08a`, Batch 1: `reports.ts`/`appointmentRequests.ts`/`dentalChart.ts`), **PR [#195](https://github.com/MustafaBasol/DisKlinikCRM/pull/195)** (`b372406a191e4c557c58a1823cd441bd67f7ae27`, Batch 2: `paymentPlans.ts`/`inventory.ts`/`insuranceProvisions.ts`), **PR [#196](https://github.com/MustafaBasol/DisKlinikCRM/pull/196)** (`47dea4d534aa8e464e186e448e51a31a31e61cf3`, Batch 3: `messages.ts`/`postTreatment.ts`/`services.ts`), **PR [#198](https://github.com/MustafaBasol/DisKlinikCRM/pull/198)** (`4712870fbf9b1cc1dee582fb8981648c482014ad`, message read/send record-scope follow-up on `messages.ts`), **PR [#199](https://github.com/MustafaBasol/DisKlinikCRM/pull/199)** (`0a5be5e4e77cea864ea785451acb1d05f184bc9a`, Batch 4 target-clinic quota-scope fix, `middleware/planLimits.ts`) — plus **PR [#200](https://github.com/MustafaBasol/DisKlinikCRM/pull/200)** (`6ec61ba036849153382c965fe333af9a176a35e5`, Batch 4 characterization/product-decision document, documentation-only). All 47 previously-classified occurrences, the messages.ts record-scope defect, and the target-clinic quota gap are now fixed — **runtime implementation is complete**. Each merged PR's own evidence document reports source-level review plus local/disposable in-process simulated regression tests only (every one explicitly records no live Postgres connection was available in its task environment) — **no disposable-PostgreSQL-DB-backed or production test evidence exists for any of the six merged implementation PRs.** **R-071 remains `OPEN`** — not `MITIGATED`/`CLOSED`; the risk this row tracks (inconsistent direct `req.user.clinicId` use bypassing the centralized scope contract) is now source-fixed but not yet independently, DB-backed, or production-verified. **Next task: combined post-merge independent verification of KVKK-HIGH-006 on current `origin/main`, followed by disposable PostgreSQL DB-backed integration verification.** Production deployment not authorized. | Privacy/Tenant Security | KVKK-HIGH-006 | **[Updated 2026-07-22, post-DB-verification program reconciliation]:** ~~OPEN — `IMPLEMENTATION_MERGED — VERIFICATION_PENDING` (2026-07-22)~~ → **OPEN — `IMPLEMENTATION_MERGED — SOURCE_AND_DB_VERIFIED — PRODUCTION_VERIFICATION_PENDING`**: combined post-merge independent source verification ([PR #202](https://github.com/MustafaBasol/DisKlinikCRM/pull/202), 15 core checks pass, 549/550 test assertions, `PASS WITH CONDITIONS`) and disposable-PostgreSQL DB-backed verification ([PR #203](https://github.com/MustafaBasol/DisKlinikCRM/pull/203), all existing migrations applied cleanly, 63/63 DB-backed assertions passed, `PASS WITH CONDITIONS`) are both now complete; no genuine runtime/security defect found by either. Operationally: **`TECHNICALLY VERIFIED — PRODUCTION VERIFICATION PENDING`**. Production deployment and a production-safe smoke verification remain outstanding — see [evidence/KVKK-HIGH-006-PRODUCTION_DEPLOYMENT_GATE.md](evidence/KVKK-HIGH-006-PRODUCTION_DEPLOYMENT_GATE.md) for the exact next deployment sequence, smoke plan, and this risk's own closure criteria (§9 of that document). Still **not** `MITIGATED`/`CLOSED`. **[Updated 2026-07-22, PR #205 reconciliation with merged PR #204]:** [PR #204](https://github.com/MustafaBasol/DisKlinikCRM/pull/204) merged and reconciled into the deployment-gate branch; it resolved the `messageTemplateWabaBinding.test.ts` assertion referenced in PR #202's "549/550" result above (now 20/20, no production code changed) and does not itself change this row's status or composite status string. Baseline superseded to `e84d60b7dfbba8986c424accae9699552b194189`; PR ancestry now #194–#204. Status remains `OPEN — IMPLEMENTATION_MERGED — SOURCE_AND_DB_VERIFIED — PRODUCTION_VERIFICATION_PENDING`. **[Updated 2026-07-22, KVKK-HIGH-006 production deployment and smoke verification, documentation-only reconciliation of user-executed production evidence]:** production deployment executed (pre-deploy SHA `85e3ffbca7ee1b53789564e16c5e58c5ec498cf2` → deployed SHA `1aa741d1dc1e1888b1dfdb9b911d0123b4eea1ab`, clean fast-forward pull, working tree clean before/after; Prisma v7.8.0 generated; 65 migration directories; `prisma migrate status` up to date; `prisma migrate deploy` no pending migrations; only `noramedi-api` reloaded, `noramedi-worker` correctly not reloaded; PM2 process list saved; API/worker online; local health `200`/`{"status":"ok"}`; `https://noramedi.com` `200`; `https://app.noramedi.com` `302` → `/login`; no new high-severity log error; pre-existing WhatsApp-agent JSON parse errors and `pg` deprecation warnings present but non-blocking, not a KVKK-HIGH-006 failure). Production-safe authenticated smoke run against a dedicated synthetic organization/clinic/user/template fixture set (`organizationId 3f4be4f0-79de-44cd-a10f-436592319768`; existing synthetic Clinic A `d5ad7539-ed3a-44b4-be6c-50563e7566ca`; temporary synthetic Clinic B `7a8b9c10-1112-4131-8141-516171819202`, deleted in cleanup; synthetic admin user `9291dc52-2c16-44bc-ae50-2a99018078b2`, `canAccessAllClinics=true`; temporary synthetic MessageTemplate `9c101112-1314-4333-8343-536373839404`, deleted in cleanup) — **the minimum mandatory smoke set S1/S2/S3/S6/S7 all PASSED**: S1 authorized sibling-clinic access (`200`); S2 unauthorized cross-organization denial (`403`, `Access denied to requested clinic`, no foreign clinic ID exposed); S3 message-template record scope (Clinic A list `200` with template present and `metaTemplateConnectionId`/`metaWabaIdSnapshot` absent from the response; Clinic B list `200` with the Clinic A template absent); S6 insurance all-authorized-clinics behavior (`200`, empty array, no unauthorized clinic); S7 aggregate leakage analysis (zero foreign-organization/foreign-clinic data across S1/S2/S3/S6, exit `0`). **S4 (message send path) and S5 (target-clinic quota behavior) were NOT EXECUTED** — both optional and outside the minimum closure set (S4: no confirmed mock-only WhatsApp provider, sending to a real recipient prohibited; S5: minimum closure set already satisfied without it) — **neither is claimed to have passed.** All temporary fixtures (MessageTemplate, UserClinic assignment, temporary Clinic B) confirmed deleted with post-cleanup zero-row counts; temporary session cookie/response files deleted; final health check `{"status":"ok"}`. Durable evidence retained on the production host only (`/root/noramedi-kvkk-evidence/kvkk-high006-production-smoke-20260722.json`, `0600`, SHA-256 `96f31a23a1aa60f5fd12499963ed27278396b6f50402e8cce5a06715f0c58a9d`) — no secret, session token, cookie, patient data, IP address, or raw response body copied into this repository. All six [evidence/KVKK-HIGH-006-PRODUCTION_DEPLOYMENT_GATE.md](evidence/KVKK-HIGH-006-PRODUCTION_DEPLOYMENT_GATE.md) §9 closure-criteria items are now satisfied — full detail: [evidence/KVKK-HIGH-006-PRODUCTION_DEPLOYMENT_AND_SMOKE_VERIFICATION.md](evidence/KVKK-HIGH-006-PRODUCTION_DEPLOYMENT_AND_SMOKE_VERIFICATION.md). **R-071 status: `CLOSURE_PROPOSED_AWAITING_EXTERNAL_CONFIRMATION — IMPLEMENTATION_MERGED — SOURCE_DB_AND_PRODUCTION_VERIFIED — COMPLETE`** (proposed by this reconciliation; not independently re-verified by a separate task — **corrected 2026-07-25, F0-013-R1: this cell previously read `CLOSED`, which overstated this risk's actual status, since a self-proposed closure by the same task that performed its own verification is not an independent risk-owner acceptance and no independent external confirmation record exists anywhere in this repository; per this program's own rule that unilateral agent judgment is not risk acceptance, this risk is not called `CLOSED` until such a record exists**). **This does not close, mitigate, or otherwise affect R-061** (see the R-061 row below) — R-061 remains `OPEN` and requires its own separate Package A production evidence, which the clinic-level synthetic fixtures used in this smoke run do not supply. | `docs/compliance/KVKK_COMPLIANCE_AUDIT_AND_REMEDIATION.md` KVKK-HIGH-006 row; [NORAMEDI_MASTER_TRACKER.md §12 item 8-9, §13](NORAMEDI_MASTER_TRACKER.md); [evidence/KVKK-HIGH-006-S1_DEFAULT_CLINIC_ID_AUTHORIZATION_PATH_VERIFICATION.md](evidence/KVKK-HIGH-006-S1_DEFAULT_CLINIC_ID_AUTHORIZATION_PATH_VERIFICATION.md) (subordinate, non-closing side-verification); [evidence/KVKK-HIGH-006-S2_REQ_USER_CLINIC_ID_OCCURRENCE_CLASSIFICATION.md](evidence/KVKK-HIGH-006-S2_REQ_USER_CLINIC_ID_OCCURRENCE_CLASSIFICATION.md) (occurrence classification and remediation plan); [evidence/KVKK-HIGH-006-S3_BATCH1_IMPLEMENTATION.md](evidence/KVKK-HIGH-006-S3_BATCH1_IMPLEMENTATION.md) (PR #194, Batch 1); [evidence/KVKK-HIGH-006-BATCH2_IMPLEMENTATION.md](evidence/KVKK-HIGH-006-BATCH2_IMPLEMENTATION.md) (PR #195, Batch 2); [evidence/KVKK-HIGH-006-BATCH3_IMPLEMENTATION.md](evidence/KVKK-HIGH-006-BATCH3_IMPLEMENTATION.md) (PR #196, Batch 3); [evidence/KVKK-HIGH-006-MESSAGES_RECORD_SCOPE_IMPLEMENTATION.md](evidence/KVKK-HIGH-006-MESSAGES_RECORD_SCOPE_IMPLEMENTATION.md) (PR #198, message read/send record scope); [evidence/KVKK-HIGH-006-BATCH4_TARGET_CLINIC_IMPLEMENTATION.md](evidence/KVKK-HIGH-006-BATCH4_TARGET_CLINIC_IMPLEMENTATION.md) (PR #199, Batch 4 target-clinic quota); [evidence/KVKK-HIGH-006-BATCH4_CHARACTERIZATION_AND_PRODUCT_DECISION.md](evidence/KVKK-HIGH-006-BATCH4_CHARACTERIZATION_AND_PRODUCT_DECISION.md) (PR #200, Batch 4 characterization/product decision); [evidence/KVKK-HIGH-006-COMBINED_POST_MERGE_VERIFICATION.md](evidence/KVKK-HIGH-006-COMBINED_POST_MERGE_VERIFICATION.md) (PR #202, combined post-merge source verification); [evidence/KVKK-HIGH-006-DISPOSABLE_POSTGRES_VERIFICATION.md](evidence/KVKK-HIGH-006-DISPOSABLE_POSTGRES_VERIFICATION.md) (PR #203, disposable-Postgres DB-backed verification); [evidence/KVKK-HIGH-006-PRODUCTION_DEPLOYMENT_GATE.md](evidence/KVKK-HIGH-006-PRODUCTION_DEPLOYMENT_GATE.md) (this reconciliation — production deployment gate, smoke plan, closure criteria); [evidence/KVKK-HIGH-006-PRODUCTION_DEPLOYMENT_AND_SMOKE_VERIFICATION.md](evidence/KVKK-HIGH-006-PRODUCTION_DEPLOYMENT_AND_SMOKE_VERIFICATION.md) (2026-07-22, production deployment evidence, S1/S2/S3/S6/S7 smoke results, R-071 closure proposal) |
| R-074 | **[New, 2026-08-11, F3-OBS-001]** No live production observability/alerting exists: prior to F3-OBS-001, this repository had no readiness/liveness split (only a combined `/api/health`), no process-level `uncaughtException`/`unhandledRejection` handling in either entrypoint, no error-tracking provider configuration, and no OTel/metrics infrastructure — and, independent of the repository-side gaps F3-OBS-001 closes, **no live dashboard, alert channel, or uptime-probe wiring against any of it has ever been configured or verified**, which is the exact condition `phases/F3_PRODUCTION_HARDENING.md`'s own exit gate names ("gözlemlenebilirlik standardı canlıda kanıtla çalışıyor"). | Medium | High | `GET /api/readyz`/`GET /api/livez` (`server/src/routes/health.ts`), `utils/fatalErrorHandlers.ts`, `utils/errorTracking.ts` (Sentry-compatible, no-op until `SENTRY_DSN` is set and `@sentry/node` installed) — all repository-side only, `EXTERNAL_CONFIG_REQUIRED` for anything to actually alert a human | External uptime-prober wiring to `/api/livez`/`/api/readyz`; log-aggregation/alert-channel setup on structured `pino` output; `SENTRY_DSN` provisioning + `npm install @sentry/node` if adopted; PM2/backup-staleness alert wiring; disk/storage monitoring (no repository-side check exists — Node 22's `fs.statfsSync` is available but unused) | F3 exit gate itself names this requirement; no further repository-side mitigation is possible without the external actions above being performed and then verified | Observability/Operations | F3 | `CLOSED` — **(2026-08-13, F3-OBS-002-CLOSE, on operator-executed production evidence accepted by architecture review).** This row required **live dashboard/alert-channel/uptime-probe evidence**, and all of it is now in hand. **External health chain production-verified:** three UptimeRobot monitors (`noramedi-api-livez`, `noramedi-api-readyz`, and `noramedi-api-readyz-redis` asserting the body keyword `"name":"redis","status":"ok"` — the §2 Redis-specific assertion, never the fail-open root `status`), 5-minute interval, real human email alerting assigned to all three. **Dead-man chain production-verified:** three Healthchecks.io checks (`noramedi-pm2`, `noramedi-disk`, `noramedi-backup`), Period 5 m / Grace 15 m, email assigned, fed by the repo-owned `noramedi-opscheck.sh` under a root systemd oneshot + 5-minute timer, `enabled`/`active`, with both manual and scheduled executions confirmed `exit=0`. **Real human DOWN delivery proven and recovery delivery proven, by two controlled drills, neither of which induced any application failure:** **Drill A** (external assertion falsified against a healthy system) — DOWN in **61 s**, human DOWN email received, recovery **12 s** after restore, recovery email `11:01:32Z`; a 15/15-sample independent `/api/readyz` witness poll bracketing the whole incident proves `database ok`/`redis ok` throughout, so the DOWN came purely from the injected assertion. **Drill B** (disk telemetry withheld from a healthy disk, which kept reporting `OK — 12% used` locally the entire time) — human-alert latency **`20 m 02 s` exact**, detection latency reported as the bounded interval **`[19 m 05 s, 20 m 02 s]`** rather than at a false second precision the provider UI does not expose, recovery email `11:48:15Z`; suppression proven **selective** — `pm2` and `backup` pinged normally on all four suppressed runs. Zero collateral impact in both drills: PM2 restart counts `12`/`12` and PIDs `607545`/`607578` unchanged, no process/timer/service ever stopped or restarted, no migration, no application-code change. **The monitoring credential exposed during evidence capture was rotated and permanently retired *before* this closure:** architecture review **REJECTED** the drill report's original `Secrets exposed = no` claim after the full UUID-bearing `noramedi-disk` ping URL appeared in an operator screenshot; the credential was treated as compromised, a semantically identical replacement check was created (Period 5 m / Grace 15 m / email / DOWN+UP), only `NORAMEDI_OPSCHECK_DISK_PING_URL` was changed, rotation was proven by exact one-to-one correspondence between host journal runs and replacement-check pings while the old check stayed frozen, and the old check was paused, verified silent, then **deleted** (Healthchecks does not reissue deleted UUIDs; never reused). Exposure scope operator-reviewed as **disk-only** — `noramedi-pm2`/`noramedi-backup` URLs were redacted — and a host audit confirmed no second on-disk copy of any ping URL exists. **No residual known monitoring-secret exposure remains in active production configuration.** **Not self-closed:** the implementation was F3-OBS-002/PR #405, the provider and host actions were the operator's, and both the drill report and the remediation report were accepted by architecture review — the `R-019/R-071/R-072/R-073/R-075` no-self-closure precedent is honoured. **Superseded and preserved as history, not deleted:** the prior token `OPEN — **[Updated 2026-08-12, F3-PROD-001, user-supplied production deployment evidence]:** GET /api/livez → 200 {"status":"ok","role":"api"}; GET .../api/readyz → 200 with database=ok/redis=ok — both confirmed live … **This is reachability/correctness evidence only, not the dashboard/alert-channel/uptime-probe evidence this risk requires to close** — no external uptime monitor, log-aggregation alert channel, or Sentry DSN was wired or verified. Status unchanged: OPEN.` **Caveats explicitly retained, not absorbed by this closure:** Sentry/error-tracking adoption remains **deliberately deferred and unimplemented** (evidence §9); log aggregation, OTel metrics/tracing, elevated-5xx-rate alerting, TLS-expiry alerting and any on-call rotation are **not** covered and were never part of this row's stated closure criteria; the `noramedi-api-readyz-redis` keyword assertion is **`KNOWN_BRITTLE`** — a key reorder in `readiness.ts`'s `{ name, status }` literal would silently disarm it with no application-level symptom (regression guard recommended, not opened here). **This closure satisfies F3 exit-gate criterion 1 only. It does not close R-018, R-076 or R-077, does not assess criteria 2 or 3, and does not satisfy the F3 exit gate**, which remains `NOT_SATISFIED`; F3 is not complete and F4 is not authorized. **This closure carries no legal/KVKK approval of any kind** — the two new monitoring providers are recorded in [`../compliance/62-kvkk-subprocessor-register.md`](../compliance/62-kvkk-subprocessor-register.md) §7 with their DPA/international-transfer fields explicitly `TO BE VERIFIED / COUNSEL REVIEW PENDING`. | [evidence/F3-OBS-001_PRODUCTION_OBSERVABILITY_MINIMUM.md](evidence/F3-OBS-001_PRODUCTION_OBSERVABILITY_MINIMUM.md) §1/§6/§7, [evidence/F3-PROD-001_PRODUCTION_DEPLOYMENT_AND_VERIFICATION.md](evidence/F3-PROD-001_PRODUCTION_DEPLOYMENT_AND_VERIFICATION.md) §4/§6; **kapanış kanıtı: [evidence/F3-OBS-002_LIVE_OBSERVABILITY_WIRING_ALERT_VERIFICATION.md](evidence/F3-OBS-002_LIVE_OBSERVABILITY_WIRING_ALERT_VERIFICATION.md) §17/§18/§19/§20** |
| R-075 | **[New, 2026-08-12, F3-PROD-001 — production dependency vulnerability audit]** Production `npm audit` reports 0 critical, 2 high, 5 moderate, 1 low severity advisories in the deployed dependency tree (user-supplied production evidence — see [evidence/F3-PROD-001_PRODUCTION_DEPLOYMENT_AND_VERIFICATION.md](evidence/F3-PROD-001_PRODUCTION_DEPLOYMENT_AND_VERIFICATION.md) §6; exact package/advisory list not itemized by this reconciliation). **[Updated 2026-08-12 by F3-SEC-003 — all 8 itemized, classified and remediated at the repository level.]** All 8 were reproduced exactly in `server/` and itemized: `brace-expansion` ×2 (high, via `archiver`), `fast-uri` (high, via the `@prisma/dev` subtree), `hono`/`@hono/node-server`/`valibot`/`@prisma/dev`/`prisma` (5 moderate, all one `@prisma/dev` subtree), `body-parser` (low, via `express`). **None was found exploitable from any NoraMedi request or job path** — evidence per finding in the F3-SEC-003 doc §5. **`server/` is now at 0 vulnerabilities** (both `--omit=dev` and full). | Medium | Low (post-remediation; was Medium/High pending classification) | Repository-level remediation complete and locally verified: Prisma family 7.8.0→7.9.1 (minor), non-forced `npm audit fix` for `brace-expansion`/`fast-uri`/`body-parser`, orphan `@hono/node-server` override removed; new regression guard `server/src/tests/dependencyExploitabilityInvariants.test.ts` freezes the invariants the non-exploitability rests on; full local CI matrix green incl. 3 disposable-DB profiles and `npm ci` | ~~**Production re-verification** — post-deploy `npm audit --omit=dev` on `disklinik-prod-01` has not been run (this task performed no production access). Also unverified: `binaries.prisma.sh` egress from the production host, required for the changed Prisma engine hash (see F3-SEC-003 §13)~~ **[Both satisfied 2026-08-12 by F3-IMPL-002-PROD-RECON(R1), operator-supplied production evidence.]** Post-deploy `npm audit --omit=dev` **was** run on the production host at deployed SHA `aa18b064…` and reports **`found 0 vulnerabilities`**, reproducing F3-SEC-003's local `8 → 0` result in production. Engine fetch is verified **exactly, not by inference**: the production Prisma engine hash `e922089b7d7502aff4249d5da3420f6fa55fc6ad` is **byte-identical** to the `@prisma/engines-version` pin `7.9.0-1.e922089b…` in `server/package-lock.json` at the deployed SHA, and `prisma@7.9.1`/`@prisma/client@7.9.1` installed successfully with both API and worker generating `Prisma Client v7.9.1`. **Residual scope retained, not absorbed by the closure:** this is a point-in-time `--omit=dev` measurement of the API host's `server/` tree; ~~**no continuous dependency monitoring** (Dependabot alerts / CI-side `npm audit` gate) is confirmed, so a future advisory would not be detected by anything evidenced here~~ **[Updated 2026-08-13, F3-SEC-EXIT-001-R2]:** Dependabot alerts and Dependabot security updates are now independently verified `enabled` on `MustafaBasol/DisKlinikCRM` (see [evidence/F3-EXIT-C2_EXTERNAL_SECURITY_SETTINGS_VERIFICATION.md](evidence/F3-EXIT-C2_EXTERNAL_SECURITY_SETTINGS_VERIFICATION.md) §15) — continuous GitHub-side vulnerability alerting now exists. A CI-side `npm audit` gate is still **not** confirmed to exist; and the frontend's two accepted `react-router` advisories lie outside `--omit=dev` scope and remain accepted-not-fixed | Deploy the merged change, then re-run `npm audit --omit=dev` in `$APP_DIR/server` and confirm 0; verify engine fetch before deploying — **all performed and confirmed**. Remaining follow-on, tracked as residual rather than as this row: stand up continuous dependency monitoring so the 0-vulnerability state is maintained rather than merely observed once | Security/Dependencies | F3 (non-blocking — **not named by F3's own 3-item exit gate; this closure does not advance the F3 exit gate, which remains `NOT SATISFIED`**) | `CLOSED` — **(2026-08-12, F3-IMPL-002-PROD-RECON(R1))** externally confirmed by the PR #402 reviewer on the accepted operator-supplied production evidence chain (deployed SHA = PR #401's merge commit; production `npm audit --omit=dev` = 0 vulnerabilities; engine hash byte-identical to the deployed lockfile pin; `Prisma Client v7.9.1` generated by both processes). Every requirement this row itself named is met, and the sole remaining condition was external confirmation rather than further evidence. Superseded: the prior `CLOSURE_PROPOSED_AWAITING_EXTERNAL_CONFIRMATION` token *(not self-closed, per this program's R-019/R-071/R-072/R-073 precedent that a task cannot close the risk it just remediated)* is **preserved here as history, not deleted** — that precedent is honoured, since the confirming task is not the remediating task (F3-SEC-003) and the confirmation is external to both. This closure is **narrow**: it does **not** resolve continuous dependency monitoring, the accepted frontend `react-router` advisories, **R-076** (stored-XSS, unaffected and still `OPEN`), **R-077**, or **R-033/R-034/R-040**, none of which share this row's criteria. | [evidence/F3-IMPL-002-PROD-RECON_PRODUCTION_WORKER_CONTRACT_VERIFICATION.md](evidence/F3-IMPL-002-PROD-RECON_PRODUCTION_WORKER_CONTRACT_VERIFICATION.md) §3.5, §4 checks 12–13, **§6.5**; [evidence/F3-SEC-003_PRODUCTION_DEPENDENCY_VULNERABILITY_REMEDIATION.md](evidence/F3-SEC-003_PRODUCTION_DEPENDENCY_VULNERABILITY_REMEDIATION.md); prior context [evidence/F3-PROD-001_PRODUCTION_DEPLOYMENT_AND_VERIFICATION.md](evidence/F3-PROD-001_PRODUCTION_DEPLOYMENT_AND_VERIFICATION.md) §6 |
| R-076 | **[New, 2026-08-12, F3-SEC-003 — incidental finding, NOT a dependency issue and NOT remediated by that task]** `ClinicLegalProfile.website` accepts **any** scheme and is rendered as a live `href` on an unauthenticated public page — probable stored XSS. `server/src/routes/clinicLegalProfile.ts:37` declares `website: z.string().max(300).optional().nullable()` with no URL/scheme validation, in direct contrast to lines 34/35 which use `.email()`. Settable by `OWNER`/`ORG_ADMIN`/`CLINIC_MANAGER` (`LEGAL_PROFILE_ROLES`, line 24); returned by the unauthenticated `GET /clinics/:clinicSlug/kvkk` (`publicClinicKvkk.ts:34,67`) once the profile is published; rendered by `src/pages/clinic/ClinicKvkkPublicPage.tsx:116` as `<a href={data.legalProfile.website} target="_blank" rel="noopener noreferrer">`. A `javascript:` value therefore reaches a live `href` on a public page; React warns but does not block it. Verified at all three layers (route schema, public endpoint, render sink). | Medium (requires a privileged clinic role to set; victim is any public visitor who clicks) | High (stored XSS on a public, unauthenticated clinic-facing KVKK page; session/token theft potential; KVKK/reputational exposure) | Field is length-capped (300) and the page sets `rel="noopener noreferrer"`; profile must be explicitly published to become public | **No scheme/URL validation on write and no render-time guard.** No test covers it | Add `.url()` + an explicit `http`/`https` scheme allowlist on write (mirroring the `.email()` treatment two lines above), plus a render-time scheme guard for existing stored rows, plus a regression test; consider a one-off scan of existing `ClinicLegalProfile.website` values | Security/Frontend+API | F3 (not part of F3's 3-item exit gate; unrelated to R-075) | OPEN — deliberately not fixed by F3-SEC-003, whose PR scope is dependency remediation only; needs its own task | [evidence/F3-SEC-003_PRODUCTION_DEPENDENCY_VULNERABILITY_REMEDIATION.md](evidence/F3-SEC-003_PRODUCTION_DEPENDENCY_VULNERABILITY_REMEDIATION.md) §12 item 2, §14 objection 10 |
| R-077 | **[New, 2026-08-12, F3-IMPL-002-PROD-RECON — production-observed, next available ID, no existing row reused/renumbered]** Operasyonel script sapması: production'da çalıştırılan `/usr/local/sbin/noramedi-*.sh` kopyalarının depodaki `scripts/*.sh` kopyalarıyla aynı sürümde olduğunu garanti eden **hiçbir mekanizma yok**. Deploy yolu `/usr/local/sbin/noramedi-deploy.sh`, fakat `git pull` yalnızca `/var/www/noramedi/scripts/noramedi-deploy.sh`'i günceller; kuruluma taşıma ayrı, elle yapılan, doğrulanmayan bir adımdır. **Kanıtlanmış, varsayımsal değil:** kurulu deploy scripti 2026-06-29 revizyonunda donmuştu (`8dffcd13…` = commit `96313f39`'un blob'u, git geçmişine karşı bağımsız doğrulandı) — 44 gün ve bir F3 sertleştirme değişikliği geride; bu, F3-IMPL-002'nin **tüm worker deploy sözleşmesini sessizce geçersiz kıldı** (yalnızca `pm2 reload noramedi-api`, `ecosystem.config.cjs` referansı yok, worker reload yok, worker doğrulaması yok) ve 2026-08-12'deki iki production deploy'unda da hiçbir uyarı/hata üretmeden geçerli kaldı; worker bayat çalışmaya devam etti ve ancak elle keşfedildi. **Kapsam yalnızca düzeltilen dosyadan ibaret değil:** `scripts/noramedi-deploy.sh:37` `HEALTHCHECK="/usr/local/sbin/noramedi-healthcheck.sh"` çağırır — bu ikinci kurulu scriptin `scripts/noramedi-healthcheck.sh` ile eşleşip eşleşmediği **doğrulanmadı** (`UNVERIFIED`); her iki depo scriptindeki gelecekteki her düzenleme aynı sessiz-no-op arıza moduna sahiptir | Medium (her script düzenlemesinde yeniden oluşur; kurulum elle) | High (sessizce çalışmayan deploy adımları; F3-IMPL-002 örneğinde worker'ın deploy edilen kodu hiç yüklememesi, hiçbir sinyal üretmeden) | Deploy scripti `set -euo pipefail` ile çalışır ve **kendi** adımlarında fail-closed'dur — ancak eksik bir adımı tespit edemez. `bash -n` sözdizimi kontrolü kurulum sırasında elle yapıldı; rollback kopyası alındı (`/usr/local/sbin/noramedi-deploy.sh.bak.20260812-174738`) | **Kurulu kopya ile depo kopyası arasında sürüm/hash eşitliğini zorlayan hiçbir otomatik kontrol yok.** `noramedi-healthcheck.sh`'in mevcut sapma durumu bilinmiyor | Üç seçenekten biri (hiçbiri bu dokümantasyon görevinde uygulanmadı — hepsi runtime/deployment değişikliğidir): (a) deploy'un doğrudan depo kopyasını çağırması; (b) deploy'un kurulu kopyanın hash'ini depo kopyasıyla karşılaştırıp uyuşmazlıkta abort etmesi; (c) scriptlerin kurulumunun açık, kanıtlanan bir deploy adımı hâline getirilmesi. Ayrıca: `sha256sum /usr/local/sbin/noramedi-healthcheck.sh` ile depo kopyasının karşılaştırılması | Deployment | F3 | `OPEN` — **(güncellendi 2026-08-12, F3-PROD-002: iki yarıdan biri kapandı, satır açık kalıyor.)** **(a) `noramedi-healthcheck.sh` sapma durumu → ÇÖZÜLDÜ.** Kurulu ve depo kopyalarının SHA-256'sı **tam eşleşiyor**: `14a148982d74eda76d7adf757e09a32c0329b65195413961ca32cd4b8c6cef7e`. Bu, o dosyanın kurulu kopyasının **ilk kez ölçülmesidir** — F0-002 yalnızca "mevcut ve çalıştırılabilir" demişti. Beklenen sonuçtu ama **varsayılmadı, ölçüldü**: dosyanın tüm git geçmişinde tek bir commit var (`4600c3e`) ve 2026-06-29 bayat dönem commit'i `96313f39` ile bugünkü `origin/main` arasında bayt-özdeş, dolayısıyla bayat bir kurulum bile eşleşirdi. Deploy scripti de `794375b4…` olarak yeniden doğrulandı; her iki hash bu görevde `git cat-file -p … \| sha256sum` ile bağımsız olarak yeniden üretildi. **Hash'in ötesinde davranışsal kanıt:** koşu steps 5-8'i yürüttü, `ecosystem.config.cjs`'e başvurdu, worker'ı reload etti ve `verify_pm2_online`'ın `scripts/noramedi-deploy.sh:119`'daki biçim dizgisini birebir üretti — bunların hiçbiri 2026-08-12'ye dek kurulu olan 2026-06-29 revizyonunda yok. **(b) Sapmayı önleyecek mekanizma → HÂLÂ YOK, satırın açık kalma sebebi budur.** Satırın kendi üç seçeneğinden — (a) deploy'un depo kopyasını doğrudan çağırması, (b) deploy'un hash karşılaştırıp uyuşmazlıkta abort etmesi, (c) kurulumun açık/kanıtlanan bir deploy adımı olması — **hiçbiri uygulanmadı**; üçü de runtime/deployment değişikliğidir ve bu dokümantasyon görevinin kapsamı dışındadır. **PM2 reboot kalıcılığı da bu satırda kalır** (`pm2 save` kanıtlanmadı). **Windows/CRLF tuzağı — karşılaştırmayı tekrarlayacaklar için:** bu deponun Windows checkout'u her iki scripti CRLF ile açar ve `917ee40a…` (deploy) / `0d42ccfb…` (healthcheck) verir; bunları Linux host kopyalarıyla karşılaştırmak **yanlış bir uyuşmazlık** üretir. Doğru temel LF blob hash'idir. Önceki ifade, tarih olarak korunur, silinmez: `OPEN — deploy scripti bu bulguyla senkronize edildi (794375b4…, her iki kopya aynı), fakat sapmayı önleyecek bir mekanizma hâlâ yok ve noramedi-healthcheck.sh doğrulanmadı` | [evidence/F3-IMPL-002-PROD-RECON_PRODUCTION_WORKER_CONTRACT_VERIFICATION.md](evidence/F3-IMPL-002-PROD-RECON_PRODUCTION_WORKER_CONTRACT_VERIFICATION.md) §3.4/§4/§5/§8; [evidence/F3-PROD-002_DEPLOY_SCRIPT_EXECUTION_VERIFICATION.md](evidence/F3-PROD-002_DEPLOY_SCRIPT_EXECUTION_VERIFICATION.md) §3.5/§9.4/§15 |
| R-070 | **[New, 2026-07-20, F0-011-P2]** `prisma migrate deploy`'s bookkeeping table (`_prisma_migrations`) does not self-reconcile after a *physical* (hand-authored DDL) schema rollback: after dropping the KVKK-HIGH-007/HIGH-008 tables directly, `_prisma_migrations` still listed both migrations `finished_at`-applied, and a subsequent `prisma migrate deploy` reported `No pending migrations to apply` — i.e. Prisma's own tooling cannot detect or repair drift between its bookkeeping and the true schema state. This generalizes beyond the two KVKK migrations: **any** future incident response that physically rolls back a migration's DDL by hand (not just these two) will leave `_prisma_migrations` silently wrong, which could cause a later `prisma migrate deploy` to skip re-applying a migration an operator believes is pending, i.e. an operator/tool could reach an incorrect belief about actual schema state and make an unsafe deploy decision on that basis. | Low | Medium | `VERIFIED_DISPOSABLE_REHEARSAL` (F0-011-P2: independently reproduced against a disposable Postgres 16 database — see evidence §C) | No automated drift-detection between `_prisma_migrations` bookkeeping and actual schema state; no documented incident-response procedure for reconciling bookkeeping after a manual physical rollback; no approved emergency runbook exists for the exceptional-physical-rollback path either (backup/export, reverse-DDL approval, reconciliation, and DBA/program sign-off are not yet codified as a documented procedure) | **Default/preferred posture: avoid physical (hand-authored DDL) rollback of additive migrations entirely.** Retain the additive schema during any application cutback/rollback — an application redeploy to a prior compatible commit is independently verified compatible with the additive tables retained (evidence §C/§Part E.4) — and use **forward-fix as the default remediation path**, not a schema rollback. **If an exceptional physical rollback is ever authorized despite the above, it must include, as mandatory steps, all of:** (1) a verified backup/export of the new table(s)' data before any `DROP`; (2) an explicitly approved reverse-DDL script, not ad hoc statements; (3) explicit schema-and-ledger reconciliation of `_prisma_migrations` after the DDL runs; (4) post-rollback schema verification (confirming actual live schema matches the intended rolled-back state); (5) `_prisma_migrations` state verification via `prisma migrate status`; and (6) explicit DBA/program approval **before** execution, not after the fact. **Direct mutation of `_prisma_migrations` (e.g. `DELETE FROM _prisma_migrations WHERE migration_name IN (...)`) is emergency-only, unsupported by Prisma's own tooling, and must never be documented or treated as the normal/default production rollback procedure** — in this task it was observed only as a disposable-environment recovery experiment (evidence §C), not exercised, recommended, or endorsed as a production runbook step. `npx prisma migrate resolve --rolled-back <name>` was attempted and confirmed **not** to work for this case: it only operates on migrations Prisma considers to have failed mid-apply, rejecting a cleanly-applied migration with `P3012`. **[2026-07-20, KVKK-HIGH-008-F1-PROD-DOCS]** PR #186's production deployment used a standard `prisma migrate deploy` to apply `20260720180000_add_platform_admin_audit_event` — no physical (hand-authored DDL) rollback occurred and no direct `_prisma_migrations` mutation occurred during this deployment; therefore no new ledger drift of the kind this risk describes was introduced by it. This does not close R-070 — the underlying tooling gap (no drift-detection, no documented reconciliation runbook) is unchanged. | Database/Tooling | Isolated documentation/tooling-procedure task (not this task's authority to implement) | OPEN | [F0-011-P2 evidence §C finding](evidence/F0-011-P2_KVKK_HIGH007_HIGH008_ROLLBACK_TENANT_VERIFICATION.md), [evidence/KVKK-HIGH-008-F1_PRODUCTION_DEPLOYMENT_VERIFICATION.md](evidence/KVKK-HIGH-008-F1_PRODUCTION_DEPLOYMENT_VERIFICATION.md) |
| R-078 | **[New, 2026-08-14, F3-C2-ERR-001 — next available ID, no existing row reused/renumbered]** Harici hata izleme (external error tracking) etkinleştirmesi iki ayrı riski aynı anda açar. **(a) Egress:** `server/src/utils/errorTracking.ts` bir üçüncü tarafa giden **ikinci** bir çıkış yolu olur — F3-IMPL-004/F3-IMPL-006'nın log akışı için kapattığı sızıntı sınıfının aynısı, farklı bir kanaldan. SDK varsayılanları bunu kendiliğinden güvenli hâle **getirmez**: kurulu `@sentry/node@10.70.0` yapısına karşı doğrulandığı üzere 17 varsayılan entegrasyonun her biri (istek gövdesi/başlık/çerez/query, `console.*` breadcrumb'ları, giden URL'ler, yakalanmamış hataların ham mesaj+stack ile otomatik gönderimi, kaynak kodu bağlamı, **yerel değişken değerleri**, os/device/CPU/locale/timezone, kurulu paket envanteri, `error.cause` zinciri) sözleşmeyi tek başına ihlal ederdi; ayrıca `server_name` = `os.hostname()` ve `contexts.runtime` entegrasyon **değildir** ve `defaultIntegrations: false` ile kapanmaz. **(b) Tedarik zinciri:** aktivasyon `server/` ağacına 31 paket / ~54 MB ekledi — sekiz `@opentelemetry/*` paketi, `import-in-the-middle`, `require-in-the-middle` ve `@apm-js-collab/*` kod-dönüştürücü paketleri dahil — ki bu, görevin kendi "OpenTelemetry ekleme" kısıtıyla gerilim hâlindedir. **(c) KVKK:** sağlayıcı seçilirse veri işleyen/alt işleyen ilişkisi doğar; SaaS seçeneklerinde yurt dışına aktarım KVKK m.9 mekanizması gerektirir ve Türkiye için yeterlilik kararı bulunmamaktadır. | Medium | High | **Kod tarafı doğrulandı (`VERIFIED`, kurulu SDK'ya ve tel üzerindeki gerçek zarfa karşı):** deny-by-default `Sentry.init` (`defaultIntegrations: false` + `integrations: []`, `skipOpenTelemetrySetup: true`, `registerEsmLoaderHooks: false`, `includeServerName: false`, `tracesSampleRate: 0`, `maxBreadcrumbs: 0`, `attachStacktrace: false`, `enableLogs: false`, `sendClientReports: false`, `spotlight: false`, `debug: false`); `beforeSend` SDK'nın olayını atıp 9 alanlık izin listesinden yeniden kurar ve mesaj sabit değilse `null` döner; `role`/`requestId`/`route` sınırlı desenlerle yeniden doğrulanır; `captureMessage` kullanılır, `captureException` asla. `server/src/tests/errorTracking.test.ts` 24/24. **Çalışma zamanı riski bugün sıfırdır:** import dinamiktir ve `SENTRY_DSN`'e bağlıdır, hiçbir ortamda ayarlı değildir, dolayısıyla SDK sürece hiç yüklenmez. | Sağlayıcı kararı ve KVKK alt-işleyen kaydı (`docs/compliance/62-kvkk-subprocessor-register.md` §7 hâlâ `NO SUBPROCESSOR IDENTIFIED`); üretimde DSN'in gizli tutularak yapılandırılması; **tek bir sentetik olayın sağlayıcı arayüzünde doğrulanması** (yapılmadı); olay akışının kendisinin metadata olduğu gerçeğine karşı bir saklama/erişim politikası; `sendDefaultPii` v11'de kaldırılacağı için (`dataCollection` ile değiştirildi) majör yükseltmede incelemenin tekrarlanması; ~54 MB'lik bağımlılık ağacının sürekli güvenlik izlemesi. | Kısa vade: sağlayıcı olarak **kendi altyapısında barındırılan GlitchTip/Sentry** tercih edilirse yurt dışı aktarım ve yeni alt işleyen tamamen ortadan kalkar — sınır protokol uyumlu olduğu için kod değişikliği gerekmez. Alternatif olarak `@sentry/core` üzerine elle kurulmuş istemci (2 paket / ~15 MB, sıfır OpenTelemetry, uçtan uca doğrulandı) tedarik zinciri yüzeyini ortadan kaldırır. Etkinleştirme yalnızca [evidence/F3-OBS-001_PRODUCTION_OBSERVABILITY_MINIMUM.md](evidence/F3-OBS-001_PRODUCTION_OBSERVABILITY_MINIMUM.md) §15.8 runbook'u üzerinden; geri alma `SENTRY_DSN`'i kaldırıp desteklenen PM2/deploy yolundan yeniden başlatmaktır (veri geri alımı yok, migration yok). | Observability/Operations + Compliance | F3 | `OPEN` (2026-08-14 — kod sınırı doğrulandı ve üretimde etkin değil; sağlayıcı/KVKK/doğrulama adımları açık. **`F3-SEC-EXIT-001` §5 madde 10 `NOT_SATISFIED` olarak kalır**; bu satır o maddenin kapanışı değil, kapanışın önkoşulunun karşılandığının kaydıdır.) | [evidence/F3-OBS-001_PRODUCTION_OBSERVABILITY_MINIMUM.md](evidence/F3-OBS-001_PRODUCTION_OBSERVABILITY_MINIMUM.md) §15.2/§15.3/§15.4/§15.6/§15.8/§15.10, `server/src/utils/errorTracking.ts`, `server/src/tests/errorTracking.test.ts` |
| R-079 | **[New, 2026-08-16, F4-3 — next available ID, no existing row reused/renumbered]** `LabOrderAttachment` modelinde **`legalHold` alanı yoktur**, dolayısıyla `DELETE /api/lab-orders/:id/attachments/:attId` — deponun üç fiziksel silme yolundan biri — **hiçbir legal-hold kapısı taşımaz**. `PatientAttachment` ve `ImagingStudy` PR #160–#163 ile alan + rol kapılı PATCH + audit kazandı; lab siparişi ekleri o turda kapsanmadı ve o gün bugündür kapsanmamıştır (F2-PREP-005 `IMG-13` bulgusu ile aynı boşluk, ayrı kayıt). Somut sonuç: aynı klinik içinde bir dava/denetim dosyası olarak tutulan bir lab eki (ör. teknisyene gönderilen ağız içi fotoğraf), hasta ekleri korunurken `RECEPTIONIST`/`ASSISTANT` dâhil `LAB_ORDER_MANAGE_ROLES`'un tamamı tarafından kalıcı olarak silinebilir. | Low (silme kullanıcı-tetiklidir, otomatik/toplu bir yol yoktur) | High (özel nitelikli veri; geri dönüşü yoktur ve legal-hold'un var olma sebebi tam olarak budur) | Silme yolu **kiracı-kapsamlıdır** ve F4-3 ile birlikte koşullu `deleteMany` (id + `labWorkOrderId` + `clinicId`) hâline getirilmiştir; fiziksel silme artık `deleteStoredObjectWithEvidence` üzerinden **kanıt üretir** (aktör, klinik, nesne anahtarı, sonuç, `requestedAt`/`executedAt`). Bu, silmeyi **görünür** kılar — **engellemez**. | `LabOrderAttachment.legalHold` / `legalHoldReason` alanları, bunları set/release eden rol-kapılı endpoint, ve silme sorgusundaki atomik `legalHold: false` koşulu. Üçü de **yok**. | **Ek (additive) migration gerektirir ve bu nedenle F4-3'te KASITLI OLARAK UYGULANMAMIŞTIR** — görev sözleşmesi program incelemesi olmadan migration yaratmayı yasaklar. Önerilen en küçük değişiklik `PatientAttachment` emsalini birebir izler: iki nullable/`@default(false)` sütun, `@@index([clinicId, legalHold])`, `OWNER`/`ORG_ADMIN` kapılı `PATCH .../legal-hold` (her iki yönde gerekçe zorunlu, her iki yön denetlenir) ve `deleteMany` WHERE'ine `legalHold: false` eklenmesi. Karar program sahibinindir; F4-3 boşluğu yalnızca kaydeder. | Storage/Compliance | F4 | `OPEN` — **hiçbir şey bu satırı kapatmaz; F4-3 yalnızca silmeyi kanıtlanabilir hâle getirmiştir** | `server/src/routes/labOrders.ts` (DELETE rotası), `server/prisma/schema.prisma` (`model LabOrderAttachment` — `legalHold` yok), `server/src/tests/storageDeletionEvidence.test.ts` §9, [phases/F4_STORAGE_AND_BACKUP.md](phases/F4_STORAGE_AND_BACKUP.md) F4-3 bölümü |
| R-080 | **[New, 2026-08-16, F4-3 — next available ID, no existing row reused/renumbered]** Fiziksel nesne silme için **dayanıklı (durable) silme niyeti ve otomatik yeniden deneme yoktur.** F4-3 öncesinde başarısız bir depo silmesi tamamen kayboluyordu: DB satırı (depolama anahtarını taşıyan **tek** yer) çoktan silinmiş oluyor, `deleteFile` hatası yutuluyordu ve nesne kalıcı, görünmez bir sızıntı hâline geliyordu. F4-3 bunu **kanıt** ile kapattı — başarısızlık artık `AuditLog`'a anahtarıyla birlikte yazılır ve `severity=error` bir `OperationalEvent` üretir. **F4-3-R1 düzeltmesi (PR #430 mimari incelemesi):** ilk uygulama bu kanıtı `writeAuditLog` + `recordOperationalEvent` ile yazıyordu; **her ikisi de kendi kalıcılık hatasını yutan** (fire-and-forget) yazıcılardır, dolayısıyla "DB satırı silindi → depo silmesi başarısız → her iki kanıt yazımı da sessizce başarısız" dizisi hâlâ mümkündü ve fonksiyon bunu **izlenen bir başarısızlık gibi** (`failed`) döndürüyordu. Kanıt yazımı artık **yutmayan** `writeAuditLogInTx` ile yapılır; kanıt commit edilemezse sonuç `evidence_persistence_failed` olarak ayrı bir değerle raporlanır ve rota `500` + `STORAGE_DELETE_UNEVIDENCED` döner. Buna rağmen **yeniden deneme hâlâ elle yapılan bir operatör eylemidir**: silme niyetini tutan bir tablo, onu işleyen bir worker ve bayat/başarısız niyetleri süpüren bir uzlaştırma (reconciliation) koşusu **yoktur**. | Medium (geçici depo hatası; S3'e geçişte olasılık artar) | Medium (KVKK silme talebi yarım kalır; operatör görür ama sistem kendiliğinden tamamlamaz) | `writeAuditLogInTx` ile **yutmayan** `AuditLog` kanıt kaydı (yetkili kayıt) + ikincil `OperationalEvent` uyarısı (`error`; kanıt commit edilemediyse `critical`); kanıt commit edilemez ve nesne de kesin silinememişse sonuç `evidence_persistence_failed` olarak **ayrıca** raporlanır ve süreç günlüğüne `UNEVIDENCED ORPHAN RISK` olarak yükseltilir — sessiz üçüncü durum yoktur; `already deleted = idempotent success` semantiği; `fileStorage.deleteFile` artık yalnızca `ENOENT`'i yutar, gerçek bir hatayı **yükseltir** (F4-3'te düzeltildi — öncesinde yerel modda **her** hata yutuluyordu ve her çağıran silmeyi başarılı sanıyordu) | Dayanıklı silme niyeti (outbox benzeri satır), onu tüketen worker, ve "başarısız/bayat niyet" ile "DB satırı var, nesne yok" durumlarını raporlayan uzlaştırma koşusu. Ters-yetim tespiti (nesne var, DB satırı yok) da **hâlâ yoktur** — `orphanFileInspection.ts` yalnızca DB satırlarını dolaşır; F4-3 yükleme geri-alma yolunu kanıtlar hâle getirerek bu boşluğun **yeni** örneklerini görünür kılar, geçmiştekileri değil. | Ek bir migration gerektirir (`StorageDeletionIntent` benzeri tek tablo) ve bu nedenle F4-3'te **uygulanmamıştır**; ayrıca sağlayıcı seçimi netleştiğinde S3 `DeleteObject` hata sınıflarına göre yeniden deneme politikası yazılmalıdır. Kısa vadede operatör yolu: `OperationalEvent` (`source=system`, `severity=error`, mesaj "Storage object deletion did not complete") satırlarını izlemek ve `metadata.storageKey` ile elle temizlemek. | Storage/Operations | F4 | `OPEN` | `server/src/services/storageObjectDeletion.ts`, `server/src/services/fileStorage.ts` (`deleteFile`), `server/src/tests/storageDeletionEvidence.test.ts` §2/§3/§7 |

## Kullanım kuralları

- Yeni risk eklerken bir sonraki `R-###` ID'si alınır; mevcut ID'ler yeniden kullanılmaz.
- "Mevcut kontrol" yalnızca depo/deployment kanıtına bağlanabildiğinde `UNVERIFIED` etiketinden kurtulur; kanıt sütununa referans (dosya, commit, test çıktısı) yazılır.
- Riskler faz çıkış kapılarında gözden geçirilir; G0–G6 kapıları ([RELEASE_GATES.md](RELEASE_GATES.md)) ilgili risklerin durumunu kanıt olarak ister.