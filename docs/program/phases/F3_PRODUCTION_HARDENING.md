# F3 — Production Hardening

Faz durumu: `IN_PROGRESS` · Son güncelleme: 2026-08-13 (F3-EXIT-C2 — External Security-Settings Verification and Decision-Owner Acceptance; verification/evidence-only, hiçbir runtime/şema/production/dış-kontrol-düzlemi değişikliği yok; **çıkış kriteri 2 → `NOT_SATISFIED`, artık "değerlendirilmedi" değil, ölçülüp başarısız bulundu**; çıkış kapısı hâlâ `NOT SATISFIED`)

**F3-EXIT-C2 (2026-08-13).** Criterion 2'yi ilk kez fiilen ölçer. Baseline `origin/main` @ `021c43d5fbeae3c9b03c904ac30b0bb4708c80ad` (PR #406 merge commit), temiz. **Dört dış kalem canlı olarak sınandı, ikisi kapanmadı ve biri doğrudan `FAIL` verdi.** **GitHub — `FAIL`:** F3-SEC-EXIT-001 §5 madde 1 "Dependabot alerts **ve** Dependabot security updates ikisi de `Enabled`" diyor; canlı `gh api` ölçümü ikisini de **`disabled`** buldu (`vulnerability-alerts` → HTTP 404 *"Vulnerability alerts are disabled."*; `automated-security-fixes` → `{"enabled":false}`). Bu bir *kanıt eksikliği* değil, **gözlenmiş bir başarısızlık** — üç bağımsız uç nokta aynı sonucu verdi, hiçbiri yetki hatası değildi. Sonuç olarak R-075'in "üretimde `npm audit` = 0 açık" kapanışı **noktasal** kalır: şu anda **sürekli açık izleme yok**. Depo ayrıca **`public`** (`"private": false`) — secret scanning ve push protection bu yüzden ücretsiz ve **`enabled`** (ikisi de `PASS`). **TLS — `PASS`:** dört üretim host'unun tamamı **aynı** sertifikayı sunuyor (thumbprint `57063EAF…`, Let's Encrypt `CN=YE2`), SAN dördünü de kapsıyor, zincir doğrulandı, TLS 1.2/1.3 çalışıyor ve **TLS 1.0/1.1 sunucu tarafında reddediliyor** — bu son nokta yerel Schannel reddine dayandırılmadı, elle kurulmuş ham `ClientHello` kayıtları TCP üzerinden gönderilip dört host'tan da ölümcül `protocol_version` uyarısı alınarak **tel seviyesinde** kanıtlandı. Son kullanma `2026-09-26` (**44 gün**) — F3-SEC-EXIT-001'in kendi talimatı uyarınca güvenilmeyip **yeniden ölçüldü**. Şifre paketi envanteri/OCSP stapling/CT **`NOT VERIFIED`**, varsayımla `PASS` yapılmadı; otomatik yenileme de kanıtlanmadı. **Redis/replica — `PARTIAL`/`BLOCKED`:** kriterin "replica" kelimesi kaynağına kadar izlendi ve **API PM2 örnek sayısını** kastettiği saptandı (item L + §5 madde 3), Redis replikasyonunu veya PostgreSQL streaming replication'ı değil — ikincisi F7'ye tasarımla ertelenmiş (`F7_HORIZONTAL_SCALE_AND_HA.md:42`). `REDIS_URL`'in üretimde kurulu olduğu **kanıtlı** (`redis.ts:41` yalnızca değişken doluyken bu satırı basar; `/api/readyz` `redis=ok` bağlantının da çalıştığını gösterir); örnek sayısı depo sözleşmesine göre 1 (`exec_mode:'fork'`, `instances` yok) ama checklist'in kendi komutu (`pm2 jlist`) hiç çalıştırılmadı; Redis `bind`/`protected-mode`/`requirepass`/dış erişim **hiçbir şekilde kanıtlanmadı**. **Platform Admin MFA — `BLOCKED`:** MFA kayıtlıyken fail-closed çalışıyor (`platformAdmin.ts:95-107`) ama **kayıt zorunlu değil** (`schema.prisma:1547` nullable, kısıt yok), §5 madde 2'nin zorunlu tuttuğu kapsam SQL'i **hiç çalıştırılmadı**, ve login-anı MFA kapısının **hiç negatif testi yok** (`totpCode|MFA_REQUIRED|MFA_INVALID` tüm `server/src` içinde yalnız rota kaynağında geçiyor). **`R-073` ve `R-019` kapatılmadı ve kapatılamaz** — ikisi de `EXTERNAL_ACCEPTANCE_PENDING`; F3-PROD-001 §9'daki iki şablonun `Decision owner:` satırı **boş**, hiçbir yerde adlandırılmış karar sahibi yok, ve programın kendi kuralı bir ajanın kendi remediye ettiği riski kapatmasını yasaklıyor. **İki kapsam bulgusu kayda geçirildi, kolaya kaçılmadı:** (1) bu dosyanın 12. satırındaki dört kalemlik özet, atıf yaptığı checklist'in **on** kalemlik §5 listesinden dardır — kalan altı kalem (webhook secret'ları, `CSRF_SECRET`, nginx diff, firewall/WAF, yedek şifreleme/off-host, harici hata izleme) de karşılanmamış durumda; hangi listenin bağlayıcı olduğu **karar sahibine bırakıldı**, dar olan seçilmedi; (2) `:142`'deki "Security requirements" R-018 **ve** R-019'u birlikte anıyor ve R-019 kriter 2'nin blokeri sayılıyor — R-018'in de bloker olup olmadığı metinden **çözülemiyor**, uydurulmadı, karar sahibine işaretlendi. **Kriter 2 kapsamı dışında ama gerçek** olarak ayrıca raporlandı (kriteri sessizce genişletmemek için bloker sayılmadı): `main` ruleset'i **0 onay** ve **hiç zorunlu status check** istemiyor — oysa `ci-pr.yml:45-50` `PR Gate` işini tam da bu amaçla "tek kararlı check" olarak açığa çıkarıyor; CodeQL yok; private vulnerability reporting kapalı; Actions `allowed_actions:"all"` + SHA pinning yok; apex `noramedi.com` HSTS/CSP/XFO/XCTO başlıkları olmadan `200` dönüyor. **Kriter 1'e dokunulmadı, kriter 3 değerlendirilmedi ve çıkarsanmadı.** **`F3_EXIT_GATE = NOT SATISFIED`. `F3_COMPLETE = NO`. `F4_TRANSITION_AUTHORIZED = NO`.** Migration YOK, şema değişmedi, üretim erişimi yapılmadı, hiçbir dış ayar değiştirilmedi. Tam kanıt: [../evidence/F3-EXIT-C2_EXTERNAL_SECURITY_SETTINGS_VERIFICATION.md](../evidence/F3-EXIT-C2_EXTERNAL_SECURITY_SETTINGS_VERIFICATION.md).

Faz durumu: `IN_PROGRESS` · Son güncelleme: 2026-08-13 (F3-OBS-002-CLOSE — Live Observability Closure Evidence and Program-State Update; documentation/evidence-only, no production mutation by the task itself, production facts operator-executed and architecture-review-accepted; **R-074 → `CLOSED`**, **F3 çıkış kriteri 1 → `SATISFIED`**; **çıkış kapısı hâlâ: NOT SATISFIED** — kriter 2 ve 3 bu görevin kapsamı dışındaydı ve **değerlendirilmedi**)

**F3-OBS-002-CLOSE (2026-08-13).** Records the production activation, live drill verification and credential remediation of F3-OBS-002, and closes this phase's **first** exit criterion. Baseline `origin/main` @ `0478c86bf97b74b2aa9f465130d2a4daaa3579ec` — PR #405's merge commit, **which is also the deployed production SHA (exact match)**. Implementation: PR #405 `MERGED`, targeted tests **42/42**, PR CI **13/13**, **no migration**. **Live wiring:** three UptimeRobot monitors (`noramedi-api-livez`, `noramedi-api-readyz`, `noramedi-api-readyz-redis` asserting the body keyword `"name":"redis","status":"ok"` — the Redis-specific assertion, deliberately never the fail-open root `status`), 5-minute interval, email assigned 3/3; three Healthchecks.io dead-man checks (`noramedi-pm2`/`noramedi-disk`/`noramedi-backup`), Period 5 m / Grace 15 m, email assigned 3/3, fed by the repo-owned `noramedi-opscheck.sh` under a root systemd oneshot + 5-minute timer (`enabled`/`active`), with manual **and** scheduled executions both `exit=0`. **Drill A** (external assertion falsified against a healthy system): DOWN in **61 s**, real human DOWN email received, UP **12 s** after restore, recovery email `11:01:32Z`; an independent **15/15**-sample `/api/readyz` witness poll bracketing the entire incident proves `database ok`/`redis ok` throughout — a genuine negative control, not an outage. **Drill B** (disk telemetry withheld while the disk stayed healthy at `12% used`): human-alert latency **`20 m 02 s` exact**, detection latency **bounded `[19 m 05 s, 20 m 02 s]`** rather than falsely precise, recovery email `11:48:15Z`, suppression proven **selective** (`pm2`/`backup` pinged normally on all four suppressed runs). **Neither drill induced any application failure**, and neither stopped, restarted or reloaded anything: PM2 restart counts `12`/`12` and PIDs `607545`/`607578` unchanged end-to-end. **One rejected claim, preserved:** the drill report's `Secrets exposed = no` was **REJECTED** by architecture review after the full UUID-bearing `noramedi-disk` ping URL appeared in an operator screenshot; the credential was rotated and the old check **deleted** before closure, with rotation proven by exact one-to-one correspondence between host journal runs and replacement-check pings, and exposure scope operator-reviewed as **disk-only**. **R-074 → `CLOSED`** on its own stated criteria (not self-closed: implementation was PR #405, provider/host actions the operator's, both reports architecture-review-accepted). **Criterion 1 `SATISFIED`.** **Not claimed:** Sentry/error-tracking adoption (still deliberately deferred), log aggregation, OTel, elevated-5xx or TLS-expiry alerting, on-call rotation. **`KNOWN_BRITTLE` and recorded:** a key reorder in `readiness.ts`'s `{ name, status }` literal would silently disarm the Redis keyword monitor. Five documentation defects found by live validation corrected in the same PR; two monitoring subprocessors added to `docs/compliance/62-kvkk-subprocessor-register.md` §7 with **all legal/DPA/international-transfer fields `TO BE VERIFIED / COUNSEL REVIEW PENDING` — no legal or KVKK approval is asserted by this closure.** **Exit gate: `NOT SATISFIED`** — criteria 2 and 3 remain, and this task assessed neither. **F3 COMPLETE: `NO`. F4 transition authorized: `NO`.** `git diff --check` clean; no runtime/schema/migration/dependency/lockfile file changed. Full evidence: [../evidence/F3-OBS-002_LIVE_OBSERVABILITY_WIRING_ALERT_VERIFICATION.md](../evidence/F3-OBS-002_LIVE_OBSERVABILITY_WIRING_ALERT_VERIFICATION.md) §17–§20.

**Remaining F3 exit criteria after this closure, in this document's own order (§ "Exit gate (Çıkış kapısı)" below, verbatim and unreordered):**

| # | Criterion (as written below) | State | What remains |
|---|---|---|---|
| 1 | Gözlemlenebilirlik standardı canlıda kanıtla çalışıyor (log/metrik/trace/alarm) | **`SATISFIED`** (2026-08-13, F3-OBS-002-CLOSE) | Nothing for this criterion. R-074 `CLOSED`. |
| 2 | Güvenlik sertleştirme kontrol listesi kapatılmış | `NOT SATISFIED` — **unchanged and unassessed by this task** | Independent external verification of GitHub repository Code security and analysis settings, TLS certificate/protocol, Redis/replica topology, and platform-admin MFA-enrollment coverage (all `PASS_WITH_EXTERNAL_VERIFICATION`/`OPEN_EXTERNAL_CONFIGURATION` since F3-SEC-EXIT-001); plus decision-owner acceptance records for **R-073** and **R-019**, both still `CLOSURE_PROPOSED_AWAITING_EXTERNAL_CONFIRMATION`. |
| 3 | Olay müdahale prosedürü tatbikatla doğrulanmış | `NOT SATISFIED` — **unchanged and unassessed by this task** | A program-owner decision on whether F3-IR-001's explicitly `SIMULATED`/`NOT_PRODUCTION_VERIFIED` repository-based tabletop drill satisfies this criterion. No repository task can make this determination. |

The satisfaction of criterion 1 is **not** transitive to criteria 2 or 3, and does **not** advance the gate as a whole. `F3_EXIT_GATE = NOT SATISFIED`, `F3_COMPLETE = NO`, `F4_TRANSITION_AUTHORIZED = NO`.

Faz durumu: `IN_PROGRESS` · Son güncelleme: 2026-08-12 (F3-PROD-002 — Deploy-Script Execution Verification and API Ecosystem-Contract Confirmation; documentation-only, no production access by the task itself, facts operator-executed and user-supplied; the repository-synchronized deploy mechanism ran end-to-end with `deploy_exit=0`; **R-033 → `CLOSED`**, **R-040 → `CLOSED`**, R-077 half-resolved but still `OPEN`, R-063 evidence strengthened; **F3-IMPL-002 → `PRODUCTION_VERIFIED`**; **exit gate unchanged: NOT SATISFIED**)

**F3-PROD-002 (2026-08-12).** Closes the one gap F3-IMPL-002-PROD-RECON left precisely defined: the deploy script it synchronized had **never been executed**. Baseline `origin/main` @ `0d1b4ae9597b07dc0509f992a2a05663c015388f`; production deployed SHA `aa18b064267ff5846ae60f73889c3322030bd4a8`, unchanged before and after (`--skip-pull`). The two SHAs differ only in `docs/**` and all 8 contract files are byte-identical blob-for-blob, so the contract compared against production *is* the bytes production runs. **One operator invocation** of `/usr/local/sbin/noramedi-deploy.sh --skip-pull --skip-build --skip-migrate --skip-generate` — the minimal form exercising exactly steps 5–8 — produced **`deploy_exit=0`**. **Observed:** API reloaded from `ecosystem.config.cjs` (PID `607545`, `restart_time` `11`→`12`, `online`, `0` unstable, `cwd` `/var/www/noramedi/server`, `npm run start`, `role=api (declared=true) ownsJobs=false (RUN_BACKGROUND_JOBS=false — jobs delegated to a dedicated worker process)`); worker reloaded (PID `607578`, `11`→`12`, continuous with the `11` the prior reconciliation recorded — same PM2 app record, restarted once —, `online`, `0` unstable, `npm run start:worker`, `role=worker (declared=true) ownsJobs=true`, `All background jobs scheduled.`); step 8 `OK — PM2 process 'noramedi-worker' is online after 1 attempt(s).`, the exact `scripts/noramedi-deploy.sh:119` format literal; external `/api/health` `{"status":"ok"}`; both installed operational scripts hash-match their repository copies (`794375b4…`, `14a14898…`). **A 14-point reconciliation of the observed lifecycle against `ecosystem.config.cjs` + `scripts/noramedi-deploy.sh` found zero mismatches**, and required evidence A–F is satisfied in full by the single run. **`declared=true` is the load-bearing token** — `processRole.ts:75` is its only path and requires the env var present, recognized and matching, so it is direct runtime proof the ecosystem file's `env` block reached the process; an unset variable would still print `role=api` with `declared=false`. Likewise `ownsJobs=false` arises only on exact `RUN_BACKGROUND_JOBS === 'false'` (`backgroundJobsOwnership.ts:38`). Both are startup-log facts, not static config reads — no HTTP endpoint exposes `ownsJobs`. **Risk dispositions, each from its own row's stated criteria and none self-closed** (remediation F3-IMPL-002, installation F3-IMPL-002-PROD-RECON, execution the operator's): **R-033 `CLOSED`** (its named *"steps 5-8 in one successful run … `exit 0`"* — all five elements); **R-040 `CLOSED`** (its named *"ecosystem-sourced API `startOrReload` + `role=api (declared=true)` + `cwd` teyidi"* — all three, plus matching `script`/`args`); **R-034 not re-closed**, already `CLOSED` by the R2 reconciliation and merely corroborated with stronger provenance; **R-077 stays `OPEN`** with its `noramedi-healthcheck.sh` half resolved (first-ever measurement of that installed copy) but **no drift-prevention mechanism adopted**, PM2 reboot persistence still unevidenced; **R-063 strengthened, not closed**; **R-037** observation only. **Availability, measured and disclosed:** step 7 returned `000`/`000`/`000` then `200` at attempt **4 of 12**, API healthy ≈**19 s** after deploy start — bounded startup recovery the script explicitly permits and exactly what `ecosystem.config.cjs:25-33` predicts for the first ecosystem-driven `startOrReload`; **not a failure and not zero-downtime**, and input to later F7 HA work rather than a trigger for implementation here. **Operational-contract nuance:** `--skip-generate` suppressed deploy step 4 but **cannot** suppress Prisma Client generation, which also lives in `server/package.json:10-11`'s `start`/`start:worker` scripts — generation occurred at both startups (migration NO, pull NO, build NO, explicit generate phase SKIPPED, startup generation YES); this is R-063's own named root cause and warrants a separate task, not opened here. **Historical `pm2 logs` findings mapped to R-066/R-067/R-068/R-069 and to PR #252's revenue-report fix** (verified an ancestor of the deployed SHA), none attributed to this deploy for want of temporal evidence; the Redis `ECONNREFUSED` lines are explicitly **not** a current fault (`[redis] Shared store enabled via REDIS_URL.`, `/api/readyz` `redis=ok`). **Rollback-anchor correction, additive:** `b21cae911a0aa3444ebcd6e714a92c4f0802608a` **does exist on production** (type `commit`, subject `fix(external-calendar): enforce practitioner eligibility on the mapping write path (F3-DIGIDENTIS-MAP-001-R1)`) — no longer describable as globally nonexistent — but `git merge-base --is-ancestor` exits `1` against `aa18b06…`, so it is **off-lineage and invalid as the documented rollback anchor**; the valid anchor is **`aa18b064267ff5846ae60f73889c3322030bd4a8`** and the repository has zero tags. **F3-IMPL-002 → `PRODUCTION_VERIFIED`** (both halves). **Not claimed:** background-job *execution*, PM2 reboot persistence, drift prevention, zero-downtime. **Exit gate: `NOT SATISFIED`, unchanged** — R-033/R-040 are deployment/configuration-topology rows named by none of the three criteria, the same non-advancing precedent as R-034's and R-075's closures; criterion 1 gains only a *negative* signal, since the 19-second outage produced no automated detection at all (**R-074**'s cost, observed). **F3 COMPLETE: `NO`. F4 transition authorized: `NO`.** `git diff --check` clean; diff limited to `docs/program/**`. `AGENT_COMPLETED`/`PR_OPENED` — `NOT_MERGED`; **operator execution is not repository-task completion**. Full evidence: [../evidence/F3-PROD-002_DEPLOY_SCRIPT_EXECUTION_VERIFICATION.md](../evidence/F3-PROD-002_DEPLOY_SCRIPT_EXECUTION_VERIFICATION.md).

Faz durumu: `IN_PROGRESS` · Son güncelleme: 2026-08-12 (F3-IMPL-002-PROD-RECON — Production Worker Contract Verification and Deploy-Script Drift Reconciliation; documentation-only, no production access by the task itself; R-033/R-034/R-040 all remain `OPEN`, new risk R-077 added, **exit gate unchanged: NOT SATISFIED**)

**F3-IMPL-002-PROD-RECON (2026-08-12).** Reconciles a second set of operator-supplied production evidence against F3-IMPL-002's worker/deploy contract. Branch `docs/f3-impl-002-prod-recon`, baseline `origin/main` @ `aa18b064267ff5846ae60f73889c3322030bd4a8` — **which is also the supplied production deployed SHA (exact match)**, and is PR #401's squash-merge of F3-SEC-003. **Accepted production evidence:** `noramedi-api` online, `health`/`livez`/`readyz` all `200` with `database=ok`/`redis=ok`; a **stale `noramedi-worker` instance** (PID `603932`, `restart_count` `10`) discovered *after* deployment and manually reconciled via `pm2 startOrReload /var/www/noramedi/ecosystem.config.cjs --only noramedi-worker --update-env` to PID `605889` / `restart_count` `11` — clean `SIGINT`, `Prisma Client v7.9.1`, `role=worker (declared=true)`, `ownsJobs=true`, `All background jobs scheduled.`; and synchronization of the drifted deploy script (backup `…bak.20260812-174738`, `bash -n` clean both sides, installed `root:root` `0755`, both copies now `794375b4…`, `--help` smoke, **no deploy executed**). **Repository-side corroboration this task performed with no production access:** the supplied post-sync hash is byte-identical to `git show HEAD:scripts/noramedi-deploy.sh`, and the stale `8dffcd13…` is byte-identical to the blob at commit `96313f39` (2026-06-29) — so the installed script was **44 days stale and predated F3-IMPL-002 entirely**. **Consequence, and the main finding: F3-PROD-001's recorded deploy procedure is corrected.** Both 2026-08-12 production deploys ran the old six-step script (`pm2 reload noramedi-api --update-env` only — no `ecosystem.config.cjs`, no worker reload, no worker verification), so F3-PROD-001 §3's worker row described the **stale** instance (PM2 `online` ≠ running the deployed code) and its API row's `RUN_BACKGROUND_JOBS=false` comes from PM2's cached environment, not the repository-defined config source. An additive correction banner was added to that evidence file; its body is preserved unedited. **Risk dispositions, each argued from its own row's stated criteria — **[R1] one closed — `R-075` → `CLOSED`** (post-deploy production `npm audit --omit=dev` = `found 0 vulnerabilities` at the deployed SHA; production Prisma engine hash `e922089b…` byte-identical to the deployed lockfile pin; sole remaining condition was external confirmation, provided by the PR #402 reviewer; prior token preserved as history; narrow — continuous dependency monitoring, the accepted frontend `react-router` advisories and R-076 are untouched; **and explicitly non-blocking to this phase's exit gate**). **The rest not closed, and R-075's closure is not transitive to them:** **R-033** `OPEN` — automation now installed at the production invocation path and hash-verified, but **never executed** (the single worker reload was manual); **[R2] R-034** → **`CLOSED`** — its named missing control was verification of the real production `RUN_BACKGROUND_JOBS` value, which the API's observed `ownsJobs=false` supplies directly (only literal `'false'` yields that state per `backgroundJobsOwnership.ts`); with the worker owning jobs the matrix is **exactly one owner**. R1's provenance-based `OPEN` withdrawn as over-strict — provenance is R-040's. Registration-level only; no job execution claimed; prior token preserved; **does not touch this phase's exit gate**; **R-040** `OPEN — partially verified` — the worker app's first production application of `ecosystem.config.cjs` is confirmed (path resolves, `env` delivered, `declared=true`), the API app's `cwd`/`env` are not. **New risk R-077** (installed-vs-repository operational script drift; `scripts/noramedi-healthcheck.sh` sync state `UNVERIFIED`). **F3 exit gate: `NOT SATISFIED`, unchanged** — no criterion's status changes; criterion 1 gains an indirect negative signal (a stale worker went undetected and was found manually, not by any monitor) and criterion 2's evidence base is corrected rather than advanced. `AGENT_COMPLETED`/`PR_OPENED` — `NOT_MERGED`/`NOT_DEPLOYED`/`NOT_PRODUCTION_VERIFIED`. See [evidence/F3-IMPL-002-PROD-RECON_PRODUCTION_WORKER_CONTRACT_VERIFICATION.md](../evidence/F3-IMPL-002-PROD-RECON_PRODUCTION_WORKER_CONTRACT_VERIFICATION.md).

Faz durumu: `IN_PROGRESS` · Son güncelleme: 2026-08-12 (F3-SEC-003 — Production Dependency Vulnerability Remediation and Exploitability Classification; remediation + regression guard complete, PR opened, not merged, not deployed, not production-verified)

**F3-SEC-003 (2026-08-12).** Production Dependency Vulnerability Remediation and Exploitability Classification — the dedicated follow-up F3-PROD-001 opened for **R-075**. Fresh isolated worktree `f3-sec-003`, branch `fix/f3-sec-003-production-dependency-vulnerabilities`, baseline `origin/main` @ `aa8c467a62a75f845d99c9690ebaff69210a1a47` (PR #400 merge), clean, no drift. **All 8 advisories reproduced exactly** in `server/` (1 low / 5 moderate / 2 high) and itemized with exact dependency chains: `brace-expansion` ×2 (high) via `archiver@5.3.2`; `fast-uri` (high) via `@prisma/client → prisma → @prisma/dev → @prisma/streams-local → ajv`; `hono`/`@hono/node-server`/`valibot`/`@prisma/dev`/`prisma` (5 moderate) — **all one `@prisma/dev` subtree**, present only because `@prisma/client` declares `prisma` as an *optional peer*; `body-parser` (low) via `express@5.2.1`. **Topology correction:** production runs plain `npm ci` **including devDependencies** (`scripts/noramedi-deploy.sh:73/76`), so the production install is the full server tree — the count is 8 either way, but the Prisma CLI subtree really is installed in production. **Exploitability: none of the 8 is reachable from any NoraMedi request or job path**, each with code evidence rather than advisory-only reasoning — `brace-expansion` is unreachable on three independent layers (app code calls only `archive.append()`; the repo's own ambient typing does not expose `glob`/`directory`; and archiver's own lib never calls `archiver-utils`' glob helpers), and `fast-uri` is unreachable because NoraMedi never imports `ajv` anywhere (URL validation is `zod .url()`/WHATWG `URL`), a claim upgraded from `UNVERIFIED` to **verified** by an adversarial module-load trace of the Prisma CLI. **Remediated anyway, minimum safe fix:** Prisma family `7.8.0`→`7.9.1` (**minor within major 7 — no major upgrade**; `7.9.0` was insufficient as it still ships the vulnerable `@prisma/dev@0.24.14`, whereas `7.9.1` ships `0.24.17`, which is patched *and* drops `hono`/`@hono/node-server` entirely and pins the patched `valibot@1.4.2`), plus a **non-forced** `npm audit fix` for `brace-expansion` (1.1.14→1.1.18, 2.1.0→2.1.4), `fast-uri` (3.1.3→3.1.5) and `body-parser` (2.2.2→2.3.0); `allowScripts` keys re-pinned to 7.9.1; the orphaned `overrides["@hono/node-server"] = "1.19.13"` **removed** because it force-pinned a version *below* that advisory's own fix. `npm audit fix --force` was rejected outright. **`server/` is now 0 vulnerabilities in both `--omit=dev` and full scopes** (8 → 0), verified again after a clean-room `npm ci`. Frontend root tree 3 → 2: `js-yaml` (high) patched; **`react-router` accepted, not fixed** — the 6.x line ends at 6.30.4 and the fix lands only at 7.18.0 (a v6→v7 major), while both advisories are structurally inapplicable to this client-only `createRoot` SPA (no SSR hydration path; every navigation target is a literal or a client-built `/patients/${uuid}`). **New regression guard** `server/src/tests/dependencyExploitabilityInvariants.test.ts` (7/7, wired into `server:test:non-disposable`) freezes the structural invariants the non-exploitability rests on, rather than adding hypothetical DoS/SSRF payload tests against code paths that do not exist. **No schema/migration/data/tenant change** — 73 migrations, unchanged. **Verification:** `server` typecheck exit `0`; frontend `build` exit `0`; 8 root tooling typechecks/tests exit `0`; `guardrail:scan` and `log-privacy-guard:scan --strict-baseline` clean (no new violations, 103 grandfathered unchanged); `test:vitest` 141/141; `server:test:non-disposable` 1698 passed / 0 failed; all three disposable-runtime profiles (`postgres`, `postgres-compat`, `storage`) `outcome.exitCode: 0` with zero failures — these are load-bearing here because the Prisma **query-compiler** engine hash changed and tenant scoping is expressed through the `where` clauses it compiles; `npm ci` reproduces the tree clean-room at 0 vulnerabilities. **An independent adversarial review** was run against the applied change: all four core exploitability calls survived, and its 10 objections were folded in — most materially, the `@prisma/dev` 0.24.3→**0.24.17** 0.x jump is now reported explicitly, `valibot` is restated as *loaded at deploy time but not attacker-influenced* (the original "not reachable" label was wrong), a second `brace-expansion` chain via `exceljs → unzipper → fstream → rimraf → glob` was added to the inventory, and **rollback is corrected from "deterministic" to "lockfile-deterministic but not hermetic"** — the Prisma engine binary is an unhashed CDN download and the changed engine hash has never been fetched by the production host, making `binaries.prisma.sh` egress a **deployment prerequisite**. **New risk R-076** — an incidental, non-dependency **probable stored XSS**: `ClinicLegalProfile.website` accepts any scheme (no `.url()`, unlike the `.email()` fields two lines above) and is rendered as a live `href` on the unauthenticated public KVKK page; verified end-to-end and **deliberately not fixed here**, as this PR's scope is dependency remediation only. **R-075 → `CLOSURE_PROPOSED_AWAITING_EXTERNAL_CONFIRMATION`** (not self-closed, per this program's R-019/R-071/R-072/R-073 precedent) — post-deploy production `npm audit` has not been run; this task performed no production access. `AGENT_COMPLETED`/`TESTS_PASSED`/`PR_OPENED` — `NOT_MERGED`/`NOT_DEPLOYED`/`NOT_PRODUCTION_VERIFIED`. **Does not close F3 or satisfy its exit gate** — none of this phase's three named exit-gate criteria is touched, and R-075 was already classified non-blocking to that gate by F3-PROD-001. Full detail: [../evidence/F3-SEC-003_PRODUCTION_DEPENDENCY_VULNERABILITY_REMEDIATION.md](../evidence/F3-SEC-003_PRODUCTION_DEPENDENCY_VULNERABILITY_REMEDIATION.md).

Faz durumu: `IN_PROGRESS` · Son güncelleme: 2026-08-12 (F3-PROD-001 — Production Deployment and Exit-Gate Closure Reconciliation of user-supplied production evidence; documentation-only, this task performed no production access itself)

**F3-PROD-001 (2026-08-12).** Reconciles user-supplied evidence that the merged F3 train (F3-IMPL-001…006, F3-OBS-001, F3-SEC-002, F3-SEC-EXIT-001, F3-IR-001, F3-DIGIDENTIS-MAP-001) is now **deployed to production**: previous SHA `b21cae911a0aa3444ebcd6e714a92c4f0802608a` → deployed SHA `f0ff4c70abe08b192233d17cdcd1b7a77dbe4b08`, via the repository's own `scripts/noramedi-deploy.sh` procedure, with a verified pre-deploy backup (SHA-256 `de8bf3989f52a8d800319c3c778e3b347b5b61f53285f07ac29923a80edb1934`). Migration `20260811120000_add_platform_admin_password_changed_at` (R-073's column) confirmed applied (73 migrations, schema up to date). `noramedi-api`/`noramedi-worker` both `ONLINE`, matching the repository-defined `ecosystem.config.cjs` role/job-ownership contract (F3-IMPL-002) for the first time in production. `/api/health`, `/api/livez`, `/api/readyz` all `200` with real dependency checks passing (`database=ok`/`redis=ok`); normal Platform Admin login confirmed working; no destructive credential-revocation test performed. **This closes the single concrete first-customer blocker `F3-PROGRAM-RECON-001` §6 named ("deploying the merged F3 train to production") — it does NOT satisfy F3's exit gate.** All three named exit-gate criteria below remain unresolved: observability wiring is still repository-only-confirmed-live, not externally monitored/alerted (R-074 unchanged `OPEN`); the security checklist still has items pending external settings verification (GitHub security settings, TLS, Redis/replica topology, MFA-enrollment coverage) and R-073/R-019 remain `CLOSURE_PROPOSED_AWAITING_EXTERNAL_CONFIRMATION`, not independently closed; the incident-response drill remains simulated, sufficiency undecided. **New risk R-075 added** (production `npm audit`: 0 critical/2 high/5 moderate/1 low), assigned to a separate not-yet-started follow-up task F3-SEC-003 — no dependency changed by this task. **Correction found during this reconciliation:** this branch's own prior state was stale by 10 `origin/main` commits — **F3-IMPL-007 (PR #399) and F3-CI-OPT-001 (PR #398) are both confirmed `MERGED`**, ancestors of the very SHA (`f0ff4c70...`) deployed by this task, so the log-privacy regression guard is already live in production's deployed codebase, not merely repository-side as previously recorded. It still does not close R-018 (unchanged, `OPEN` — 103 grandfathered + 92 review-only sites remain unremediated, per its own evidence doc). **F3 COMPLETE: NO. F4 transition: NOT authorized.** Full detail, including the two-path R-073/R-019 acceptance-record templates prepared for the decision owner: [evidence/F3-PROD-001_PRODUCTION_DEPLOYMENT_AND_VERIFICATION.md](../evidence/F3-PROD-001_PRODUCTION_DEPLOYMENT_AND_VERIFICATION.md). No runtime/schema/migration/test file touched by this reconciliation task itself — diff limited to `docs/program/**`.

**F3-PROGRAM-RECON-001 (2026-08-12).** Reconciles this file's per-task "PR opened, not merged" wording against actual current `origin/main` state. **All six of the most recent F3 tasks below — F3-SEC-002 (PR #396), F3-IMPL-006 (PR #364), F3-IMPL-005 (PR #365), F3-OBS-001 (PR #363), F3-SEC-EXIT-001 (PR #362), F3-IR-001 (PR #361) — are now confirmed `MERGED` into `origin/main`**, each independently re-verified via `git merge-base --is-ancestor` plus a green post-merge `ci-main-and-nightly` run. Exact merge commits/CI run IDs: [evidence/F3-PROGRAM-RECON-001_POST_MERGE_RECONCILIATION.md](../evidence/F3-PROGRAM-RECON-001_POST_MERGE_RECONCILIATION.md) §2. **Per this program's own convention, the individual task narratives below are left unedited as dated historical/capture-time text, not rewritten in place** — their "NOT_MERGED" wording reflects each task's own point in time. **This does not close F3 or satisfy its exit gate.** Full three-criterion exit-gate matrix, lifecycle table, risk reconciliation (R-018/R-019/R-073/R-074 — none edited, all found already correct), first-customer-blocker classification, and F3-IMPL-007 status are in the evidence doc above. **F3 exit gate: NOT satisfied** — no live observability dashboard/alert-channel/uptime-probe evidence; security checklist has items pending external settings verification; incident-response drill was simulated/repository-based, sufficiency not yet decided by a program owner. Deployment and production verification of the entire merged F3 train remain outstanding and are the concrete next blockers for a real first customer. Does not authorize deployment. No runtime/schema/migration/test file touched — diff limited to `docs/program/**`.

**Corrected same-day, F3-PROGRAM-RECON-001-R1 (edited in place, same open PR #397, per this program's `+R1` same-task-revision convention).** F3-IMPL-007 ("Sensitive Runtime Logging Regression Guard") — originally recorded above as having no repository trace — is now confirmed by the program controller to exist as an external NoraMedi program-backlog item (**program-management input, not repository evidence**; a full `docs/program/` grep still finds zero repository trace). Its recorded dependency, "F3-IMPL-006 must be merged before F3-IMPL-007 starts," is satisfied: F3-IMPL-006 is confirmed `MERGED` above (PR #364, `c636311e`). **F3-IMPL-007 is therefore `UNBLOCKED` / `TO_DO`, `NO_REPOSITORY_IMPLEMENTATION_EVIDENCE`, `NOT_IMPLEMENTED`, `NOT_MERGED`, `NOT_DEPLOYED`, `NOT_PRODUCTION_VERIFIED`.** It is classified **non-blocking F3 exit-hardening follow-up** — none of this file's own three named exit-gate criteria below reference it, so it is not a formal exit-gate blocker on current repository evidence. It is the proposed regression-guard implementation for the gap `RISK_REGISTER.md` R-018 already names (missing lint rule/automated check preventing sensitive runtime logging regression) — it does **not** close R-018, and R-018's status is unchanged (`OPEN`). It is now the immediate next repository development task per the evidence doc's corrected §7/§8; production deployment planning for the merged F3 train remains immediately after it. Not implemented by this correction. Full detail: [evidence/F3-PROGRAM-RECON-001_POST_MERGE_RECONCILIATION.md](../evidence/F3-PROGRAM-RECON-001_POST_MERGE_RECONCILIATION.md) §7/§8 (as corrected).

Faz durumu: `IN_PROGRESS` · Son güncelleme: 2026-08-11 (F3-SEC-002 — Platform Admin JWT Session Revocation and Credential-Change Kill Switch; CRITICAL/first-customer security blocker, R-073; implementation + DB-backed tests complete, PR opened, not merged)

**F3-SEC-002 (2026-08-11).** Platform Admin JWT Session Revocation and Credential-Change Kill Switch — CRITICAL/first-customer security blocker, risk R-073. Fresh isolated worktree, branch `feature/f3-sec-002-platform-admin-session-revocation`, baseline `origin/main` @ `2d87d7dd3f9dcc3818703bf32814e70b091d2c3c` (PR #361/F3-IR-001 merge commit), clean, no drift. No `.codegraph/` directory exists in this repository — `codegraph_explore` (the MCP tool, index-independent) was used instead, targeted exactly at `middleware/platformAuth.ts`, `routes/platformAdmin.ts` (no separate `platformAuth*` route file exists), `scripts/platform-admin-recover-password.ts`, `services/platformAdminAudit.ts`, `schema.prisma`, and the existing platform-admin auth tests; no unrelated business domain scanned. **Confirmed root cause:** `authenticatePlatformAdmin` checked only JWT signature/expiry/type — unlike clinic-user auth's `authenticate()`, it never looked up the admin row, so a previously issued Platform Admin JWT (the highest-privilege identity in the system) stayed usable for its full 8h lifetime after a password reset, deactivation, or account deletion. The recovery CLI's own prior output already named this gap and its intended fix ("passwordChangedAt-based invalidation, mirroring the clinic User model") — this task implements exactly that. **Schema:** additive, nullable `PlatformAdmin.passwordChangedAt DateTime?` (migration `20260811120000_add_platform_admin_password_changed_at`), byte-for-byte mirroring `User.passwordChangedAt`; existing rows land `null` (no forced logout, pre-migration outstanding tokens keep working until their own natural ≤8h expiry). **Auth contract:** `authenticatePlatformAdmin` now does a persistent, uncached (deliberately — Platform Admin traffic is a handful of operators, and a cache would reintroduce a revocation-staleness window), fail-closed DB lookup every request, rejecting a missing/inactive admin, a missing/non-numeric `iat`, or an `iat` predating `passwordChangedAt` — all via the same generic 401, leaking no account-state distinction; `req.platformAdmin` is now populated from the DB row, never the token claim. **Credential-reset integration:** the emergency recovery CLI (the *only* credential-reset path in this codebase — no self-service password-change route exists) now sets `passwordChangedAt` in the same transaction as the password-hash update, so a reset genuinely revokes every outstanding session, not just future logins; its `sessionsInvalidated: number` (always `0`, with a comment saying revocation was impossible) is replaced with `credentialsInvalidatedAt: Date | null`, and its CLI output no longer claims tokens can't be revoked. **MFA decision:** MFA disable deliberately does **not** invalidate sessions — it requires re-proof of both password and TOTP, and the caller already holds a live authenticated session by definition; a threat-model decision recorded and asserted by a test, not an oversight. **Tests:** the pre-existing fabricated-JWT pattern (`generatePlatformToken({id:'admin-1',...})` fed to the middleware with no backing DB row) was refactored per-file to seed real disposable fixtures where the middleware now requires DB-backed identity (`platformAdmin.test.ts`'s `admin-1` fixture moved earlier in the file; `platformAdminPasswordRecovery.test.ts` extended with a real end-to-end CLI-then-middleware revocation proof; `platformBackup.test.ts`'s 3 un-awaited middleware calls fixed and its "valid token" case switched to a collision-proof random id, since it is a `server:test:non-disposable`/"zero external infra" member and must stay deterministic without a live DB; `adminScriptsLogPrivacy.test.ts`'s two `printResult` fixture literals updated for the renamed result field) plus a new dedicated `platformAdminSessionRevocation.test.ts` (12/12: valid-admin-accepted-from-DB-not-token, nonexistent-admin, inactive-admin including mid-session deactivation, before/after `passwordChangedAt` including an exact-second boundary proof, null-checkpoint-never-rejects, missing/non-numeric `iat` — the latter hand-HMAC-signed since `jwt.sign()` itself refuses to produce a non-numeric `iat`, cross-admin isolation two ways, no-sensitive-logging). **Verification:** `npm run typecheck` exit `0`; `npm run test:auth` (`platformAdmin.test.ts`) `82/82`; `npm run test:platform-admin-password-recovery` `22/22`; `npm run test:platform-admin-session-revocation` `12/12`; `npm run test:security-incidents` `57/57`; `npm run test:retention-manual-run-audit` `29/29`; `npm run test:platform-backup` `25/25`; `npm run test:platform-backup-audit` `3/3`; `npm run test:sms` `77/77`; full `npm run test:runtime:postgres` (`server:test:disposable-db`) and `npm run test:runtime:postgres-compat` (`server:test:legacy-db-required`) orchestrator runs both `outcome.exitCode: 0`, migrations applied cleanly (73 total), Docker cleanup confirmed; `npm run server:test:non-disposable` (68 members, re-run since this task touches two of its member files) exit `0`. **Migration verified** via `prisma migrate deploy`/`prisma migrate status` against a disposable Postgres 16. **Security/tenant/KVKK:** high positive security impact (bounds compromised-platform-admin-token lifetime to the next credential reset); zero tenant/clinic-facing behavior touched; reduces KVKK risk of privileged unauthorized tenant/patient-data access. **Rollback:** additive-only — application-code revert with the column retained is the recommended path; manual `DROP COLUMN` explicitly not recommended and not exercised. **R-073 (new risk, this task) status: `CLOSURE_PROPOSED_AWAITING_EXTERNAL_CONFIRMATION`** — not self-marked `CLOSED`, per this program's own convention that a task cannot unilaterally close the risk it just remediated. `AGENT_COMPLETED`/`TESTS_PASSED`/`PR_OPENED` — `NOT_MERGED`/`NOT_DEPLOYED`/`NOT_PRODUCTION_VERIFIED`. Full detail: [evidence/F3-SEC-002_PLATFORM_ADMIN_SESSION_REVOCATION.md](evidence/F3-SEC-002_PLATFORM_ADMIN_SESSION_REVOCATION.md).

Faz durumu: `IN_PROGRESS` · Son güncelleme: 2026-08-11 (F3-OBS-001-R1 — External Error-Telemetry Privacy Hardening, Risk-ID Reconciliation, and Main Reconciliation; same PR #363, exit gate still not satisfied)

**F3-OBS-001-R1 (2026-08-11, same worktree/branch/PR #363 as F3-OBS-001 below).** Fixes two architecture-review blockers found on F3-OBS-001: (1) `server/src/utils/errorTracking.ts`'s `captureFatalError` previously called `Sentry.captureException(err, ...)`, handing the SDK the raw caught error — `sendDefaultPii:false` only suppresses the SDK's own auto-attached request/user context, it does not sanitize what application code explicitly passes it, so `err.message`/`err.stack`/nested `err.cause` (routinely request-derived) would have reached an external third-party provider verbatim. Fixed: the function now never forwards `err` or anything derived from it — it sends only a fixed generic message plus `err.name`/requestId/role/safe-route-template/environment/release via `Sentry.captureMessage()` instead. New tests in `errorTracking.test.ts` (using a test-only injected fake Sentry module, mirroring `utils/readiness.ts`'s injected-dependency pattern) prove a sentinel error containing a patient name/email/phone/token never appears in the captured payload, including via a nested `Error.cause`. (2) PR #362 (open, `F3-SEC-EXIT-001`) independently claims **R-073** for the platform-admin session-revocation gap — this row's own R-073 (added by F3-OBS-001 below) collided and is **renumbered to R-074** across `RISK_REGISTER.md`/`NORAMEDI_MASTER_TRACKER.md`/`CURRENT_PHASE.md`/this file/`evidence/README.md`; R-073 itself untouched. Also reconciled against `origin/main` (PR #361/F3-IR-001, 2 commits) via a normal, non-force `git merge`; the resulting conflicts in this file and the other shared program-control docs (this section included — both F3-OBS-001's and F3-IR-001's entries preserved below) were resolved additively. No schema/migration change. `cd server && npm run typecheck` exit `0`. `AGENT_COMPLETED`/`TESTS_PASSED`/`PR_UPDATED` (pushed to existing PR #363) — `NOT_MERGED`/`NOT_DEPLOYED`/`NOT_PRODUCTION_VERIFIED`/`LIVE_ALERT_VERIFIED: NO`. Still does not satisfy this phase's exit gate.

**F3-OBS-001 (2026-08-11):** CRITICAL/F3-exit-gate task — the smallest repository-supported observability foundation for operating NoraMedi safely for the first customer, directly addressing this phase's own exit-gate wording ("gözlemlenebilirlik standardı canlıda kanıtla çalışıyor") which had zero repository-side signal beyond the pre-existing `/api/health`. Fresh worktree/branch `feature/f3-obs-001-production-observability-minimum`, baseline `origin/main` @ `92fc0c0c5eee34ae71bd2508bbfcc2f0309e3055` (PR #359/F3-WA-META-COEX-002-R4 merge commit — includes F3-IMPL-001…004(+R1), F3-DIGIDENTIS-MAP-001(+R1)), no drift. 16-item pre-work inventory classified every item (`PRODUCTION_READY`/`PARTIAL`/`MISSING`/`EXTERNAL_CONFIG_REQUIRED`) before writing any code. **Repository-side minimum delivered:** `GET /api/livez` (process-alive only, no dependency check) and `GET /api/readyz` (`server/src/routes/health.ts` + `server/src/utils/readiness.ts` — database mandatory/3s-bounded, matching `/api/health`'s pre-existing budget; Redis explicitly optional/non-blocking, consistent with `utils/redis.ts`'s own pre-existing fail-open design, not a new policy invented here), neither leaking a connection string, credential, or raw driver error (closed reason-code vocabulary only); `server/src/utils/fatalErrorHandlers.ts` installs `uncaughtException`/`unhandledRejection` handling in both `index.ts` and `worker.ts` — previously absent entirely, confirmed by repository-wide grep — reusing `utils/logger.ts`'s existing production-safe redaction policy via one export-visibility change (`safeErrorLog`, no behavior change); `server/src/utils/errorTracking.ts`, a low-lock-in Sentry-protocol-compatible boundary that is a pure no-op unless `SENTRY_DSN` is set and stays a safe no-op even then unless `@sentry/node` is separately installed (deliberately not added as a dependency by this task). **Evaluated reusing `OperationalEvent` for process-level fatal errors and found it structurally unsuited** — `organizationId` is a required, non-nullable field, so a tenant-less process crash cannot be represented without a schema change, which this task's own brief instructs against absent a compelling need; documented as a finding, not implemented. Full OTel tracing/metrics explicitly deferred as a P1/F7 follow-up, per the task's own instruction when full OTel would not be small/low-risk. **No worker HTTP server added** — PM2 process status remains the accepted worker-liveness signal (F3-IMPL-002 precedent, reaffirmed, not re-litigated). No schema/migration change; `GET /api/health`, `scripts/noramedi-healthcheck.sh`, `scripts/noramedi-deploy.sh`, `ecosystem.config.cjs`, and PM2 process names are all unchanged. New tests: `readiness.test.ts` (8/8), `healthRoutes.test.ts` (7/7), `fatalErrorHandlers.test.ts` (5/5), `errorTracking.test.ts` (4/4) — all pure/in-process (live `app.listen(0)` + injected fake dependency checks), no disposable Postgres/Redis required since `/readyz`'s real DB check is the same one-line query `/api/health` already runs in production, now unit-tested via dependency injection rather than needing a second live-DB integration test for an unchanged query. `cd server && npm run typecheck` exit `0`; regression re-run of `backgroundJobsOwnership.test.ts`/`processRole.test.ts`/`requestIdCorrelation.test.ts`/`httpRequestLogPrivacy.test.ts` (the logger-adjacent suite), 64/64, 0 failures. Defines a repository-visible alerting contract (§6 of the evidence doc: API unreachable, readiness failing, worker offline, database unavailable, repeated background-job failure, backup stale/failing, disk/storage critical, elevated 5xx) against existing or newly-added signals, and enumerates exactly what external/production-side action (§7: uptime-prober wiring, log-aggregation/alert-channel setup, optional `SENTRY_DSN`+`@sentry/node` provisioning, PM2/backup-staleness alert wiring, disk monitoring) would make any condition actually page a human — none of that external wiring is performed or claimed by this task. `RISK_REGISTER.md` R-074 added (the "no live observability/alerting" gap itself, distinct from and broader than the repository-side gaps this task closes). `AGENT_COMPLETED`/`TESTS_PASSED`/`PR_OPENED` — `NOT_MERGED`/`NOT_DEPLOYED`/`NOT_PRODUCTION_VERIFIED`/`LIVE_ALERT_VERIFIED: NO`. **Does not satisfy this phase's exit gate** — no live dashboard/alarm evidence exists or is claimed; F3 exit-gate status is unchanged (still not satisfied). Full detail: [evidence/F3-OBS-001_PRODUCTION_OBSERVABILITY_MINIMUM.md](../evidence/F3-OBS-001_PRODUCTION_OBSERVABILITY_MINIMUM.md).

Faz durumu: `IN_PROGRESS` · Son güncelleme: 2026-08-11 (F3-IMPL-005, Platform Admin Remaining Privileged Mutation Audit Coverage — closes R-019's audit-coverage gap at the repository level)

**F3-IMPL-005 (2026-08-11, Platform Admin Remaining Privileged Mutation Audit Coverage, R-019; branch `feature/f3-impl-005-platform-admin-audit-final`, dedicated worktree, baseline `origin/main` @ `92fc0c0c5eee34ae71bd2508bbfcc2f0309e3055`, no drift).** Direct follow-up to F3-IMPL-003, closing the exact 12-endpoint residual gap (org/clinic/plan/user lifecycle mutations [10], file-backup operational triggers [2]) that task deliberately deferred. A fresh re-derived inventory of all 70 authenticated platform-admin routes confirmed zero drift from F3-IMPL-003's classification. All 12 now write durable `PlatformAdminAuditEvent` rows via the existing `writePlatformAdminAuditEventInTx`/`writePlatformAdminAuditEvent` helpers — 10 transactionally (mutation+audit atomic, proven via forced-FK-violation rollback tests against real disposable Postgres), 2 via the same best-effort pattern already used by the DB-backup routes. **All 37 platform-admin mutation-only endpoints are now `AUDITED_DURABLY`; 0 remain `UNAUDITED_PERSISTED_MUTATION`.** No schema/migration change. `typecheck` exit `0`; `platformAdmin.test.ts` 118/118 (36 new); new `platformFileBackupAudit.test.ts` 5/5; `test:runtime:postgres-compat`/`test:runtime:postgres` both exit `0`. `RISK_REGISTER.md` R-019 row updated to `CLOSURE_PROPOSED_AWAITING_EXTERNAL_CONFIRMATION` (not unilaterally `CLOSED`, per this program's own R-071/R-072 precedent that self-verification is not risk acceptance) — R-019's break-glass-procedure/scope-boundary mitigations remain separately open. Does not change the overall F3 production-hardening exit gate (still not satisfied — no live observability dashboard, no security-hardening checklist sign-off, no incident-response drill). **Task status: `AGENT_COMPLETED`/`TESTS_PASSED`/`PR_OPENED`** ([PR #365](https://github.com/MustafaBasol/DisKlinikCRM/pull/365)) — `NOT_MERGED`/`NOT_DEPLOYED`/`NOT_PRODUCTION_VERIFIED`. Full detail: [evidence/F3-IMPL-005_PLATFORM_ADMIN_FINAL_AUDIT_COVERAGE.md](../evidence/F3-IMPL-005_PLATFORM_ADMIN_FINAL_AUDIT_COVERAGE.md).

**F3-IMPL-005-R1 (2026-08-11, architecture-review remediation of PR #365, same branch/worktree, not a second PR).** Architecture review accepted the core audit implementation above but held merge approval on one blocker: the 4 file-backup best-effort audit-write-failure `console.error` sites logged the raw `auditErr`, conflicting with the F3-IMPL-004/F3-IMPL-006 sensitive-logging policy. PR #364 (F3-IMPL-006) confirmed not yet merged; `origin/main` had advanced (PR #362) with zero file overlap, so no merge was needed — fixed independently, reusing the already-merged `safeErrorFields()` helper. All 4 sites now log `safeErrorFields(auditErr)`; 2 pre-existing structurally-identical sites elsewhere in the same file, confirmed to predate this PR, left out of scope. Two new negative tests (poisoned audit-write, sentinel PII/secret fixtures) prove the log line never contains raw error content — `platformFileBackupAudit.test.ts` 7/7 (was 5/5); `platformAdmin.test.ts` unchanged 118/118; `typecheck` exit `0`; 9 log-privacy suites re-run, 0 failures; `test:runtime:postgres`/`postgres-compat` both exit `0`. No schema/migration/tenant/auth change; audit coverage inventory unaffected (37/37). `RISK_REGISTER.md` R-019 row left `CLOSURE_PROPOSED_AWAITING_EXTERNAL_CONFIRMATION`. **Task status: `AGENT_COMPLETED`/`TESTS_PASSED`/`PR_UPDATED`** (pushed to existing PR #365) — `PR_CI_PENDING` (no new GitHub Actions run repository-wide for 20+ minutes after this push, not independently confirmed green) — `NOT_MERGED`/`NOT_DEPLOYED`/`NOT_PRODUCTION_VERIFIED`. Full detail: [evidence/F3-IMPL-005_PLATFORM_ADMIN_FINAL_AUDIT_COVERAGE.md](../evidence/F3-IMPL-005_PLATFORM_ADMIN_FINAL_AUDIT_COVERAGE.md) §11.

Faz durumu: `IN_PROGRESS` · Son güncelleme: 2026-08-11 (F3-IMPL-006-R1 — post-F3-IR-001 main reconciliation for PR #364; this row updated additively, both F3-IMPL-006 and F3-IR-001 entries below preserved in full)

**F3-IMPL-006-R1 (2026-08-11):** Post-F3-IR-001 Main Reconciliation for PR #364 — same worktree/branch `feature/f3-impl-006-runtime-log-hygiene-wave2` as F3-IMPL-006 below, no new branch/PR. PR #361 (F3-IR-001) merged first (merge commit `2d87d7dd3f9dcc3818703bf32814e70b091d2c3c`, `mergedAt` 2026-08-11T20:16:50Z), causing PR #364 to conflict on the shared program-control docs. Previous branch head `fa144091e77352da8a20f45edb40a1f804a87ea2`. Drift inventory (`92fc0c0c5eee34ae71bd2508bbfcc2f0309e3055..origin/main`) confirmed the only intervening event is PR #361’s 2 commits — shared docs (`CURRENT_PHASE.md`, `NORAMEDI_MASTER_TRACKER.md`, `RISK_REGISTER.md`, `evidence/README.md`, this file) plus two new files disjoint from PR #364 (`evidence/F3-IR-001_INCIDENT_RESPONSE_TABLETOP_DRILL.md`, `runbooks/F3_FIRST_CUSTOMER_INCIDENT_RESPONSE.md`); no schema/migration/package change; zero overlap with PR #364’s 49 changed runtime/test files. Normal `git merge origin/main` (no rebase, no force-push); 3 conflicts (`CURRENT_PHASE.md`, `NORAMEDI_MASTER_TRACKER.md`, this file; `RISK_REGISTER.md`/`evidence/README.md` auto-merged clean), all resolved additively — both PR #361’s program evidence and every prior F3-IMPL-006 entry preserved, neither side dropped. This reconciliation itself touches **zero** runtime/schema/package files — diff limited to `docs/program/**`. Post-merge validation: `cd server && npm ci && npx tsc --noEmit` plus the F3-IMPL-006 dedicated log-privacy/redaction suites re-run (see evidence file for exact results); CI left to GitHub’s disposable PostgreSQL/MinIO run. No F3-IMPL-006 architecture change. See [../evidence/F3-IMPL-006-R1_POST_F3-IR-001_MAIN_RECONCILIATION.md](../evidence/F3-IMPL-006-R1_POST_F3-IR-001_MAIN_RECONCILIATION.md).

**F3-IMPL-006 (2026-08-11):** PII and Message-Content Runtime Log Hygiene, Wave 2 — dedicated worktree, branch `feature/f3-impl-006-runtime-log-hygiene-wave2`, baseline `origin/main` @ `92fc0c0` (PR #359/F3-WA-META-COEX-002-R4 merge, includes F3-IMPL-004/-R1, F3-DIGIDENTIS-MAP-001/-R1 above), independently confirmed via `git fetch`, no drift. Direct follow-up to [F3-IMPL-004](#) below, which fixed every `SECRET_TOKEN`/`CONFIRMED_PII`/`PHI_MEDICAL` finding (43 sites) and explicitly deferred "~116 `POTENTIAL_PII` + 2 `MESSAGE_CONTENT`" sites as its own named next task. **Re-inventoried current `main` rather than trusting Wave 1's estimate:** 423 current `console.*`/`logger.*` call sites across `routes/`/`services/`/`jobs/`/`middleware/`/`utils/` (excluding tests and F3-IMPL-005's platform-admin conflict surface, confirmed clean at task start) — 214 `SAFE_METADATA`, 4 `DEBUG_ONLY`, 92 `POTENTIAL_PII_SAFE_AFTER_REVIEW`, 107 `RAW_ERROR_REQUIRES_SAFE_ERROR_FIELDS`, 6 `MESSAGE_CONTENT_REQUIRES_REMOVAL`, 0 `PII_REQUIRES_REDACTION`/`SECRET_REQUIRES_REMOVAL`/`UNKNOWN_REQUIRES_REVIEW`. **Fixed the full 113-site residual gap across 49 files** — every site now routes through the existing `safeErrorFields()` helper (or, for 6 SMTP-bounce mail-send catches, the pre-existing sibling `err.name`-only convention) — including Wave 1's own 2 explicitly-named deferred `instagramAiConversationProcessor.ts` message-save sites (current lines 2573/2642) plus 4 newly-discovered sites of the identical class (`metaWhatsAppAiProcessor.ts`'s WhatsApp-side message-persistence catches; two `instagramAiConversationProcessor.ts` appointment-create-error sites carrying `rawMessage: args.text`). 14 new + 7 extended test files (static-source-scan/runtime-spy, mirroring Wave 1's precedent), all registered in `server/package.json`. **Process note:** three parallel fix-agent groups working the same shared worktree each independently attempted `npm install`, producing 7 concurrent racing processes that corrupted `node_modules` — killed and replaced with one centralized `npm ci`; two test-authoring defects (a literal `*/` inside a JSDoc comment; two ambiguous static-scan label matches) found and fixed centrally, neither a production-code defect. `npx tsc --noEmit` exit `0`. 65 named test scripts: 57 pass outright, 8 initial failures — 7 share one root cause (no Postgres reachable in this sandbox at all, independently confirmed via `Test-NetConnection`, unrelated to this task's diff since no fix touches a Prisma query/schema/data-access path) and 1 was the test-anchor defect, fixed and re-run 65/65. Wave 1's own 6 dedicated log-privacy suites re-confirmed not regressed. No schema/migration/tenant/authorization/business-logic/provider-payload/AI-prompt change. **R-018 explicitly not marked closed** — 92 `POTENTIAL_PII_SAFE_AFTER_REVIEW` sites remain safe-by-manual-review rather than by enforced invariant, and no lint rule yet exists to prevent regression (Wave 1's own named future work, still not done); `RISK_REGISTER.md`'s R-018 row updated additively, status unchanged. `AGENT_COMPLETED`/`TESTS_PASSED` — `PR_OPENED` ([PR #364](https://github.com/MustafaBasol/DisKlinikCRM/pull/364)) — `NOT_MERGED`/`NOT_DEPLOYED`/`NOT_PRODUCTION_VERIFIED`. Full detail: [evidence/F3-IMPL-006_PII_MESSAGE_CONTENT_LOG_HYGIENE_WAVE2.md](../evidence/F3-IMPL-006_PII_MESSAGE_CONTENT_LOG_HYGIENE_WAVE2.md).

Faz durumu: `IN_PROGRESS` · Son güncelleme: 2026-08-11 (F3-SEC-EXIT-001-R1, security gate evidence correction and main reconciliation — corrects the dependabot.yml overstatement below and merges F3-IR-001; F3 exit gate remains explicitly NOT satisfied)

**F3-SEC-EXIT-001-R1 (2026-08-11, Security Gate Evidence Correction and Main Reconciliation, same branch/worktree/PR #362 as F3-SEC-EXIT-001 below).** Architecture review accepted R-073 and the overall assessment, but flagged that item W's evidence overstated `.github/dependabot.yml` as closing a "dependency/security-scanning mechanism" gap. **Corrected:** `.github/dependabot.yml` is dependency **update automation** only (scheduled version-update PRs) — it does not by itself prove Dependabot vulnerability alerts, Dependabot security updates, code scanning, or `npm audit` in CI are enabled; those remain `PASS_WITH_EXTERNAL_VERIFICATION`/`OPEN_EXTERNAL_CONFIGURATION` pending independent verification of the repository's Code security and analysis settings — the broader dependency/security-scanning gap is not claimed closed. Corrected item W's checklist status/description and §3.1 in the evidence doc, and every shared program doc that repeated the overstated claim. **R-073 unchanged, not renumbered** (F3-OBS-001 will use R-074). Also merges `origin/main` now that **PR #361 (F3-IR-001, entry below) has merged** — normal `git merge origin/main` (no rebase, no force-push), shared docs resolved additively, both histories preserved. No runtime/schema/migration file touched; diff limited to `.github/dependabot.yml` and `docs/program/**`. `cd server && npm run typecheck` re-run as baseline sanity. Full detail: [evidence/F3-SEC-EXIT-001_FIRST_CUSTOMER_SECURITY_HARDENING_GATE.md](../evidence/F3-SEC-EXIT-001_FIRST_CUSTOMER_SECURITY_HARDENING_GATE.md) §11 (R1). **Task status: `AGENT_COMPLETED`/`TESTS_PASSED` (baseline typecheck only)/`PR_UPDATED`** (existing [PR #362](https://github.com/MustafaBasol/DisKlinikCRM/pull/362), no new PR) — `NOT_MERGED`/`NOT_DEPLOYED`/`NOT_PRODUCTION_VERIFIED`. **F3 exit gate remains NOT satisfied**, unchanged by this correction.

**F3-SEC-EXIT-001 (2026-08-11, First-Customer Production Security Hardening Exit Checklist and Gap Closure, CRITICAL/F3-EXIT-GATE priority, dedicated worktree `E:\Ek Gelir\Siteler\DisKlinikCRM-worktrees\f3-sec-exit-001`, branch `feature/f3-sec-exit-001-first-customer-security-gate`, baseline `origin/main` @ `92fc0c0c5eee34ae71bd2508bbfcc2f0309e3055` — the PR #359/F3-WA-META-COEX-002-R4 merge commit, no drift).** Independently verified 24 named security checklist items (A–X) against current `origin/main`: production JWT/session-secret/DATABASE_URL/encryption-key fail-hard, cookie flags, CSRF enforcement, session expiry/invalidation, platform-admin and clinic-user MFA, platform-admin audit state, auth/public-endpoint rate limiting, Redis fallback, security headers, CORS, webhook signature verification, log PII exposure, production error responses, default/dev credential fallbacks, env-var validation, backup/admin privilege boundaries, worker process isolation, tenant-scope safeguards, dependency update automation, and TLS/nginx repo-vs-production evidence separation. **Most severe finding (item F): platform-admin sessions have no revocation mechanism at all** — `middleware/platformAuth.ts`'s `authenticatePlatformAdmin` does pure `jwt.verify`, zero DB lookup, so neither an `isActive` re-check nor a `passwordChangedAt` kill-switch exists (the column isn't even on the `PlatformAdmin` model), unlike the already-correct clinic-user path. **Deliberately not fixed**: the required schema-migration-plus-DB-lookup change would break the established test convention across 5 test files (`platformAdmin.test.ts` and 4 others) that construct fabricated, non-DB-backed admin JWTs specifically to test pure-JWT-layer behavior — assessed as exceeding "small, bounded" and risking exactly the authentication redesign this task's brief prohibits. Recorded as this task's own top `OPEN_BLOCKER` and new risk row **R-073**, with the exact recommended fix specified for a dedicated follow-up. **`.github/dependabot.yml` added (item W)** — dependency update automation (scheduled version-update PRs); the repository had zero mechanism of this kind before this task. **This does not by itself prove vulnerability alerts, Dependabot security updates, code scanning, or `npm audit` in CI are enabled — see the F3-SEC-EXIT-001-R1 entry above for the corrected, non-overstated description** (purely additive, cannot fail any existing workflow). **Two items explicitly deferred to their already-assigned owning tasks, not duplicated or re-worked here, per this task's explicit "DO NOT TOUCH" instruction:** item I (platform-admin audit coverage) → F3-IMPL-005 (open/unmerged); item P (PII/PHI log hygiene wave 2) → F3-IMPL-006 (open/unmerged). Re-confirmed correct/unchanged: item U (worker process role isolation, F3-IMPL-002) and items A/B/C/D/N/Q/T (secret fail-hard, cookie flags, CORS, error responses, backup privilege boundaries). Every production-dependent claim (Redis/replica topology, webhook-secret-per-connection completeness, TLS cert/protocol, platform-admin MFA-enrollment coverage, Dependabot-alerts repository setting) is `PASS_WITH_EXTERNAL_VERIFICATION` with an exact command in the evidence doc's §5 — none asserted as verified from repository evidence alone, per the task's own "no security theater" instruction (TLS/Redis/backup-encryption/secrets/firewall/WAF/error-tracking were never claimed verified from repo evidence alone). No schema/migration/route/response-shape change. `cd server && npm run typecheck` run as baseline sanity (no runtime file changed, so no broader suite was in scope per the brief's own "run tests only for runtime files actually changed" instruction). Full detail: [evidence/F3-SEC-EXIT-001_FIRST_CUSTOMER_SECURITY_HARDENING_GATE.md](../evidence/F3-SEC-EXIT-001_FIRST_CUSTOMER_SECURITY_HARDENING_GATE.md). **Task status: `AGENT_COMPLETED`/`TESTS_PASSED` (baseline typecheck only)/`PR_OPENED`** ([PR #362](https://github.com/MustafaBasol/DisKlinikCRM/pull/362)) — `NOT_MERGED`/`NOT_DEPLOYED`/`NOT_PRODUCTION_VERIFIED`. **This task's own explicit verdict: the F3 exit gate remains NOT satisfied** — item F/R-073 is an open blocker, F3-IMPL-005/F3-IMPL-006/F3-OBS-001 remain unmerged, and no F3-IR-001 incident-response drill has occurred. **Exact next task:** a dedicated "Platform-admin session revocation" follow-up (mirror `middleware/auth.ts:138-150`, add the `passwordChangedAt` migration, update the affected tests to seed real fixture rows); independently, program-owner review/merge decision for this task's PR.

**F3-IR-001 (2026-08-11).** First-Customer Incident Response Runbook and Tabletop Drill Evidence — documentation/runbook/evidence-only task, no application/runtime/schema/migration file changed. Fresh isolated worktree, branch `docs/f3-ir-001-incident-response-drill`, baseline `origin/main` @ `92fc0c0c5eee34ae71bd2508bbfcc2f0309e3055` (includes F3-DIGIDENTIS-MAP-001(+R1) and F3-WA-META-COEX-002-R4 below), clean, no drift. No `.codegraph/` directory exists in this repository, so CodeGraph was skipped per this session's own instructions and operational components (`server/src/index.ts`/`worker.ts`, `utils/logger.ts`/`jobLock.ts`/`processRole.ts`/`backgroundJobsOwnership.ts`, `services/backupService.ts`, `jobs/*`, `services/security/*`, `routes/platformSecurityIncidents.ts`, `routes/platformAdmin.ts`, `scripts/noramedi-deploy.sh`/`noramedi-healthcheck.sh`/`backup-restore-rehearsal.sh`, `ecosystem.config.cjs`) were read directly. Produced [../runbooks/F3_FIRST_CUSTOMER_INCIDENT_RESPONSE.md](../runbooks/F3_FIRST_CUSTOMER_INCIDENT_RESPONSE.md) — SEV-1/2/3 classification with concrete NoraMedi examples, a read-only operator toolkit, 10 required runbooks (API outage; worker offline; PostgreSQL unavailable; suspected tenant data exposure; leaked/revoked secret; Meta/WhatsApp provider outage; backup failure/restore requirement; object/file storage unavailable; high 5xx/error spike; security incident/suspicious privileged access), and a KVKK breach-path section that separates the existing technical `SecurityIncident` lifecycle from legal/controller notification responsibility — the latter recorded as `LEGAL_VERIFICATION_REQUIRED` (no invented deadline), citing `docs/compliance/KVKK_COMPLIANCE_AUDIT_AND_REMEDIATION.md`'s own "written breach-response plan content — not yet legally approved" and `LAUNCH_GATES.md` §2.H's outstanding per-clinic controller/processor determination and platform↔clinic DPA. Produced [../evidence/F3-IR-001_INCIDENT_RESPONSE_TABLETOP_DRILL.md](../evidence/F3-IR-001_INCIDENT_RESPONSE_TABLETOP_DRILL.md) — a `SIMULATED`/`NOT_PRODUCTION_VERIFIED` repository-based tabletop drill of (1) worker-stops-while-API-healthy (JobLock + per-recipient dedup-key duplicate-send protection, an explicitly-flagged day-boundary silently-missed-reminder edge case) and (2) suspected cross-tenant patient-data exposure (decision-owner-gated tenant suspension, audit evidence, narrow-scope-only determination, no destructive evidence cleanup, KVKK-breach-path handoff) — confirming every cited path/command/route against the actual repository and surfacing one unremediated gap (no per-patient-record freeze action exists, only organization-/clinic-level suspension) for a future task. **No production application behavior modified; no destructive command executed or simulated as executed.** Whether this satisfies the exit gate's "incident response procedure verified through drill" criterion is a program-decision-owner call, not self-declared here — the F3 exit gate remains **NOT** satisfied (no live observability dashboard, no signed-off security-hardening checklist, either). `AGENT_COMPLETED`/`DOC_VALIDATION_PASSED`/`PR_OPENED` — `NOT_MERGED`/`NOT_DEPLOYED`/`NOT_PRODUCTION_VERIFIED`/`NOT_A_REAL_INCIDENT_DRILL`. No `RISK_REGISTER.md` row status changed.

**F3-WA-META-COEX-002-R4 (2026-08-11, post-PR #360/F3-DIGIDENTIS-MAP-001-merge reconciliation, same branch/worktree/PR #359 as F3-WA-META-COEX-002 below).** Reconciles PR #359 against `origin/main` now that PR #360/F3-DIGIDENTIS-MAP-001(+R1) has also merged (merge commit `ab5f39a08a95fc5fdd2f7e7df03cd00c8522377a`, `mergedAt` 2026-08-11T14:39:04Z, post-main CI `ci-main-and-nightly` run `31502703267` `SUCCESS`, exact-head-verified). Normal `git merge origin/main` (no rebase, no force-push); conflicts limited to `NORAMEDI_MASTER_TRACKER.md`, `evidence/README.md`, and this file, all resolved additively — both PR #360's newly-merged program evidence and every prior F3-WA-META-COEX-001/002/R1/R2/R3 entry preserved in full, neither side dropped. This does **not** clear the overall F3 production-hardening exit gate, does **not** authorize any COEX production pilot/deployment, and does **not** authorize `F3-WA-META-COEX-003`. Both confirmed security findings (OAuth `state` CSRF gap; server-side Meta WABA/phone-number ownership-verification gap) remain documented, unfixed, and allocated to COEX-003/COEX-004. No Meta-provider-contract verdict changed by this reconciliation; Meta Dashboard evidence items #1–#3 remain outstanding. No runtime/schema/migration file touched by this reconciliation; diff limited to `docs/program/**`. Full detail: [evidence/F3-WA-META-COEX-002_META_APP_PRODUCTION_READINESS.md](../evidence/F3-WA-META-COEX-002_META_APP_PRODUCTION_READINESS.md) (R4 amendment).

**F3-WA-META-COEX-002-R3 (2026-08-11, post-F3-IMPL-004-merge reconciliation, same branch/worktree/PR #359 as F3-WA-META-COEX-002 below).** Reconciles PR #359 against `origin/main` now that PR #356/F3-IMPL-004 has merged (merge commit `13caabb2644d586097d133d72c258ceed33e1f35`, post-main CI `ci-main-and-nightly` run `31495634507` `SUCCESS`, both independently verified). Normal `git merge origin/main` (no rebase, no force-push); conflicts limited to this file, `CURRENT_PHASE.md`, and `evidence/README.md` (`NORAMEDI_MASTER_TRACKER.md` auto-merged clean), all resolved additively — both F3-IMPL-004/-R1's and the R1/R2 COEX narrative below preserved in full. The PR #356 runtime-overlap conflict-surface concern raised by R2 (`CURRENT_PHASE.md`) is now resolved, since PR #356 is merged rather than merely open. This does **not** clear the overall F3 production-hardening exit gate and does **not** authorize any COEX production pilot/deployment; both confirmed security findings (OAuth `state` CSRF gap, server-side Meta asset ownership-verification gap) remain documented and unfixed; Meta Dashboard evidence items #1–#3 remain outstanding. **`F3-WA-META-COEX-003` remains explicitly NOT authorized.** No runtime/schema/migration file touched by this reconciliation; diff limited to `docs/program/**`. Full detail: [evidence/F3-WA-META-COEX-002_META_APP_PRODUCTION_READINESS.md](../evidence/F3-WA-META-COEX-002_META_APP_PRODUCTION_READINESS.md) (R3 amendment).

**F3-WA-META-COEX-002-R1 (2026-08-11, program-control reconciliation + primary-source readiness correction, same branch/worktree as F3-WA-META-COEX-002 below).** Corrects two findings of the initial COEX-002 pass rather than resolving them as open questions. **(1) COEX-001 program-history discrepancy — resolved, not open:** F3-WA-META-COEX-001 (ClickUp `869egqrff`) was deliberately executed as `READ_ONLY`/`REPOSITORY_UNCHANGED`/`NO_PR`/`NOT_DEPLOYED`/`NOT_PRODUCTION_VERIFIED` and was subsequently reviewed and accepted by the program owner; its absence from Git/GitHub is expected, not evidence it did not happen. Recorded additively in `NORAMEDI_MASTER_TRACKER.md` §7 (previously absent — the initial COEX-002 pass declined to write it, per its own §0, precisely because it could not distinguish "intentionally no trace" from "did not happen"; the program owner has now clarified it was the former). **(2) `NORAMEDI_MASTER_TRACKER.md` and `CURRENT_PHASE.md` were not actually updated by the initial COEX-002 commit** despite this evidence file's own §14 claiming `CURRENT_PHASE.md` was — confirmed by `git diff origin/main --name-only` showing only the evidence file, `evidence/README.md`, and this phase file changed; both corrected in this R1 pass, and evidence file §14 corrected to state the sequence honestly. **(3) Primary Meta source access re-attempted and partially succeeded:** the official Meta Postman collection (`postman.com/meta/whatsapp-business-platform/...`) itself could not be fetched (returns an empty JS-rendered placeholder to this session's fetch tool), but `developers.facebook.com`'s official WhatsApp Business Platform documentation **was directly reachable in this session** (unlike the original COEX-002 session, which reported `EGRESS_BLOCKED`) and was used instead, per Gate 0's own hierarchy (primary Meta documentation is an accepted evidence class alongside the Postman collection and Dashboard evidence). Verbatim-confirmed, `PRIMARY_META_VERIFIED`, dated 2026-08-11: Embedded Signup is launched via the Facebook JavaScript SDK's `FB.login()` with `config_id`/`response_type: 'code'`/`override_default_response_type: true`/`extras.setup` (`developers.facebook.com/docs/whatsapp/embedded-signup/embed-the-flow/`); Advanced Access is required for `whatsapp_business_management` (and other permissions auto-selected in the flow) before general release (`.../embedded-signup/version-4`); the webhook-subscription endpoint is `POST /{Version}/{WABA-ID}/subscribed_apps` (`.../reference/whatsapp-business-account/subscribed-apps-api`); WABA discovery uses `client_whatsapp_business_accounts`/`owned_whatsapp_business_accounts` and system-user assignment uses `POST /{WABA-ID}/assigned_users` (corroborated via `developers.facebook.com`-scoped search, not a direct page fetch — one notch below the other verbatim-quoted findings); and Coexistence (`.../embedded-signup/onboarding-business-app-users/`) is explicitly defined as onboarding via an *existing* WhatsApp Business app account and phone number, with the documentation explicitly stating phone-number registration is **skipped** for Coexistence since the number is already registered — directly resolving the one question the initial COEX-002 pass flagged as most consequential and explicitly declined to assume. Coexistence-specific `extras.featureType`/`sessionInfoVersion` exact values, QR lifecycle, and country/account eligibility remain `UNVERIFIED` — not promoted past this session's access. Credit-line/billing ownership is **not** automatically a NoraMedi requirement — Meta's generic Embedded Signup partner flow distinguishes Tech Provider/Partner (customer adds own payment method) from Solution Partner (partner shares its credit line); classified `ARCHITECTURE_DECISION_REQUIRED`/`CONDITIONAL` on NoraMedi's eventual enrollment model, not asserted either way. **Task remains `STATE C — BLOCKED/USER_ACTION_REQUIRED`**, unchanged — the three Meta-Dashboard-dependent evidence items COEX-002 named are still outstanding. **`F3-WA-META-COEX-003` remains explicitly NOT authorized.** No runtime/schema/migration file touched; diff limited to `docs/program/**` (`NORAMEDI_MASTER_TRACKER.md`, `CURRENT_PHASE.md`, this file, `evidence/README.md`, the COEX-002 evidence file). `AGENT_COMPLETED`/`TESTS_PASSED: NOT_APPLICABLE`/`PR_OPENED` — `NOT_MERGED`/`NOT_DEPLOYED`/`NOT_PRODUCTION_VERIFIED`/`EXTERNAL_META_CONFIG_VERIFIED: NO`. See [evidence/F3-WA-META-COEX-002_META_APP_PRODUCTION_READINESS.md](evidence/F3-WA-META-COEX-002_META_APP_PRODUCTION_READINESS.md) §0/§4–9/§14.

**F3-WA-META-COEX-002 (2026-08-11):** NoraMedi Meta App production readiness & official Coexistence enrollment verification, run in parallel with F3-IMPL-004 (disjoint scope — this task touched only `docs/program/**`). Baseline `origin/main` @ `1e7c3bbd1012f0c83ec1747b9f12f5cde9c12661`, clean, no drift. **Predecessor F3-WA-META-COEX-001, which the task brief asserted was `ACCEPTED by program owner`, could not be corroborated anywhere in this repository or on GitHub** — a full `docs/program/` grep and a GitHub PR search for Coexistence/Embedded-Signup/WA-META terms found zero trace of it; recorded as an open program-history discrepancy (evidence doc §0/§13 item #0), not resolved here — no COEX-001 status entry was written into `NORAMEDI_MASTER_TRACKER.md`. Independently, targeted inspection (CodeGraph unavailable in this session — documented fallback to direct `Grep`/`Read`, bounded to the task's five named files plus `MetaCallbackPage.tsx`) **confirmed** several of COEX-001's reported findings straight from current source: the frontend launches Embedded Signup via a raw `facebook.com/{version}/dialog/oauth` popup rather than the Meta JS SDK (`src/pages/WhatsAppConnections.tsx:528-557`), the OAuth `state` parameter is round-tripped but never generated with a stored nonce nor validated anywhere in the callback path (`CONFIRMED` CSRF gap), and no `subscribed_apps`/phone-registration-PIN/token-refresh Graph API calls exist in the provider/callback code (`server/src/services/whatsapp/MetaCloudWhatsAppProvider.ts`, `server/src/routes/organizationWhatsApp.ts`, `server/src/services/metaTemplateService.ts`). A related, previously-unflagged gap was also found by direct inspection: the callback route persists client-submitted `wabaId`/`phoneNumberId`/`businessId` with no server-side Graph API call to confirm the exchanged access token actually owns that WABA/phone number. **All Meta-provider-contract verification (account model, permissions/Advanced Access, Embedded Signup JS SDK contract, Coexistence QR behavior, App Review, `subscribed_apps`/provisioning requirements) is classified `UNVERIFIED`** per this task's Gate 0: `WebFetch` to `developers.facebook.com` and `www.facebook.com` returned `EGRESS_BLOCKED` on every attempt, confirmed as a network-policy-level 403 via the proxy's own status endpoint (not transient) — independently reproducing COEX-001's own reported inability to reach primary Meta documentation. `WebSearch` snippets referencing a possible `extras.featureType`/`sessionInfoVersion` contract were found but explicitly **not** treated as final evidence per Gate 0. Task ends **`STATE C — BLOCKED/UNVERIFIED`** with three dashboard-dependent `USER ACTION REQUIRED` items (Business verification/App Review/Advanced Access status; actual production env-var presence; Embedded Signup Configuration ID and Coexistence feature availability) plus the COEX-001 history discrepancy. No Meta configuration made or recommended for action; no runtime file modified (`organizationWhatsApp.ts`, `metaWhatsAppWebhook.ts`, `services/whatsapp/**`, `WhatsAppConnections.tsx`, `schema.prisma`, migrations all untouched). `AGENT_COMPLETED`/`TESTS_PASSED: NOT_APPLICABLE`/`PR_OPENED` (pending) — `NOT_MERGED`/`NOT_DEPLOYED`/`NOT_PRODUCTION_VERIFIED`/`EXTERNAL_META_CONFIG_VERIFIED: NO`. Full detail: [evidence/F3-WA-META-COEX-002_META_APP_PRODUCTION_READINESS.md](../evidence/F3-WA-META-COEX-002_META_APP_PRODUCTION_READINESS.md).

**F3-DIGIDENTIS-MAP-001 (2026-08-11):** DigiDentiS integration pilot stabilization bug fix — Platform Admin → Harici Takvim → DigiDentiS mapping screen's NoraMedi-side practitioner dropdown showed only "—" for clinics with active dentists (DigiDentiS-side dropdown populated correctly; connection test green). Branch `fix/f3-digidentis-map-001-noramedi-practitioner-dropdown`, baseline `origin/main` @ `13caabb2644d586097d133d72c258ceed33e1f35` (includes F3-IMPL-002/003/004/-R1 above). **Root cause:** `GET /api/platform/clinics/:clinicId/external-calendar/local-options` (`server/src/routes/platformExternalCalendar.ts`) filtered strictly on `role: 'DENTIST'` against `User.role`. Per `utils/roles.ts`'s own documented normalization table, dentists can be stored with a legacy lowercase role (`doctor`/`dentist`) and/or have their effective role for a clinic come from `UserClinic.role` (branch assignment) rather than org-wide `User.role` — exactly the two conditions `routes/whatsapp.ts`'s pre-existing `getClinicPractitioners()` already accounts for via `role: { in: ['DENTIST', 'dentist', 'doctor'] }` across both `UserClinic` and `User.clinicId`. The strict equality filter silently excluded any such dentist; empty-dropdown behavior was data-shape-dependent, not universal, which is why it was not caught earlier. **Fix:** mirrored `getClinicPractitioners()`'s existing, already-shipped two-source/legacy-role-tolerant query pattern into `local-options` — no second practitioner source of truth introduced, no schema/migration change, no clinic/org scoping touched, DigiDentiS-side (`remote-options`) route untouched. New `platformExternalCalendarLocalOptions.test.ts`: real-HTTP E2E against the actual unmodified router (14/14: auth, multi-dentist, legacy-role, branch-role, non-dentist exclusion, inactive exclusion, other-clinic/other-org exclusion, single-dentist, zero-dentist-vs-404, DTO shape). Pre-existing `externalCalendarMapping.test.ts` (10/10) and `externalCalendarConnectionService.test.ts` (22/22) re-run unchanged, zero regression; `cd server && npm run typecheck` exit `0`. Does not change the F3 exit-gate status (unchanged, not satisfied). `AGENT_COMPLETED`/`TESTS_PASSED`/`PR_OPENED` ([PR #360](https://github.com/MustafaBasol/DisKlinikCRM/pull/360)) — `MERGED` (merge commit `ab5f39a08a95fc5fdd2f7e7df03cd00c8522377a`), `NOT_DEPLOYED`/`NOT_PRODUCTION_VERIFIED`. Full detail: [evidence/F3-DIGIDENTIS-MAP-001_NORAMEDI_PRACTITIONER_DROPDOWN_ROOT_CAUSE.md](../evidence/F3-DIGIDENTIS-MAP-001_NORAMEDI_PRACTITIONER_DROPDOWN_ROOT_CAUSE.md).

**F3-DIGIDENTIS-MAP-001-R1 (2026-08-11):** architecture-review remediation on PR #360, same branch `fix/f3-digidentis-map-001-noramedi-practitioner-dropdown`. Review accepted the read-path (dropdown) fix above but found a **blocking gap**: the practitioner-eligibility invariant it introduced was enforced only on `GET .../local-options`, not on the mapping write path — `upsertExternalCalendarMapping()`'s `verifyLocalEntityBelongsToClinic()` (`server/src/services/externalCalendar/externalCalendarMappingService.ts`) accepted any active clinic user as a `practitioner` mapping regardless of role, so a direct API call could still map a receptionist/manager/billing user to a DigiDentiS doctor even though the UI no longer listed them. **Fix:** extracted one shared contract — `listClinicPractitioners()`/`findClinicPractitioner()` — into `server/src/utils/relationGuards.ts` (the existing domain-neutral home for clinic-scoped entity lookups, already used across routes); both `local-options` and the mapping write-path validator now delegate to it, so they can never disagree about who counts as a practitioner. No second practitioner source of truth, no schema change, no scoping change; `relationGuards.ts`'s pre-existing exports were not modified, only two new functions added. New `externalCalendarMappingPractitionerEligibility.test.ts`: 18/18, covering branch-scoped and legacy eligible dentists, receptionist/clinic-manager/billing/inactive/other-clinic/other-org rejection at the actual write path (`upsertExternalCalendarMapping`, not just the shared helper), plus a direct cross-check that the write path's accept/reject set exactly matches the dropdown's listing. Pre-existing suites re-run: `platformExternalCalendarLocalOptions.test.ts` 14/14 unchanged, `externalCalendarMapping.test.ts` 10/10 (fixtures updated to add a `UserClinic` fake the new shared helper now always queries; assertions unchanged), `externalCalendarConnectionService.test.ts` 22/22 unaffected; `cd server && npm run typecheck` exit `0`. `test:runtime:postgres`/`postgres-compat` judged not required (only additive exports to a shared utils file) and not run — recorded judgment call. Does not change the F3 exit-gate status. `AGENT_COMPLETED`/`TESTS_PASSED` — updates existing [PR #360](https://github.com/MustafaBasol/DisKlinikCRM/pull/360), not a second PR — `MERGED` (merge commit `ab5f39a08a95fc5fdd2f7e7df03cd00c8522377a`, `mergedAt` 2026-08-11T14:39:04Z), post-main CI (`ci-main-and-nightly` run `31502703267`) `SUCCESS` (exact-head verified by F3-WA-META-COEX-002-R4) — `NOT_DEPLOYED`/`NOT_PRODUCTION_VERIFIED`. Full detail: [evidence/F3-DIGIDENTIS-MAP-001_NORAMEDI_PRACTITIONER_DROPDOWN_ROOT_CAUSE.md](../evidence/F3-DIGIDENTIS-MAP-001_NORAMEDI_PRACTITIONER_DROPDOWN_ROOT_CAUSE.md) §8-10.

**F3-IMPL-004 (2026-08-10):** PII/PHI runtime log hygiene, wave 1 — ran in parallel with F3-IMPL-002/F3-IMPL-003 under a separate task ID; dedicated worktree, branch `feature/f3-impl-004-pii-log-hygiene-wave1`, baseline `origin/main` @ `1909b186a01611c8be90313b7166085a887d05f4`. Full detail in [evidence/F3-IMPL-004_PII_PHI_LOG_HYGIENE_WAVE1.md](../evidence/F3-IMPL-004_PII_PHI_LOG_HYGIENE_WAVE1.md). Follows F3-IMPL-001 below, which fixed R-018 at 7 sites in `routes/platformAdmin.ts` and flagged the remaining repo-wide `console.*` sites as future work. Full production-runtime `console.*`/`logger.*` inventory across `routes/`, `services/`, `jobs/`, `scripts/`, `utils/`, `middleware/` (excluding tests): ~541 call sites classified `SAFE_METADATA`/`POTENTIAL_PII`/`CONFIRMED_PII`/`PHI_MEDICAL`/`SECRET_TOKEN`/`MESSAGE_CONTENT`/`DEBUG_ONLY`/`TEST_ONLY`. **Bounded wave 1: fixed every `SECRET_TOKEN` (7), `CONFIRMED_PII` (12), and `PHI_MEDICAL` (24) finding — 43 call sites across 18 files** — an Instagram/Facebook access-token prefix logged on every outbound Meta API call, two Google AI Studio API-key-in-URL leaks reachable via generic catch blocks, a `dropdb`-failure message leaking `PGHOST`/`PGPORT`/`PGUSER`, 9 admin/user emails removed or masked across 2 CLI scripts and 2 routes, 12 structured WhatsApp/Instagram assistant logs that redacted phone but still logged the co-located treatment/practitioner name in the clear, 5 clinical-write error catches routed through the existing `safeErrorFields()` helper instead of raw `err.message`, and 1 unmasked inbound rate-limiter sender identifier. **Explicitly not touched (deferred as wave 2):** ~116 `POTENTIAL_PII` sites and 2 `MESSAGE_CONTENT` sites (same underlying `console.error(label, err)`/raw-error pattern, but the leak is possible rather than routine — retrofitting would mean ~50+ additional files, a repo-wide rewrite this task is scoped not to do), plus ~378 `SAFE_METADATA` and 2 `DEBUG_ONLY` sites (benign, left alone per instruction). 6 new test files + 5 extended existing ones (negative test per changed seam, proving the sensitive fixture is absent and safe metadata remains, mix of runtime console-spy and static source-scan per the pre-existing `whatsappBookingFlowLogRedaction.test.ts` precedent) — all passing. `cd server && npm run typecheck` exit `0`; 13 regression suites across every touched domain re-run, 0 failures. No schema/migration, tenant-scope, or authorization change. `test:runtime:postgres` judged not required (no Prisma query/data-access path changed, only logging arguments) and not run — decision recorded rather than silently skipped. `AGENT_COMPLETED`/`TESTS_PASSED` — PR to be opened — `NOT_MERGED`/`NOT_DEPLOYED`/`NOT_PRODUCTION_VERIFIED`. **Exact next task:** a wave 2 scoped to the deferred `POTENTIAL_PII`/`MESSAGE_CONTENT` sites using the same fix pattern this wave established, plus a later lint-rule evaluation to prevent regression.

**F3-IMPL-004-R1 (2026-08-11):** reconciliation-only task — reconciles existing PR #356 (`feature/f3-impl-004-pii-log-hygiene-wave1`) against current `origin/main` now that F3-IMPL-002 (PR #357) and F3-IMPL-003 (PR #358, +R1) have both merged. Normal `git merge` (no rebase, no force-push). Overlap limited to program-control docs (this file, `CURRENT_PHASE.md`, `NORAMEDI_MASTER_TRACKER.md`, `evidence/README.md`) plus `server/package.json` test-script entries — all resolved additively, neither side's content dropped. Zero overlap between F3-IMPL-004's 18 changed production/test files and the F3-IMPL-002/F3-IMPL-003 changed-file set — no runtime conflict. Full detail: [evidence/F3-IMPL-004-R1_CURRENT_MAIN_RECONCILIATION.md](../evidence/F3-IMPL-004-R1_CURRENT_MAIN_RECONCILIATION.md).

**F3-IMPL-002 (2026-08-10):** second F3 implementation task; closes the worker/job-ownership gap F3-IMPL-001 deliberately left open (see that task's own docstring in `backgroundJobsOwnership.ts`: it declined to flip the API's job-ownership default until a dedicated worker process was actually part of the deployment topology). Baseline `origin/main` @ `1909b186a01611c8be90313b7166085a887d05f4` (PR #355/F3-IMPL-001 merge commit), independently confirmed via `git fetch`/`git rev-parse`, no drift. Pre-work inventory found the gap narrower than the task brief assumed: `server/src/worker.ts` (a dedicated worker entrypoint) already existed and already worked correctly (`JobLock`-protected, always owns jobs); the actual gap was entirely in deployment/process-management — no `ecosystem.config.*` in-repo (R-040), `scripts/noramedi-deploy.sh` only ever reloaded `noramedi-api` (R-033), and the API's job-ownership default (owns jobs unless `RUN_BACKGROUND_JOBS=false`) was never safely flippable without a repository-defined worker lifecycle (R-034). **Three coupled changes, in the same PR as required:** (1) `ecosystem.config.cjs` — repository-defined PM2 process contract for both `noramedi-api` and `noramedi-worker` (script/cwd/env), the api app explicitly setting `RUN_BACKGROUND_JOBS=false` now that the worker's lifecycle is itself repository-managed; (2) `scripts/noramedi-deploy.sh` — reloads/starts **both** processes via `pm2 startOrReload ecosystem.config.cjs --only <name> --update-env`, then verifies the worker reached PM2 `online` status (Node-parsed `pm2 jlist`, no HTTP endpoint added) before declaring success, aborting the deploy (`exit 1`) if it does not; (3) `NORAMEDI_PROCESS_ROLE` (`server/src/utils/processRole.ts`) — an optional, validated env var asserted at startup by both entrypoints, failing closed on an unrecognized/mismatched declared role while remaining fully backward compatible when unset. `resolveWorkerBackgroundJobsOwnership()` added alongside the existing `resolveApiBackgroundJobsOwnership()` so the intended production ownership matrix (worker owns, API does not, no duplicate, no zero-owner) is asserted by a test rather than read by eye. No schema/migration change; no HTTP surface added to the worker. `AGENT_COMPLETED`/`TESTS_PASSED`/`PR_OPENED` — `NOT_MERGED`/`NOT_DEPLOYED`/`NOT_PRODUCTION_VERIFIED` (this task performed no production deployment or verification — RISK_REGISTER.md R-033/R-034/R-040 remain `OPEN`, now with a repository-level mitigation recorded). Full detail: [evidence/F3-IMPL-002_PRODUCTION_WORKER_PROCESS_CONTRACT.md](../evidence/F3-IMPL-002_PRODUCTION_WORKER_PROCESS_CONTRACT.md).

**F3-IMPL-003 (2026-08-10):** Platform-Admin Privileged Mutation Audit Coverage (R-019) — the direct follow-up F3-IMPL-001 itself named as the next R-019 slice; runs in parallel with F3-IMPL-002 (disjoint files: only `routes/platformAdmin.ts` + its test file touched here). Full inventory of all 70 authenticated platform-admin routes (`platformAdmin.ts` 46, `platformSecurityIncidents.ts` 13, `platformExternalCalendar.ts` 11): 28 `READ_ONLY`, 5 `OUT_OF_SCOPE`, 19 already `AUDITED_DURABLY` (5 via `PlatformAdminAuditEvent`, 9 security-incident routes via a separate `SecurityIncidentActivity` trail, 5 external-calendar routes via `writeAuditLog()` — both left untouched, already covered), 18 `UNAUDITED` at task start. Closed the 6 highest-risk (priority classes 1–3 of 5): `POST /auth/mfa/{setup,verify,disable}` (security/auth config), `PUT`/`DELETE /sms-providers` (provider/credential config), `PATCH /privacy/data-retention/settings` (privacy/runtime control — the most glaring gap, its sibling `legacy-consent-correction` toggle already had this exact audit pattern). All six write `PlatformAdminAuditEvent` transactionally with the mutation, id-attributed, zero PII/secrets. 12 endpoints (priority classes 4–5: org/clinic/plan/user lifecycle, file-backup triggers) intentionally deferred — see evidence file §4. No schema change. `platformAdmin.test.ts`: 82/82 (22 new). Full `postgres`/`postgres-compat` orchestrator runs: both exit `0`. R-019 reduced, not closed — 25/37 mutation endpoints now audited, 12 remain. See [evidence/F3-IMPL-003_PLATFORM_ADMIN_AUDIT_COVERAGE.md](../evidence/F3-IMPL-003_PLATFORM_ADMIN_AUDIT_COVERAGE.md).

**F3-IMPL-001 (2026-08-10):** first F3 implementation task assigned and worked. Bounded pre-work inventory (items A–O) executed against `origin/main` @ `9cb256856b628a5ed5cc6ff85f84ec01a4a12cf0` (PR #354/F2-DOC-005 merge commit); full matrix in [evidence/F3-IMPL-001_FIRST_CUSTOMER_PRODUCTION_HARDENING.md](../evidence/F3-IMPL-001_FIRST_CUSTOMER_PRODUCTION_HARDENING.md). Five bounded, coupled runtime gaps closed: (1) `DATABASE_URL` production fail-hard (`utils/databaseUrl.ts`, closes a silent-local-Postgres-fallback risk), (2) explicit, unit-tested API/worker background-job ownership decision + startup diagnostic (`utils/backgroundJobsOwnership.ts`) — **deliberately does not flip the default**, see evidence doc for why, (3) `X-Request-Id` response header for client/log correlation (`middleware/requestId.ts`), (4) removed admin email from 7 `console.log` sites in `routes/platformAdmin.ts` (R-018), (5) durable `PlatformAdminAuditEvent` audit trail added for `POST /backups/run` and `POST /backups/restore-test`, previously fully unaudited (R-019). R-018/R-019 are **reduced, not closed** — most platform-admin mutation endpoints remain unaudited and app-level `console.log` PII discipline is not repo-wide; both are explicitly out of this task's bounded scope. `AGENT_COMPLETED`/`TESTS_PASSED`/`PR_OPENED` ([PR #355](https://github.com/MustafaBasol/DisKlinikCRM/pull/355)), `NOT_MERGED`/`NOT_DEPLOYED`/`NOT_PRODUCTION_VERIFIED`.

## Objective (Hedef)

Production ortamını kurumsal seviyeye sertleştirmek: gözlemlenebilirlik standardı (ADR-012), güvenlik sertleştirme, olay müdahale, log hijyeni (PII/PHI) ve platform-admin denetimi.

## Business reason (İş gerekçesi)

Pilot ve ticari lansman öncesi; kesinti, sızıntı ve yetki aşımı risklerinin operasyonel kontrollerle kapatılması gerekir. Gözlemlenemeyen sistem ölçeklenemez.

## Entry conditions (Giriş koşulları)

- F2 çıkışı
- F0-006 production topoloji kanıtı

## Exit gate (Çıkış kapısı)

- Gözlemlenebilirlik standardı canlıda kanıtla çalışıyor (log/metrik/trace/alarm)
- Güvenlik sertleştirme kontrol listesi kapatılmış
- Olay müdahale prosedürü tatbikatla doğrulanmış

## Dependencies (Bağımlılıklar)

- F2; ADR-012

## Allowed work (İzinli işler)

- İzleme/alarm kurulumu, log hijyeni, güvenlik başlıkları/limitleri, admin denetim izi

## Prohibited work (Yasak işler)

- Şema/mimari büyük değişiklikler (F5+ konusu)
- KVKK dondurma sınırı işleri (teyit gelmeden)

## Initial task backlog (Yüksek seviyeli kategoriler)

> Ayrıntılı görev ID'leri, F2 kanıtları incelendikten sonra atanacaktır.

- Gözlemlenebilirlik standardının uygulanması (ADR-012)
- PII/PHI log politikası ve log denetimi
- Platform-admin yetki denetimi ve break-glass prosedürü
- Rate limiting ve kötüye kullanım korumaları gözden geçirmesi
- Olay müdahale (incident response) runbook'ları ve tatbikat
- Güvenlik sertleştirme kontrol listesi (headers, TLS, secrets)

## Required evidence (Gerekli kanıt)

- Canlı izleme panosu/alarm kanıtı; tatbikat kayıtları; sertleştirme kontrol listesi çıktısı

## Required tests (Gerekli testler)

- Güvenlik regresyon testleri; smoke testleri; alarm tetikleme testleri

## Security requirements (Güvenlik gereksinimleri)

- R-018 (log sızıntısı) ve R-019 (admin aşımı) kontrollerinin kanıtla kapatılması

## Tenant requirements (Tenant gereksinimleri)

- İzleme/loglar tenant bazında ayrıştırılabilir ama izolasyonu bozmaz

## KVKK/privacy requirements (KVKK/gizlilik gereksinimleri)

- Log ve telemetri verilerinde veri minimizasyonu

## Rollback expectations (Geri alma beklentileri)

- İzleme/limit değişiklikleri konfigürasyonla geri alınabilir olmalı

## Risks (Riskler)

- R-003, R-018, R-019, R-022, R-073, R-074, R-075, R-076, R-077
- **Güncellendi 2026-08-13, F3-OBS-002-CLOSE:** `R-074` artık **`CLOSED`** (canlı dış-prob + dead-man alarm zincirleri üretimde doğrulandı, gerçek insana DOWN **ve** kurtarma e-postası kontrollü tatbikatla kanıtlandı, kanıt toplama sırasında ifşa olan izleme kimlik bilgisi kapanıştan **önce** rotasyona alınıp kalıcı olarak emekliye ayrıldı). Bu kapanış yalnızca **çıkış kriteri 1**'i karşılar; `R-018`, `R-019`, `R-073`, `R-076`, `R-077` **değişmedi** ve çıkış kapısı hâlâ `NOT SATISFIED`
- Deployment/process-topology riskleri, F3 kapsamında aktif olarak izleniyor — **güncellendi 2026-08-12, F3-PROD-002:** `R-033` ve `R-040` artık **`CLOSED`** (üretimde uçtan uca doğrulanmış deploy yaşam döngüsü; bkz. F3-PROD-002), `R-034` zaten `CLOSED` (F3-IMPL-002-PROD-RECON R2); **hâlâ `OPEN`: R-036, R-037, R-063, R-077** (R-077'nin yalnızca `noramedi-healthcheck.sh` yarısı çözüldü — sapma-önleme mekanizması ve PM2 reboot kalıcılığı açık)

## Open questions (Açık sorular)

- Gözlemlenebilirlik araç seti seçimi (ADR-012)

## Change history (Değişiklik geçmişi)

| Tarih | Görev | Değişiklik |
|---|---|---|
| 2026-07-17 | F0-001 | Faz dokümanı oluşturuldu (yüksek seviyeli). |
| 2026-08-10 | F3-IMPL-001 | Faz durumu `TODO` → `IN_PROGRESS`. First implementation task: bounded pre-work inventory (A–O) + 5 P0 runtime hardening fixes (DATABASE_URL fail-hard, background-job ownership diagnostics, X-Request-Id correlation, platform-admin email log removal, backup-endpoint audit trail). Exit gate NOT satisfied — no live observability dashboard, no security-hardening checklist sign-off, no incident-response drill evidence. R-018/R-019 reduced, not closed. See [evidence/F3-IMPL-001_FIRST_CUSTOMER_PRODUCTION_HARDENING.md](../evidence/F3-IMPL-001_FIRST_CUSTOMER_PRODUCTION_HARDENING.md). |
| 2026-08-10 | F3-IMPL-001-R1 | PR #355 exact-head CI (run `31394080617`) surfaced one deterministic Layer 5 failure: `platformAdmin.test.ts`'s toggle-log test still asserted the pre-R0 email-in-log contract that R0 itself had just removed for R-018. Test-only fix — no production code changed; the accepted R0 security decision (id logged, email never logged) is unchanged and now additionally covered by a new negative assertion. `platformAdmin.test.ts`: 60/60 (was 59/60). Full `postgres-compat` orchestrator: exit `0`. See evidence doc §25. |
| 2026-08-10 | F3-IMPL-002 | Production worker process contract, deploy lifecycle & job-ownership hardening. `ecosystem.config.cjs` (repository-defined PM2 contract for `noramedi-api`/`noramedi-worker`), `scripts/noramedi-deploy.sh` extended to reload/verify both processes (fails closed if worker doesn't reach `online`), `NORAMEDI_PROCESS_ROLE` startup guard (`server/src/utils/processRole.ts`, fails closed on mismatch, backward-compatible when unset), `resolveWorkerBackgroundJobsOwnership()` added for symmetry/testability. Closes R-033/R-040 at the repository level, reduces R-034; none closed/mitigated pending production deployment. See [evidence/F3-IMPL-002_PRODUCTION_WORKER_PROCESS_CONTRACT.md](../evidence/F3-IMPL-002_PRODUCTION_WORKER_PROCESS_CONTRACT.md). |
| 2026-08-10 | F3-IMPL-003 | Platform-Admin Privileged Mutation Audit Coverage (R-019): full 70-route inventory, 6 highest-risk endpoints (MFA setup/verify/disable, SMS provider PUT/DELETE, data-retention runtime-toggle PATCH) migrated onto `PlatformAdminAuditEvent`; 12 endpoints (org/clinic/plan/user lifecycle, file-backup triggers) intentionally deferred. No schema change. `platformAdmin.test.ts`: 82/82 (22 new). Both `postgres`/`postgres-compat` orchestrators: exit `0`. R-019 reduced (25/37 mutation endpoints audited), not closed. See [evidence/F3-IMPL-003_PLATFORM_ADMIN_AUDIT_COVERAGE.md](../evidence/F3-IMPL-003_PLATFORM_ADMIN_AUDIT_COVERAGE.md). |
| 2026-08-11 | F3-WA-META-COEX-002 | NoraMedi Meta App production readiness & official Coexistence enrollment verification (documentation/evidence-only). Predecessor F3-WA-META-COEX-001 not found anywhere in repo/GitHub despite the task brief's "ACCEPTED" claim — recorded as an open discrepancy, not resolved. Confirmed by direct code inspection: raw `facebook.com/dialog/oauth` launch (not Meta JS SDK), unvalidated OAuth `state` (CSRF gap), no `subscribed_apps`/phone-registration/token-refresh calls, no server-side WABA/phone ownership check on callback. All Meta-provider-contract questions (account model, permissions/Advanced Access, Embedded Signup contract, Coexistence QR behavior) classified `UNVERIFIED` — `developers.facebook.com`/`www.facebook.com` egress blocked by network policy in this session (confirmed via proxy status, not transient), reproducing COEX-001's own reported blocker. Ends `STATE C — BLOCKED/UNVERIFIED` with 3 dashboard-dependent `USER ACTION REQUIRED` items. No Meta config changed; no runtime file touched. See [evidence/F3-WA-META-COEX-002_META_APP_PRODUCTION_READINESS.md](../evidence/F3-WA-META-COEX-002_META_APP_PRODUCTION_READINESS.md). |
| 2026-08-11 | F3-IMPL-004-R1 | Reconciliation-only: existing PR #356 (F3-IMPL-004, `NOT_MERGED`) merged against current `origin/main` (now includes F3-IMPL-002/PR #357 and F3-IMPL-003/PR #358+R1). Normal `git merge`, no rebase/force-push. 5 conflicts, all in program-control docs + `server/package.json` test-script entries, all resolved additively; zero runtime-file conflicts (F3-IMPL-004's 18 changed files are disjoint from F3-IMPL-002/003's changed files). No schema/migration/tenant/authorization change. See [evidence/F3-IMPL-004-R1_CURRENT_MAIN_RECONCILIATION.md](../evidence/F3-IMPL-004-R1_CURRENT_MAIN_RECONCILIATION.md). |
| 2026-08-11 | F3-WA-META-COEX-002-R1/-R2/-R3 | R1: resolved COEX-001 program-history discrepancy (program-owner-accepted read-only audit), actually wrote `NORAMEDI_MASTER_TRACKER.md`/`CURRENT_PHASE.md` entries, upgraded 5 Embedded-Signup/Coexistence facts to `PRIMARY_META_VERIFIED` via `developers.facebook.com`. R2: narrowed `CURRENT_PHASE.md`'s "independent tracks" wording (COEX prep may run parallel to F3 hardening; COEX production is not) and flagged PR #356/F3-IMPL-004 as an active runtime-overlap surface. R3 (this reconciliation): merged `origin/main` now that PR #356 has merged (`13caabb2644d586097d133d72c258ceed33e1f35`, post-main CI run `31495634507` `SUCCESS`) — the R2 overlap blocker is resolved; Meta Dashboard evidence items #1–#3 remain outstanding; `F3-WA-META-COEX-003` remains NOT authorized. See [evidence/F3-WA-META-COEX-002_META_APP_PRODUCTION_READINESS.md](../evidence/F3-WA-META-COEX-002_META_APP_PRODUCTION_READINESS.md). |
| 2026-08-11 | F3-DIGIDENTIS-MAP-001 | Bug fix: NoraMedi practitioner dropdown empty on the DigiDentiS mapping screen. Root cause: `local-options` route filtered strictly on canonical-cased `role: 'DENTIST'` against `User.role`, missing legacy-cased (`doctor`/`dentist`) and branch-scoped (`UserClinic.role`) dentists — the same two conditions `routes/whatsapp.ts`'s `getClinicPractitioners()` already handles. Fix mirrors that existing pattern; no schema change, no scoping change, DigiDentiS side untouched. New E2E suite 14/14; 2 pre-existing suites re-run unchanged (32/32); typecheck clean. `AGENT_COMPLETED`/`TESTS_PASSED`/`PR_OPENED` ([PR #360](https://github.com/MustafaBasol/DisKlinikCRM/pull/360)) — `MERGED` — `NOT_DEPLOYED`/`NOT_PRODUCTION_VERIFIED`. See [evidence/F3-DIGIDENTIS-MAP-001_NORAMEDI_PRACTITIONER_DROPDOWN_ROOT_CAUSE.md](../evidence/F3-DIGIDENTIS-MAP-001_NORAMEDI_PRACTITIONER_DROPDOWN_ROOT_CAUSE.md). |
| 2026-08-11 | F3-DIGIDENTIS-MAP-001-R1 | Architecture-review remediation on PR #360: closed a blocking gap where the mapping **write path** (`upsertExternalCalendarMapping`/`verifyLocalEntityBelongsToClinic` in `externalCalendarMappingService.ts`) accepted any active clinic user as a practitioner mapping, regardless of role — only the read-path dropdown had enforced eligibility. Extracted one shared contract, `listClinicPractitioners()`/`findClinicPractitioner()`, into `utils/relationGuards.ts`; both `local-options` and the write-path validator now delegate to it. New `externalCalendarMappingPractitionerEligibility.test.ts`: 18/18 (branch/legacy accept, receptionist/manager/billing/inactive/other-clinic/other-org reject, read/write consistency check). 3 pre-existing suites re-run: 14/14 + 10/10 + 22/22, typecheck clean. No schema change. `AGENT_COMPLETED`/`TESTS_PASSED` — updates existing PR #360 — `MERGED` (merge commit `ab5f39a08a95fc5fdd2f7e7df03cd00c8522377a`, post-main CI run `31502703267` `SUCCESS`) — `NOT_DEPLOYED`/`NOT_PRODUCTION_VERIFIED`. See [evidence/F3-DIGIDENTIS-MAP-001_NORAMEDI_PRACTITIONER_DROPDOWN_ROOT_CAUSE.md](../evidence/F3-DIGIDENTIS-MAP-001_NORAMEDI_PRACTITIONER_DROPDOWN_ROOT_CAUSE.md) §8-10. |
| 2026-08-11 | F3-WA-META-COEX-002-R4 | Reconciliation-only: existing PR #359 merged against current `origin/main` now that PR #360/F3-DIGIDENTIS-MAP-001(+R1) has also merged (`ab5f39a08a95fc5fdd2f7e7df03cd00c8522377a`, post-main CI run `31502703267` `SUCCESS`, exact-head-verified). Normal `git merge`, no rebase/force-push. Conflicts limited to program-control docs (`NORAMEDI_MASTER_TRACKER.md`, `evidence/README.md`, this file), all resolved additively; zero runtime/schema/package/deployment file overlap. No Meta-provider verdict changed; OAuth CSRF gap and server-side WABA/phone ownership-verification gap remain open; COEX-003 remains NOT authorized. See [evidence/F3-WA-META-COEX-002_META_APP_PRODUCTION_READINESS.md](../evidence/F3-WA-META-COEX-002_META_APP_PRODUCTION_READINESS.md) (R4 amendment). |
| 2026-08-11 | F3-IMPL-006 | PII/PHI Runtime Log Hygiene Wave 2 — full residual gap Wave 1 named now closed: 113 sites across 49 files (`RAW_ERROR_REQUIRES_SAFE_ERROR_FIELDS` 107 + `MESSAGE_CONTENT_REQUIRES_REMOVAL` 6, incl. Wave 1's 2 explicitly-deferred sites + 4 newly-discovered ones of the same class), all routed through `safeErrorFields()` or the sibling `err.name` convention. 14 new + 7 extended test files. `typecheck` exit `0`; 65 named scripts, 65/65 green after fixing 1 test-authoring defect (7 other initial failures were a pre-existing no-local-Postgres environment limitation, independently confirmed, unrelated to this task's diff). No schema/migration/tenant/authorization change. R-018 reduced, not closed — 92 `POTENTIAL_PII_SAFE_AFTER_REVIEW` sites remain safe-by-review only, no lint-rule regression guard yet. See [evidence/F3-IMPL-006_PII_MESSAGE_CONTENT_LOG_HYGIENE_WAVE2.md](../evidence/F3-IMPL-006_PII_MESSAGE_CONTENT_LOG_HYGIENE_WAVE2.md). |
| 2026-08-11 | F3-SEC-EXIT-001 | First-customer production security-hardening exit checklist (24 items, A–X) independently verified against `origin/main`. Top finding: platform-admin sessions have zero revocation mechanism (`middleware/platformAuth.ts` — pure JWT verify, no DB lookup, no `isActive`/`passwordChangedAt` check) — new risk **R-073**, deliberately not fixed (would break the established fabricated-JWT test convention in 5 test files), reported as `OPEN_BLOCKER` with an exact recommended fix. `.github/dependabot.yml` added — dependency update automation (see F3-SEC-EXIT-001-R1 row below for the corrected description; originally overstated as closing dependency-scan availability). Items I/P explicitly deferred to F3-IMPL-005/F3-IMPL-006 (both open, not duplicated). All production-dependent claims marked `PASS_WITH_EXTERNAL_VERIFICATION` with exact commands, none asserted from repo evidence alone. No schema/runtime/route change. **F3 exit gate explicitly NOT satisfied by this task.** See [evidence/F3-SEC-EXIT-001_FIRST_CUSTOMER_SECURITY_HARDENING_GATE.md](../evidence/F3-SEC-EXIT-001_FIRST_CUSTOMER_SECURITY_HARDENING_GATE.md). |
| 2026-08-11 | F3-SEC-EXIT-001-R1 | Security gate evidence correction and main reconciliation, same PR #362. Architecture review accepted R-073 and the overall assessment, but flagged that item W's evidence overstated `.github/dependabot.yml` as closing a "dependency/security-scanning mechanism" gap. Corrected: `.github/dependabot.yml` is dependency update automation only (scheduled version-update PRs) — it does not by itself prove vulnerability alerts, Dependabot security updates, code scanning, or `npm audit` in CI are enabled; those remain `PASS_WITH_EXTERNAL_VERIFICATION`/`OPEN_EXTERNAL_CONFIGURATION` pending independent verification of repository Code security and analysis settings. R-073 unchanged, not renumbered (F3-OBS-001 will use R-074). Also merges `origin/main` now that PR #361/F3-IR-001 (row below) has merged, resolved additively. No runtime/schema change; diff limited to `.github/dependabot.yml` and `docs/program/**`. F3 exit gate remains NOT satisfied. See [evidence/F3-SEC-EXIT-001_FIRST_CUSTOMER_SECURITY_HARDENING_GATE.md](../evidence/F3-SEC-EXIT-001_FIRST_CUSTOMER_SECURITY_HARDENING_GATE.md) §11 (R1). |
| 2026-08-11 | F3-IR-001 | First-Customer Incident Response Runbook and Tabletop Drill Evidence (documentation/runbook/evidence only). SEV-1/2/3 classification, 10 required runbooks, KVKK breach-path section (legal timing/process marked `LEGAL_VERIFICATION_REQUIRED`, no invented deadline), and a `SIMULATED`/`NOT_PRODUCTION_VERIFIED` repository-based tabletop drill of a worker-stop scenario (JobLock + per-recipient dedup-key duplicate-send protection) and a suspected-cross-tenant-exposure scenario (decision-owner-gated containment, no destructive evidence cleanup). Surfaced one gap for a future task: no per-patient-record freeze action exists in the platform-admin API. No application/runtime/schema file changed; F3 exit gate remains NOT satisfied (no observability dashboard, no security-hardening checklist sign-off). See [../runbooks/F3_FIRST_CUSTOMER_INCIDENT_RESPONSE.md](../runbooks/F3_FIRST_CUSTOMER_INCIDENT_RESPONSE.md) and [../evidence/F3-IR-001_INCIDENT_RESPONSE_TABLETOP_DRILL.md](../evidence/F3-IR-001_INCIDENT_RESPONSE_TABLETOP_DRILL.md). |
| 2026-08-11 | F3-SEC-002 | Platform Admin JWT Session Revocation and Credential-Change Kill Switch (CRITICAL/first-customer security blocker, new risk R-073). Root cause: `authenticatePlatformAdmin` had no persistent DB-backed admin lookup at all — a Platform Admin JWT stayed valid for its full 8h life after a password reset/deactivation. Additive `PlatformAdmin.passwordChangedAt DateTime?` (migration `20260811120000_add_platform_admin_password_changed_at`, mirrors `User.passwordChangedAt`); middleware now does an uncached, fail-closed, per-request DB lookup (exists/active/`iat` vs `passwordChangedAt`, generic 401, no state leaked); emergency recovery CLI now sets `passwordChangedAt` transactionally with the password-hash update, so a reset genuinely revokes prior sessions. MFA disable deliberately does not invalidate sessions (recorded threat-model decision). Fabricated-JWT tests refactored to seed real disposable fixtures where the middleware now requires DB-backed identity; new dedicated `platformAdminSessionRevocation.test.ts` (12/12). `typecheck` exit `0`; `test:auth` 82/82; `test:platform-admin-password-recovery` 22/22; `test:platform-admin-session-revocation` 12/12; `test:security-incidents` 57/57; `test:retention-manual-run-audit` 29/29; `test:platform-backup` 25/25; `test:platform-backup-audit` 3/3; `test:sms` 77/77; full `test:runtime:postgres`/`test:runtime:postgres-compat` orchestrator runs both `outcome.exitCode: 0`; `server:test:non-disposable` (68 members) exit `0`. No tenant/clinic-facing change. R-073 `CLOSURE_PROPOSED_AWAITING_EXTERNAL_CONFIRMATION` — not self-marked `CLOSED`. `AGENT_COMPLETED`/`TESTS_PASSED`/`PR_OPENED` — `NOT_MERGED`/`NOT_DEPLOYED`/`NOT_PRODUCTION_VERIFIED`. See [evidence/F3-SEC-002_PLATFORM_ADMIN_SESSION_REVOCATION.md](../evidence/F3-SEC-002_PLATFORM_ADMIN_SESSION_REVOCATION.md). |
| 2026-08-11 | F3-IMPL-006-R1 | Reconciliation-only: existing PR #364 (F3-IMPL-006, `NOT_MERGED`) merged against current `origin/main` now that PR #361/F3-IR-001 has also merged (`2d87d7dd3f9dcc3818703bf32814e70b091d2c3c`, post-main event confirmed via `gh pr view 361`). Normal `git merge`, no rebase/force-push. Conflicts limited to program-control docs (`CURRENT_PHASE.md`, `NORAMEDI_MASTER_TRACKER.md`, this file), all resolved additively; zero runtime/schema/package/deployment file overlap (PR #364’s 49 files are disjoint from PR #361’s 2 new evidence/runbook files). No F3-IMPL-006 architecture change. See [../evidence/F3-IMPL-006-R1_POST_F3-IR-001_MAIN_RECONCILIATION.md](../evidence/F3-IMPL-006-R1_POST_F3-IR-001_MAIN_RECONCILIATION.md). |
| 2026-08-11 | F3-OBS-001 | First-customer production observability minimum: `GET /api/livez`/`GET /api/readyz` (DB mandatory, Redis optional/non-blocking); `uncaughtException`/`unhandledRejection` handling in both `index.ts`/`worker.ts`; a low-lock-in, no-op-until-configured Sentry-compatible error-tracking boundary. `OperationalEvent` reuse for process-level fatal errors evaluated and rejected (required non-nullable `organizationId`, no tenant context for a process crash) rather than adding a schema change. Full OTel deferred to P1/F7. No worker HTTP server added (PM2 status remains the accepted worker-liveness signal, F3-IMPL-002 precedent unchanged). No schema/migration; `/api/health`/deploy scripts/`ecosystem.config.cjs`/PM2 names unchanged. 24 new test assertions + 64 regression, all passing; `typecheck` exit `0`. Adds R-074 (no live observability/alerting exists). **Exit gate NOT satisfied** — this task is repository-side only; no live dashboard/alert-channel/uptime-probe evidence exists or is claimed. See [evidence/F3-OBS-001_PRODUCTION_OBSERVABILITY_MINIMUM.md](../evidence/F3-OBS-001_PRODUCTION_OBSERVABILITY_MINIMUM.md). |
| 2026-08-11 | F3-OBS-001-R1 | External error-telemetry privacy hardening + risk-ID reconciliation + main reconciliation (same PR #363). `captureFatalError` no longer forwards the raw caught error to the external provider (fixed generic message + `err.name`/requestId/role/route/environment/release only, via `Sentry.captureMessage()`); new sentinel-secret tests prove it. R-073 (added by F3-OBS-001 above) renumbered to R-074 to avoid colliding with PR #362's independent R-073. Merged `origin/main` (PR #361/F3-IR-001), all conflicts (this file, `RISK_REGISTER.md`, `NORAMEDI_MASTER_TRACKER.md`, `CURRENT_PHASE.md`) resolved additively. No schema/migration. `typecheck` exit `0`. Exit gate still NOT satisfied. See [evidence/F3-OBS-001_PRODUCTION_OBSERVABILITY_MINIMUM.md](../evidence/F3-OBS-001_PRODUCTION_OBSERVABILITY_MINIMUM.md). |
| 2026-08-12 | F3-PROGRAM-RECON-001 | Post-merge program-state reconciliation (documentation-only). Independently re-confirmed F3-IMPL-005/006, F3-OBS-001, F3-SEC-002, F3-SEC-EXIT-001, and F3-IR-001 (PRs #365/#364/#363/#396/#362/#361) all `MERGED` into `origin/main` with green post-merge CI — prior per-task `NOT_MERGED` wording above is capture-time, left unedited. Full exit-gate matrix, lifecycle table, and risk reconciliation (R-018/R-019/R-073/R-074 — all found already correctly stated, none edited): [evidence/F3-PROGRAM-RECON-001_POST_MERGE_RECONCILIATION.md](../evidence/F3-PROGRAM-RECON-001_POST_MERGE_RECONCILIATION.md). **F3 exit gate: NOT satisfied** — no live observability evidence, security checklist pending external verification, IR drill sufficiency undecided. Deployment/production verification of the merged train are the concrete next blockers. Minor gap noted, not fixed: this table has no row for F3-IMPL-005(+R1) despite prose coverage above — left for a future documentation-hygiene pass. No runtime/schema/migration/test file touched. |
| 2026-08-12 | F3-PROGRAM-RECON-001-R1 | F3-IMPL-007 program-control correction (documentation-only, same PR #397, edited in place). Program controller confirms F3-IMPL-007 ("Sensitive Runtime Logging Regression Guard") exists as an external backlog item with dependency on F3-IMPL-006 — program-management input, not repository evidence; repository trace remains zero. F3-IMPL-006's merge is independently repository-confirmed (row above, PR #364). F3-IMPL-007 is therefore `UNBLOCKED`/`TO_DO`, `NOT_IMPLEMENTED`, `NO_REPOSITORY_IMPLEMENTATION_EVIDENCE`. Classified non-blocking F3 exit-hardening follow-up (not a formal exit-gate criterion); addresses R-018's already-tracked regression-guard gap without closing R-018. No repository evidence fabricated, nothing implemented. F3 exit gate unchanged: NOT satisfied. See [evidence/F3-PROGRAM-RECON-001_POST_MERGE_RECONCILIATION.md](../evidence/F3-PROGRAM-RECON-001_POST_MERGE_RECONCILIATION.md) §7/§8. |
| 2026-08-12 | F3-IMPL-002-PROD-RECON | Production Worker Contract Verification and Deploy-Script Drift Reconciliation (documentation/program-control only; no production access by the task, facts operator-supplied). Baseline `origin/main` @ `aa18b064267ff5846ae60f73889c3322030bd4a8` = the supplied production deployed SHA, exact match. Accepts: API online + `health`/`livez`/`readyz` `200` (`database=ok`/`redis=ok`); a **stale worker** (PID `603932`, `restart_count` `10`) found post-deploy and manually reconciled via `pm2 startOrReload /var/www/noramedi/ecosystem.config.cjs --only noramedi-worker --update-env` → PID `605889`, clean `SIGINT`, `Prisma Client v7.9.1`, `role=worker (declared=true)`, `ownsJobs=true`; deploy-script synchronization (both copies now `794375b4…`, `root:root` `0755`, `bash -n` clean, backup taken, **no deploy executed**). Repository-side corroboration identified the stale installed script **exactly** as the 2026-06-29 blob (`8dffcd13…` = commit `96313f39`) — 44 days stale, predating F3-IMPL-002. **Corrects F3-PROD-001 §2/§3** (additive banner, body preserved): both 2026-08-12 deploys ran the old worker-blind script, so the worker row described a stale instance and the API's `RUN_BACKGROUND_JOBS=false` is PM2-cached, not ecosystem-sourced. **R-033/R-034/R-040 remain `OPEN`** with sharper wording (installed-but-never-executed / worker-verified-API-not / partially-verified respectively); **new risk R-077** (installed-vs-repository script drift). No runtime/schema/CI/deploy-script change; diff limited to `docs/program/**`; `git diff --check` clean. **Exit gate NOT satisfied — unchanged.** See [evidence/F3-IMPL-002-PROD-RECON_PRODUCTION_WORKER_CONTRACT_VERIFICATION.md](../evidence/F3-IMPL-002-PROD-RECON_PRODUCTION_WORKER_CONTRACT_VERIFICATION.md). |
| 2026-08-12 | F3-PROD-002 | Deploy-Script Execution Verification and API Ecosystem-Contract Confirmation (documentation/program-control only; no production access by the task, facts operator-executed and user-supplied). Closes the one gap the previous row left precisely defined: **the synchronized deploy script had never been executed.** Baseline `origin/main` @ `0d1b4ae9597b07dc0509f992a2a05663c015388f`; production SHA `aa18b064267ff5846ae60f73889c3322030bd4a8`, unchanged before and after; the two differ only in `docs/**` and all 8 contract files are byte-identical blob-for-blob. One operator run of `/usr/local/sbin/noramedi-deploy.sh --skip-pull --skip-build --skip-migrate --skip-generate` → **`deploy_exit=0`**. API reloaded from `ecosystem.config.cjs` (PID `607545`, `11`→`12`, `cwd` `/var/www/noramedi/server`, `npm run start`, `role=api (declared=true) ownsJobs=false`); worker reloaded (PID `607578`, `11`→`12`, continuous with the prior `11`, `npm run start:worker`, `role=worker (declared=true) ownsJobs=true`, `All background jobs scheduled.`); step 8 `OK — PM2 process 'noramedi-worker' is online after 1 attempt(s).`; `/api/health` `{"status":"ok"}`; both installed scripts hash-match repo (`794375b4…`, `14a14898…` — the healthcheck's first-ever measurement). **14-point contract reconciliation: zero mismatches; required evidence A–F satisfied.** **R-033 → `CLOSED`**, **R-040 → `CLOSED`** (each on its own row's criteria, neither self-closed); R-034 corroborated, not re-closed; **R-077 still `OPEN`** (healthcheck half resolved, drift-prevention mechanism and reboot persistence open); **R-063 strengthened, not closed** — first raw committed measurement of the reload window (`000`×3 → `200` at 4/12, ≈19 s; bounded recovery, **not zero-downtime**, not a failure) plus the finding that `--skip-generate` **cannot** suppress `prisma generate`, which lives in `server/package.json:10-11`'s start scripts; R-037 observation only. Historical `pm2 logs` findings mapped to R-066/R-067/R-068/R-069 and PR #252's revenue fix, none attributed to this deploy; Redis fallback lines explicitly not a current fault. Rollback anchor corrected additively: `b21cae91…` exists on production but is **off-lineage** (`merge-base --is-ancestor` exit `1`) — valid anchor is `aa18b06…`. **F3-IMPL-002 → `PRODUCTION_VERIFIED`** (both halves). No runtime/schema/CI/deploy-script change; diff limited to `docs/program/**`; `git diff --check` clean. **Exit gate NOT satisfied — unchanged; F3 COMPLETE: NO; F4 authorized: NO.** See [evidence/F3-PROD-002_DEPLOY_SCRIPT_EXECUTION_VERIFICATION.md](../evidence/F3-PROD-002_DEPLOY_SCRIPT_EXECUTION_VERIFICATION.md). |
| 2026-08-13 | F3-OBS-002-CLOSE | Live Observability Closure Evidence and Program-State Update (documentation/evidence-only; no production mutation by the task, production facts operator-executed and architecture-review-accepted). Records F3-OBS-002's production activation and live verification: UptimeRobot `livez`/`readyz`/`readyz-redis` (body-keyword Redis assertion, never the fail-open root `status`) + Healthchecks `pm2`/`disk`/`backup` (Period 5 m / Grace 15 m, email 3/3) driven by a root systemd oneshot + 5-minute timer; manual and scheduled runs both `exit=0`. **Drill A** — external assertion falsified against a healthy system: DOWN in **61 s**, real human DOWN email, UP **12 s** after restore, recovery email `11:01:32Z`, with a **15/15**-sample independent `/api/readyz` witness poll bracketing the incident (`database ok`/`redis ok` throughout) proving it a negative control rather than an outage; the DOWN-email header time is recorded as **`NOT PRECISELY MEASURED`** rather than estimated. **Drill B** — one dead-man ping withheld while the disk stayed healthy at `12% used`: human-alert latency **`20 m 02 s` exact**, detection latency **bounded `[19 m 05 s, 20 m 02 s]`** (the provider UI exposes minutes only), recovery email `11:48:15Z`, suppression proven **selective**. Zero collateral impact: PM2 `12`/`12` restarts, PIDs `607545`/`607578` unchanged, nothing stopped or restarted, no migration. **R3:** the drill report's `Secrets exposed = no` was **REJECTED** by architecture review (the `noramedi-disk` UUID ping URL was visible in an operator screenshot); the credential was rotated, rotation proven by one-to-one correspondence of journal runs to replacement-check pings, and the old check paused then **deleted** before closure — exposure scope operator-reviewed as **disk-only**. **R-074 → `CLOSED`; exit criterion 1 → `SATISFIED`.** Five live-validation documentation defects corrected in the same PR (missing `install -d /etc/noramedi`; an incorrect Drill B service/timer-restart instruction — `EnvironmentFile` is re-read on every oneshot `ExecStart`; Period+Grace restated as **~20 min** to dead-man DOWN rather than a 15-minute SLA; a provider-UI `+02:00` timezone hazard; an obsolete host artifact flagged for post-merge operator cleanup, not executed here). UptimeRobot and Healthchecks.io added to [../../compliance/62-kvkk-subprocessor-register.md](../../compliance/62-kvkk-subprocessor-register.md) §7 with **all legal/DPA/international-transfer fields `TO BE VERIFIED / COUNSEL REVIEW PENDING` — no legal/KVKK approval asserted.** **Exit gate NOT satisfied — criteria 2 and 3 were out of scope and are unassessed; F3 COMPLETE: NO; F4 authorized: NO.** No runtime/schema/migration/dependency/lockfile file changed; `git diff --check` clean. See [../evidence/F3-OBS-002_LIVE_OBSERVABILITY_WIRING_ALERT_VERIFICATION.md](../evidence/F3-OBS-002_LIVE_OBSERVABILITY_WIRING_ALERT_VERIFICATION.md) §17–§20. |
