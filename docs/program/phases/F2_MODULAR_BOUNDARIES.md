# F2 — Modular Boundaries and Public Contracts

Late-breaking correction (same task, discovered before commit, 2026-08-04, F2-DOC-001): while drafting this file's own F2-DOC-001 edits below, `gh pr view 318` was re-checked and now shows **F2-SEC-001 / PR #318 has `MERGED`** — merge commit `2452b8c4a4fcd963878bbc0fbc37045d9cef170c`, `mergedAt: 2026-08-04T08:18:03Z`; post-merge main CI confirmed: run `30891381612` (`ci-main-and-nightly`, `push`) `success`, all 9 `ci-layers` check-runs `success`. **F2-SEC-001 status: `MERGED` / `MAIN_CI_PASSED` / `NOT_DEPLOYED` / `NOT_PRODUCTION_VERIFIED`.** References below reading F2-SEC-001/PR #318 as `OPEN`/`NOT_MERGED` were accurate moments earlier in this same task and are now stale, not rewritten in place. **F2-SEC-002 / PR #319 remains `OPEN`/`NOT_MERGED`.** Guardrail CI implementation remains **NOT AUTHORIZED** and Stage 2 Imaging remains **BLOCKED** while F2-SEC-002 is open — closing one of two blockers does not authorize either. No evidence conclusion, count, or classification changes; the F2-SEC-001/F2-SEC-002 branches themselves are not touched.

**F2-DOC-001-R1 correction (2026-08-04, PR #320 review follow-up):** the paragraph immediately above is a frozen, dated correction from the F2-DOC-001 task itself and is left unedited, per this program's own reconciliation convention. It was flagged by review as internally contradictory with the "Faz durumu" line directly below it, which still read F2-SEC-001/PR #318 as `OPEN`/`NOT_MERGED` — that line **is** this document's live current-status summary, not an append-only log entry, so it is corrected in place below (not merely appended around). **F2-SEC-002 / PR #319 evidence refreshed** (re-verified via `gh pr view 319`/`gh pr checks 319`, independent of the paragraph above): current head `fb4e23766478db413f9e34f009302d4f4f0948f7`, PR CI run `30893070050` (`ci-layers`, all jobs) `success`, still `OPEN`/`NOT_MERGED`/`NOT_DEPLOYED`/`NOT_PRODUCTION_VERIFIED` — the head cited above (`e53116c393d02be900ce48e187e1d90c59a6d015`, see Change history) is superseded by later commits on the same PR #319 branch. F2-SEC-001/PR #318's `MERGED`/`MAIN_CI_PASSED` status (above) is unchanged and re-confirmed. This correction does **not** close F2-SEC-002, does **not** authorize guardrail CI, and does **not** unblock Stage 2 Imaging — it corrects only the internal contradiction in the current-status line.

**F2-DOC-001-R2 correction (2026-08-04, PR #320 review follow-up):** the R1 paragraph immediately above is left unedited, per this program's own convention; it recorded F2-SEC-002/PR #319 as `OPEN`/`NOT_MERGED` at head `fb4e23766478db413f9e34f009302d4f4f0948f7`, accurate at the time, now stale. **PR #319 has since merged:** `gh pr view 319` confirms state `MERGED`, merge commit `09ee20b7f1f655a4025a32927a8e81e596e1bb97`, independently confirmed an ancestor of `origin/main` (`git merge-base --is-ancestor`, exit `0`; also now `origin/main`'s exact tip). This branch was reconciled against that advanced `origin/main` via `git merge --no-ff` — zero conflicts, since PR #319's runtime changes and its two new evidence files (`F2-SEC-001_INSTAGRAM_INBOX_CLINIC_MEMBERSHIP.md`, `F2-SEC-002_WHATSAPP_EXPLICIT_CLINIC_RESOLUTION.md`) did not touch this or any of the other three shared program-control documents. **Post-merge main CI status at the time of this paragraph:** run `30949232722` — `status: in_progress`, now stale, see R3 below. **F2-SEC-002 status at the time of this paragraph: `MERGED` / `MAIN_CI_PENDING`.** This correction did **not** authorize guardrail CI and did **not** unblock Stage 2 Imaging.

**F2-DOC-001-R3 correction (2026-08-04, PR #320, same-task CI resolution):** the R2 paragraph immediately above is left unedited, per this program's own convention; it recorded post-merge main CI run `30949232722` as `in_progress` — that run has since completed. Re-verified via `gh run view 30949232722`: `headSha` `09ee20b7f1f655a4025a32927a8e81e596e1bb97` (exact match), `status: completed`, `conclusion: success`, `event: push`, `workflowName: ci-main-and-nightly`. **F2-SEC-002 final status: `MERGED` / `MAIN_CI_PASSED` / `NOT_DEPLOYED` / `NOT_PRODUCTION_VERIFIED`.** **Both concrete security blockers are now repository-level closed.** The "Faz durumu" line below is corrected in place to match. This correction does **not** authorize guardrail CI, does **not** unblock Stage 2 Imaging, and does **not** accept any canonical tenant-scoped mutation contract — each requires a separate, explicit program-owner decision. A further F2-SEC-003 reconciliation (deployment/production-verification for F2-SEC-001/F2-SEC-002) remains required.

Faz durumu: `PREPARATION_IN_PROGRESS` (boundary/tooling/module **implementation**: Stage 1's own scope — the additive, zero-caller `ImagingLifecyclePort` facade skeleton — is now implemented by F2-IMPL-001-A, re-implemented by F2-IMPL-001-A-R2 against the F2-PREP-009-amended explicit-`clinicId` contract (all four methods now require an explicit, caller-supplied, authorization-validated `clinicId`), and corrected by F2-IMPL-001-A-R3 (`redactForAnonymization`'s legal-hold check is now enforced atomically inside the mutation's own write predicate, closing a TOCTOU race, not only via an earlier in-memory read); caller migration, Stage 2+, and program-wide boundary enforcement remain `TODO`; **current status (corrected by F2-SEC-003, 2026-08-05): F2-SEC-001/PR #318 and F2-SEC-002/PR #319 are both `MERGED`/`MAIN_CI_PASSED` (merge commits `2452b8c4a4fcd963878bbc0fbc37045d9cef170c`/`09ee20b7f1f655a4025a32927a8e81e596e1bb97`, post-merge main CI runs `30891381612`/`30949232722`, both `success`) and F2-SEC-003 independently re-confirmed both CONFIRMED CLOSED at the repository level via direct source-code and test-file read plus verification of already-passing CI-owned disposable-PostgreSQL test results from PR CI and post-merge main CI (no local/task-specific test re-execution); deployment readiness classified F2-SEC-001 `CODE_DEPLOYABLE`, F2-SEC-002 `CONFIGURATION_VERIFICATION_REQUIRED` (production `WhatsAppConnection`/`ClinicWhatsAppConnection` topology not inspected — no production access); repository-only, non-blocking/report-only guardrail implementation is now `AUTHORIZED` (see `F2-GUARDRAIL-IMPL-001`, not yet implemented); CI-blocking/production enforcement remains explicitly `NOT_AUTHORIZED`, gated on baseline/false-positive validation of the guardrail's own report-only output plus F2-SEC-001/F2-SEC-002 deployment + production verification (neither performed or claimed by F2-SEC-003); Stage 2 Imaging remains `BLOCKED`, additionally gated on the guardrail task itself merging and passing main CI** — see [evidence/F2-SEC-003_SECURITY_GATE_RECONCILIATION.md](../evidence/F2-SEC-003_SECURITY_GATE_RECONCILIATION.md)); **current status (corrected by F2-GUARDRAIL-IMPL-001, 2026-08-05): the report-only guardrail F2-SEC-003 named is now implemented** (`scripts/architecture-guardrail/`, TypeScript-compiler-API-based CLI, no new dependency; 56/56 unit tests pass; exit 0 verified regardless of finding count; deterministic; no absolute paths/secret patterns in output; real-scope scan on `origin/main` @ `da23f6fd4b86d4a37dc56f90859b0806f8ce60c6` produced 1,031 findings [1,018 `NEW`/13 `EXISTING`] against the frozen 71-edge baseline, 0 errors — the high `NEW` count is expected/documented, not claimed as 1,031 real violations; one new standalone non-blocking `ci-layers.yml` Layer 1 job added); **CI-blocking/production enforcement remains explicitly `NOT_AUTHORIZED`**, unchanged, gated on a program-owner false-positive-review pass plus F2-SEC-001/F2-SEC-002 deployment + production verification (neither performed); Stage 2 Imaging remains `BLOCKED` — see [evidence/F2-GUARDRAIL-IMPL-001_EXACT_EDGE_GUARDRAIL_IMPLEMENTATION.md](../evidence/F2-GUARDRAIL-IMPL-001_EXACT_EDGE_GUARDRAIL_IMPLEMENTATION.md)); **current status (corrected by F2-GUARDRAIL-VAL-001, 2026-08-05): the false-positive-validation pass F2-SEC-003/F2-GUARDRAIL-IMPL-001 both named as the CI-blocking-enforcement prerequisite is now complete** — 169-finding deterministic stratified sample (exceeds the required 120 minimum), classified via direct source reads: 81.1% `EXPECTED_PUBLIC_CONTRACT_EDGE`/`EXPECTED_PLATFORM_SHARED_EDGE`, 8.9% `DOMAIN_CLASSIFICATION_FALSE_POSITIVE`, 0% `PARSER_FALSE_POSITIVE`, only 1 genuinely new `REAL_BOUNDARY_VIOLATION` and 0 new legacy-debt discoveries (all 9 `LEGACY_TECHNICAL_DEBT` sample findings trace to already-documented prior evidence); all 4 F0-003-declared ownership collisions reconciled (3 recommended for map correction — `utils/encryption.ts`/`utils/secrets.ts` are F0-003's own "true platform primitive" files, `services/treatmentStockDeduction.ts`'s location is already settled — 1, `routes/organizationDashboard.ts`, requires a program-owner ADR); a distinct, larger domain-map coverage gap discovered (23 files/~250 findings `UNRESOLVED` purely from missing map entries, not declared disputes); a directly-measured `NEW`/`EXISTING` baseline-match undercount (12/12 sampled already-accepted edges misreported `NEW`). **Enforcement readiness assessed against all 8 required criteria: `NOT_READY`** — CI-blocking/production enforcement remains explicitly `NOT_AUTHORIZED`; this task made no scanner/domain-map/application code change; Stage 2 Imaging remains `BLOCKED` — see [evidence/F2-GUARDRAIL-VAL-001_SIGNAL_QUALITY_VALIDATION.md](../evidence/F2-GUARDRAIL-VAL-001_SIGNAL_QUALITY_VALIDATION.md)); **current status (corrected by F2-GUARDRAIL-VAL-002, 2026-08-06): the domain-map completeness correction F2-GUARDRAIL-VAL-001 named as its exact next task is now complete** — config-only change to `scripts/architecture-guardrail/config/domain-map.json` (72 new entries across 13 domain groups closing the previously-discovered coverage gap in full — 0 of 247 scan-root files remain unmapped, independently re-verified; 5 reclassifications; 3 of the 4 F0-003-declared collisions resolved, introducing a new `core-platform-crypto` domain; `routes/organizationDashboard.ts` deliberately left `UNRESOLVED`, its real boundary violation still `NEW`/actionable, pending the still-outstanding `F2-ADR-ORG-DASH-001`); before/after scan (both deterministic) 1,031→1,038 findings (net +7, fully traced to 61 previously-hidden true edges surfaced minus 54 false-cross-domain edges removed, all individually risk-checked — not a suppression), `UNRESOLVED`-owner findings 308→1; **enforcement readiness re-assessed against all 8 criteria: `NOT_READY`, unchanged** (the scanner's own baseline-match-key limitation, the `organizationDashboard.ts` ADR, program-owner sign-off, and a fresh quantified false-positive re-sample all remain open); this task explicitly declines to bulk-author new `F2-GUARDRAIL-PREP-010-A` baseline entries, handing that off as a separate follow-up; CI-blocking/production enforcement remains explicitly `NOT_AUTHORIZED`; Stage 2 Imaging remains `BLOCKED` — see [evidence/F2-GUARDRAIL-VAL-002_DOMAIN_MAP_AND_BASELINE_RECONCILIATION.md](../evidence/F2-GUARDRAIL-VAL-002_DOMAIN_MAP_AND_BASELINE_RECONCILIATION.md)); **current status (corrected by F2-GUARDRAIL-VAL-003, 2026-08-06): the ~88-finding baseline-entry-authoring follow-up F2-GUARDRAIL-VAL-002 §9/§15 named is now complete** (task-ID corrected from the assigning brief's already-taken `F2-GUARDRAIL-PREP-010-B` to `F2-GUARDRAIL-VAL-003`, continuing the VAL sequence). Re-derived the exact 88-candidate set independently, then **disproved F2-GUARDRAIL-VAL-002's own causal claim**: direct inspection of `lib/baseline.ts` plus its own passing regression test proves the `callerSymbol`-exclusion match-key behavior is intentional and already correct, not a defect; 87/88 candidates simply had no baseline entry at all for their exact edge, 1/88 had entries for the same caller file covering a different edge — a data-completeness gap, not a matcher defect. **Option D selected** (author exact baseline entries, no matcher/scanner code change) after explicitly rejecting Options A/B/C on this evidence. All 88 candidates individually re-verified against current source before being added as `CDA-073..CDA-160` to `F2-GUARDRAIL-PREP-010-A_cross_domain_access_inventory.json` (append-only, all 71 pre-existing edges byte-verified unchanged). Before/after scan, byte-identical determinism confirmed 2-3×: `1,039` findings unchanged; `NEW` `1,024 → 846`; `EXISTING` `15 → 193` — the full `−178`/`+178` delta traced to 88 explicit candidates plus 90 verified same-edge sibling imports (the `callerSymbol`-exclusion design correctly matching multiple named imports from one already-vetted target in one statement), 100% attributable, 0 involving `organizationDashboard.ts`. 47/88 new entries touch a high-risk domain, individually reviewed — all are the correct centralized-security-utility-import pattern, none hiding a violation. No scanner/application/schema/migration/tenant/auth code touched; `74/74` guardrail unit tests, `74/74` runtime-orchestrator unit tests, `typecheck:guardrail` clean, `git diff --check` clean. **CI-blocking/production enforcement remains explicitly `NOT_AUTHORIZED`**, unchanged; does not decide or baseline anything involving `organizationDashboard.ts`, per the parallel `F2-ADR-ORG-DASH-002` ownership-ADR task's isolation rule; Stage 2 Imaging remains `BLOCKED` — see [evidence/F2-GUARDRAIL-VAL-003_BASELINE_MATCH_KEY_RECONCILIATION.md](../evidence/F2-GUARDRAIL-VAL-003_BASELINE_MATCH_KEY_RECONCILIATION.md)); **current status (corrected by F2-ADR-ORG-DASH-002, 2026-08-06, PR #331, MERGED — merge commit `9196ceb9a8beff561af55ff2d15b13c0a7874bc2`, post-merge main CI run `31157923920` `success`): the program-owner ADR for `routes/organizationDashboard.ts` that F2-GUARDRAIL-VAL-001/VAL-002/F2-ADR-ORG-DASH-001 all deferred is now resolved** — `domain-map.json` maps it to `core-org-clinic-membership` (Option A), the **last** `UNRESOLVED` domain-map entry and **fourth and final** F0-003-declared ownership collision closed (0 `UNRESOLVED` remain, 247/247 mapped); repository-only guardrail signal quality work is now fully complete on `main`; CI-blocking/production enforcement remains explicitly `NOT_AUTHORIZED`; Stage 2 Imaging remains `BLOCKED` — see [evidence/F2-ADR-ORG-DASH-002_ORGANIZATION_DASHBOARD_OWNERSHIP.md](../evidence/F2-ADR-ORG-DASH-002_ORGANIZATION_DASHBOARD_OWNERSHIP.md)); **current status (corrected by F2-GUARDRAIL-VAL-003-R2, 2026-08-07, PR #330, still OPEN/NOT_MERGED): PR #330 rebased onto the post-F2-ADR-ORG-DASH-002 `main` (`9196ceb9a8beff561af55ff2d15b13c0a7874bc2`, new head `52e99d1c1fe8b25a453ce64125ce65624898791f`) and the entire VAL-003 pipeline independently re-derived from scratch against it** — identical 88/88/90/178/0/0 candidate/accepted/explicit-flip/sibling-flip/total-flip/unattributed/organizationDashboard-involvement counts to R1, no difference; `organizationDashboard.ts` isolation re-confirmed (still `core-org-clinic-membership`, 0 VAL-003 involvement); all four of this PR's prior automated review threads independently re-verified still fixed and resolved; CI on the exact new head SHA is `success` (run `31161018540`, all 10 `ci-layers` jobs pass — one transient concurrency-stress-test flake in an unrelated pre-existing test, `patientEmergencyContactsPrimaryConcurrency.test.ts`, cleared on re-run of that job alone). **This PR is not yet merged** — external review/merge decision remains outside this task's authorization. CI-blocking/production enforcement remains explicitly `NOT_AUTHORIZED`; Stage 2 Imaging remains `BLOCKED` — see [evidence/F2-GUARDRAIL-VAL-003_BASELINE_MATCH_KEY_RECONCILIATION.md](../evidence/F2-GUARDRAIL-VAL-003_BASELINE_MATCH_KEY_RECONCILIATION.md); **current status (corrected by F2-GUARDRAIL-VAL-004, 2026-08-07): PR #330 is `MERGED`** (merge commit `81eab4cfb45e115f61d3a151c987d1de97a10cdc`, `mergedAt` 2026-08-07T09:01:11Z, independently re-confirmed via `gh pr view 330`, not merely cited from the assigning brief) — the "not yet merged" clause immediately above is now stale, left unedited per this document's own convention. `origin/main` = `81eab4cfb45e115f61d3a151c987d1de97a10cdc`; PR #331 also independently reconfirmed `MERGED` (`9196ceb9a8beff561af55ff2d15b13c0a7874bc2`); both merge commits confirmed ancestors of current `origin/main` via `git merge-base --is-ancestor`. Post-merge closure independently re-verified via `gh run view 31165704904`: `workflow_dispatch`, `headSha` exactly `81eab4cfb45e115f61d3a151c987d1de97a10cdc`, `conclusion: success`, 10/10 jobs `success` (an earlier push-triggered run's `startup_failure` history is preserved, not erased). Fresh deterministic scan reproduced 3× at this baseline, byte-identical: `1,039` total / `846` `NEW` / `193` `EXISTING`, `247/247` files, `0` errors/warnings — matching VAL-003's own final counts exactly. **F2-GUARDRAIL-VAL-004 performed a fresh stratified false-positive re-sample against this now-fully-reconciled baseline/domain-map** — the exact task VAL-003/ADR-ORG-DASH-002 both named as the still-open next step. Methodology revised mid-task (`F2-GUARDRAIL-VAL-004-REVIEW-A` addendum): sampling/classification unit is the distinct baseline-matcher edge, not the raw finding — `846` `NEW` findings collapse to `552` distinct edges; target `n = max(150, ceil(0.20×552)) = 150`; `192` edges actually reviewed after mandatory census (organizationDashboard.ts callers, route→route, cluster≥5, small high-risk/small-cell domains) and high-risk oversampling floors. `organizationDashboard.ts` re-confirmed `core-org-clinic-membership`; `4` distinct `NEW` edges as caller, `0` `EXISTING`, all `4` in-sample by census; source unchanged since F2-ADR-ORG-DASH-002, ADR not reopened. Does not author baseline entries, does not change domain-map.json or scanner semantics. CI-blocking/production enforcement remains explicitly `NOT_AUTHORIZED`; Stage 2 Imaging remains `BLOCKED` — see [evidence/F2-GUARDRAIL-VAL-004_FRESH_STRATIFIED_SIGNAL_QUALITY.md](../evidence/F2-GUARDRAIL-VAL-004_FRESH_STRATIFIED_SIGNAL_QUALITY.md)); **current status (corrected by F2-GUARDRAIL-VAL-004-R1, 2026-08-07, PR #332, still OPEN/NOT_MERGED): an external architecture review of PR #332 (reviewed head `e8e57bd8bb6ce7e3dd950a808cf4f1d31c1a8628`) found 2 blocking methodology defects in F2-GUARDRAIL-VAL-004's sampling/metrics tooling, both now fixed in place** — the high-risk census/oversample partition was keyed only by `ownerDomain` despite `isHighRisk(e)` correctly considering either endpoint, silently under-sampling caller-side-only high-risk edges (fixed with a new deterministic `highRiskDomainKey(e)`, plus a fix-adjacent double-counting defect the rework surfaced, both in `buildVal004Sample.mjs`); the 21.10% figure was mislabeled a formal `conservative violation-rate ceiling` with no simultaneous confidence-coverage guarantee (relabeled, computation unchanged, as `zeroEventSensitivityAnalysis()` with explicit `hasConfidenceCoverage: false` in `buildVal004Metrics.mjs`). Reviewed sample grew `192 → 266` edges (159 reused, 33 dropped, 107 newly classified against current source, 0 new true positives/ambiguous/scanner-defects found); weighted accepted-rate `99.27% → 99.64%`; sensitivity value `21.10% → 28.02%` (broader zero-event coverage, not more uncertainty). Regenerated deterministically (2×+ reruns, byte-identical); 6 new regression tests (50/50 total). CI-blocking/production enforcement remains explicitly `NOT_AUTHORIZED`; Stage 2 Imaging remains `BLOCKED`; **PR #332 is not yet merged — external re-review required** — see [evidence/F2-GUARDRAIL-VAL-004_FRESH_STRATIFIED_SIGNAL_QUALITY.md §0.1](../evidence/F2-GUARDRAIL-VAL-004_FRESH_STRATIFIED_SIGNAL_QUALITY.md)); **current status (corrected by F2-DOC-002, 2026-08-07): PR #332 has `MERGED`** (merge commit `4680e1d1ae50a1bce5f1a9a99425f0f2de42de23`, `mergedAt` 2026-08-07T17:48:27Z, independently re-confirmed via `gh pr view 332`, confirmed an ancestor of `origin/main`) — the "not yet merged" clause immediately above is now stale, left unedited per this document's own convention. **PR #333 (F2-CT-23) also independently re-confirmed `MERGED`** (merge commit `06f832c229b9f5398c7f52def16e0d37b18c6417`, also `origin/main`'s exact current tip, `mergedAt` 2026-08-07T17:49:04Z), fixing the `CT-23` `VERIFIED_DEFECT` (guarded atomic `updateMany` patient/clinic-consistency invariant on `LinkImagingStudy`; bounded cursor+`take` historical-mismatch detector, default batch 500; 12/12 + 7/7 new regression; no schema/migration; production detector not run). **Post-merge main CI independently re-verified** via `gh run view 31204059995`: `headSha` exactly `06f832c229b9f5398c7f52def16e0d37b18c6417`, `status: completed`, `conclusion: success`, 10/10 `ci-layers` jobs `success`. VAL-004 final reconciled metrics unchanged from the R1 correction directly above (266-edge sample, 99.64% weighted accepted-rate, 28.02% sensitivity, 56/56 determinism tests). `CT-23`/PR #333 changed runtime backend code and is `NOT_DEPLOYED`/`NOT_PRODUCTION_VERIFIED` (not `N/A`); **current status (corrected by F2-DOC-002-R1, same PR, before merge): Stage 2 Imaging entry gate: `STAGE_2_ENTRY_GATE = SATISFIED` / `AUTHORIZED_TO_BEGIN_STAGE_2_IMPLEMENTATION = TRUE`**, scoped strictly to the already-accepted Stage 2 scope (`OVL-01` duplicate manual/bridge ingest convergence + `ImagingRequest` PATCH/cancel concurrency hardening `CR-03`/`BLK-02`/`FP-06`/`CT-32`, per this file's own §15 expand-migrate-contract sequence — that row lists `CR-03`/`CT-32` as work inside Stage 2's own scope, a Stage-2 exit condition, not an entry precondition; **`CR-03`/`CT-32` is NOT implemented on current `main`**, worked separately by the parallel `F2-CT-32-R1` task, not inspected/modified here) — explicitly does **not** authorize Stage 3, does **not** authorize CI-blocking/production architecture-guardrail enforcement (remains explicitly `NOT_AUTHORIZED`, unchanged), and does **not** authorize any microservices/Kafka/Kubernetes/database-per-tenant change; **Stage 2 itself remains `IN_PROGRESS` / `NOT_COMPLETE` / `EXIT_GATE_NOT_SATISFIED`**; this correction does not itself begin or complete Stage 2 implementation — see `CURRENT_PHASE.md`'s top F2-DOC-002 entry §D; **current status (corrected by F2-OVL-01, 2026-08-08, branch `fix/f2-ovl-01-imaging-ingest-convergence`, baseline `origin/main` @ `5a6944ae60b182f1c79c96c9febead7b25c59d9b`): the `OVL-01` half of Stage 2's own scope (manual/bridge ingest convergence) is now implemented** — `POST /api/imaging/studies` and `POST /api/public/imaging/bridge/studies` share one new internal application function, `ingestImagingStudyCore()` (`server/src/services/imaging/imagingIngestCore.ts`: file-signature validation, `buildStorageKey`+`saveFile`, the `ImagingStudy`+`ImagingImage` `$transaction` with the identical pre-existing tenant-scoped `ImagingRequest` CAS `updateMany`, and best-effort storage compensation) behind an explicit, mandatory `clinicId` input the core never derives itself; auth, clinic resolution, `validateClinicalLinks`, the `ImagingRequest` open-state lookup, bridge `ingestKey` dedupe/P2002 recovery, rate limiting, and — **explicit architecture decision** — audit (`auditImaging`/`writeAuditLog`) and `ActivityLog` all remain exactly as before in each route, unchanged; the two known audit/ActivityLog asymmetries (bridge pre-check duplicates unaudited; bridge never calls `logActivity`) are deliberately not resolved here, tracked separately as `F2-IMG-AUDIT-001`/`F2-IMG-AUDIT-002`. No schema/migration change. `typecheck` clean; `test:imaging` 103/103; `test:imaging-bridge-pairing`/`-onboarding`/`-update` 50/14/44; `guardrail:test` 74/74; `guardrail:scan` exit 0; `git diff --check` clean; real disposable-Postgres run (`test:runtime:postgres`, exit 0) re-ran the unmodified `imagingCharacterizationIngestStorage.test.ts` (CT-07/08/10/11/12/13/14/27) end-to-end with zero test-body changes, 13/13 passed, plus a new additive `imagingIngestCoreConvergence.test.ts` (8/8) proving core-only tenant-scoped-CAS/path-specific-field/no-audit-dependency properties and a new bridge-side `ImagingRequest` CAS-conflict race (gap-fill: previously only characterized for the manual route as CT-13). **`ImagingRequest` PATCH/cancel concurrency hardening (`CR-03`/`BLK-02`/`FP-06`/`CT-32`), Stage 2's other named half, was already merged separately by `F2-CT-32-R1`/PR #335 (this task's own hard-start gate) before `OVL-01` began — not newly done here.** **`OVL-01` itself is `AGENT_COMPLETED`/`TESTS_PASSED`, `PR_OPENED` once opened, `NOT_MERGED`/`NOT_DEPLOYED`/`NOT_PRODUCTION_VERIFIED`. Stage 2 remains `IN_PROGRESS`/`NOT_COMPLETE`/`EXIT_GATE_NOT_SATISFIED`** until `OVL-01` merges and post-merge main CI is independently re-verified; CI-blocking/production architecture-guardrail enforcement remains explicitly `NOT_AUTHORIZED`, unchanged — see [evidence/F2-OVL-01_IMAGING_INGEST_CONVERGENCE_EVIDENCE.md](../evidence/F2-OVL-01_IMAGING_INGEST_CONVERGENCE_EVIDENCE.md)); **current status (corrected by F2-DOC-004, 2026-08-08): PR #338 (`OVL-01`) is `MERGED`** (merge commit `babedf82913740c9b73f3ec85a6253200661c52f`, `mergedAt` 2026-08-08T17:53:57Z, independently re-confirmed via `gh pr view 338`, confirmed ancestor of `origin/main` via `git merge-base --is-ancestor`) — the `NOT_MERGED`/`EXIT_GATE_NOT_SATISFIED` clause immediately above is now stale, left unedited per this document's own convention. **Exact post-merge main CI independently re-verified**: `gh api repos/MustafaBasol/DisKlinikCRM/actions/runs?head_sha=babedf82913740c9b73f3ec85a6253200661c52f` returned exactly one workflow run on that exact SHA — `ci-main-and-nightly` run `31270570690`, `event: push`, `status: completed`, `conclusion: success`, 10/10 `ci-layers` jobs `success` (Layers 1–5, including the advisory-only architecture-guardrail job). No other workflow (e.g. `windows-bridge-pr`, which triggers only on `pull_request` events) ran on this exact SHA. **PR #337 (`F2-DOC-003`), an earlier independent attempt at this same reconciliation, is confirmed `CLOSED`/`NOT_MERGED`** (`gh pr view 337`: `state: CLOSED`, `mergedAt: null`, `mergeCommit: null`), closed ~11 minutes after PR #338 merged — superseded by the very event it was waiting on; none of its conclusions cited as merged program state. **Both named halves of Stage 2's own accepted scope (this file's own §15 expand-migrate-contract sequence) are now `MERGED` with independently re-verified, exact-merge-SHA, green post-merge `main` CI: `CR-03`/`BLK-02`/`FP-06`/`CT-32` (PR #335/`F2-CT-32-R1`, merge commit `5a6944a`, main CI run `31243089121` `success`, already recorded above) and `OVL-01` (PR #338, merge commit `babedf8`, main CI run `31270570690` `success`, this correction).** Source-level re-verification (targeted CodeGraph on `imagingIngestCore.ts`/`imaging.ts`/`imagingBridgePublic.ts` plus a direct grep for both call sites) independently confirms the merged shared core's on-disk implementation matches its documented authorized boundary exactly — no scope drift. **`F2-IMG-AUDIT-001`/`F2-IMG-AUDIT-002`** (the two audit/`ActivityLog` asymmetries OVL-01 itself named and deferred) are reconciled as **accepted, non-blocking deferred follow-up debt, not Stage-2 exit blockers** — neither was named in this file's own §15 Stage-2 scope, neither is security/tenant-critical per OVL-01's own merged evidence; not implemented by this correction. **`STAGE_2_EXIT_GATE = SATISFIED`.** This does **not** authorize Stage 3 (`AUTHORIZED_TO_BEGIN_STAGE_3 = FALSE` — a distinct, separately-scoped future task evaluating this file's own §14 precondition set) and does **not** change CI-blocking/production architecture-guardrail enforcement, which remains explicitly `NOT_AUTHORIZED`, unchanged — see [evidence/F2-DOC-004_STAGE_2_EXIT_GATE_RECONCILIATION.md](../evidence/F2-DOC-004_STAGE_2_EXIT_GATE_RECONCILIATION.md)); **current status (corrected by F2-STAGE3-AUTH-001, 2026-08-08, read-only architecture/program-control review — Stage 3 not implemented by this task): the dedicated Stage-3 authorization task F2-DOC-004 named as its own exact next task is now complete.** Hard-start gate independently re-run: `origin/main` unchanged at `5eedf8f`, exact post-merge main CI re-confirmed `success` (run `31273018579`). **Port contract** (`server/src/services/imaging/public.ts`) re-verified drift-free against the accepted F2-PREP-009 explicit-`clinicId` four-method signature on all 10 sub-checks (mandatory `clinicId`; no wildcard/nullable bypass; no ambient context; fail-closed, cross-tenant-indistinguishable-from-not-found predicates on every read AND write; atomic legal-hold write predicate; no authorization/audit re-resolution inside the port); zero production callers re-confirmed. **All three Stage-3 caller files** (`patientAnonymization.ts`, `orphanFileInspection.ts`, `deletionReviewInventory.ts`) independently inspected — every live, HTTP-reachable `clinicId` traces to `patientPrivacy.ts`'s `resolvePatient()`, an org+`allowedClinicIds`-scoped Prisma lookup, never a raw JWT/body/query value. **`CT-05`/`CT-23`/`CT-30` independently re-run against a disposable PostgreSQL instance** (not cited from prior evidence): `CT-05` **5/5**, `CT-23` **2/2+12/12+7/7** (remediation re-confirmed present in current source), `CT-30` **7/7**, all CI-wired, zero weakened assertions. Two small, non-blocking scope gaps found and explicitly deferred to a later follow-up, not bundled into the initial migration: `orphanFileInspection.ts`'s `markConfirmedMissing` (no `clinicId` param, zero production callers) and `deletionReviewInventory.ts`'s imaging byte-size read (needs a port DTO extension not authorized here). **`STAGE_3_ENTRY_GATE = SATISFIED` / `AUTHORIZED_TO_BEGIN_STAGE_3_IMPLEMENTATION = TRUE`.** Exact next task: `F2-STAGE3-IMPL-001` — Privacy/KVKK `ImagingLifecyclePort` caller migration, initial slice (mechanical migration of the two production-reachable, cleanly-mapping call sites only; no port contract change). CI-blocking/production architecture-guardrail enforcement remains explicitly `NOT_AUTHORIZED`, unchanged — see [evidence/F2-STAGE3-AUTH-001_IMAGING_PRIVACY_CALLER_MIGRATION_AUTHORIZATION.md](../evidence/F2-STAGE3-AUTH-001_IMAGING_PRIVACY_CALLER_MIGRATION_AUTHORIZATION.md)); **current status (corrected by F2-CT-32-R2, 2026-08-08, BLOCKER/URGENT, branch `fix/f2-ct-32-r2-imaging-request-residual-race`, baseline `origin/main` @ `51e102d19a5ddda87a49ad0367172b3eaae05d09`): a real post-merge main CI failure surfaced a residual gap in the already-merged F2-CT-32-R1 CAS guard** — `ci-main-and-nightly` run `31276844302`, Layer 3 disposable-PostgreSQL job, CT-32 148 passed/6 failed, round-1 signature `PATCH=200,cancel=200,final=cancelled`. Independently reproduced deterministically via a new test-only forced-interleaving hook (`installImagingRequestRaceTestHooks`, mirrors `patientEmergencyContactsConcurrency.ts`'s established pattern), not accepted as flake (0/600 natural local reproductions — CI-timing-dependent, matching the same asymmetry already documented for that precedent). **Root cause:** CT-32 races a *chainable* transition pair (`scheduled` is itself a valid predecessor of `cancelled`); when PATCH's whole operation commits before cancel's own read executes, cancel legitimately observes `scheduled` and legitimately commits `scheduled -> cancelled` — both responses individually honest, the pair still violates the required invariant. The F2-CT-32-R1 CAS guard itself did not regress (`imagingRequestConcurrencyGuard.test.ts` re-verified 73/73). **Fix:** additive, optional `expectedStatus` precondition field on both PATCH and cancel — the caller's own pre-request belief, immune to server-side read timing by construction, mirroring `patientEmergencyContactsConcurrency.ts`'s own `expectedCurrentPrimaryContactId` token-protected mode. No schema/migration. CT-32 revised in place (its own "revise, don't delete" convention) to supply it, re-verified 504/504 at `CT32_ROUNDS=100`; new `imagingRequestConcurrencyForcedInterleaving.test.ts` (55/55) proves the fix closes the exact forced schedule that broke legacy mode, while characterizing (not silently widening) the residual gap for callers that omit the field. Full validation suite (`typecheck`, `test:imaging*` family, `guardrail:test`/`:scan`, `git diff --check`, full `test:runtime:postgres` orchestrator run matching CI Layer 3 exactly) all green. **`F2-STAGE3-AUTH-001`'s merged authorization decision is NOT revoked. `F2-STAGE3-IMPL-001` remains explicitly BLOCKED** until this PR merges and exact post-merge main CI is independently re-verified green — see [evidence/F2-CT-32-R2_IMAGING_REQUEST_RESIDUAL_RACE_REMEDIATION.md](../evidence/F2-CT-32-R2_IMAGING_REQUEST_RESIDUAL_RACE_REMEDIATION.md)); **current status (corrected by F2-STAGE3-IMPL-001, 2026-08-09, branch `feature/f2-stage3-impl-001-privacy-imaging-lifecycle-migration`, baseline `origin/main` @ `27c54f3aa81af4eecc33e79d77e366a0fe4915bc`): PR #342 (`F2-CT-32-R2`) is `MERGED`, its exact post-merge main CI (run `31304621261`) confirmed `GREEN` after rerun — the `BLOCKED` clause immediately above is now stale, left unedited per this document's own convention.** `F2-STAGE3-IMPL-001` proceeds on `F2-STAGE3-AUTH-001`'s still-valid authorization (not re-litigated) and **implements the authorized initial slice**: `patientAnonymization.ts`'s imaging redaction and `orphanFileInspection.ts`'s `inspectOrphans` lifecycle-review/existence-check path both now call `ImagingLifecyclePort` (`getImagesForLifecycleReview`/`redactForAnonymization`/`checkImageStorageExists`) instead of direct `prisma.imagingImage`/`prisma.imagingStudy` access — verified by grep, zero direct Imaging Prisma access remains on either migrated path. A caller-side legal-hold adapter (checks the DTO's `legalHold` flag before calling the port for a held image, plus a catch for a port-detected write-time hold race) reproduces the pre-migration skip/count semantics and closes a TOCTOU gap the old direct-Prisma code never guarded; a caller-side `.slice(0, 500)` reproduces the old `orphanFileInspection` `BATCH_SIZE` cap the port itself doesn't enforce. `markConfirmedMissing` (no `clinicId` param, zero production callers) and `deletionReviewInventory.ts`'s imaging `fileSize` read remain explicitly deferred, doc-commented, and now regression-tested as untouched. A counter-semantics divergence on idempotent re-runs (port DTO exposed no `originalName`) was initially regression-tested here and **fixed same-day by `F2-STAGE3-IMPL-001-R1`** (`redactForAnonymization` now returns `Promise<{ changed: boolean }>`, additive/DTO-unchanged; the caller gates `redacted++` on `changed === true`) — see [evidence/F2-STAGE3-IMPL-001-R1_IMAGING_LIFECYCLE_REDACTION_MUTATION_OUTCOME_AMENDMENT.md](../evidence/F2-STAGE3-IMPL-001-R1_IMAGING_LIFECYCLE_REDACTION_MUTATION_OUTCOME_AMENDMENT.md). No schema/migration/route/response-shape change; `clinicId` provenance unchanged (still `patientPrivacy.ts`'s `resolvePatient()`, per `F2-STAGE3-AUTH-001`'s own trace). **Tests:** `typecheck` clean; new `test:privacy-imaging-lifecycle-migration` (17/17, items A–M) wired into `server:test:disposable-db`; full `npm run test:runtime:postgres` orchestrator run (PostgreSQL 16, 72/72 migrations) `outcome.exitCode: 0`; `CT-05`/`CT-30` (`Imaging-Characterization-Tenant-Lifecycle`) 29/29; `CT-23` (`test:imaging-study-request-patient-consistency` + mismatch-detector) 12/12+7/7; `test:imaging-lifecycle-facade` 34/34; `guardrail:test` 74/74; `guardrail:scan` exit 0 (only new findings: the two migrated files' intended `services/imaging/public.ts` public-contract imports); `git diff --check` clean. **Stage 3 as a whole remains NOT complete** — this is the initial slice only; the two deferred gaps and CI-blocking/production architecture-guardrail enforcement (`NOT_AUTHORIZED`, unchanged) remain open — see [evidence/F2-STAGE3-IMPL-001_PRIVACY_IMAGING_LIFECYCLE_CALLER_MIGRATION.md](../evidence/F2-STAGE3-IMPL-001_PRIVACY_IMAGING_LIFECYCLE_CALLER_MIGRATION.md)); **PR #344 CI confirmed green** (same task, same day): 10/10 `ci-layers` jobs `success` on head `560acdd74d0751456681706dfd238c29539cb845`, run [`31307552547`](https://github.com/MustafaBasol/DisKlinikCRM/actions/runs/31307552547), independently re-verified via `gh pr checks 344`/`gh pr view 344` — Layer 3 disposable-PostgreSQL (8m30s) and Layer 5 backend fail-safe (6m30s) both pass, the two jobs that actually re-run CT-05/CT-23/CT-30 and the new `test:privacy-imaging-lifecycle-migration` in CI, not merely locally. **PR #344 is `OPEN`/`MERGEABLE`, still `NOT_MERGED`** — merge/review decision remains a program-owner action, not performed by this task. Stage 3 as a whole remains NOT complete. **current status (corrected by F2-STAGE3-IMPL-001-R1, 2026-08-09, same PR #344, same day): the counter-semantics divergence this task's own regression test locked in is now fixed, not merely accepted or deferred** — `redactForAnonymization` returns `Promise<{ changed: boolean }>` (additive, DTO unchanged, still exactly six fields) and `patientAnonymization.ts` gates `redacted++` on `changed === true`; the mutation's own `updateMany` predicate now also excludes already-redacted rows, turning the write into an atomic compare-and-set so two genuinely concurrent callers cannot both report `changed: true` — the benign loser correctly reports `changed: false`, the winner reports `changed: true`. `test:privacy-imaging-lifecycle-migration` **18/18** (17/17 + 1 new benign-concurrent-writer regression); `test:imaging-lifecycle-facade` **36/36** (34/34 + 2 new mutation-outcome regressions) — the `17/17`/`34/34` figures immediately above are now stale, left unedited per this document's own convention. Current PR #344 head `39364f117874668c2bde34fe1fbca03de7430245` (supersedes `560acdd74d0751456681706dfd238c29539cb845`); exact-head CI independently re-verified via `gh pr checks 344`/`gh run list`: `ci` run `31310907422` `SUCCESS`, `windows-bridge-pr` run `31310907297` `SUCCESS` — the `31307552547` run cited immediately above was against the pre-R1 head, now stale, left unedited per this document's own convention. See [evidence/F2-STAGE3-IMPL-001-R1_IMAGING_LIFECYCLE_REDACTION_MUTATION_OUTCOME_AMENDMENT.md](../evidence/F2-STAGE3-IMPL-001-R1_IMAGING_LIFECYCLE_REDACTION_MUTATION_OUTCOME_AMENDMENT.md). PR #344 remains `OPEN`/`MERGEABLE`/`NOT_MERGED`/`NOT_DEPLOYED`/`NOT_PRODUCTION_VERIFIED` — merge/review decision remains a program-owner action, not performed by this task. Stage 3 as a whole remains NOT complete; **current status (corrected by F2-STAGE3-IMPL-001-POSTMERGE-CI-001, 2026-08-09): a post-merge `main` CI failure surfaced on `ci-main-and-nightly` run `31313169502`, `ci-layers / Layer 5: full-suite/compatibility fail-safe (backend)`, `server/src/tests/securityIncident.test.ts` test 22 ("Repeated cross-tenant denial..."), `assert.ok(singleResourceIncident)` on `null` — independently investigated and confirmed unrelated to `F2-STAGE3-IMPL-001`/PR #344** (causality check: PR #344's diff has zero dependency-graph overlap with `securityDetectionRules.ts`/`securityIncidentService.ts`/`securitySignalService.ts` or `securityIncident.test.ts`; neither file appears in any of PR #344's five commits). **Root cause: a pre-existing (KVKK-CRIT-003 foundation, PR #167/#178) test-timing race, not a logic defect** — eight of nine `sleep(150|200)` call sites in that test file raced a fixed guess against genuinely asynchronous, intentionally non-blocking (`void`-returning) detection-rule DB work (`recordSecuritySignal` + `countSignalsInWindow` + `upsertIncidentFromSignal`'s own multi-statement transaction), with no durable-completion barrier; reproduced 0/7 under normal/throttled-DB conditions, reproduced the exact CI assertion 4/4 times under an adversarial artificial delay, isolating the mechanism precisely. **Fix:** `securityDetectionRules.ts`'s shared `safely()` wrapper now tracks its own in-flight promise (`pendingDetectionWork`, a module-level `Set`) and exports `flushPendingSecurityDetectionWork()`; the test file's 8 racy `sleep()` calls now await that deterministic barrier instead. No production caller signature, timing, or behavior changed — `evaluate*Signal` exports remain synchronous/non-blocking, regression-proven (test 21 unaffected, including under the same adversarial delay). No assertion weakened. No schema/migration. `test:security-incidents` **55/55**, repeated 6× post-fix (3× normal, 3× under the same adversarial delay that broke the pre-fix code 4/4 times) — 0 failures in all 6 runs; `typecheck` clean; `git diff --check` clean. A separate, pre-existing, environment-specific (Windows `core.autocrlf`/CRLF-checkout) false failure in the unrelated `test:clinic-bulk-export` suite was observed while attempting the full `server:test:legacy-db-required` chain locally — out of scope, not caused by this task, not expected on the Linux CI runners this repository's workflows actually use, not modified. See [evidence/F2-STAGE3-IMPL-001-POSTMERGE-CI-001_SECURITY_INCIDENT_MAIN_CI_REMEDIATION.md](../evidence/F2-STAGE3-IMPL-001-POSTMERGE-CI-001_SECURITY_INCIDENT_MAIN_CI_REMEDIATION.md)) · Son güncelleme: 2026-08-09 (F2-STAGE3-IMPL-001-POSTMERGE-CI-001 — post-merge main CI security-incident test-flake remediation, unrelated to Stage 3's own imaging scope — see Change history)

F2-PREP-001 through F2-PREP-004 (domain ownership inventory, cross-domain dependency/direct-access map, feature-intake/ClickUp mapping, modularization sequencing) are merged discovery/design evidence. F2-PREP-005 (Consolidated Modularization Charter, [../architecture/F2-PREP-005_CONSOLIDATED_MODULARIZATION_CHARTER.md](../architecture/F2-PREP-005_CONSOLIDATED_MODULARIZATION_CHARTER.md)) reconciles all four into one authoritative charter and **recommends, but does not itself approve,** Imaging as the first modularization pilot. F2-PREP-006-A..D (four independent discovery siblings) and F2-PREP-006-E (the consolidation of all four, [../architecture/F2-PREP-006-E_IMAGING_BOUNDARY_CONTRACT.md](../architecture/F2-PREP-006-E_IMAGING_BOUNDARY_CONTRACT.md)) define one authoritative Imaging/Bridge boundary contract, a 20-command/14-query/1-event contract catalogue, a 32-test characterization gate, and an 8-stage expand-migrate-contract sequence. Readiness decision: `READY_FOR_CHARACTERIZATION_TEST_IMPLEMENTATION` — runtime modularization is **not** approved. F2-PREP-007-A..D (four independent Stage 0 characterization test-implementation siblings, `MERGED` PRs #293/#294/#295/#296) and F2-PREP-007-E (the consolidation of all four, [../evidence/F2-PREP-007-E_IMAGING_CHARACTERIZATION_WAVE_CONSOLIDATION_EVIDENCE.md](../evidence/F2-PREP-007-E_IMAGING_CHARACTERIZATION_WAVE_CONSOLIDATION_EVIDENCE.md), `MERGED` PR [#298](https://github.com/MustafaBasol/DisKlinikCRM/pull/298)) implement and consolidate 21 of the contract's 32 characterization tests (all 18 mandatory-before-refactor + all 3 mandatory-before-caller-migration), establish permanent test ownership (`server/package.json`'s `test:imaging-characterization`, wired into the existing `server:test:disposable-db` CI-owned aggregate), and demonstrate all 10 **Stage 0** closure acceptance conditions. F2-PREP-008 ([../evidence/F2-PREP-008_STAGE1_IMAGING_FACADE_PREP_AND_AUTHORIZATION.md](../evidence/F2-PREP-008_STAGE1_IMAGING_FACADE_PREP_AND_AUTHORIZATION.md)) independently re-verified PR #298's `MERGED` state and its post-merge main CI (run `30797032835`, 9/9 `ci-layers` jobs `success`) — **Stage 0: `CLOSED`** per its own stated criterion (F2-PREP-007-E's own entry left unedited; this is F2-PREP-008's own confirmation). Two verified findings — `CT-23` (`VERIFIED_DEFECT`, `LinkImagingStudy` patient/request-consistency gap) and `CT-32`/`CR-03` (verified `ImagingRequest` PATCH/cancel concurrency clobber) — remain explicitly tracked as unresolved, pre-contract-exposure blockers, not fixed. **F2-PREP-008 additionally produced a paper-only Stage 1 internal facade design** (`server/src/services/imaging/public.ts`, a 4-method `ImagingLifecyclePort` slice — `markStorageMissing`/`redactForAnonymization`/`getImagesForLifecycleReview`/`checkImageStorageExists` — explicitly excluding `LinkImagingStudy`/`UpdateImagingRequest`/`CancelImagingRequest`, i.e. the `CT-23`/`CT-32` surfaces) and issued decision **`AUTHORIZED_FOR_STAGE_1_IMPLEMENTATION`**, naming **`F2-IMPL-001-A` — Additive Unused Internal Imaging Facade Skeleton** as the next task. **F2-IMPL-001-A ([../evidence/F2-IMPL-001-A_ADDITIVE_UNUSED_INTERNAL_IMAGING_FACADE_SKELETON.md](../evidence/F2-IMPL-001-A_ADDITIVE_UNUSED_INTERNAL_IMAGING_FACADE_SKELETON.md)) implements exactly that authorized design**: `server/src/services/imaging/public.ts` now exists, with a purpose-built DTO (`ImagingLifecycleImageDto`, `legalHoldReason` deliberately excluded) and a small closed typed-error contract, tenant-enforced for the three `imageId`-only methods via ownership re-derivation (`ImagingImage -> ImagingStudy -> clinicId`, fail-closed) rather than an ambient ​tenant parameter the accepted signature does not carry — 25 new tests (disposable PostgreSQL) plus `test:imaging`/`test:patient-privacy` regression, all passing; zero production import (independently proven twice). `CT-23`/`CT-32`/`OVL-01`/`BLK-01` remain exactly as unresolved as before this task — none is fixed, exposed, or newly gated by it. **The facade remains wholly unused: no route, no Privacy service, and no other production caller imports it. Runtime modularization, Stage 2+ (ingest convergence, `ImagingRequest` concurrency hardening), and Stage 3 (Privacy/KVKK caller migration) remain `NOT_APPROVED` and not begun.** F2-IMPL-001-A/R2/R3 subsequently `MERGED` via [PR #304](https://github.com/MustafaBasol/DisKlinikCRM/pull/304) (merge commit `c731e7af9a552dde2da45fee52fa318192da28f8`, `mergedAt: 2026-08-03T17:47:49Z`; post-merge main CI run `30838364039` `success`) — `MAIN_CI_PASSED`, `NOT_DEPLOYED`, `NOT_PRODUCTION_VERIFIED`.

**F2-GUARDRAIL-PREP-010-D** ([../evidence/F2-GUARDRAIL-PREP-010-D_PARALLEL_WAVE_CONSOLIDATION.md](../evidence/F2-GUARDRAIL-PREP-010-D_PARALLEL_WAVE_CONSOLIDATION.md), documentation/evidence-reconciliation only) reconciles a parallel guardrail-baseline evidence wave — at the time of that consolidation, three open PRs, none touching each other's files or any shared program-control document; **corrected 2026-08-04 by F2-DOC-001: all three have since `MERGED`** — PR #315 (merge commit `0881d389ade37606a561659409cc27e38857c318`, `mergedAt 2026-08-03T21:26:22Z`, post-merge main CI run `30854633672` success), PR #313 (merge commit `9e4bd3cf12100f06c6068d29cb35d9d37f0031fa`, `mergedAt 2026-08-03T21:36:04Z`, post-merge main CI run `30855318200` success), PR #314 (merge commit `e9c1765fa191223cb036ebfdb9c72b898e2fc52e`, `mergedAt 2026-08-03T21:45:29Z`, post-merge main CI run `30855966630` success) — merge order #315, #313, #314, after the consolidation itself (PR #317). See the Change history table below for the full correction entry. **PR #313** (F2-GUARDRAIL-PREP-010-A-R1, [evidence/F2-GUARDRAIL-PREP-010-A_CROSS_DOMAIN_ACCESS_INVENTORY.md](../evidence/F2-GUARDRAIL-PREP-010-A_CROSS_DOMAIN_ACCESS_INVENTORY.md)) freezes **71 exact cross-domain edges** (identity tuple `(callerPath, callerSymbol, ownerDomain, targetModelOrSymbol, accessKind)`, classified `ACCEPTED_PUBLIC_CONTRACT` 12 / `ACCEPTED_RESTRICTED_CONTRACT` 7 / `UNRESOLVED_OWNERSHIP` 5 / `VERIFIED_SECURITY_DEFECT_REQUIRES_SEPARATE_FIX` 1 / `PROHIBITED_NEW_PATTERN` 10 / `LEGACY_ALLOWLISTED_DIRECT_ACCESS` 36) — the legacy-allowlisted bucket is **descriptive evidence only, never a target architecture or authorization for new/broadened edges**. **PR #315** (F2-GUARDRAIL-PREP-010-B-R1, [evidence/F2-GUARDRAIL-PREP-010-B_TENANT_SCOPE_USAGE_AND_BYPASS_INVENTORY.md](../evidence/F2-GUARDRAIL-PREP-010-B_TENANT_SCOPE_USAGE_AND_BYPASS_INVENTORY.md)) inventories 127 tenant-scope records and discovers **F2-SEC-001**/**F2-SEC-002** — two live, unpatched tenant-isolation defects, independently re-confirmed present on current `main` by this consolidation. **PR #314** (F2-GUARDRAIL-PREP-010-C-R1, [evidence/F2-GUARDRAIL-PREP-010-C_REFERENCE_PUBLIC_CONTRACT_PATTERN_INVENTORY.md](../evidence/F2-GUARDRAIL-PREP-010-C_REFERENCE_PUBLIC_CONTRACT_PATTERN_INVENTORY.md)) inspects 7 reference-mutation-pattern candidates, selects 3 as reference bases (none canonical) — **no canonical tenant-scoped mutation contract exists in this repository.** CAND-07 in that document **is** the `ImagingLifecyclePort` facade (F2-IMPL-001-A) — originally `REJECTED_AS_REFERENCE` for lacking a caller-supplied `clinicId`; this consolidation re-evaluates it **`STRENGTHENED_REFERENCE_BASE`** now that F2-IMPL-001-A-R2/R3 (merged) close that gap, but explicitly does **not** promote it to canonical (additive/unused, retains a public test-seam export). **Decision: CI guardrail implementation (lint/dependency-cruiser/CI boundary enforcement of any kind) is NOT authorized while F2-SEC-001/F2-SEC-002 remain open** — see `CURRENT_PHASE.md`'s F2-GUARDRAIL-PREP-010-D entry for the full authorization decision and recommended PR merge order (this consolidation, then #315, #313, #314).

## Objective (Hedef)

Depo-doğrulanmış modül haritası ([../MODULE_MAP.md](../MODULE_MAP.md)) üzerinden modül sınırlarını, public contract'ları ve bağımlılık kurallarını ([../DEPENDENCY_MAP.md](../DEPENDENCY_MAP.md)) uygulamaya koymak; feature flag / entitlement / permission ayrımını netleştirmek (ADR-014, ADR-015).

## Business reason (İş gerekçesi)

Opsiyonel ticari modüller, hızlı etki-bazlı test ve güvenli paralel geliştirme; ancak net modül sınırları ve sözleşmelerle mümkündür. Sınırsız iç bağımlılık, her değişikliği tüm sisteme yayar.

## Entry conditions (Giriş koşulları)

- F1 çıkışı (etki-bazlı CI çalışıyor)
- F0-003/F0-004 haritaları ve ADR-001/014/015 kabulü

## Exit gate (Çıkış kapısı)

- Modül sınırları ve public contract seti kabul edilmiş
- Sınır ihlali denetimi (lint/CI kuralı) çalışıyor
- Pilot modül(ler) yeni sınır modeline **kanıtla** uyumlu

## Dependencies (Bağımlılıklar)

- F1; KVKK taban çizgisi teyidi (fiziksel refactoring için)

## Allowed work (İzinli işler)

- Contract tanımları, sınır denetim araçları
- Onaylı, kademeli fiziksel modül düzenlemeleri (KVKK teyidi sonrası)

## Prohibited work (Yasak işler)

- Büyük patlama (big-bang) refactoring
- KVKK dondurma sınırındaki işler (teyit gelmeden)
- Microservice bölünmesi

## Initial task backlog (Yüksek seviyeli kategoriler)

> Ayrıntılı görev ID'leri, F1 kanıtları incelendikten sonra atanacaktır.

- Public contract biçimi ve konum standardı
- Modül sınır denetimi (import lint/CI kuralı)
- Entitlement'ın backend/service/job katmanında zorlanması
- Devre dışı modül worker/job durdurma mekanizması
- Pilot modül sınır uygulaması ve kanıtı
- Kademeli modül taşıma planı

## Required evidence (Gerekli kanıt)

- Contract listesi; ihlal denetimi CI kanıtı; pilot modül diff/test kanıtı

## Required tests (Gerekli testler)

- Public contract testleri; etkilenen modül testleri; core güvenlik regresyonu

## Security requirements (Güvenlik gereksinimleri)

- Sınır değişiklikleri tenant/permission/audit kontrollerini atlayamaz

## Tenant requirements (Tenant gereksinimleri)

- Modül sınırları tenant bağlamını açıkça taşımalı

## KVKK/privacy requirements (KVKK/gizlilik gereksinimleri)

- Privacy/consent/retention modeli taşıma işleri yalnızca KVKK teyidi sonrası ve ayrı onayla

## Rollback expectations (Geri alma beklentileri)

- Her kademeli taşıma adımı bağımsız revert edilebilir olmalı

## Risks (Riskler)

- R-025 (entitlement uygulanmaması), R-026 (aşırı modülerleşme), R-002 (KVKK regresyonu)

## Open questions (Açık sorular)

- Contract sözdizimi (TypeScript interface + runtime doğrulama?) — ADR-015'te kararlaştırılacak

## Change history (Değişiklik geçmişi)

| Tarih | Görev | Değişiklik |
|---|---|---|
| 2026-08-09 | F2-STAGE3-IMPL-001-POSTMERGE-CI-001 | Post-merge `main` CI failure investigation/remediation, `ci-main-and-nightly` run `31313169502`, `securityIncident.test.ts` test 22 (`null == true`) — independently confirmed unrelated to PR #344 (zero dependency-graph overlap). Root cause: pre-existing test-timing race (8 of 9 fixed `sleep()` calls raced genuinely async, intentionally non-blocking detection-rule DB work with no durable-completion barrier); reproduced 0/7 naturally, 4/4 under an adversarial artificial delay — isolating the exact mechanism. Fix: `securityDetectionRules.ts`'s `safely()` now tracks its own in-flight promise; new `flushPendingSecurityDetectionWork()` export replaces the 8 racy sleeps in the test file. No production behavior/signature change (non-blocking contract regression-proven unaffected). `test:security-incidents` 55/55 × 6 runs (3 normal, 3 adversarial-delay). No schema/migration. `AGENT_COMPLETED`/`TESTS_PASSED`, `PR_OPENED` once opened, `NOT_MERGED`. |
| 2026-08-08 | F2-STAGE3-AUTH-001 | Read-only architecture/program-control review evaluating whether Stage 3 (Privacy/KVKK caller migration onto `ImagingLifecyclePort`) may begin — Stage 3 itself is not implemented by this task. Hard-start gate independently re-run (`origin/main` unchanged at `5eedf8f`, exact post-merge main CI re-confirmed `success`). Port contract (`public.ts`) re-verified drift-free on all 10 sub-checks against the accepted F2-PREP-009 explicit-`clinicId` signature; zero production callers re-confirmed. All three caller files' live `clinicId` provenance independently traced to `patientPrivacy.ts`'s `resolvePatient()` — VALIDATED. `CT-05`/`CT-23`/`CT-30` independently re-run against a disposable PostgreSQL instance: 5/5, 2/2+12/12+7/7, 7/7 — all CI-wired, zero weakened assertions. Two small scope gaps found and explicitly deferred (not blockers): `orphanFileInspection.ts`'s `markConfirmedMissing` (no `clinicId` param, zero production callers) and `deletionReviewInventory.ts`'s imaging read (needs a port DTO extension, not authorized here). `STAGE_3_ENTRY_GATE = SATISFIED` / `AUTHORIZED_TO_BEGIN_STAGE_3_IMPLEMENTATION = TRUE`. Exact next task: `F2-STAGE3-IMPL-001`, scoped to the two production-reachable, cleanly-mapping call sites only. No `server/src`, schema/migration, workflow, or guardrail scanner/config/baseline file touched. |
| 2026-08-08 | F2-OVL-01 | Stage 2, `OVL-01` half: converges manual (`POST /api/imaging/studies`) and bridge (`POST /api/public/imaging/bridge/studies`) `ImagingStudy` ingest behind one new internal function, `ingestImagingStudyCore()` (`server/src/services/imaging/imagingIngestCore.ts`) — file-signature validation, `buildStorageKey`+`saveFile`, the `ImagingStudy`+`ImagingImage` `$transaction` with the pre-existing tenant-scoped `ImagingRequest` CAS `updateMany`, best-effort storage compensation; mandatory explicit `clinicId`, never derived internally. Auth, clinic resolution, `validateClinicalLinks`, request open-state lookup, bridge dedupe/P2002 recovery, rate limiting, and audit/ActivityLog all remain unchanged in each route (explicit decision: audit stays out of the shared core; two known asymmetries tracked separately as `F2-IMG-AUDIT-001`/`F2-IMG-AUDIT-002`, not resolved here). No schema/migration change. `test:imaging` 103/103, bridge pairing/onboarding/update 50/14/44, `guardrail:test` 74/74, `guardrail:scan` exit 0, `git diff --check` clean; real disposable-Postgres run re-ran the unmodified CT-07/08/10/11/12/13/14/27 suite end-to-end (13/13, zero test-body changes) plus a new `imagingIngestCoreConvergence.test.ts` (8/8, tenant-scoped CAS + path-specific fields + no-audit-dependency + a new bridge-side CAS-conflict gap-fill). `AGENT_COMPLETED`/`TESTS_PASSED`, `PR_OPENED` once opened, `NOT_MERGED`/`NOT_DEPLOYED`/`NOT_PRODUCTION_VERIFIED`. Stage 2's other half (`CT-32`/`CR-03` `ImagingRequest` concurrency hardening) was already merged separately via `F2-CT-32-R1`/PR #335 before this task began. Stage 2 remains `IN_PROGRESS`/`NOT_COMPLETE`/`EXIT_GATE_NOT_SATISFIED`. |
| 2026-08-03 | F2-IMPL-001-A | Implements the F2-PREP-008-authorized Stage 1 `ImagingLifecyclePort` facade (`server/src/services/imaging/public.ts`, additive, zero-caller). Tenant enforcement for the three `imageId`-only methods via ownership re-derivation (no ambient-tenant mechanism exists in this codebase; ID provenance is clinicId-scoped by construction plus a fail-closed re-derivation check, verified strictly stronger than the two existing direct-Prisma callers). `legalHoldReason` excluded from the DTO (role-gated, unavailable to a request-context-free facade). 25 new tests + `test:imaging`/`test:patient-privacy` regression, all passing; unused-facade proof independently confirmed twice. `CT-23`/`CT-32`/`OVL-01`/`BLK-01` unchanged. No schema/migration/route/workflow file touched. |
| 2026-07-17 | F0-001 | Faz dokümanı oluşturuldu (yüksek seviyeli). |
| 2026-08-02 | F2-PREP-005 | Durum `TODO` → `PREPARATION_IN_PROGRESS` (implementation still `TODO`). Consolidated Modularization Charter reconciles F2-PREP-001..004; recommends (not approves) Imaging as first pilot. No implementation authorized. |
| 2026-08-02 | F2-PREP-006-A | Discovery/evidence only, one of four independent parallel siblings (A..D) under frozen baseline `4cb334d213b4dbbac4193f1a8c1878deddb55714`; reconciliation deferred to F2-PREP-006-E. Repository-grounded IMG/BRG (`imaging-server-viewer`/`imaging-device-bridge`) ownership and implementation inventory: 32 routes (27 in `imaging.ts` + 5 in `imagingBridgePublic.ts`), 8 of `imaging.ts`'s own routes found to be BRG-owned; duplicated study-ingest transaction logic found between the manual and bridge upload paths; BRG's role classified as an unresolved combination split by edge (ACL/adapter device-facing, ordinary domain logic admin-facing). Proposed (not approved) IMG/BRG split recorded. No implementation, dependency-cruiser, module extraction, or caller migration authorized. |
| 2026-08-02 | F2-PREP-006-B | Discovery/evidence only, sibling of A/C/D under the same frozen baseline. 12-model ownership/tenant/KVKK assessment: tenant scoping enforced/inherited/unverified breakdown; PZ-IMG-03 refined from "location unverified" to "location resolved, authentication confirmed, RBAC granularity unproven" (remains OPEN); 9 risks (R1-R9) identified, backup-erasure-propagation gap confirmed live-tested. No implementation authorized. |
| 2026-08-02 | F2-PREP-006-C | Discovery/evidence only, sibling of A/B/D under the same frozen baseline. Caller/direct-access/transaction map: 20 inbound callers, 11 direct-access findings across 4 categories, 10 transaction flows, 9 failure points, 2 named blockers (`BLK-01` storage/DB compensation gap, `BLK-02` `ImagingRequest` PATCH concurrency), 6-step proposed (not implemented) migration order. Zero live cross-domain transaction coupling confirmed (headline positive finding). No implementation authorized. |
| 2026-08-02 | F2-PREP-006-D | Discovery/design only, sibling of A/B/C under the same frozen baseline. Public contract and characterization-test design: 20 candidate commands, 12 candidate queries, 1 candidate event (`ImagingStudyReceived`), 31 candidate characterization tests (20 blocking/11 non-blocking), report-only dependency-cruiser POC spec drafted (not installed). Nothing in this document is authoritative pending F2-PREP-006-E. No implementation authorized. |
| 2026-08-02 | F2-PREP-006-E | Consolidation of A-D into one authoritative Imaging/Bridge boundary contract, created directly from current `origin/main` (not the shared A-D frozen baseline). 5 contradictions found and resolved (notably: Notifications removed as an accepted Imaging caller — a domain-cluster naming collision, not a real caller). BRG module-structure decision: one module, two documented edges. F2-CC-14: accepted and revised (read-side query methods added). Duplicate ingest logic: decision to converge. Contract catalogue: 20 commands accepted/6 rejected, 14 queries accepted, 1 candidate event. Characterization-test gate: 32 tests (21 blocking/11 non-blocking). 8-stage expand-migrate-contract sequence defined. Dependency-cruiser: approved to proceed, report-only, Imaging-scoped, not installed. Readiness decision: `READY_FOR_CHARACTERIZATION_TEST_IMPLEMENTATION` — runtime modularization NOT approved. No implementation, schema, test, or workflow file touched. |
| 2026-08-02 | F2-PREP-007-A | Stage 0 characterization, one of four independent parallel test-implementation siblings (A..D), `MERGED` PR #293. Implements 5 of the 18 mandatory-before-refactor characterization tests (`CT-06`, `CT-16`, `CT-19`, `CT-21`, `CT-28`) — 36/36 assertions pass, no defect found. Test-only; reconciliation deferred to F2-PREP-007-E. |
| 2026-08-02 | F2-PREP-007-B | Stage 0 characterization, sibling of A/C/D, `MERGED` PR #294. Implements 7 tests (`CT-02`, `CT-03`, `CT-05`, `CT-17`, `CT-23`, `CT-26`, `CT-30`) — 29/29 assertions pass. **`CT-23` found and reported as `VERIFIED_DEFECT`**: `LinkImagingStudy` can change `ImagingStudy.patientId` while retaining an `imagingRequestId` still belonging to the original patient — reproduced, reported, not fixed (test-only scope). Test-only; reconciliation deferred to F2-PREP-007-E. |
| 2026-08-02 | F2-PREP-007-C | Stage 0 characterization, sibling of A/B/D, `MERGED` PR #295 (also `origin/main`'s current tip). Implements 8 tests covering manual/bridge upload, idempotency, and compensation (`CT-07`, `CT-08`, `CT-10` through `CT-14`, `CT-27`) — 13/13 assertions pass per run, no defect found. Characterizes without converging the manual/bridge ingest duplication (`OVL-01`). Test-only; reconciliation deferred to F2-PREP-007-E. |
| 2026-08-02 | F2-PREP-007-D | Stage 0 characterization, sibling of A/B/C, `MERGED` PR #296. Implements `CT-32` — `ImagingRequest` PATCH/cancel concurrency, making the pre-catalogued `CR-03`/`BLK-02`/`FP-06` silent-clobber gap test-tracked. 153/153 assertions pass (30/30 rounds deterministic silent-clobber reproduction). Test-only, not wired into any aggregate by this task; reconciliation deferred to F2-PREP-007-E. |
| 2026-08-03 | F2-PREP-007-E | Consolidation of A-D, PR [#298](https://github.com/MustafaBasol/DisKlinikCRM/pull/298) (open, not merged). Stage 0 coverage matrix built (32 tests: 21 blocking implemented/passing — all 18 mandatory-before-refactor + all 3 mandatory-before-caller-migration — 11 non-blocking not yet implemented). `CT-23` (`VERIFIED_DEFECT`) and `CT-32`/`CR-03` (verified concurrency gap) preserved as explicit unresolved, pre-contract-exposure blockers, not fixed; storage/DB compensation gap and manual/bridge ingest duplication (`OVL-01`) remain explicitly deferred. Permanent test ownership established on the PR branch: new `server/package.json` script `test:imaging-characterization`, wired into the existing CI-owned `server:test:disposable-db` aggregate (Layer 3) — zero new CI workflow files, zero duplicate execution across layers; increases Layer 3 execution time by the four suites' own runtime. All 10 Stage 0 closure acceptance conditions demonstrated on the PR branch — **Stage 0: `CLOSURE_PROPOSED_AWAITING_MERGE_AND_MAIN_CI`**, authoritative only once PR #298 merges and its post-merge main CI passes. No application/schema/migration file touched. Next: (1) program-owner approval + PR #298 merge/main-CI closure; (2) Stage 1 (additive, unused facade) as a separately-gated future task, not blocked by `CT-23`, not authorized here; (3) `CT-23` remediation required before Stage 3 caller migration/exposure, tracked independently of Stage 1. |
| 2026-08-03 | F2-PREP-008 | Stage 1 Imaging Internal Facade Preparation and Authorization — documentation/evidence and program-control only, no facade code added. Independently re-verified (`gh pr view`/`gh run view`/`git merge-base --is-ancestor`, not trusted from F2-PREP-007-E's own pre-merge prose) that PR #298 is `MERGED` and its post-merge main CI (run `30797032835`) passed 9/9 `ci-layers` jobs on the exact baseline SHA — **Stage 0: `CLOSED`** per its own stated criterion; F2-PREP-007-E's own entry left unedited. Targeted repository-evidence analysis with exact file/line citations for `CT-23` (`imaging.ts:807-854`), `CT-32`/`CR-03`/`BLK-02`/`FP-06` (`imaging.ts:487-524`/`530-552`), `OVL-01` (`imaging.ts:614-656` vs `imagingBridgePublic.ts:301-341`), `BLK-01` (`imaging.ts:679`). Produced a paper-only Stage 1 facade design (`server/src/services/imaging/public.ts`, the location already named by this contract's own Stage 1 definition) implementing exactly the accepted `F2-CC-14`/`ImagingLifecyclePort` 4-method slice, explicitly excluding `LinkImagingStudy`/`UpdateImagingRequest`/`CancelImagingRequest` (the `CT-23`/`CT-32` surfaces) from the operation inventory. Authorization decision: **`AUTHORIZED_FOR_STAGE_1_IMPLEMENTATION`**, scoped strictly to that 4-method slice; full checklist (no caller migration required, no runtime behavior change, no schema/storage migration, no external exposure, tenant/auth boundaries unweakened, no new forbidden cross-domain access, rollback = deletion/revert, `CT-23`/`CT-32` remain gated, `OVL-01`/`BLK-01` remain deferred/open, no accepted decision contradicted) verified in the task's own evidence document. Named next task **`F2-IMPL-001-A` — Additive Unused Internal Imaging Facade Skeleton** (confirmed unused before assignment). No implementation performed by this task. |
| 2026-08-03 | F2-IMPL-001-A (implementation, PR #304, `OPEN`, not merged) | Implemented the Stage 1 facade (`server/src/services/imaging/public.ts`) per F2-PREP-008's authorization; zero production callers wired. **Review correction (this same date, F2-IMPL-001-A-R1):** two defects confirmed against the implementation's own self-assessment. (1) `checkImageStorageExists`'s accepted 1-argument signature was altered by an optional `fileExistsForTest` test-injection parameter — a public-contract change. (2) The three `imageId`-only methods' `findOwnedImage()` re-derives `ImagingImage.clinicId`/`ImagingStudy.clinicId` and rejects only on mutual disagreement between those two stored values — a data-integrity consistency check, not caller tenant authorization; no caller-supplied `clinicId`/principal/context value exists anywhere in the accepted signatures to check against. **`BLOCKED_TENANT_CONTEXT_CONTRACT_INSUFFICIENT` confirmed.** PR #304 left open, unmerged, unmodified, marked blocked — not authorized to merge or deploy. Correction: [F2-IMPL-001-A_R1_TENANT_CONTEXT_REVIEW_CORRECTION.md](../evidence/F2-IMPL-001-A_R1_TENANT_CONTEXT_REVIEW_CORRECTION.md). |
| 2026-08-03 | F2-PREP-009 (proposal, not yet accepted) | `ImagingLifecyclePort` Tenant Context Contract Amendment, triggered by F2-IMPL-001-A-R1. Proposes and justifies **Option A** — explicit `clinicId` as the leading parameter on all four methods (`markStorageMissing(clinicId, imageId)`, `redactForAnonymization(clinicId, imageId, reason)`, `checkImageStorageExists(clinicId, imageId)`, `getImagesForLifecycleReview(clinicId, patientId)` unchanged) — over a rejected context-bound-port-factory Option B (no existing session-scoped-factory convention in this codebase; hides the authorization boundary from the call site; does not reduce the underlying work). Defines tenant context source (existing session/system resolution, never re-derived from `imageId`), principal/system-caller distinction (both require an explicit, single-clinic-scoped `clinicId` per call, no bypass), fail-closed DB predicates (`clinicId` in every `where` clause, not a post-fetch comparison), no cross-tenant existence leakage (single undifferentiated `ImagingNotFoundError` preserved), unchanged audit ownership, a mechanical Stage-3 caller-migration path (thread each caller's already-resolved `clinicId`), no production backward-compatibility obligation (PR #304 unmerged, zero callers), documentation-only rollback, and 5 required test additions (cross-tenant rejection, same-clinic regression, denormalization-mismatch-within-own-clinic, no-existence-side-channel, corrected signature-arity assertions). Status `PROPOSED` — requires program-owner review/acceptance before F2-IMPL-001-A is reconciled and re-implemented. Full document: [F2-PREP-009_IMAGING_LIFECYCLE_PORT_TENANT_CONTEXT_CONTRACT_AMENDMENT.md](../architecture/F2-PREP-009_IMAGING_LIFECYCLE_PORT_TENANT_CONTEXT_CONTRACT_AMENDMENT.md). |
| 2026-08-03 | F2-PREP-009 correction (same PR #307, tenant-context wording hardening + PR #304 head reconciliation) | Corrected F2-PREP-009's tenant-context-source language: previously readable as permitting `clinicId` to come directly from a session/JWT clinic claim; now requires validation via `resolveEffectiveClinicId`/`validateAndGetClinicIdScope`/`getAccessibleClinicIds` (`server/src/utils/clinicScope.ts`) or an equivalent already-access-scoped record lookup before the port is ever called — a raw JWT/default `clinicId` is explicitly documented as insufficient authorization. Reconciled PR #304's head (`f8a37b72...` → `abac5e3...`, independently re-verified via `gh pr view 304`): **Finding 1** (tenant-authorization gap) confirmed still open/blocking on the current head; **Finding 2** (`fileExistsForTest` signature drift) confirmed corrected on the current head (`checkImageStorageExists` back to one parameter) — classified `VERIFIED_ON_PRIOR_HEAD`/`CORRECTED_ON_CURRENT_PR304_HEAD` (Finding 2) and `VERIFIED_ON_PRIOR_HEAD`/`STILL_OPEN_ON_CURRENT_PR304_HEAD` (Finding 1), not erased, in new sections appended to both F2-PREP-009 and its triggering [F2-IMPL-001-A_R1_TENANT_CONTEXT_REVIEW_CORRECTION.md](../evidence/F2-IMPL-001-A_R1_TENANT_CONTEXT_REVIEW_CORRECTION.md) (original §§1-8 left unedited). Independently verified (not assumed) the three intended Privacy callers' `clinicId` source by direct read of `patientPrivacy.ts`'s `resolvePatient()` — authorization-validated (org + `allowedClinicIds`-scoped), recorded as an explicit Stage 3 re-verification precondition rather than a standing guarantee. **Option A contract retained unchanged.** F2-PREP-009 remains `PROPOSED`; F2-IMPL-001-A remains `BLOCKED_TENANT_CONTEXT_CONTRACT_INSUFFICIENT` / PR #304 `OPEN`, unmerged, not modified by this correction. No application/schema/migration/package-script/workflow file touched. |
| 2026-08-03 | F2-PREP-009 — PR #307 main reconciliation (same PR #307, documentation/architecture-only) | PR #307's pre-reconciliation head (`eb0865c4`) had failed CI twice on run [30813103465](https://github.com/MustafaBasol/DisKlinikCRM/actions/runs/30813103465) on `ci-layers / Layer 3` (`patientEmergencyContactsPrimaryConcurrency.test.ts`) — a pre-existing tenant-unrelated race, independently fixed via `MERGED` PR #310 (merge commit `a59c4b7c8b9d09f2dc353fdf38a63705e0881c56`, now `origin/main`'s tip) with duplicate PR #311 closed unmerged. Merged `origin/main` into the PR #307 branch via one plain `git merge --no-ff` (no rebase, no force-push, no blanket `-ours`/`-theirs`) — automatic, zero conflicts (disjoint file sets). New head: `56329369656b1338a358a243935823bc8b067b0a`, pushed. Contract preservation re-verified unchanged on the reconciled head (explicit `clinicId` on all four methods, fail-closed predicates, no bypass, correctly-split cross-tenant test cases, Finding 1 open/Finding 2 historical against PR #304's unchanged head `abac5e361a`). PR #307 body corrected: removed the stale "CI passed in full" claim, recorded the twice-failed-run/PR #310/PR #311 history, required CI be evaluated only on the final reconciled head, no deployment/production-verification claim made. **Final CI on reconciled head, run [30819294580](https://github.com/MustafaBasol/DisKlinikCRM/actions/runs/30819294580): 9/9 required `ci-layers` jobs `SUCCESS`**, including the previously-failing Layer 3 job. No application/schema/migration/package-script/workflow file authored by this task's own commits — application-code diff arrived solely via the `origin/main` merge commit. F2-PREP-009 remained `PROPOSED` as of this entry (CI passing is not program-owner acceptance) — since accepted and merged via PR #307 (see F2-IMPL-001-A-R2 below); F2-IMPL-001-A remained `BLOCKED_TENANT_CONTEXT_CONTRACT_INSUFFICIENT` as of this entry; PR #304 untouched. Task status: `AGENT_COMPLETED` / `DOC_VALIDATION_PASSED` / `PR_CI_PASSED` / `PR_OPENED_AWAITING_PROGRAM_OWNER_ACCEPTANCE`. |
| 2026-08-03 | F2-IMPL-001-A-R2 (re-implementation, same PR #304, `OPEN`, not merged) | F2-PREP-009 accepted and merged via PR #307 (merge commit `967ed95c114f07580bfb69fe0386becc45b4313d`, confirmed ancestor of `origin/main`; post-merge main CI run `30823445835`: `success`). Reconciled PR #304's branch with `origin/main` (`git merge --no-ff`, merge commit `d2eaeaa9240e576c201eb5e84bb3defc9d7d53de`, 4 conflicted files resolved manually — `docs/program/CURRENT_PHASE.md`, `docs/program/NORAMEDI_MASTER_TRACKER.md`, `docs/program/evidence/README.md`, `server/package.json`). Re-implemented `server/src/services/imaging/public.ts` against the amended contract: `markStorageMissing(clinicId, imageId)`, `redactForAnonymization(clinicId, imageId, reason)`, `checkImageStorageExists(clinicId, imageId)` — all now require an explicit, caller-supplied, already-authorization-validated `clinicId`, applied as a top-level `where` predicate (`{ id: imageId, clinicId, study: { clinicId } }`) on every read AND write, never a post-fetch/self-comparison; `getImagesForLifecycleReview(clinicId, patientId)` unchanged. **Finding 1 (tenant-authorization gap) closed.** No compatibility overload, no ambient/global tenant context, no system bypass. `server/src/tests/imagingLifecycleFacade.test.ts` rewritten with cross-tenant-denial, missing-vs-cross-tenant indistinguishability, denormalization-mismatch-still-fails-closed, and rejected-write-leaves-target-row-untouched assertions — **30/30 passing**; `test:imaging` **103/103** and `test:patient-privacy` **38/38** regression-clean; full `server:test:disposable-db` aggregate (incl. `test:imaging-characterization`, unmodified) exit `0`, disposable-PostgreSQL cleanup succeeded. `npx prisma generate && tsc --noEmit`: exit `0`. Facade remains additive and unused — zero production callers, independently re-confirmed. No schema/migration file touched. Full evidence: [F2-IMPL-001-A_ADDITIVE_UNUSED_INTERNAL_IMAGING_FACADE_SKELETON.md §27](../evidence/F2-IMPL-001-A_ADDITIVE_UNUSED_INTERNAL_IMAGING_FACADE_SKELETON.md#27-f2-impl-001-a-r2--re-implementation-against-the-accepted-explicit-tenant-contract-2026-08-03). Task status: `AGENT_COMPLETED` / `TESTS_PASSED` / `PR_CI_PASSED` / `PR_OPENED_AWAITING_PROGRAM_REVIEW`. Merge safe: no (awaiting program review). Deployment safe: no. |
| 2026-08-03 | F2-IMPL-001-A-R3 (atomic legal-hold correction, same PR #304, `OPEN`, not merged) | `redactForAnonymization` previously checked `ImagingStudy.legalHold` only in memory (read-time), then wrote via `updateMany` without `legalHold` in the predicate — a hold acquired between the read and the write went unenforced (TOCTOU race), silently redacting a now-held image. Fixed: the write predicate is now `{ id: imageId, clinicId, study: { clinicId, legalHold: false } }` — legal hold is enforced by the same database statement that performs the write, not a prior check. A 0-row result is classified using a second, fully tenant-scoped `findOwnedImage` follow-up only (never unscoped-by-id): not-found-under-tenant → `ImagingNotFoundError`; already-redacted (concurrent writer) → benign no-op; otherwise → `ImagingLegalHoldViolationError`. A cross-tenant image is rejected before this logic is reached, so it can never surface `ImagingLegalHoldViolationError` — directly tested. Added a deterministic, promise-gated (no sleep) test-only synchronization seam, `__setRedactionPreMutationBarrierForTest` (a bare `() => Promise<void>` with no access to `clinicId`/`imageId`/any predicate), and a real disposable-Postgres regression test proving the race is closed (asserts `ImagingLegalHoldViolationError`, `originalName` unchanged, unrelated cross-tenant row untouched), plus 3 more direct tests (mutation-predicate-includes-`legalHold:false` capture, cross-tenant-held-image, denormalized-mismatch-under-hold). Test-seam review: both this seam and the pre-existing `__setImagingStorageExistenceCheckerForTest` retain Option B (module-private variable + narrow test-only setter) with full, precisely-scoped evidence (sequential single-process execution, process-local state, every override `finally`-restored, zero production importers, cannot alter tenant predicates — explicitly scoped to sequential execution, not claimed safe under parallel test execution). No parameter added to any of the four accepted methods. **34/34 facade tests passing** (was 30; +4 net, zero regressions); `test:imaging-characterization`'s `CT-32` sub-suite re-confirmed **153/153**; full `server:test:disposable-db` aggregate exit `0`, disposable-PostgreSQL cleanup succeeded, zero residual Docker resources; `test:imaging` **103/103**, `test:patient-privacy` **38/38** regression-clean; `npx prisma generate && tsc --noEmit` exit `0`; `git diff --check` clean; zero production callers re-confirmed for both the facade and the new test seam. No schema/migration/route/workflow file touched. `origin/main` had not advanced since R2 — no reconciliation required. **Windows Bridge `.NET` job** (`BridgeOrchestratorTests.Heartbeat_AfterPairing_TransitionsToOnlineAndOmitsNullCapabilities`, `Assert.Contains` against an empty captured string) failed on this task's own fresh push then passed on an identical-commit rerun — the same signature independently observed on the R2 push. Classified `FLAKY_UNVERIFIED`: zero `windows-bridge/` files touched by this PR at any point in its history rules out `PR_CAUSED`; the `windows-bridge-pr` workflow triggers only on PR events and never runs against `main`, so no control run exists to confirm `PRE_EXISTING_REPRODUCIBLE_ON_MAIN`; the fail-then-pass-on-identical-commit pattern is flakiness's defining signature. Full evidence: [F2-IMPL-001-A_ADDITIVE_UNUSED_INTERNAL_IMAGING_FACADE_SKELETON.md §28](../evidence/F2-IMPL-001-A_ADDITIVE_UNUSED_INTERNAL_IMAGING_FACADE_SKELETON.md). Final CI status and task status recorded in this task's own delivery report (CI-conditional per this program's status-ceiling convention). Merge safe: no. Deployment safe: no. |
| 2026-08-03 | F2-IMPL-001-A reconciliation (PR #304 merge) | `gh pr view 304` confirms **`MERGED`** — merge commit `c731e7af9a552dde2da45fee52fa318192da28f8`, `mergedAt: 2026-08-03T17:47:49Z`; post-merge main CI run `30838364039` (`ci-main-and-nightly`, `push`) `conclusion: success` — independently confirmed by F2-GUARDRAIL-PREP-010-D. Corrects every prior row above still reading "`OPEN`, not merged." F2-IMPL-001-A: `AGENT_COMPLETED`/`TESTS_PASSED`/`PR_CI_PASSED`/`MERGED`/`MAIN_CI_PASSED`/`NOT_DEPLOYED`/`NOT_PRODUCTION_VERIFIED`. |
| 2026-08-03 | F2-GUARDRAIL-PREP-010-D | Parallel Wave Consolidation and Guardrail Enforcement Authorization — documentation/evidence-reconciliation/program-control only. Reconciled PR #304 (above) against three open, non-overlapping evidence PRs: #313 (71 frozen cross-domain edges, tuple identity `(callerPath, callerSymbol, ownerDomain, targetModelOrSymbol, accessKind)`), #315 (127 tenant-scope records; discovers and this task independently re-confirms present on current `main`: **F2-SEC-001** — Instagram inbox status mutation missing clinic-membership check; **F2-SEC-002** — legacy WhatsApp public API global default-clinic resolution), #314 (7 reference-mutation-pattern candidates, 3 selected reference bases, none canonical — no canonical tenant-scoped mutation contract exists). Re-evaluated CAND-07 (= the `ImagingLifecyclePort` facade) as **`STRENGTHENED_REFERENCE_BASE`**, not promoted to canonical, since F2-IMPL-001-A-R2/R3 close its original rejection basis (missing `clinicId`) but it remains additive/unused with a public test-seam export. **Decision: CI guardrail implementation is NOT authorized while F2-SEC-001/F2-SEC-002 remain open.** Recommended merge order: this consolidation, then #315, #313, #314 — none merged by this task. Full evidence: [F2-GUARDRAIL-PREP-010-D_PARALLEL_WAVE_CONSOLIDATION.md](../evidence/F2-GUARDRAIL-PREP-010-D_PARALLEL_WAVE_CONSOLIDATION.md). |
| 2026-08-04 | F2-DOC-001 | Guardrail Evidence Status Reconciliation — documentation-only, corrects the row immediately above, whose own text still reads "none merged by this task" (accurate as authored 2026-08-03, now stale; not edited in place). **All three evidence PRs are now `MERGED`/`MAIN_CI_PASSED`, in the order #315, #313, #314** (after consolidation PR #317, already merged first): PR #315 merge commit `0881d389ade37606a561659409cc27e38857c318`, `mergedAt 2026-08-03T21:26:22Z`, PR CI `success`, post-merge main CI run `30854633672` `success`; PR #313 merge commit `9e4bd3cf12100f06c6068d29cb35d9d37f0031fa`, `mergedAt 2026-08-03T21:36:04Z`, PR CI `success`, post-merge main CI run `30855318200` `success`; PR #314 merge commit `e9c1765fa191223cb036ebfdb9c72b898e2fc52e`, `mergedAt 2026-08-03T21:45:29Z`, PR CI `success`, post-merge main CI run `30855966630` `success` (also this repository's current `origin/main` tip as of this reconciliation). Status for all three: `AGENT_COMPLETED` / `EVIDENCE_VALIDATED` / `PR_CI_PASSED` / `REVIEW_THREADS_RESOLVED` / `MERGED` / `MAIN_CI_PASSED` / `NOT_DEPLOYED` / `NOT_PRODUCTION_VERIFIED`. **This correction changes no evidence conclusion, count, classification, or candidate-selection result** — CAND-07 remains `STRENGTHENED_REFERENCE_BASE`, not canonical; PR #314's own raw evidence file still reads `REJECTED_AS_REFERENCE` at its own original observation head (historical, not edited). **CI guardrail implementation remains NOT authorized** — the open condition is now phrased against the dedicated security-fix PRs, not the (now-merged) evidence PRs: **F2-SEC-001 — PR #318** (`OPEN`, head `0bb832baf78be430cfdfd428382084b14b042fae`, `NOT_MERGED`; CI still partially in-progress at last check) and **F2-SEC-002 — PR #319** (`OPEN`, head `e53116c393d02be900ce48e187e1d90c59a6d015`, CI all `SUCCESS`, `NOT_MERGED`). Stage 2 Imaging remains blocked, unchanged. No application/schema/migration/test/workflow/package file touched; no evidence-file conclusion altered. |
| 2026-08-04 | F2-DOC-001-R1 | Current F2 Status Header and F2-SEC-002 Evidence Refresh — documentation-only, same PR #320, reviewed head `cdfb564dd85d18295376983ccd5682d26142f777`. Fixes a review-flagged internal contradiction: this document's own "Faz durumu" line (the current-status summary, distinct from this append-only Change history log) still read F2-SEC-001/PR #318 as `OPEN`/`NOT_MERGED`, directly contradicting the "late-breaking correction" paragraph above it, which already recorded PR #318 as `MERGED`. **Faz durumu corrected in place** (the one field in this document treated as live status, not history) to read: F2-SEC-001/PR #318 `MERGED`/`MAIN_CI_PASSED` (merge commit `2452b8c4a4fcd963878bbc0fbc37045d9cef170c`, post-merge main CI run `30891381612` `success`); F2-SEC-002/PR #319 `OPEN`/`NOT_MERGED` (head `fb4e23766478db413f9e34f009302d4f4f0948f7`, PR CI run `30893070050` `success`); guardrail CI `NOT_AUTHORIZED`; Stage 2 Imaging `BLOCKED`. **PR #319 evidence refreshed** everywhere it appears as a current reference: prior head `e53116c393d02be900ce48e187e1d90c59a6d015` (rows/paragraphs above, left unedited as historical) has been superseded on the PR #319 branch by head `fb4e23766478db413f9e34f009302d4f4f0948f7`, PR CI run `30893070050` `success` — both independently re-verified via `gh pr view 319`/`gh pr checks 319`/`gh run view 30893070050`. **F2-SEC-002 is not closed by this task; guardrail CI is not authorized by this task; Stage 2 Imaging is not unblocked by this task.** No evidence-file conclusion, count, or classification touched; no application/schema/migration/test/workflow/package file touched. Files also corrected by this same task: `CURRENT_PHASE.md`, `NORAMEDI_MASTER_TRACKER.md`, `evidence/README.md`. Task status: `AGENT_COMPLETED` / `DOCUMENTATION_VALIDATION_PASSED`. |
| 2026-08-04 | F2-DOC-001-R2 | F2-SEC-002 Merge Reconciliation and Main-Drift Resolution — documentation-only, same PR #320, reviewed head `9119bbed0229b9414696723d4ae7b85b65130a6a`. **PR #319 merged:** `gh pr view 319` confirms state `MERGED`, merge commit `09ee20b7f1f655a4025a32927a8e81e596e1bb97`, independently confirmed an ancestor of `origin/main` (`git merge-base --is-ancestor`, exit `0`; also `origin/main`'s exact tip at task start). **Branch reconciled against `origin/main`** via `git merge --no-ff` — zero conflicts in this or any of the other three shared program-control documents; PR #318/#319's actual diff (`server/src/routes/instagramInbox.ts`, `server/src/routes/whatsapp.ts`, `server/package.json`, two new evidence files `evidence/F2-SEC-001_INSTAGRAM_INBOX_CLINIC_MEMBERSHIP.md`/`evidence/F2-SEC-002_WHATSAPP_EXPLICIT_CLINIC_RESOLUTION.md`, two new test files) arrived via the merge commit, not authored by this task. **Post-merge main CI for `09ee20b7f1f655a4025a32927a8e81e596e1bb97` at the time of this row:** run `30949232722` — `status: in_progress`, now stale, see F2-DOC-001-R3 row below. **F2-SEC-002 status at the time of this row: `MERGED` / `MAIN_CI_PENDING` / `NOT_DEPLOYED` / `NOT_PRODUCTION_VERIFIED`.** **Faz durumu corrected in place again** to read (at that time): F2-SEC-001/PR #318 `MERGED`/`MAIN_CI_PASSED`; F2-SEC-002/PR #319 `MERGED`/`MAIN_CI_PENDING`; guardrail CI `NOT_AUTHORIZED`; Stage 2 Imaging `BLOCKED`. No evidence-file conclusion, count, or classification touched; no application/schema/migration/test/workflow/package file authored by this task (the merge commit's application-code diff arrived solely from `origin/main`). Files also corrected by this same task: `CURRENT_PHASE.md`, `NORAMEDI_MASTER_TRACKER.md`, `evidence/README.md`. Task status: `AGENT_COMPLETED` / `DOCUMENTATION_VALIDATION_PASSED` / `MAIN_SYNCHRONIZED` / `PR_CI_PENDING`. |
| 2026-08-04 | F2-DOC-001-R3 | F2-SEC-002 Post-Merge Main CI Confirmation — documentation-only, same PR #320, reviewed head `3b9c6e9d21a4405c3da635caa93c35c400636a7b`. The R2 row above recorded post-merge main CI run `30949232722` as `in_progress` — that run has since completed. Re-verified via `gh run view 30949232722`: `headSha` `09ee20b7f1f655a4025a32927a8e81e596e1bb97` (exact match), `status: completed`, `conclusion: success`, `event: push`, `workflowName: ci-main-and-nightly`. **F2-SEC-002 final status: `MERGED` / `MAIN_CI_PASSED` / `NOT_DEPLOYED` / `NOT_PRODUCTION_VERIFIED`.** **Both concrete security blockers are now repository-level closed: F2-SEC-001 `MERGED`/`MAIN_CI_PASSED`; F2-SEC-002 `MERGED`/`MAIN_CI_PASSED`.** **Faz durumu corrected in place again** to match. This does **not** authorize guardrail CI implementation, does **not** unblock Stage 2 Imaging, and does **not** accept any canonical tenant-scoped mutation contract — each requires a separate, explicit program-owner decision not made by this documentation task. **A further F2-SEC-003 reconciliation remains required** (deployment/production-verification status for F2-SEC-001/F2-SEC-002 is unaddressed by merge/CI alone). Guardrail CI: `NOT_AUTHORIZED`. Stage 2 Imaging: `BLOCKED`. No evidence-file conclusion, count, or classification touched; no application/schema/migration/test/workflow/package file touched. Files also corrected by this same task: `CURRENT_PHASE.md`, `NORAMEDI_MASTER_TRACKER.md`, `evidence/README.md`. Task status: `AGENT_COMPLETED` / `DOCUMENTATION_VALIDATION_PASSED`. |
| 2026-08-05 | F2-SEC-003 | Security Blocker Closure, Deployment Readiness, and Guardrail Authorization Reconciliation — documentation/repository-verification/deployment-readiness-analysis only, fresh worktree/branch `docs/f2-sec-003-security-gate-reconciliation` off `origin/main` @ `be72eb9b44829c4e6ad9d88e6bd80bcfe88c8fdd` (PR #320's merge commit). Independently re-verified PR #318/#319/#320's exact head/merge/CI facts against live GitHub state (all matched the assigning brief) and their merge commits' ancestry of `origin/main`. **Independently re-confirmed, by direct source-code and test-file read plus verification of already-passing CI-owned disposable-PostgreSQL test results from PR CI and post-merge main CI (no local/task-specific test re-execution by this task; not cited from prior evidence alone), that F2-SEC-001 and F2-SEC-002 are both CONFIRMED CLOSED on current `main`:** F2-SEC-001's `PATCH /instagram/inbox/:id/status` uses a single atomic clinic-membership-scoped `updateManyAndReturn` write-and-return with no pre-write target-row read, correct empty-array handling, and identical fail-closed 404s for cross-org/cross-clinic/nonexistent ids (`server/src/tests/instagramInboxStatusClinicScope.test.ts`, 13 sections incl. a deterministic race regression). F2-SEC-002's six legacy `/api/public/whatsapp/*` routes all share one connection-bound resolution middleware requiring exactly one active-connection secret match, exactly one org-consistent clinic link, and only a strict single-connection legacy-secret fallback (`server/src/tests/dbVerification/whatsappPublicApiExplicitClinicBinding.test.ts`). **Deployment readiness:** F2-SEC-001 `CODE_DEPLOYABLE` (no migration, no config dependency); F2-SEC-002 `CONFIGURATION_VERIFICATION_REQUIRED` (no migration, but production `WhatsAppConnection`/`ClinicWhatsAppConnection` topology — active-connection count, per-connection secret presence, schema-unenforced duplicate-secret risk, decrypt-key readability, one-clinic-link-per-connection, org consistency — was not inspected, no production access granted or used). Neither `NOT_DEPLOYABLE`. Defined (not executed) pre-deploy read-only SQL queries, smoke-test scenarios, rollback plans, and a 15-scenario production-verification checklist. **Guardrail authorization:** F2-GUARDRAIL-PREP-010-D's specific blocking condition (F2-SEC-001/002 open) is now satisfied at the repository level; per this task's own conservative model, that authorizes only **repository-only, non-blocking/report-only** guardrail implementation — CI-blocking/production enforcement remains explicitly **NOT authorized**, gated on a baseline/false-positive-validation review of the guardrail's own report-only output plus F2-SEC-001/F2-SEC-002 deployment + production verification, neither performed by this task. **Stage 2 Imaging remains BLOCKED**, additionally gated on the next guardrail task merging and passing main CI. **Names, does not implement, `F2-GUARDRAIL-IMPL-001`** — exact-edge (PR #313's frozen 5-field tuple) and tenant-scope report-only drift-detection baseline, legacy allowlist descriptive-only, no new legacy edge permitted, machine-readable output, no runtime modularization, no ESLint-wide rewrite, no Stage 2 Imaging caller migration, own measurable promotion-to-blocking criteria required. No runtime code changed (no verified blocking defect found); no deployment performed; no production verification claimed. Full evidence: [F2-SEC-003_SECURITY_GATE_RECONCILIATION.md](../evidence/F2-SEC-003_SECURITY_GATE_RECONCILIATION.md). Files changed: this file; `CURRENT_PHASE.md`; `NORAMEDI_MASTER_TRACKER.md`; `evidence/README.md`; new evidence file. Task status: `AGENT_COMPLETED` / `DOCUMENTATION_VALIDATION_PASSED` / `PR_OPENED` (once opened) — `NOT_MERGED` / `NOT_DEPLOYED` / `NOT_PRODUCTION_VERIFIED`. |
| 2026-08-05 | F2-GUARDRAIL-IMPL-001 | Exact-Edge Architecture Guardrail, Report-Only Baseline — implementation, repository-only/report-only/non-blocking only, isolated worktree/branch `feature/f2-guardrail-impl-001-report-only-exact-edge` off `origin/main` @ `da23f6fd4b86d4a37dc56f90859b0806f8ce60c6` (PR #321's own merge commit, independently re-confirmed via `gh pr view 321`/`gh run view 30996305595`/`git merge-base --is-ancestor`). Implements the guardrail F2-SEC-003 §6 named but did not build: a small TypeScript-compiler-API CLI (`scripts/architecture-guardrail/`, no new dependency — `package-lock.json` unchanged) that scans exactly the five authorized roots (`server/src/{routes,services,jobs,middleware,utils}`), extracts cross-domain relative-import edges into the unchanged frozen `(callerPath, callerSymbol, ownerDomain, targetModelOrSymbol, accessKind)` tuple, and compares them read-only against the existing `F2-GUARDRAIL-PREP-010-A` 71-edge baseline — **no new baseline artifact is committed**. Domain ownership resolved via a checked-in map (179 entries) mechanically derived from the already-accepted `F0-003_module_ownership_inventory.json`. Exit-code contract (0 regardless of finding count; non-zero only for a genuine config/tool failure) tested both as a pure function and a real spawned subprocess. Deterministic (byte-identical across two independent runs, verified); no absolute path or secret-like pattern in any generated report (both independently verified using the exact regex `ci-layers.yml`'s own disposable-runtime jobs already use). **56/56 focused unit tests pass.** Root frontend build, server typecheck, and the pre-existing `scripts/test-runtime` typecheck/unit tests were re-run unaffected (all exit 0; 74/74 pre-existing tests still pass). `actionlint v1.7.12` clean. **Real-scope scan result: 247 files discovered/parsed, 0 skipped, 1,031 findings (1,018 `NEW` / 13 `EXISTING`), 0 errors, 0 warnings, exit 0** — the high `NEW` count is expected and explicitly documented, not claimed as 1,031 real violations (exhaustive detection vs. a curated manual baseline; exactly the false-positive-validation gap F2-SEC-003 §5.2 names as a prerequisite before any blocking use, not closed by this task). CI integration: one new standalone Layer 1 job, `architecture-guardrail-report-only`, added to `.github/workflows/ci-layers.yml` (SHA-pinned actions; JSON-validates and secret-scans its own report; uploads it as an artifact) — deliberately not added to any other job's `needs:` list and nothing needs it, so a guardrail-tool failure can never gate application tests; no existing job/needs-list/path-coverage narrowed. Every report carries an explicit tenant-scope disclaimer (advisory signal only; absence of findings is not proof of tenant isolation; runtime tenant tests/authorization tests/ORM guards/future RLS remain independent controls); no tenant filter read, changed, or relied upon. **This task does not authorize CI-blocking enforcement, does not commit a self-referential baseline, and does not claim any tenant-isolation proof.** No application/schema/migration/production-route/service/repository/worker/auth/storage/messaging/imaging/integration source file touched; `server/prisma/migrations/` and `server/prisma/schema.prisma` unchanged. **Discrepancy note:** the assigning brief cited `docs/program/evidence/F2-GUARDRAIL-PREP-011*`; no such file exists on `origin/main` — recorded as a factual correction, not a stop condition (F2-SEC-003 independently supplies full authorization). Full evidence: [F2-GUARDRAIL-IMPL-001_EXACT_EDGE_GUARDRAIL_IMPLEMENTATION.md](../evidence/F2-GUARDRAIL-IMPL-001_EXACT_EDGE_GUARDRAIL_IMPLEMENTATION.md). Files changed: this file; `CURRENT_PHASE.md`; `NORAMEDI_MASTER_TRACKER.md`; `evidence/README.md`; new evidence files; `scripts/architecture-guardrail/` (new); `package.json` (3 new scripts); `.github/workflows/ci-layers.yml`; `.gitignore`. **Exact next task, not implemented, not started:** a program-owner-reviewed false-positive-validation pass over this task's own report-only output, in parallel with/after F2-SEC-001/F2-SEC-002 deployment + production verification (tracked independently) — only after both may a future task define measurable promotion criteria and propose CI-blocking enforcement. Task status: `AGENT_COMPLETED` / `TESTS_PASSED` / `PR_OPENED` (once opened) — `NOT_MERGED` / `NOT_DEPLOYED` / `NOT_PRODUCTION_VERIFIED`. |
| 2026-08-05 | F2-GUARDRAIL-VAL-001 | Architecture Guardrail Signal-Quality Validation — read-only validation/evidence only, no scanner/domain-map/application code change, isolated worktree/branch `docs/f2-guardrail-val-001-signal-quality` off `origin/main` @ `9b10bc9fde0ceaeb58d90860a950143de7123910` (PR #323's own merge commit, independently re-confirmed via `gh pr view 323`/`gh run view 31002888303`/`git merge-base --is-ancestor`). Performs the false-positive-validation pass F2-SEC-003 §5.2 and F2-GUARDRAIL-IMPL-001 both named as the prerequisite before any CI-blocking-enforcement proposal. Re-ran the guardrail unchanged first: exact-current-main counts identical to previously documented (247 files, 1,031 findings, 1,018 `NEW`/13 `EXISTING`, 0 errors, deterministic, no absolute paths/secrets). Built a deterministic, checked-in, reproducible stratified sample of 169 findings (exceeds the required `max(120,10%)=120` minimum; script: `scripts/architecture-guardrail-validation/buildSample.js`), covering every required stratum (all 13 `EXISTING`, all 4 declared ownership collisions, the top-25 highest-frequency edge families = 76.3% of all findings, every distinct owner/caller domain incl. `UNRESOLVED`, every named sector). Classified all 169 via direct source reads (no CodeGraph available) against the 9-category taxonomy: 87 `EXPECTED_PLATFORM_SHARED_EDGE`, 50 `EXPECTED_PUBLIC_CONTRACT_EDGE`, 15 `DOMAIN_CLASSIFICATION_FALSE_POSITIVE`, 9 `LEGACY_TECHNICAL_DEBT` (all trace to already-documented prior evidence — 0 new debt discoveries), 7 `DATA_OWNERSHIP_REVIEW_REQUIRED`, 1 `REAL_BOUNDARY_VIOLATION` (genuinely new), 0 `PARSER_FALSE_POSITIVE`, 0 `GENERATED_OR_TOOLING_NOISE` — precision (actionable, A+H) 5.9%, false-positive rate (E+F+G) 8.9%, expected-edge rate (B+C) 81.1%, ownership-ambiguity rate (D) 4.1%. **All 4 F0-003-declared ownership collisions independently reconciled** against `F0-003_MODULE_OWNERSHIP_EVIDENCE.md`: `utils/encryption.ts`/`utils/secrets.ts` are F0-003's own documented "true platform primitives, correctly placed" (recommended shared/platform map correction, not a genuine dispute); `services/treatmentStockDeduction.ts`'s file location is already settled by F0-003 as `clinical-dental-chart-procedures` (recommended map correction; its separate cross-domain write into `InventoryTransaction` remains real, already-tracked debt per F0-003's own "Inventory stock-adjustment command" candidate); `routes/organizationDashboard.ts` is the one genuine content-level mixed-responsibility ambiguity (org-administration route also serving `reporting-analytics`, itself F0-003-labeled `"shared/ambiguous"`) — recommended deferred ADR, not resolved by this task. **Separately discovered and characterized:** 308/1,031 findings (29.9%) carry `ownerDomain=UNRESOLVED`, but the large majority (23 distinct target files, ~250+ findings — `db.ts` 100, `utils/helpers.ts` 72, `schemas/index.ts` 46, etc.) trace not to the 4 declared collisions but to a `domain-map.json` *coverage gap* (files simply absent from its 179-entry list), a distinct and larger data-completeness issue conflated with genuine collisions by the scanner's shared `UNRESOLVED` sentinel. **Also directly measured:** of 12 sampled findings independently confirmed to already match an accepted `CDA-0xx` baseline entry, 12/12 (100%) were flagged `NEW` (not `EXISTING`) by the guardrail's own comparison logic — a direct, measured consequence of its documented `callerSymbol`-exclusion limitation materially undercounting already-accepted edges. **Enforcement readiness assessed against all 8 required criteria: `NOT_READY`** (false-positive rate not acceptably closed at the config level; ownership collisions not resolved; no allowlist/encoding mechanism for accepted edges exists; `NEW`/`EXISTING` classification directly shown untrustworthy; high-risk test coverage not independently re-verified by this task; program-owner authorization not obtained; rollback/emergency-disable mechanism does already exist per the guardrail's own standalone non-blocking CI-job design). **This task made no scanner, domain-map, or application code change** — every finding is a data-completeness/config gap, not a scanner logic defect, so nothing met the narrow-code-change-with-before/after-proof bar for an in-task fix. **This task does not authorize CI-blocking enforcement.** No application/schema/migration/production-route/service/worker/auth/storage/messaging/imaging/integration/CI-workflow/dependency source file touched; `server/prisma/migrations/` and `server/prisma/schema.prisma` unchanged. Full evidence: [F2-GUARDRAIL-VAL-001_SIGNAL_QUALITY_VALIDATION.md](../evidence/F2-GUARDRAIL-VAL-001_SIGNAL_QUALITY_VALIDATION.md), plus checked-in JSON companions (`raw_scan_report`, `sample_manifest`, `classified_sample`) under `evidence/tooling/`. Files changed: this file; `CURRENT_PHASE.md`; `NORAMEDI_MASTER_TRACKER.md`; `evidence/README.md`; new evidence file + JSON companions; `scripts/architecture-guardrail-validation/buildSample.js` (new). **Exact next task, not implemented, not started:** (1) a domain-map completeness correction (config-only) covering the 23 missing files plus the 3 map-correctable collisions; (2) a program-owner ADR for `organizationDashboard.ts`; only after both, plus F2-SEC-001/F2-SEC-002 deployment + production verification (independent, still open), may a future task define measurable promotion criteria and propose CI-blocking enforcement. Task status: `AGENT_COMPLETED` / `TESTS_PASSED` / `PR_OPENED` (once opened) — `NOT_MERGED` / `NOT_DEPLOYED` / `NOT_PRODUCTION_VERIFIED`. |
| 2026-08-06 | F2-GUARDRAIL-VAL-002 | Domain-Map Completeness and Accepted-Baseline Reconciliation — config-only correction of `scripts/architecture-guardrail/config/domain-map.json`, no scanner-logic/scan-roots/baseline-file/application code change, isolated worktree/branch `chore/f2-guardrail-val-002-domain-map-reconciliation` off `origin/main` @ `61ca9d82507b3f24f3c943104cb6aa20ad13faa0` (PR #327's own merge commit, independently re-confirmed via `gh pr view 327`/`git merge-base --is-ancestor`). Performs the domain-map completeness correction F2-GUARDRAIL-VAL-001 named as its exact next task. Exhaustive (not sampled) audit: full walk of the five scan roots diffed against `domain-map.json`'s 179 keys found 70 of 247 discovered files unmapped plus 2 further out-of-root import-target files (`db.ts`, `schemas/index.ts`) — 72 total. 33 resolved by verbatim citation against `F2-PREP-001_domain_ownership_inventory.json` (a more-current, already-accepted 38-domain reconciliation), correcting VAL-001's own tentative "new communication-consent domain?" recommendation (it already belongs to the existing `core-privacy-consent-retention-dsr` domain per F2-PREP-001) and recognizing `services/externalCalendar/*` as F2-PREP-001's own already-accepted `external-calendar-integration` domain, not a new invention. Remaining 37 classified via direct source reads plus a new reverse-dependency evidence tool (`scripts/architecture-guardrail-validation/buildReverseDeps.ts`, checked in, reuses the guardrail's own `moduleResolution.ts`). Applied: 72 new entries (2 new domains — `core-shared-platform-infrastructure`, `messaging-shared-channel-infrastructure`), 5 path-heuristic reclassifications, 3 of 4 F0-003-declared ownership collisions resolved (`utils/encryption.ts`/`utils/secrets.ts` → new `core-platform-crypto` domain; `services/treatmentStockDeduction.ts` → its settled `clinical-dental-chart-procedures` location, its separate real cross-domain write explicitly not closed), 2 dead-key removals. `routes/organizationDashboard.ts` deliberately left `UNRESOLVED` per the assigning brief; its real `getDateRange` violation independently re-confirmed still `NEW`/actionable, pending the still-not-performed `F2-ADR-ORG-DASH-001`. **Domain-map completeness independently re-verified: 0 of 247 scan-root files unmapped** (was 70). Before/after scan (each run twice, byte-identical both times): 1,031→1,038 findings (net +7, fully traced — not estimated — to 61 truly-added previously-hidden edges minus 54 truly-removed false-cross-domain edges; an increase is the correct result here, not a regression, since no application code changed), `UNRESOLVED`-owner findings 308→1, `UNRESOLVED`-caller findings 75→4, one baseline edge (`CDA-031`) newly machine-matched `EXISTING` with zero baseline-file edit. All 54 removed findings individually risk-checked against the 7 high-risk domains; exactly one qualified and was confirmed a genuine same-domain false positive, not a suppressed violation. **Enforcement readiness re-assessed against all 8 VAL-001 criteria: `NOT_READY`, unchanged** (stronger evidence on the false-positive-rate criterion specifically; the scanner's own `callerSymbol`-exclusion baseline-match limitation, the `organizationDashboard.ts` ADR, program-owner sign-off, and a fresh quantified re-sample against the corrected map all remain open). **This task explicitly declines to bulk-author new `F2-GUARDRAIL-PREP-010-A` baseline entries** for the ~88 VAL-001-pre-classified-but-still-`NEW` findings, handing that off as a separately-scoped follow-up requiring the same per-edge authoring rigor as the original baseline task. 74/74 `guardrail:test` and 74/74 `test:runtime:unit` pass before and after; `typecheck:guardrail` clean; `git diff --check` clean. No scanner (`lib/`, `cli.ts`), scan-roots, baseline-file, or application-source file touched; no schema/migration touched; no dependency added. **This task does not authorize CI-blocking enforcement.** Full evidence: [F2-GUARDRAIL-VAL-002_DOMAIN_MAP_AND_BASELINE_RECONCILIATION.md](../evidence/F2-GUARDRAIL-VAL-002_DOMAIN_MAP_AND_BASELINE_RECONCILIATION.md), plus checked-in JSON companions under `evidence/tooling/`. Files changed: this file; `CURRENT_PHASE.md`; `NORAMEDI_MASTER_TRACKER.md`; `evidence/README.md`; new evidence file + JSON companions; `scripts/architecture-guardrail-validation/buildReverseDeps.ts` (new); `scripts/architecture-guardrail/config/domain-map.json`. **Exact next task, not implemented, not started:** (1) `F2-GUARDRAIL-PREP-010-A` baseline-entry authoring for the ~88 already-classified-but-unmatched findings; (2) the program-owner ADR for `routes/organizationDashboard.ts`; (3) a fresh stratified false-positive sample against the corrected domain-map; only after all three, plus F2-SEC-001/F2-SEC-002 deployment + production verification (independent, still open), may a future task define measurable promotion-to-blocking criteria and propose CI-blocking enforcement. Task status: `AGENT_COMPLETED` / `TESTS_PASSED` / `PR_OPENED` (once opened) — `NOT_MERGED` / `NOT_DEPLOYED` / `NOT_PRODUCTION_VERIFIED`. |
| 2026-08-06 | F2-ADR-ORG-DASH-002 | Resolve organizationDashboard Domain Ownership Ambiguity — ADR + one-value config change, PR [#331](https://github.com/MustafaBasol/DisKlinikCRM/pull/331) (open, not merged), isolated worktree/branch `docs/f2-adr-org-dashboard-002-ownership` off `origin/main` @ `46acae8415020cb0bd340fbc854c4187c43e3662`. Closes the **last** `UNRESOLVED` entry in `scripts/architecture-guardrail/config/domain-map.json` and the **fourth and final** F0-003-declared ownership collision — the item deferred in turn by F2-GUARDRAIL-VAL-001 §9 (collision #4), F2-GUARDRAIL-VAL-002 §5.5, and F2-ADR-ORG-DASH-001, and named by all three as an exact next task. PRs #329 (`99436e5…`) and #328 (`46acae8…`, the tip) both re-verified `MERGED` via `gh pr view` and confirmed ancestors of `origin/main` via `git merge-base --is-ancestor`; post-merge `main` CI run `31098023681` (`ci-main-and-nightly`) `success`. **Baseline correction recorded:** the pre-created agent worktree sat one commit behind at `99436e5`, so the task branch was created explicitly from `origin/main` rather than the inherited HEAD — recorded rather than silently accepted, since it would otherwise have produced a stale baseline. **CodeGraph limitation recorded:** a `.codegraph/` index exists but resolved to the *primary* worktree (HEAD `255392c`) and was demonstrably stale (it still showed `getDateRange` inside `organizationDashboard.ts`, the pre-PR-#328 state); it was used only to corroborate caller-graph shape, and every ownership-relevant fact comes from direct source reads at the baseline SHA. **Decision (ADR [`ADR-F2-ORG-DASH-OWNERSHIP`](../../architecture/f2/ADR-F2-ORG-DASH-OWNERSHIP.md), Option A, HIGH confidence): `server/src/routes/organizationDashboard.ts` → `core-org-clinic-membership`; no code split is a prerequisite for the assignment.** Adjudicated on authorization semantics, tenant-scope derivation, owned-model access, response identity, and accepted consumer ownership — explicitly not on filename, and explicitly not to reduce finding counts. Decisive evidence: the route's gate (`authorize(['OWNER','ORG_ADMIN'])` + `canAccessOrganizationDashboard()` = `isOrganizationAdmin()`) is byte-identical to `organizationBranches.ts`'s organization-administration gate and **strictly narrower** than the gate every one of `reports.ts`'s five endpoints uses (`OWNER|ORG_ADMIN|CLINIC_MANAGER|BILLING`, the exact set `canAccessReports()` returns); the handler derives its own security boundary from `core-org-clinic-membership`-owned state (`Clinic.organizationId`, `Clinic.status`, `UserClinic`, `canAccessAllClinics`, `allowedClinicIds`), which a reporting-owned classification would convert into a cross-domain read *into the tenant-critical membership tables to compute a security scope*; its sole consumer `src/pages/OrganizationDashboard.tsx` is already accepted-listed under `core-org-clinic-membership` in **both** F0-003 and F2-PREP-001 (whose `reporting-analytics` frontend lists only `Reports.tsx`/`Dashboard.tsx`); its response rows are an organization branch directory (`clinicId`/`clinicName`/`clinicSlug`/`status`/`address`) enriched with metrics; and the map already assigns dashboard routes by subject domain (`financeDashboard.ts` → `finance-advanced-compensation`, `dashboard.ts` → `reporting-analytics`). An eight-block responsibility decomposition placed 6 blocks unambiguously in ORG, 1 already relocated to `core-shared-platform-infrastructure` by merged PR #328, and 1 mixed — **no block resolved to `reporting-analytics`**, which owns none of the four models the aggregation reads. Options considered: **B (reporting-owned) rejected** — mischaracterizes org scope as a filter when it is the security boundary, and would add cross-domain reads into `Clinic`/`UserClinic`; **C (physical split) rejected for now, not in principle** — the file has one handler and one composed response whose `summary`/`insights` both derive from the same `clinicMetrics` array, so there is no clean seam; a split would relocate rather than remove the cross-domain reads, need at least four new public contracts, and risk response-shape drift, all of which the task's own implementation-authorization boundary requires be stopped and reported; **D (composition shell over accepted public read contracts) accepted as the target end-state but not achievable now**, since none of the required contracts exists — and Option A is its necessary first step, because D presupposes a single-owner shell. **Implementation (minimal, mechanical, inside the authorization boundary — the brief's permitted example "update one exact domain-map entry"):** exactly one `domain-map.json` value, `UNRESOLVED` → `core-org-clinic-membership`, plus an *additive* `provenance.subsequentCorrections[]` note that supersedes F2-GUARDRAIL-VAL-002's entry **without rewriting it**; the historical `ambiguousFilesMappedToUnresolved[0]` record and VAL-002's `corrections` block are left byte-unchanged. `provenance` is typed `Record<string, unknown>` and never read by the scanner, so the addition cannot affect scan behaviour; `fileCount` (249) and the key set are unchanged. **Zero application source, Prisma schema, migration, route-path, response-shape, or authorization change**; guardrail `lib/`, `cli.ts`, `scan-roots.json`, the `F2-GUARDRAIL-PREP-010-A` baseline file, and `ARCHITECTURE_DECISIONS.md` are all untouched. **Guardrail before/after (four `--deterministic` runs, byte-compared pairwise — 415,403 B before ×2, 415,483 B after ×2; the 80-byte delta is exactly 5 × the 16-character `UNRESOLVED`→`core-org-clinic-membership` growth):** total findings **1,039 → 1,039**; `NEW`/`EXISTING` **1,024/15 unchanged**; errors/warnings **0/0** and files discovered/parsed/skipped **247/247/0** in all four runs; **0 tuples added, 0 removed, 0 finding IDs changed** (`computeFindingId` hashes only the frozen 5-field tuple and deliberately excludes `callerDomain`); findings with `callerDomain: UNRESOLVED` **5 → 0**; findings *targeting* `organizationDashboard.ts` **0 → 0**, durably re-confirming PR #328's route-to-route fix; high-risk-domain-touching findings **526 → 526, identical set by finding ID** (3 of the 5 relabelled findings target high-risk domains — `core-identity-access` ×2, `core-permissions-roles` ×1 — and all 3 survive); `resolvedBaselineEdgeIds` `["CDA-072"]` unchanged. **Exactly five finding labels changed and nothing else — no violation suppressed, no wildcard allowlist (the map supports only exact keys; `lib/classification.ts` does an exact lookup and never guesses), no baseline edit.** None of the five edges' targets is `core-org-clinic-membership`, so not one edge became same-domain and vanished. Domain map now: **0 `UNRESOLVED` entries (247/247 mapped), 0 open F0-003 ownership collisions, 37 domains, `fileCount` 249.** **Explicitly NOT resolved, and stated rather than hidden:** the handler's direct cross-domain Prisma reads of `Appointment`/`Patient`/`TreatmentCase`/`Payment` remain open technical debt — this decision *increases* their visibility rather than reducing it (under `reporting-analytics` they would have been domain-normal for a cross-cutting read surface; under `core-org-clinic-membership` they are explicit, non-normalized accesses requiring a contract), and they are invisible to the guardrail in either case because `lib/edgeExtraction.ts` is deliberately import-only and not Prisma-aware. Pre-existing route-level test gaps (no HTTP-level `403`/`400`/`500` assertions, no endpoint-level cross-organization leakage test, metric logic re-implemented in the unit test rather than imported) are recorded as unchanged and **not** closed — closing them would require either re-creating the route-as-utility-module pattern PR #328 removed, or adding route-level HTTP tests, both outside this task's scope. **Tests:** `npm ci` (root + server) exit 0; `typecheck` clean; `typecheck:guardrail` clean; `test:orgdash` 35/35; `guardrail:test` 74/74; `test:runtime:unit` 74/74; `test:finance` 25/25; `test:dashboard` 38/38; `test:roles` 142/142; `git diff --check` clean. Every script the brief named exists verbatim and was run as named — no substitution required. `server:test:non-disposable` deliberately not run: its trigger condition ("if implementation changes route or service code") is not met, a condition-checked decision rather than an omission. **Deployment classification: docs/config-only — no application deployment required**; `domain-map.json` is consumed only by the developer/CI-time `guardrail:scan` script, is not imported by `server/src/**` or `src/**`, is not bundled, and is never read at application runtime. **This task does not authorize CI-blocking enforcement** and did not enable it; the guardrail's findings-still-exit-0 contract is unmodified and still asserted by `guardrail:test`. Resolving this entry removes one documented blocker from VAL-001's enforcement-readiness checklist but does not make enforcement ready — the ~88 pre-classified-but-formally-`NEW` findings and the `callerSymbol` baseline match-key limitation both remain open. ADR placed at `docs/architecture/f2/` per the repository's real convention (**no `docs/program/adr/` directory exists**; the numbered ADR index is `docs/program/ARCHITECTURE_DECISIONS.md` and full ADR content lives under `docs/architecture/`, which already has an `f2/` subdirectory), and **no registry number is claimed** — assigning `ADR-018` extends a program-owner-controlled sequence and is left as a program-owner action. Full evidence: [F2-ADR-ORG-DASH-002_ORGANIZATION_DASHBOARD_OWNERSHIP.md](../evidence/F2-ADR-ORG-DASH-002_ORGANIZATION_DASHBOARD_OWNERSHIP.md), plus four checked-in scan reports under `evidence/tooling/`. Files changed: this file; `CURRENT_PHASE.md`; `NORAMEDI_MASTER_TRACKER.md`; `evidence/README.md`; new ADR + evidence file + 4 JSON scan reports; `scripts/architecture-guardrail/config/domain-map.json`. **Exact next task, not implemented, not started:** `F2-ORG-DASH-METRICS-CONTRACT` (proposed ID) — execute Option D by introducing accepted public read contracts for the four cross-domain metric reads, converting this file from a correctly-owned monolithic handler into a correctly-owned composition shell; prerequisites are this PR merged and post-merge `main` CI green. Task status: `AGENT_COMPLETED` / `TESTS_PASSED` / `GUARDRAIL_DELTA_VERIFIED` / `PR_OPENED` — `NOT_MERGED` / `NOT_DEPLOYED` / `NOT_PRODUCTION_VERIFIED`. |
| 2026-08-06 | F2-GUARDRAIL-VAL-003 | Baseline Match-Key Reconciliation for Previously Classified Expected Edges — baseline-data authoring only, no scanner/matcher/application code change, isolated worktree/branch `fix/f2-guardrail-val-003-baseline-match-key-reconciliation` off `origin/main` @ `46acae8415020cb0bd340fbc854c4187c43e3662` (PR #328's own merge commit). **Task-ID correction:** assigned as `F2-GUARDRAIL-PREP-010-B`, already taken by a distinct merged task (PR #315) — independently confirmed before any file was written, adopted as `F2-GUARDRAIL-VAL-003` (continues the VAL sequence; verified unclaimed via `gh pr list`/`git ls-remote`/directory grep). Performs the baseline-entry authoring item (1) above named as F2-GUARDRAIL-VAL-002's own next task. Independently re-derived the exact 88-candidate set (137 VAL-001 `B`/`C` findings cross-referenced by finding ID against a fresh before-scan: 39 ID-changed excluded, 10 already `EXISTING`, 88 still `NEW`). **Disproves F2-GUARDRAIL-VAL-002's own causal claim:** direct inspection of `lib/baseline.ts` plus its own passing regression test (`__tests__/baseline.test.ts:58-68`) proves the `callerSymbol`-exclusion match-key behavior is intentional and already correct — 87/88 candidates simply had no baseline entry at all for their exact edge, 1/88 had entries for the same caller file covering a different edge; a data-completeness gap, not a matcher defect. **Option D selected** (author exact baseline entries, no matcher change) after explicitly rejecting Options A/B/C on this evidence — none justified, since no defect exists and each would have broken 90 already-legitimate same-edge sibling matches this task's own after-scan reveals. All 88 candidates individually re-verified against current source (import re-confirmed present via direct regex re-check; each of 48 distinct target files' own header read directly) before being added as `CDA-073..CDA-160` to `F2-GUARDRAIL-PREP-010-A_cross_domain_access_inventory.json` — append-only, all 71 pre-existing edges script-verified byte-identical before/after, same exact-`callerPath` convention the document's own `CDA-067..072` entries already established. Before/after scan, byte-identical determinism confirmed 3×: `1,039` total findings unchanged; `NEW` `1,024 → 846` (**−178**); `EXISTING` `15 → 193` (**+178**); `resolvedBaselineEdgeIds` unchanged. The −178 (not −88) fully traced: 88 explicit candidates plus 90 verified "swept-in" same-edge sibling imports (additional named symbols from one already-vetted target in one already-verified caller file, matched because `callerSymbol` is correctly excluded from the key) — a checked-in script independently confirmed 100% (178/178) attributable by exact key, 0 unattributed, 0 involving `organizationDashboard.ts`. 47/88 new entries touch a high-risk domain, each individually reviewed — all are routes/services/jobs importing centralized security/tenant/audit/privacy utilities (e.g. `middleware/clinicAccess.ts` → `evaluateCrossTenantDenialSignal`), the correct pattern, no violation hidden. No scanner (`lib/`, `cli.ts`), scan-roots, domain-map, or application/schema/migration/tenant/auth source file touched. `74/74` `guardrail:test`, `74/74` `test:runtime:unit`, `typecheck:guardrail` clean, `git diff --check` clean. **This task does not authorize CI-blocking enforcement.** Does not decide, edit, or baseline anything involving `organizationDashboard.ts`, per the parallel `F2-ADR-ORG-DASH-002` ownership-ADR task's isolation rule. Full evidence: [F2-GUARDRAIL-VAL-003_BASELINE_MATCH_KEY_RECONCILIATION.md](../evidence/F2-GUARDRAIL-VAL-003_BASELINE_MATCH_KEY_RECONCILIATION.md), plus checked-in JSON companions under `evidence/tooling/`. Files changed: this file; `CURRENT_PHASE.md`; `NORAMEDI_MASTER_TRACKER.md`; `evidence/README.md`; new evidence file + JSON companions; `scripts/architecture-guardrail-validation/{buildVal003Candidates,verifyVal003Candidates,generateVal003BaselineEntries,verifyVal003AfterScan}.mjs` (new); `evidence/F2-GUARDRAIL-PREP-010-A_cross_domain_access_inventory.json` (data, append-only). **Exact next task, not implemented, not started:** (1) a fresh stratified false-positive re-sample against the now-twice-corrected baseline/domain-map (item (3) above, still open); (2) the program-owner ADR for `routes/organizationDashboard.ts` (`F2-ADR-ORG-DASH-002`, in progress in parallel, not decided by this task); only after both, plus F2-SEC-001/F2-SEC-002 deployment + production verification (independent, still open), may a future task define measurable promotion-to-blocking criteria and propose CI-blocking enforcement. Task status: `AGENT_COMPLETED` / `TESTS_PASSED` / `PR_OPENED` (once opened) — `NOT_MERGED` / `NOT_DEPLOYED` / `NOT_PRODUCTION_VERIFIED`. |
| 2026-08-07 | F2-GUARDRAIL-VAL-003-R2 | Rebase/Reconciliation onto post-F2-ADR-ORG-DASH-002 main — no scanner/matcher/application code change, existing worktree/branch `fix/f2-guardrail-val-003-baseline-match-key-reconciliation`, PR [#330](https://github.com/MustafaBasol/DisKlinikCRM/pull/330) (open, not merged). Reconciles the corrected F2-GUARDRAIL-VAL-003-R1 entry (above) against `origin/main` now that PR #331 (`F2-ADR-ORG-DASH-002`) merged (`9196ceb9a8beff561af55ff2d15b13c0a7874bc2`). Pre-rebase head `6dc657a62397737441922c3c97686c2086cde3c8`; `git rebase origin/main` replayed 2 commits, conflicts in `CURRENT_PHASE.md`, `NORAMEDI_MASTER_TRACKER.md`, and this file, all resolved by preserving both F2-ADR-ORG-DASH-002's and F2-GUARDRAIL-VAL-003's entries intact (`evidence/README.md` merged with no real conflict); new head `52e99d1c1fe8b25a453ce64125ce65624898791f`. **The entire VAL-003 pipeline (`buildVal003Candidates.mjs` → `verifyVal003Candidates.mjs` → `generateVal003BaselineEntries.mjs` → `verifyVal003AfterScan.mjs`) was independently re-derived from scratch** against the rebased head, starting from the pre-VAL-003 71-edge baseline restored from `origin/main` — **result: no count or content difference from R1** (88 candidates, 0 organizationDashboard-involved; 88 re-confirmed against current source; 88 new entries `CDA-073..CDA-160`; before/after `NEW` `1,024 → 846`, `EXISTING` `15 → 193`; 88 explicit + 90 swept-in siblings = 178 total flips, 0 unattributed, 0 organizationDashboard-involved). `edges[]` and all four VAL-003 tooling JSON companions are semantically byte-identical to the pre-rebase committed versions. **organizationDashboard.ts isolation independently re-verified:** `domain-map.json` still maps it to `core-org-clinic-membership` exactly as PR #331 assigned, untouched by this task; 0 of the 88/90 candidates/siblings involve that file (one swept-in sibling's `callerSymbol`, `canAccessOrganizationDashboard`, is a substring false-positive on the symbol name only, imported from `utils/roles.ts`, not `organizationDashboard.ts`). Before-scan and after-scan each run 2×/3× and byte-compared, all identical (determinism confirmed). **Tests:** `typecheck:guardrail` clean; `guardrail:test` 74/74; `test:runtime:unit` 74/74; `guardrail:scan` exit 0; `node --test scripts/architecture-guardrail-validation/__tests__/*.test.js` 30/30 (incl. no-basename/no-substring-match and docblock-opener-extraction regression tests); `git diff --check` clean (benign CRLF notices only). No scanner/domain-map/application/schema/migration/tenant/auth-source file touched; no dependency manifest changed; no install required. **This task does not authorize CI-blocking enforcement.** Full evidence: [F2-GUARDRAIL-VAL-003_BASELINE_MATCH_KEY_RECONCILIATION.md](../evidence/F2-GUARDRAIL-VAL-003_BASELINE_MATCH_KEY_RECONCILIATION.md) (original, not re-authored — this rebase introduced no evidence-conclusion change). Files changed: `F2-GUARDRAIL-PREP-010-A_cross_domain_access_inventory.json` (correction-history/counts only — `edges[]` content unchanged); six regenerated before/after scan JSON companions (repo-sha field + 5 organizationDashboard `callerDomain` values only); `CURRENT_PHASE.md`; `NORAMEDI_MASTER_TRACKER.md`; this file. All four of PR #330's prior automated review threads independently re-verified still fixed post-rebase and resolved (0 unresolved of 4 total). **CI on the exact new head SHA `16c1e441dce77b6321c3704ba2ef9b59957baa81`: `success`** (run `31161018540`, all 10 `ci-layers` jobs pass — one transient concurrency-stress-test flake in an unrelated pre-existing test, `patientEmergencyContactsPrimaryConcurrency.test.ts`, cleared on re-run of that job alone). **Exact next task:** external review/merge decision for PR #330 (not performed by this task). Task status: `AGENT_COMPLETED` / `TESTS_PASSED` / `PR_OPENED` (already open) / `CI_PASSED` — `NOT_MERGED` / `NOT_DEPLOYED` / `NOT_PRODUCTION_VERIFIED`. |
| 2026-08-07 | F2-ORG-DASH-METRICS-CONTRACT | Organization Dashboard Metrics Public Read Contracts — executes `ADR-F2-ORG-DASH-OWNERSHIP` §9 follow-up 1 (Option D), isolated worktree/branch `refactor/f2-org-dashboard-metrics-public-contracts` off `origin/main` @ `06f832c229b9f5398c7f52def16e0d37b18c6417`. Closes the ADR's explicitly-named open debt: `organizationDashboard.ts`'s 11 direct foreign-domain Prisma reads (Appointment ×5, Patient ×2, TreatmentCase ×2, Payment ×2 — exact count from a full re-read of the base-SHA handler, not assumed from the four model families named in the brief). **Introduces four new same-process public read contracts**, one per owning domain: `server/src/services/appointments/organizationAppointmentMetrics.ts` (`clinical-appointments-availability`, `getOrganizationAppointmentMetrics(clinicId, range)` → `{todayAppointments, appointments, completedAppointments, cancelledAppointments, noShowRate}`), `server/src/services/patientOrganizationMetrics.ts` (`clinical-patients`, `getOrganizationPatientMetrics(clinicId, range)` → `{newPatients, totalPatients}`), `server/src/services/treatmentCaseOrganizationMetrics.ts` (`clinical-treatment-cases`, `getOrganizationTreatmentCaseMetrics(clinicId, range)` → `{activeTreatmentPlans, completedTreatments}`), `server/src/services/paymentOrganizationMetrics.ts` (`clinical-basic-payments`, `getOrganizationPaymentMetrics(clinicId, range)` → `{revenue, outstandingBalance}`). Every `where` predicate, `_sum` shape, and post-processing formula (the `noShowRate` rounding, the `Number(...) || 0` coercions) is copied unchanged from the pre-refactor handler; each contract takes an already-resolved `clinicId` as its leading parameter and performs no authorization of its own — no raw request body/query clinic id is ever passed in. `organizationDashboard.ts` itself is unchanged in ownership (`core-org-clinic-membership`, not relocated, not reopened), authorization (`authorize(['OWNER','ORG_ADMIN'])` + `canAccessOrganizationDashboard()`), or tenant-scope derivation (`organizationId` → `scopeClinicIds` → per-clinic `Promise.all`); it now composes the four contracts' DTOs plus its own two in-domain `UserClinic` counts instead of querying `Appointment`/`Patient`/`TreatmentCase`/`Payment` directly — **zero response field renamed, removed, or added.** `domain-map.json` updated additively (4 new `files` keys + one `provenance.corrections[]` entry; `fileCount` 249 → 253); `organizationDashboard.ts`'s own entry unchanged. **Guardrail before/after** (clean-base-via-`git stash` vs. this branch): `totalFindings` 1,039 → 1,047 (**+8**), `existingFindings` 193 → 193 (unchanged), **0 findings removed**, 8 added — 4 are `organizationDashboard.ts` → each new contract (the intended explicit public-contract import) and 4 are each new contract → `db.ts` (the universal Prisma-client import every service file already has); no baseline/allowlist edit. **Source-level direct-Prisma proof** (the scanner is import-syntax-only and cannot see this): `organizationDashboard.ts` contains zero occurrences of `prisma.appointment.`/`prisma.patient.`/`prisma.treatmentCase.`/`prisma.payment.` after the refactor — asserted as an executable regression case, not just read. **Tests:** `typecheck` (server) clean; `test:orgdash` 35/35; `test:finance` 25/25; `test:dashboard` 38/38; `test:roles` 142/142; `guardrail:test` 74/74; `git diff --check` clean; new `server/src/tests/dbVerification/organizationDashboardMetricsContracts.test.ts` run against a real disposable PostgreSQL 16.14 instance (`orgdash-metrics-pg`, `localhost:55432`; 72/72 migrations applied via `prisma migrate deploy`), **13/13 passed** — covering each contract's exact DTO shape under clinic scoping + date-range filtering (including the two deliberately-all-time, non-date-filtered fields), sibling-clinic isolation, a zero-data clinic, OWNER/`canAccessAllClinics`, ORG_ADMIN single-clinic restriction, a multi-clinic restricted user, a spoofed cross-org `allowedClinicIds` entry (excluded), a zero-clinic organization (`EMPTY_SUMMARY`), summary/per-clinic aggregation equivalence, and the direct-Prisma-isolation proof above. No schema/migration change. No new patient-level PII exposed — every contract returns only aggregate `number`s, never a Prisma entity or per-record field. Full evidence: [F2-ORG-DASH-METRICS-CONTRACT_PUBLIC_READ_CONTRACTS.md](../evidence/F2-ORG-DASH-METRICS-CONTRACT_PUBLIC_READ_CONTRACTS.md). Files changed: this file; `CURRENT_PHASE.md`; `NORAMEDI_MASTER_TRACKER.md`; `evidence/README.md`; new evidence file; `server/src/routes/organizationDashboard.ts`; 4 new service files; 1 new DB-verification test file; `scripts/architecture-guardrail/config/domain-map.json`. **Exact next task:** none named by this task's brief; the ADR's remaining follow-ups (`ARCHITECTURE_DECISIONS.md` registry-number assignment, the ~88 pre-classified guardrail findings, the `callerSymbol` baseline match-key limitation) are unaffected and remain owned by their originating threads. Task status: `AGENT_COMPLETED` / `TESTS_PASSED` — `PR_OPENED` once opened, `NOT_MERGED` / `NOT_DEPLOYED` / `NOT_PRODUCTION_VERIFIED`. |
| 2026-08-09 | F2-STAGE3-IMPL-001 | Privacy/KVKK ImagingLifecyclePort Caller Migration, Initial Slice — implementation, isolated branch `feature/f2-stage3-impl-001-privacy-imaging-lifecycle-migration` off `origin/main` @ `27c54f3aa81af4eecc33e79d77e366a0fe4915bc` (the exact PR #342/`F2-CT-32-R2` merge commit, independently re-confirmed via `git fetch`/`git rev-parse`/`git log` before branching). Unblocked by `F2-CT-32-R2`/PR #342's confirmed merge and green post-merge main CI (run `31304621261`); proceeds on `F2-STAGE3-AUTH-001`'s still-valid `STAGE_3_ENTRY_GATE = SATISFIED` authorization, not re-litigated. Migrates exactly the authorized initial slice: `patientAnonymization.ts`'s `redactPatientImagingImages` now calls `ImagingLifecyclePort.getImagesForLifecycleReview`/`redactForAnonymization` (a caller-side legal-hold adapter checks the DTO's `legalHold` flag before calling the port for a held image, and separately catches a port-detected write-time hold-acquisition race, mapping both onto the pre-existing `skippedLegalHold` counter — closing a TOCTOU gap the old direct-Prisma code never guarded, never surfacing a new failure); `orphanFileInspection.ts`'s `inspectOrphans` now calls the same `getImagesForLifecycleReview` plus `checkImageStorageExists`, with caller-side `.slice(0, 500)` truncation reproducing the old `BATCH_SIZE` cap the port itself does not enforce. Zero direct Imaging Prisma access remains on either migrated path (verified by grep). `markConfirmedMissing` (no `clinicId` parameter, zero production callers) and `deletionReviewInventory.ts`'s imaging `fileSize` read are both explicitly deferred, doc-commented as such, and now regression-tested as untouched by two new static-source assertions. A counter-semantics divergence — an idempotent (already-redacted) re-run initially counted toward `redacted` instead of being silently excluded from every bucket, since the port's DTO exposes no `originalName` — was locked in by an explicit regression test and then **fixed same-day by `F2-STAGE3-IMPL-001-R1`**: `redactForAnonymization` now returns `Promise<{ changed: boolean }>` (additive, DTO unchanged), and the caller gates `redacted++` on `changed === true`, restoring exact pre-migration semantics; see `evidence/F2-STAGE3-IMPL-001-R1_IMAGING_LIFECYCLE_REDACTION_MUTATION_OUTCOME_AMENDMENT.md`. No schema/migration/route/response-shape change; `clinicId` provenance unchanged. **Tests:** `typecheck` clean; new `server/src/tests/dbVerification/privacyImagingLifecyclePortMigration.test.ts` (wired as `test:privacy-imaging-lifecycle-migration` into `server:test:disposable-db`) covering items A–M plus idempotent-rerun and legal-hold-race regressions — **17/17 passed**; full `npm run test:runtime:postgres` orchestrator run (Docker-provisioned digest-pinned `postgres:16-alpine`, PostgreSQL 16, 72/72 migrations applied via `prisma migrate deploy`) — `outcome.exitCode: 0`, `"tests passed"`, `"cleanup succeeded"`; within that run, `CT-05`/`CT-30` (`Imaging-Characterization-Tenant-Lifecycle`, combined with CT-02/03/17/23/26) **29/29**; `CT-23` (`test:imaging-study-request-patient-consistency` + mismatch-detector) **12/12 + 7/7**; `test:imaging-lifecycle-facade` **34/34**; `test:imaging-characterization`'s other members (auth-shape 36/36, ingest-storage 14/14, concurrency characterization/guard/forced-interleaving/ingest-core-convergence all pass) unaffected; `guardrail:test` **74/74**; `guardrail:scan` exit 0 (only new findings: the two migrated files' intended, sanctioned `services/imaging/public.ts` public-contract imports; unrelated pre-existing baseline drift on `imagingBridgePublic.ts` observed but not touched, not caused by this task); `git diff --check` clean. No schema/migration/CI-workflow/guardrail-scanner/domain-map/baseline file touched. Full evidence: [F2-STAGE3-IMPL-001_PRIVACY_IMAGING_LIFECYCLE_CALLER_MIGRATION.md](../evidence/F2-STAGE3-IMPL-001_PRIVACY_IMAGING_LIFECYCLE_CALLER_MIGRATION.md). Files changed: `server/src/services/privacy/patientAnonymization.ts`, `server/src/services/privacy/orphanFileInspection.ts`, `server/package.json` (2 script additions), new test file, this file, `CURRENT_PHASE.md`, `NORAMEDI_MASTER_TRACKER.md`, `evidence/README.md`, new evidence file. **Stage 3 as a whole is NOT complete** — this is the initial slice only; the two deferred gaps (a `clinicId`-bearing redesign of `markConfirmedMissing`; a program-owner-authorized port DTO extension for `deletionReviewInventory.ts`'s `fileSize` read) remain open follow-ups, not started; CI-blocking/production architecture-guardrail enforcement remains explicitly `NOT_AUTHORIZED`, unchanged. **Exact next task:** program-owner review/merge decision for [PR #344](https://github.com/MustafaBasol/DisKlinikCRM/pull/344) (not performed by this task — PR is `OPEN`/`MERGEABLE`, CI `success` 10/10, still `NOT_MERGED`). Task status: `AGENT_COMPLETED` / `TESTS_PASSED` / `PR_OPENED` / `PR_CI_PASSED` (10/10 `ci-layers` jobs, run `31307552547`) — `NOT_MERGED` / `NOT_DEPLOYED` / `NOT_PRODUCTION_VERIFIED`. |
| 2026-08-07 | F2-GUARDRAIL-VAL-004-R1 | Address External Architecture Review Findings on PR #332 — docs/evidence/tooling scripts only, no scanner/domain-map/baseline/runtime/Prisma/auth/tenancy/CI-enforcement code change, existing worktree/branch `docs/f2-guardrail-val-004-fresh-signal-quality`, PR [#332](https://github.com/MustafaBasol/DisKlinikCRM/pull/332) (open, not merged), starting/reviewed head `e8e57bd8bb6ce7e3dd950a808cf4f1d31c1a8628`. Resolves 2 blocking findings from an external architecture review of PR #332. **(1) High-risk sampling partition asymmetric:** `highRiskCategories(e)`/`isHighRisk(e)` correctly flag an edge high-risk from either `ownerDomain` or `callerDomain`, but `buildVal004Sample.mjs`'s census/oversample allocation was keyed only by `ownerDomain`, so caller-side-only high-risk edges could fall into an ordinary standard-proportional stratum. **Fixed:** new deterministic, mutually-exclusive `highRiskDomainKey(e)` (owner domain if itself high-risk, else caller domain) drives the partition; a fix-adjacent double-counting defect the rework surfaced (standard-proportional population/selection re-querying the full remaining pool without excluding already-high-risk-routed edges) was caught and fixed in the same pass — corrected partition independently verified to sum to exactly N=552 with 0 unassigned/duplicated edges. **(2) 21.10% mislabeled a formal ceiling:** `conservativeWeightedUpperBound()` substituted a one-sided Clopper-Pearson bound only for zero-observed-violation non-census strata, kept an unbounded plug-in rate for strata with ≥1 observed violation, complete-case-excluded ambiguous edges, and summed independent per-stratum marginal 95% bounds without any stated simultaneous/global coverage guarantee. **Fixed via the review's own preferred minimal-relabel path** (computation unchanged in substance): renamed `zeroEventSensitivityAnalysis()`, output fields `sensitivityAcceptedRate`/`sensitivityViolationRate` (was `p_hat_conservative_*`), new explicit `hasConfidenceCoverage: false` + `confidenceCoverageNote` in `buildVal004Metrics.mjs`. **Consequence:** the corrected partition changed the reviewed sample from 192 to **266 edges** (159 reused with prior classification carried over unchanged, 33 not re-selected — including 2 of the pre-R1 round's 4 true positives, still valid against the population, simply outside this round's sample — 107 newly and independently classified against current source by 4 parallel sub-agents, 27/27/27/26, 0 overlap/0 gaps verified: 0 new true positives, 0 new ambiguous, 0 new scanner/domain-map defects found). Headline weighted-accepted-rate `99.27% → 99.64%`; raw as-sampled rate `97.87% → 99.24%`; sensitivity value `21.10% → 28.02%` (more non-census zero-event strata now covered, not more uncertainty — the previously-fully-ambiguous `highRiskDomainCensus:imaging-device-bridge` stratum, N=3→8, is no longer fully ambiguous and is now included in the weighted sum with 0 excluded strata, versus 1 excluded pre-R1). Sample manifest and metrics each regenerated and re-run 2×+, byte-identical both times; 6 new regression tests added directly covering the caller-only/owner-only/both-sides-high-risk partition cases, exact-N accounting (the double-counting regression), rerun determinism, and the sensitivity-analysis relabeling (`__tests__/val004Determinism.test.js`, now 50/50 passing, up from 44/44). `typecheck:guardrail` clean; `guardrail:test` 74/74; `test:runtime:unit` 74/74; `git diff --check` clean. Files changed: `scripts/architecture-guardrail-validation/buildVal004Sample.mjs`, `buildVal004Metrics.mjs`, `__tests__/val004Determinism.test.js`; `docs/program/evidence/tooling/F2-GUARDRAIL-VAL-004_sample_manifest.json`/`_classifications_merged.json`/`_metrics.json` (regenerated); 4 new `F2-GUARDRAIL-VAL-004-R1_batch{1..4}_classified.json`; this file; `CURRENT_PHASE.md`; `NORAMEDI_MASTER_TRACKER.md`; `evidence/README.md`; the main evidence doc (updated in place, §0.1). No scanner, domain-map, accepted-baseline, runtime, Prisma/migration, auth, tenancy, or CI-enforcement file touched. `BLOCKING_ENFORCEMENT = NOT_AUTHORIZED`, unchanged. Full evidence: [F2-GUARDRAIL-VAL-004_FRESH_STRATIFIED_SIGNAL_QUALITY.md §0.1](../evidence/F2-GUARDRAIL-VAL-004_FRESH_STRATIFIED_SIGNAL_QUALITY.md). **Exact next task:** external re-review of this correction round; PR #332 not yet merged, do not merge without it. Task status: `AGENT_COMPLETED` / `TESTS_PASSED` / `PR_OPENED` (already open) — `CI_PASSED` (pending independent verification at delivery time) / `NOT_MERGED` / `NOT_DEPLOYED` / `NOT_PRODUCTION_VERIFIED`. |
| 2026-08-07 | F2-DOC-002 | Post-VAL-004/CT-23 Merge Reconciliation and Stage 2 Imaging Gate Refresh — documentation/program-control only, no runtime/test/schema/workflow/package file touched, fresh isolated worktree/branch `docs/f2-doc-002-post-merge-stage2-gate-reconciliation` off freshly-fetched `origin/main` @ `06f832c229b9f5398c7f52def16e0d37b18c6417`. Corrects the row immediately above (and its equivalent entries in `CURRENT_PHASE.md`/`NORAMEDI_MASTER_TRACKER.md`), left unedited per this program's own convention: PR [#332](https://github.com/MustafaBasol/DisKlinikCRM/pull/332) (`F2-GUARDRAIL-VAL-004`/`VAL-004-R1`) independently re-verified `MERGED` (merge commit `4680e1d1ae50a1bce5f1a9a99425f0f2de42de23`, `mergedAt` 2026-08-07T17:48:27Z, `gh pr view 332`, confirmed ancestor of `origin/main`); PR [#333](https://github.com/MustafaBasol/DisKlinikCRM/pull/333) (`F2-CT-23`) independently re-verified `MERGED` (merge commit `06f832c229b9f5398c7f52def16e0d37b18c6417` — also `origin/main`'s exact current tip, `mergedAt` 2026-08-07T17:49:04Z, `gh pr view 333`, confirmed ancestor of `origin/main`). Post-merge main CI independently re-verified via `gh run view 31204059995`: workflow `ci-main-and-nightly`, `status: completed`, `conclusion: success`, `headSha` exactly `06f832c229b9f5398c7f52def16e0d37b18c6417`, **10/10 `ci-layers` jobs `success`**. **VAL-004 final reconciled metrics (R2, unchanged from the row above):** 266-edge sample; 99.64% weighted accepted-rate; 28.02% sensitivity (`hasConfidenceCoverage: false`); `val004Determinism.test.js` 56/56; `guardrail:test`/`test:runtime:unit` 74/74 each — the superseded pre-R1 192-edge/99.27%/21.10%/44-test figures are not reintroduced. **CT-23 (`VERIFIED_DEFECT`, originally found by F2-PREP-007-B) now remediated:** `LinkImagingStudy` write is a guarded conditional `updateMany` whose WHERE predicate (patient/clinic consistency) is re-evaluated by Postgres at the same statement as the write, fail-closed under concurrent `/link`-vs-`/unlink` activity; F2-CT-23-R1 (same PR) broadened the 409 message and rewrote the read-only historical-mismatch detector to bounded id-ascending cursor+`take` pagination, default batch size 500 (`DETECTOR_BATCH_SIZE`-overridable); independently re-counted `imagingStudyRequestPatientConsistency.test.ts` 12/12 and `detectImagingStudyRequestPatientMismatch.test.ts` 7/7; no `server/prisma/schema.prisma`/migration file touched; production historical-mismatch detector **not run**, not claimed; `CT-23`/PR #333 changed runtime backend code (`server/src/routes/imaging.ts`) and is therefore `NOT_DEPLOYED`/`NOT_PRODUCTION_VERIFIED`, not `N/A` (corrected by F2-DOC-002-R1, same PR, before merge — see below). Rollback: revert the merge commit; no DB rollback applicable. Tenant/KVKK impact: fail-closed single-tenant/cross-tenant data-integrity hardening, no new disclosure. **Stage 2 Imaging entry gate decision, per this file's own §15 expand-migrate-contract sequence — the §15 Stage-2 row lists `ImagingRequest` PATCH/cancel concurrency hardening (`CR-03`/`BLK-02`/`FP-06`/`CT-32`) as work contained inside Stage 2's own accepted scope, a Stage-2 exit condition, not an entry precondition; `CR-03`/`CT-32` is NOT implemented on current `main`, worked separately by the parallel `F2-CT-32-R1` task; Stage 2's own entry preconditions (Stage 1 merged since 2026-08-03; `CT-23` remediated/merged/main-CI-green) are satisfied: `STAGE_2_ENTRY_GATE = SATISFIED` / `AUTHORIZED_TO_BEGIN_STAGE_2_IMPLEMENTATION = TRUE`**, scoped strictly to the already-accepted Stage 2 scope (`OVL-01` duplicate manual/bridge ingest convergence + `ImagingRequest` PATCH/cancel concurrency hardening `CR-03`/`BLK-02`/`FP-06`/`CT-32`) — explicitly does **not** authorize Stage 3, does **not** authorize CI-blocking/production architecture-guardrail enforcement (`BLOCKING_ENFORCEMENT = NOT_AUTHORIZED`, unchanged, still gated on F2-SEC-001/F2-SEC-002 production verification plus a program-owner promotion-criteria decision, neither performed here), and does **not** authorize any microservices/Kafka/Kubernetes/database-per-tenant architecture change (none proposed, prohibited by this phase's own "Prohibited work" list). **Stage 2 itself remains `IN_PROGRESS` / `NOT_COMPLETE` / `EXIT_GATE_NOT_SATISFIED`** — this entry authorizes Stage 2 entry only and does not itself begin or complete Stage 2 implementation. `F2-CT-32-R1`/`F2-ORG-DASH-METRICS-CONTRACT`, if running in parallel, not inspected, not modified, completion not claimed. No new evidence file authored — following the `F2-DOC-001` precedent of pure program-control reconciliation with no runtime/evidence-conclusion change; no old VAL-004 or CT-23 evidence conclusion rewritten. Files changed: this file; `CURRENT_PHASE.md`; `NORAMEDI_MASTER_TRACKER.md`; `evidence/README.md`. Validation: `git diff --check` clean; no JSON authored this task. **Exact next task:** program-owner review/merge of this reconciliation PR; Stage 2 implementation (`OVL-01` convergence + `ImagingRequest` concurrency hardening) as a distinct, separately-scoped future task, not started by this entry. **F2-DOC-002 task status: `AGENT_COMPLETED` / `DOC_VALIDATION_PASSED` / `PR_OPENED` / `PR_CI_PASSED` (run `31211249908`, `completed`/`success`, current reviewed head `ae97d71351cfb64af3d665445f01a8eb65dca1f8` — supersedes the earlier run `31208957602` on the since-superseded head `5499262ce69bfdab275878c941059d6b245f4fc3`) — `NOT_MERGED` (this PR itself, per instruction not to merge) / `DEPLOYED = N/A` / `PRODUCTION_VERIFIED = N/A` (this task itself is documentation-only; `CT-23`/PR #333's own status is corrected above, `NOT_DEPLOYED`/`NOT_PRODUCTION_VERIFIED`, not `N/A`).** (**F2-DOC-002-R1, same PR, before merge:** an independent architecture review found this row's original text incorrectly stated `CT-23` as `DEPLOYED = N/A`/`PRODUCTION_VERIFIED = N/A` and overstated Stage 2 as fully `READY`/`AUTHORIZED_FOR_STAGE_2_IMPLEMENTATION` implying `CR-03` was already implemented; both corrected in place above, same PR, before merge.) |
| 2026-08-09 | F2-STAGE3-IMPL-001-R1 | Mutation-Outcome Amendment — fixes the counter-semantics divergence the row immediately above locked in via regression test; same PR [#344](https://github.com/MustafaBasol/DisKlinikCRM/pull/344), same branch `feature/f2-stage3-impl-001-privacy-imaging-lifecycle-migration`, one additional commit `39364f117874668c2bde34fe1fbca03de7430245`. **Fix, not merely accepted/deferred:** `ImagingLifecyclePort.redactForAnonymization` (`server/src/services/imaging/public.ts`) now returns `Promise<{ changed: boolean }>` instead of `Promise<void>` (Option B, additive — `ImagingLifecycleImageDto` unchanged, still exactly six fields: `id`/`studyId`/`clinicId`/`patientId`/`legalHold`/`storageKey`); `patientAnonymization.ts`'s `redactPatientImagingImages` now gates `counters.redacted++` on `outcome.changed === true` instead of unconditionally incrementing on every call, restoring exact pre-migration `RedactionCounters` semantics on an idempotent re-run. The mutation's own `updateMany` predicate now also excludes already-redacted rows (`originalName: { not: REDACTED_PLACEHOLDER }`), turning the write into an atomic compare-and-set: a first, real redaction reports `changed: true`; an already-redacted/idempotent re-run reports `changed: false`; when two callers genuinely race the same row, the CAS lets exactly one writer's `updateMany` match, so the benign loser reports `changed: false` (not `true`) and only the winner's own call increments its own `redacted` counter — this was caught by this task's own new concurrency regression test before being fixed, not discovered after the fact. All tenant/legal-hold/TOCTOU predicates are unchanged from `F2-IMPL-001-A-R3`; no schema/migration. **Tests:** `test:privacy-imaging-lifecycle-migration` **18/18** (17/17 + 1 new benign-concurrent-writer `changed: false` regression); `test:imaging-lifecycle-facade` **36/36** (34/34 + 2 new mutation-outcome regressions — first-real/idempotent `changed` outcome, and the benign-concurrent-writer race); server `typecheck` clean; `git diff --check` clean. Full evidence: [F2-STAGE3-IMPL-001-R1_IMAGING_LIFECYCLE_REDACTION_MUTATION_OUTCOME_AMENDMENT.md](../evidence/F2-STAGE3-IMPL-001-R1_IMAGING_LIFECYCLE_REDACTION_MUTATION_OUTCOME_AMENDMENT.md). Files changed: `CURRENT_PHASE.md`, `NORAMEDI_MASTER_TRACKER.md`, this file, `evidence/README.md`, new evidence file, `server/src/services/imaging/public.ts`, `server/src/services/privacy/patientAnonymization.ts`, `server/src/tests/dbVerification/privacyImagingLifecyclePortMigration.test.ts`, `server/src/tests/imagingLifecycleFacade.test.ts`. **Current PR #344 head `39364f117874668c2bde34fe1fbca03de7430245`** (supersedes `560acdd74d0751456681706dfd238c29539cb845`, the row immediately above's head — now stale, left unedited per this document's own convention); exact-head CI independently re-verified via `gh pr checks 344`/`gh run list`: `ci` run `31310907422` `SUCCESS`, `windows-bridge-pr` run `31310907297` `SUCCESS` (the `31307552547` run cited in the row immediately above was against the pre-R1 head, now stale). **Stage 3 as a whole remains NOT complete** — the two deferred gaps (`markConfirmedMissing` `clinicId` redesign; `deletionReviewInventory.ts` port DTO extension) and CI-blocking/production architecture-guardrail enforcement (`NOT_AUTHORIZED`, unchanged) remain open, not started. **Exact next task:** program-owner review/merge decision for PR #344 (not performed by this task). Task status: `AGENT_COMPLETED` / `TESTS_PASSED` / `PR_CI_PASSED` — `NOT_MERGED` / `NOT_DEPLOYED` / `NOT_PRODUCTION_VERIFIED`. |
