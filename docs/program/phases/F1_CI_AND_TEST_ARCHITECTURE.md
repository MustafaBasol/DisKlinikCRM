# F1 — CI and Test Architecture

Faz durumu: `IN_PROGRESS` (2026-07-30, F1-003-P3-R1 — **Main Reconciliation, Backend Compatibility Fail-Safe, and Remote CI Evidence Closure**, continued on the existing open PR #268 branch `feature/f1-003-p3-layered-ci-workflows` — no new PR opened, not merged, not rebased, not force-pushed. Reconciled with `origin/main` (merge commit `7ab630c9a5a54acc7bb757b06c908b772c5f138d`, 0 conflicts); root-caused and fixed a real GitHub Actions-only defect the original P3 run exposed (`non-disposable-backend-tests` never generated the Prisma Client on a from-scratch checkout); closed the backend full-suite compatibility gap with an exact, machine-verified 23-script inventory (corrected from the original P3's "roughly 14" estimate) via a new additive `postgres-compat` orchestrator profile reusing 100% of F1-003-P2's provisioning; fixed a real artifact-generation defect (combined stdout mislabeled as JSON) plus a second defect found during that fix (a write failure could mask the real test exit code). `AGENT_COMPLETED` / `PR_OPENED_AWAITING_REVIEW`, unchanged maximum status — see [F1-003-P3-R1 subsection below](#f1-003-p3-r1--main-reconciliation-backend-compatibility-fail-safe-and-remote-ci-evidence-closure) and its evidence documents. Does not by itself satisfy F1's exit gate; parent F1-003 not fully complete (P1/B1/P2 merged, P3/P3-R1 agent-completed/PR open, P4 not started); R-070 confirmed still `OPEN`; G1/G2 remain `NOT_APPROVED`.)

Prior status: `IN_PROGRESS` (2026-07-30, F1-003-P3 — **Layered CI Workflows and Full-Suite Fail-Safe** implemented as one reusable GitHub Actions workflow (`ci-layers.yml`, 8 jobs / 5 layers) plus two thin triggers (`ci-pr.yml`, `ci-main-and-nightly.yml`), locally validated (`AGENT_COMPLETED`, PR opened, not merged) — see [F1-003-P3 subsection below](#f1-003-p3--layered-ci-workflows-and-full-suite-fail-safe) and its evidence documents. F1-003-P2 (merged as PR #260, the direct dependency this task was blocked on) is unaffected and unmodified. Does not by itself satisfy F1's exit gate; parent F1-003 not fully complete (P1/B1/P2 merged or agent-completed as recorded below, P3 agent-completed/PR open, P4 not started); R-070 confirmed still `OPEN`; G1/G2 remain `NOT_APPROVED`.)

Prior status: `IN_PROGRESS` (2026-07-29, F1-003-P2 — **Disposable PostgreSQL and MinIO Provisioning and Collision Avoidance** implemented and locally runtime-verified against a real Docker Desktop engine (`AGENT_COMPLETED`, PR opened, not merged) — see [F1-003-P2 subsection below](#f1-003-p2--disposable-postgresql-and-minio-provisioning-and-collision-avoidance) and its evidence documents. Does not by itself satisfy F1's exit gate; parent F1-003 not fully complete (P1/B1 merged, P2 agent-completed/PR open, P3/P4 not started); R-070 confirmed still `OPEN`; G1/G2 remain `NOT_APPROVED`.)

Prior status: `IN_PROGRESS` (2026-07-29, F1-003-R1B — **Post-B1 Main Reconciliation and Status Refresh** (documentation-only, on open PR #256, not merged): reconciles PR #256 with `origin/main` after **F1-003-B1** merged (PR #258, merge commit `bb4186793323485c71b91ad6939a0d8469f886cd`, mergedAt `2026-07-28T22:13:04Z`) — `server:test:non-disposable` is now confirmed **exit `0`** (68/68 members, `test:overdue-installments` 9/9), superseding F1-003-P1's original exit-`1`/7-of-9 result, which is preserved below as historical evidence, not restated as current. **F1-003-P1 is `MERGED`** (PR #257). F1-003-R1A's MinIO correction (`test:kvkk-lifecycle` PostgreSQL-required/MinIO-free; `test:file-backup-db-integration` the sole genuine MinIO target) is retained unchanged. F1-003-B1 is removed from parent F1-003-P3's blocker list; P3 remains blocked on F1-003-P2's not-yet-started disposable-Postgres/MinIO provisioning and collision avoidance. Full detail: [F1-003-P1 subsection below](#f1-003-p1--test-script-closure-and-authoritative-execution-contract), [F1-003-B1 subsection below](#f1-003-b1--overdue-installments-baseline-drift-resolution), and [evidence/F1-003-P1_TEST_SCRIPT_CLOSURE_AND_EXECUTION_CONTRACT.md](../evidence/F1-003-P1_TEST_SCRIPT_CLOSURE_AND_EXECUTION_CONTRACT.md) §18. Does not by itself satisfy F1's exit gate; parent F1-003 not fully complete (P1 and B1 merged; P2/P3/P4 not started); R-070 confirmed still `OPEN`; G1/G2 remain `NOT_APPROVED`.)

Prior update: 2026-07-28 (F1-002-R2 — **External R-072 Closure Recording and Final PR Metadata Correction**: the external architecture review (ChatGPT architecture review) accepted the F1-002/F1-002-P1/F1-002-P2 evidence chain and, jointly with Mustafa Basol's merge decision, granted closure confirmation for R-072. **R-072 → `CLOSED`**, closure date 2026-07-28, closure basis F1-002 inventory/ownership reconciliation + F1-002-P1 script-reachability evidence + F1-002-P2 runtime-dependency evidence + 0 `UNKNOWN` canonical ownership + complete package-script reachability classification + current-`main` reconciliation completed; external confirmation authority ChatGPT architecture review + Mustafa Basol. This closure is narrow and does **not** resolve: 8 unscripted backend test files; 17 aggregate-chain-excluded scripts; only 7/116 CI-reached test files; disposable-Postgres provisioning; MinIO provisioning; collision avoidance; the 1 runtime `UNKNOWN` (`messagesConsentGate.test.ts`); the unimplemented CI selector; or R-070 — see `RISK_REGISTER.md`. **F1-003 — Baseline CI Test Execution and Disposable Runtime Readiness is assigned below, status `READY, not started` — this pass assigns it, it does not execute it.** PR #253's own description is corrected (17, not 16, chain-excluded scripts; states P1/P2 merged and integrated; states R-072 `CLOSED` by external confirmation; does not claim F1 completion or CI/runtime readiness). This closure-recording pass does not implement CI or the test-selection engine and does not by itself satisfy F1's own exit gate below.)

Prior update: 2026-07-28 (F1-002-R1 — **Parallel Evidence Integration and Main Reconciliation**: PR #253 (F1-002 main) reconciled with `origin/main` (merge commit `035d4db59cf429777a4bb71a5e72203f6c99b571`, which itself contains PR #255/F1-002-P1 merge commit `954168ed4399f572e776fbfd30fa391aad574610` and PR #254/F1-002-P2 merge commit `035d4db59cf429777a4bb71a5e72203f6c99b571`, both confirmed `MERGED`). F1-002-P1 (test script reachability: 103 test-related scripts, 116 current test-related files [105 backend + 9 frontend + 2 fixture-loader helper modules — a narrower JS/TS-only scope than F1-002 main's 137 test/verification targets, see [TEST_OWNERSHIP.md](../TEST_OWNERSHIP.md) §0.1 for the reconciliation] , 17 scripts excluded from the `server:test` aggregate chain, 8 UNSCRIPTED backend files, 0 stale script references, only 7/116 files reached by any CI workflow) and F1-002-P2 (test runtime dependency baseline: 22 files require a disposable Postgres, 1 additionally requires a MinIO/S3-compatible object-storage emulator with zero committed provisioning of either, migration-rollback tooling gap tracked as R-070, zero confirmed live external-provider/network calls, 1 explicit UNKNOWN — `messagesConsentGate.test.ts`) are now indexed into the authoritative F1-002 documentation set. **This integration task does not implement CI or the test-selection engine, does not satisfy F1's own exit gate below, and does not authorize F1-003.** Full process/validation record: [evidence/F1-002_TEST_INVENTORY_REFRESH_AND_OWNERSHIP_RECONCILIATION.md](../evidence/F1-002_TEST_INVENTORY_REFRESH_AND_OWNERSHIP_RECONCILIATION.md).)

Prior update: 2026-07-28 (F1-002 — **Test Inventory Refresh and Ownership Reconciliation delivered (`AGENT_COMPLETED`, PR #253 opened, not merged)**: [F1-002_TEST_INVENTORY_REFRESH_AND_OWNERSHIP_RECONCILIATION.md](../evidence/F1-002_TEST_INVENTORY_REFRESH_AND_OWNERSHIP_RECONCILIATION.md) refreshes the test inventory backing F1-001's design against execution baseline `08f2eaf82a205cf3f997c57e6a295fedd66b142d` (105 backend + 9 frontend + 9 bridge-agent + 8 windows-bridge + 4 manual verify + 2 smoke = **137 test/verification targets**, up from F0-005's 100), remediating R-072 (test-inventory evidence staleness, discovered by F1-001). Every target has an explicit canonical owner (0 UNKNOWN), package scripts fully reconciled (0 stale references, 8 never-scripted backend files identified and recorded), and R-072 closure is **proposed** pending external review. Superseded in part by F1-002-R1 above (count/script/runtime vocabulary reconciled against the two parallel evidence subtasks).)

Prior update: 2026-07-28 (F1-001 — **F1-001 design delivered, `MERGED`** via PR #241, merge commit `94cc4ac58f0487dd186886878c5628627f0b1ce3` (2026-07-28T07:22:15Z): [F1-001_IMPACT_BASED_TEST_SELECTION_ARCHITECTURE.md](../architecture/F1-001_IMPACT_BASED_TEST_SELECTION_ARCHITECTURE.md) resolves this document's own "Open question" below (path-vs-graph-based selection mechanism) by deliberately deferring the static-snapshot-vs-live-graph-query choice to the future implementation task, while fully specifying the classification taxonomy (24 categories), risk model (LOW/MEDIUM/HIGH/CRITICAL with measurable properties), 7 test-scope levels (S0-S6), 12 mandatory escalation rules, fail-safe/confidence model, shadow-mode rollout (4 phases, 8 metrics, zero-false-negative requirement for CRITICAL changes at every phase), and 17 repository-grounded example scenarios — see [evidence/f1-001-test-scope-classification.json](../architecture/evidence/f1-001-test-scope-classification.json) and [evidence/f1-001-impact-selection-rules.json](../architecture/evidence/f1-001-impact-selection-rules.json). **This task's own completion does not by itself satisfy F1's exit gate below, and does not authorize F1-002.** It discovered one new risk (evidence staleness in F0-005's test inventory relative to current HEAD) recorded as R-072 in `RISK_REGISTER.md`. Full process/validation record: [evidence/F1-001_IMPACT_TEST_SELECTION_DESIGN_EVIDENCE.md](../evidence/F1-001_IMPACT_TEST_SELECTION_DESIGN_EVIDENCE.md).)

Prior update: 2026-07-25 (F0-014 — phase entered following G0 `APPROVED_WITH_CONDITIONS`; phase entry authorizes only the assigned first task below, not the full initial backlog) · Son güncelleme: 2026-07-25 (**F0-014 — G0 Approval Recording, F0 Closure, and F1 Initial Task Assignment.** G0 ("F0 Architecture Validation Complete") was externally approved `APPROVED_WITH_CONDITIONS` (ChatGPT architecture review + Mustafa Basol decision, 2026-07-25) — see [../RELEASE_GATES.md](../RELEASE_GATES.md). This phase's own entry condition ("G0 onayı (F0 çıkışı)") is now satisfied; phase status moves `TODO`→`IN_PROGRESS`. Per this phase document's own "Initial task backlog," detailed task IDs are assigned only after F0 evidence is reviewed (post-G0) — that review is now complete (F0-013), so the first concrete task is assigned below: **F1-001 — Impact-Based Test-Selection Architecture and Test-Scope Classification**, matching this backlog's first-listed category, "Etki-bazlı test seçim mekanizması tasarımı ve kurulumu." F1-001 is design/evidence-first (status `READY`) — **this task assigns F1-001, it does not execute it.** None of G0's ten preserved conditions (KVKK freeze, R-046/R-071, G1/G2, `NEEDS_POC` ADRs, no RLS/queue/object-storage/backfill/Kafka/Kubernetes/microservices/database-per-tenant/rewrite, modular-monolith/public-contract boundaries) is loosened by this phase transition. Roadmap order below (initial task backlog, dependencies, prohibited work) is otherwise unchanged.)

Prior update: 2026-07-17 (F0-001)

## Objective (Hedef)

Etki-bazlı (affected) test seçimi, katmanlı CI modeli (PR / main / nightly / release) ve güvenilir test altyapısını kurmak; hedef mimari [../TEST_OWNERSHIP.md](../TEST_OWNERSHIP.md) dokümanında tanımlıdır.

## Business reason (İş gerekçesi)

Modüler geliştirme hızının ön koşulu, hızlı ve güvenilir geri bildirim döngüsüdür. Tam suite'e bağımlı yavaş CI; küçük PR politikasını, sık merge'i ve güvenli mimari değişimi engeller.

## Entry conditions (Giriş koşulları) — `SATISFIED` (2026-07-25, F0-014)

- G0 onayı (F0 çıkışı) — **`SATISFIED`**: `APPROVED_WITH_CONDITIONS`, 2026-07-25 ([../RELEASE_GATES.md](../RELEASE_GATES.md)). Conditional — see that gate's full preserved-conditions list; this phase's own KVKK/tenant/ADR-readiness prohibitions below are unaffected.
- F0-005 test envanteri tamamlanmış — **`SATISFIED`**: `MERGED` ([../NORAMEDI_MASTER_TRACKER.md](../NORAMEDI_MASTER_TRACKER.md) §6/§7).

## Exit gate (Çıkış kapısı)

- Etki-bazlı CI modeli **kanıtla** çalışıyor (PR'da yalnızca etkilenen kapsam + zorunlu core testleri)
- Nightly tam regresyon kurulu ve istikrarlı
- Flaky test envanteri kapatılmış veya karantinada

## Dependencies (Bağımlılıklar)

- F0 (özellikle F0-005)

## Allowed work (İzinli işler)

- CI workflow tasarımı ve kurulumu (bu fazda CI dosyaları değiştirilebilir — F0'daki yasak bu fazda kalkar, dış onayla)
- Test altyapı iyileştirmeleri (disposable Postgres, izolasyon, hız)
- Test sahiplik etiketleme ve tetikleyici path haritası

## Prohibited work (Yasak işler)

- Uygulama davranış değişiklikleri (test edilebilirlik için zorunlu, onaylı DI seam'leri hariç)
- KVKK dondurma sınırındaki işler (taban çizgisi teyit edilmediyse)

## Initial task backlog (Yüksek seviyeli kategoriler)

Detailed task IDs are assigned as each category is reached, in the order below, following its own dependency (F0-005 test inventory, this document's own "Open questions"). **F1-001, the first task, is assigned (2026-07-25, F0-014), delivered and `MERGED` (2026-07-28).** **F1-002, the evidence-refresh prerequisite, is assigned, delivered (`AGENT_COMPLETED`), and R-072 externally `CLOSED` (2026-07-28, F1-002-R2)** — see full definitions below. **F1-003, combining backlog items 2 and 3 below, is now assigned (2026-07-28, F1-002-R2), status `READY, not started`** — see full definition below. The remaining categories (4-7) remain high-level roadmap entries; none has a task ID yet, and none is invented by this update.

1. **Etki-bazlı test seçim mekanizması tasarımı ve kurulumu → F1-001 (`MERGED`)**
2. **PR / main / nightly / release CI katmanlarının kurulumu → part of F1-003 (assigned below, `READY, not started`)**
3. **Test veritabanı stratejisi (disposable Postgres standardı) → part of F1-003 (assigned below, `READY, not started`)**
4. Flaky test tespiti ve karantina süreci
5. Migration test katmanı
6. Core güvenlik/tenancy regresyon paketinin zorunlu hale getirilmesi
7. CI süre/maliyet ölçüm panosu

### F1-001 — Impact-Based Test-Selection Architecture and Test-Scope Classification

| Field | Value |
|---|---|
| Task ID | F1-001 |
| Title | Impact-Based Test-Selection Architecture and Test-Scope Classification |
| Phase | F1 — CI and Test Architecture |
| Status | `MERGED` (PR #241, merge commit `94cc4ac58f0487dd186886878c5628627f0b1ce3`, 2026-07-28T07:22:15Z; see [F1-001_IMPACT_BASED_TEST_SELECTION_ARCHITECTURE.md](../architecture/F1-001_IMPACT_BASED_TEST_SELECTION_ARCHITECTURE.md) and [evidence/F1-001_IMPACT_TEST_SELECTION_DESIGN_EVIDENCE.md](../evidence/F1-001_IMPACT_TEST_SELECTION_DESIGN_EVIDENCE.md). Verified `MERGED` by F1-002-R1 via `gh pr view 241`, 2026-07-28.) |
| Purpose | Design (not implement) the impact-based ("affected") test-selection architecture named as F1's own first initial-backlog category and as this phase's own exit-gate requirement ("Etki-bazlı CI modeli **kanıtla** çalışıyor"): given a diff/changed-file set, define the deterministic mechanism that classifies which test files/suites are "affected" and must run on a PR, versus "core" tests that always run, versus tests deferred to nightly/full regression. Must resolve this phase document's own named "Open question": whether the selection mechanism is path-based or dependency-graph-based, using F0-005's test-inventory/ownership data and F0-004's cross-module dependency map as evidence inputs. |
| Dependencies | F0-005 — Test Inventory, Runtime Measurement, and Ownership Map (`MERGED`; provides the 100-target test inventory, ownership map, and F0-004 high-risk-edge test-coverage mapping this design must classify against). F0-004 — Cross-Module Dependency Map (`MERGED`; provides the 37-domain/833-edge dependency matrix a graph-based selection mechanism would need). G0 `APPROVED_WITH_CONDITIONS` (satisfied, 2026-07-25). |
| Allowed scope | Documentation and design only: a written test-selection architecture/design document; a path-to-test-scope classification scheme (or dependency-graph-based scheme, per the open question this task must resolve) built from existing F0-004/F0-005 evidence; a proposed core/always-run test set (starting from F0-005's own named tenant-isolation/cross-tenant/security test list); a proposed CI-layer model (PR / main / nightly / release) at the design level; measurable success criteria for the phase's own exit gate. Read-only analysis of the existing `server`/root `package.json` test scripts and `TEST_OWNERSHIP.md`/`DEPENDENCY_MAP.md` is in scope. |
| Prohibited scope | No CI workflow file (`.github/workflows/**`) may be created, modified, or enabled by this task. No test file may be added, modified, moved, or deleted. No `package.json` script may be added or changed. No application/runtime/schema/migration/dependency/deployment/environment file may be touched. No KVKK/tenant/consent/retention code path may be touched (KVKK physical-architecture freeze remains active, per G0 condition 3-4). No RLS, tenant-key backfill, queue/outbox, object-storage, Kafka, Kubernetes, microservices, database-per-tenant, or framework-rewrite work (G0 condition 8). This task may not itself declare F1's exit gate satisfied — that requires the mechanism to run with evidence, a later task's job. |
| Deliverables | A new design document (e.g. `docs/architecture/f1-001-impact-based-test-selection-design.md`, following this program's established PoC/design-document naming convention) covering: the resolved path-vs-graph-based selection-mechanism decision with rationale; the classification scheme itself (which changed paths map to which test scopes); the proposed core/always-run test set; the proposed PR/main/nightly/release CI-layer model; a measurement/success-criteria definition for the phase exit gate; a rollout plan (design → prototype/dry-run in a disposable branch → CI enablement, with each stage requiring its own separate task authorization). Program-tracker updates recording this task's own completion (`NORAMEDI_MASTER_TRACKER.md`, `CURRENT_PHASE.md`, this phase document). |
| Exact evidence required | File:line or file-path citations from `TEST_OWNERSHIP.md` (F0-005) and `DEPENDENCY_MAP.md` (F0-004) for every classification rule proposed — no invented/assumed test-to-path mapping. If any classification cannot be derived from existing F0-004/F0-005 evidence, it must be recorded as an open question for a follow-up task, not guessed. |
| Validation requirements | Documentation-only: no automated test suite is required to validate a design document. If a documentation-native validator exists in this repository, it must be run and its exact command/result recorded — none is known to exist as of this task's writing; do not invent one. |
| Tenant/security/KVKK impact | None expected — design-only, no runtime/data-path change. The proposed core/always-run test set must explicitly include this repository's existing tenant-isolation/cross-tenant-negative/permission-matrix test files (per `TEST_OWNERSHIP.md`'s "Tenant Security and Scope" and "Identity and Access" domains) as non-negotiable "always run" scope — omitting them from the core set is a defect in this task's own deliverable, not an acceptable design choice. |
| Rollback expectations | Single documentation-commit `git revert`. No database, migration, deployment, or CI rollback applicable — no such file is touched by this task. |
| Completion/status rules | This task may reach at most `AGENT_COMPLETED` (an agent may never self-assign `REVIEW_REQUIRED`/`MERGED`/`DEPLOYED`, per `NORAMEDI_MASTER_TRACKER.md` §2.3). `MERGED` requires external review and an actual merged PR. This design task's own completion does **not** by itself satisfy F1's exit gate ("Etki-bazlı CI modeli **kanıtla** çalışıyor") — that requires a later, separate implementation/dry-run task with runtime evidence. |
| Next-task dependency | F1-001 is now `MERGED` (see Status row). This precondition for the next F1 backlog category (PR / main / nightly / release CI katmanlarının kurulumu, backlog item 2) is satisfied. It is not yet unconditionally actionable — F1-002-R1's runtime-dependency findings (disposable Postgres/MinIO provisioning gaps, unscripted/chain-excluded tests) mean backlog item 3 ("Test veritabanı stratejisi") is a co-requisite, not a later, independent step. See F1-002's own "Next-task dependency" row below and `RISK_REGISTER.md` R-072 for the exact scope this implies. Its exact task ID is still assigned by that future task's own authorization, not invented here. |

### F1-002 — Test Inventory Refresh and Ownership Reconciliation

| Field | Value |
|---|---|
| Task ID | F1-002 |
| Title | Test Inventory Refresh and Ownership Reconciliation |
| Phase | F1 — CI and Test Architecture |
| Status | `AGENT_COMPLETED` (2026-07-28 — refreshed inventory/ownership/script-reconciliation delivered; updated 2026-07-28 by F1-002-R1 to integrate F1-002-P1/P2 parallel evidence and reconcile against `origin/main`; updated 2026-07-28 by F1-002-R2 to record R-072's external closure confirmation and correct PR #253's description; PR #253 open, not merged; see [F1-002_TEST_INVENTORY_REFRESH_AND_OWNERSHIP_RECONCILIATION.md](../evidence/F1-002_TEST_INVENTORY_REFRESH_AND_OWNERSHIP_RECONCILIATION.md).) |
| Purpose | Remediate R-072 (F1-001's own discovered risk: `TEST_OWNERSHIP.md`/F0-005 test-inventory evidence pinned to 2026-07-19, stale relative to current `HEAD`). Enumerate every current backend/frontend test file, reconcile ownership/domain/test-type/runtime-requirements, reconcile every test-related package script, and identify script/inventory gaps — a documentation/evidence prerequisite for any future CI implementation of F1-001's design, not an implementation of that design itself. |
| Dependencies | F1-001 (`MERGED`, PR #241, `94cc4ac58f0487dd186886878c5628627f0b1ce3`; provides the classification taxonomy/domain vocabulary this task reuses). F1-002-P1 and F1-002-P2 (both `MERGED` — PR #255/`954168ed4399f572e776fbfd30fa391aad574610` and PR #254/`035d4db59cf429777a4bb71a5e72203f6c99b571` — parallel evidence subtasks integrated by F1-002-R1). |
| Allowed scope | Documentation/evidence only: `TEST_OWNERSHIP.md` refresh, new `evidence/F1-002_test_inventory.json`/`F1-002_test_script_reconciliation.json`/`F1-002_test_ownership_gaps.json`, tracker updates, `RISK_REGISTER.md` R-072 status update. |
| Prohibited scope | No test file, package script, CI workflow, application/runtime/schema/migration/deployment/environment file may be touched. No KVKK/tenant/consent/retention code path may be touched. No R-072 closure without evidence proving every exit condition is met; no self-declared `MERGED`. |
| Deliverables | [TEST_OWNERSHIP.md](../TEST_OWNERSHIP.md) (refreshed §3–§9), [evidence/F1-002_test_inventory.json](../evidence/F1-002_test_inventory.json) (137 targets, supersedes F0-005's 100 as current authoritative), [evidence/F1-002_test_script_reconciliation.json](../evidence/F1-002_test_script_reconciliation.json), [evidence/F1-002_test_ownership_gaps.json](../evidence/F1-002_test_ownership_gaps.json), [evidence/F1-002_TEST_INVENTORY_REFRESH_AND_OWNERSHIP_RECONCILIATION.md](../evidence/F1-002_TEST_INVENTORY_REFRESH_AND_OWNERSHIP_RECONCILIATION.md), tracker updates. |
| Exact evidence required | Exact `find`/PowerShell-equivalent enumeration commands and their output counts; programmatic (not manual) package-script parsing; per-file ownership citation (header docblock, import, or carried-forward F0-005 record) — no invented/guessed ownership, `UNKNOWN` permitted and explicit where warranted (0 such cases were needed). |
| Validation requirements | `node -e "JSON.parse(...)"` on all three new JSON files; `git diff --check`; prohibited-path grep; status-consistency grep across `docs/program`. No documentation validator script exists in this repository (unchanged finding from every prior F0/F1 task). |
| Tenant/security/KVKK impact | None — no runtime/data-path change. The refreshed inventory preserves explicit coverage visibility for tenant isolation, org/clinic scope, auth/session/CSRF, role authorization, KVKK/privacy/consent, audit, financial, migrations/data integrity, webhooks/messaging, storage, AI, imaging, and official integrations (per `highRiskDimensions` on every relevant entry). |
| Rollback expectations | Single documentation-commit `git revert`. No DB/migration/deployment/CI rollback applicable. |
| Completion/status rules | This task may reach at most `AGENT_COMPLETED`. `MERGED` requires external review and an actual merged PR. **R-072 closure was externally confirmed 2026-07-28 (F1-002-R2) — `CLOSED`**, not unilaterally declared by any agent task — see `RISK_REGISTER.md`. |
| Next-task dependency | **Resolved 2026-07-28 (F1-002-R2): F1-003 — Baseline CI Test Execution and Disposable Runtime Readiness is now assigned**, combining backlog items 2 ("PR / main / nightly / release CI katmanlarının kurulumu") and 3 ("Test veritabanı stratejisi (disposable Postgres standardı)") together, not item 2 alone — F1-002-P2's findings show CI-layer setup cannot be usefully sequenced ahead of a committed disposable-Postgres/MinIO provisioning mechanism, since 22+1 of 137 current targets cannot run in any CI today without one. See the F1-003 definition below for its exact scope. |

### F1-003 — Baseline CI Test Execution and Disposable Runtime Readiness

| Field | Value |
|---|---|
| Task ID | F1-003 |
| Title | Baseline CI Test Execution and Disposable Runtime Readiness |
| Phase | F1 — CI and Test Architecture |
| Status | `IN_PROGRESS` (updated 2026-07-30 — F1-003-P1 `MERGED` PR #257, F1-003-B1 `MERGED` PR #258, F1-003-P2 `MERGED` PR #260, F1-003-P3/P3-R1 `AGENT_COMPLETED` / PR #268 opened, not merged; see subsections below). Originally assigned `READY, not started` 2026-07-28, F1-002-R2. **Parent task remains not fully complete — P1, B1, and P2 are merged; P3/P3-R1 are agent-completed with a PR open and awaiting review; P4 not started.** |
| Purpose | Combines backlog items 2 ("PR / main / nightly / release CI katmanlarının kurulumu") and 3 ("Test veritabanı stratejisi (disposable Postgres standardı)") into one task, per F1-002/F1-002-R1's own "Next-task dependency" finding that CI-layer setup cannot be usefully sequenced ahead of a committed disposable-runtime provisioning mechanism. Must close the gaps R-072's closure explicitly left open: the 8 unscripted backend test files and the 17 scripts excluded from the `server:test` aggregate chain; authoritative CI command coverage (only 7 of 116 JS/TS test files reached by any workflow as of F1-002-P1's evidence); a disposable-Postgres provisioning design/implementation (no committed script/Compose file exists — F1-002-P2 §8); the MinIO/S3-emulator test dependency (`fileBackupDbIntegration.test.ts`, zero provisioning exists); safe parallel-worktree/CI-run collision avoidance (no port/database-name isolation mechanism exists today); resolution or explicit tracking of the 1 remaining runtime `UNKNOWN` (`messagesConsentGate.test.ts`); and a default full-suite fail-safe for anything not yet classified. R-070 (migration-rollback tooling gap) is a related, not identical, dependency this task should reference, not silently absorb. |
| Dependencies | F1-001 (`MERGED`, PR #241) — classification taxonomy. F1-002/F1-002-P1/F1-002-P2 (all `AGENT_COMPLETED`/`MERGED`, PR #253/#255/#254) — current test inventory, script reachability, and runtime dependency evidence. R-072 `CLOSED` (2026-07-28, F1-002-R2) — the evidence-currency prerequisite this task's own scope depends on. |
| Allowed scope | Not yet started — full scope (CI workflow design/creation, disposable-Postgres/MinIO provisioning mechanism, collision-avoidance design) to be defined at execution time, within F1's own "Allowed work" above (CI workflow files may be modified in this phase, with external approval, per F0's KVKK-freeze exception). |
| Prohibited scope | Same as this phase's own "Prohibited work" above: no application behavior change outside approved testability DI seams; no work inside the still-active KVKK physical-architecture freeze boundary. No self-declared `MERGED`/`DEPLOYED` (an agent may reach at most `AGENT_COMPLETED`, per `NORAMEDI_MASTER_TRACKER.md` §2.3). |
| Deliverables | See F1-003-P1 subsection below for the first ordered subtask's actual deliverables; the remaining scope (disposable-Postgres/MinIO provisioning, collision avoidance, CI workflow activation) is not yet produced. |
| Completion/status rules | This task may reach at most `AGENT_COMPLETED` when executed. `MERGED` requires external review and an actual merged PR. This task's own eventual completion is what F1's exit gate below requires evidence of — F1-003-P1 alone does not satisfy it. |
| Next-task dependency | F1-003-P1 `MERGED` (PR #257); F1-003-B1 `MERGED` (PR #258, removed the sole `server:test:non-disposable` failure); F1-003-P2 `MERGED` (PR #260, mergedAt 2026-07-30T09:45:38Z — disposable Postgres/MinIO provisioning, collision avoidance); F1-003-P3 `AGENT_COMPLETED`, PR opened, not yet merged (layered CI workflows + full-suite fail-safe — implemented and locally validated, see subsection below). F1-003-P4 is blocked until F1-003-P3's PR is externally reviewed and merged, with real GitHub Actions evidence reviewed. |

#### F1-003-P1 — Test Script Closure and Authoritative Execution Contract

| Field | Value |
|---|---|
| Task ID | F1-003-P1 |
| Title | Test Script Closure and Authoritative Execution Contract |
| Parent task | F1-003 (first ordered subtask) |
| Status | `MERGED` (PR #257) — originally `AGENT_COMPLETED` (2026-07-28); externally reviewed and merged. Isolated worktree `E:\Ek Gelir\Siteler\DisKlinikCRM-worktrees\f1-003-p1-test-script-closure`, branch `feature/f1-003-p1-test-script-closure`, execution baseline `origin/main` @ `e3a29a3bdf4e862c17a18bbad75c3a4b0b36d80a` (identical to the task's own expected baseline; `git merge-base --is-ancestor` exit `0`; zero intervening commits; no reconciliation required). **`server:test:non-disposable`'s originally-reported exit `1` (below) is historical — F1-003-B1 (PR #258, `MERGED`) subsequently brought it to exit `0`, 9/9; see the F1-003-B1 subsection below.** |
| Purpose | Close the items R-072's external closure (F1-002-R2) explicitly left open, except disposable-Postgres/MinIO provisioning and collision avoidance themselves (F1-003-P2's job): the 8 unscripted backend test files; the 17 aggregate-chain-excluded scripts; the 1 runtime `UNKNOWN` (`messagesConsentGate.test.ts`); an authoritative machine-readable execution contract covering all current canonical test targets; a forward-looking PR/main/nightly/release CI command matrix (contract only, no workflow implementation); exact inputs/acceptance criteria for F1-003-P2. |
| Dependencies | F1-002-R2 (`AGENT_COMPLETED`, R-072 `CLOSED`) — F1-002/F1-002-P1/F1-002-P2 evidence used as the authoritative starting point, extended not replaced (114 canonical script records, up from F1-002-P1's 103). |
| Allowed scope (honored) | `server/package.json` additive script changes only (8 new leaf scripts for the previously-unscripted files + 3 new aggregate scripts — `server:test:non-disposable`/`server:test:disposable-db`/`server:test:storage-integration`); legacy `server:test` left completely unmodified; two new evidence files; tracker/phase/ownership/risk-register/evidence-index updates where justified. |
| Prohibited scope (honored) | No `.github/workflows/**` file created or enabled. No PostgreSQL or MinIO provisioned (no Docker, no Compose file, no port/database/bucket allocation logic). No impact-based selector implemented. No application/runtime/schema/migration/deployment/environment file changed. No KVKK/consent/retention/tenant/authentication/storage-production/messaging-provider/AI-provider code path touched — KVKK physical-architecture freeze untouched. No test weakened/skipped/no-op'd to make an aggregate pass — the known `test:overdue-installments` drift is reported honestly (`executed_fail`, 2/9 assertions), not hidden. No exit-code-swallowing construct (`\|\| true`, etc.) used in any new script. |
| Deliverables | [evidence/F1-003-P1_test_execution_contract.json](../evidence/F1-003-P1_test_execution_contract.json) (114 canonical-script records: 103 pre-existing from F1-002-P1 + 8 new leaf + 3 new aggregate, each with target path/canonical owner/canonical script/aggregate profile/runtime class/Postgres-required/MinIO-required/external-network-required/parallel-safety class/cleanup expectation/CI eligibility/current execution status/rationale/evidence source; includes `f1003p2Handoff` with 7 falsifiable acceptance criteria and a forward-looking PR/main/nightly/release CI command matrix). [evidence/F1-003-P1_TEST_SCRIPT_CLOSURE_AND_EXECUTION_CONTRACT.md](../evidence/F1-003-P1_TEST_SCRIPT_CLOSURE_AND_EXECUTION_CONTRACT.md) (17-section narrative, exact commands/counts, explicit "what this task did not do" section). `server/package.json`: 8 new leaf scripts (`test:channel-consent-gate`, `test:clinic-legal-profile`, `test:patient-shared-phone`, `test:platform-backup`, `test:treatment-package-permissions`, `test:kvkk-high006-batch2`, `test:meta-whatsapp-post-booking`, `test:plan-limits-target-clinic-fix`) + 3 new aggregates (`server:test:non-disposable`, `server:test:disposable-db`, `server:test:storage-integration`). |
| Exact evidence required | Every classification cites either F1-002-P1/F1-002-P2/F1-002 evidence by file/field, or this task's own direct `Grep`/`Read`/execution verification, cited as "F1-003-P1 direct verification". Two corrections to prior evidence found and documented: `test:platform-admin-password-recovery` (F1-002-P1 MD-prose-only miscategorization, its own JSON was already correct) and `platformBackup.test.ts` (F1-002 main + F1-002-P1 file-reachability JSON both incorrectly carried forward `databaseRequired:true` from stale F0-005 evidence; corrected via direct file read + direct execution: `npm run test:platform-backup`, no `DATABASE_URL`, exit `0`, "24 passed, 0 failed"). |
| Validation requirements | `node -e "JSON.parse(...)"` on the new JSON contract; `node -e "require('./server/package.json')"`; programmatic verification that every `npm run <name>` reference and every `tsx <file>` reference in `server/package.json` resolves (0 missing in both cases); real command execution of all 7 new non-DB leaf scripts individually (all pass) + 5 spot-checked reclassified scripts individually (all pass) + the new `server:test:non-disposable` aggregate (executed, exit `1`, sole failure the known `test:overdue-installments` drift, 0 new/unexpected failures among the other 67 members — **historical result, superseded by F1-003-B1's merged fix: exit `0`, 9/9**); `git diff --check` clean. |
| Tenant/security/KVKK impact | None — script/doc/JSON changes only; KVKK physical-architecture freeze untouched; `messagesConsentGate.test.ts`'s provider-mock finding directly confirms zero live-provider access is possible from any test this task wired or ran. |
| Rollback expectations | Single `git revert` of this task's one commit — reverses all 11 new `server/package.json` scripts (legacy `server:test` was never modified) and all documentation/evidence changes in the same commit. No database/storage/deployment rollback applicable. |
| Completion/status rules | This task reached `AGENT_COMPLETED`, then externally reviewed and `MERGED` (PR #257). This task's own completion does not by itself satisfy F1's exit gate, does not mark parent F1-003 complete, and does not close R-046/R-070/R-071. |
| Next-task dependency | **F1-003-P2 — Disposable Postgres and MinIO Provisioning and Collision Avoidance** — not started; its merge-dependency on this task is satisfied (this task is now `MERGED`). Exact inputs and 7 falsifiable acceptance criteria: [evidence/F1-003-P1_TEST_SCRIPT_CLOSURE_AND_EXECUTION_CONTRACT.md](../evidence/F1-003-P1_TEST_SCRIPT_CLOSURE_AND_EXECUTION_CONTRACT.md) §13. F1-003-P3 and F1-003-P4 remain not started; F1-003-B1 (`MERGED`, PR #258) already removed the sole `server:test:non-disposable` failure independently of P2/P3/P4. |

#### F1-003-B1 — Overdue Installments Baseline Drift Resolution

| Field | Value |
|---|---|
| Task ID | F1-003-B1 |
| Title | Overdue Installments Baseline Drift Resolution |
| Parent task | F1-003. Explicitly authorized to run in parallel with F1-003-R1 (PR #256, disposable-runtime evidence) — did not touch PR #256's branch, F1-003-P2A evidence files, or any R1/P2A-owned worktree. |
| Status | `MERGED` (PR #258, merge commit `bb4186793323485c71b91ad6939a0d8469f886cd`, mergedAt `2026-07-28T22:13:04Z`) — originally `AGENT_COMPLETED` (2026-07-29); externally reviewed and merged; confirmed an ancestor of `origin/main` via `git merge-base --is-ancestor`. Isolated worktree `E:\Ek Gelir\Siteler\DisKlinikCRM-worktrees\f1-003-b1-overdue-installments`, branch `fix/f1-003-b1-overdue-installments-drift`, execution baseline `origin/main` @ `1e983fe7134f9224cdaeb0065d32b1e8ef2d0904` (the F1-003-P1 merge commit, PR #257 — identical to the task's own expected baseline; `git merge-base --is-ancestor` exit `0`; zero intervening commits). |
| Purpose | Resolve the sole known cause of `npm run server:test:non-disposable` returning exit code 1: the deterministic 2/9-assertion drift in `server/src/tests/overdueInstallments.test.ts` first documented by F1-003-P1. |
| Root-cause classification | **Case A — stale test.** Commit `81323a88` (2026-07-09, `fix(finance): include persisted overdue installments in totals`) deliberately extended `server/src/utils/overdueInstallments.ts` to treat the legacy/persisted literal `'overdue'` status as overdue (alongside `'pending'` + past due date), and added `paymentId`-linked exclusion — corroborated by `server/prisma/schema.prisma:1041`'s own `// pending, paid, overdue` comment, `src/pages/PaymentPlans.tsx`, `financeDashboard.ts`/`dashboard.ts` consumers, and the sibling `overdueReceivables.test.ts` (already green with this exact contract). That commit updated `overdueReceivables.test.ts` but never touched `overdueInstallments.test.ts`, leaving its 2 assertions and docstring asserting the pre-`81323a88` (pending-only) contract. Full analysis: [evidence/F1-003-B1_OVERDUE_INSTALLMENTS_BASELINE_DRIFT_RESOLUTION.md](../evidence/F1-003-B1_OVERDUE_INSTALLMENTS_BASELINE_DRIFT_RESOLUTION.md). |
| Fix (allowed scope, honored) | `server/src/tests/overdueInstallments.test.ts` only — corrected the 2 failing assertions to the current, already-deployed contract (`assert.deepEqual(where.status, { in: ['pending','overdue'] })`; `isInstallmentOverdue(...,'overdue',...) === true` for a past-due, unpaid legacy row) and the stale docstring text. No production/application/schema/migration/CI/Docker/Compose file changed. No assertion weakened or removed — still exactly 9 assertions, now 9/9 passing. |
| Test results | `test:overdue-installments`: 9/9 passed (was 7/9). `test:overdue-receivables`: 12/12 passed, unaffected. `test:finance`: 25/25 passed, unaffected. `test:dashboard`: 38/38 passed, unaffected. `npm run typecheck`: clean. `server:test:non-disposable` (68 members): **exit 0** (was exit 1) — two runs, byte-identical output, no failure marker in ~4,089-line combined log. |
| Tenant/security/KVKK impact | None — no Prisma query changed (`overdueInstallmentWhere`'s `plan: { clinicId: ... }` nesting untouched, confirmed by 2 unaffected clinic-scope assertions in the same test file); no route/auth/consent/retention code touched; KVKK physical-architecture freeze untouched. |
| Rollback | Single `git revert` of this task's one commit; no schema/data/deployment rollback applicable. |
| Completion/status rules | This task reached `AGENT_COMPLETED`, then externally reviewed and `MERGED` (PR #258). Does not mark parent F1-003 complete, does not satisfy F1's exit gate, does not modify F1-003-P2/R1/P2A status, does not close R-046/R-070/R-071, does not declare G1/G2 approved or the KVKK baseline stable. |
| Relationship to F1-003-P3 | **Resolved (F1-003-R1B, 2026-07-29): F1-003-B1 is removed from P3's blocker list** — this task's merge removed the one known non-disposable-aggregate failure. F1-003-P3/P4 remain not started; P3 remains blocked on F1-003-P2's not-yet-started disposable-Postgres/MinIO provisioning and collision-avoidance dependencies and on broader CI-evidence dependencies, not on the overdue-installments drift. |

#### F1-003-P2 — Disposable PostgreSQL and MinIO Provisioning and Collision Avoidance

| Field | Value |
|---|---|
| Task ID | F1-003-P2 |
| Title | Disposable PostgreSQL and MinIO Provisioning and Collision Avoidance |
| Parent task | F1-003 (next ordered subtask after P1/B1) |
| Status | `MERGED` (PR #260, merge commit `04723f9add600b6c432fb517f0607235944d6c2e`, mergedAt `2026-07-30T09:45:38Z`) — originally `AGENT_COMPLETED` (2026-07-29); externally reviewed and merged; independently confirmed via `gh pr view 260` and `git merge-base --is-ancestor` (exit `0`) by F1-003-P3's own baseline verification. Isolated worktree `E:\Ek Gelir\Siteler\DisKlinikCRM-worktrees\f1-003-p2-disposable-runtime`, branch `feature/f1-003-p2-disposable-runtime`, execution baseline `origin/main` @ `ed568c9db3b0d62a049bc291d2137f5c913a7ac7` (PR #256's own merge commit — `git merge-base --is-ancestor` exit `0`, zero intervening commits). |
| Purpose | Implement the exact implementation contract F1-003-P2A/R1 produced (§K/§L of [F1-003-P2A_DISPOSABLE_RUNTIME_PROVISIONING_DESIGN.md](../evidence/F1-003-P2A_DISPOSABLE_RUNTIME_PROVISIONING_DESIGN.md)): committed PowerShell/Bash entry points, a shared TypeScript orchestrator, PostgreSQL/MinIO one-container-per-run provisioning, dynamic host-port allocation, collision-resistant naming, a fail-closed production-endpoint guard, bounded readiness, forward-only migration execution, real execution of `server:test:disposable-db`/`server:test:storage-integration`, mandatory parallel-collision verification, mandatory failure-injection verification, deterministic cleanup, and a stale-resource sweeper. |
| Dependencies | F1-003-P1 (`MERGED`, PR #257) and F1-003-P2A/R1/R1A/R1B (open PR #256) — the target-script inventory and full implementation contract this task follows verbatim. |
| Allowed scope (honored) | New `scripts/test-runtime/**` tooling (test/dev-only, never imported by application runtime); 6 additive root `package.json` scripts; focused Docker-free unit tests; this evidence document and its JSON companion; tracker/phase/evidence-index documentation updates. |
| Prohibited scope (honored) | No `.github/workflows/**` file created or enabled. No application/route/service/domain file changed. No Prisma schema or migration file created/modified. No `server/package.json` change (byte-for-byte unchanged — the 9+1 disposable-runtime target scripts already existed from F1-003-P1). No KVKK/consent/retention/tenant/authentication code path touched. No production access. No legacy `server:test` provisioning (explicitly deferred, per §9a item 4/§K.5's own two-acceptable-outcomes framing — silence is not acceptable, so the deferral is recorded here explicitly). |
| Deliverables | `scripts/test-runtime/orchestrator.ts` + 11 `lib/` modules; `provision.ps1`/`provision.sh`; `sweep.ps1`/`sweep.sh`; `scripts/test-runtime/tsconfig.json`; `scripts/test-runtime/__tests__/orchestratorUnit.test.ts` (50 assertions); root `package.json` additions (`test:runtime:unit/postgres/storage/parallel/cleanup-stale`, `typecheck:runtime`); [evidence/F1-003-P2_DISPOSABLE_RUNTIME_IMPLEMENTATION_AND_VERIFICATION.md](../evidence/F1-003-P2_DISPOSABLE_RUNTIME_IMPLEMENTATION_AND_VERIFICATION.md) (37-section narrative) + [evidence/F1-003-P2_disposable_runtime_verification.json](../evidence/F1-003-P2_disposable_runtime_verification.json) (machine-readable). |
| Exact evidence required | Every acceptance criterion in the merged design's §L (34 items) is independently, live-verified — not asserted from code inspection alone — against a real Docker Desktop engine started for this task (it was not running at session start). PostgreSQL `postgres:16-alpine` resolved to version `16.14`, digest `sha256:57c72fd2a128e416c7fcc499958864df5301e940bca0a56f58fddf30ffc07777`. MinIO `minio/minio:RELEASE.2025-04-08T15-41-24Z`, digest `sha256:8834ae47a2de3509b83e0e70da9369c24bbbc22de42f2a2eddc530eee88acd1b` (first MinIO use in this program; tag+digest both recorded, `latest` never used). `server:test:disposable-db` (9 members): exit `0`. `server:test:storage-integration` (`test:file-backup-db-integration`, sole genuine MinIO-dependent leaf target): exit `0`, 21/21 assertions. `verify-parallel`: 2×postgres + 2×storage concurrent, run twice, zero collisions both times, zero labeled resources left behind either time **[initial pre-F1-003-P2-R1 observation, 2 runs only — see the F1-003-P2-R2 row below: a 3rd/4th rerun hit a real Windows EBUSY, since found and fixed, then re-verified via 5 consecutive clean runs]**. All 6 mandatory failure-injection scenarios (test/migration/readiness/cleanup-alone/test+cleanup-combined/production-guard) reproduced with genuine, non-fabricated failure signals. Stale sweeper verified dry-run and `--live` against artificial fixtures (removed only the deliberately-stale one). |
| Validation requirements | `npm run test:runtime:unit` (50 passed, 0 failed, no Docker); `npx tsc --noEmit -p scripts/test-runtime/tsconfig.json` (0 errors); `npm run typecheck` (server, 0 errors); PowerShell syntax via `[System.Management.Automation.Language.Parser]::ParseFile` (both `.ps1` files clean — one real parser bug found and fixed, an em-dash+apostrophe combination inside a double-quoted string); `bash -n` on both `.sh` files (clean); `node -e "require(...)"` on root and `server` `package.json` (both OK, `server/package.json` unchanged); `git diff --check` (clean); `npm run server:test:non-disposable` re-run, exit `0` (68/68, unaffected baseline from F1-003-B1 — confirms no regression from this task's additive changes). |
| Tenant/security/KVKK impact | None — no application code touched; every fixture used is already synthetic (via the existing, unmodified `dbVerificationHarness.ts`); no production tenant data; no consent/retention logic changed; no KVKK freeze boundary touched; this task's own evidence documents contain no PII/secrets (only run IDs, container/database names, host ports, exit/pass/fail counts) — every credential is generated via `node:crypto`, passed only through in-memory child-process `env`, and fully redacted (no partial reveal) before any logging path. |
| Rollback expectations | Single `git revert` of this task's commit(s) — removes all new `scripts/test-runtime/**` files and reverts the additive root `package.json` script changes. Any stale labeled local Docker resource is removable via `npm run test:runtime:cleanup-stale -- --live` (label-scoped, never a broad Docker cleanup). No database/migration/deployment rollback applicable. |
| Completion/status rules | This task reached `AGENT_COMPLETED`. `MERGED` requires external review and an actual merged PR. This task's own completion does not by itself satisfy F1's exit gate, does not mark parent F1-003 complete, does not close R-046/R-070/R-071, and does not declare G1/G2 approved or the KVKK baseline stable. |
| Explicit non-claims | Disposable-runtime provisioning is CI-wired (no `.github/workflows/**` file exists); ~~production PostgreSQL major-version parity established~~ **SUPERSEDED 2026-07-29 by F1-003-P2-R1 below — parity at the major-version level (`16`) is now CONFIRMED via merged F1-003-P2V evidence (PR #259); exact image/build/package parity with production is still NOT claimed**; R-070 resolved/mitigated/closed (remains `OPEN`); full network-egress denial implemented; cleanup guaranteed to survive a hard kill/OS crash/Docker-daemon crash (only best-effort `SIGINT`/`SIGTERM` handling exists); legacy `server:test`'s own DB-required members provisioned or executed (deliberately deferred); production access of any kind. |
| Relationship to F1-003-P3 | **Resolved (2026-07-30): PR #260 merged**, unblocking F1-003-P3, which is now implemented (`AGENT_COMPLETED`, PR opened) — see the F1-003-P3 subsection below. This task's own scripts/orchestrator files are unmodified by F1-003-P3. |

#### F1-003-P2V — PostgreSQL Major-Version Evidence Closure

| Field | Value |
|---|---|
| Task ID | F1-003-P2V |
| Title | PostgreSQL Major-Version Evidence Closure |
| Parent task | F1-003. Explicitly authorized to run in parallel with **F1-003-P2** (Disposable PostgreSQL and MinIO Provisioning and Collision Avoidance) — did not touch any P2 implementation file, runtime script, package script, test file, or P2 evidence artifact; the active P2 worktree (`f1-003-p2-disposable-runtime`) was only listed via `git worktree list`, never entered. |
| Status | `AGENT_COMPLETED`, PR opened, not merged — `PR_OPENED_AWAITING_REVIEW`. Isolated worktree `E:\Ek Gelir\Siteler\DisKlinikCRM-worktrees\f1-003-p2v-postgres-version-evidence`, branch `docs/f1-003-p2v-postgres-version-evidence`, execution baseline `origin/main` @ `ed568c9db3b0d62a049bc291d2137f5c913a7ac7` (identical to the task's own expected authoritative ancestor, PR #256's merge commit; `git merge-base --is-ancestor` exit `0`; zero intervening commits). |
| Purpose | Determine, from repository-committed and already-accepted infrastructure evidence only, the production PostgreSQL major version, or formally preserve `postgres:16-alpine` as provisional without a parity claim if no such evidence exists. No production access, no SSH, no live database query, no secret read. |
| Result | **CASE A — authoritative production version found.** `evidence/F0-002_PRODUCTION_BASELINE_EVIDENCE.md` §B.3/§B.7 (`VERIFIED_PRODUCTION_OBSERVED`, `2026-07-19T13:43:12+03:00`) directly records PostgreSQL `16.14` on production; independently corroborated by `evidence/F0-006_PRODUCTION_TOPOLOGY_EVIDENCE.md`, `PRODUCTION_TOPOLOGY.md`, and two later independent tasks (`KVKK-HIGH-008-PMVR`, `R046_PRODUCTION_VERIFICATION`) that each explicitly matched a disposable Postgres to this same figure — 5 documents, 3 task lineages, zero contradiction found anywhere in the repository. **Production PostgreSQL major version = 16 (exact `16.14`).** F1-003-P2's provisional `postgres:16-alpine` already matches at the major-version level — major-only parity, not exact (`postgres:16-alpine` is a floating tag, not pinned to `16.14`). No change to F1-003-P2's image choice is required or instructed by this task. |
| Correction recorded | F1-003-P2A_DISPOSABLE_RUNTIME_PROVISIONING_DESIGN.md §13/§G's "no authoritative repository/deployment evidence exists" finding is corrected as an **incomplete search** (it never directly opened F0-002/F0-006/`PRODUCTION_TOPOLOGY.md`, only grepped `docs/program/` generally and the R-046/R-070-cited evidence chain), not a false one. This task does **not** edit F1-003-P2A's own files (separate task lineage, out of this task's scope) — the correction is recorded only in this task's own new evidence document, for a future task in that lineage (or F1-003-P2 itself) to apply if desired. |
| Deliverables | [evidence/F1-003-P2V_POSTGRESQL_MAJOR_VERSION_EVIDENCE.md](../evidence/F1-003-P2V_POSTGRESQL_MAJOR_VERSION_EVIDENCE.md) (14-section narrative: sources reviewed, CodeGraph findings, 13 version references classified, production topology conclusion, Case A conclusion, exact-version conclusion, provisional-image decision, parity status, conflicts, security/tenant/KVKK impact, explicit non-claims). [evidence/F1-003-P2V_postgresql_version_evidence.json](../evidence/F1-003-P2V_postgresql_version_evidence.json) (machine-readable companion). |
| Allowed scope (honored) | Evidence/documentation only — two new evidence files, minimal updates to `NORAMEDI_MASTER_TRACKER.md`/`CURRENT_PHASE.md`/this file/`evidence/README.md`. No runtime provisioning code, no `server/package.json` change, no test change, no Docker/Compose change, no production deployment change, no CI workflow change, no Prisma schema/migration change, no application code change, no production access. |
| CodeGraph | Confirmed unavailable (`ToolSearch` zero matches) — sixth independent confirmation in this program. Fallback: narrowly-bounded `Grep`/`Read` scoped to `docs/program/**`, `server/.env.example`, `server/prisma/schema.prisma` datasource block, `.github/workflows/` listing, Docker/Compose existence check (none exist). |
| Validation | `node -e "JSON.parse(...)"` on the new JSON — valid, 31 top-level keys. Path-existence check for all 21 cited repository files — 21/21 present. Cross-file consistency: all 5 production-environment references (`16.14`) internally consistent, zero contradiction. |
| Tenant/security/KVKK impact | None — no application code touched; no new production access; no credential read, referenced, or exposed. The `16.14` figure and `noramedi_crm` database name are already-published, non-secret facts already present in five pre-existing, accepted evidence documents this task only reads and cites. |
| R-070 / R-046 | Both confirmed `OPEN` by direct read of `RISK_REGISTER.md` (lines 142/116) — unchanged, not touched. |
| Rollback | Single `git revert` of this task's one commit; no schema/data/deployment rollback applicable — documentation/evidence only. |
| Completion/status rules | This task reached `AGENT_COMPLETED`. Does not mark parent F1-003 complete, does not satisfy F1's exit gate, does not authorize or start F1-003-P2's own implementation, does not close R-046/R-070/R-071, does not declare G1/G2 approved or the KVKK baseline stable. This task is not itself a new production verification event — the `16.14` figure is cited from the pre-existing `2026-07-19` F0-002 Stage B evidence session. |
| Relationship to F1-003-P2/P3 | Does not unblock or start F1-003-P2. **Corrected (F1-003-P2V-R1, 2026-07-29): the "F1-003-P2 remains not started" wording above was itself stale** — `git worktree list` (recorded by this same task, §2 above) already showed an active, isolated P2 worktree (`f1-003-p2-disposable-runtime`) containing pre-existing, uncommitted/unverified implementation work under `scripts/test-runtime/`. Correct status: **F1-003-P2 is `AGENT_WORK_IN_PROGRESS`**, not `NOT_STARTED` — see the F1-003-P2V-R1 subsection below for the full reconciliation. This task's own scope (provisioning implementation, collision avoidance) remains entirely unaffected by F1-003-P2V beyond the now-resolved production-parity question. F1-003-P3 remains blocked on F1-003-P2 being delivered, reviewed, and merged. |

#### F1-003-P2V-R1 — Active P2 Status Reconciliation

| Field | Value |
|---|---|
| Task ID | F1-003-P2V-R1 |
| Title | Active P2 Status Reconciliation |
| Parent task | F1-003-P2V (continuation on the same branch/PR — `docs/f1-003-p2v-postgres-version-evidence`, PR #259). Not a new PR, not merged, not rebased, not force-pushed. |
| Purpose | Correct a factual program-status contradiction: F1-003-P2V's own §2 (Baseline and worktree) directly observed, via `git worktree list`, that the isolated F1-003-P2 worktree (`f1-003-p2-disposable-runtime`) already exists and contains pre-existing untracked implementation work under `scripts/test-runtime/` — yet other F1-003-P2V-introduced text elsewhere in this file, `NORAMEDI_MASTER_TRACKER.md`, and `CURRENT_PHASE.md` stated F1-003-P2 was simply "not started". This task corrects only that specific contradiction. |
| Method | Used exclusively the worktree observation already recorded by F1-003-P2V itself (§2 above / the evidence document's §2). The P2 worktree was **not** inspected, read, diffed, modified, staged, cleaned, or stashed by this reconciliation task — no new entry into that worktree occurred. |
| Canonical corrected status | **F1-003-P2 — `AGENT_WORK_IN_PROGRESS`.** Qualifiers: isolated worktree exists; uncommitted/unverified implementation work observed (worktree-listing level only); no P2 delivery report received; no P2 runtime verification reviewed; no P2 commit evidence available to this task; no P2 PR opened; not merged; not deployed; not production-verified. **F1-003-P3 remains blocked until F1-003-P2 is delivered, reviewed, and merged.** |
| Strict status separation (honored) | Agent work started: yes. Implementation delivered: no. Implementation validated: no. Tests passed: no evidence yet. Commit created: no evidence from this task. PR opened: no (F1-003-P2's own PR, not this one). Merged: no. Deployed: no. Production verified: no. |
| PostgreSQL conclusion | Unchanged from F1-003-P2V: production observed PostgreSQL `16.14`, production major version `16`, F1-003-P2's provisional `postgres:16-alpine` choice remains major-only parity (not exact-version parity). No new production verification performed by this task. |
| Files corrected | This file (row above + this subsection); `NORAMEDI_MASTER_TRACKER.md` (new item); `CURRENT_PHASE.md` (new dated entry); `evidence/F1-003-P2V_POSTGRESQL_MAJOR_VERSION_EVIDENCE.md` (§3 wording); `evidence/F1-003-P2V_postgresql_version_evidence.json` (new `p2StatusCorrection` field); PR #259 description. |
| Files explicitly not touched | The P2 worktree/branch itself; `scripts/test-runtime/**`; `server/package.json`; any test file; any application/schema/migration/Docker/Compose/CI-workflow/deployment file; `RISK_REGISTER.md`. |
| Tenant/security/KVKK impact | None — documentation/status-wording correction only; no code, schema, or production-access change. |
| R-070 / R-046 | Both remain `OPEN`, unchanged, not touched by this task. |
| Rollback | Single `git revert` of this task's one commit; no schema/data/deployment rollback applicable. |
| Completion/status rules | This task reaches at most `AGENT_COMPLETED`. Does not mark F1-003-P2 delivered, validated, merged, deployed, or production-verified — it only corrects the program's own status *wording* about F1-003-P2 from `NOT_STARTED` to `AGENT_WORK_IN_PROGRESS`, a distinction already implied by the pre-existing worktree observation. Does not close R-046/R-070/R-071, does not satisfy F1's exit gate, does not declare G1/G2 approved. |

#### F1-003-P2-R1 — Post-P2V Main Reconciliation, PostgreSQL Parity Correction, and Image-Pinning Verification

| Field | Value |
|---|---|
| Task ID | F1-003-P2-R1 |
| Title | Post-P2V Main Reconciliation, PostgreSQL Parity Correction, and Image-Pinning Verification |
| Parent task | F1-003-P2 (continuation on the existing open PR #260 branch `feature/f1-003-p2-disposable-runtime` — no new PR opened, not merged, not rebased, not force-pushed). |
| Purpose | Reconcile the still-open F1-003-P2 branch/PR #260 with `origin/main` after F1-003-P2V/P2V-R1 merged as PR #259; correct the now-stale "production PostgreSQL major-version parity remains unconfirmed" wording left in F1-003-P2's own entries; verify actual image-pinning semantics for both runtime images rather than using "pinned" ambiguously; and preserve every P2 implementation/runtime evidence item unchanged. |
| Baseline / merge | `git merge --no-ff origin/main` from HEAD `df7b545fa0f89340ccd7cb5e34e97f412585935f` (P2's own prior commit) into `feature/f1-003-p2-disposable-runtime`, after confirming `git merge-base --is-ancestor 8592a570708c6308d7a19aff703db6e0a699ece7 origin/main` exits `0`. 3 documentation conflicts (`CURRENT_PHASE.md`, `evidence/README.md`, this file), resolved manually and semantically — no blanket `--ours`/`--theirs`; `NORAMEDI_MASTER_TRACKER.md` auto-merged cleanly. |
| PostgreSQL parity correction | **Canonical wording, effective now:** production PostgreSQL observed version `16.14`; disposable PostgreSQL observed version `16.14`; major-version parity **CONFIRMED**; observed patch-version equality `16.14 = 16.14`; exact image/build/package parity **NOT claimed**. F1-003-P2V's evidence was merged via PR #259 (merge commit `8592a570708c6308d7a19aff703db6e0a699ece7`, mergedAt `2026-07-29T07:57:11Z`). F1-003-P2's own prior "production PostgreSQL major-version parity remains unconfirmed" line (§ Explicit non-claims above) is superseded by this correction — the original statement is preserved above, marked superseded, not silently rewritten. |
| Image-pinning verification | **PostgreSQL** — runtime previously invoked only the floating tag `postgres:16-alpine` (not digest-pinned; only the resolved digest was recorded after pulling, so future byte-level reproducibility was not guaranteed). **Option A applied**: `scripts/test-runtime/lib/postgres.ts`'s `POSTGRES_IMAGE` now resolves to the digest-pinned reference `postgres@sha256:57c72fd2a128e416c7fcc499958864df5301e940bca0a56f58fddf30ffc07777` (re-verified live via `docker pull postgres:16-alpine` in this task — identical digest, "Image is up to date"); the floating tag is retained separately as `POSTGRES_IMAGE_TAG` for documentation/logging only, never passed to `docker run`. **MinIO** — already invoked as `minio/minio@sha256:8834ae47a2de3509b83e0e70da9369c24bbbc22de42f2a2eddc530eee88acd1b` (`tag@sha256:digest` form) since F1-003-P2's original implementation; re-verified live via `docker pull` in this task (identical digest) — no change required, digest-pinned claim retained. |
| Allowed scope (honored) | `scripts/test-runtime/lib/postgres.ts` (digest-pin switch only); this file, `CURRENT_PHASE.md`, `evidence/README.md`, `NORAMEDI_MASTER_TRACKER.md`, `evidence/F1-003-P2_DISPOSABLE_RUNTIME_IMPLEMENTATION_AND_VERIFICATION.md`, `evidence/F1-003-P2_disposable_runtime_verification.json`, PR #260 description; reruns of the already-existing real-runtime profiles against the new digest-pinned reference. |
| Prohibited scope (honored) | No `.github/workflows/**` file touched. No application/route/service/domain file changed. No Prisma schema or migration file created; existing migrations applied only to disposable databases during reruns. No `server/package.json` change. No production access, no deployment. No merge/rebase/force-push of PR #260. |
| Completion/status rules | This task reaches at most `AGENT_COMPLETED`. Does not merge PR #260, does not mark F1-003-P2 `MERGED`, does not satisfy F1's exit gate, does not close R-046/R-070/R-071, does not declare G1/G2 approved. Maximum status: `AGENT_COMPLETED` / `PR_OPENED_AWAITING_REVIEW`. |
| Relationship to F1-003-P3 | **F1-003-P3 remains blocked** until PR #260 is externally reviewed and merged — unchanged by this reconciliation. |

#### F1-003-P2-R2 — Parallel Prisma Generation Race Elimination and Deterministic Runtime Verification

| Field | Value |
|---|---|
| Task ID | F1-003-P2-R2 |
| Title | Parallel Prisma Generation Race Elimination and Deterministic Runtime Verification |
| Parent task | F1-003-P2-R1 (continuation on the existing open PR #260 branch `feature/f1-003-p2-disposable-runtime` — no new PR opened, not merged, not rebased, not force-pushed). |
| Blocking finding | F1-003-P2-R1's own `verify-parallel` reruns (§ its own table above): run 1 success, **run 2 — one storage invocation failed with a real Windows `EBUSY`** during concurrent `prisma generate`, run 3 success. Not a Docker name/port/database collision — a runtime filesystem collision within F1-003-P2's own orchestration boundary. |
| Root cause | CodeGraph confirmed unavailable (`ToolSearch` zero matches); bounded `Read`/`Grep` scoped to `scripts/test-runtime/**`, `server/prisma/schema.prisma`, `server/prisma.config.ts`, `server/src/db.ts`. Every disposable-profile invocation's `runMigrations()` ran `npx prisma generate` unconditionally, writing to the shared, schema-derived default output `server/node_modules/.prisma/client` (no custom `output` in the schema's `generator client` block or in `prisma.config.ts`). `verify-parallel`'s storage pair (2-way concurrency — not full 4-way, since the postgres pair completes and tears down before the storage pair starts) raced two such concurrent `generate` calls on that one shared directory. Generation output depends only on `schema.prisma`, never `DATABASE_URL` (confirmed via `server/src/db.ts`), so one generation safely covers every child in the same invocation; `migrate deploy` does not require generation first; no cross-process lock is needed since all `verify-parallel` concurrency is same-process. |
| Chosen remediation | **Option A — generate once before parallel fan-out**, the narrowest safe fix supported by this evidence. New module `scripts/test-runtime/lib/prismaGeneration.ts`: `generatePrismaClientOnce()` runs `prisma generate` exactly once per orchestrator invocation and mints a single-process authorization token (`node:crypto.randomBytes`, tracked in a private module-scoped `Set`). `runMigrations()` (`lib/process.ts`) skips its own generate step only when presented a token this same process actually minted — any absent/foreign/invalid token falls back to generating (fail-safe, never fail-skip); standalone `postgres`/`storage` invocations are byte-for-byte unaffected (no authorization is ever passed to them). `orchestrator.ts`'s `runVerifyParallel()` now generates once up front and returns immediately with zero children/Docker resources if that generation fails. New tiny module `lib/paths.ts` extracts `REPO_ROOT`/`SERVER_DIR` to avoid a circular import between `process.ts` and `prismaGeneration.ts`. Options B (cross-process lock) and C (per-run isolated Prisma output) considered and rejected as unnecessary complexity/import-changing for a same-process-only concurrency problem. |
| Unit tests | 9 new Docker-free tests in `__tests__/orchestratorUnit.test.ts`, exercising the coordination logic via an injectable `GenerateRunner` (no real subprocess spawned): only one generation owner; a single token authorizes multiple independent verifiers; failure exit code propagates untouched; a failed generation mints no token; the `--inject-failure=parent-generate` flag forces deterministic failure; a slow-but-successful runner still resolves (no premature timeout); a fabricated/foreign token never validates; an absent authorization never allows skipping generation (the exact gate `runMigrations()` itself uses); reset revokes prior tokens. **59/59 total unit tests passing** (was 50/50). Stale-lock behavior and a dedicated cross-process timeout test are explicitly recorded as not applicable — Option A introduces no lock and no wait loop. |
| Standalone/failure-injection reruns | `postgres` profile exit `0` (`server:test:disposable-db` 9 members exit `0`); `storage` profile exit `0` (`server:test:storage-integration` 21/21); `server:test:non-disposable` exit `0` (68/68, no regression); all 4 postgres failure-injection modes (test/migration/readiness/cleanup) reproduce the same real failure signals as before, unchanged; new `--inject-failure=parent-generate` against `verify-parallel`: `generation.succeeded: false`, `postgresPair`/`storagePair` both empty, exit `1`, **zero Docker containers/networks created** (confirmed via `docker ps -a`/`docker network ls` immediately after). |
| Parallel stability gate | **8 total `verify-parallel` attempts across F1-003-P2-R1+R2 combined** — every attempt recorded, none hidden: (1) R1 success, (2) R1 **FAILED** (the EBUSY finding), (3) R1 success, (4)-(8) R2 all success. **5 consecutive clean runs achieved post-fix (attempts 4-8); the counter was never reset to hide attempt 2's failure.** Every post-fix run: all 4 children exit `0`; `collisionCheck` all `true`; exactly **1** `Generated Prisma Client` log line per full run (not up to 4 — direct confirmation of the fix); zero `EBUSY`; zero `nmtest-*` Docker resources remaining after every single run (`docker ps -a`/`docker network ls`, confirmed after each of the 5). |
| Allowed scope (honored) | `scripts/test-runtime/lib/paths.ts` (new), `lib/prismaGeneration.ts` (new), `lib/process.ts`, `lib/profiles.ts`, `orchestrator.ts`, `__tests__/orchestratorUnit.test.ts`; this file, `CURRENT_PHASE.md`, `NORAMEDI_MASTER_TRACKER.md`, `evidence/F1-003-P2_DISPOSABLE_RUNTIME_IMPLEMENTATION_AND_VERIFICATION.md`, `evidence/F1-003-P2_disposable_runtime_verification.json`, PR #260 description; reruns of already-existing real-runtime profiles. |
| Prohibited scope (honored) | No schema/migration file created (existing migrations applied only to disposable databases during reruns). No production deployment behavior. No CI workflow file. No microservices/new framework. No global Docker cleanup — only this tool's own labeled resources touched, exactly as before. No production access. No `server/package.json`, application-domain, or route/service file changed. |
| Migration status | No schema/migration creation. No production DB access. No `_prisma_migrations` manual change. No rollback performed. |
| Security/tenant/KVKK impact | The generate-once mechanism is an in-process, same-invocation trust boundary only — `validTokens` is a private module-scoped `Set` with no persisted lock/temp file of any kind, so no stale-lock-file deletion-safety question applies. No new shell-string concatenation (reuses the existing argv-array `spawnAsync` pattern). No application-runtime import. No tenant-scoping/authorization behavior changed. No real patient data. No KVKK freeze boundary touched. |
| Rollback | Single `git revert` of this task's commit — removes `lib/paths.ts`/`lib/prismaGeneration.ts` and reverts the `process.ts`/`profiles.ts`/`orchestrator.ts`/unit-test changes. No lock/temp artifact exists to clean up (none is ever created). No schema/data/deployment rollback applicable. |
| Acceptance-criteria correction | Items 17/18 ("at least two concurrent postgres/storage runs succeed without collision") corrected from "`MET` based on exactly 2 runs, later found to rest on an unproven assumption (F1-003-P2-R1)" to "`MET`, re-verified via 5 consecutive clean runs post-fix (F1-003-P2-R2)." The other 32 acceptance-criteria items are unaffected by this correction. |
| Completion/status rules | This task reaches at most `AGENT_COMPLETED`. Does not merge PR #260, does not mark F1-003-P2 `MERGED`, does not satisfy F1's exit gate, does not close R-046/R-070/R-071, does not declare G1/G2 approved. Maximum status: `AGENT_COMPLETED` / `PR_OPENED_AWAITING_REVIEW`. |
| Relationship to F1-003-P3 | **F1-003-P3 remains blocked** until PR #260 is externally reviewed and merged — unchanged by this remediation. |

#### F1-003-P2-R3 — Parallel Signal Cleanup Registry and Interruption Verification

| Field | Value |
|---|---|
| Task ID | F1-003-P2-R3 |
| Title | Parallel Signal Cleanup Registry and Interruption Verification |
| Parent task | F1-003-P2-R2 (continuation on the existing open PR #260 branch `feature/f1-003-p2-disposable-runtime` — no new PR opened, not merged, not rebased, not force-pushed). |
| Defect | `orchestrator.ts` tracked SIGINT/SIGTERM cleanup state in one module-scoped `let activeCleanupTargets` slot, overwritten and nulled by each `runDisposableProfile()` invocation. `verify-parallel`'s `Promise.all`-based postgres pair and storage pair each keep two invocations concurrently live as their normal, expected steady state — a signal during that window could see at most one invocation's containers/network, or `null`, invalidating the claimed best-effort parallel signal cleanup. Reproduced by direct code-path proof (reading the pre-fix source and `runVerifyParallel()`'s own `Promise.all` usage), not an observed live crash. |
| Root cause | CodeGraph confirmed unavailable (`ToolSearch` zero matches, consistent with every prior program task including F1-003-P2-R2's own confirmation for this same file set); bounded `Read`/`Grep` scoped to `orchestrator.ts`, `lib/cleanup.ts`, `lib/docker.ts`, `__tests__/orchestratorUnit.test.ts`, and this task's own evidence predecessors. A single module-scoped slot cannot represent more than one invocation's live cleanup state; nothing about its use was concurrency-aware. |
| Chosen remediation | New module `scripts/test-runtime/lib/cleanupRegistry.ts` — a `Map<runId, TeardownTargets>` registry storing each invocation's targets **by reference** (in-place mutation as containers/networks are created is automatically visible, no separate "update" API). `registerCleanupTargets()`/`unregisterCleanupTargets()` replace the old slot; `teardownAllRegistered()` snapshots every registered invocation and tears each down via `Promise.allSettled` (never `Promise.all`, so one invocation's teardown failure cannot block the others); `runSignalCleanupOnce()` caches its in-flight promise so a second SIGINT/SIGTERM delivered mid-teardown reuses the same pass instead of starting an overlapping one. `teardownFn` is injectable (defaults to the real `teardown()`), mirroring F1-003-P2-R2's injectable `GenerateRunner` pattern, so unit tests exercise the coordination logic without Docker. `lib/cleanup.ts` itself is unmodified — its existing per-invocation contract (attempt every step, aggregate errors, never throw) is reused unchanged. |
| Unit tests | 9 new Docker-free tests in `__tests__/orchestratorUnit.test.ts`: two concurrent targets registered simultaneously; removing one leaves the other; a signal snapshot includes all active targets including a container name pushed on after registration; a fake teardown fn failing for target A still lets target B succeed (`Promise.allSettled` proof); duplicate registration throws a typed `DuplicateRunRegistrationError`; removing an unknown/already-removed ID is harmless; a second signal call mid-teardown reuses the same in-flight pass, proven via a call-counter; the registry is empty after a normal single-invocation completion; `resetCleanupRegistryForTest()` clears both registered targets and an in-flight signal promise. **68/68 total unit tests passing** (was 59/59). |
| Standalone/failure-injection reruns | `postgres` profile exit `0` (run ID `20260730T081436Z-bd393684-17508`); `storage` profile exit `0`, 21/21 (run ID `20260730T081916Z-fb4fcbe1-34736`); `server:test:non-disposable` exit `0` (68/68, no regression); all 4 postgres failure-injection modes (test/migration/readiness/cleanup) reproduce the same real failure signals as before, including the digest-pinned cleanup-failure sidecar (below); `verify-parallel --inject-failure=parent-generate` still aborts before fan-out with zero Docker resources created, confirming F1-003-P2-R2's guard is untouched by this change. |
| Parallel stability reruns | `verify-parallel` run 3 consecutive times this task (distinct from R2's own 5-run gate) — all 3 exit `0`, all `collisionCheck` fields `true` every run, exactly 1 `Generated Prisma Client` log line and zero `EBUSY` in the 2 runs where this was explicitly captured, zero labeled Docker resources after the batch. One incidental, honestly-recorded finding: an unrelated shell command-timeout hard-killed one standalone `postgres` run mid-execution during this task's own regression testing, leaving one labeled container/network behind — exactly the already-documented "not guaranteed to survive a hard kill" limitation (no JS signal handler, old or new, runs after a hard kill); removed manually as test-harness hygiene, not counted as a registry defect. |
| Live interruption verification | Two automated approaches attempted on this Windows 11 machine to deliver a real interrupt to a running orchestrator process: (1) `child_process.spawn` + `child.kill('SIGINT')` from a Node parent — empirically confirmed, via an isolated minimal repro (not merely asserted from documentation), that the child process exits and Node reports `signal: 'SIGINT'`, but the child's own `process.on('SIGINT', ...)` handler never actually executes; a genuine Windows platform limitation, not a code defect. (2) Win32 `GenerateConsoleCtrlEvent(CTRL_C_EVENT, 0)` via PowerShell `Add-Type` P/Invoke — the task's own suggested "strongest supported graceful interruption path," the mechanism a real human Ctrl+C triggers — was attempted but **not completed** in this session (the tool call required an interactive approval step not granted during this run); **not claimed** as working or non-working. Per the task's own explicit fallback instruction, this task relies instead on the deterministic unit-level verification above as proof of the multi-target signal-handler logic; the orchestrator's real `process.on('SIGINT'/'SIGTERM', ...)` handlers call the exact same unit-verified `runSignalCleanupOnce()` function unconditionally, so a genuine interactive Ctrl+C from a real terminal exercises the identical code path. Live, real-time-observed Docker interruption of a running `verify-parallel` invocation is **not claimed** as performed in this task. |
| Optional supply-chain cleanup | The cleanup-failure-injection sidecar's `alpine:latest` (mutable tag) replaced with the exact digest already cached locally on this machine, `alpine@sha256:28bd5fe8b56d1bd048e5babf5b10710ebe0bae67db86916198a6eec434943f8b`, as a named `CLEANUP_FAILURE_SIDECAR_IMAGE` constant in `orchestrator.ts` with a comment documenting its narrow test-only role. No new pull performed. Re-verified live still reproducing the same genuine `"has active endpoints"` cleanup failure. This sidecar is never part of the actual disposable PostgreSQL/MinIO runtime under test. |
| Allowed scope (honored) | `scripts/test-runtime/lib/cleanupRegistry.ts` (new), `orchestrator.ts`, `__tests__/orchestratorUnit.test.ts`; this file, `CURRENT_PHASE.md`, `NORAMEDI_MASTER_TRACKER.md`, `evidence/README.md`, `evidence/F1-003-P2_DISPOSABLE_RUNTIME_IMPLEMENTATION_AND_VERIFICATION.md`, `evidence/F1-003-P2_disposable_runtime_verification.json`, PR #260 description; reruns of already-existing real-runtime profiles. |
| Prohibited scope (honored) | No schema/migration file created. No CI workflow file. No application/route/service/domain file changed. No `server/package.json` change. No production access, no deployment. No global Docker cleanup/prune — only this tool's own labeled resources touched, exactly as before. `lib/cleanup.ts` not modified. Fail-fatal cleanup policy, per-run label ownership, the stale-resource sweeper, the production-endpoint guards, F1-003-P2-R2's parent-level Prisma generation fix, digest-pinned PostgreSQL/MinIO references, and existing failure-injection behavior are all preserved unchanged and reconfirmed working. |
| Migration status | No schema/migration creation. No production DB access. No `_prisma_migrations` manual change. No rollback performed. **R-070 remains OPEN**, untouched. |
| Security/tenant/KVKK impact | The registry is in-process, module-scoped `Map` state only — no persisted lock/temp file of any kind, dies with the orchestrator process, same trust boundary as F1-003-P2-R2's generation-authorization mechanism. No new shell-string concatenation (the sidecar image-reference change is a literal-string swap through the existing argv-array `runDockerSync()` call). No application-runtime import. No tenant-scoping/authorization behavior changed. No real patient data. No KVKK freeze boundary touched. |
| Rollback | Single `git revert` of this task's commit — removes `lib/cleanupRegistry.ts` and reverts the `orchestrator.ts`/unit-test changes, restoring the pre-R3 single-slot behavior. No lock/temp artifact exists to clean up (none is ever created). No schema/data/deployment rollback applicable. |
| Completion/status rules | This task reaches at most `AGENT_COMPLETED`. Does not merge PR #260, does not mark F1-003-P2 `MERGED`, does not satisfy F1's exit gate, does not close R-046/R-070/R-071, does not declare G1/G2 approved. Maximum status: `AGENT_COMPLETED` / `PR_OPENED_AWAITING_REVIEW`. |
| Relationship to F1-003-P3 | **F1-003-P3 remains blocked** until PR #260 is externally reviewed and merged — unchanged by this remediation. |

#### F1-003-P3 — Layered CI Workflows and Full-Suite Fail-Safe

| Field | Value |
|---|---|
| Task ID | F1-003-P3 |
| Title | Layered CI Workflows and Full-Suite Fail-Safe |
| Parent task | F1-003 (next ordered subtask after P2/P2V/P2-R1/P2-R2/P2-R3, all merged or superseded). Unblocked 2026-07-30 when PR #260 (F1-003-P2) merged — confirmed by this task's own baseline verification (`gh pr view 260`: `MERGED`, `mergedAt 2026-07-30T09:45:38Z`; `git merge-base --is-ancestor 04723f9a... origin/main` exit `0`; fetched `origin/main` IS PR #260's merge commit). |
| Status | `AGENT_COMPLETED`, PR opened, not merged — `PR_OPENED_AWAITING_REVIEW`. Isolated worktree `E:\Ek Gelir\Siteler\DisKlinikCRM-worktrees\f1-003-p3-layered-ci-workflows`, branch `feature/f1-003-p3-layered-ci-workflows`, execution baseline `origin/main` @ `04723f9add600b6c432fb517f0607235944d6c2e` (PR #260's own merge commit, zero intervening commits). |
| Purpose | Implement F1-003-P1's own forward-looking `futureCiCommandMatrix` (`evidence/F1-003-P1_test_execution_contract.json`) as real, additive GitHub Actions workflows: a layered PR/main/nightly CI model invoking the canonical F1-003-P1 scripts and the F1-003-P2 disposable-runtime orchestrator, plus a full-suite/compatibility fail-safe layer, with least-privilege permissions, SHA-pinned actions, digest-pinned images (inherited unchanged from P2), explicit timeouts/concurrency, and fail-fatal cleanup. |
| Dependencies | F1-003-P1 `MERGED` (PR #257, canonical script membership/`futureCiCommandMatrix`). F1-003-P2 `MERGED` (PR #260, disposable PostgreSQL/MinIO orchestrator, used exactly as merged). |
| Allowed scope (honored) | Three new `.github/workflows/*.yml` files only, plus documentation/evidence updates (this file, `NORAMEDI_MASTER_TRACKER.md`, `CURRENT_PHASE.md`, `evidence/README.md`, two new evidence files). |
| Prohibited scope (honored) | No `scripts/test-runtime/**` file changed — an initial design considered a small `--full-suite` orchestrator flag so Layer 5 could also cover legacy `server:test`'s own additional DB-required members, but this was deliberately not implemented as outside this task's CI-workflow-only authorized scope (no rewrite of the accepted, merged P1/P2 test architecture). No `server/package.json` or root `package.json` change. No Prisma schema/migration. No application/route/service/domain file. No affected-test/impact-based selector implemented (F1-001's design remains unimplemented at the CI-wiring level). No production access, no deployment. `windows-bridge-pr.yml`/`windows-bridge-release.yml` untouched. |
| Deliverables | `.github/workflows/ci-layers.yml` (reusable, 8 jobs mapping to the 5 required layers), `.github/workflows/ci-pr.yml` (pull-request trigger), `.github/workflows/ci-main-and-nightly.yml` (push-to-main + nightly schedule + manual dispatch); [evidence/F1-003-P3_LAYERED_CI_WORKFLOWS_AND_FULL_SUITE_FAIL_SAFE.md](../evidence/F1-003-P3_LAYERED_CI_WORKFLOWS_AND_FULL_SUITE_FAIL_SAFE.md) (16-section narrative) + [evidence/F1-003-P3_layered_ci_verification.json](../evidence/F1-003-P3_layered_ci_verification.json) (machine-readable companion). |
| Layer map | Layer 1 (static/type/syntax): `frontend-typecheck-and-build` (`npm run build`), `server-typecheck` (`npm run typecheck`), `tooling-typecheck-and-unit` (`typecheck:runtime` + `test:runtime:unit`), `workflow-and-syntax-lint` (actionlint v1.7.12 pinned by checksum, `bash -n`, PowerShell `Parser::ParseFile`, JSON validation). Layer 2: `non-disposable-backend-tests` (`server:test:non-disposable`, 68 members). Layer 3: `disposable-postgres-tests` (`test:runtime:postgres`, the merged P2 orchestrator's `postgres` profile, unmodified). Layer 4: `storage-integration-tests` (`test:runtime:storage`, the `storage` profile, unmodified). Layer 5: `full-suite-compatibility-failsafe` (the 7 frontend leaf scripts + `test:vitest` previously reached by zero CI workflow per F1-002-P1's own evidence; no path filter, runs on every trigger). `eslint`/`npm run lint` is confirmed genuinely non-functional (ESLint 9, no `eslint.config.js` exists anywhere in the repository) and is honestly documented as such, not wired into any job. |
| Exact evidence required | All local validation commands and exact exit codes/pass-fail counts recorded in the evidence `.md`/`.json` pair: `test:runtime:unit` 68/68; `server:test:non-disposable` 68/68; `test:runtime:postgres` exit `0` (`server:test:disposable-db` 9/9, cleanup clean); `test:runtime:storage` exit `0` (21/21 assertions, cleanup clean); `test:runtime:parallel` regression exit `0` (collision-free, exactly 1 Prisma generation, 0 `EBUSY`, proving this task's workflow-only changes did not alter the P2 orchestrator contract); zero labeled Docker resources remaining after any run; 7 frontend leaf scripts + `test:vitest` all passing (156 + 29 assertions); actionlint v1.7.12 (SHA-256 `8aca8db9...`, verified) zero findings against all 5 workflow files (3 new + 2 pre-existing); `bash -n`/PowerShell `Parser::ParseFile`/JSON validation all clean; `npm run build` exit `0`; server `npm run typecheck` exit `0`; `npm run lint` exit `2`, confirmed non-functional. |
| Validation requirements | See the evidence `.md`'s §11 table (22 numbered local-validation commands with exact exit codes). GitHub Actions execution itself remains unverified until the PR is opened and real runs are inspected — never claimed passing in advance. |
| Tenant/security/KVKK impact | None — CI/tooling only. No production DATABASE_URL/S3 credential in any job; no repository secret used in any `pull_request`-triggered job; least-privilege `permissions: contents: read` everywhere; no `pull_request_target`; sanitized, redacted-by-construction orchestrator JSON summaries uploaded as artifacts only on failure, bounded 7-day retention. KVKK physical-architecture freeze untouched, still active. |
| Rollback expectations | Single `git revert` of this task's commit(s) — removes all three new workflow files; `windows-bridge-pr.yml`/`windows-bridge-release.yml` and all P1/P2 package scripts/test-runtime tooling are unaffected and need no rollback. No database/storage/deployment/production rollback applicable. |
| Completion/status rules | This task reached `AGENT_COMPLETED`. `MERGED` requires external review and an actual merged PR plus real GitHub Actions evidence. Does not mark parent F1-003 complete, does not satisfy F1's exit gate, does not close R-046/R-070/R-071, does not declare G1/G2 approved or the KVKK baseline stable, does not start F1-003-P4, does not implement affected-test selection. |
| Explicit non-claims | Legacy `server:test`'s own additional DB-required members are now covered by CI (deliberately, explicitly deferred — see Prohibited scope row); actual GitHub Actions execution has passed (no remote run exists until the PR is opened); Windows graceful-interruption behavior is newly verified (unchanged F1-003-P2-R3 limitation — all new jobs run on `ubuntu-latest`, which says nothing about the Windows-specific gap); F1's exit gate satisfied; R-070/R-046/R-071 resolved. |
| Relationship to F1-003-P4 | **F1-003-P4 remains blocked** on this task's own PR being externally reviewed and merged, with real GitHub Actions run/job/step evidence reviewed — not started, not authorized to start by this entry. |

#### F1-003-P3-R1 — Main Reconciliation, Backend Compatibility Fail-Safe, and Remote CI Evidence Closure

| Field | Value |
|---|---|
| Task ID | F1-003-P3-R1 |
| Title | Main Reconciliation, Backend Compatibility Fail-Safe, and Remote CI Evidence Closure |
| Parent task | F1-003-P3 (continuation on the existing open PR #268 branch `feature/f1-003-p3-layered-ci-workflows` — no new PR opened, not merged, not rebased, not force-pushed). |
| Status | `AGENT_COMPLETED`, PR #268 updated, not merged — `PR_OPENED_AWAITING_REVIEW` (unchanged maximum status). |
| Purpose | (1) Reconcile the P3 branch with `origin/main`, which had advanced past the original P3 baseline. (2) Close the backend full-suite/compatibility gap the original P3 evidence left open and had mis-estimated. (3) Fix the artifact-generation defect the original P3 evidence itself flagged as a risk (combined stdout mislabeled as JSON). (4) Replace the original P3 evidence's `null` GitHub Actions fields with real run data, and root-cause/fix whatever that real data revealed. |
| Baseline reconciliation | Fetched `origin/main` = `cc31aeb7a0c379a641e7ab4558031e9adcdf1e59` (PR #266, calendar drag/resize hardening) — not an ancestor of the existing P3 HEAD (`git merge-base --is-ancestor origin/main HEAD` exit `1`). `git diff --name-only` confirmed a completely disjoint file set (`src/components/CalendarTimelineView.*`, `src/locales/*/appointments.json`) versus this task's own paths. `git merge --no-ff origin/main` → merge commit `7ab630c9a5a54acc7bb757b06c908b772c5f138d`, **0 conflicts**. No rebase, no force push, no `--ours`/`--theirs`, no reset. Post-merge ancestor checks (both directions) exit `0`. |
| Exact uncovered-backend-script inventory (corrected) | Programmatically diffed legacy `server:test`'s 77-member chain against the union of `server:test:non-disposable` (68) + `server:test:disposable-db` (9) + `server:test:storage-integration` (1): **exactly 23** scripts uncovered, all `postgresRequired:true`/`minioRequired:false` per `F1-003-P1_test_execution_contract.json`'s own per-script fields — full name list in the evidence document §17.2. The original P3 evidence's "roughly 14" estimate is corrected here as inaccurate, not merely refined. |
| Backend compatibility closure (honored constraints) | No second disposable-PostgreSQL implementation. No hand-maintained test membership in workflow YAML (the 23 names live in one new `server/package.json` aggregate script, invoked by name). No change to the existing `postgres`/`storage` profiles' behavior or to legacy `server:test` itself. Closure: new additive `server:test:legacy-db-required` aggregate (23 members, exact order preserved from the legacy chain) + new additive `postgres-compat` orchestrator profile (`scripts/test-runtime/lib/profiles.ts`'s `resolvePostgresOnlyTestScript()`, `orchestrator.ts`'s `RunOptions.profile` widened) that reuses the `postgres` profile's entire provisioning/guard/migration/cleanup/pinning/naming code path unchanged, only swapping which already-existing package script runs. New additive root script `test:runtime:postgres-compat`. New CI job `full-suite-compatibility-failsafe-backend` (part of Layer 5), same `if: always()` cleanup-check and failure-only sanitized-artifact pattern as Layers 3/4. |
| Root-caused GitHub Actions defect | The original P3 PR run (`30536185063`, head `e99dd096d2d6d1222c9f7c7f59c0735ab4380979`) **completed with overall conclusion `failure`** — `Layer 2: non-disposable backend tests` failed with `SyntaxError: The requested module '@prisma/client' does not provide an export named 'PrismaClient'`. Root cause: that job never ran `npx prisma generate` before invoking `server:test:non-disposable`; `server/package.json` has no `postinstall` hook for this; Layers 3/4 (and the new backend-compat job) succeed only because the F1-003-P2 orchestrator's `runMigrations()` always generates the client itself. Not reproduced by this task's own local validation, whose working directory had already generated the client via earlier, unrelated local commands — a from-scratch checkout hits it every time. **Fix:** added an explicit `npx prisma generate` step to `non-disposable-backend-tests` in `ci-layers.yml`. No test/application/schema file changed. |
| Artifact-generation fix | Replaced `npm run test:runtime:postgres \| tee postgres-run-summary.json` (combined stdout, not valid standalone JSON) with a new, additive `--summary-file=<path>` CLI flag (`scripts/test-runtime/lib/summaryFile.ts`, `maybeWriteSummaryFile()`) that writes the exact already-redacted summary object to a dedicated file as pure JSON — wired into all three single-invocation profiles plus `verify-parallel`/`cleanup-stale` for consistency. CI now validates the file is real JSON (`node -e JSON.parse`) and scans it for prohibited secret-like patterns before upload, failure-only, both gates able to refuse the upload. **A second real defect found and fixed during this same work** (by this task's own local verification, before reaching CI): an unhandled `writeFileSync` failure could propagate out of `main()` and override the real, already-determined test-outcome exit code with a generic fatal-error exit — fixed by making the write failure-tolerant (caught, logged to stderr, never thrown). 4 new Docker-free unit tests added. |
| Deliverables | `.github/workflows/ci-layers.yml` (prisma-generate fix; renamed/added Layer-5 jobs; `--summary-file` + JSON-validation + secret-scan wiring for Layers 3/4/5-backend). `server/package.json` (+1 additive aggregate, `server:test:legacy-db-required`, 23 members; legacy `server:test` unmodified). `scripts/test-runtime/lib/profiles.ts` (+`postgres-compat` profile, +`resolvePostgresOnlyTestScript()`). `scripts/test-runtime/orchestrator.ts` (`RunOptions.profile` widened; scriptName resolution updated; `--summary-file` wiring). `scripts/test-runtime/lib/summaryFile.ts` (new). `scripts/test-runtime/__tests__/orchestratorUnit.test.ts` (+6 tests: 2 profile-resolution, 4 summary-file — 68→74 total). Root `package.json` (+1 additive script, `test:runtime:postgres-compat`). Updated [F1-003-P3_LAYERED_CI_WORKFLOWS_AND_FULL_SUITE_FAIL_SAFE.md](../evidence/F1-003-P3_LAYERED_CI_WORKFLOWS_AND_FULL_SUITE_FAIL_SAFE.md) §17 (new) and [F1-003-P3_layered_ci_verification.json](../evidence/F1-003-P3_layered_ci_verification.json) (new `p3r1*` fields + real `githubActionsEvidence`). Tracker/phase/evidence-index updates (this entry). |
| Exact evidence required / validation | See evidence doc §17.7 (17 numbered local-validation commands, exact exit codes) and §17.9 (real GitHub Actions run data, both pre-R1 and post-R1). `test:runtime:unit` 74/74; `server:test:non-disposable` 68/68; `postgres`/`storage` profiles regression-clean (unaffected by the new profile); `postgres-compat` 116/117 (the 1 failure proven a Windows-local-only CRLF artifact, not an application or test defect — see §17.8 — expected and confirmed to pass on the real Linux CI run); `verify-parallel` regression collision-free, 1 Prisma generation, 0 `EBUSY`; zero residual labeled Docker resources after every run, including the one with a real test failure; `test:vitest` 35/35 (3 files, after the main-reconciliation merge added one); 7 frontend leaf scripts unchanged/passing; `npm run lint` still confirmed non-functional. |
| Tenant/security/KVKK impact | None — CI/tooling only. The new `server:test:legacy-db-required` aggregate and `postgres-compat` profile execute only already-existing, already-accepted test files against a disposable, synthetic-fixture-only PostgreSQL — no new test content, no new fixture, no production access, no credential newly introduced. KVKK physical-architecture freeze untouched, still active. |
| Rollback expectations | Single `git revert` of this task's commit(s) — removes the new aggregate script, new profile, new module, new CI job/steps, and reverts the `ci-layers.yml` prisma-generate fix and artifact-generation changes; the original P3 workflows/jobs and all P1/P2 package scripts/test-runtime tooling this task did not touch are unaffected. No database/storage/deployment/production rollback applicable. |
| Completion/status rules | This task reached `AGENT_COMPLETED`. `MERGED` requires external review and an actual merged PR plus reviewed, completed, passing GitHub Actions evidence on the post-remediation head SHA. Does not mark parent F1-003 complete, does not satisfy F1's exit gate, does not close R-046/R-070/R-071, does not declare G1/G2 approved or the KVKK baseline stable, does not start F1-003-P4. |
| Explicit non-claims | Legacy `server:test` itself is now directly invoked in CI (it is not — its members are reached indirectly via the new aggregate, per its own unchanged status); F1's exit gate satisfied; R-070/R-046/R-071 resolved; the Windows graceful-interruption limitation (F1-003-P2-R3) is newly verified (unchanged — all jobs run on `ubuntu-latest`); the post-remediation GitHub Actions run is claimed passing without having actually been inspected (see evidence doc §17.9 / the final delivery report for the real, checked result). |
| Relationship to F1-003-P4 | **F1-003-P4 remains blocked** on PR #268 being externally reviewed and merged, with real, completed GitHub Actions evidence on the post-remediation head SHA reviewed — not started, not authorized to start by this entry. |
#### F1-003-B2 — Retention Manual/Scheduled Run Mutual-Exclusion Race

| Field | Value |
|---|---|
| Task ID | F1-003-B2 |
| Title | Retention Manual/Scheduled Run Mutual-Exclusion Race |
| Type | Blocking production-behavior defect, discovered by **F1-003-P3's** own Layer 5 compatibility CI (PR #268, `feature/f1-003-p3-layered-ci-workflows`) — **PR #268 left completely open, unmodified, and blocked by this task.** Fresh isolated worktree/branch `fix/f1-003-b2-retention-run-lock-race`, execution baseline `origin/main` @ `70b1690c1a656c95cead7b42812cc9ae6447bfb7`, not the F1-003-P3 worktree, zero intervening commits. |
| Defect | GitHub Actions run `30542271302` (PR #268), job `ci-layers / Layer 5: full-suite/compatibility fail-safe (backend, legacy server:test DB-required members)`: `server/src/tests/retentionManualRunAudit.test.ts`'s concurrent-manual-live-runs test failed — `actual=[200,200]` vs `expected=[200,409]` (`Toplam: 20 ✓ 19 ✗ 1`). The immediately-following scheduled/manual-collision test in the same run passed. Reproduced locally against a real disposable PostgreSQL container: pre-fix code passed 9/9 consecutive full-suite runs, consistent with a narrow, CI-runner-timing-dependent race, not a deterministic failure. |
| Root cause | `server/src/utils/jobLock.ts`'s `acquireJobLock`/`releaseJobLock` (shared by the manual route and the scheduled cron via the identical lock key `'data-retention-cleanup'`, `JobLock.name` genuinely the Prisma `@id` primary key per `migrations/20260706150000_add_job_lock/migration.sql`) is atomic and race-free — proved empirically, not assumed: 130 concurrent-acquisition trials (2-way and 4-way, create-path and update-path) against a real disposable PostgreSQL container using the exact production Prisma `7.8.0` + `@prisma/adapter-pg` driver-adapter stack, **0 double-grants**. The actual defect: `server/src/routes/platformAdmin.ts`'s `POST /privacy/data-retention/run` acquired the shared lock only *after* two other variable-latency DB round trips both racers perform independently (`getPlatformSetting` policy-gate read, `recordManualRunStarted` durable audit-write transaction). Because live-run cleanup against a small/disposable dataset completes in single-digit milliseconds, a fast racer's entire acquire→cleanup→release cycle could finish before a simultaneously-`Promise.all`-fired second racer even reached its own lock attempt — the second racer then legitimately (sequentially, not concurrently) acquired the now-free lock too, and both returned `200`. **Not a lock-atomicity flaw; a genuine application-contract violation, not a test-scheduling assumption** — CodeGraph unavailable (consistent with every prior program task), bounded `Read`/`Grep` used, scoped to `jobLock.ts`, `platformAdmin.ts`'s retention route, `dataRetentionCleanupJob.ts`, and the failing test file. |
| Chosen remediation | Smallest correction to the existing distributed `JobLock` contract — reorder, not replace. `acquireJobLock`/`releaseJobLock` exported from `jobLock.ts` (previously private, reachable only through `withJobLock`); `withJobLock` itself untouched, still used unchanged by the scheduled cron and every other job in the codebase. The manual route's live-run branch now calls `acquireJobLock()` **first**, before either audit write, making the lock attempt the very first DB operation of the "genuine attempt" path (after the one unavoidable, identical-shape `getPlatformSetting` read both dry-run and live paths already require). Both the winner and the loser still write their own `manual_run_started` row (existing, already-accepted audit-shape contract preserved exactly); the loser branches immediately to `manual_run_blocked`/`concurrent_run_in_progress` + `409`, zero cleanup invoked. Cleanup wrapped in `try { ... } finally { await releaseJobLock(...) }` — released on success, thrown error, or any other exit. A **new** edge case introduced by the reordering (lock acquired, then the started-write itself fails) now releases the lock before the `500` response, preventing a newly-possible 2-hour TTL orphan that could not have occurred under the old ordering. No new locking subsystem, no Redis/BullMQ/Kafka, no process-local mutex, no sleep, no schema/migration change. |
| Tests | 5 new + 1 strengthened in `server/src/tests/retentionManualRunAudit.test.ts` (20→25 assertions, all against real PostgreSQL, no mocked Prisma): (1) a new **10-consecutive-round in-test race gate** repeating the exact `Promise.all`-fired 2-way concurrent live-run scenario, asserting `[200,409]` and exactly-one-deletion every round; (2) 3 new tests exercising the exported `acquireJobLock`/`releaseJobLock` directly — 4-way truly-concurrent acquisition on a fresh key (exactly one grant), acquire→release→re-acquire (no orphaning after success), acquire→simulated-throw-inside-the-route's-exact-try/finally-shape→release (no orphaning after a thrown exception); (3) 1 new test for the lock-acquired-then-started-write-fails edge case (FK-violation injection, same technique as the pre-existing analogous test), confirming zero audit rows left behind **and** the lock released immediately (re-verified by successfully re-acquiring it); (4) the existing scheduled/manual-collision test strengthened with a seeded-row-untouched assertion and a lock-ownership-untouched assertion. No test skipped, quarantined, retried-until-green, or weakened; no sleeps added; no test-only serialization introduced. |
| Results | `npm run test:retention-manual-run-audit` run **8 consecutive times** post-fix: **25/25 every time** (80 total race-gate rounds embedded, 0 double-successes, 0 double-blocks) — combined with the isolated primitive stress test, **210+ total concurrent-acquisition trials, zero races**. Also: `npm run typecheck` (0 errors), `npm run test:data-retention` (42/42, unaffected — exercises the cleanup function via injected deps, not the route/lock), `npm run server:test:non-disposable` (exit `0`, 0 failure markers across the combined log), root `npm run test:runtime:postgres` (exit `0`, `server:test:disposable-db` exit `0`, cleanup succeeded), `git diff --check` clean. `docker ps -a`/`docker network ls --filter label=com.noramedi.test-runtime=true` show zero residual resources after every disposable run this task performed. |
| `server:test:legacy-db-required` note | This script/aggregate does not exist on `origin/main` — defined only inside PR #268's (not merged) `.github/workflows/ci-layers.yml`. Per instruction, the P3 CI aggregate was not copied onto this branch; the exact failing test was instead run directly against a disposable PostgreSQL provisioned the same way F1-003-P2's accepted runtime provisions one (same digest-pinned image, same `prisma migrate deploy` step), as a bounded one-off command. |
| Allowed scope (honored) | `server/src/utils/jobLock.ts`, `server/src/routes/platformAdmin.ts`, `server/src/tests/retentionManualRunAudit.test.ts`; this file, `CURRENT_PHASE.md`, `NORAMEDI_MASTER_TRACKER.md`, `evidence/README.md`, `evidence/F1-003-B2_RETENTION_RUN_MUTUAL_EXCLUSION_RACE.md` (new), `evidence/F1-003-B2_retention_run_mutual_exclusion_race.json` (new); a new isolated worktree/branch/PR. |
| Prohibited scope (honored) | No schema/migration file. No CI workflow file. No new locking subsystem/Redis/BullMQ/Kafka. No process-local mutex. No sleep-based test fix. PR #268 (F1-003-P3) not modified, not merged, not closed — referenced only for its own discovery-lineage evidence. |
| Migration status | No schema/migration creation. No production DB access. No `_prisma_migrations` change. No rollback performed. The existing `JobLock` model/table (migration `20260706150000_add_job_lock`) is reused exactly as-is. |
| Security/tenant/KVKK impact | Lock scope remains **global/job-type-scoped** (one row for `'data-retention-cleanup'`, not tenant-scoped) — correct, since a single live retention-cleanup pass spans all tenants in one run (unmodified no-cross-tenant-leakage test confirms this). Audit trail fully preserved for every branch, including both new edge cases — no audit row newly lost or duplicated. Closes a real (never observed to cause actual double-deletion — the already-atomic lock always prevented true simultaneous overlap) concurrent-manual-trigger risk directly relevant to retention operational integrity. Crash/connection-loss behavior relies on the pre-existing, unchanged 2-hour TTL lease-expiry mechanism, not independently re-verified by this task — no crash guarantee claimed beyond what is directly tested. |
| Rollback | Single `git revert` of this task's commit fully restores the pre-fix ordering — no data migration, no schema change, no lock-table cleanup required (the `JobLock` row format is identical before and after). |
| Completion/status rules | This task reaches at most `AGENT_COMPLETED`. Does not merge PR #268, does not close R-046/R-070/R-071, does not declare G1/G2 approved, does not change the KVKK freeze or baseline-stable declaration. Maximum status: `AGENT_COMPLETED` / `PR_OPENED_AWAITING_REVIEW`. |
| Relationship to F1-003-P3 | **F1-003-P3/PR #268 remains open, blocked, and unmerged** — this task's own fix must itself be externally reviewed and merged first; the exact next task after that is returning to PR #268 for F1-003-P3-R2 main reconciliation and full-green CI closure. |

#### F1-003-B2-R1 — Admission-Point Lock Closure, Current-Main Reconciliation, Remote CI Verification, and Evidence Sanitization

| Field | Value |
|---|---|
| Task ID | F1-003-B2-R1 |
| Title | Admission-Point Lock Closure, Current-Main Reconciliation, Remote CI Verification, and Evidence Sanitization |
| Continuation | Continued on the existing open branch `fix/f1-003-b2-retention-run-lock-race` / PR #268's sibling PR [#280](https://github.com/MustafaBasol/DisKlinikCRM/pull/280) — no new branch, no new PR, not merged, not rebased, not force-pushed. |
| Main reconciliation | Fetched `origin/main` (`e8e05f669f049c4895e0c9a7f95db6f3fbd463fe`, PR #277 merge commit); confirmed PR #276 (`0de9a04a8c7b4fefe5e7f525f9cab031b55fcb83`) and PR #277 merge commits are ancestors of `origin/main`; `git merge --no-ff origin/main` — **zero conflicts**, only 4 new additive F2-PREP-001/003 evidence files introduced, no schema/migration change; merge commit `2fc2d9b1985a33125d14251bca6077c149f589c4`; post-merge `git merge-base --is-ancestor origin/main HEAD` confirmed true. |
| Residual defect closed | F1-003-B2's own evidence (§9) explicitly documented one remaining gap: the live-run route still called `getPlatformSetting` (policy/runtime-toggle read) *before* `acquireJobLock`, so a sufficiently delayed second concurrent request could in principle still reach admission after the first had already completed and released it — a residual race the original fix's own author flagged as unsolved, not something newly discovered by CI. |
| Chosen remediation | Route body extracted into an exported factory, `createDataRetentionRunHandler(overrides?)` (`server/src/routes/platformAdmin.ts`), so tests can inject a controllable `getPlatformSetting`; production always uses the real dependency. New required order for a live attempt: (1) acquire the shared lock — now the very first variable-latency DB operation; (2) if unavailable, read policy (metadata only) and write the required `started`+`blocked` audit pair, `409`, no cleanup; (3) if acquired, read policy and evaluate the runtime/policy gate while still holding the lock; (4) if the gate rejects, release the lock and preserve the exact pre-existing policy-block response/audit shape (`403`, single terminal-only row); (5) if the policy read itself throws, release the lock if held and fail safely with a sanitized `500` and no fabricated audit row (no valid metadata could ever be built); (6) write the `started` row, releasing the lock and failing safely if that write itself fails; (7) run cleanup inside `try { ... } finally { releaseJobLock(...) }`; (8) record the terminal outcome; (9) the lock is released on every code path. Lock key, TTL, and scheduled-cron locking are all unchanged; no new locking subsystem, no process-local mutex, no sleep. `releaseJobLock` (`jobLock.ts`) now logs (previously fully silent) when its own underlying DB release update genuinely fails — documented and tested as a deliberate trade-off: it still never rejects (an already-completed cleanup's success must never be retroactively reported as a failure), so the lock is honestly left held until its own TTL expires rather than silently cleared. |
| Deterministic concurrency test | Not a repeated `Promise.all` timing-luck loop (that remains supplementary). A new test fires two concurrent live requests through a `getPlatformSetting` override that immediately signals (resolves a per-call `Deferred`) the instant it is entered — proving the calling racer has already completed `acquireJobLock` — then blocks on its own `Deferred` (never a `setTimeout`/sleep) until the test explicitly releases it. While both racers are deliberately parked mid-policy-lookup, the test queries the `JobLock` row directly and asserts exactly one row is held — admission is fully decided *before either policy lookup is allowed to resolve at all* — then releases both barriers and asserts the final result is still exactly `[200, 409]`. This proves the required property holds regardless of policy-lookup latency, not merely under favorable timing. |
| Tests | 4 new tests in `server/src/tests/retentionManualRunAudit.test.ts` (25→29 assertions, all against real PostgreSQL): the deterministic admission proof above; "policy disabled after live lock acquisition" (`403`, no cleanup, lock immediately reusable); "policy lookup throws after lock acquisition" (no cleanup, lock released, sanitized `500`, no fabricated audit row); `releaseJobLock()`'s genuine-DB-failure trade-off (forced via the same table-rename fault-injection technique used by the pre-existing started/terminal-audit-write-failure tests — never rejects, lock honestly remains held). Two required failure branches ("started-write fails", "cleanup throws") were already covered by F1-003-B2's own suite and remain passing unmodified. |
| Results | `npm run typecheck` 0 errors; `npm run test:retention-manual-run-audit` `29/29`, run **6 consecutive times, 29/29 every time**; `npm run test:data-retention` `42/42` (unaffected); `npm run server:test:non-disposable` exit `0`, 0 failure markers; root `npm run test:runtime:postgres` exit `0`, cleanup succeeded; `git diff --check` clean; zero residual `com.noramedi.test-runtime=true` Docker containers/networks after every disposable run. |
| Remote CI | Pushed the code commit to the existing branch/PR. `gh pr checks 280` → **"no checks reported on the 'fix/f1-003-b2-retention-run-lock-race' branch."** `gh pr view 280 --json statusCheckRollup` → `{"statusCheckRollup": []}`. `gh run list --branch ...` → exactly one run, an unrelated Copilot code-review bot (`completed`/`success`), not a test workflow. **Diagnosed, not assumed:** `origin/main` currently has only `windows-bridge-pr.yml` (`pull_request`-triggered but `paths:`-filtered to imaging-bridge paths this PR does not touch) and `windows-bridge-release.yml` (`workflow_dispatch`-only, never auto-triggers); the layered CI system that would actually run `test:retention-manual-run-audit` in CI (`ci-layers.yml`/`ci-pr.yml`/`ci-main-and-nightly.yml`) exists only on PR #268 (F1-003-P3), not yet merged to `main` — a genuine, pre-existing structural gap this task is not authorized to fix. **`REMOTE_CI_VERIFIED` is explicitly NOT claimed** anywhere in this task's evidence; every claim above is `TEST_REPRODUCED` (local, real disposable PostgreSQL) or `CODE_PATH_CONFIRMED` (direct source read). |
| Evidence sanitization | Searched all task-owned evidence/docs for author-machine absolute paths (`D:\`, `D:/`, `C:\`, `C:/`, `E:\`, `E:/`, `DisKlinikCRM-worktrees`, local usernames/home paths). Found and corrected exactly one occurrence — `F1-003-B2_RETENTION_RUN_MUTUAL_EXCLUSION_RACE.md`'s "Branch/worktree" field, replaced with "fresh isolated worktree; local filesystem path intentionally omitted" (branch name and baseline SHA preserved). No occurrence existed in this task's own newly-authored files. Unrelated historical evidence files elsewhere in the repository (dozens, from prior unrelated task lineages) were left untouched — out of this task's scope. F1-003-B2's own evidence §9 residual-limitation bullet marked `[SUPERSEDED 2026-08-02 by F1-003-B2-R1]`, preserved verbatim, not silently rewritten. |
| Allowed scope (honored) | `server/src/routes/platformAdmin.ts`, `server/src/utils/jobLock.ts`, `server/src/tests/retentionManualRunAudit.test.ts`; `evidence/F1-003-B2_RETENTION_RUN_MUTUAL_EXCLUSION_RACE.md` (§9 superseded note only), `evidence/F1-003-B2-R1_ADMISSION_LOCK_CLOSURE.md` (new), `evidence/F1-003-B2-R1_admission_lock_closure.json` (new), this file, `CURRENT_PHASE.md`, `NORAMEDI_MASTER_TRACKER.md`, `evidence/README.md`, PR #280 description; the existing branch/PR. |
| Prohibited scope (honored) | No new branch, no new PR. No schema/migration file. No new locking subsystem/Redis/BullMQ/Kafka. No process-local mutex. No sleep. PR #268 not modified, not merged, not closed. |
| Migration status | No schema/migration creation, by this task or by the `origin/main` reconciliation merge. No production DB access. No `_prisma_migrations` change. No rollback performed. |
| Security/tenant/KVKK impact | No tenant-scoped behavior change; lock remains global/job-type-scoped, correctly. Audit trail preserved for every branch — no row fabricated when required inputs are unavailable. Closes the last theoretical (never empirically observed, in either task's own testing) admission-timing gap; does not change what data is eligible for cleanup. |
| Rollback | Single `git revert` of this task's commit fully restores F1-003-B2's own ordering (lock before "started" write, but after the policy read) — no data migration, no schema change, no lock-table cleanup required. |
| Completion/status rules | This task reaches at most `AGENT_COMPLETED`. Does not merge PR #280, does not close R-046/R-070/R-071, does not declare G1/G2 approved, does not claim `REMOTE_CI_VERIFIED` or `PRODUCTION_VERIFIED`. Maximum status: `AGENT_COMPLETED` / `PR_OPENED_AWAITING_REVIEW`. |
| Relationship to F1-003-P3 | **F1-003-P3/PR #268 remains open, blocked, and unmerged, confirmed unchanged by this task.** Exact next task: after PR #280 is reviewed and merged, return to PR #268 for F1-003-P3-R2 main reconciliation and full-green CI closure. |

## Required evidence (Gerekli kanıt)

- CI çalıştırma kayıtları (PR'da etkilenen-kapsam kanıtı, nightly tam kapsam kanıtı)
- Süre ölçümleri (öncesi/sonrası)

## Required tests (Gerekli testler)

- CI modelinin kendisinin doğrulanması: bilinçli değişikliklerle tetikleme testleri

## Security requirements (Güvenlik gereksinimleri)

- CI secret yönetimi; log'lara secret sızmaması

## Tenant requirements (Tenant gereksinimleri)

- Tenant izolasyon regresyonunun her PR'da zorunlu koşması

## KVKK/privacy requirements (KVKK/gizlilik gereksinimleri)

- Test verilerinde gerçek hasta verisi kullanılamaz

## Rollback expectations (Geri alma beklentileri)

- CI değişiklikleri workflow dosyası revert'iyle geri alınabilir olmalı

## Risks (Riskler)

- R-024 (migration hatası), R-027 (branch sapması), R-028 (yanlış tamamlandı beyanı)

## Open questions (Açık sorular)

- Etki-bazlı seçim mekanizmasının aracı (yol-bazlı mı, graph-bazlı mı) — F1-001 (2026-07-28) bu kararı bilinçli olarak gelecekteki bir implementasyon görevine erteledi (statik anlık görüntü ile canlı graph sorgusu arasındaki farklı bayatlama-hata modları nedeniyle); bkz. [F1-001_IMPACT_BASED_TEST_SELECTION_ARCHITECTURE.md](../architecture/F1-001_IMPACT_BASED_TEST_SELECTION_ARCHITECTURE.md) §12 açık soru 1. Hâlâ açık, F1-002 tarafından da çözülmedi (F1-002'nin kapsamı test envanteri/sahiplik/script-erişilebilirliği yenilemesidir, bu mimari kararı değil).
- F1-001'in §7 madde 1'de belirttiği "implementasyon görevinin kendi taban çizgisine karşı yenilenmiş F0-005-eşdeğeri test envanteri" ön koşulu — **F1-002 tarafından karşılandı** (2026-07-28, taban çizgisi `08f2eaf82a205cf3f997c57e6a295fedd66b142d`, bkz. [F1-002_test_inventory.json](../evidence/F1-002_test_inventory.json)), F1-002-P1/P2 tarafından ayrıca derinleştirildi. F1-001'in §7'deki diğer 3 ön koşul hâlâ açık: path-vs-graph kararı, disposable-Postgres mekanizması (F1-002-P2 tarafından 10 soruluk bir değerlendirmeyle somutlaştırıldı, hâlâ implemente edilmedi — bkz. [F1-002-P2_disposable_environment_capabilities.json](../evidence/F1-002-P2_disposable_environment_capabilities.json)), CodeGraph durumu (F1-002-P1/P2'de de kullanılamadı, değişmedi). **F1-001'in kendi dış incelemesi/merge'i artık tamamlandı** (PR #241, `MERGED`, 2026-07-28T07:22:15Z) — bu madde artık açık değil.

## Change history (Değişiklik geçmişi)

| Tarih | Görev | Değişiklik |
|---|---|---|
| 2026-07-17 | F0-001 | Faz dokümanı oluşturuldu (yüksek seviyeli). |
| 2026-07-25 | F0-014 | G0 `APPROVED_WITH_CONDITIONS` externally granted; entry conditions satisfied; faz durumu `TODO`→`IN_PROGRESS`; first concrete task F1-001 assigned (`READY`, not executed). |
| 2026-07-28 | F1-001 | Design delivered (`AGENT_COMPLETED`, PR opened, not merged): impact-based test-selection architecture, classification taxonomy, risk model, escalation/fail-safe rules, shadow-mode rollout, 17 scenarios. Resolves this document's own "Open question" (below) by deferring the static-vs-live-graph implementation choice to a future task, with rationale. New risk R-072 (test-inventory evidence staleness) recorded in `RISK_REGISTER.md`. Faz durumu remains `IN_PROGRESS`; exit gate NOT satisfied by this task. |
| 2026-07-28 | F1-002 | Test inventory refreshed and ownership reconciled (`AGENT_COMPLETED`, PR #253 opened, not merged): 137 test/verification targets inventoried (up from F0-005's 100), 0 UNKNOWN ownership, package scripts fully reconciled (0 stale references, 8 never-scripted backend files identified), R-072 closure proposed. Faz durumu remains `IN_PROGRESS`; exit gate NOT satisfied by this task; F1-003 not started. |
| 2026-07-28 | F1-001 (external) | PR #241 externally reviewed and `MERGED` (merge commit `94cc4ac58f0487dd186886878c5628627f0b1ce3`, 2026-07-28T07:22:15Z). |
| 2026-07-28 | F1-002-P1 (external) | PR #255 (Test Script Reachability and Runner Reconciliation) externally reviewed and `MERGED` (merge commit `954168ed4399f572e776fbfd30fa391aad574610`). |
| 2026-07-28 | F1-002-P2 (external) | PR #254 (Test Runtime Dependency and Disposable Environment Baseline) externally reviewed and `MERGED` (merge commit `035d4db59cf429777a4bb71a5e72203f6c99b571`). |
| 2026-07-28 | F1-002-R1 | Parallel Evidence Integration and Main Reconciliation: F1-002 branch (PR #253) merged with `origin/main` (`--no-ff`, no conflicts, evidence-only diff), F1-002-P1/P2 evidence indexed into `TEST_OWNERSHIP.md`, this document, and the tracker; count/script-chain/runtime-dependency vocabulary reconciled against P1/P2's independently-derived numbers (17, not the previously-stated 16, scripts excluded from the aggregate chain; frontend CI-uncovered corrected 7/9→6/9); stale "F1-001 not yet merged" statements corrected. Faz durumu remains `IN_PROGRESS`; exit gate NOT satisfied; R-072 remains `CLOSURE_PROPOSED_AWAITING_EXTERNAL_CONFIRMATION`, not `CLOSED`; F1-003 not started. |
| 2026-07-28 | F1-002-R2 | External R-072 Closure Recording and Final PR Metadata Correction: external architecture review (ChatGPT) + Mustafa Basol's merge decision granted closure confirmation for R-072 → `CLOSED` (closure date 2026-07-28, basis F1-002+P1+P2 evidence, narrow scope — 8 unscripted tests/17 chain-excluded scripts/7-of-116 CI coverage/disposable-Postgres+MinIO provisioning/collision avoidance/1 runtime UNKNOWN/unimplemented selector/R-070 all remain unresolved). **F1-003 — Baseline CI Test Execution and Disposable Runtime Readiness assigned, `READY, not started`** (see full definition above). PR #253 description corrected (16→17 chain-excluded scripts figure, P1/P2 merged-and-integrated statement, R-072 CLOSED statement, no F1-complete/CI-readiness claim). Faz durumu remains `IN_PROGRESS`; exit gate NOT satisfied by this task. |
| 2026-07-28 | F1-003-P1 | Test Script Closure and Authoritative Execution Contract (`AGENT_COMPLETED`, PR opened, not merged): 8 unscripted backend test files scripted (post-task UNSCRIPTED count 0); 17 aggregate-chain-excluded scripts reclassified using the required vocabulary (DB-required group corrected to 10, no-apparent-reason group corrected to 7 — a prose-only F1-002-P1 miscategorization of `test:platform-admin-password-recovery` corrected); `messagesConsentGate.test.ts`'s runtime `UNKNOWN` resolved to `DISPOSABLE_POSTGRES + EXTERNAL_PROVIDER_MOCK`, confidence `HIGH`; 3 new additive `server/package.json` aggregates added (`server:test:non-disposable` 68 members executed exit 1 sole known failure, `server:test:disposable-db` 9 members structurally validated only, `server:test:storage-integration` 1 member structurally validated only); legacy `server:test` left completely unmodified, its own 23 silently-DB-required members documented as an explicit unresolved finding for F1-003-P2. One additional correction found (`platformBackup.test.ts`, incorrectly carried-forward DB-required classification, corrected via direct read + direct execution). Machine-readable 114-record execution contract + narrative evidence doc produced. Faz durumu remains `IN_PROGRESS`; exit gate NOT satisfied; parent F1-003 not fully complete (P2/P3/P4 not started); F1-003-P2 next, not started. |
| 2026-07-28 | F1-003-R1A | Narrow canonical-evidence correction (documentation-only, no application/test/schema/CI file touched): F1-003-R1 found the F1-003-P1 claim above — that legacy `server:test` silently chains "23 DB-required + 1 MinIO-required" members (`test:kvkk-lifecycle`) — to be an objective factual error. `test:kvkk-lifecycle`'s S3 provider is fully mocked in every path the test exercises; no live MinIO/S3 endpoint is ever contacted. Corrected: legacy `server:test` is PostgreSQL-required, MinIO-free (23 DB-required members, 0 additionally MinIO-required); `test:kvkk-lifecycle` itself is PostgreSQL-required only. `test:file-backup-db-integration` remains the sole genuine MinIO-dependent target program-wide. P1's historical `AGENT_COMPLETED` status, execution counts, and all other findings are unchanged and preserved — see [evidence/F1-003-P1_TEST_SCRIPT_CLOSURE_AND_EXECUTION_CONTRACT.md](../evidence/F1-003-P1_TEST_SCRIPT_CLOSURE_AND_EXECUTION_CONTRACT.md) §18 for the full correction record. Faz durumu remains `IN_PROGRESS`; exit gate NOT satisfied; parent F1-003 not fully complete; R-070 unaffected (still `OPEN`); F1-003-P2 not started. |
| 2026-07-28 | F1-003-P1 (external) | PR #257 externally reviewed and `MERGED`. |
| 2026-07-29 | F1-003-B1 | Overdue Installments Baseline Drift Resolution (`AGENT_COMPLETED`, PR opened, not merged; ran in parallel with F1-003-R1/PR #256, did not touch its branch or worktree): resolved the sole known cause of `server:test:non-disposable` exit 1 — corrected 2 stale assertions + docstring in `server/src/tests/overdueInstallments.test.ts` (Case A, stale test; product code `overdueInstallments.ts` unchanged, matches `overdueReceivables.test.ts`'s already-green contract). `test:overdue-installments` now 9/9 (was 7/9); `server:test:non-disposable` now exit `0` (was exit `1`), verified twice, byte-identical output. No application/schema/migration/CI/Docker/Compose file changed beyond the one test file. Faz durumu remains `IN_PROGRESS`; exit gate NOT satisfied; parent F1-003 not fully complete; F1-003-P2/P3/P4/R1/P2A status unchanged by this task. |
| 2026-07-28 | F1-003-B1 (external) | PR #258 externally reviewed and `MERGED` (merge commit `bb4186793323485c71b91ad6939a0d8469f886cd`, mergedAt `2026-07-28T22:13:04Z`). |
| 2026-07-29 | F1-003-R1B | Post-B1 Main Reconciliation and Status Refresh (documentation-only, on open PR #256, not merged; no new PR, no rebase, no force-push): `git merge --no-ff origin/main` reconciled PR #256 with `origin/main` after F1-003-B1's merge — 3 documentation conflicts (`CURRENT_PHASE.md`, `NORAMEDI_MASTER_TRACKER.md`, `evidence/README.md`) resolved manually/semantically, no blanket `--ours`/`--theirs`. Preserved both F1-003-B1's merged status (exit `0`, 9/9, PR #258) and F1-003-R1A's MinIO correction (`test:kvkk-lifecycle` PostgreSQL-required/MinIO-free) unchanged. F1-003-P1's historical exit-`1`/7-of-9 result preserved as historical evidence, explicitly marked superseded, not silently rewritten. Corrected inaccurate "P2A design already merged" wording — P2A/R1/R1A remain inside open PR #256, only P1 (PR #257) and B1 (PR #258) are merged. F1-003-B1 removed from F1-003-P3's blocker list; P3 remains blocked on F1-003-P2's unstarted provisioning/collision-avoidance scope. No test executed by this task (B1's own evidence cited, not re-run). Faz durumu remains `IN_PROGRESS`; exit gate NOT satisfied; parent F1-003 not fully complete (P1/B1 merged, P2/P3/P4 not started); R-070 confirmed still `OPEN`; G1/G2 remain `NOT_APPROVED`. |
| 2026-07-29 | F1-003-P2 | Disposable PostgreSQL and MinIO Provisioning and Collision Avoidance (`AGENT_COMPLETED`, PR #260 opened, not merged): implemented the full F1-003-P2A/R1 contract (§K/§L) — shared TypeScript orchestrator + PowerShell/Bash wrappers + stale-resource sweeper under `scripts/test-runtime/`, 6 additive root `package.json` scripts, `server/package.json` byte-for-byte unchanged. All four profiles (`postgres`/`storage`/`verify-parallel`/`cleanup-stale`) **live-verified against a real Docker Desktop engine**: `postgres` — PostgreSQL 16.14 (`postgres:16-alpine`), migration exit `0`, `server:test:disposable-db` (9 members) exit `0`; `storage` — additionally MinIO `RELEASE.2025-04-08T15-41-24Z` (digest-pinned), `server:test:storage-integration` exit `0` (21/21 assertions); `verify-parallel` — 2×postgres + 2×storage concurrent, run twice, zero collisions and zero labeled resources left behind both times; all 6 mandatory failure-injection scenarios reproduced with genuine failure signals; stale sweeper verified dry-run/`--live` against fixtures. Three real bugs found and fixed by this task's own verification (guard run-ID/db-name normalization, Windows `spawn EINVAL` on `npm.cmd`/`npx.cmd`, `docker rm -f` idempotency requiring a redesigned cleanup-failure-injection technique) — reported transparently in the evidence document. Legacy `server:test`'s 23 DB-required members explicitly DEFERRED, not provisioned. No `.github/workflows/**`, no application/schema/migration file, no deployment, no production access. `server:test:non-disposable` re-confirmed exit `0` (68/68) — no regression. R-070 confirmed still `OPEN`. Full evidence: [evidence/F1-003-P2_DISPOSABLE_RUNTIME_IMPLEMENTATION_AND_VERIFICATION.md](../evidence/F1-003-P2_DISPOSABLE_RUNTIME_IMPLEMENTATION_AND_VERIFICATION.md). Faz durumu remains `IN_PROGRESS`; exit gate NOT satisfied; parent F1-003 not fully complete (P1/B1 merged, P2 agent-completed/PR open, P3/P4 not started); G1/G2 remain `NOT_APPROVED`. **Historical note (superseded 2026-07-29 by F1-003-P2-R1 below): this entry's "production PostgreSQL major-version parity remains unconfirmed" framing predates F1-003-P2V's merge and is preserved here verbatim as originally written, not rewritten.** Exact next task: F1-003-P3, blocked until this PR is externally reviewed and merged. |
| 2026-07-29 | F1-003-P2V-R1 | Active P2 Status Reconciliation (documentation-only, continued on existing PR #259, no new PR, not merged, not rebased, not force-pushed): corrected a factual contradiction in F1-003-P2V's own text — `git worktree list` (already recorded by F1-003-P2V §2) showed the isolated F1-003-P2 worktree exists with pre-existing, uncommitted/unverified implementation work under `scripts/test-runtime/`, yet other F1-003-P2V wording stated F1-003-P2 was simply "not started". Canonical status corrected to **F1-003-P2 — `AGENT_WORK_IN_PROGRESS`** (isolated worktree exists; unverified implementation observed; no delivery report/validation/commit/PR/merge/deploy/production-verification evidence). P2 worktree itself was not entered, read, diffed, or modified by this reconciliation. PostgreSQL conclusion (major `16`, exact `16.14`, major-only parity) unchanged. Faz durumu remains `IN_PROGRESS`; exit gate NOT satisfied; parent F1-003 not fully complete; F1-003-P3 remains blocked until F1-003-P2 is delivered, reviewed, and merged; R-070/R-046 unchanged, both `OPEN`. |
| 2026-07-29 | F1-003-P2V (external) | PR #259 externally reviewed and `MERGED` (merge commit `8592a570708c6308d7a19aff703db6e0a699ece7`, mergedAt `2026-07-29T07:57:11Z`). Authoritative on `origin/main`: production PostgreSQL `16.14`, major version `16`, F1-003-P2 major-version parity CONFIRMED, exact image/build/package parity NOT claimed. |
| 2026-07-29 | F1-003-P2-R1 | Post-P2V Main Reconciliation, PostgreSQL Parity Correction, and Image-Pinning Verification (documentation + one focused source change, continued on existing open PR #260, no new PR, not merged, not rebased, not force-pushed): merged `origin/main` into `feature/f1-003-p2-disposable-runtime` after confirming PR #259's merge commit is an ancestor of `origin/main`; resolved 3 documentation conflicts (`CURRENT_PHASE.md`, `evidence/README.md`, this file) semantically, preserving both F1-003-P2's implementation evidence and F1-003-P2V/P2V-R1's authoritative PostgreSQL-parity evidence; corrected this lineage's own stale "parity remains unconfirmed" wording (superseded, not silently rewritten — see the F1-003-P2 row above and its table's "Explicit non-claims" row). **Image-pinning verified and corrected:** PostgreSQL's `POSTGRES_IMAGE` switched from the floating tag `postgres:16-alpine` to the digest-pinned reference `postgres@sha256:57c72fd2a128e416c7fcc499958864df5301e940bca0a56f58fddf30ffc07777` (Option A); MinIO's `MINIO_IMAGE_REF` was already digest-pinned (`tag@sha256:digest`) and required no change. All real profiles rerun against the new PostgreSQL reference (see this task's own evidence-document update for exact results). **`verify-parallel` reruns discovered a real Windows `EBUSY` in concurrent `prisma generate` on the 2nd of 3 reruns — see F1-003-P2-R2 below, which fixes this.** Faz durumu remains `IN_PROGRESS`; exit gate NOT satisfied; parent F1-003 not fully complete (P1/B1 merged; P2 `AGENT_COMPLETED`/PR #260 open; P3/P4 not started); R-070 confirmed still `OPEN`; G1/G2 remain `NOT_APPROVED`. Maximum status: `AGENT_COMPLETED` / `PR_OPENED_AWAITING_REVIEW`. Exact next task: F1-003-P3, blocked until PR #260 is externally reviewed and merged. |
| 2026-07-30 | F1-003-P2 (external) | PR #260 externally reviewed and `MERGED` (merge commit `04723f9add600b6c432fb517f0607235944d6c2e`, mergedAt `2026-07-30T09:45:38Z`) — includes F1-003-P2-R1/R2/R3. Unblocks F1-003-P3. |
| 2026-07-30 | F1-003-P3 | Layered CI Workflows and Full-Suite Fail-Safe (`AGENT_COMPLETED`, PR opened, not merged): baseline-verified PR #260 merged (`gh pr view 260`: `MERGED`; `git merge-base --is-ancestor` exit `0`; fetched `origin/main` IS the PR #260 merge commit). Implemented one reusable workflow `ci-layers.yml` (8 jobs / 5 layers: static typecheck+build+lint, `server:test:non-disposable`, the unmodified P2 `postgres`/`storage` orchestrator profiles, and a full-suite fail-safe covering the 7 previously-CI-unreached frontend leaf scripts + `test:vitest`) plus two thin triggers `ci-pr.yml`/`ci-main-and-nightly.yml`. `windows-bridge-pr.yml`/`windows-bridge-release.yml` untouched. No `scripts/test-runtime/**`, `server/package.json`, or root `package.json` file modified — a considered `--full-suite` orchestrator extension (to also cover legacy `server:test`'s ~14 additional DB-required members) was deliberately not implemented as outside this task's scope; that gap remains an explicit, continued deferral from F1-003-P1/P2, not silent. Actions pinned by SHA (reused, independently re-verified from `windows-bridge-pr.yml`); actionlint v1.7.12 pinned by checksum, wired as a CI step. Local validation: `test:runtime:unit` 68/68; `server:test:non-disposable` 68/68; `test:runtime:postgres` exit `0` (9/9, cleanup clean); `test:runtime:storage` exit `0` (21/21, cleanup clean); `test:runtime:parallel` regression exit `0` (collision-free, 1 Prisma generation, 0 `EBUSY`, zero residual resources — proves no orchestrator-contract change); 7 leaf scripts + `test:vitest` all passing; `npm run lint` confirmed genuinely non-functional (no `eslint.config.js`), not wired into CI. No Prisma schema/migration/application/production change. R-070 confirmed still `OPEN`. Faz durumu remains `IN_PROGRESS`; exit gate NOT satisfied; parent F1-003 not fully complete (P1/B1/P2 merged; P3 agent-completed/PR open; P4 not started); G1/G2 remain `NOT_APPROVED`. Maximum status: `AGENT_COMPLETED` / `PR_OPENED_AWAITING_REVIEW`. Exact next task: F1-003-P4, blocked until this PR is externally reviewed and merged with real GitHub Actions evidence reviewed. |
| 2026-07-30 | F1-003-P2-R2 | Parallel Prisma Generation Race Elimination and Deterministic Runtime Verification (continued on existing open PR #260, no new PR, not merged, not rebased, not force-pushed): fixed the EBUSY race F1-003-P2-R1 discovered — root cause was `runMigrations()` running `npx prisma generate` unconditionally on every disposable-profile invocation against the shared `server/node_modules/.prisma/client` output; `verify-parallel`'s storage pair (2-way concurrency) raced two such concurrent calls. **Option A applied**: generate once in the parent orchestrator before fan-out (new `lib/prismaGeneration.ts`, single-process authorization token via `node:crypto`), children skip their own generate only with a valid token, fail-safe fallback otherwise. 9 new Docker-free unit tests (59/59 total). **Parallel stability gate: 8 total attempts across R1+R2 (success, FAILED-EBUSY, success, then 5/5 success post-fix) — 5 consecutive clean runs achieved, the R1 failure recorded not hidden.** Every post-fix run: 4/4 children exit `0`, collision check all true, exactly 1 generation per run, zero EBUSY, zero leftover Docker resources. Standalone profiles/failure-injections/`server:test:non-disposable` all rerun unchanged. New `--inject-failure=parent-generate` verified zero fan-out on generation failure. Acceptance-criteria items 17/18 corrected from "MET on 2 runs, later found unproven" to "MET, re-verified via 5 consecutive clean runs." No schema/migration/CI/production-access change. R-070 confirmed still `OPEN`. Faz durumu remains `IN_PROGRESS`; exit gate NOT satisfied; parent F1-003 not fully complete (P1/B1 merged; P2 `AGENT_COMPLETED`/PR #260 open; P3/P4 not started); G1/G2 remain `NOT_APPROVED`. Maximum status: `AGENT_COMPLETED` / `PR_OPENED_AWAITING_REVIEW`. Exact next task: F1-003-P3, blocked until PR #260 is externally reviewed and merged. |
| 2026-07-30 | F1-003-P3-R1 | Main Reconciliation, Backend Compatibility Fail-Safe, and Remote CI Evidence Closure (continued on existing open PR #268, no new PR, not merged, not rebased, not force-pushed): reconciled with `origin/main` (merge commit `7ab630c9a5a54acc7bb757b06c908b772c5f138d`, 0 conflicts, disjoint file set). **Root-caused a real GitHub Actions-only defect**: the original P3 run (`30536185063`) completed with overall conclusion `failure` — `non-disposable-backend-tests` never ran `npx prisma generate` before a from-scratch checkout, causing `SyntaxError: ... does not provide an export named 'PrismaClient'`; not reproduced locally since this task's own working directory had already generated the client via unrelated earlier commands. Fixed with an explicit generate step. **Closed the backend full-suite compatibility gap** the original P3 evidence had mis-estimated as "roughly 14" scripts — exact, machine-verified figure is **23** (all `postgresRequired:true`/`minioRequired:false`, full names in the evidence doc §17.2) — via a new additive `server:test:legacy-db-required` aggregate + new additive `postgres-compat` orchestrator profile reusing 100% of F1-003-P2's provisioning/guard/migration/cleanup/pinning code path; legacy `server:test` itself untouched; no second PostgreSQL implementation. **Fixed the artifact-generation defect** (combined stdout mislabeled as JSON) with a genuine `--summary-file` mechanism (`lib/summaryFile.ts`) plus a secret-pattern scan before upload — and found/fixed a second real defect during that same work (an unhandled write failure could have masked the real test exit code), before it reached CI. Local validation: `test:runtime:unit` 74/74 (was 68, +6 new); `server:test:non-disposable` 68/68; `postgres`/`storage` profiles regression-clean and unaffected by the new profile; `postgres-compat` 116/117 (the 1 failure proven a Windows-local-only CRLF checkout artifact via direct reproduction, not an application defect, expected to pass on the real Linux CI run); `verify-parallel` regression collision-free; `test:vitest` 35/35 (3 files, after the reconciliation merge added one); 7 frontend leaf scripts unchanged; `npm run lint` still confirmed non-functional. No schema/migration/application/production change. R-070 confirmed still `OPEN`. Faz durumu remains `IN_PROGRESS`; exit gate NOT satisfied; parent F1-003 not fully complete (P1/B1/P2 merged; P3/P3-R1 agent-completed/PR #268 open; P4 not started); G1/G2 remain `NOT_APPROVED`. Maximum status: `AGENT_COMPLETED` / `PR_OPENED_AWAITING_REVIEW`. Exact next task: F1-003-P4, blocked until PR #268 is externally reviewed and merged with real, completed GitHub Actions evidence on the post-remediation head SHA reviewed. |
| 2026-08-02 | F1-003-B2 | Retention Manual/Scheduled Run Mutual-Exclusion Race (blocking production-behavior defect discovered by F1-003-P3's Layer 5 compatibility CI on PR #268 — PR #268 left open, unmodified, blocked; fresh worktree/branch `fix/f1-003-b2-retention-run-lock-race`, baseline `origin/main` @ `70b1690c1a656c95cead7b42812cc9ae6447bfb7`): fixed a genuine mutual-exclusion race in `POST /privacy/data-retention/run` — the shared `data-retention-cleanup` JobLock primitive (`jobLock.ts`) was proved atomic/race-free via 130 direct concurrent-acquisition trials against real PostgreSQL (0 double-grants); the actual defect was that the route acquired the lock only after two other variable-latency DB round trips both concurrent racers perform independently, letting a fast racer's whole acquire→cleanup→release cycle finish before a simultaneous second racer even attempted the lock. **Fix:** `acquireJobLock`/`releaseJobLock` exported from `jobLock.ts` (unchanged internals, `withJobLock` itself untouched); the route now acquires the lock first, before either audit write, with both winner and loser still writing their own `started` row and the loser never invoking cleanup; a new lock-acquired-then-started-write-fails edge case releases the lock before its `500`. 5 new + 1 strengthened test (20→25 assertions in `retentionManualRunAudit.test.ts`), including a 10-round in-test race gate. Post-fix: 8 consecutive full-suite runs, 25/25 every time (80 race-gate rounds, 0 double-successes); `typecheck`/`test:data-retention`/`server:test:non-disposable`/root `test:runtime:postgres` all clean; zero residual Docker resources. No schema/migration change. Full evidence: [../evidence/F1-003-B2_RETENTION_RUN_MUTUAL_EXCLUSION_RACE.md](../evidence/F1-003-B2_RETENTION_RUN_MUTUAL_EXCLUSION_RACE.md). Faz durumu remains `IN_PROGRESS`; exit gate NOT satisfied; parent F1-003 not fully complete; R-070/R-046 `OPEN`, R-071 unchanged; G1/G2 remain `NOT_APPROVED`. Maximum status: `AGENT_COMPLETED` / `PR_OPENED_AWAITING_REVIEW`. Exact next task: after this bugfix PR is reviewed and merged, return to PR #268 for F1-003-P3-R2 main reconciliation and full-green CI closure. |
| 2026-08-02 | F1-003-B2-R1 | Admission-Point Lock Closure, Current-Main Reconciliation, Remote CI Verification, and Evidence Sanitization (continued on existing branch `fix/f1-003-b2-retention-run-lock-race` / PR #280, no new branch/PR, not merged): reconciled with `origin/main` (PR #276/#277 merged) — zero conflicts, merge commit `2fc2d9b1985a33125d14251bca6077c149f589c4`. Closed the residual admission-timing gap F1-003-B2's own evidence had flagged: moved `acquireJobLock` ahead of the `getPlatformSetting` policy read entirely (previously it ran before the lock), so admission is now the very first variable-latency DB operation a live attempt performs. Route extracted into an exported `createDataRetentionRunHandler` factory for deterministic dependency injection; new deterministic (barrier-based, non-timing-luck) concurrency test proves admission is fully resolved before either racer's policy lookup can complete. 4 new tests (25→29 assertions), 6 consecutive clean runs. Full regression suite clean; pushed to the existing PR — `gh pr checks 280` reported no checks (diagnosed: no CI workflow on `origin/main` covers this PR's changed paths; the layered CI system lives only on unmerged PR #268) — `REMOTE_CI_VERIFIED` explicitly not claimed. Evidence sanitized (one absolute-path occurrence found/corrected in task-owned files). No schema/migration change. Full evidence: [../evidence/F1-003-B2-R1_ADMISSION_LOCK_CLOSURE.md](../evidence/F1-003-B2-R1_ADMISSION_LOCK_CLOSURE.md). Faz durumu remains `IN_PROGRESS`; exit gate NOT satisfied; R-070/R-046 `OPEN`, R-071 unchanged; G1/G2 remain `NOT_APPROVED`. Maximum status: `AGENT_COMPLETED` / `PR_OPENED_AWAITING_REVIEW`. Exact next task: after PR #280 is reviewed and merged, return to PR #268 for F1-003-P3-R2 main reconciliation and full-green CI closure. |
